${frontmatter}
<!-- frontmatter keys (Phase 72-04): up, related, collections, created, tags,
     type, file_type, source_file, source_location, community, reasoning_relations -->
# ${label}

${wayfinder_callout}

${body}

${connections_callout}

${metadata_callout}
