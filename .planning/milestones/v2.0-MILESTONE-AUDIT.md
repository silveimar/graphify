---
milestone: v2.0
audited: 2026-05-12T07:25:00-06:00
status: passed
requirements_satisfied: 13/13
integration_checks_passed: 5/5
status_history:
  - 2026-05-12T07:25:00-06:00: tech_debt — VBUG-01/02 REQUIREMENTS.md checkboxes + traceability rows stale
  - 2026-05-12T07:30:00-06:00: passed — typos fixed inline before milestone close
---

# v2.0 "Graph Schema Deepening" — Milestone Audit

Audit gate for phases 71–75. Verifies shipped-code evidence for 13 requirements and 5 cross-phase integration claims. Phases 72–75 had `verifier: false`; this report is their sole verification gate. Phase 71 had its own `71-VERIFICATION.md` (4/4 PASS) — re-confirmed here against current code.

## Requirements Coverage

| REQ-ID | Phase | Verdict | Evidence |
|--------|-------|---------|----------|
| TEMP-01 | 71 | SATISFIED | `graphify/build.py:375-376` stamps `valid_from`/`valid_until` on every edge via `setdefault`; `graphify/validate.py:312-319` enforces in write-mode |
| TEMP-02 | 71 | SATISFIED | `build.py:377-385` calls `compute_decay_weight`; `temporal.py` + `temporal_config.yaml` ship per-relation decay; write-mode bound check at `validate.py:314-318` (must be float ∈ [0,1]) |
| TEMP-03 | 71 | SATISFIED | `temporal.py::stamp_supersessions` invoked from `build.py:31` import; 4-site supersession filter in analyze: `analyze.py:88-92` (god_nodes via edge_subgraph), `:273-274` (_cross_file), `:366-367` (_cross_community), `:441-442` (suggest_questions) |
| TEMP-04 | 71 | SATISFIED | `report.py:434-446` renders `## Temporal Health`; `wiki.py:128-149` renders `## Historical relations` only when non-empty |
| REAS-01 | 72 | SATISFIED | `validate.py:72-77` `REASONING_RELATIONS` frozenset; `validate.py:235-243` rejects edges with `file_type == "code"` on either endpoint |
| REAS-02 | 72 | SATISFIED (prompt content verified externally) | Prompts live in `graphify/skill.md` (6 references to supersedes/contradicts) and 9 platform variants — per project architecture ("skill orchestrates the library, library is standalone"). `graphify/prompts.py:10` bumps `PROMPT_VERSION = "1.14.0"` invalidating cache. Per-edge `confidence_score` validation for REASONING_RELATIONS at `validate.py:246-258` (INFERRED requires float ∈ [0,1]). **Note:** Skill-markdown prompt-text fidelity (ADR exemplars, JSON shape) not re-verified line-by-line in this audit; relies on Phase 72-02 drift-gate test (`tests/test_skill_prompt_drift.py`). |
| REAS-03 | 72 | SATISFIED | `analyze.py:730+` `contradictions_and_chains()` produces contradiction pairs + supersession chains with confidence gate (EXTRACTED OR score ≥ 0.5), cycle guard, frozenset dedup; `analyze.py:697-706` exempts reasoning-anchored nodes from `isolated` knowledge-gap classification via `_has_reasoning_edge` |
| REAS-04 | 72 | SATISFIED | `report.py:453-462` renders `## Contradictions and Supersession Chains`; `wiki.py:69-101` renders per-community `## Reasoning relations` block; `export.py:787-820` injects `reasoning_relations` frontmatter list into Obsidian notes (typed, distinguishable from structural relations) |
| DEDUP-01 | 73 | SATISFIED | `.planning/phases/73-dedup/73-SPIKE.md` reports aggregate 18.78% raw collision rate (residual 18.78%) across `claude-code-templates` (1,090 files, 21.75%) + `claude-cookbooks` (238 files, 0.94%); **Ship** recommendation; PDF corpus explicitly noted unmeasured with rationale (LLM extraction gated behind skill orchestration). Methodology recipe locked from CONTEXT D-02. |
| VBUG-01 | 74 | SATISFIED | `__main__.py:3529` (`update-vault`) and `:3684` (`vault-promote`) both declare `--vault required=False, default=None`; post-parse auto-adopt guards at `:3583` (and ~30 lines below for vault-promote per 74-02 comment). Fix applied uniformly to gated subcommands. |
| VBUG-02 | 74 | SATISFIED | `tests/test_vault_cwd_gate.py` exists with 2 parametrize decorators × `GATED_COMMANDS` list (14 sites, not 15 — Phase 74-02 SUMMARY documents that CONTEXT's "15th site at line 2947" is inside the elicit branch's existing gate call). Test asserts absence of argparse signature `the following arguments are required: --vault`. Debug session frontmatter at `.planning/debug/vault-cwd-gate-argparse-required.md` shows `status: resolved`, `resolved_in: phase-74-vbug`. |
| PKG-01 | 75-01 | SATISFIED | `pyproject.toml` line 2: `version = "2.0.0"`; `scripts/bump_version.py` flow followed per 75-01 SUMMARY |
| PKG-02 | 75-02 | SATISFIED (with CI deferral) | `server.json:5` version `2.0.0`; `server.json:25` `manifest_content_hash: "1bc8765726576fd0cb6c4e2e536e13396b19f0e6250396f6d7f76a000fa3c330"` matches REQUIREMENTS prefix `1bc87657…`. CI-as-contract pytest deferral acknowledged in REQUIREMENTS PKG-02 itself. |

**Score: 13/13 SATISFIED**

## Cross-Phase Integration

| # | Check | Verdict | Evidence |
|---|-------|---------|----------|
| 1 | **TEMP × REAS** — write-mode validator accepts edges carrying BOTH temporal cols AND reasoning relations | WIRED | `validate.py:312-319` write-mode temporal checks operate on the edge dict; `validate.py:235-258` reasoning-relation checks operate on the same edge dict in the same loop pass. No rule cross-rejects: a reasoning edge with `valid_from`/`decay_weight`/non-code endpoints/INFERRED+score passes both gates. |
| 2 | **TEMP × analyze (Phase 72 respects superseded edges)** | WIRED | `analyze.py:730+ contradictions_and_chains` reads edges via `G.edges(data=True)`. **Note:** unlike the 4 god-node/surprise/gap sites, `contradictions_and_chains` does NOT explicitly skip `valid_until is not None`. However, the confidence gate (EXTRACTED or score ≥ 0.5) and `stamp_supersessions` chain (which auto-stamps superseded outbound edges of B when `A supersedes B`) ensures historical chains *are* expected in this analysis section by design — REAS-03 deliverable is a "supersession chain" listing, so showing superseded relations is the contract, not a leak. Cross-phase intent preserved. |
| 3 | **Legacy graph.json backward compat** | WIRED | `validate.py:289-291` `validate_extraction_for_read` returns plain `validate_extraction` (no schema_version requirement, no temporal field requirement). REASONING_RELATIONS check at `:235-243` only triggers if the relation IS in the frozenset — legacy graphs without reasoning relations never hit the rule. Fixture `tests/fixtures/graph_legacy_v113.json` per 71-VERIFICATION exercises legacy path. |
| 4 | **Schema version stamp aligned with PyPI bump** | WIRED (with semantic gap) | `build.py:113` `SCHEMA_VERSION = "2.0"` matches `pyproject.toml version = "2.0.0"` major. Note: SCHEMA_VERSION is `"2.0"` (two parts) while package is `"2.0.0"` (three parts) — this is intentional per the v1.13/CCONF precedent (schema_version uses MAJOR.MINOR, package uses semver). |
| 5 | **Confidence pipeline (CCONF v1.13 → REAS-02)** | WIRED | Extraction prompt: `prompts.py:10` bumps `PROMPT_VERSION = "1.14.0"` invalidating cached extractions so the new reasoning prompt re-runs. Validator: `validate.py:246-258` requires `confidence_score ∈ [0,1]` for INFERRED reasoning edges (parallels the structural-edge rule at `:210-220`). Report: `report.py` Contradictions section renders confidence_score; `wiki.py:75-101` reasoning list includes `score_f`; Obsidian: `export.py:797-820` writes `reasoning_relations: [{type, target, confidence_score}]` frontmatter list. End-to-end flow confirmed. |

**Score: 5/5 WIRED**

## Known Deferred Items (tech debt — does not block milestone)

1. **CI matrix gate (PKG-02)** — Local pytest red signal verified as env-leak, deferred to a "test-triage" phase. Reliance on D3 CI-as-contract for Python 3.10/3.12 acceptance.
2. **Git tag for v2.0.0** — Not in audit scope; no `v2.0.0` tag verified in this run.
3. **PyPI publish** — Not in audit scope.
4. **CLAUDE.md doc fix** — Carryover doc-debt referenced in 75-pkg notes; not blocking.
5. **Test-env-leak triage** — Pre-existing, owned by separate phase.
6. **VBUG-01/02 traceability typo** — `.planning/REQUIREMENTS.md` lines 76–77 still show `VBUG-01 | Phase 74 | Pending` and `VBUG-02 | Phase 74 | Pending` despite shipping evidence (`__main__.py:3529, :3684`, `tests/test_vault_cwd_gate.py`, debug-session `status: resolved`). Must be flipped to `Complete (2026-05-12)`. Same defect: lines 33–34 use unchecked `- [ ]` boxes for VBUG-01/02 while every other shipped item uses `- [x]`.
7. **REAS-02 prompt content** — Not re-verified line-by-line in this audit. The 5 reasoning relations + 2 ADR exemplars + JSON shape live in `graphify/skill.md` and 9 platform variants. Drift-gate test `tests/test_skill_prompt_drift.py` is the contract; this audit did not re-execute it (no pytest run per audit constraints) or diff the skill markdown for the required ADR exemplars verbatim.
8. **REAS-03 superseded-edge inclusion in contradictions_and_chains** — Note 2 above flags that this analysis deliberately surfaces historical chains. If future intent is "current-only contradiction listing", a 5th-site `valid_until is None` filter would be needed. Currently consistent with REAS-03 contract.

## Audit Verdict

**`tech_debt`**

Rationale: All 13 requirements are SATISFIED by shipped code at the file:line level, and all 5 cross-phase integration checks are WIRED. The milestone is functionally complete and ready for release. Verdict is `tech_debt` rather than `passed` because of item 6 above: the REQUIREMENTS.md traceability table actively mis-states VBUG-01/02 as `Pending` while the code, tests, and debug-session resolution prove otherwise. This is a documentation drift, not a code gap — flip the rows and the milestone is `passed`.
