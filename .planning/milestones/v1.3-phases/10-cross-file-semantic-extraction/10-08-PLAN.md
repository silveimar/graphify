---
phase: 10-cross-file-semantic-extraction
plan: 08
type: execute
wave: 1
gap_closure: true
depends_on: []
files_modified:
  - graphify/analyze.py
  - graphify/report.py
  - tests/test_analyze.py
  - tests/test_dedup_pipeline.py
  - tests/fixtures/dedup_composed_extraction.json
autonomous: true
requirements:
  - GRAPH-02
  - GRAPH-03
gap_refs:
  - uat-test-6
must_haves:
  truths:
    - "After dedup folds multiple source_files into a list, `god_nodes(G)` returns cleanly — no TypeError from `Path(list)`"
    - "`surprising_connections(G)` handles list-valued source_file without raising (set construction, equality check, _file_category, _top_level_dir all tolerate list[str])"
    - "`_is_file_node`, `_is_concept_node`, `_file_category`, `_top_level_dir` accept `source_file: str | list[str]` and behave identically when the list has length 1 as when the value is a plain str"
    - "The full composed flow dedup.dedup() → build.build_from_json() → cluster.cluster() → analyze.god_nodes() + analyze.surprising_connections() runs end-to-end on a multi-source fixture"
    - "`report.generate()` renders surprising-connection rows for canonical nodes whose source_file is a list (files[0]/files[1] renders cleanly as a comma-joined path string, not a Python list repr like `['a.py', 'b.py']`)"
    - "Backward compatibility preserved: single-string source_file nodes behave identically to pre-Phase-10 behavior (byte-for-byte on fixtures that don't exercise dedup)"
  artifacts:
    - path: "graphify/analyze.py"
      provides: "source_file type-adapter helper _iter_sources() or equivalent; every call site reads through it"
      contains: "list"
    - path: "graphify/report.py"
      provides: "source_files list-safe rendering in surprising-connections section"
    - path: "tests/test_analyze.py"
      provides: "regression tests for list-valued source_file in _is_file_node, _is_concept_node, _file_category, _top_level_dir, surprising_connections"
    - path: "tests/test_dedup_pipeline.py"
      provides: "integration test exercising dedup → build → cluster → analyze end-to-end"
    - path: "tests/fixtures/dedup_composed_extraction.json"
      provides: "multi-source fixture whose dedup output produces a canonical node with source_file: list[str] of length ≥ 2"
  key_links:
    - from: "graphify/analyze.py:_is_file_node"
      to: "Path(source_file).name comparison"
      via: "iterate list[str] or normalize to str before Path()"
      pattern: "source_file.*list|isinstance.*list|_iter_sources"
    - from: "tests/test_dedup_pipeline.py"
      to: "god_nodes + surprising_connections on post-dedup graph"
      via: "dedup.dedup() → build.build_from_json() → analyze.*"
      pattern: "from graphify.dedup import dedup|build_from_json|god_nodes"
---

<objective>
Close the major-severity UAT gap: `analyze._is_file_node` and its sibling helpers assume `source_file` is always a `str`, but Phase 10 D-12 extended the schema so canonical nodes after dedup carry `source_file: list[str]`. The composed pipeline (`dedup → build → analyze`) currently crashes with `TypeError: expected str, bytes or os.PathLike object, not list` at `graphify/analyze.py:27`. This plan fixes every call site in `analyze.py` (and any downstream renderer) that reads `source_file`, adds regression coverage for list-valued input, and lands the first end-to-end integration test that proves the whole flow composes cleanly.

Purpose: The Phase 10 success criteria require `dedup → build → analyze` to work as a single pipeline. Currently it doesn't — unit tests for each module pass, but the composition is broken. This plan is gap closure for UAT test 6 (severity: major).

Output: list-tolerant `source_file` handling across analyze.py + report.py, one new integration test file (`test_dedup_pipeline.py`), one new fixture, and one new regression test class in `test_analyze.py`.

Scheduled as Wave 1, no dependencies — every plan touched after 10-07 is complete and the codebase is stable on main. This plan owns `graphify/analyze.py` exclusively for this gap-closure wave.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/10-cross-file-semantic-extraction/10-UAT.md
@.planning/phases/10-cross-file-semantic-extraction/10-03-SUMMARY.md
@graphify/analyze.py
@graphify/dedup.py
@graphify/build.py
@graphify/report.py
@graphify/validate.py
@tests/test_analyze.py
@tests/test_dedup.py
@tests/fixtures/multi_file_extraction.json

<interfaces>
<!-- Key types and contracts the executor needs. Extracted from codebase. -->

Schema extension locked in Phase 10 (see 10-03-SUMMARY and validate.py):
```python
# Node attribute "source_file" may now be:
#   - str           (pre-Phase-10 shape, still valid)
#   - list[str]     (canonical nodes after dedup, sorted, ≥1 entries)
# REQUIRED_NODE_FIELDS still includes "source_file" (validate.py unchanged)
```

From graphify/analyze.py (call sites that read source_file — all must be audited):
```python
# Line 24-27  _is_file_node   — Path(source_file).name  ← CRASH SITE
# Line 80-84  surprising_connections — set comprehension with source_file values
# Line 103-111 _is_concept_node — source.split("/")[-1]
# Line 115-128 _file_category / _top_level_dir — path.rsplit, path.split
# Line 152-159 _surprise_score — calls _file_category, _top_level_dir with u/v source
# Line 213-214 _cross_file_surprises — u_source / v_source fetch + equality check
# Line 230-232, 273-275, 309-311 surprise-dict "source_files" list — passes raw values through to report.generate
```

From graphify/report.py (consumer that renders analyze output):
```python
# Line 124  files = s.get("source_files", ["", ""])
# Line 135  f"  {files[0]} → {files[1]}"  ← renders Python list repr if files[0] is a list
```

From graphify/dedup.py line 446-455 (existing behavior — the source of list-valued source_file):
```python
# _merge_extraction folds source_file via a set:
#   sf_set.add(str) or sf_set.update(list)
#   canon["source_file"] = sorted(sf_set) if len(sf_set) > 1 else (next(iter(sf_set)) if sf_set else "")
# So a canonical node ends up with list[str] IFF ≥2 source_files contributed.
```

From tests/fixtures/multi_file_extraction.json: 7 nodes, 1 near-duplicate pair (AuthService×2). Existing fixture; dedup collapses it to 6 nodes, producing one canonical with source_file: list[str] of length 2.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: RED — write failing regression tests for list-valued source_file</name>
  <files>tests/test_analyze.py, tests/test_dedup_pipeline.py, tests/fixtures/dedup_composed_extraction.json</files>
  <behavior>
    New test functions in tests/test_analyze.py (append, do not modify existing tests):
    - test_is_file_node_list_source_file: build a graph with a node where source_file=["auth/service.py", "auth/impl.py"] and label="service.py"; assert _is_file_node(G, nid) is True (label matches basename of at least one source).
    - test_is_file_node_list_source_file_no_match: list source_file with non-matching label; assert False.
    - test_is_concept_node_list_source_file: node with source_file=["a.py", "b.py"]; assert False (has extension → real file).
    - test_is_concept_node_list_source_file_no_extension: node with source_file=["README", "LICENSE"]; assert True (no extension).
    - test_file_category_list_source_file: _file_category(["x.py", "y.md"]) returns a category derived from the first/any entry deterministically (pick first-by-sort to stay stable); assert it returns "code" for list starting with .py.
    - test_top_level_dir_list_source_file: _top_level_dir(["repo1/a.py", "repo2/b.py"]) returns the first sorted entry's top dir ("repo1"); exact contract documented in the helper.
    - test_surprising_connections_list_source_file_no_crash: graph with two nodes whose source_file is list[str], one cross-file edge between them; assert surprising_connections() returns a list and does not raise; assert source_files in the output dict are stringifiable (no TypeErrors on the downstream report path).

    New integration test file tests/test_dedup_pipeline.py:
    - test_dedup_then_god_nodes_composes: load tests/fixtures/dedup_composed_extraction.json → dedup.dedup() → build.build_from_json() → cluster.cluster() → analyze.god_nodes(G, top_n=5); assert result is a list, no exception.
    - test_dedup_then_surprising_connections_composes: same setup; assert analyze.surprising_connections(G, communities, top_n=3) returns a list, no exception.
    - test_dedup_then_report_generate_composes: same setup; assert report.generate(G, communities, {}, god_nodes_out, surprises_out) produces a non-empty string and does not contain any Python list repr artifact (regex: `\['.*\.py'.*\]` must NOT appear in the surprising-connections section).

    New fixture tests/fixtures/dedup_composed_extraction.json: 8 nodes across 3 source files with at least one near-duplicate pair that dedup will collapse (fuzzy ≥ 0.90, cosine ≥ 0.85). Structure: two `auth_service` variants in different files (`src/auth.py`, `lib/auth_impl.py`), one bridging concept node with empty source_file, and cross-file edges wired so surprising_connections has material to work with. Keep fixture minimal but realistic (model after multi_file_extraction.json).
  </behavior>
  <action>
    1. Create tests/fixtures/dedup_composed_extraction.json with the structure above. Reuse the shape from tests/fixtures/multi_file_extraction.json and add one more near-dup pair across different source_files so dedup produces at least one list-valued canonical.
    2. Append the 7 unit tests to tests/test_analyze.py. Import `_is_file_node` and `_top_level_dir` from `graphify.analyze` (they're private but already imported in existing tests).
    3. Create tests/test_dedup_pipeline.py with the 3 integration tests. Follow the style of tests/test_analyze.py (pure unit, no network, stdlib + pytest only).
    4. Run `pytest tests/test_analyze.py tests/test_dedup_pipeline.py -x 2>&1 | tail -30` — EVERY new test MUST fail (some with TypeError from Path(list), some with AttributeError from list.split, some because the helpers don't accept list yet). This is the RED state; capture the failure summary in the commit message.
    5. Commit with message: `test(10-08): add RED regression tests for source_file: list[str] composed pipeline`. Use HEREDOC for commit body including test names and expected failure modes.

    CRITICAL: do NOT touch any graphify/ source files in this task. RED means the production code is still broken on purpose.
  </action>
  <verify>
    <automated>pytest tests/test_analyze.py tests/test_dedup_pipeline.py -x 2>&1 | grep -E "FAILED|ERROR" | head -15</automated>
  </verify>
  <done>
    New fixture file exists at tests/fixtures/dedup_composed_extraction.json.
    7 new tests appended to tests/test_analyze.py (all FAIL).
    3 new tests in tests/test_dedup_pipeline.py (all FAIL).
    Pre-existing 1161 passing tests remain green (no regressions from fixture/test-file additions).
    Git commit exists with subject `test(10-08): add RED regression tests for source_file: list[str] composed pipeline`.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: GREEN — make source_file list-tolerant across analyze.py + report.py</name>
  <files>graphify/analyze.py, graphify/report.py</files>
  <behavior>
    All 10 RED tests from Task 1 pass.
    Pre-existing 1161-test suite still passes.
    No new dependencies added.
    No change to the shape of dicts returned by god_nodes/surprising_connections (callers downstream unchanged).
  </behavior>
  <action>
    Audit + fix every source_file read site in graphify/analyze.py. Use a single shared helper:

    ```python
    def _iter_sources(source_file: str | list[str] | None) -> list[str]:
        """Normalize source_file to a list of strings. str -> [str], list -> list, None/empty -> []."""
        if not source_file:
            return []
        if isinstance(source_file, str):
            return [source_file]
        if isinstance(source_file, list):
            return [s for s in source_file if isinstance(s, str) and s]
        return []
    ```

    Place it near the top of analyze.py (after _node_community_map), exported via `from graphify.analyze import _iter_sources` for testability. Then fix each call site:

    1. `_is_file_node` (line 24-27): replace `if label == _Path(source_file).name` with `any(label == _Path(s).name for s in _iter_sources(source_file))`.
    2. `surprising_connections` (line 80-84): flatten list-valued source_file into the set. Use `{s for _, data in G.nodes(data=True) for s in _iter_sources(data.get("source_file"))}`.
    3. `_is_concept_node` (line 103-111): if `_iter_sources(source)` is empty → True (concept). If any source in the list lacks an extension → still treat the node as "concept" (unchanged semantics). Implementation: `sources = _iter_sources(source); if not sources: return True; return not any("." in s.split("/")[-1] for s in sources)` — a node is concrete if ANY source looks like a real file.
    4. `_file_category` / `_top_level_dir` (line 115-128): keep their str signature, but document at the top that callers must pass a single source. For list inputs, call them per-element and aggregate. In `_surprise_score`, if u_source is a list, iterate and collect categories; use the first-sorted category for the comparison bonus (`cat_u = _file_category(sorted(_iter_sources(u_source))[0])` — deterministic). Do NOT change _file_category's signature to accept list; keep the type-adapter at the caller for readability.
    5. `_cross_file_surprises` (line 213-216): normalize u_source/v_source to tuples: `u_sources = tuple(sorted(_iter_sources(...)))`. Equality check becomes `u_sources == v_sources` — tuple comparison works for both shapes. For the emitted surprise dict (line 230-232), preserve the raw value (str stays str, list stays list) so callers that know about the list can use it, BUT flatten to a comma-joined string for pretty rendering paths in report.py.
    6. `_cross_community_surprises` (line 273-275, 309-311): same pattern — raw value preserved in dict.

    In graphify/report.py line 122-135: render list source_files cleanly. Change `files = s.get("source_files", ["", ""])` followed by `files[0] → files[1]` to: if files[0] is list, join with `, ` before rendering. Explicit helper:
    ```python
    def _fmt_source(value) -> str:
        if isinstance(value, list):
            return ", ".join(value) if value else ""
        return str(value or "")
    ```
    Place it near the top of report.py as module-level helper. Use in the surprising-connections renderer: `f"  {_fmt_source(files[0])} → {_fmt_source(files[1])}"`.

    Run the new RED tests: `pytest tests/test_analyze.py tests/test_dedup_pipeline.py -x` — all 10 must now pass.
    Then run the full suite: `pytest tests/ -q 2>&1 | tail -5` — must still show ≥1161 passing (the 3 pre-existing test_delta.py failures are out of scope).

    Commit with message: `fix(10-08): handle source_file: list[str] in analyze.py + report.py (closes UAT gap, test 6)`. Reference the UAT gap in the body. Do NOT refactor anything beyond the source_file paths.
  </action>
  <verify>
    <automated>pytest tests/test_analyze.py tests/test_dedup_pipeline.py tests/test_report.py -q 2>&1 | tail -10</automated>
  </verify>
  <done>
    All 10 new tests from Task 1 pass.
    Full suite: ≥1161 passing (same as pre-plan baseline; 3 pre-existing test_delta.py failures unchanged).
    `_iter_sources` helper exists in graphify/analyze.py and is used at all source_file call sites.
    `_fmt_source` helper exists in graphify/report.py and is used in the surprising-connections renderer.
    Manual spot-check: running the UAT reproduction (dedup.dedup() → build.build_from_json() → analyze.god_nodes()) on tests/fixtures/multi_file_extraction.json no longer raises TypeError.
    Git commit exists with subject starting `fix(10-08):`.
  </done>
</task>

<task type="auto">
  <name>Task 3: REFACTOR + doc — consolidate helper, annotate schema invariants</name>
  <files>graphify/analyze.py, graphify/dedup.py</files>
  <action>
    Only runs if Task 2 left cleanup debt. Audit:
    1. Is `_iter_sources` referenced from `graphify/dedup.py`, `graphify/report.py`, or `graphify/export.py`? If any of those files also read source_file directly without list-safety, consolidate via `from graphify.analyze import _iter_sources` (or lift the helper to a shared location if cycle risk — e.g. `graphify/_schema.py`).
    2. In graphify/dedup.py line 446-455 (the write site that produces list[str]), add an inline docstring pointing readers at _iter_sources as the canonical read adapter. This is documentation only — no behavior change.
    3. If any export.py or report.py paths still pass `source_file` to `Path()` or `sanitize_label()` without going through the helper, fix them. Spot-check the lines identified in the interfaces block (export.py:372, export.py:708).

    If Task 2 is already clean, this task is a no-op — commit an empty note: `chore(10-08): confirmed no residual str-only source_file reads outside analyze.py + report.py` with --allow-empty is NOT permitted; instead, skip the commit entirely and record the audit result in the Task 3 SUMMARY entry.

    DO NOT introduce speculative refactors. Scope is bounded to list-safety for source_file reads. Every change must trace back to a specific Path()/split()/set() call site on source_file.
  </action>
  <verify>
    <automated>pytest tests/ -q 2>&1 | tail -5</automated>
  </verify>
  <done>
    Audit documented: either (a) additional call sites fixed with a follow-up commit `refactor(10-08): ...`, or (b) SUMMARY notes no further sites found.
    Full suite still passes at ≥1161 tests.
    No new dependencies, no API surface changes.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| dedup output → analyze input | list[str] source_file values cross from the merge layer to the analysis layer; malformed (non-str list element, nested list) could cause helper misbehavior |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-10-08-01 | Tampering | _iter_sources helper | mitigate | Filter list elements via `isinstance(s, str) and s` — defends against nested lists, None entries, or non-string types slipping through dedup edge cases |
| T-10-08-02 | Information Disclosure | report.py _fmt_source renderer | accept | Source paths are already graph-internal (validated by security.py at extraction time); joining a list for display does not expose new data |
</threat_model>

<verification>
All three tasks complete → the dedup → build → cluster → analyze → report pipeline runs end-to-end on `tests/fixtures/dedup_composed_extraction.json` without raising. The 1161-test baseline still passes. The 10 new regression tests cover list-valued source_file at every analyze.py call site identified in UAT test 6's `missing` list.
</verification>

<success_criteria>
1. `pytest tests/test_analyze.py tests/test_dedup_pipeline.py -q` shows all new tests passing (10 new tests minimum).
2. `pytest tests/ -q 2>&1 | tail -3` shows ≥1161 passing (matching pre-plan baseline; 3 pre-existing test_delta.py failures unchanged).
3. Manual reproduction of UAT test 6 (`python -c "from graphify.dedup import dedup; ... ; god_nodes(G)"` on multi_file_extraction.json) no longer raises TypeError.
4. 2-3 atomic commits (RED → GREEN → optional REFACTOR) on the working branch.
5. No new runtime dependencies. No API-breaking changes to any public analyze.py / report.py symbol.
</success_criteria>

<output>
After completion, create `.planning/phases/10-cross-file-semantic-extraction/10-08-SUMMARY.md` following the standard template. Include:
- the three commit SHAs
- list of analyze.py call sites fixed (with line-number before/after)
- confirmation that UAT test 6 is closed
- test count delta (before / after)
</output>
