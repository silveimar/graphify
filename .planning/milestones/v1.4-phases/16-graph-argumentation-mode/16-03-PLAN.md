---
phase: 16
plan: 03
type: execute
wave: 2
depends_on: ["16-01"]
files_modified:
  - graphify/commands/argue.md
  - graphify/skill.md
  - tests/test_commands.py
autonomous: true
requirements:
  - ARGUE-06
  - ARGUE-09
  - ARGUE-10
  - ARGUE-11
  - ARGUE-12
  - ARGUE-13
tags: [argue, skill, slash-command, spar-kit, blind-label, advisory-only, deferred-p2]

must_haves:
  truths:
    - "graphify/commands/argue.md exists with name=graphify-argue, disable-model-invocation: true, no target: field, and body references argue_topic MCP tool"
    - "skill.md has a new /graphify-argue SPAR-Kit orchestration section implementing rounds loop, per-round blind-label shuffle, {claim, cites} validation, Jaccard convergence detection, and advisory-only output to graphify-out/GRAPH_ARGUMENT.md"
    - "Phase 9 blind-label harness at skill.md lines ~1388-1550 is preserved unmodified (Judge 1/2/3 shuffle rotation anchor intact)"
    - "SPAR-Kit orchestration enforces round cap=6, temperature ≤ 0.4, and accepts dissent/inconclusive/consensus as valid terminal verdicts (no consensus-forcing synthesizer persona)"
    - "Deferred P2 requirements (ARGUE-11 INTERROGATE, ARGUE-12 persona memory, ARGUE-13 clash scoring) are explicitly documented as v1.4.x backlog in skill.md — not implemented, not forgotten"
    - "tests/test_commands.py::test_argue_md_frontmatter passes"
    - "Full suite green (Plan 01 and Plan 02 tests still pass; blind-harness regression test from Plan 01 still green)"
  artifacts:
    - path: "graphify/commands/argue.md"
      provides: "/graphify-argue slash command"
      min_lines: 25
      contains: "argue_topic"
    - path: "graphify/skill.md"
      provides: "SPAR-Kit debate orchestration section for /graphify-argue"
      contains: "/graphify-argue"
    - path: "tests/test_commands.py"
      provides: "test_argue_md_frontmatter"
  key_links:
    - from: "graphify/commands/argue.md"
      to: "argue_topic MCP tool (Plan 02 output)"
      via: "body text invokes MCP tool argue_topic with topic=$ARGUMENTS"
      pattern: "argue_topic"
    - from: "graphify/skill.md /graphify-argue section"
      to: "graphify/argue.py ROUND_CAP, MAX_TEMPERATURE, validate_turn, compute_overlap"
      via: "documented usage in orchestration loop"
      pattern: "validate_turn|compute_overlap|ROUND_CAP|temperature"
    - from: "graphify/skill.md /graphify-argue section"
      to: "graphify/skill.md Phase 9 blind-label harness"
      via: "explicit cross-reference to reuse (not duplication)"
      pattern: "blind-label|Judge 1: Analysis-1=A|Phase 9"
---

<objective>
Ship the user-facing side of Phase 16: `/graphify-argue <question>` slash command + the SPAR-Kit debate orchestration block in `graphify/skill.md`. The orchestration calls `argue_topic` via MCP to get the evidence subgraph + `ArgumentPackage`, then runs up to 6 rounds of blind-labeled 4-persona debate (reusing the Phase 9 harness verbatim, per-round shuffle per D-05), validates every persona turn via `argue.validate_turn`, applies Jaccard-based early-stop (D-06 thresholds: ≥0.7×2 for consensus, <0.2×3 for dissent, cap=6 for inconclusive), and writes the advisory-only transcript to `graphify-out/GRAPH_ARGUMENT.md`. P2 requirements (ARGUE-11/12/13) are documented as deferred — explicitly called out in skill.md and roadmap to prevent silent scope creep.

Purpose: This is the only user-visible entry point for Phase 16. Without the command file + skill block, the substrate and MCP tool from Plans 01/02 are unreachable.

Output: `/graphify-argue <question>` is a usable slash command; the skill.md SPAR-Kit block is reviewable prompt-for-prompt; Phase 9 harness is demonstrably preserved; advisory-only invariant (ARGUE-09) is enforced in the orchestration text.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/16-graph-argumentation-mode/16-CONTEXT.md
@.planning/phases/16-graph-argumentation-mode/16-RESEARCH.md
@.planning/phases/16-graph-argumentation-mode/16-PATTERNS.md
@.planning/phases/16-graph-argumentation-mode/16-01-PLAN.md
@.planning/phases/16-graph-argumentation-mode/16-02-PLAN.md
@.planning/REQUIREMENTS.md
@graphify/commands/ask.md
@graphify/skill.md
@tests/test_commands.py

<interfaces>
<!-- Key contracts this plan MUST honor. Extracted from Plan 01/02 outputs + existing skill.md. -->

From graphify/argue.py (Plan 01):
```python
ROUND_CAP: int = 6
MAX_TEMPERATURE: float = 0.4
_FIXED_LENSES = ["security", "architecture", "complexity", "onboarding"]

def validate_turn(turn: dict, G: nx.Graph) -> list[str]:
    """Return list of node_ids in turn['cites'] not in G.nodes. Empty = valid."""

def compute_overlap(cite_sets: list[set[str]]) -> float:
    """Jaccard over non-empty cite sets; abstentions dropped."""
```

From Plan 02 (`argue_topic` MCP tool envelope):
```
text_body + "\n---GRAPHIFY-META---\n" + json({
  status: "ok" | "no_graph" | "no_results",
  verdict: null,                                   # Plan 03 skill sets this post-debate
  rounds_run: 0,                                   # Plan 03 skill increments as it runs
  argument_package: {nodes, edge_count, perspectives, evidence_count},
  citations: [{node_id, label, source_file}, ...],
  resolved_from_alias: {canonical: [aliases], ...},
  output_path: "graphify-out/GRAPH_ARGUMENT.md"    # hardcoded
})
```

From graphify/commands/ask.md (ENTIRE file — template for argue.md):
```markdown
---
name: graphify-ask
description: Ask a natural-language question about the codebase and receive a graph-grounded narrative answer with citations.
argument-hint: <question>
disable-model-invocation: true
---

Arguments: $ARGUMENTS

Call the graphify MCP tool `chat` with:
- `query`: "$ARGUMENTS"
...
```

From tests/test_commands.py::test_ask_md_frontmatter (lines 195-209) — template to copy:
```python
def test_ask_md_frontmatter():
    path = _commands_dir() / "ask.md"
    assert path.exists(), "graphify/commands/ask.md missing"
    fm = _parse_frontmatter(path)
    assert fm.get("name") == "graphify-ask"
    assert fm.get("description"), "description field required"
    assert fm.get("argument-hint"), "argument-hint field required"
    assert fm.get("disable-model-invocation") == "true"
    assert "target" not in fm, "ask.md must NOT have a target: field"
    body = path.read_text()
    assert "chat" in body, "ask.md body must invoke the chat MCP tool"
    assert "$ARGUMENTS" in body, "ask.md must pass $ARGUMENTS to query"
```

From graphify/skill.md (existing anchor that MUST NOT be modified):
- Lines 1388-1550: Phase 9 blind-label harness (anti-pattern note, lens focus bullets, Judge rotation)
- Line 1433: Lens focus bullets (security/architecture/complexity/onboarding) — referenced by pointer, not duplicated
- Line 1511: `Judge 1: Analysis-1=A, Analysis-2=B, Analysis-3=AB` rotation — regression anchor from Plan 01 test_blind_label_harness_intact
- Line ~1607: GRAPH_ANALYSIS.md write pattern (Phase 9) — shape to mirror for GRAPH_ARGUMENT.md write
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Create /graphify-argue command file + test_argue_md_frontmatter (RED → GREEN)</name>
  <files>graphify/commands/argue.md, tests/test_commands.py</files>
  <read_first>
    - graphify/commands/ask.md (ENTIRE file — exact frontmatter template; body style reference)
    - tests/test_commands.py (lines 1-50 imports and _commands_dir / _parse_frontmatter helpers; lines 195-209 test_ask_md_frontmatter — copy as template)
    - .planning/phases/16-graph-argumentation-mode/16-CONTEXT.md (ARGUE-10 requirement; canonical_refs section on ask.md precedent)
    - .planning/phases/16-graph-argumentation-mode/16-PATTERNS.md (argue.md command file pattern block — full file content)
    - .planning/phases/16-graph-argumentation-mode/16-RESEARCH.md (Pattern 5 argue.md command file example)
  </read_first>
  <behavior>
    - `graphify/commands/argue.md` exists with frontmatter: `name: graphify-argue`, `description: ...`, `argument-hint: <decision question>`, `disable-model-invocation: true`. NO `target:` field.
    - Body passes `$ARGUMENTS` as `topic` to the `argue_topic` MCP tool; includes D-02 envelope parsing instructions for `no_graph` / `no_results` / `ok` statuses; renders `meta.resolved_from_alias` as a small note when non-empty (NOT `alias_redirects`).
    - Body contains advisory-only disclaimer language (ARGUE-09).
    - `test_argue_md_frontmatter` asserts all frontmatter invariants + body references `argue_topic` + body passes `$ARGUMENTS`.
    - `CORE_COMMANDS` / any registry in test_commands.py is untouched unless ask.md already required it (recommendation: add a dedicated test function per 16-RESEARCH.md Open Question 3).
  </behavior>
  <action>
    STEP A — Create `graphify/commands/argue.md` with this EXACT content:

    ```markdown
    ---
    name: graphify-argue
    description: Run a structurally-enforced multi-perspective graph debate on a decision question, grounded in the knowledge graph.
    argument-hint: <decision question>
    disable-model-invocation: true
    ---

    Arguments: $ARGUMENTS

    Call the graphify MCP tool `argue_topic` with:
    - `topic`: "$ARGUMENTS"
    - `scope`: "topic"

    The response is a D-02 envelope: a short text body, then `---GRAPHIFY-META---`, then a JSON object.

    Parse `meta.status`.

    **If `status` is `no_graph`:** render verbatim — "No graph loaded. Run `/graphify` first to build a knowledge graph, then retry `/graphify-argue`."

    **If `status` is `no_results`:** render — "The question didn't match any nodes in the graph. Try rephrasing with terms that appear in the codebase, or widen scope with the `argue_topic` tool."

    **If `status` is `ok`:**
    1. Read `meta.argument_package` — it contains the evidence subgraph summary (nodes, edge_count, perspectives) that the debate will be grounded in.
    2. Follow the SPAR-Kit debate orchestration in skill.md `/graphify-argue` section (see §Phase 16 Graph Argumentation Mode). The skill loop runs up to 6 rounds of 4-persona debate with per-round blind labels (reusing the Phase 9 blind-label harness), validates every persona turn via `graphify.argue.validate_turn`, and writes the final advisory-only transcript to `graphify-out/GRAPH_ARGUMENT.md`.
    3. After the debate completes, summarize the verdict (consensus / dissent / inconclusive), the per-round cite-overlap Jaccard trajectory, and the path to `GRAPH_ARGUMENT.md` for the user.

    If `meta.resolved_from_alias` is non-empty, append a small note: "Some node references were redirected to canonical IDs: {list}."

    **Advisory-only (ARGUE-09):** the transcript is strictly advisory — graphify does not and will not mutate code, the knowledge graph, or any project file as a result of `/graphify-argue`. The debate is evidence for a human decision, never an action.

    Keep the user-facing summary under 400 tokens; the full transcript lives in `graphify-out/GRAPH_ARGUMENT.md`.
    ```

    STEP B — Edit `tests/test_commands.py`. Add AFTER `test_ask_md_frontmatter`:

    ```python
    def test_argue_md_frontmatter():
        """ARGUE-10: /graphify-argue command file exists with ask.md-style frontmatter."""
        path = _commands_dir() / "argue.md"
        assert path.exists(), "graphify/commands/argue.md missing"
        fm = _parse_frontmatter(path)
        assert fm.get("name") == "graphify-argue"
        assert fm.get("description"), "description field required"
        assert fm.get("argument-hint"), "argument-hint field required"
        assert fm.get("disable-model-invocation") == "true"
        assert "target" not in fm, "argue.md must NOT have a target: field"
        body = path.read_text()
        assert "argue_topic" in body, "argue.md body must invoke the argue_topic MCP tool"
        assert "$ARGUMENTS" in body, "argue.md must pass $ARGUMENTS to topic"
        # Pitfall 4 guard — the canonical meta key is resolved_from_alias, NOT alias_redirects.
        assert "alias_redirects" not in body, "argue.md must use resolved_from_alias, never alias_redirects"
        # ARGUE-09 advisory-only enforcement — body must state non-mutation invariant.
        assert "advisory" in body.lower(), "argue.md must document advisory-only invariant (ARGUE-09)"
    ```

    STEP C — Run `pytest tests/test_commands.py::test_argue_md_frontmatter -q`. Must PASS.

    STEP D — Run full suite `pytest tests/ -q`. Zero regressions.

    Commit: `feat(16-03): add /graphify-argue command file + frontmatter test (ARGUE-10)`.
  </action>
  <verify>
    <automated>pytest tests/test_commands.py::test_argue_md_frontmatter -q &amp;&amp; pytest tests/ -q</automated>
  </verify>
  <acceptance_criteria>
    - `test -f graphify/commands/argue.md` exits 0.
    - `grep -E "^name: graphify-argue$" graphify/commands/argue.md` matches.
    - `grep -E "^disable-model-invocation: true$" graphify/commands/argue.md` matches.
    - `grep -c "^target:" graphify/commands/argue.md` returns 0.
    - `grep "argue_topic" graphify/commands/argue.md` matches at least once.
    - `grep "\\$ARGUMENTS" graphify/commands/argue.md` matches.
    - `grep -i "advisory" graphify/commands/argue.md` matches (ARGUE-09 invariant present).
    - `grep "alias_redirects" graphify/commands/argue.md` returns 0 (Pitfall 4 guard).
    - `grep "resolved_from_alias" graphify/commands/argue.md` matches.
    - `grep -c "def test_argue_md_frontmatter" tests/test_commands.py` returns 1.
    - `pytest tests/test_commands.py::test_argue_md_frontmatter -q` passes.
    - `pytest tests/ -q` — full suite green.
  </acceptance_criteria>
  <done>`graphify/commands/argue.md` created with exact frontmatter (mirroring ask.md); `test_argue_md_frontmatter` passes; advisory-only invariant documented in body; commit `feat(16-03): add /graphify-argue command file + frontmatter test (ARGUE-10)` created.</done>
</task>

<task type="auto">
  <name>Task 2: Add SPAR-Kit /graphify-argue orchestration section to skill.md (preserve Phase 9 harness; document P2 deferrals)</name>
  <files>graphify/skill.md</files>
  <read_first>
    - graphify/skill.md (lines 1388-1550 Phase 9 blind-label harness — DO NOT MODIFY; line 1433 lens focus bullets; line 1511 Judge rotation anchor; line ~1607 GRAPH_ANALYSIS.md write pattern — mirror for GRAPH_ARGUMENT.md)
    - graphify/commands/argue.md (Task 1 output — body points to this section)
    - graphify/argue.py (Plan 01 — public API: ROUND_CAP, MAX_TEMPERATURE, validate_turn, compute_overlap, _FIXED_LENSES)
    - .planning/phases/16-graph-argumentation-mode/16-CONTEXT.md (D-04/D-05/D-06/D-09/D-11/D-12/D-13 — debate mechanics; Deferred Ideas section for ARGUE-11/12/13 disposition)
    - .planning/phases/16-graph-argumentation-mode/16-RESEARCH.md (SPAR-Kit skill.md block structure — Step B1-B5; Anti-Patterns to Avoid section; Threat Model)
    - .planning/phases/16-graph-argumentation-mode/16-PATTERNS.md (no skill.md pattern block because it's prose, not code; cross-reference Phase 9 analogs only)
    - .planning/REQUIREMENTS.md (ARGUE-06, ARGUE-09, ARGUE-11/12/13 — P2 deferral disposition)
    - tests/test_argue.py (Plan 01 — test_blind_label_harness_intact anchors on `"Judge 1: Analysis-1=A"` literal string which MUST remain in skill.md)
  </read_first>
  <action>
    STEP A — Open `graphify/skill.md`. Locate a safe insertion point: AFTER the `/graphify analyze` section (which ends around line 1607 where GRAPH_ANALYSIS.md is written) and BEFORE any Phase 17 `/graphify-ask` section. DO NOT touch lines 1388-1550 — Plan 01 test_blind_label_harness_intact asserts the literal string `"Judge 1: Analysis-1=A"` at line 1511 remains unmodified.

    STEP B — Insert a new section with this structure (adapt to skill.md's markdown heading conventions — match the heading level of adjacent command sections):

    ```markdown
    ## /graphify-argue <question> — SPAR-Kit Graph Argumentation Mode (Phase 16)

    **Purpose:** Run a structurally-enforced multi-perspective debate about a decision question, grounded in the knowledge graph. Every persona claim cites a real node_id. Produces `graphify-out/GRAPH_ARGUMENT.md` — advisory-only (ARGUE-09).

    **Cross-phase rule:** This orchestration MUST NOT invoke the Phase 17 `chat` MCP tool (Pitfall 18 recursion guard). The manifest declares `argue_topic.composable_from: []`. Use `argue_topic` and the deterministic graph primitives only.

    ### Step B1 — Evidence subgraph
    Call the MCP tool `argue_topic` with `{topic: "<question>", scope: "topic"}`. Parse the D-02 envelope. If `meta.status != "ok"`, render the fallback message per `graphify/commands/argue.md` and STOP.

    From `meta.argument_package`, extract:
    - `nodes: [{id, label, source_file}, ...]` — the evidence subgraph
    - `perspectives: [{lens}, ...]` — the 4 fixed lenses from `graphify/argue.py::_FIXED_LENSES` (`security`, `architecture`, `complexity`, `onboarding`)
    - `citations: [...]` — flat list to render into the final transcript

    ### Step B2 — Evidence context block
    Build an `{EVIDENCE_SUBGRAPH}` text block from `meta.argument_package.nodes` + `edge_count`. For structured rendering, call `graphify.analyze.render_analysis_context(subG, ...)` if a full subgraph object is available; otherwise format a concise markdown list of `- [node_id:label] (source_file)` lines. This block is fed to every persona prompt unchanged across rounds.

    ### Step B3 — Debate rounds (up to ROUND_CAP = 6)

    Import from `graphify.argue`: `ROUND_CAP` (= 6), `MAX_TEMPERATURE` (= 0.4), `validate_turn`, `compute_overlap`.

    For `round in 1..ROUND_CAP`:

    1. **Per-round blind-label shuffle (D-05, ARGUE-06).** Shuffle A/B/C/D persona labels at round start — reuse the Phase 9 blind-label harness shuffle pattern documented above (see §Phase 9 tournament harness — "Judge 1: Analysis-1=A, Analysis-2=B, Analysis-3=AB"). The Jaccard detector never sees a stable persona→label mapping across rounds. DO NOT identify personas by their role name in any overlap computation or in any output surface that feeds back into a later round.

    2. **Four parallel persona LLM calls (D-04).** For each persona in `[security, architecture, complexity, onboarding]`:
       - Use the lens focus bullets from §Phase 9 harness (line ~1433 in this file — point to them, do NOT duplicate).
       - Prompt framing: "Argue a position on the user's question `<question>`, citing only `node_id`s present in the provided `{EVIDENCE_SUBGRAPH}`. Output JSON only: `{claim: string, cites: [node_id]}`. Do not identify yourself as a specific persona role in the output."
       - Include the `{EVIDENCE_SUBGRAPH}` block + all previous rounds' validated claims (labeled by their round-N shuffle label, not by persona role).
       - **Temperature ≤ MAX_TEMPERATURE (0.4) — HARD CONSTRAINT.** If the LLM client API requires an explicit temperature argument, set it to exactly `0.4`; never higher.

    3. **Validate each turn via `argue.validate_turn(turn, G)` (ARGUE-05).**
       - If the returned list is non-empty (unknown cites fabricated): hard-reject the turn, re-prompt THE SAME persona ONCE with the message "The following cited node_ids do not exist in the provided subgraph: {list}. Please revise. Cite only `node_id` values that appear in `{EVIDENCE_SUBGRAPH}`."
       - If the retry is STILL invalid: record the turn as `{claim: "[NO VALID CLAIM]", cites: []}` — this is an abstention (D-09). Do NOT accept a third attempt.

    4. **Compute cite-overlap (D-06).** Build `cite_sets = [set(turn["cites"]) for turn in this_round_turns]`. Call `argue.compute_overlap(cite_sets)` — abstentions (empty sets) are dropped by the function before computing Jaccard. Record the round's Jaccard in the trajectory array.

    5. **Check early-stop conditions (D-06):**
       - If `overlap >= 0.7` for TWO consecutive rounds → `verdict = "consensus"` and break.
       - If `overlap < 0.2` for THREE consecutive rounds → `verdict = "dissent"` and break (valid terminal outcome — no consensus-forcing, D-07).
       - If `round == ROUND_CAP` without either condition firing → `verdict = "inconclusive"` and break (valid terminal outcome).

    ### Step B4 — Write advisory-only transcript (ARGUE-09, D-11, D-12, D-13)

    Write `graphify-out/GRAPH_ARGUMENT.md` — mirror the write pattern used for `GRAPH_ANALYSIS.md` elsewhere in this file. Contents (per D-11/D-12):

    ```
    # Graph Argument — <topic>

    > **Advisory only.** This transcript is the record of a structurally-enforced graph-grounded debate. No code, no graph data, and no project file has been modified as a result of this run.

    ## Round 1
    ### Security
    - <claim> [node_id:label] [node_id:label]
    ### Architecture
    - ...
    ### Complexity
    - ...
    ### Onboarding
    - ...

    ## Round 2
    ...

    ## Verdict
    - verdict: <consensus | dissent | inconclusive>
    - rounds_run: <N>
    - cite-overlap trajectory (Jaccard): 0.15 → 0.28 → 0.55 → 0.78 → 0.82
    - cited nodes:
      - [node_id:label]
      - ...

    > This transcript is advisory only — no code or graph mutations result from this run.
    ```

    - **Inline cite format is `[node_id:label]` (D-13)** — always use the CANONICAL `node_id` after alias redirect (D-16); labels pass through `graphify.security.sanitize_label` before emission.
    - **Output path is HARDCODED:** `graphify-out/GRAPH_ARGUMENT.md` (ARGUE-09, T-16-05). Do not accept a user-parameterized output path. Validate confinement via `graphify.security.validate_graph_path(output_path, base=project_root)` at write time.
    - **Default overwrite behavior** (matches `GRAPH_ANALYSIS.md` precedent). Timestamped variants deferred to v1.4.x backlog.

    ### Step B5 — Report to user
    Return a summary per `graphify/commands/argue.md` step 3: verdict + Jaccard trajectory + path to `graphify-out/GRAPH_ARGUMENT.md`. Keep under 400 tokens.

    ### Anti-patterns (DO NOT)

    - **DO NOT invoke the `chat` MCP tool from this orchestration.** `composable_from: []` is the manifest-level guard; this is the prose-level reminder. Use `argue_topic` + primitive graph tools only.
    - **DO NOT add a "synthesizer" or "consensus" fifth persona** that merges claims into a fake unified verdict (D-07). Consensus is *detected* mechanically from Jaccard overlap — never produced.
    - **DO NOT strip invalid cites from a claim to "rescue" it** (Pitfall 2, D-10). The `{claim, cites}` unit is atomic — reject the whole turn or accept it whole.
    - **DO NOT echo unmatched topic tokens back to the user** when the evidence subgraph is empty (Pitfall 6). Use the generic `no_results` fallback from `graphify/commands/argue.md`.
    - **DO NOT exceed ROUND_CAP = 6 rounds** for any reason. The cap is a hard constraint.
    - **DO NOT use the key `alias_redirects`** in any transcript section or summary — the canonical meta key is `resolved_from_alias` (Pitfall 4, D-16).

    ### Deferred P2 requirements (v1.4.x backlog — NOT IMPLEMENTED in this phase)

    The following requirements are explicitly deferred per `.planning/phases/16-graph-argumentation-mode/16-CONTEXT.md` "Deferred Ideas" section. They are NOT implemented in the v1 debate loop above. Do not implement them in-session without a new gsd planning pass.

    - **ARGUE-11 [P2] — SPAR-Kit INTERROGATE step.** Optional cross-examination turn between rounds (+40% synthesis quality per protocol docs). Activation: future `--interrogate` flag. Status: v1.4.x backlog.
    - **ARGUE-12 [P2] — Persona memory across rounds.** Each persona retains its own prior claims for consistency. Substrate fields would live on `PerspectiveSeed`. Status: v1.4.x backlog.
    - **ARGUE-13 [P2] — Clash/rumble/domain intensity scoring.** Conflict-density metrics annotating the transcript. Status: v1.4.x backlog.
    ```

    STEP C — Verify Phase 9 harness anchor still matches. Run:
    ```
    grep -n "Judge 1: Analysis-1=A" graphify/skill.md
    ```
    Must return at least one line in the 1388-1550 range (original anchor). The new /graphify-argue section may also contain a reference to it (that's fine), but the original line MUST still match.

    STEP D — Run `pytest tests/test_argue.py::test_blind_label_harness_intact -q`. Must PASS (Phase 9 anchor unmodified).

    STEP E — Run full suite `pytest tests/ -q`. Zero regressions.

    Commit: `feat(16-03): add SPAR-Kit /graphify-argue orchestration to skill.md (ARGUE-06/09/10; document P2 deferrals for ARGUE-11/12/13)`.
  </action>
  <verify>
    <automated>pytest tests/test_argue.py::test_blind_label_harness_intact -q &amp;&amp; pytest tests/ -q</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "^## /graphify-argue" graphify/skill.md` returns ≥ 1 (new section heading present).
    - `grep -n "Judge 1: Analysis-1=A" graphify/skill.md` returns at least 1 line (Phase 9 anchor preserved).
    - `grep "ROUND_CAP" graphify/skill.md | grep -E "6"` matches (round cap = 6 documented).
    - `grep -E "temperature.*(<=|≤)?\\s*0\\.4|MAX_TEMPERATURE" graphify/skill.md` matches at least once in the new section.
    - `grep -iE "advisory[- ]only" graphify/skill.md | wc -l` returns ≥ 1.
    - `grep "graphify-out/GRAPH_ARGUMENT.md" graphify/skill.md` matches.
    - `grep "composable_from" graphify/skill.md | grep -E "\\[\\]"` matches (recursion guard prose reminder present).
    - `grep -c "alias_redirects" graphify/skill.md` returns 0 — OR, if the literal token appears only as a "DO NOT use" warning, then `grep "DO NOT use the key \`alias_redirects\`" graphify/skill.md` matches. (Accept either: zero usage, or solely inside the anti-pattern warning.) Quick accept: `grep -v "DO NOT" graphify/skill.md | grep -c "alias_redirects"` returns 0.
    - `grep -E "ARGUE-11.*(deferred|backlog)|Deferred P2" graphify/skill.md` matches (P2 deferrals documented).
    - `grep -E "ARGUE-12.*(deferred|backlog)" graphify/skill.md` matches.
    - `grep -E "ARGUE-13.*(deferred|backlog)" graphify/skill.md` matches.
    - `grep -E "dissent|inconclusive" graphify/skill.md` matches (valid terminal outcomes documented).
    - `grep -i "synthesizer" graphify/skill.md | grep -i "DO NOT"` matches (D-07 anti-pattern present).
    - `pytest tests/test_argue.py::test_blind_label_harness_intact -q` passes (anchor unmodified).
    - `pytest tests/ -q` — full suite green (Plans 01 + 02 + 03 tests all passing).
  </acceptance_criteria>
  <done>SPAR-Kit /graphify-argue orchestration section added to skill.md with all D-04/D-05/D-06/D-09/D-11/D-12/D-13 mechanics documented; Phase 9 blind-label harness at line 1511 preserved; P2 deferrals (ARGUE-11/12/13) explicitly documented as v1.4.x backlog; all acceptance criteria met; full suite green; commit `feat(16-03): add SPAR-Kit /graphify-argue orchestration to skill.md (ARGUE-06/09/10; document P2 deferrals for ARGUE-11/12/13)` created.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| User → `/graphify-argue` slash command | Untrusted question text enters here as `$ARGUMENTS`. |
| Slash command → `argue_topic` MCP tool | `$ARGUMENTS` passes as `topic` parameter (Plan 02 owns input validation + tokenization). |
| Persona LLM → skill.md orchestration | LLM-generated `{claim, cites}` turns cross here. Fabrication + prompt-injection-via-persona-name risks. |
| Skill.md orchestration → `graphify-out/GRAPH_ARGUMENT.md` | Transcript write boundary. Path-traversal risk. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-16-01 | Tampering (prompt injection via question / persona names / node labels) | Skill.md prompt assembly | mitigate | (1) `$ARGUMENTS` tokenized by Plan 02's `_extract_entity_terms` (strips all special chars before reaching LLM); (2) persona roster is fixed at 4 lenses — never user-controlled; (3) all node labels and persona labels rendered through `graphify.security.sanitize_label` before transcript emission (D-13). Severity: High. |
| T-16-02 | Tampering (node-ID injection / path-traversal) | Skill.md citation rendering | mitigate | Citations always use CANONICAL `node_id` from `meta.resolved_from_alias` — opaque graph keys, not filesystem paths. The `[node_id:label]` format confines node_id to its role as a graph key. Pitfall 2 regression: skill explicitly notes "DO NOT strip invalid cites" — only the validator's hard-reject + abstain path is accepted. |
| T-16-03 | Tampering (fabricated-cite bypass) | Skill.md round loop | mitigate | Every persona turn passes through `argue.validate_turn(turn, G)` BEFORE entering the transcript. Max 1 retry per turn; second failure → abstain with `[NO VALID CLAIM]` (D-09). Grep-testable regression: skill.md text mentions `validate_turn` and the 1-retry cap explicitly. Severity: High. |
| T-16-04 | DoS (round-cap / token-budget exhaustion) | Skill.md `for round in 1..ROUND_CAP` | mitigate | `ROUND_CAP = 6` imported from `graphify.argue`; skill.md text hardcodes "DO NOT exceed ROUND_CAP = 6 rounds for any reason". Early-stop on consensus or dissent reduces typical budget below cap. Each round is ≤ 4 LLM calls + ≤ 4 retries = ≤ 8 calls × 6 rounds = ≤ 48 calls worst case — parity with `/graphify analyze` users already accept. |
| T-16-05 | Tampering (GRAPH_ARGUMENT.md path traversal / write outside graphify-out/) | Skill.md write step B4 | mitigate | Output path HARDCODED to literal `"graphify-out/GRAPH_ARGUMENT.md"` in skill.md text; `validate_graph_path(output_path, base=project_root)` from `graphify.security` validates at write time (inherited Phase 18 sentinel). No user-parameterization of output path. Severity: High. |
| T-16-06 | DoS / recursion (argue → chat) | Skill.md orchestration | mitigate | (1) Manifest guard `composable_from: []` (Plan 02); (2) Skill.md anti-pattern section explicitly says "DO NOT invoke the `chat` MCP tool from this orchestration"; (3) Plan 02's `test_argue_does_not_invoke_chat` grep-checks serve.py body. Three-layer defense. Severity: High. |
| Phase 9 harness deletion / drift | Tampering (regression) | skill.md:1388-1550 | mitigate | Task 2 inserts the new section AFTER the Phase 9 block without modifying it. `test_blind_label_harness_intact` (Plan 01) asserts the literal string `"Judge 1: Analysis-1=A"` at line ~1511 remains in skill.md. Severity: High. |
| Silent scope creep (P2 implementation) | Tampering | skill.md SPAR-Kit section | mitigate | P2 requirements (ARGUE-11/12/13) explicitly documented as "v1.4.x backlog — NOT IMPLEMENTED"; acceptance criteria grep for `ARGUE-11.*deferred`, `ARGUE-12.*deferred`, `ARGUE-13.*deferred`. Prevents future executors from silently adding interrogate/memory/clash features in-session. |
</threat_model>

<verification>
- `pytest tests/test_commands.py::test_argue_md_frontmatter -q` passes.
- `pytest tests/test_argue.py::test_blind_label_harness_intact -q` passes (Phase 9 anchor preserved by this plan).
- `pytest tests/ -q` — full suite green (baseline 1347+ tests, plus Plans 01/02/03 additions).
- `grep -n "## /graphify-argue" graphify/skill.md` matches.
- `grep -n "Judge 1: Analysis-1=A" graphify/skill.md` returns the original anchor line (Phase 9 harness intact).
- `grep -E "ROUND_CAP|temperature.*0\\.4|advisory[- ]only|composable_from.*\\[\\]" graphify/skill.md` matches multiple lines in the new section.
- `grep -E "ARGUE-1[123].*(deferred|backlog)" graphify/skill.md | wc -l` returns ≥ 3 (all 3 P2s called out).
- `grep "$ARGUMENTS" graphify/commands/argue.md` matches.
- `grep -c "^target:" graphify/commands/argue.md` returns 0.
</verification>

<success_criteria>
- ARGUE-06: Phase 9 blind-label harness reused VERBATIM — original anchor at skill.md:1511 (`"Judge 1: Analysis-1=A"`) preserved; new /graphify-argue section references it by pointer, does NOT duplicate or modify; per-round shuffle (D-05) documented in the new section.
- ARGUE-09: Output is strictly advisory. `graphify/commands/argue.md` body and `graphify/skill.md` new section both state "advisory only — no code or graph mutations"; `output_path` hardcoded to `graphify-out/GRAPH_ARGUMENT.md`; `validate_graph_path` confinement documented at write step.
- ARGUE-10: `/graphify-argue <question>` slash command exists (`graphify/commands/argue.md`) with ask.md-shaped frontmatter (no `target:`, `disable-model-invocation: true`); body invokes `argue_topic` MCP tool with `$ARGUMENTS`; test `test_argue_md_frontmatter` passes.
- ARGUE-11 [P2]: INTERROGATE step explicitly documented as deferred to v1.4.x backlog in skill.md new section — NOT implemented.
- ARGUE-12 [P2]: Persona memory across rounds explicitly documented as deferred to v1.4.x backlog in skill.md new section — NOT implemented.
- ARGUE-13 [P2]: Clash/rumble/domain intensity scoring explicitly documented as deferred to v1.4.x backlog in skill.md new section — NOT implemented.
</success_criteria>

<output>
After completion, create `.planning/phases/16-graph-argumentation-mode/16-03-SUMMARY.md`. Cite D-04, D-05, D-06, D-07, D-09, D-11, D-12, D-13. Explicitly note that ARGUE-11/12/13 were disposed as `documented-deferred` (not implemented) per the CONTEXT.md Deferred Ideas section, and link to `.planning/RETROSPECTIVE.md` if a v1.4.x backlog tracker exists.
</output>
