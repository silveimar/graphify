---
phase: 14-obsidian-thinking-commands
plan: 02
type: execute
wave: 2
depends_on: [00, 01]
files_modified:
  - graphify/commands/graphify-moc.md
  - tests/test_commands.py
autonomous: true
requirements:
  - OBSCMD-03
  - OBSCMD-08
threat_model_ref:
  - TM-14-01
user_setup: []
tags: [obsidian, command, moc, trust-boundary]

must_haves:
  truths:
    - "User runs `/graphify-moc <community_id>` and receives a proposed MOC note rendered via the vault profile template — pending user `graphify approve` before any vault file is touched."
    - "Command is installed by `_install_commands` because `target: obsidian` matches claude's `supports: [code, obsidian]`."
    - "Skill body never directly writes to the vault; it always routes through `propose_vault_note`."
  artifacts:
    - path: graphify/commands/graphify-moc.md
      provides: "slash command for community MOC generation with profile fallback + trust-boundary write"
      contains: "get_community"
    - path: tests/test_commands.py
      provides: "test_graphify_moc_frontmatter + test_moc_trust_boundary_and_contract"
      contains: "test_graphify_moc_frontmatter"
  key_links:
    - from: graphify/commands/graphify-moc.md
      to: MCP tool `get_community`
      via: "skill body `Call the graphify MCP tool get_community`"
      pattern: "get_community"
    - from: graphify/commands/graphify-moc.md
      to: MCP tool `propose_vault_note`
      via: "D-06 trust-boundary suffix (never auto-write)"
      pattern: "propose_vault_note"
    - from: graphify/commands/graphify-moc.md
      to: `graphify.profile.load_profile`
      via: "vault-first + built-in fallback per D-03"
      pattern: "load_profile"
---

<objective>
Ship the `/graphify-moc <community_id>` slash command as `graphify/commands/graphify-moc.md`. The command is skill-orchestrated Markdown (no new Python); it calls `get_community` to fetch the cluster, `load_profile(vault_path)` to pick a template (vault-first, built-in fallback per D-03), renders a MOC note via profile.obsidian dataview/folder conventions, and ALWAYS routes the final write through `propose_vault_note` followed by user-driven `graphify approve` (D-06 / OBSCMD-08 / TM-14-01).

Purpose: headline validation path for the Ideaverse Integration milestone — `/graphify-moc` exercises profile adapter end-to-end.
Output: one new command file + two unit tests verifying frontmatter + trust-boundary contract.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/14-obsidian-thinking-commands/14-CONTEXT.md
@.planning/phases/14-obsidian-thinking-commands/14-RESEARCH.md
@.planning/phases/14-obsidian-thinking-commands/14-PATTERNS.md
@.planning/phases/14-obsidian-thinking-commands/14-VALIDATION.md
@.planning/phases/14-obsidian-thinking-commands/14-01-PLAN.md
@graphify/commands/argue.md
@graphify/commands/ask.md
@graphify/profile.py

<interfaces>
Analog: `graphify/commands/argue.md` (Phase 16) — 24-line skill file with single $ARGUMENTS, D-02 envelope parse, status branches.
Analog: `graphify/commands/ask.md` (Phase 17) — single-arg MCP dispatch + status branch.

MCP tool contracts (existing):
- `get_community(community_id: int)` — returns nodes, edges, cohesion score, community metadata
- `propose_vault_note(title, body_markdown, suggested_folder, note_type, rationale)` — returns proposal ID; user must run `graphify approve <id>` to commit

Profile loader:
- `graphify.profile.load_profile(vault_path) -> dict` — returns deep-merge of user `.graphify/profile.yaml` over built-in `_DEFAULT_PROFILE`. Key fields: `profile.obsidian.dataview.moc_query`, `profile.folder_mapping.moc`, `profile.obsidian.frontmatter_template`.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Write failing frontmatter + trust-boundary + contract tests for /graphify-moc</name>
  <files>tests/test_commands.py</files>
  <read_first>
    - tests/test_commands.py:195-230 — `test_ask_md_frontmatter` (analog shape, `_parse_frontmatter` helper)
    - .planning/phases/14-obsidian-thinking-commands/14-PATTERNS.md §"tests/test_commands.py — MODIFY" (test skeletons)
    - .planning/phases/14-obsidian-thinking-commands/14-VALIDATION.md rows: `test_graphify_moc_frontmatter`, `test_moc_trust_boundary_and_contract`, `test_trust_boundary_invariant_all_p1`
  </read_first>
  <behavior>
    - `test_graphify_moc_frontmatter`: file `graphify/commands/graphify-moc.md` exists; frontmatter has `name: graphify-moc`, `description` non-empty, `argument-hint` non-empty, `disable-model-invocation: true`, `target: obsidian`.
    - `test_moc_trust_boundary_and_contract`: body contains `get_community`, `load_profile`, `propose_vault_note`, `$ARGUMENTS`; body does NOT contain direct-write patterns (`Path.write_text`, `write_note_directly`, `open(` + `'w'`).
  </behavior>
  <action>
    Append to `tests/test_commands.py`:

    ```python
    def test_graphify_moc_frontmatter():
        """OBSCMD-03: /graphify-moc command file exists with vault-target frontmatter."""
        path = _commands_dir() / "graphify-moc.md"
        assert path.exists(), "graphify/commands/graphify-moc.md missing"
        fm = _parse_frontmatter(path)
        assert fm.get("name") == "graphify-moc"
        assert fm.get("description"), "description required"
        assert fm.get("argument-hint"), "argument-hint required"
        assert fm.get("disable-model-invocation") == "true"
        assert fm.get("target") == "obsidian", "graphify-moc must declare target: obsidian"

    def test_moc_trust_boundary_and_contract():
        """OBSCMD-03 + OBSCMD-08 + TM-14-01: body invokes correct MCP tools AND routes writes through propose_vault_note."""
        import re
        path = _commands_dir() / "graphify-moc.md"
        body = path.read_text()
        assert "get_community" in body, "body must invoke get_community MCP tool"
        assert "load_profile" in body, "body must call load_profile for profile-first + fallback (D-03)"
        assert "propose_vault_note" in body, "OBSCMD-08: all vault writes via propose_vault_note"
        assert "$ARGUMENTS" in body, "must accept <community_id> as $ARGUMENTS"
        for forbidden in [r"Path\.write_text", r"write_note_directly", r"open\([^)]*['\"]w['\"]"]:
            assert not re.search(forbidden, body), (
                f"TM-14-01: graphify-moc.md must not call direct-write pattern {forbidden!r}"
            )
    ```

    Run `pytest tests/test_commands.py::test_graphify_moc_frontmatter tests/test_commands.py::test_moc_trust_boundary_and_contract -q` — both FAIL (file does not yet exist). Commit: `test(14-02): add failing /graphify-moc frontmatter + trust-boundary tests`.
  </action>
  <verify>
    <automated>pytest tests/test_commands.py::test_graphify_moc_frontmatter tests/test_commands.py::test_moc_trust_boundary_and_contract -q</automated>
  </verify>
  <acceptance_criteria>
    - Both test functions present in `tests/test_commands.py`
    - Both fail with a "missing" / "file not found" style error
  </acceptance_criteria>
  <done>RED established for /graphify-moc contract.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Author `graphify/commands/graphify-moc.md` — skill file that satisfies the contract</name>
  <files>graphify/commands/graphify-moc.md</files>
  <read_first>
    - graphify/commands/argue.md — full file (24 lines) — overall envelope + status-branch template
    - graphify/commands/ask.md — full file (29 lines) — single-$ARGUMENTS status branching
    - graphify/profile.py `load_profile` — profile-first + built-in fallback semantics
    - .planning/phases/14-obsidian-thinking-commands/14-PATTERNS.md §"graphify-moc.md — NEW" (adaptation notes)
    - .planning/phases/14-obsidian-thinking-commands/14-RESEARCH.md §"Pattern 1" (MOC rendering template)
  </read_first>
  <action>
    Create `graphify/commands/graphify-moc.md` with this exact structure:

    ```markdown
    ---
    name: graphify-moc
    description: Generate a proposed MOC (Map of Content) note for a graphify community, rendered via the active Obsidian vault profile (built-in fallback applies when no .graphify/profile.yaml is present).
    argument-hint: <community_id>
    disable-model-invocation: true
    target: obsidian
    ---

    Arguments: $ARGUMENTS

    Parse `$ARGUMENTS` as an integer `community_id`. If it is not a non-negative integer, render:
    > `$ARGUMENTS` is not a valid community id. Pass an integer (e.g. `/graphify-moc 0`).
    and stop.

    Call the graphify MCP tool `get_community` with:
    - `community_id`: <parsed int>

    The response is a D-02 envelope: text body, then `---GRAPHIFY-META---`, then JSON.

    Parse `meta.status`.

    **If `status` is `no_graph`:** render verbatim:
    > No graph found at graphify-out/graph.json. Run `/graphify` to build one, then re-invoke `/graphify-moc`.
    and stop.

    **If `status` is `community_not_found`:** render:
    > Community `<community_id>` is not in the current graph. Check `graphify-out/GRAPH_REPORT.md` for valid community IDs.
    and stop.

    **If `status` is `ok`:** proceed.

    Call the graphify MCP tool `load_profile` with:
    - `vault_path`: <current Obsidian vault root>

    The loader returns the active profile (user `.graphify/profile.yaml` deep-merged over the built-in default). Use:
    - `profile.obsidian.frontmatter_template` for the note's YAML frontmatter fields (tags, type, created)
    - `profile.folder_mapping.moc` as the `suggested_folder`
    - `profile.obsidian.dataview.moc_query` as the Dataview block to embed

    Render a MOC note body in Markdown with these sections:

    1. A YAML frontmatter block populated from `profile.obsidian.frontmatter_template` with `type: moc` and `community_id: <id>`.
    2. `# <Community title>` — use the community's dominant label from `meta.top_labels[0]` if present, else `Community <community_id>`.
    3. `## Members` — bulleted wikilinks of the community's nodes (`[[<label>]]`), one per line, capped at the first 25 nodes.
    4. `## Cohesion` — one line with `cohesion_score: <float>` from `meta.cohesion`.
    5. `## Dataview` — embed the `profile.obsidian.dataview.moc_query` block verbatim.
    6. `## Related Communities` — bulleted wikilinks to other MOCs (names `MOC - <community_id>`) for communities that share cross-edges (from `meta.related_communities`).

    Call the graphify MCP tool `propose_vault_note` with:
    - `title`: `MOC - <Community title>`
    - `body_markdown`: <rendered body above>
    - `suggested_folder`: `profile.folder_mapping.moc`
    - `note_type`: `"moc"`
    - `rationale`: `"Phase 14 /graphify-moc output for community <community_id>"`

    Tell the user the proposal ID returned by `propose_vault_note` and instruct them to run `graphify approve <id>` to commit the MOC to their vault — do NOT write to the vault yourself.
    ```

    Do NOT include any direct write helper references. Do NOT mention `Path.write_text`, `open('w')`, or `write_note_directly`.

    Run `pytest tests/test_commands.py::test_graphify_moc_frontmatter tests/test_commands.py::test_moc_trust_boundary_and_contract -q` — both green. Run `pytest tests/ -q` for regression. Commit: `feat(14-02): add /graphify-moc command (OBSCMD-03, OBSCMD-08)`.
  </action>
  <verify>
    <automated>pytest tests/test_commands.py::test_graphify_moc_frontmatter tests/test_commands.py::test_moc_trust_boundary_and_contract -q</automated>
  </verify>
  <acceptance_criteria>
    - `graphify/commands/graphify-moc.md` exists
    - `grep -c "^target: obsidian$" graphify/commands/graphify-moc.md` == 1
    - `grep -c "get_community\|load_profile\|propose_vault_note\|\$ARGUMENTS" graphify/commands/graphify-moc.md` >= 4
    - `grep -cE "Path\\.write_text|write_note_directly|open\\(.*['\"]w['\"]" graphify/commands/graphify-moc.md` == 0
    - `pytest tests/test_commands.py::test_graphify_moc_frontmatter tests/test_commands.py::test_moc_trust_boundary_and_contract -q` exits 0
    - `pytest tests/ -q` exits 0
  </acceptance_criteria>
  <done>/graphify-moc ships with profile-driven render + trust-boundary write; tests green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| slash command → vault filesystem | `/graphify-moc` proposes a MOC note — must never write directly; always routes through `propose_vault_note` + user `approve` |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| TM-14-01 | Tampering | `/graphify-moc` write path | mitigate | Skill body explicitly invokes `propose_vault_note` and ends with "do NOT write to the vault yourself". `test_moc_trust_boundary_and_contract` grep-asserts absence of direct-write patterns (`Path.write_text`, `write_note_directly`, `open('w')`). |
| T-14-02-01 | Spoofing | `$ARGUMENTS` int parse | mitigate | Skill explicitly validates `$ARGUMENTS` as a non-negative integer before calling `get_community` — non-integer input short-circuits with a friendly error, no tool call fires. |
| T-14-02-02 | Information Disclosure | community member rendering | accept | Labels are already part of the graph the user built locally; no new data surface. Cap of 25 members prevents pathological expansion. |
</threat_model>

<verification>
- `pytest tests/test_commands.py -q` exits 0.
- `pytest tests/ -q` exits 0.
- `cat graphify/commands/graphify-moc.md` shows a complete skill file with the trust-boundary suffix.
</verification>

<success_criteria>
- [ ] `/graphify-moc` command file exists with `target: obsidian`
- [ ] Skill invokes `get_community`, `load_profile`, `propose_vault_note` (all three literal names present)
- [ ] No direct-write helpers referenced
- [ ] `test_graphify_moc_frontmatter` + `test_moc_trust_boundary_and_contract` green
- [ ] Full suite `pytest tests/ -q` green
</success_criteria>

<output>
After completion, create `.planning/phases/14-obsidian-thinking-commands/14-02-SUMMARY.md`.
</output>
