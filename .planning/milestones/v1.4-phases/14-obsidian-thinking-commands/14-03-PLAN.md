---
phase: 14-obsidian-thinking-commands
plan: 03
type: execute
wave: 2
depends_on: [00, 01]
files_modified:
  - graphify/commands/graphify-related.md
  - tests/test_commands.py
autonomous: true
requirements:
  - OBSCMD-04
threat_model_ref:
  - TM-14-03
user_setup: []
tags: [obsidian, command, focus-context, phase-18-consumer]

must_haves:
  truths:
    - "User invokes `/graphify-related <note-path>` and sees graph-connected notes scoped to that note's `source_file` neighborhood."
    - "Skill reads the note's frontmatter `source_file`, then calls `get_focus_context(focus_hint={\"file_path\": source_file})` — no active-note auto-detect (D-02)."
    - "When `get_focus_context` returns `status == no_context`, the skill renders a friendly explanation (never silent)."
  artifacts:
    - path: graphify/commands/graphify-related.md
      provides: "read-only `/graphify-related` skill orchestration"
      contains: "get_focus_context"
    - path: tests/test_commands.py
      provides: "test_related_contract + test_related_handles_no_context"
      contains: "test_related_contract"
  key_links:
    - from: graphify/commands/graphify-related.md
      to: MCP tool `get_focus_context`
      via: "focus_hint={file_path: <note.source_file>}"
      pattern: "get_focus_context"
    - from: graphify/commands/graphify-related.md
      to: Phase 18 CR-01 snapshot-root fix
      via: "explicit status == no_context branch"
      pattern: "no_context"
---

<objective>
Ship the `/graphify-related <note-path>` slash command as `graphify/commands/graphify-related.md`. The command is READ-ONLY (no vault write, OBSCMD-08 does not apply): it reads the YAML frontmatter `source_file` from the user-supplied note path, calls `get_focus_context(focus_hint={"file_path": source_file})`, and renders the community + 1-hop neighbor list. Explicit `status == no_context` handling is mandatory (TM-14-03: spoofed/outside-project `source_file` must produce a user-visible explanation, not silence).

Purpose: closes OBSCMD-04; demonstrates Phase 18 `get_focus_context` consumer integration.
Output: one new command file + two unit tests.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/14-obsidian-thinking-commands/14-CONTEXT.md
@.planning/phases/14-obsidian-thinking-commands/14-RESEARCH.md
@.planning/phases/14-obsidian-thinking-commands/14-PATTERNS.md
@.planning/phases/14-obsidian-thinking-commands/14-VALIDATION.md
@.planning/phases/18-focus-aware-graph-context/18-VERIFICATION.md
@.planning/phases/18-focus-aware-graph-context/18-CONTEXT.md
@graphify/commands/ask.md

<interfaces>
Analog: `graphify/commands/ask.md` — single-$ARGUMENTS, D-02 envelope, status-branch render. Copy shape verbatim.

MCP tool contract (Phase 18):
- `get_focus_context(focus_hint: dict, neighborhood_depth: int = 2, include_community: bool = true)` — `focus_hint` schema: `{"file_path": "<absolute or project-relative>", "function_name": "<optional>", "line": <int optional>}`.
- Status envelope values: `ok`, `no_graph`, `no_context` (spoofed / outside-project path / not in graph). Per Phase 18 SC2 + CR-01 snapshot-root fix, `no_context` is the canonical "path doesn't resolve" signal — skill MUST handle it explicitly.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Write failing contract + no_context handling tests for /graphify-related</name>
  <files>tests/test_commands.py</files>
  <read_first>
    - tests/test_commands.py:195-230 — `_parse_frontmatter` + `test_ask_md_frontmatter`
    - .planning/phases/14-obsidian-thinking-commands/14-VALIDATION.md rows `test_related_contract`, `test_related_handles_no_context`
    - .planning/phases/14-obsidian-thinking-commands/14-PATTERNS.md §"graphify-related.md — NEW"
  </read_first>
  <behavior>
    - `test_related_contract`: file `graphify/commands/graphify-related.md` exists; frontmatter name=`graphify-related`, `target: obsidian`, `argument-hint: <note-path>`; body references `get_focus_context`, `source_file` (reads from note frontmatter), `$ARGUMENTS`.
    - `test_related_handles_no_context`: body contains explicit handling of `no_context` status (literal `no_context` string + user-facing render prose).
  </behavior>
  <action>
    Append to `tests/test_commands.py`:

    ```python
    def test_related_contract():
        """OBSCMD-04: /graphify-related exists with correct frontmatter and tool dispatch."""
        path = _commands_dir() / "graphify-related.md"
        assert path.exists()
        fm = _parse_frontmatter(path)
        assert fm.get("name") == "graphify-related"
        assert fm.get("target") == "obsidian"
        assert fm.get("argument-hint"), "argument-hint required"
        assert fm.get("disable-model-invocation") == "true"
        body = path.read_text()
        assert "$ARGUMENTS" in body, "must accept <note-path> as $ARGUMENTS"
        assert "get_focus_context" in body, "body must invoke get_focus_context"
        assert "source_file" in body, "body must read source_file from note frontmatter (D-02)"

    def test_related_handles_no_context():
        """OBSCMD-04 + TM-14-03: /graphify-related explicitly handles status == no_context
        (spoof-silent invariant from Phase 18 SC2 / CR-01)."""
        path = _commands_dir() / "graphify-related.md"
        body = path.read_text()
        assert "no_context" in body, (
            "body must explicitly branch on status == no_context so spoofed/outside-project "
            "source_file values produce a user-visible explanation, not silence"
        )
    ```

    Run `pytest tests/test_commands.py::test_related_contract tests/test_commands.py::test_related_handles_no_context -q` — both FAIL. Commit: `test(14-03): add failing /graphify-related contract + no_context tests`.
  </action>
  <verify>
    <automated>pytest tests/test_commands.py::test_related_contract tests/test_commands.py::test_related_handles_no_context -q</automated>
  </verify>
  <acceptance_criteria>
    - Both tests present; both fail with "file missing" style assertion error.
  </acceptance_criteria>
  <done>RED for /graphify-related contract.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Author `graphify/commands/graphify-related.md`</name>
  <files>graphify/commands/graphify-related.md</files>
  <read_first>
    - graphify/commands/ask.md — full file (copy structural shape)
    - .planning/phases/18-focus-aware-graph-context/18-VERIFICATION.md — `get_focus_context` status enum + `no_context` semantics
    - .planning/phases/14-obsidian-thinking-commands/14-PATTERNS.md §"graphify-related.md — NEW" (adaptation)
  </read_first>
  <action>
    Create `graphify/commands/graphify-related.md` with this exact structure:

    ```markdown
    ---
    name: graphify-related
    description: Show graph-connected notes (community peers + 1-hop neighbors) for the given vault note, scoped to that note's source_file.
    argument-hint: <note-path>
    disable-model-invocation: true
    target: obsidian
    ---

    Arguments: $ARGUMENTS

    Treat `$ARGUMENTS` as a path to an Obsidian note (absolute, vault-relative, or home-relative).

    Read the note file at `$ARGUMENTS`. Parse its YAML frontmatter block (lines between the first two `---` markers). Extract the `source_file` field — the path of the real project file this note summarises.

    If the note has no YAML frontmatter, or no `source_file` field, render:
    > The note at `$ARGUMENTS` has no `source_file:` frontmatter field. `/graphify-related` needs this field to look up the note's position in the graph. Add `source_file: <path-in-project>` to the note's frontmatter and retry.
    and stop.

    Call the graphify MCP tool `get_focus_context` with:
    - `focus_hint`: `{"file_path": "<source_file value>"}`
    - `neighborhood_depth`: 2
    - `include_community`: true

    The response is a D-02 envelope: text body, then `---GRAPHIFY-META---`, then JSON.

    Parse `meta.status`.

    **If `status` is `no_graph`:** render verbatim:
    > No graph found at graphify-out/graph.json. Run `/graphify` to build one, then re-invoke `/graphify-related`.
    and stop.

    **If `status` is `no_context`:** render verbatim (TM-14-03 explicit-silence mitigation):
    > The `source_file` path `<source_file value>` does not resolve to any node in the graph. Possible causes:
    > - The path is outside the project root snapshot (Phase 18 CR-01 guard).
    > - The file was added after the last `/graphify` run — rebuild the graph.
    > - The note's frontmatter points at a path that doesn't exist on disk.
    and stop.

    **If `status` is `ok`:** render the `text_body` verbatim. It already contains:
    1. `## Community peers` — other nodes in the same community as `source_file`.
    2. `## 1-hop neighbors` — direct graph neighbors outside the community.
    3. `## Citations` — `{node_id, label, source_file}` per reference.

    Do NOT re-summarize or paraphrase. Do NOT write to the vault — this command is read-only.
    ```

    Run `pytest tests/test_commands.py::test_related_contract tests/test_commands.py::test_related_handles_no_context -q` — both green. Full suite `pytest tests/ -q` green. Commit: `feat(14-03): add /graphify-related command (OBSCMD-04)`.
  </action>
  <verify>
    <automated>pytest tests/test_commands.py::test_related_contract tests/test_commands.py::test_related_handles_no_context -q</automated>
  </verify>
  <acceptance_criteria>
    - `graphify/commands/graphify-related.md` exists with `target: obsidian`
    - `grep -c "get_focus_context\|source_file\|no_context\|\$ARGUMENTS" graphify/commands/graphify-related.md` >= 4
    - `grep -c "propose_vault_note\|Path.write_text" graphify/commands/graphify-related.md` == 0 (read-only)
    - Targeted pytest exits 0; full `pytest tests/ -q` exits 0
  </acceptance_criteria>
  <done>/graphify-related read-only, `no_context` handled explicitly; tests green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| user-supplied `<note-path>` + note's frontmatter `source_file` → `get_focus_context` lookup | Spoofed or out-of-project `source_file` must not silently fail |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| TM-14-03 | Spoofing / Information Disclosure | `source_file` → `focus_hint` lookup | mitigate | Skill explicitly branches on `status == no_context` (test-enforced) so the user always sees an explanation. Phase 18 CR-01 snapshot-root fix already confines `file_path` resolution to the project root, preventing path-traversal or out-of-tree lookups. |
| T-14-03-01 | Information Disclosure | reading note frontmatter | accept | The note lives in the user's own vault under their control; reading its frontmatter is inherent to the command's purpose. |
| T-14-03-02 | Tampering | no write surface | mitigate | `/graphify-related` is read-only by design. Test asserts absence of `propose_vault_note` / direct-write patterns. |
</threat_model>

<verification>
- `pytest tests/test_commands.py -q` exits 0.
- `pytest tests/ -q` exits 0.
- `grep -n "no_context" graphify/commands/graphify-related.md` returns at least 2 hits (status branch label + user-facing render).
</verification>

<success_criteria>
- [ ] `/graphify-related` command file exists with `target: obsidian`
- [ ] Skill reads note `source_file` frontmatter before calling `get_focus_context`
- [ ] `no_context` status branch rendered with user-facing explanation
- [ ] Read-only: no `propose_vault_note` reference
- [ ] `test_related_contract` + `test_related_handles_no_context` green
- [ ] Full suite green
</success_criteria>

<output>
After completion, create `.planning/phases/14-obsidian-thinking-commands/14-03-SUMMARY.md`.
</output>
