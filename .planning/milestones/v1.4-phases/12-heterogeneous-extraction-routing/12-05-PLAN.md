---
phase: 12-heterogeneous-extraction-routing
plan: 05
type: execute
wave: 3
depends_on:
  - 12-03-PLAN.md
files_modified:
  - graphify/routing.py
  - graphify/routing_canary.py
  - graphify/routing_cost.py
  - tests/test_routing_p2.py
autonomous: true
requirements:
  - ROUTE-08
  - ROUTE-09
  - ROUTE-10
threat_refs:
  - T-12-05
must_haves:
  truths:
    - "ROUTE-09: pre-flight abort before first LLM when projected cost > GRAPHIFY_COST_CEILING"
    - "ROUTE-08: canary warning to stderr (default) when ratio &lt; 0.6"
---

<objective>
Ship **P2** items: **canary probes** (ROUTE-08), **`GRAPHIFY_COST_CEILING`** pre-flight projection (ROUTE-09), **image → vision slot** completion (ROUTE-10) beyond YAML stub in Plan 01 — wire skip fallback and vision model selection into `extract`/router paths.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
</execution_context>

<context>
@.planning/REQUIREMENTS.md (ROUTE-08–10)
@.planning/phases/12-heterogeneous-extraction-routing/12-CONTEXT.md
</context>

<tasks>

<task type="execute">
  <name>Task 1: GRAPHIFY_COST_CEILING pre-flight</name>
  <read_first>
    - graphify/routing.py
    - graphify/extract.py
  </read_first>
  <action>
    Add `routing_cost.py` with `estimate_run_cost(files: list[Path], router: Router) -> int` using **token heuristic** (reuse batch `CHARS_PER_TOKEN` or file size) × per-tier price table **optional** in YAML; if no prices, estimate by **file count × tier weight**. Read `GRAPHIFY_COST_CEILING` from env; if set and estimate &gt; ceiling, **`sys.exit`** or raise `CostCeilingError` with message **before** any worker starts — ROUTE-09. Message must include ceiling and estimate integers.
  </action>
  <acceptance_criteria>
    - `GRAPHIFY_COST_CEILING=1 pytest tests/test_routing_p2.py::test_cost_ceiling_aborts -q` exits with non-zero or test catches exception per chosen API
    - `grep -n "GRAPHIFY_COST_CEILING" graphify/routing_cost.py graphify/extract.py` ≥ 2 lines
  </acceptance_criteria>
</task>

<task type="execute">
  <name>Task 2: Canary probes ROUTE-08</name>
  <read_first>
    - .planning/REQUIREMENTS.md (ROUTE-08)
  </read_first>
  <action>
    Implement every **N**th cheap-routed file re-run on expensive model in **test double** / mock; compare **edge counts** of resulting extraction dicts; if ratio &lt; 0.6, print **`routing quality regressed`** to **stderr** (default CONTEXT). Configurable `N` via YAML or env. Integrate hook in router pipeline after extract.
  </action>
  <acceptance_criteria>
    - `pytest tests/test_routing_p2.py::test_canary_warns -q` passes with capsys stderr check for substring `routing quality regressed`
  </acceptance_criteria>
</task>

<task type="execute">
  <name>Task 3: ROUTE-10 image path</name>
  <read_first>
    - graphify/detect.py (image file typing if present)
  </read_first>
  <action>
    Ensure image suffixes resolve through `resolve_model` to vision tier when configured; when vision model missing, **skip** image (no extraction) matching REQ. Add test with `.png` path.
  </action>
  <acceptance_criteria>
    - Test proves skipped image does not call cache save for content extraction when vision absent
  </acceptance_criteria>
</task>

</tasks>

<threat_model>
| Threat ID | Category | Mitigation |
|-----------|----------|------------|
| T-12-05 | Information disclosure | Cost error messages must not echo file contents — only paths/counts |
</threat_model>

<verification>
- `pytest tests/test_routing_p2.py -q`
</verification>

<success_criteria>
- [ ] ROUTE-08–10 covered by automated tests
</success_criteria>
