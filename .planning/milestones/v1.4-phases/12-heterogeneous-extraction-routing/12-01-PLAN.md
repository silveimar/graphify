---
phase: 12-heterogeneous-extraction-routing
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - graphify/routing.py
  - graphify/routing_models.yaml
  - tests/test_routing.py
  - pyproject.toml
autonomous: true
requirements:
  - ROUTE-01
  - ROUTE-02
  - ROUTE-06
  - ROUTE-10
threat_refs:
  - T-12-01
must_haves:
  truths:
    - "`ComplexityClass` (or equivalent) ordering is deterministic: trivial < simple < complex < vision"
    - "Code file types never resolve to a model below `mid` tier (ROUTE-06)"
  artifacts:
    - path: "graphify/routing_models.yaml"
      provides: "tier → model_id, endpoint, optional vision slot"
    - path: "graphify/routing.py"
      provides: "classify_path(path) → metrics + tier + resolved model config"
---

<objective>
Introduce **`graphify/routing.py`** and **`graphify/routing_models.yaml`**: AST-derived complexity classification (ROUTE-01), declarative per-tier model mapping (ROUTE-02), **hard floor** so code files never route below mid-tier (ROUTE-06), and **vision** slot for image file types with skip fallback (ROUTE-10). No CLI or `extract()` wiring yet — Plan 03 consumes this API.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
</execution_context>

<context>
@.planning/phases/12-heterogeneous-extraction-routing/12-CONTEXT.md
@.planning/phases/12-heterogeneous-extraction-routing/12-RESEARCH.md
@graphify/extract.py
@pyproject.toml
</context>

<tasks>

<task type="execute">
  <name>Task 1: routing_models.yaml schema + defaults</name>
  <read_first>
    - .planning/REQUIREMENTS.md (ROUTE-02, ROUTE-10 verbatim)
    - graphify/extract.py (suffix dispatch `_DISPATCH` keys for file typing)
    - pyproject.toml (`[project.optional-dependencies]`)
  </read_first>
  <action>
    Add `graphify/routing_models.yaml` with keys at minimum: `tiers.trivial`, `tiers.simple`, `tiers.complex`, each containing `model_id`, `endpoint` (string URI or logical name). Add `vision` tier OR `image` section for ROUTE-10 with `model_id` optional — when absent, router returns explicit **skip** sentinel (document in routing.py docstring). Add `thresholds` / `weights` section placeholders for YAML-driven tunability (D-05) with **safe defaults** merged in code when keys omitted.
  </action>
  <acceptance_criteria>
    - `grep -E '^(tiers|thresholds|vision|image):' graphify/routing_models.yaml` returns at least one match for tiers
    - `python -c "import yaml; yaml.safe_load(open('graphify/routing_models.yaml'))"` exits 0
  </acceptance_criteria>
</task>

<task type="execute">
  <name>Task 2: routing.py — classification + resolve_model</name>
  <read_first>
    - graphify/routing.py (create)
    - graphify/extract.py (LanguageConfig / per-language extractors for non-Python metrics: import counting, nesting — pick minimal tree-sitter-free heuristics for v1: line count + symbol regex where needed)
    - .planning/phases/12-heterogeneous-extraction-routing/12-CONTEXT.md (D-05, D-06)
  </read_first>
  <action>
    Implement `load_routing_config(path: Path | None = None) -> dict` using **only** `yaml.safe_load` on resolved file under repo/package. Implement `classify_file(path: Path) -> ComplexityMetrics` using **radon** for `.py` when `import radon` succeeds (optional extra `routing` or document `pip install radon`); otherwise use conservative default complexity **without** silent downgrade. For other extensions, compute **nesting depth**, **import count**, **symbol count** via lightweight parsing consistent with ROUTE-01 text. Implement `resolve_model(metrics, file_type, config) -> ResolvedRoute` enforcing ROUTE-06: if `file_type == "code"` (or path suffix in code set), **tier rank** must be ≥ simple (mid) even when metrics say trivial — bump tier to `simple` minimum. For image types, return vision model or skip per ROUTE-10. Export a `Router` dataclass/class holding config + methods `classify`, `resolve`, `tier_order` for Plans 03–05.
  </action>
  <acceptance_criteria>
    - `pytest tests/test_routing.py -q` exits 0
    - `grep -n "def classify_file\|def resolve_model\|class Router" graphify/routing.py` returns ≥ 3 lines
    - Test file includes: (1) Python file classified trivial by raw metrics but **resolves to simple/mid** tier; (2) YAML load picks alternative model when `routing_models.yaml` temp copy overrides `tiers.simple.model_id`
  </acceptance_criteria>
</task>

<task type="execute">
  <name>Task 3: Optional extra for radon</name>
  <read_first>
    - pyproject.toml
  </read_first>
  <action>
    Add optional dependency group e.g. `routing = ["radon"]` and include `radon` in `all = [...]` if project policy allows; document in `CLAUDE.md` one-line under Build & Test.
  </action>
  <acceptance_criteria>
    - `grep 'radon' pyproject.toml` matches at least once
  </acceptance_criteria>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| User YAML → loader | Only `yaml.safe_load` on user-controlled `routing_models.yaml` from known path; no `eval` |

## STRIDE

| Threat ID | Category | Disposition | Mitigation |
|-----------|----------|-------------|------------|
| T-12-01 | Tampering | mitigate | Invalid YAML / missing keys fall back to code defaults; never skip ROUTE-06 floor |
</threat_model>

<verification>
- `pytest tests/test_routing.py -q`
- `python -c "from graphify.routing import Router, load_routing_config"`
</verification>

<success_criteria>
- [ ] ROUTE-01 metrics implemented per language strategy above
- [ ] ROUTE-02 mapping driven by YAML
- [ ] ROUTE-06 floor enforced for code
- [ ] ROUTE-10 vision or skip encoded in API
</success_criteria>
