# Phase 8.1: Approve & Pipeline Wiring - Discussion Log

> **Audit trail only.** Do not use as input to planning, research, or execution agents.
> Decisions are captured in CONTEXT.md — this log preserves the alternatives considered.

**Date:** 2026-04-12
**Phase:** 08.1-approve-pipeline-wiring
**Areas discussed:** Manifest threading for approve, Approve conflict behavior, Snapshot/delta presentation, Snapshot wiring placement

---

## Manifest Threading for Approve

| Option | Description | Selected |
|--------|-------------|----------|
| graphify-out/ relative | Manifest always at graphify-out/vault-manifest.json, same as --obsidian. Consistent with Phase 8 D-08. | **Yes** |
| Derive from --vault path | Manifest at <vault-path>/../vault-manifest.json. Works if vault is outside graphify-out/, but fragile. | No |
| You decide | Claude picks approach. | No |

| Option | Description | Selected |
|--------|-------------|----------|
| Yes, add --force | Consistent with --obsidian --force. Bypass user-modified detection, preserve sentinel blocks. | **Yes** |
| No --force for approve | Always respect user modifications. User deletes note to re-approve. | No |
| You decide | Claude picks. | No |

## Approve Conflict Behavior

| Option | Description | Selected |
|--------|-------------|----------|
| Skip + warn | SKIP_PRESERVE the note, print warning, proposal stays pending. User can --force or delete. | **Yes** |
| Skip silently | SKIP_PRESERVE without message. User discovers via dry-run. | No |
| Fail the approve | Exit with error. Force explicit choice. | No |

| Option | Description | Selected |
|--------|-------------|----------|
| Delete proposal | Proposal deleted after successful write. Keeps proposals/ clean. | **Yes** |
| Move to archive | Move to graphify-out/proposals/approved/ as audit trail. | No |
| You decide | Claude picks. | No |

## Snapshot/Delta Presentation

| Option | Description | Selected |
|--------|-------------|----------|
| Summary line | Brief line: "Snapshot saved (N nodes). Delta: +X added, -Y removed, Z migrated." | **Yes** |
| Full delta excerpt | Print summary section of GRAPH_DELTA.md inline. More verbose. | No |
| Silent + file path | Just print file paths. User reads files themselves. | No |
| You decide | Claude picks. | No |

| Option | Description | Selected |
|--------|-------------|----------|
| Snapshot only, no delta | First run saves snapshot, skips delta generation. auto_snapshot_and_delta returns None. | **Yes** |
| Empty delta report | Generate GRAPH_DELTA.md showing all nodes as 'added'. Noisy baseline. | No |

## Snapshot Wiring Placement

| Option | Description | Selected |
|--------|-------------|----------|
| After graph.json export | Call after to_json(G, communities, 'graphify-out/graph.json') ~line 425. Graph finalized. | **Yes** |
| After all exports complete | Call after HTML/JSON/SVG/report/obsidian exports. Later in pipeline. | No |
| You decide | Claude picks. | No |

| Option | Description | Selected |
|--------|-------------|----------|
| Yes, include cluster-only | Cluster-only produces valid graph. Add snapshot at ~line 947 too. | **Yes** |
| No, full pipeline only | Cluster-only is lightweight, skip side artifacts. | No |
