# Phase 8.2: MCP Query Enhancements - Discussion Log

> **Audit trail only.** Do not use as input to planning, research, or execution agents.
> Decisions are captured in CONTEXT.md — this log preserves the alternatives considered.

**Date:** 2026-04-13
**Phase:** 08.2-mcp-query-enhancements
**Areas discussed:** Provenance field presentation, Agent-edges query design, Staleness computation context
**Mode:** --auto (all decisions auto-selected)

---

## Provenance Field Presentation

| Option | Description | Selected |
|--------|-------------|----------|
| Append to existing text format | Add extracted_at, source_hash, staleness as new lines in get_node output | ✓ |
| Return structured JSON | Change get_node to return JSON instead of text | |
| Separate provenance tool | New tool `get_node_provenance` instead of extending get_node | |

**User's choice:** [auto] Append to existing text format (recommended default)
**Notes:** Keeps backward compatibility — agents already parse multi-line text. Adding lines is non-breaking.

---

## Agent-Edges Query Design

| Option | Description | Selected |
|--------|-------------|----------|
| Match get_annotations pattern | peer_id + session_id filters, JSON array return | ✓ |
| Minimal (no filters) | Return all agent-edges always, let agent filter client-side | |
| Full filter set | peer_id + session_id + time_range + node_id filters | |

**User's choice:** [auto] Match get_annotations pattern (recommended default)
**Notes:** Mirrors Phase 7 D-06 filter pattern. Keeps tool surface consistent across query tools.

---

## Staleness Computation Context

| Option | Description | Selected |
|--------|-------------|----------|
| Compute live using classify_staleness() | Call delta.py classifier on each get_node call | ✓ |
| Return raw provenance fields only | Let agent compute staleness from extracted_at + source_hash | |
| Cache staleness at graph load time | Pre-compute on reload, store as node attribute | |

**User's choice:** [auto] Compute live using classify_staleness() (recommended default)
**Notes:** Single Path.exists() + stat() per call is negligible. Returns actionable FRESH/STALE/GHOST instead of raw hashes agents can't interpret.

---

## Claude's Discretion

- JSON Schema for get_agent_edges inputSchema
- Whether to add optional node_id filter
- Import style for classify_staleness
- Tool description text

## Deferred Ideas

None — discussion stayed within phase scope
