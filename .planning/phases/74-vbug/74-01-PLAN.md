---
phase: 74-vbug
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - graphify/__main__.py
autonomous: true
requirements: [VBUG-01]

must_haves:
  truths:
    - "Running `graphify update-vault` from a vault CWD without --vault does not exit with argparse code 2"
    - "Running `graphify vault-promote` from a vault CWD without --vault does not exit with argparse code 2"
    - "Running either command from a non-vault CWD without --vault exits 2 with a friendly stderr error (not argparse's terse 'the following arguments are required' wording)"
  artifacts:
    - path: "graphify/__main__.py"
      provides: "update-vault and vault-promote argparse specs with required=False, default=None"
      contains: "required=False"
  key_links:
    - from: "graphify/__main__.py:~3312 (update-vault argparse)"
      to: "post-parse auto-adopt guard in same dispatch branch"
      via: "opts.vault is None reaches the guard instead of argparse exiting first"
      pattern: "required=False.*default=None"
    - from: "graphify/__main__.py:~3358 (vault-promote argparse)"
      to: "post-parse auto-adopt guard in same dispatch branch"
      via: "opts.vault is None reaches the guard instead of argparse exiting first"
      pattern: "required=False.*default=None"
---

<objective>
Apply the locked Approach (b) fix from `.planning/debug/vault-cwd-gate-argparse-required.md` to the two defective argparse sites (`update-vault`, `vault-promote`) so the existing post-parse auto-adopt guard becomes reachable. Tighten the guard so a non-auto-adopt invocation without `--vault` produces a friendly stderr error and exit 2.

Purpose: VBUG-01 — eliminate argparse exit-2 from a vault CWD without `--vault` for these two commands, preserving the contract that `--vault` is effectively required outside a vault CWD.
Output: Two argparse-spec edits + tightened post-parse guard in `graphify/__main__.py`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/74-vbug/74-CONTEXT.md
@.planning/debug/vault-cwd-gate-argparse-required.md
@graphify/__main__.py
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Flip update-vault and vault-promote --vault to required=False with tightened post-parse guard</name>
  <files>graphify/__main__.py</files>
  <read_first>
    - graphify/__main__.py (use smart_outline; read lines 3280–3370 spanning both argparse sites and the post-parse guards)
    - .planning/debug/vault-cwd-gate-argparse-required.md (fix-design section — Approach (b))
    - .planning/phases/74-vbug/74-CONTEXT.md (locked decisions — exact wording for help string and friendly error)
  </read_first>
  <behavior>
    - From a vault CWD with no --vault: argparse does NOT exit; the post-parse guard sees gate == "auto-adopt" and sets opts.vault = str(Path.cwd()); auto-adopt notice prints to stderr.
    - From a non-vault CWD with no --vault: argparse does NOT exit; post-parse guard sees gate != "auto-adopt" and opts.vault is None; emits friendly stderr error and exit 2.
    - With --vault explicitly supplied: behavior unchanged (argparse accepts; guard no-ops because opts.vault is set).
  </behavior>
  <action>
    1. In `graphify/__main__.py` at the `update-vault` argparse spec (~line 3312), replace the existing `--vault` declaration with:
       `_p_uv.add_argument("--vault", required=False, default=None, help="Path to the Obsidian vault directory (optional when invoked from a vault CWD)")`
       (preserve any other kwargs the existing call has, e.g. `metavar`, `dest`, `type` — only `required` and `default` change, plus the help-string suffix).
    2. In `graphify/__main__.py` at the `vault-promote` argparse spec (~line 3358), apply the identical change to `_p_vp.add_argument("--vault", ...)`.
    3. In BOTH dispatch branches, locate the existing post-parse guard `if gate == "auto-adopt" and not _uv_vault: _uv_vault = str(Path.cwd())` (and the corresponding vault-promote variable). Immediately AFTER that guard, add a new branch:
       ```python
       if gate != "auto-adopt" and opts.vault is None:
           print("error: --vault is required when not running from a vault directory", file=sys.stderr)
           sys.exit(2)
       ```
       Adapt the variable name (`opts.vault` vs whatever the local already uses — match the existing code style at each site). The friendly error string must be EXACTLY: `error: --vault is required when not running from a vault directory`.
    4. Do NOT touch the other 13 gated dispatch branches — they use hand-rolled parsing and already work correctly per CONTEXT.md.
  </action>
  <verify>
    <automated>cd /Users/silveimar/Documents/silogia-repos/engineering-tools/graphify && grep -n 'required=False' graphify/__main__.py | grep -i vault &amp;&amp; grep -n 'required is --vault is required when not running from a vault directory\|--vault is required when not running from a vault directory' graphify/__main__.py</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c 'required=False, default=None' graphify/__main__.py` returns at least 2
    - `grep -c -- '--vault is required when not running from a vault directory' graphify/__main__.py` returns at least 2 (one per dispatch branch)
    - `grep -c 'required=True' graphify/__main__.py` does NOT include any `--vault` argparse line (verify with `grep -n 'required=True' graphify/__main__.py | grep -- '--vault'` returns empty)
    - `pytest tests/ -q -x` passes on the developer's installed Python (full suite, no regressions); existing skipped RED tests in tests/test_vault_cwd.py remain skipped (they are moved/unskipped in plan 02)
    - `python -c "import ast; ast.parse(open('graphify/__main__.py').read())"` succeeds (file still parses)
  </acceptance_criteria>
  <done>Both argparse sites use `required=False, default=None`; both dispatch branches emit the exact friendly error and exit 2 when `gate != "auto-adopt"` and `opts.vault is None`; help text on `--vault` includes "(optional when invoked from a vault CWD)"; no other code paths modified.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| user shell → CLI argparse | local CWD detection only; no new external input vectors |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-74-01 | Tampering | post-parse error message | mitigate | error string is a fixed literal printed via `print(..., file=sys.stderr)` — no f-string interpolation of user input |
| T-74-02 | Information Disclosure | friendly error | accept | message reveals only that --vault is required; no path or environment data leaked |
| T-74-03 | Elevation of Privilege | argparse default change | accept | `default=None` is strictly less permissive than current `required=True` (which crashes); auto-adopt guard already exists and is the intended path |
</threat_model>

<verification>
- All 15 gated subcommands run from a vault CWD without `--vault` succeed past the argparse layer (validated by plan 74-02).
- `update-vault` and `vault-promote` from a non-vault CWD without `--vault` exit 2 with the exact friendly error string.
- No regressions in `pytest tests/ -q`.
</verification>

<success_criteria>
- VBUG-01 satisfied: argparse exit-2 no longer fires from a vault CWD for `update-vault` or `vault-promote`.
- Friendly error replaces argparse's terse error for the non-vault-CWD-no-flag case.
- All existing tests pass.
</success_criteria>

<output>
After completion, create `.planning/phases/74-vbug/74-01-SUMMARY.md`.
</output>
