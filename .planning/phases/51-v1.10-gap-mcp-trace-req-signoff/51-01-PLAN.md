---
phase: 51-v1.10-gap-mcp-trace-req-signoff
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified:
  - .planning/phases/47-mcp-trace-integration/47-VERIFICATION.md
  - .planning/REQUIREMENTS.md
  - .planning/phases/47-mcp-trace-integration/47-VALIDATION.md
  - .planning/phases/51-v1.10-gap-mcp-trace-req-signoff/51-VALIDATION.md
  - .planning/phases/51-v1.10-gap-mcp-trace-req-signoff/51-01-SUMMARY.md
  - .planning/ROADMAP.md
autonomous: true
requirements:
  - CCODE-03
  - CCODE-04
must_haves:
  truths:
    - "`python -m graphify capability --validate` exits 0 from repo root after `pip install -e \".[mcp,pdf,watch]\"` (D-51.02 / D-47.01; slim `server.json` parity via hash + tool_count, not substring-in-JSON alone per 51-RESEARCH)."
    - "`pytest tests/test_concept_code_mcp.py::test_concept_code_hops_golden_path -q` exits 0 (CCODE-04 golden-path `implements` hops via `_run_concept_code_hops` per D-51.03)."
    - "`pytest tests/ -q` exits 0 (full gate before REQ ticks per D-51.04 wrap)."
    - "Grep anchors exist: `grep -n concept_code_hops graphify/mcp_tool_registry.py`, `grep -n _run_concept_code_hops graphify/serve.py`, `grep -n concept_code_hops graphify/capability_tool_meta.yaml`, `grep -n concept_code_hops docs/RELATIONS.md docs/ARCHITECTURE.md` (per D-51.01)."
    - "`47-VERIFICATION.md` states explicit REQ ↔ implementation mapping: typed concept↔code hops = `concept_code_hops`; `/trace` in `skill.md` + `entity_trace` MCP = temporal snapshot; `entity_trace` alone does not satisfy typed-hop audit (D-51.03)."
    - "Exactly two checked rows: `grep -E '^- \\[x\\] \\*\\*CCODE-0[34]:\\*\\*' .planning/REQUIREMENTS.md` yields line count **2** (CCODE-03 and CCODE-04 only)."
  artifacts:
    - path: ".planning/phases/47-mcp-trace-integration/47-VERIFICATION.md"
      provides: "Canonical verification + REQ mapping + Automated transcripts + Gaps (D-51.01, D-51.04)"
    - path: ".planning/REQUIREMENTS.md"
      provides: "CCODE-03 and CCODE-04 checkboxes ticked with evidence-backed format"
  key_links:
    - from: "47-VERIFICATION.md REQ table"
      to: "tests/test_concept_code_mcp.py::test_concept_code_hops_golden_path"
      via: "Automated section cites pytest + commit SHA"
    - from: "REQUIREMENTS.md CCODE-04 row"
      to: "D-51.03 text in 47-VERIFICATION.md"
      via: "No implication that entity_trace satisfies typed hops"
---

<objective>
Produce **`47-VERIFICATION.md`**, run the automated proof stack, tick **CCODE-03** and **CCODE-04** in **`REQUIREMENTS.md`** only with recorded evidence, and close Phase **51** planning metadata (ROADMAP, optional validation files, **`51-01-SUMMARY.md`**).

Purpose: Close gap-closure debt from Phase **47** per **D-51.01–D-51.04** without new MCP surface area.

Output: Signed verification artifact, two REQ checkboxes, updated ROADMAP Phase **51** Plans line, optional Nyquist touch-up on phase validation frontmatter.
</objective>

<execution_context>
@$HOME/.cursor/get-shit-done/workflows/execute-plan.md
@$HOME/.cursor/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/51-v1.10-gap-mcp-trace-req-signoff/51-CONTEXT.md
@.planning/phases/51-v1.10-gap-mcp-trace-req-signoff/51-RESEARCH.md
@.planning/phases/45-baselines-detect-self-ingestion/45-VERIFICATION.md
@.planning/phases/47-mcp-trace-integration/47-01-PLAN.md
@.planning/phases/47-mcp-trace-integration/47-02-PLAN.md
@CLAUDE.md
</context>

<read_first>
Executor must read before any edits: `51-CONTEXT.md`, `51-RESEARCH.md`, `45-VERIFICATION.md` (structure template), `47-01-PLAN.md`, `47-02-PLAN.md`, `CLAUDE.md`. Optional template: `49-add-version-flag-to-graphify-command-and-also-print-current-/49-VERIFICATION.md` if additional frontmatter examples help.
</read_first>

<tasks>

<task type="auto">
  <name>Task 1 — Evidence sweep and 47-VERIFICATION.md draft</name>
  <read_first>
    - .planning/phases/51-v1.10-gap-mcp-trace-req-signoff/51-CONTEXT.md
    - .planning/phases/51-v1.10-gap-mcp-trace-req-signoff/51-RESEARCH.md
    - .planning/phases/45-baselines-detect-self-ingestion/45-VERIFICATION.md
    - .planning/phases/47-mcp-trace-integration/47-01-PLAN.md
    - .planning/phases/47-mcp-trace-integration/47-02-PLAN.md
  </read_first>
  <acceptance_criteria>
    - **D-51.01:** New file `.planning/phases/47-mcp-trace-integration/47-VERIFICATION.md` exists with YAML frontmatter (`status`, `phase: 47`, `phase_name`, `verified` date), **Must-haves** table, **REQ mapping** subsection that quotes **D-51.03** mapping (`concept_code_hops` vs `/trace` vs `entity_trace`), **Evidence details (grep anchors)** listing representative `grep -n` / `rg -n` one-liners for `concept_code_hops` in `graphify/mcp_tool_registry.py`, `_run_concept_code_hops` / `_tool_concept_code_hops` in `graphify/serve.py`, `graphify/capability_tool_meta.yaml`, `docs/RELATIONS.md`, `docs/ARCHITECTURE.md`, and `graphify/skill.md` (`/trace` cross-reference per **47-02-03**), **Automated** section with fenced placeholders labeled “executor fills after Task 2”, **Gaps** section (minimum: `skill-excalidraw.md` omission rationale per **47-02-03** if still zero matches; any **47-02** partial per **D-51.04**), and note on slim `server.json` + **`python -m graphify capability --validate`** (not sole reliance on `grep concept_code_hops server.json`).
    - **D-51.04:** Checklist rows in the doc for **47-02** deliverables (`docs/RELATIONS.md`, `docs/ARCHITECTURE.md`, `server.json` `_meta`, skill enumerations) marked Met / Partial with file paths — partials stay under **Gaps**, not hidden.
    - Do **not** tick **REQUIREMENTS.md** in this task (deferred to Task 2 after automated proof).
  </acceptance_criteria>
  <files>.planning/phases/47-mcp-trace-integration/47-VERIFICATION.md</files>
  <action>
    Create **`47-VERIFICATION.md`** mirroring **`45-VERIFICATION.md`** structure. Populate static evidence from repo reads and grep anchors with real line references where possible. Embed **CCODE-03** / **CCODE-04** rows: cite **D-51.03** so auditors see that **CCODE-04** REQ wording is reconciled via **`concept_code_hops`** golden path + explicit separation of temporal **`entity_trace`** / **`/trace`**. Reference **`tests/test_concept_code_mcp.py`** (golden-path test name) in the REQ table. Leave **Automated** fenced blocks empty or clearly stubbed until Task 2 pastes transcripts (`pytest` slice, full suite, `capability --validate`, optional `python -c` tool registration assert from **51-RESEARCH**). Do not invent passing output.
  </action>
  <verify>
    <automated>test -f .planning/phases/47-mcp-trace-integration/47-VERIFICATION.md && grep -qE 'D-51.03|concept_code_hops|entity_trace' .planning/phases/47-mcp-trace-integration/47-VERIFICATION.md && grep -q 'CCODE-03' .planning/phases/47-mcp-trace-integration/47-VERIFICATION.md && grep -q 'CCODE-04' .planning/phases/47-mcp-trace-integration/47-VERIFICATION.md</automated>
  </verify>
  <done>**47-VERIFICATION.md** on disk with REQ mapping, grep anchor section, Gaps, and stubbed/partial Automated section ready for Task 2 transcripts.</done>
</task>

<task type="auto">
  <name>Task 2 — Full automated gate, transcripts, REQ ticks</name>
  <read_first>
    - CLAUDE.md
    - .planning/phases/47-mcp-trace-integration/47-VERIFICATION.md
    - .planning/REQUIREMENTS.md
  </read_first>
  <acceptance_criteria>
    - **`pip install -e ".[mcp,pdf,watch]"`** then **`python -m graphify capability --validate`** exit 0 from repo root. If validate fails, run **`python -m graphify capability --stdout > server.json`** (or **`python scripts/sync_mcp_server_json.py`** when PyPI/version bump applies per **CLAUDE.md**), re-validate, and **do not** tick REQs until validate passes (**D-51.04 / Claude discretion**).
    - Append to **`47-VERIFICATION.md` → Automated**: fenced outputs for at least `pytest tests/test_concept_code_mcp.py -q`, `pytest tests/test_concept_code_mcp.py::test_concept_code_hops_golden_path -q`, `pytest tests/ -q`, and `python -m graphify capability --validate`; include `git rev-parse --short HEAD` line for commit evidence.
    - Set **`47-VERIFICATION.md`** frontmatter **`status: passed`** only if all above exit 0 and no blocking gap remains undocumented.
    - In **`.planning/REQUIREMENTS.md`**, change **only** the **CCODE-03** and **CCODE-04** lines to checked format exactly: `- [x] **CCODE-03:**` and `- [x] **CCODE-04:**` (preserve remainder of each line’s description text).
  </acceptance_criteria>
  <files>.planning/phases/47-mcp-trace-integration/47-VERIFICATION.md, .planning/REQUIREMENTS.md, server.json</files>
  <action>
    Execute automated proof per **D-51.02** and **51-RESEARCH**. After green runs, paste real command transcripts into **`47-VERIFICATION.md`**. Then tick the two REQ rows. If **`server.json`** was regenerated, commit rationale aligns with **47-02-02**. Do not tick other requirements. Do not fake **CCODE-04** as satisfied by **`entity_trace`** alone — doc already states **D-51.03**.
  </action>
  <verify>
    <automated>pip install -e ".[mcp,pdf,watch]" && pytest tests/test_concept_code_mcp.py::test_concept_code_hops_golden_path -q && pytest tests/ -q && python -m graphify capability --validate && test "$(grep -E '^- \[x\] \*\*CCODE-0[34]:\*\*' .planning/REQUIREMENTS.md | wc -l | tr -d '[:space:]')" = "2"</automated>
  </verify>
  <done>Transcripts in **47-VERIFICATION.md**; **`capability --validate`** green; **`pytest tests/ -q`** green; exactly **two** checked CCODE lines in **REQUIREMENTS.md** per grep gate.</done>
</task>

<task type="auto">
  <name>Task 3 — Summaries, ROADMAP, validation Nyquist touch-up</name>
  <read_first>
    - .planning/ROADMAP.md
    - .planning/phases/51-v1.10-gap-mcp-trace-req-signoff/51-VALIDATION.md
    - .planning/phases/47-mcp-trace-integration/47-VALIDATION.md
    - $HOME/.cursor/get-shit-done/templates/summary.md
  </read_first>
  <acceptance_criteria>
    - **`51-01-SUMMARY.md`** exists under `.planning/phases/51-v1.10-gap-mcp-trace-req-signoff/` summarizing artifacts, commands run, commit SHA, and any residual **Gaps** pointers.
    - **`ROADMAP.md`** Phase **51** `<details>` block: **`Plans:`** line reads **`1 plan — [x] 51-01-PLAN.md — …`** (brief objective text; mirror Phase **50** wording style).
    - **`51-VALIDATION.md`**: set `nyquist_compliant: true`, `status` to completed/passed (choose one consistent with repo convention), update **Per-Task Verification Map** statuses for **51-01-01..03** to done.
    - **`47-VALIDATION.md`**: optional touch-up — set pending rows for **47-01**/**47-02** to passed if verification confirms, and `nyquist_compliant: true` if all mapped commands were executed in Task 2; otherwise leave a one-line **Note** under Gaps in **47-VERIFICATION.md** instead of lying.
  </acceptance_criteria>
  <files>.planning/phases/51-v1.10-gap-mcp-trace-req-signoff/51-01-SUMMARY.md, .planning/ROADMAP.md, .planning/phases/51-v1.10-gap-mcp-trace-req-signoff/51-VALIDATION.md, .planning/phases/47-mcp-trace-integration/47-VALIDATION.md</files>
  <action>
    Write **SUMMARY** for executor handoff. Patch **ROADMAP** Phase **51** only (Plans line + keep Goal/Depends/Requirements consistent). Align validation YAML with post-execution truth.
  </action>
  <verify>
    <automated>grep -q '51-01-PLAN.md' .planning/ROADMAP.md && test -f .planning/phases/51-v1.10-gap-mcp-trace-req-signoff/51-01-SUMMARY.md && grep -q 'nyquist_compliant: true' .planning/phases/51-v1.10-gap-mcp-trace-req-signoff/51-VALIDATION.md</automated>
  </verify>
  <done>ROADMAP shows **51-01** complete; **51-01-SUMMARY.md** present; **51-VALIDATION.md** reflects Nyquist compliance post-sign-off.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Planning artifacts ↔ repo | Markdown and REQUIREMENTS edits are trusted maintainer output; no new runtime input surface. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-51-01 | I | `.planning/REQUIREMENTS.md` REQ ticks | mitigate | Tick **only** after Task 2 automated gate; **47-VERIFICATION.md** holds transcripts; grep count = 2 for CCODE-03/04. |
| T-51-02 | I | `47-VERIFICATION.md` narrative | mitigate | **D-51.03** mapping stated explicitly; no claim that **`entity_trace`** satisfies typed **`implements`** hops. |
| T-51-03 | R | Stale `server.json` / false sign-off | mitigate | **`python -m graphify capability --validate`** mandatory; regen + re-validate if fail (**D-51.04**). |
| T-51-04 | E | Doc drift vs code | accept | Phase scope is verification-only; residual gaps listed under **Gaps** per **D-51.04**. |
</threat_model>

<verification>
After all tasks: **`pytest tests/ -q`** and **`python -m graphify capability --validate`** both succeeded during Task 2; **`grep -E '^- \[x\] \*\*CCODE-0[34]:\*\*' .planning/REQUIREMENTS.md | wc -l`** prints **2**; **`47-VERIFICATION.md`** contains **Automated** transcripts and **D-51.03** mapping.
</verification>

<success_criteria>
- **CCODE-03** and **CCODE-04** each have a verification row in **`47-VERIFICATION.md`** tied to concrete commands and grep anchors.
- **D-51.01–D-51.04** satisfied without implementing deferred items (**47-CONTEXT** rename / slash UX).
- ROADMAP Phase **51** shows **`[x] 51-01-PLAN.md`**.
</success_criteria>

<output>
After completion, ensure `.planning/phases/51-v1.10-gap-mcp-trace-req-signoff/51-01-SUMMARY.md` exists (per Task 3).
</output>

## Source coverage audit

| Source | Item | Covered by |
|--------|------|------------|
| GOAL (ROADMAP P51) | Close CCODE-03/04 via 47 + verification + manifest/skill alignment | Tasks 1–3 |
| REQ | **CCODE-03** | Task 1 table + Task 2 validate/tool evidence |
| REQ | **CCODE-04** | Task 1 **D-51.03** mapping + Task 2 golden-path pytest |
| RESEARCH | Slim manifest, capability validate, skill-excalidraw gap | Task 1 Gaps + Automated notes |
| CONTEXT | **D-51.01–D-51.04** | Task 1–2; **Deferred** (rename `entity_trace`) not in tasks |
