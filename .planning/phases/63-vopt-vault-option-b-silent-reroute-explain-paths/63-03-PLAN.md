---
phase: 63
plan: 03
type: tdd
wave: 2
depends_on: [63-01]
files_modified:
  - graphify/output.py
  - tests/test_output_path_matrix.py
  - tests/test_detect.py
  - .graphifyignore
autonomous: true
requirements: [VOPT-02]
nyquist_compliant: true
tags: [vault, breadcrumb, legacy-detection, ignore-rules]

must_haves:
  truths:
    - "When <vault>/graphify-out/ (legacy) coexists with Option B, breadcrumb appends a third '  hint: legacy graphify-out/ detected — run `graphify doctor` to review' line"
    - "When no legacy graphify-out/ exists, breadcrumb is exactly two lines (no third hint)"
    - "Detection is read-only — no move, no delete, no migration of graphify-out/ contents"
    - "graphify never self-ingests its own .graphify-out/ outputs on subsequent runs (ignore rule in place AND verified at the detect.py / file-discovery layer, not just by static-asset assertion — W7)"
    - "Routing-audit tests still pass unchanged (Success Criterion #4)"
    - "Acceptance criteria 1–4 from ROADMAP.md all hold after this plan lands"
  artifacts:
    - path: "graphify/output.py"
      provides: "Option B branch passes extra_hint to _emit_vault_info when legacy graphify-out/ detected at vault root"
      contains: "legacy graphify-out/ detected"
    - path: "tests/test_output_path_matrix.py"
      provides: "Legacy-detection test cases — third hint line appears when (vault/'graphify-out').is_dir(); two-line breadcrumb when absent (regression)"
      contains: "legacy"
    - path: "tests/test_detect.py"
      provides: "W7 behavior test test_graphify_out_ignored_in_vault_cwd — exercises real collect_files / discovery API to prove .graphify-out/ contents are excluded"
      contains: "test_graphify_out_ignored_in_vault_cwd"
    - path: ".graphifyignore"
      provides: "Project-level ignore rule preventing graphify from re-ingesting its own .graphify-out/ outputs (Pitfall 5 mitigation)"
      contains: ".graphify-out/"
  key_links:
    - from: "graphify/output.py::resolve_output (option-b branch)"
      to: "graphify/output.py::_emit_vault_info"
      via: "passes extra_hint=... when (cwd_resolved / 'graphify-out').is_dir()"
      pattern: "extra_hint="
    - from: ".graphifyignore"
      to: "graphify/detect.py corpus collection"
      via: "ignore rule consumed by file discovery"
      pattern: "\\.graphify-out/"
---

<objective>
Polish Option B by closing the third locked behavior of D-01 (legacy `graphify-out/` detection emits a third `hint:` line in the breadcrumb), add a project-level `.graphifyignore` rule preventing graphify from self-ingesting its own `.graphify-out/` outputs (RESEARCH.md Pitfall 5 / Assumption A4), and run a full regression sweep including routing-audit (Success Criterion #4).

Purpose: Completes VOPT-02 (the third hint line was deferred from Plan 01) and hardens Option B against the "graphify ingests its own outputs" footgun that becomes possible the moment outputs land inside the vault.

Output: Modified `graphify/output.py` (legacy detection in option-b branch), appended `tests/test_output_path_matrix.py` cases, new/updated root `.graphifyignore`.

Locked decisions implemented:
- D-01 third hint line: `"  hint: legacy graphify-out/ detected — run `graphify doctor` to review"`
- D-04 detect-only — no move, no delete, no migration
- Pitfall 5 / A4 — `.graphify-out/` ignore rule (deferred verification from Plan 01)

Depends on Plan 01 because the `_emit_vault_info(extra_hint=...)` parameter and the option-b branch must already exist.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/63-vopt-vault-option-b-silent-reroute-explain-paths/63-CONTEXT.md
@.planning/phases/63-vopt-vault-option-b-silent-reroute-explain-paths/63-RESEARCH.md
@.planning/phases/63-vopt-vault-option-b-silent-reroute-explain-paths/63-PATTERNS.md
@.planning/phases/63-vopt-vault-option-b-silent-reroute-explain-paths/63-VALIDATION.md
@.planning/phases/63-vopt-vault-option-b-silent-reroute-explain-paths/63-01-SUMMARY.md
@./CLAUDE.md
@graphify/output.py
@graphify/detect.py
@tests/test_output_path_matrix.py

<interfaces>
<!-- After Plan 01, _emit_vault_info already accepts extra_hint. -->

```python
def _emit_vault_info(msg: str, hint: str, *, extra_hint: str | None = None) -> None:
    print(f"[graphify] info: {msg}", file=sys.stderr)
    print(f"  hint: {hint}", file=sys.stderr)
    if extra_hint is not None:
        print(f"  hint: {extra_hint}", file=sys.stderr)
```

Plan 03 adds the caller-side computation (in `resolve_output`'s option-b branch):
```python
extra = None
if (cwd_resolved / "graphify-out").is_dir():
    extra = "legacy graphify-out/ detected — run `graphify doctor` to review"
_emit_vault_info(
    "vault CWD without .graphify/profile.yaml — Option B reroute active",
    f"outputs → {artifacts_dir}/",
    extra_hint=extra,
)
```

`.graphifyignore` lives at repo root. Existing format (per `graphify/detect.py`) is gitignore-style, one pattern per line. Read `graphify/detect.py` to confirm the parser handles patterns like `.graphify-out/`.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: RED+GREEN — third-hint-line legacy detection in Option B branch</name>
  <files>graphify/output.py, tests/test_output_path_matrix.py</files>
  <read_first>
    - graphify/output.py (option-b branch added in Plan 01 — confirm location and current call signature of `_emit_vault_info`)
    - tests/test_output_path_matrix.py (Plan 01's `_make_vault_no_profile` helper; the `test_option_b_breadcrumb_shape` line-count assertion that currently expects exactly two stderr lines — that test must remain valid for the no-legacy case)
  </read_first>
  <behavior>
    Append two new test cases to `tests/test_output_path_matrix.py`:

    - `test_option_b_legacy_dir_emits_third_hint(tmp_path, capsys)`:
        vault = _make_vault_no_profile(tmp_path)
        (vault / "graphify-out").mkdir()  # simulate legacy artifacts dir
        resolved = resolve_output(vault); err = capsys.readouterr().err
        assert resolved.source == "option-b"
        assert "[graphify] info: vault CWD without .graphify/profile.yaml — Option B reroute active" in err
        assert "outputs → " in err
        assert "  hint: legacy graphify-out/ detected — run `graphify doctor` to review" in err
        # Three stderr lines now (was two without legacy):
        non_empty_lines = [ln for ln in err.splitlines() if ln.strip()]
        assert len(non_empty_lines) == 3
        # Detect-only — legacy dir untouched:
        assert (vault / "graphify-out").is_dir()
        assert list((vault / "graphify-out").iterdir()) == []  # nothing written to it

    - `test_option_b_no_legacy_dir_two_line_breadcrumb(tmp_path, capsys)`:
        vault = _make_vault_no_profile(tmp_path)  # no graphify-out/ dir created
        resolve_output(vault); err = capsys.readouterr().err
        assert "legacy graphify-out/ detected" not in err
        non_empty_lines = [ln for ln in err.splitlines() if ln.strip()]
        assert len(non_empty_lines) == 2

    Run `pytest tests/test_output_path_matrix.py -k legacy -q` to confirm RED.

    GREEN implementation in `graphify/output.py`:
    - In the option-b branch added by Plan 01, BEFORE the `_emit_vault_info(...)` call, compute `extra = None; if (cwd_resolved / "graphify-out").is_dir(): extra = "legacy graphify-out/ detected — run `graphify doctor` to review"`. Pass `extra_hint=extra` to `_emit_vault_info`.

    Run `pytest tests/test_output_path_matrix.py -k option_b -q` and confirm all 8 Option B tests pass. Specifically verify `test_option_b_breadcrumb_shape` (Plan 01) still passes with line count == 2 (no legacy dir present in that test's fixture).
  </behavior>
  <action>
    1. Append both test functions to the END of `tests/test_output_path_matrix.py`.
    2. Run `pytest tests/test_output_path_matrix.py -k legacy -q` — RED expected.
    3. Commit: `test(63-03): RED — failing legacy graphify-out/ detection tests`.
    4. Edit the option-b branch in `graphify/output.py` to compute `extra` and pass `extra_hint=extra`.
    5. Run `pytest tests/test_output_path_matrix.py -k option_b -q`. All 8 must pass.
    6. Run the graphify watch rebuild snippet per CLAUDE.md.
    7. Commit: `feat(63-03): GREEN — legacy graphify-out/ detection emits third hint line (VOPT-02)`.
  </action>
  <verify>
    <automated>pytest tests/test_output_path_matrix.py -k option_b -q</automated>
  </verify>
  <acceptance_criteria>
    - `grep -n "legacy graphify-out/ detected" graphify/output.py` returns exactly one match
    - `grep -n "extra_hint=" graphify/output.py` returns >= 1 match
    - `pytest tests/test_output_path_matrix.py::test_option_b_legacy_dir_emits_third_hint -q` exits 0
    - `pytest tests/test_output_path_matrix.py::test_option_b_no_legacy_dir_two_line_breadcrumb -q` exits 0
    - `pytest tests/test_output_path_matrix.py::test_option_b_breadcrumb_shape -q` still exits 0 (Plan 01 regression)
    - Two new git commits visible: `test(63-03): RED ...` and `feat(63-03): GREEN ...`
  </acceptance_criteria>
  <done>Three-line breadcrumb fires only when legacy graphify-out/ is present; two-line shape preserved otherwise; legacy dir contents untouched (detect-only).</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Add .graphify-out/ to .graphifyignore + final regression sweep</name>
  <files>.graphifyignore, tests/test_output_path_matrix.py, tests/test_detect.py</files>
  <read_first>
    - graphify/detect.py (locate the `.graphifyignore` parser and confirm pattern syntax — gitignore-style line-per-pattern)
    - graphify/security.py (confirm path-confinement rules accept `.graphify-out/` — should be no-op since outputs land inside the user's CWD/vault, but verify nothing prefix-matches `.graphify-` and rejects)
    - .graphifyignore (current root file if it exists; if not, this task creates it)
  </read_first>
  <action>
    1. Verify whether a root `.graphifyignore` exists: `ls -la .graphifyignore 2>&1`. If absent, create it with at minimum:
       ```
       # graphify project-level ignores
       .graphify-out/
       graphify-out/
       ```
       If present, append `.graphify-out/` (and `graphify-out/` if not already there) preserving existing content. Place additions under a `# Phase 63 (VOPT-02): self-ingest prevention` comment for traceability.
    2. Audit RESEARCH Pitfall 5 / A4: grep `graphify/detect.py` and `graphify/security.py` for any prefix-match on `.graphify` that could mishandle `.graphify-out/`:
       ```
       grep -n "\.graphify" graphify/detect.py graphify/security.py
       ```
       If a prefix-match (e.g., `name.startswith(".graphify")`) is found that would over-match, document the finding in the SUMMARY and consider tightening to exact match `.graphify` or `.graphify/` — only if the audit reveals a real issue. Otherwise, A4 is confirmed clean.
    3. Add a regression test to `tests/test_output_path_matrix.py`:
       - `test_option_b_graphify_out_in_ignore_default(tmp_path)`: verify `.graphify-out/` and `graphify-out/` both appear in the project-shipped `.graphifyignore`. Read the repo-root `.graphifyignore` (use `Path(__file__).parent.parent / ".graphifyignore"`), assert `".graphify-out/" in text` and `"graphify-out/" in text`. (Static-asset test, kept minimal.)
    3b. **W7 behavior test (new) — `tests/test_detect.py::test_graphify_out_ignored_in_vault_cwd`:**
       Read `graphify/detect.py` to learn the actual file-discovery API name (likely `collect_files(root: Path, ...) -> list[Path]` or similar) and how `.graphifyignore` parsing is wired in. Then add this test to `tests/test_detect.py` (create the file if it does not exist, mirroring patterns in nearby `tests/test_*.py`):
       ```python
       def test_graphify_out_ignored_in_vault_cwd(tmp_path):
           # Build a vault fixture that contains .graphify-out/ with files
           vault = tmp_path / "v"
           vault.mkdir()
           (vault / ".obsidian").mkdir()
           # Create a real input file we expect to be discovered
           (vault / "doc.md").write_text("# real input")
           # Create .graphify-out/ with a generated note we MUST NOT re-ingest
           graphify_out = vault / ".graphify-out"
           (graphify_out / "obsidian").mkdir(parents=True)
           emitted = graphify_out / "obsidian" / "Generated.md"
           emitted.write_text("# emitted by graphify — do not re-ingest")
           # Drop the project .graphifyignore content into the vault root so the
           # vault inherits the default ignore rule (collect_files reads from CWD).
           (vault / ".graphifyignore").write_text(".graphify-out/\ngraphify-out/\n")
           # Exercise the real discovery API
           from graphify.detect import collect_files  # adjust if API is named differently
           found = list(collect_files(vault))  # adapt args/kwargs as required
           found_strs = [str(p) for p in found]
           # Real input is discovered:
           assert any(s.endswith("doc.md") for s in found_strs)
           # Self-emitted output is NOT discovered:
           assert not any(".graphify-out" in s for s in found_strs), \
               f"Self-ingest leak: {found_strs}"
       ```
       If `collect_files` requires extra arguments (extensions list, exclude_globs, etc.), match the signature exactly. If the discovery API is split across functions, exercise the public composite that the CLI actually uses. The test must call the REAL ignore-aware code path — not a re-implementation.
    4. Final regression sweep: `pytest tests/ -q`. Must be fully green. If any test outside the Phase 63 surface fails, investigate before commit.
    5. Manual acceptance walk-through of ROADMAP success criteria 1–4 (read SUMMARY; confirm each holds):
       - (1) Vault CWD with `.obsidian/` and no profile → outputs under `<vault>/.graphify-out/` ✓ (Plan 01 + Plan 03)
       - (2) Exactly one two-line breadcrumb (or three lines with legacy hint) ✓ (Plan 01 + this plan)
       - (3) `--explain-paths` prints table, exits 0, no pipeline ✓ (Plan 02)
       - (4) Non-vault CWDs continue to use `default_graphify_artifacts_dir()` — `pytest tests/test_routing_audit.py tests/test_output.py -q` clean ✓
    6. Run the graphify watch rebuild snippet per CLAUDE.md.
    7. Commit: `chore(63-03): add .graphify-out/ to .graphifyignore + final Phase 63 regression sweep`.
  </action>
  <verify>
    <automated>pytest tests/ -q</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "\.graphify-out/" .graphifyignore` returns >= 1
    - `grep -c "graphify-out/" .graphifyignore` returns >= 1 (covers both legacy and new)
    - `pytest tests/test_output_path_matrix.py::test_option_b_graphify_out_in_ignore_default -q` exits 0
    - `pytest tests/test_detect.py::test_graphify_out_ignored_in_vault_cwd -q` exits 0 (W7 behavior verification — real discovery API excludes .graphify-out/)
    - `grep -c "test_graphify_out_ignored_in_vault_cwd" tests/test_detect.py` returns 1
    - `pytest tests/test_routing_audit.py -q` exits 0 (Success Criterion #4 regression canary)
    - `pytest tests/test_output.py -q` exits 0 (default_graphify_artifacts_dir regression canary)
    - `pytest tests/ -q` exits 0 (full suite)
    - Git log shows commit subject starting with `chore(63-03)`
    - SUMMARY.md (created at output step below) documents the result of the `.graphify` prefix audit (clean / tightened / N/A)
  </acceptance_criteria>
  <done>.graphifyignore contains both ignore lines; full pytest suite green; ROADMAP acceptance criteria 1–4 manually verified.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| `<vault>/.graphify-out/` (own outputs) → next graphify run's corpus | Without an ignore rule, graphify would re-ingest its own emitted notes/JSON into the next graph build, polluting analysis. |
| Legacy `<vault>/graphify-out/` (pre-v1.13 artifacts) | Read-only detection only; no migration, no delete (D-04). |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-63-09 | Tampering / DoS | graphify self-ingests `.graphify-out/` causing recursive corpus growth | mitigate | `.graphifyignore` excludes `.graphify-out/` and `graphify-out/` at the project default level; `graphify/detect.py` parser already handles this pattern. Test `test_option_b_graphify_out_in_ignore_default` locks the rule in place. |
| T-63-10 | Information Disclosure | Third hint line surfaces existence of legacy `graphify-out/` to stderr | accept | Path is in user's own vault; no privilege boundary. The hint is the user-visible signal that motivates running `graphify doctor`. |
| T-63-11 | Tampering | Phase 63 modifies legacy `graphify-out/` contents | mitigate by design | D-04 explicit: detect-only. Test `test_option_b_legacy_dir_emits_third_hint` asserts `(vault / "graphify-out").is_dir()` and `list(...iterdir()) == []` after `resolve_output` runs (legacy dir present and untouched). |
| T-63-12 | Spoofing | Attacker creates `.graphifyignore` with `*` to hide all files from graphify | accept | Single-user CLI; user controls own ignore file. Out of threat scope. |
</threat_model>

<verification>
- `pytest tests/test_output_path_matrix.py -k option_b -q` — all 8 Option B tests pass (6 from Plan 01 + 2 from this plan + 1 ignore static-asset test)
- `pytest tests/ -q` — full suite green
- `grep -n "legacy graphify-out/ detected" graphify/output.py` — exactly one match
- `grep -n ".graphify-out/" .graphifyignore` — >= 1 match
- All four ROADMAP acceptance criteria (1–4) manually walked and confirmed in SUMMARY
</verification>

<success_criteria>
- VOPT-02 fully closed: third hint line emits when legacy `graphify-out/` is present at vault root, no false-positives when absent.
- Self-ingest loop prevented via `.graphifyignore` defaults.
- Phase 63 ROADMAP acceptance criteria 1–4 all hold; full pytest suite green; routing-audit unaffected.
</success_criteria>

<output>
After completion, create `.planning/phases/63-vopt-vault-option-b-silent-reroute-explain-paths/63-03-SUMMARY.md`. Include:
- Result of the `.graphify` prefix audit in detect.py / security.py (clean / tightened / N/A)
- Final acceptance walk-through of ROADMAP criteria 1–4
- Pointer to Phase 64 (AUDIT-A) for the upcoming stderr snapshot lock that will need to widen its valid-prefix list from `error:` to `error: | info:`.
</output>
