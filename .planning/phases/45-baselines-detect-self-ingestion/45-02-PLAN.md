---
phase: 45
plan: "02"
type: execute
wave: 2
depends_on:
  - "01"
files_modified:
  - graphify/profile.py
  - graphify/detect.py
  - graphify/doctor.py
  - graphify/__main__.py
autonomous: true
requirements:
  - HYG-01
must_haves:
  truths:
    - "Default merged profile includes optional `corpus.dot_graphify` schema keys validated without breaking profiles that omit them."
    - "`profile.yaml` and declared config filenames under `.graphify/` are never returned as ingestible documents from `detect()` on vault-shaped corpuses."
    - "Operator can persist discovered `.graphify/` corpus paths via documented CLI/doctor flag (non-interactive default off)."
    - "`pytest tests/test_profile*.py tests/test_detect.py -q` exits 0 (or full subset specified in tasks)."
---

<objective>
Deliver **D-45.04–D-45.06**: profile-driven **`.graphify/`** corpus include/exclude; hard block YAML profile/config blobs from ingestion; optional persistence workflow for discovered eligible paths under `.graphify/`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<threat_model>
| Threat | Mitigation |
|--------|------------|
| Writing attacker-controlled paths into profile | Only persist repo-relative normalized paths validated by **`validate_vault_path`** / existing profile path guards |
| YAML injection in discovered list | Store as YAML string list with literal scanning only — no **`yaml.load`** of untrusted content |
</threat_model>

<context>
@graphify/profile.py
@graphify/detect.py
@graphify/doctor.py
@graphify/security.py (path validators referenced by profile export)
@.planning/phases/45-baselines-detect-self-ingestion/45-CONTEXT.md
</context>

<tasks>

<task type="execute">
  <name>Task 45-02-01 — Schema: corpus.dot_graphify in profile defaults + validation</name>
  <read_first>
    - graphify/profile.py (`_DEFAULT_PROFILE`, validation functions for mapping/output blocks)
    - tests/test_profile*.py (nearest schema validation tests)
  </read_first>
  <acceptance_criteria>
    - `_DEFAULT_PROFILE` contains key path **`corpus`** → **`dot_graphify`** dict with keys: **`include_globs`** (list str, default `[]`), **`exclude_globs`** (list str, default `["**/*.yaml", "**/profile.yaml"]` or equivalent), **`auto_track_discoveries`** (bool default `False`), **`tracked_paths`** (list str default `[]`).
    - Invalid types in user profile produce validation error strings containing **`dot_graphify`** when malformed.
    - `pytest tests/test_profile_composition.py -q` (or narrowest passing file) exits 0.
  </acceptance_criteria>
  <action>
    Extend **`graphify/profile.py`** defaults and preflight/schema validation so composed profiles accept optional **`corpus.dot_graphify`** block.

    Minimum exclude set must always treat **`profile.yaml`** as non-corpus (even if user empties **`exclude_globs`**, enforce hard denylist in **`detect`** separately per D-45.05).

    Add unit test in **`tests/test_profile.py`** or **`tests/test_profile_composition.py`**: merge profile with **`corpus.dot_graphify.include_globs: ["**/*.md"]`** validates.
  </action>
</task>

<task type="execute">
  <name>Task 45-02-02 — detect() applies .graphify/ policy + hard YAML denylist</name>
  <read_first>
    - graphify/detect.py (`detect`, `classify_file`, profile loading hooks if any — may need `load_profile` from CLI only; detect may stay profile-agnostic)
    - graphify/__main__.py (`run_corpus` calls `detect(target, resolved=resolved)` — profile available via **`load_profile`** pattern from **`run`**)
  </read_first>
  <acceptance_criteria>
    - **`detect`** gains optional **`profile: dict | None = None`** OR **`dot_graphify_cfg: dict | None`** kwarg documented — when **`None`**, behavior matches pre-phase for non-vault runs.
    - Test: **`tmp_path/.graphify/profile.yaml`** exists + **`tmp_path/.graphify/notes.md`**; **`detect(tmp_path, profile=<merged>)`** includes **`notes.md`** as document only when **`include_globs`** matches; never includes **`profile.yaml`** as document.
    - `pytest tests/test_detect.py -q` exits 0.
  </acceptance_criteria>
  <action>
    Thread **`load_profile`** result from **`run_corpus`** / **`main`** into **`detect(..., profile=merged_profile)`** only when **`resolved.vault_detected`** or **`.graphify/profile.yaml`** exists under **`target`** — exact condition mirrors vault adoption rules in **`__main__.py`** (executor reads **`run_corpus`** callsites).

    Implement filtering **before** **`classify_file`**: skip paths under **`.graphify/`** unless matched by **`include_globs`** AND not matched by **`exclude_globs`** AND not hard-deny **`profile.yaml`** / **`*.yaml`** per D-45.05 minimum list (**executor**: enumerate deny suffixes **`{".yaml", ".yml"}`** for files directly under `.graphify/` except documented opt-in impossible — **simplest**: never ingest any **`*.yaml`** under `.graphify/`).

    Use **`fnmatch`** against posix relative paths consistent with **`_is_ignored`** style.
  </action>
</task>

<task type="execute">
  <name>Task 45-02-03 — Persist discoveries via doctor or CLI flag</name>
  <read_first>
    - graphify/doctor.py (`run_doctor`, argparse wiring in `__main__.py`)
    - graphify/profile.py (atomic profile write helpers if exist — else follow vault-promote patterns)
  </read_first>
  <acceptance_criteria>
    - New flag documented in **`graphify doctor --help`** OR **`graphify --help`** subcommand table e.g. **`doctor --apply-dot-graphify-track`** (exact name Claude discretion).
    - Dry-run prints unified diff of paths that **would** append to **`tracked_paths`** without writing; **`--apply`** writes YAML merging tracked paths sorted unique.
    - Test uses **`tmp_path`** vault with `.graphify/profile.yaml` minimal stub — exits 0.
  </acceptance_criteria>
  <action>
    Implement minimal UX for **D-45.06**: after **`detect()`**, collect eligible `.graphify/` relative paths discovered (that passed include rules). **`doctor`** mode compares to **`corpus.dot_graphify.tracked_paths`** and proposes additions.

    **`auto_track_discoveries: true`** in profile skips prompt and appends on **`run`** end — gated behind **`resolved`** vault runs only; document stderr once when auto-appended.

    Keep **`graphify/profile.py`** merge preserving field order where possible using existing merge machinery.
  </action>
</task>

</tasks>
