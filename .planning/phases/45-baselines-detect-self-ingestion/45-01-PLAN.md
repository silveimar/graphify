---
phase: 45
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified:
  - graphify/detect.py
  - graphify/corpus_prune.py
  - graphify/extract.py
autonomous: true
requirements:
  - HYG-01
  - HYG-03
must_haves:
  truths:
    - "`prior_files` is populated from `resolved.artifacts_dir` when resolved is set AND from `root / \"graphify-out\"` when resolved is None and that manifest exists (D-45.02)."
    - "When `skipped[\"manifest\"]` is non-empty after `detect()`, stderr prints exactly one line containing `[graphify]` and `manifest` and `skipped` count ≥ 1 (D-45.01)."
    - "`collect_files(..., resolved=...)` applies the same directory pruning rules as `detect()` for nested output + ignore patterns (parity — D-45.08)."
    - "`pytest tests/test_detect.py tests/test_extract.py -q` exits 0."
  artifacts:
    - path: "graphify/corpus_prune.py"
      provides: "Shared helpers for manifest prior_files + dirname prune decisions reused by detect/extract"
      contains: "prior_files"
---

<objective>
Implement **D-45.01**, **D-45.02**, **D-45.03**, **D-45.08**: centralize corpus pruning + manifest loading; stderr summary for manifest skips; default-root manifest when `resolved is None`; thread **`resolved`** into **`collect_files`** so extraction cannot diverge from **`detect()`** for equivalent roots.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<threat_model>
| Threat | Mitigation |
|--------|------------|
| Path traversal via manifest entries | Only skip paths whose **`resolve()`** equals entries in **`prior_files`** set built from trusted manifest JSON under graphify-out |
| Symlink escape | Reuse existing **`detect`** symlink cycle guards; no new follows outside scan roots |
</threat_model>

<context>
@.planning/phases/45-baselines-detect-self-ingestion/45-CONTEXT.md
@graphify/detect.py
@graphify/extract.py
@CLAUDE.md
</context>

<tasks>

<task type="execute">
  <name>Task 45-01-01 — Add graphify/corpus_prune.py shared helpers</name>
  <read_first>
    - graphify/detect.py (functions `_load_output_manifest`, `_is_nested_output`, `_is_ignored`, `_load_graphifyignore`, `detect` walk loop ~511–536)
    - graphify/output.py (`ResolvedOutput` fields `artifacts_dir`, `exclude_globs`)
    - .planning/phases/45-baselines-detect-self-ingestion/45-CONTEXT.md (decisions D-45.01–03, D-45.08)
  </read_first>
  <acceptance_criteria>
    - `graphify/corpus_prune.py` exists and defines `build_prior_files(root: Path, resolved: ResolvedOutput | None) -> set[str]` that returns union of manifest file paths from (a) `resolved.artifacts_dir` when `resolved is not None`, (b) `root / "graphify-out"` when `resolved is None` and manifest exists.
    - Same module exposes `should_prune_dirname(d: str, dp: Path, root: Path, *, resolved_basenames: frozenset[str], patterns: list[str]) -> bool` OR equivalent documented API used by both detect and collect_files walk loops — pruning outcome matches previous `_is_nested_output` ∪ noise ∪ `_is_ignored(dp/d, root, patterns)` behavior for directories.
    - `python -c "from graphify import corpus_prune"` or `from graphify.corpus_prune import build_prior_files` succeeds (package exports optional — may use detect-only import path if __init__ unchanged).
  </acceptance_criteria>
  <action>
    Create **`graphify/corpus_prune.py`** with **`from __future__ import annotations`**, typing, and helpers that wrap **`_load_output_manifest`** logic WITHOUT duplicating JSON parsing — either import **`_load_output_manifest`** from **`detect`** (risk circular import: detect imports corpus_prune imports detect — **avoid**) OR **move** **`_load_output_manifest`** / **`_OUTPUT_MANIFEST_*`** constants into **`corpus_prune.py`** and re-export from **`detect.py`** via thin wrappers.

    Preferred pattern to avoid cycles: move **`_OUTPUT_MANIFEST_NAME`**, **`_OUTPUT_MANIFEST_VERSION`**, **`_load_output_manifest`** into **`corpus_prune.py`**; **`detect.py`** imports them.

    Implement **`build_prior_files(root, resolved)`**:
    - `prior: set[str] = {}`
    - If **`resolved is not None`**: load **`_load_output_manifest(resolved.artifacts_dir)`**, extend **`prior`** with **`str(Path(f).resolve())`** for each **`f`** in each run's **`files`** list (same normalization as today).
    - If **`resolved is None`**: if **`(root / "graphify-out").is_dir()`**, load manifest from **`root / "graphify-out"`** same as above.

    Implement dirname prune helper mirroring lines 525–535 in **`detect`**: dot prefix, **`_is_noise_dir`** (import or duplicate minimal set — prefer import from **`detect`** as **`from graphify.detect import _is_noise_dir, _is_nested_output`** only if no cycle; if cycle, duplicate **`_SELF_OUTPUT_DIRS`** check in **`corpus_prune`** using a shared frozenset constant in **`corpus_prune`** copied from **`detect`** comment "keep in sync").

    Document in module docstring: **memory/** allow-list stays in **`detect`** (second scan root), not in this helper.
  </action>
</task>

<task type="execute">
  <name>Task 45-01-02 — Refactor detect() to use corpus_prune + manifest stderr summary</name>
  <read_first>
    - graphify/detect.py (full `detect` after edits from 45-01-01)
    - graphify/corpus_prune.py
  </read_first>
  <acceptance_criteria>
    - `grep -n 'prior_files' graphify/detect.py` shows assignment via `build_prior_files(root, resolved)` (or equivalent single call).
    - After `detect()` returns in unit test with synthetic manifest skip, captured stderr contains substring `[graphify]` AND `manifest` when at least one file was manifest-skipped (use `capsys` in **`tests/test_detect.py`**).
    - `pytest tests/test_detect.py -q` exits 0.
  </acceptance_criteria>
  <action>
    Replace inline **`prior_files`** construction (lines ~493–500) with **`build_prior_files(root, resolved)`**.

    After processing all files (before **`return`** dict), if **`len(skipped["manifest"]) > 0`**, **`print`** to **`stderr`** one line:
    `"[graphify] skipped {n} prior-output file(s) per output-manifest.json — see graphify doctor --dry-run for detail"` (exact wording adjustable but must include **`[graphify]`**, count **`n`**, and **`manifest`** substring).

    Preserve **`skipped["manifest"]`** population behavior — still silent per-file (no spam).

    Ensure **D-45.03**: any resolved path in **`prior_files`** continues to skip after profile moves — already **`str(p.resolve()) in prior_files`**; document in comment near loop if needed.
  </action>
</task>

<task type="execute">
  <name>Task 45-01-03 — collect_files parity + resolved parameter</name>
  <read_first>
    - graphify/extract.py (`collect_files` ~2858–2901)
    - graphify/detect.py (`CODE_EXTENSIONS` if needed)
    - graphify/corpus_prune.py
  </read_first>
  <acceptance_criteria>
    - `collect_files` signature includes optional **`resolved: ResolvedOutput | None = None`** (use **`TYPE_CHECKING`** import pattern from **`detect`**).
    - New test in **`tests/test_extract.py`**: under **`tmp_path`**, create **`nested/graphify-out/x.py`** and **`src/y.py`**; call **`collect_files(tmp_path, resolved=None)`**; assert no path contains **`graphify-out`** segment; assert **`y.py`** collected.
    - `pytest tests/test_extract.py -q` exits 0.
  </acceptance_criteria>
  <action>
    Refactor **`collect_files`** to use **`os.walk`** (or keep **`rglob`** only if parity proven — **preferred: os.walk** mirroring **`detect`** skip logic) with **`corpus_prune.should_prune_dirname`** / same pruning as **`detect`** for directories.

    Load **`patterns = _load_graphifyignore(root) + (resolved.exclude_globs if resolved else [])`**.

    Apply extension filter unchanged at file level.

    Default **`resolved=None`** preserves backward-compatible behavior for CLI **`python -m graphify.extract`**.
  </action>
</task>

</tasks>
