---
phase: 45
plan: "03"
type: execute
wave: 3
depends_on:
  - "02"
files_modified:
  - tests/test_detect.py
  - tests/test_extract.py
  - tests/fixtures/phase45-mini-vault/.gitkeep
  - .planning/REQUIREMENTS.md
  - .planning/ROADMAP.md
autonomous: true
requirements:
  - HYG-02
  - HYG-03
must_haves:
  truths:
    - "`test_detect_skips_dotfiles` asserts pathlib hidden-part invariant AND documented posix string form for serialized paths returned by `detect()` (D-45.07)."
    - "Committed fixture directory `tests/fixtures/phase45-mini-vault/` contains `.obsidian/`, `.graphify/profile.yaml`, and sample markdown/code files — one integration test loads it."
    - "`.planning/REQUIREMENTS.md` lists new REQ rows OR expands HYG-* prose if planner chose formal trace IDs for expanded `.graphify` scope."
    - "`pytest tests/ -q` exits 0."
---

<objective>
Close **HYG-02**, **HYG-03**, **D-45.07**, **D-45.09**: dual path contracts in tests; golden mini-vault fixture; traceability strings updated; lightweight operator note if missing.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<threat_model>
| Threat | Mitigation |
|--------|------------|
| Fixture vault leaks secrets | Fixture contains only synthetic markdown + empty `.obsidian` marker files |
</threat_model>

<context>
@tests/test_detect.py
@tests/test_extract.py
@.planning/REQUIREMENTS.md
@.planning/ROADMAP.md (Phase 45 success criteria alignment)
</context>

<tasks>

<task type="execute">
  <name>Task 45-03-01 — Dual assertions for dot-path contract (HYG-02)</name>
  <read_first>
    - tests/test_detect.py (`test_detect_skips_dotfiles`, fixtures layout)
    - graphify/detect.py (`detect` return path shape — posix `/` separators)
  </read_first>
  <acceptance_criteria>
    - `test_detect_skips_dotfiles` includes helper assertion or comments referencing **`Path.parts`** rule: no part.startswith(\".\") except allowed documented cases.
    - Same test retains legacy string check documenting serialized GRAPH paths use **`/`** and must not contain **`/.`** substring **OR** replaces with equivalent strengthened rule documented in comment citing D-45.07.
    - `pytest tests/test_detect.py::test_detect_skips_dotfiles -q` exits 0.
  </acceptance_criteria>
  <action>
    Refactor **`test_detect_skips_dotfiles`** to iterate **`result[\"files\"]`** values and for each path call **`Path(p).parts`** assertion **`not any(part.startswith(\".\") for part in Path(p).parts)`** AND continue to validate documented posix string invariant chosen in CONTEXT.

    If **`FIXTURES`** subtree breaks assumption due to **`__pycache__`** — scope test to **`tmp_path`** tree instead while preserving original intent (**executor decides** minimal churn).
  </action>
</task>

<task type="execute">
  <name>Task 45-03-02 — Golden mini-vault fixture + smoke test</name>
  <read_first>
    - tests/conftest.py if exists
    - CLAUDE.md testing conventions
  </read_first>
  <acceptance_criteria>
    - Directory **`tests/fixtures/phase45-mini-vault/`** exists in git with **`README.md`** describing purpose (≤15 lines).
    - Test **`test_phase45_mini_vault_detect_smoke`** (name adjustable) runs **`detect(FIXTURES_VAULT)`** with **`follow_symlinks=False`** and asserts **`total_files >= 1`** AND no path contains **`graphify-out`** substring unless fixture intentionally documents exception.
    - `pytest tests/test_detect.py -q` exits 0.
  </acceptance_criteria>
  <action>
    Create minimal vault layout:
    - **`tests/fixtures/phase45-mini-vault/.obsidian/workspace.json`** or empty **`app.json`** marker (minimal file Obsidian recognizes — **executor**: smallest valid stub).
    - **`.graphify/profile.yaml`** minimal valid stub merging with defaults (**executor** copy pattern from **`tests/test_vault_promote.py`**).
    - **`notes/hello.md`** + **`src/hi.py`** synthetic files.

    Add pytest referencing **`FIXTURES_PHASE45`** **`Path`** constant near top of **`tests/test_detect.py`** or dedicated **`tests/test_phase45_fixture.py`** if cleaner.
  </action>
</task>

<task type="execute">
  <name>Task 45-03-03 — REQUIREMENTS + ROADMAP alignment for expanded scope</name>
  <read_first>
    - .planning/REQUIREMENTS.md (HYG rows)
    - .planning/ROADMAP.md Phase 45 `<details>` block
    - .planning/phases/45-baselines-detect-self-ingestion/45-CONTEXT.md expanded scope paragraph
  </read_first>
  <acceptance_criteria>
    - `grep -n 'HYG-' .planning/REQUIREMENTS.md` shows updated trace lines OR new **`HYG-04`** rows linking `.graphify` corpus policy + doctor persistence — at least one edit committed.
    - ROADMAP Phase 45 summary mentions `.graphify/` ingestion policy + manifest stderr — **≥2 lines** edited inside Phase 45 `<details>` block.
    - `pytest tests/ -q` exits 0 (documentation-only task still runs suite unchanged).
  </acceptance_criteria>
  <action>
    Amend **`REQUIREMENTS.md`** checklist and traceability table: either extend **`HYG-01`** description with manifest-default + dot-graphify bullets OR add **`HYG-04`**/**`HYG-05`** for profile ingestion persistence — **executor chooses** smallest clear mapping.

    Sync **`ROADMAP.md`** Phase 45 goal/success bullets so shipped wording matches CONTEXT expanded scope (still single phase — no split).

    Do **not** archive milestones files unless instructed.
  </action>
</task>

</tasks>
