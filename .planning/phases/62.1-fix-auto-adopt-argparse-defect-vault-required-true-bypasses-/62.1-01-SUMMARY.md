---
phase: 62.1-fix-auto-adopt-argparse-defect
plan: 01
subsystem: cli/vault-cwd
tags: [vault-cwd, argparse, auto-adopt, bugfix, tdd]
dependency_graph:
  requires: [62-02]
  provides: [VCWD-02-auto-adopt-argparse-fix]
  affects: [graphify/__main__.py, tests/test_vault_cwd.py]
tech_stack:
  added: []
  patterns: [required=False + post-parse guard, friendly-error branch]
key_files:
  modified:
    - graphify/__main__.py
    - tests/test_vault_cwd.py
decisions:
  - "No helper function extracted for auto-adopt guard — two-call duplication does not justify a helper; NOTE comment added"
metrics:
  duration: ~15min
  completed: 2026-05-04
---

# Phase 62.1 Plan 01: Fix auto-adopt argparse-required defect — SUMMARY

**One-liner:** Flip `--vault required=False` on update-vault + vault-promote subparsers and add friendly-error else branch so auto-adopt CWD fills opts.vault before any `TypeError`.

## Tasks Completed

| Task | Name | Commit | Files |
|------|------|--------|-------|
| 1 | RED — unskip regression tests | 5ee8d0d | tests/test_vault_cwd.py |
| 2 | GREEN — flip required=False + friendly-error guard | 56636ce | graphify/__main__.py |
| 3 | LOCK — friendly-error branch tests | cfe3884 | tests/test_vault_cwd.py |
| 4 | REFACTOR decision (no code change) | — | SUMMARY only |

## Lines Changed

### graphify/__main__.py — update-vault subparser (~line 3312)

**Before:**
```python
_p_uv.add_argument("--" "vault", required=True, help="Target Obsidian vault path")
```

**After:**
```python
_p_uv.add_argument("--" "vault", required=False, default=None, help="Target Obsidian vault path (omit when running from a vault CWD with auto-adopt)")
```

### graphify/__main__.py — update-vault post-parse guard (~lines 3323-3325)

**Before:**
```python
_uv_vault = opts.vault
if gate == "auto-adopt" and not _uv_vault:
    _uv_vault = str(Path.cwd())
```

**After:**
```python
# NOTE: post-parse auto-adopt guard duplicated in vault-promote (~30 lines below). Two-call duplication does not justify a helper yet.
_uv_vault = opts.vault
if gate == "auto-adopt" and not _uv_vault:
    _uv_vault = str(Path.cwd())
elif not _uv_vault:
    from graphify.output import EXIT_VAULT_REFUSAL
    print("error: --vault is required (omit only when running from a vault CWD with auto-adopt)", file=sys.stderr)
    sys.exit(EXIT_VAULT_REFUSAL)
```

### graphify/__main__.py — vault-promote subparser (~line 3358)

**Before:**
```python
_p_vp.add_argument("--vault", required=True, help="Path to the target Obsidian vault")
```

**After:**
```python
_p_vp.add_argument("--vault", required=False, default=None, help="Path to the target Obsidian vault (omit when running from a vault CWD with auto-adopt)")
```

### graphify/__main__.py — vault-promote post-parse guard (~lines 3371-3373)

**Before:**
```python
_vp_vault = opts.vault
if gate == "auto-adopt" and not _vp_vault:
    _vp_vault = str(Path.cwd())
```

**After:**
```python
_vp_vault = opts.vault
if gate == "auto-adopt" and not _vp_vault:
    _vp_vault = str(Path.cwd())
elif not _vp_vault:
    from graphify.output import EXIT_VAULT_REFUSAL
    print("error: --vault is required (omit only when running from a vault CWD with auto-adopt)", file=sys.stderr)
    sys.exit(EXIT_VAULT_REFUSAL)
```

## Test Deltas

- **2 tests unskipped (now GREEN):** `test_update_vault_auto_adopt_no_vault_flag`, `test_vault_promote_auto_adopt_no_vault_flag`
- **2 tests added (LOCK):** `test_update_vault_no_vault_flag_outside_vault_friendly_error`, `test_vault_promote_no_vault_flag_outside_vault_friendly_error`
- Total: 20 tests in tests/test_vault_cwd.py (was 16 active before)

## Refactor Decision (Task 4)

**Decision: no helper extracted.** The guard block is 5 lines per command and only appears in exactly 2 places. A helper would save ~3 lines per call site but add indirection for minimal benefit. If a third command acquires `--vault required=False` auto-adopt semantics, extract then. Decision documented via NOTE comment above the update-vault guard block.

## Deviations from Plan

**1. [Rule 3 - Blocking] Editable install pointed at main repo, not worktree**

- **Found during:** Task 2 (GREEN verification)
- **Issue:** `pip install -e` was pointed at `/Users/silveimar/Documents/silogia-repos/graphify` (main repo); subprocess tests (`sys.executable -m graphify`) loaded the unmodified main repo code, so GREEN tests still failed despite worktree edits.
- **Fix:** Reinstalled editable package from worktree path (`pip install -e <worktree>`), then confirmed tests passed.
- **Files modified:** none (pip metadata only)

## Other Gated Branches — Untouched

Per the debug artifact, exactly 2 of 15 gated dispatch branches were defective. All 13 others (hand-rolled parsers without `--vault required=True`) were confirmed unaffected and not modified.

## TDD Gate Compliance

- RED gate: commit `5ee8d0d` (`test(62.1-01): RED ...`)
- GREEN gate: commit `56636ce` (`fix(62.1-01): GREEN ...`)
- LOCK commit: `cfe3884` (`test(62.1-01): lock ...`)

## Self-Check: PASSED
