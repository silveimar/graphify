---
phase: 39-tacit-to-explicit-onboarding-elicitation
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - graphify/elicit.py
  - tests/test_elicit.py
autonomous: true
requirements:
  - ELIC-01
  - ELIC-02
  - ELIC-04
  - ELIC-05
  - ELIC-06
must_haves:
  truths:
    - "Scripted interview states exist for rhythms, decisions, dependencies, tacit knowledge, and friction with confirm-before-save."
    - "Session produces extraction-shaped dict passing validate_extraction when driven from fixtures."
    - "Sidecar JSON is written under a path validated against the artifacts root."
  artifacts:
    - path: "graphify/elicit.py"
      provides: "State machine, extraction builders, optional LLM gate hook (stub or no-op default), sidecar save/load"
      contains: "elicitation"
    - path: "tests/test_elicit.py"
      provides: "Offline tests for states, validation, and idempotent save behavior"
      contains: "test_"
  key_links:
    - from: "graphify/elicit.py"
      to: "graphify.validate.validate_extraction"
      via: "pre-flight on built dict"
      pattern: "validate_extraction"
---

<objective>
Implement the library core for hybrid tacit-to-explicit elicitation: deterministic scripted backbone, optional LLM deepening hook (off by default in tests), extraction-shaped output, and persisted sidecar bundle under the resolved artifacts directory.

Purpose: Satisfies D-03 (hybrid interview), D-06 (persisted bundle for merge), D-07 traceability via ELIC-* coverage, and grounds ELIC-01/02/04/05/06 before build/CLI wiring.
Output: `graphify/elicit.py` + `tests/test_elicit.py` with passing pytest.
</objective>

<execution_context>
@$HOME/.cursor/get-shit-done/workflows/execute-plan.md
@$HOME/.cursor/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/39-tacit-to-explicit-onboarding-elicitation/39-CONTEXT.md
@.planning/phases/39-tacit-to-explicit-onboarding-elicitation/39-RESEARCH.md
@.planning/phases/39-tacit-to-explicit-onboarding-elicitation/39-PATTERNS.md
@graphify/validate.py

<interfaces>
From `graphify.validate`:
- `validate_extraction(data: dict) -> list[str]` — empty list means schema-OK for build.

From `graphify.security` (confirm exact names via grep before implementing):
- Path confinement helpers used by harness/export paths for writes under artifacts root.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Test-first elicitation extraction dict and validation</name>
  <files>tests/test_elicit.py, graphify/elicit.py</files>
  <behavior>
    - Completing a minimal scripted path (fixture-driven, no stdin) produces a dict with non-empty nodes/edges appropriate to the five SEED-001 dimensions.
    - `validate_extraction(dict)` returns [] for that dict.
    - Duplicate confirm-and-save on the same artifact path either merges idempotently or raises a documented \"preview required\" mode — behavior must match docstring contract tested explicitly (per ELIC-04).
  </behavior>
  <action>
    Add `graphify/elicit.py` with a finite-state interview backbone (explicit state enum or string constants), mapping answers to stable node `id` values (slug pattern consistent with `build` expectations). Implement `build_extraction_from_session(session) -> dict` and `save_elicitation_sidecar(artifacts_dir: Path, data: dict, *, mode: str) -> Path` where `artifacts_dir` is validated via the same security helpers used for harness writes (`validate_graph_path` pattern per 39-RESEARCH). Gate optional LLM deepening: public `maybe_deepen_session(...)` may be a no-op unless env/flag set — tests never enable it. Per D-03, do not ship LLM-only flow. Use `file_type` values allowed by `validate.py` for interview nodes (e.g. `rationale` / `document`). Implement ELIC-04 default: second save without `--force` avoids raw duplicate nodes (e.g. merge-by-id or refuse with clear error).
  </action>
  <verify>
    <automated>pytest tests/test_elicit.py -q</automated>
  </verify>
  <done>tests/test_elicit.py passes; validate_extraction empty for golden fixture; sidecar path confined.</done>
</task>

<task type="auto">
  <name>Task 2: Sidecar serialization contract documentation in module docstring</name>
  <files>graphify/elicit.py</files>
  <action>
    Document JSON keys, filename (`elicitation.json` or chosen name), merge position for Phase 39-02, and interaction with `ResolvedOutput.artifacts_dir` in the module docstring so Phase 40/41 agents do not need to re-read planning chat.
  </action>
  <verify>
    <automated>python -c "import graphify.elicit as e; assert e.__doc__"</automated>
  </verify>
  <done>Docstring names sidecar file and merge contract explicitly.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| User answers → node labels | Untrusted text must be sanitized before persistence |
| Artifacts path | Must stay inside resolved graphify-out / artifacts_dir |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-39-01 | T | `elicit.py` save path | mitigate | `validate_graph_path` / `resolve_output` roots only |
| T-39-02 | I | interview node labels in markdown export | mitigate | reuse `security` label sanitization before write |
</threat_model>

<verification>
`pytest tests/test_elicit.py -q` passes; no network mocks required.
</verification>

<success_criteria>
Library produces valid extraction dicts, persists confined sidecar, documents merge contract; ELIC-02/04/05/06 partially satisfied pending build merge plan 02.
</success_criteria>

<output>
After completion, create `.planning/phases/39-tacit-to-explicit-onboarding-elicitation/39-01-SUMMARY.md`
</output>
