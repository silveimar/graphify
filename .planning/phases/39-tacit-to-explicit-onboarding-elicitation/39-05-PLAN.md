---
phase: 39-tacit-to-explicit-onboarding-elicitation
plan: 05
type: execute
wave: 4
depends_on:
  - 39-04
files_modified:
  - docs/ELICITATION.md
  - README.md
autonomous: true
requirements:
  - ELIC-07
must_haves:
  truths:
    - "Dedicated doc explains discovery-first vs corpus extraction and links ELIC requirements."
    - "Users know default artifact locations and sidecar merge at a glance."
  artifacts:
    - path: "docs/ELICITATION.md"
      provides: "Discovery-first workflow guide"
      contains: "elicitation"
    - path: "README.md"
      provides: "Short pointer into docs/ELICITATION.md (one paragraph max)"
      contains: "ELICITATION.md"
  key_links:
    - from: "README.md"
      to: "docs/ELICITATION.md"
      via: "markdown link"
      pattern: "ELICITATION"
---

<objective>
Ship D-08 documentation: dedicated discovery-first / elicitation workflow file and minimal README cross-link; satisfy ELIC-07.

Purpose: Requirements traceability beyond CONTEXT-only; onboarding clarity per D-02.
Output: `docs/ELICITATION.md`, small README touch.
</objective>

<execution_context>
@$HOME/.cursor/get-shit-done/workflows/execute-plan.md
@$HOME/.cursor/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/REQUIREMENTS.md
@.planning/phases/39-tacit-to-explicit-onboarding-elicitation/39-CONTEXT.md
@docs/vault-adapter.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Write docs/ELICITATION.md</name>
  <files>docs/ELICITATION.md</files>
  <action>
    Cover: (1) when to use elicitation — empty or tiny corpus / onboarding (D-02); (2) CLI command spelling as implemented in 39-04; (3) where sidecar JSON and harness markdown land under `resolve_output` / vault sibling `graphify-out`; (4) how `build()` merges elicitation (order reference to `build.py` comment); (5) link to `REQUIREMENTS.md` ELIC-* IDs; (6) explicit non-goals: Phase 40 import, Phase 41 `--vault` selector.
  </action>
  <verify>
    <automated>test -f docs/ELICITATION.md && grep -q 'ELIC-' docs/ELICITATION.md && grep -q elicit docs/ELICITATION.md</automated>
  </verify>
  <done>Doc exists and references REQ IDs + elicit command.</done>
</task>

<task type="auto">
  <name>Task 2: README cross-link</name>
  <files>README.md</files>
  <action>
    Add a single short subsection or bullet under onboarding/docs pointing to `docs/ELICITATION.md`. Do not duplicate the full workflow.
  </action>
  <verify>
    <automated>grep -q 'ELICITATION.md' README.md</automated>
  </verify>
  <done>README links to the dedicated doc.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Documentation examples | Fictional paths only |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-39-07 | I | doc examples | accept | no live credentials or real vault paths |
</threat_model>

<verification>
`test -s docs/ELICITATION.md && grep -q ELICITATION.md README.md`
</verification>

<success_criteria>
D-08 satisfied: dedicated workflow doc + README pointer; ELIC-07 acceptance criteria met.
</success_criteria>

<output>
After completion, create `.planning/phases/39-tacit-to-explicit-onboarding-elicitation/39-05-SUMMARY.md`
</output>
