---
phase: 39-tacit-to-explicit-onboarding-elicitation
plan: 03
type: execute
wave: 3
depends_on:
  - 39-02
files_modified:
  - graphify/elicit.py
  - graphify/harness_export.py
  - tests/test_elicit.py
  - tests/test_harness_export.py
autonomous: true
requirements:
  - ELIC-03
  - ELIC-05
must_haves:
  truths:
    - "Elicitation can render SOUL/HEARTBEAT/USER-shaped markdown consistent with claude.yaml without requiring a full graph when configured."
    - "When graph.json exists, harness export path still works with elicitation-enriched data."
  artifacts:
    - path: "graphify/elicit.py"
      provides: "Direct harness-class render helper OR adapter that fills template tokens from session"
      contains: "harness|Soul|soul"
    - path: "graphify/harness_export.py"
      provides: "Any small shared helper if required — avoid breaking HARNESS tests"
      contains: "export_claude_harness"
  key_links:
    - from: "graphify/elicit.py"
      to: "graphify.harness_export"
      via: "shared placeholder normalization or synthetic graph_data"
      pattern: "export_claude_harness|_normalize_placeholders"
---

<objective>
Align elicitation outputs with harness export shapes (D-04): fast path markdown from session state, and compatibility with `export_claude_harness` when `graph.json` + sidecars exist.

Purpose: ELIC-03 and harness schema stability for Phase 40.
Output: Implementation + tests proving both paths.
</objective>

<execution_context>
@$HOME/.cursor/get-shit-done/workflows/execute-plan.md
@$HOME/.cursor/get-shit-done/templates/summary.md
</execution_context>

<context>
@graphify/harness_export.py
@graphify/harness_schemas/claude.yaml
@tests/test_harness_export.py
@.planning/phases/39-tacit-to-explicit-onboarding-elicitation/39-RESEARCH.md

<interfaces>
From `graphify.harness_export`:
- `export_claude_harness(out_dir: Path, *, target: str = "claude", ...) -> list[Path]`
- `_normalize_placeholders`, `load_snapshot_inputs` / internal snapshot loader as appropriate (read file before use).

Schema tokens in `claude.yaml`: `generated_at`, `graphify_version`, `agent_identity`, `god_nodes`, `recent_deltas`, `hot_paths`.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Direct markdown path from elicitation session</name>
  <files>graphify/elicit.py, tests/test_elicit.py</files>
  <behavior>
    - Given a frozen session fixture, emitted filenames match schema (`claude-SOUL.md`, etc.) or documented elicitation-specific subfolder under harness output.
    - Template still uses safe substitution only; no Jinja.
  </behavior>
  <action>
    Implement `write_elicitation_harness_markdown(out_dir: Path, session_snapshot: dict, *, clock=None) -> list[Path]` that writes under `out_dir/harness/` with path validation. Fill placeholders from elicitation answers mapping deterministically to `god_nodes`/`recent_deltas` text sections (may be sparse but valid markdown). Reuse `_normalize_placeholders` from `harness_export` if exported or duplicate minimal safe pattern per HARNESS-03 — prefer import from `harness_export` to avoid drift.
  </action>
  <verify>
    <automated>pytest tests/test_elicit.py -q</automated>
  </verify>
  <done>Tests in test_elicit.py cover direct harness markdown path with deterministic content.</done>
</task>

<task type="auto">
  <name>Task 2: Integration with export_claude_harness when graph exists</name>
  <files>tests/test_elicit.py, graphify/harness_export.py</files>
  <action>
    Extend or reuse `tests/test_harness_export.py` fixtures: build minimal `graph.json` that includes elicitation-originated node ids; run `export_claude_harness` and assert SOUL body references expected labels. If `harness_export` needs a small hook to read elicitation-enriched annotations, add it with backward compatibility — existing tests must remain green.
  </action>
  <verify>
    <automated>pytest tests/test_harness_export.py tests/test_elicit.py -q</automated>
  </verify>
  <done>No regressions in harness export suite; elicitation labels visible in output.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Session → markdown | Injection via template placeholders |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-39-05 | I | harness markdown | mitigate | `string.Template.safe_substitute` + placeholder normalization only |
</threat_model>

<verification>
`pytest tests/test_harness_export.py tests/test_elicit.py -q`
</verification>

<success_criteria>
ELIC-03 met: direct + graph-backed harness paths both tested.
</success_criteria>

<output>
After completion, create `.planning/phases/39-tacit-to-explicit-onboarding-elicitation/39-03-SUMMARY.md`
</output>
