---
phase: 39-tacit-to-explicit-onboarding-elicitation
plan: 02
type: execute
wave: 2
depends_on:
  - "39-01"
files_modified:
  - graphify/build.py
  - graphify/elicit.py
  - tests/test_elicit.py
autonomous: true
requirements:
  - ELIC-02
  - ELIC-04
  - ELIC-05
must_haves:
  truths:
    - "build() loads elicitation sidecar when present and merges with explicit ordering."
    - "Merged graph still validates per validate_extraction rules."
    - "Duplicate node IDs follow documented last-wins or elicitation-priority rule in comments + test."
  artifacts:
    - path: "graphify/build.py"
      provides: "load_elicitation_sidecar + merge hook or build(..., elicitation_path=) API"
      contains: "elicitation"
    - path: "tests/test_elicit.py"
      provides: "Merge ordering + duplicate behavior tests"
      contains: "build"
  key_links:
    - from: "graphify/build.py"
      to: "graphify.elicit.load_elicitation_sidecar"
      via: "optional merge input"
      pattern: "elicitation"
---

<objective>
Integrate elicitation persistence with graph assembly: load sidecar JSON, merge into the extraction list with explicit ordering and dedup rules, preserve validation semantics.

Purpose: Delivers D-06 (merge in `build`, not fake `extract()` only) and completes ELIC-02/04/05 merge aspects.
Output: Updated `build.py`, loader hooks in `elicit.py` if needed, extended tests.
</objective>

<execution_context>
@$HOME/.cursor/get-shit-done/workflows/execute-plan.md
@$HOME/.cursor/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/39-tacit-to-explicit-onboarding-elicitation/39-CONTEXT.md
@.planning/phases/39-tacit-to-explicit-onboarding-elicitation/39-RESEARCH.md
@graphify/build.py
@graphify/elicit.py
@graphify/validate.py

<interfaces>
From `graphify/build.py`:
- `build(extractions: list[dict], *, directed: bool = False) -> nx.Graph`
- `build_from_json(extraction: dict, *, directed: bool = False) -> nx.Graph`

Merge policy: document order (e.g. `[code_extractions..., elic_dict]`) so elicitation labels can augment but code-derived precision wins or vice versa — pick one per research; **must match test assertions**.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: load sidecar + merge tests</name>
  <files>tests/test_elicit.py, graphify/elicit.py, graphify/build.py</files>
  <behavior>
    - `build([ast_like, elicitation_dict])` includes nodes only present in elicitation.
    - Re-run with unchanged elicitation does not duplicate nodes in graph node set (per merge strategy).
    - Corrupt sidecar file → clear error or skip with stderr warning per project convention (document in action).
  </behavior>
  <action>
    Implement `load_elicitation_sidecar(artifacts_dir: Path) -> dict | None` in `elicit.py` (returns None if missing). Extend `build.build` with optional keyword `elicitation: dict | None = None` **or** `elicitation_path: Path | None = None` — choose one API surface; if adding parameters, update all internal callers/tests. Merge elicitation dict **after** list extractions so list remains backward compatible: `combined = list(extractions) + ([elicitation] if elicitation else [])` equivalent. Path arguments must be validated. Add tests alongside existing `test_elicit` patterns.
  </action>
  <verify>
    <automated>pytest tests/test_elicit.py -q</automated>
  </verify>
  <done>Tests prove ordering; documented in `build.py` header comment.</done>
</task>

<task type="auto">
  <name>Task 2: Export helper for callers</name>
  <files>graphify/elicit.py</files>
  <action>
    Expose a single `merge_elicitation_into_build_inputs(extractions: list[dict], artifacts_dir: Path) -> list[dict]` if it reduces duplication for `__main__.py` in plan 04; otherwise document direct `build(..., elicitation=load…)` usage. Do not change `build_from_json` signature unnecessarily.
  </action>
  <verify>
    <automated>pytest tests/test_elicit.py -q</automated>
  </verify>
  <done>Executor CLI has clear import path for merged build.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Sidecar JSON on disk | Attacker-supplied file in artifacts dir |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-39-03 | T | sidecar load | mitigate | validate paths; optional max bytes read; reject non-dict JSON |
| T-39-04 | I | merged graph content | mitigate | reuse validate_extraction before `build_from_json` |
</threat_model>

<verification>
`pytest tests/test_elicit.py -q` passes.
</verification>

<success_criteria>
Elicitation data merges into `build()` predictably; ELIC-02/04 satisfied with tests.
</success_criteria>

<output>
After completion, create `.planning/phases/39-tacit-to-explicit-onboarding-elicitation/39-02-SUMMARY.md`
</output>
