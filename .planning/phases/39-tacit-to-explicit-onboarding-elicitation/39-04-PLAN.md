---
phase: 39-tacit-to-explicit-onboarding-elicitation
plan: 04
type: execute
wave: 3
depends_on:
  - 39-02
files_modified:
  - graphify/__main__.py
  - tests/test_main_flags.py
  - graphify/skill.md
  - graphify/skill-codex.md
  - graphify/skill-opencode.md
  - graphify/skill-claw.md
  - graphify/skill-droid.md
  - graphify/skill-trae.md
  - graphify/skill-aider.md
  - graphify/skill-copilot.md
autonomous: true
requirements:
  - ELIC-01
  - ELIC-02
  - ELIC-05
  - ELIC-06
must_haves:
  truths:
    - "`graphify elicit` (or chosen spelling) calls into `graphify.elicit` — not a forked interview in skill files."
    - "CLI help states onboarding / empty-corpus emphasis per D-02."
    - "Skills reference the same invocation pattern (thin wrapper per D-01)."
  artifacts:
    - path: "graphify/__main__.py"
      provides: "elicit subcommand registration + argv forwarding"
      contains: "elicit"
    - path: "tests/test_main_flags.py"
      provides: "CLI smoke / argument tests for elicit"
      contains: "elicit"
  key_links:
    - from: "graphify/__main__.py"
      to: "graphify.elicit"
      via: "library call"
      pattern: "from graphify.elicit|graphify\\.elicit"
---

<objective>
Expose elicitation as the canonical CLI command with `resolve_output` integration for default artifact paths; update primary skill files to thin-wrap the CLI/library path.

Purpose: D-01 (CLI canonical), D-02 (onboarding messaging), D-05 (vault-resolved artifacts).
Output: `__main__.py` branch, tests, skill text updates.
</objective>

<execution_context>
@$HOME/.cursor/get-shit-done/workflows/execute-plan.md
@$HOME/.cursor/get-shit-done/templates/summary.md
</execution_context>

<context>
@graphify/__main__.py
@graphify/output.py
@tests/test_main_flags.py
@graphify/skill.md
@.planning/phases/39-tacit-to-explicit-onboarding-elicitation/39-CONTEXT.md

<interfaces>
From `graphify.output`:
- `resolve_output(cwd: Path, *, cli_output: str | None = None) -> ResolvedOutput`

Mirror patterns from existing `harness export` argparse block (~line 2181).
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: CLI elicit command + tests</name>
  <files>graphify/__main__.py, tests/test_main_flags.py</files>
  <behavior>
    - `graphify elicit --help` exits 0 and mentions onboarding/empty corpus.
    - Running elicitation in `--dry-run` or `--fixtures` test mode (choose one documented approach) writes or previews sidecar under resolved `artifacts_dir` without network.
  </behavior>
  <action>
    Add `elif cmd == "elicit":` (or subparser) mirroring `harness` style. Call `resolve_output(Path.cwd(), cli_output=...)` when `--output` passed consistent with other commands. Wire to `elicit` module APIs from plans 01–02. Avoid duplicating interview logic. Use lazy import inside branch.
  </action>
  <verify>
    <automated>pytest tests/test_main_flags.py -q</automated>
  </verify>
  <done>New elicit CLI tests exist and full test_main_flags passes.</done>
</task>

<task type="auto">
  <name>Task 2: Skill thin wrappers</name>
  <files>graphify/skill.md, graphify/skill-codex.md, graphify/skill-opencode.md, graphify/skill-claw.md, graphify/skill-droid.md, graphify/skill-trae.md, graphify/skill-aider.md, graphify/skill-copilot.md</files>
  <action>
    Add a concise subsection: when corpus empty/tiny, prefer `/graphify elicit` or `graphify elicit` CLI over full extraction; state that behavior lives in CLI/package. Do not paste the full state machine into markdown.
  </action>
  <verify>
    <automated>grep -nE "elicit" graphify/skill.md graphify/skill-codex.md | head -5</automated>
  </verify>
  <done>Each listed skill file contains an elicit pointer; wording platform-appropriate.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| CLI args → paths | Same as other graphify commands |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-39-06 | T | `--output` handling | mitigate | reuse `resolve_output`; no raw path join from user without validation |
</threat_model>

<verification>
`pytest tests/test_main_flags.py -q`; manual `graphify elicit --help` optional smoke.
</verification>

<success_criteria>
Canonical CLI entry exists; skills aligned; ELIC-01 satisfied for entry surface.
</success_criteria>

<output>
After completion, create `.planning/phases/39-tacit-to-explicit-onboarding-elicitation/39-04-SUMMARY.md`
</output>
