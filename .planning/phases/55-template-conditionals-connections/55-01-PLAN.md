---
phase: 55
plan: 01
type: tdd
wave: 0
depends_on: []
files_modified:
  - graphify/templates.py
  - tests/test_templates.py
  - tests/test_profile.py
autonomous: true
requirements: [TMPL-01, TMPL-02]
must_haves:
  truths:
    - "BlockContext accepts note_type=None and flag_predicates={} as defaults so all existing call sites compile unchanged"
    - "Phase 31 byte-identical gate (test_block_free_template_renders_byte_identical) remains GREEN after BlockContext extension"
    - "RED tests exist for if_note_type_*, if_flag_*, predicate_flags validation, and BlockContext field shape"
    - "All Phase 31 backward-compat tests (nested rejection x2, empty connections loop, byte-identical) stay GREEN"
  artifacts:
    - path: "graphify/templates.py"
      provides: "BlockContext extended with note_type and flag_predicates fields (defaulted)"
      contains: "note_type: str | None = None"
    - path: "tests/test_templates.py"
      provides: "RED scaffold tests for if_note_type_* and if_flag_* + updated byte-identical gate"
    - path: "tests/test_profile.py"
      provides: "RED scaffold tests for predicate_flags validation"
  key_links:
    - from: "BlockContext (templates.py)"
      to: "test_block_free_template_renders_byte_identical (tests/test_templates.py:3249)"
      via: "frozen dataclass keyword construction"
      pattern: "BlockContext\\(.*note_type=None"
---

<objective>
Wave 0 RED scaffolding for Phase 55. Extend the frozen `BlockContext` dataclass with two defaulted fields (`note_type: str | None = None`, `flag_predicates: dict = field(default_factory=dict)`), update the byte-identical gate test for the new field shape, and write RED tests that fail correctly for `if_note_type_*`, `if_flag_*`, and `predicate_flags` validation. No production evaluator code is added in this plan — that lands in 55-02 and 55-03.

Purpose: Establish the test-first contract Phase 55 implements against. Frozen-dataclass defaults make the field addition backward-compatible at all ~30+ existing test call sites; only the byte-identical gate test (which uses positional kwargs explicitly) needs an update.
Output: Extended dataclass + RED test scaffolds.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/55-template-conditionals-connections/55-CONTEXT.md
@.planning/phases/55-template-conditionals-connections/55-RESEARCH.md
@.planning/phases/55-template-conditionals-connections/55-PATTERNS.md
@.planning/phases/55-template-conditionals-connections/55-VALIDATION.md

<interfaces>
From graphify/templates.py:206-219 (BlockContext, EXISTING — Phase 55 extends with two fields):
```python
@dataclasses.dataclass(frozen=True)
class BlockContext:
    graph: "object"
    node_id: str
    edges: list[dict]
    dataview_nonempty: bool
    # Phase 55 additions (defaulted so existing kwargs callers stay valid):
    # note_type: str | None = None
    # flag_predicates: dict = dataclasses.field(default_factory=dict)
```

From tests/test_templates.py:3249-3254 (byte-identical gate, MUST be updated to pass new fields explicitly to remain self-documenting):
```python
block_ctx = BlockContext(
    graph=G,
    node_id="n",
    edges=_build_edge_records(G, "n"),
    dataview_nonempty=False,
)
```
</interfaces>
</context>

<tasks>

<task type="tdd" tdd="true">
  <name>Task 1: Extend BlockContext with note_type and flag_predicates (defaulted, frozen-safe)</name>
  <files>graphify/templates.py</files>
  <read_first>
    - graphify/templates.py:177-272 (the @dataclass(frozen=True) class BlockContext block + _eval_predicate dispatch — DO NOT modify _eval_predicate yet, only the dataclass)
    - tests/test_templates.py:3219-3265 (byte-identical gate; confirms frozen kwargs construction style)
    - PATTERNS.md "Pattern 2 — BlockContext frozen dataclass" (templates.py:206-219)
  </read_first>
  <behavior>
    - BlockContext(graph=G, node_id="n", edges=[], dataview_nonempty=False) still works (defaults satisfy frozen-dataclass init)
    - BlockContext(...).note_type defaults to None
    - BlockContext(...).flag_predicates defaults to {} (new dict per instance via default_factory)
    - dataclasses.is_dataclass(BlockContext) True; dataclasses.fields(BlockContext) length is 6
  </behavior>
  <action>
    Open graphify/templates.py, locate the `@dataclasses.dataclass(frozen=True)` decorated `BlockContext` class (around L206). Append two fields *after* `dataview_nonempty: bool`:

    ```python
        note_type: str | None = None
        flag_predicates: dict = dataclasses.field(default_factory=dict)
    ```

    Per PATTERNS.md Pattern 2 and RESEARCH.md Risk 1 mitigation, frozen=True dataclasses support default values in Python 3.10+. Defaults make all existing kwargs-style `BlockContext(...)` callers backward-compatible — only the byte-identical gate test (Task 2) and the two production call sites (touched in Plan 55-02 / 55-03) need explicit new kwargs.

    Do NOT modify `_eval_predicate`, `_PREDICATE_CATALOG`, `_IF_ATTR_RE`, or `validate_template` in this task — those land in Plans 55-02 and 55-03.
  </action>
  <verify>
    <automated>python -c "from graphify.templates import BlockContext; import dataclasses, networkx as nx; G=nx.Graph(); G.add_node('n', label='N'); ctx=BlockContext(graph=G, node_id='n', edges=[], dataview_nonempty=False); assert ctx.note_type is None; assert ctx.flag_predicates == {}; assert len(dataclasses.fields(BlockContext)) == 6; print('OK')"</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "note_type: str | None = None" graphify/templates.py` returns >=1
    - `grep -c "flag_predicates: dict = dataclasses.field(default_factory=dict)" graphify/templates.py` returns >=1
    - The python -c smoke check above prints OK and exits 0
    - `pytest tests/test_templates.py -q -x` does NOT regress (the byte-identical test may still fail — that is fixed in Task 2)
  </acceptance_criteria>
  <done>BlockContext has 6 fields, defaults work, smoke check passes.</done>
</task>

<task type="tdd" tdd="true">
  <name>Task 2: Update byte-identical gate test + write RED scaffold tests</name>
  <files>tests/test_templates.py, tests/test_profile.py</files>
  <read_first>
    - tests/test_templates.py:3219-3265 (test_block_free_template_renders_byte_identical, the call to update)
    - tests/test_templates.py:2613-2670 (test_if_attr_* — analog for if_note_type_* / if_flag_* RED tests)
    - tests/test_profile.py:1631-1695 (test_dataview_queries_* — analog for predicate_flags RED tests)
    - PATTERNS.md "Pattern 1 — test_if_attr_* structure" + "tests/test_profile.py extend"
    - RESEARCH.md Q7 ("Backward-Compatibility Envelope") + "Wave 0 Gaps" list
  </read_first>
  <behavior>
    - Updated byte-identical gate (test_block_free_template_renders_byte_identical) passes GREEN with new BlockContext fields explicit
    - New test_if_note_type_* tests fail RED (no _IF_NOTE_TYPE_RE branch yet)
    - New test_if_flag_* tests fail RED (no _IF_FLAG_RE branch yet)
    - New test_predicate_flags_* tests in test_profile.py fail RED (no _validate_predicate_flags helper yet)
    - test_block_free_template_renders_byte_identical asserts byte-equality — must REMAIN GREEN throughout the phase
    - Phase 31 backward-compat tests (test_nested_blocks_rejected_with_specific_error L2838, test_nested_if_in_if_rejected L2852, test_connections_empty_loop_renders_nothing L2767) remain GREEN
  </behavior>
  <action>
    1. **Update byte-identical gate (tests/test_templates.py L3249).** Locate the `block_ctx = BlockContext(...)` construction inside `test_block_free_template_renders_byte_identical` and add the two new fields explicitly:
       ```python
       block_ctx = BlockContext(
           graph=G,
           node_id="n",
           edges=_build_edge_records(G, "n"),
           dataview_nonempty=False,
           note_type=None,
           flag_predicates={},
       )
       ```
       Run the test; it must PASS (byte-identical contract preserved). If it fails, the BlockContext extension in Task 1 was wrong — fix before continuing.

    2. **Add RED tests for if_note_type_* (append to tests/test_templates.py near L2670, after test_if_attr_* block).** Per PATTERNS.md Pattern 1, write at minimum:
       - `test_if_note_type_thing_renders_when_note_type_matches`: BlockContext(...note_type="thing") + template `"X{{#if_note_type_thing}}T{{/if}}Y"` → asserts result == "XTY"
       - `test_if_note_type_omits_when_note_type_mismatches`: BlockContext(...note_type="statement") with the same template → asserts result == "XY"
       - `test_if_note_type_omits_when_note_type_none`: BlockContext(...note_type=None) → asserts result == "XY"
       - One parametrized or repeated case per type in `("thing", "statement", "person", "source", "code", "moc")` confirming evaluator branch (6 total).
       - `test_if_note_type_unknown_suffix_rejected_at_validate_template`: `validate_template("{{#if_note_type_blarg}}X{{/if}}", required=set())` must return errors mentioning the unknown suffix and `_KNOWN_NOTE_TYPES` (per D-55.05).
       Each must use `from graphify.templates import BlockContext, _expand_blocks` (the existing test helper `_expand` may also be reused — it lives at the top of the test file).
       At this stage all of these tests MUST FAIL (RED) — the regex + evaluator branch lands in Plan 55-02.

    3. **Add RED tests for if_flag_*** (append after the if_note_type_* block):
       - `test_if_flag_truthy_rule_renders`: build a callable `lambda ctx: True`, place under `flag_predicates={"is_published": handler}`, template `"X{{#if_flag_is_published}}P{{/if}}Y"` → asserts "XPY"
       - `test_if_flag_equality_rule_omits_when_unequal`: handler returns False → asserts "XY"
       - `test_if_flag_unknown_name_omits_at_render` (defensive — render-time KeyError fallback already exists)
       At this stage all FAIL RED.

    4. **Add RED tests for predicate_flags validation (append to tests/test_profile.py near L1695, after test_dataview_queries_* block).** Per PATTERNS.md "tests/test_profile.py extend" list, write tests for D-55.08 rejection rules. Use `from graphify.profile import validate_profile`:
       - `test_predicate_flags_valid_truthy_rule` — `{"predicate_flags": {"is_published": {"attr": "is_published"}}}` returns `[]`
       - `test_predicate_flags_valid_equality_rule` — `{"predicate_flags": {"is_reviewed": {"attr": "status", "equals": "done"}}}` returns `[]`
       - `test_predicate_flags_non_dict_rejected` — value `"not a dict"` → error containing `"must be a dict"`
       - `test_predicate_flags_catalog_collision_rejected` — name `"god_node"` → error containing `"collides with built-in predicate"`
       - `test_predicate_flags_attr_prefix_rejected` — name `"attr_foo"` → error containing `"attr_"`
       - `test_predicate_flags_missing_attr_key_rejected` — rule `{}` → error containing `"attr"`
       - `test_predicate_flags_extra_keys_rejected` — rule `{"attr": "x", "op": "eq"}` → error containing `"unknown keys"`
       - `test_predicate_flags_top_level_key_accepted` — confirms `"predicate_flags"` is in `_VALID_TOP_LEVEL_KEYS`.
       All FAIL RED until Plan 55-03 lands `_validate_predicate_flags` and the `_VALID_TOP_LEVEL_KEYS` extension.
  </action>
  <verify>
    <automated>pytest tests/test_templates.py::test_block_free_template_renders_byte_identical tests/test_templates.py::test_nested_blocks_rejected_with_specific_error tests/test_templates.py::test_nested_if_in_if_rejected tests/test_templates.py::test_connections_empty_loop_renders_nothing -q</automated>
  </verify>
  <acceptance_criteria>
    - The 4 GREEN-must-stay-GREEN tests above all pass (exit 0)
    - `pytest tests/test_templates.py -k "if_note_type" -q` reports >=7 collected, all RED (exit nonzero)
    - `pytest tests/test_templates.py -k "if_flag" -q` reports >=3 collected, all RED
    - `pytest tests/test_profile.py -k "predicate_flags" -q` reports >=8 collected, all RED
    - `grep -c "def test_if_note_type_" tests/test_templates.py` returns >=6
    - `grep -c "def test_if_flag_" tests/test_templates.py` returns >=3
    - `grep -c "def test_predicate_flags_" tests/test_profile.py` returns >=8
    - `grep -q "note_type=None" tests/test_templates.py && grep -q "flag_predicates={}" tests/test_templates.py` exits 0
  </acceptance_criteria>
  <done>RED gate established: backward-compat green; new feature tests red; all listed counts met.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| profile.yaml → in-memory profile dict | YAML-parsed input crosses; predicate_flags rule values consulted (not rendered) |
| BlockContext → _eval_predicate | Predicate names from template text reach evaluator; existing _BLOCK_OPEN_RE charset locks the alphabet |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-55-INJ | Injection | predicate_flags rule values | mitigate | Wave 0 RED tests assert rule values are CONSULTED only (boolean output) — Plan 55-03 GREEN code emits no string sinks; existing _sanitize_wikilink_alias remains the sole rendered-text gate |
| T-55-FLAG-VAL | DoS via large profile | predicate_flags dict count | accept (pre-mitigated) | Plan 55-03 caps predicate_flags entry count (recommended 64) in _validate_predicate_flags. This Wave 0 plan only stages tests; cap is verified in 55-03. |
| T-55-NAME-COLLISION | Tampering (logic) | catalog vs predicate_flags name shadowing | mitigate | RED test `test_predicate_flags_catalog_collision_rejected` asserts shadowing names error out; GREEN landing in 55-03 |
</threat_model>

<verification>
- `pytest tests/test_templates.py -q -x` reports the 4 backward-compat tests GREEN
- `pytest tests/test_templates.py tests/test_profile.py -q` shows new tests RED but collected
- `dataclasses.fields(BlockContext)` returns 6 entries
</verification>

<success_criteria>
- BlockContext extended with two defaulted fields (frozen-safe)
- Byte-identical gate test updated and GREEN
- All Phase 31 backward-compat tests GREEN (TMPL-02 evidence preserved)
- RED scaffold tests collected for: if_note_type_* (>=7), if_flag_* (>=3), predicate_flags validation (>=8)
- No production evaluator/validator code added in this plan
</success_criteria>

<output>
After completion, create `.planning/phases/55-template-conditionals-connections/55-01-SUMMARY.md`
</output>
