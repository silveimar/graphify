---
phase: 55
plan: 06
type: execute
wave: 4
depends_on: ["55-02", "55-03", "55-04", "55-05"]
files_modified:
  - .planning/phases/55-template-conditionals-connections/55-VERIFICATION.md
  - .planning/ROADMAP.md
autonomous: true
requirements: [TMPL-01, TMPL-02]
must_haves:
  truths:
    - "55-VERIFICATION.md exists with goal-backward truths/artifacts/key_links populated and citations to all relevant tests"
    - "Phase 31 backward-compat sentinels (test_nested_blocks_rejected_with_specific_error, test_nested_if_in_if_rejected, test_connections_empty_loop_renders_nothing, test_block_free_template_renders_byte_identical) are cited as TMPL-02 evidence"
    - "ROADMAP.md Phase 55 entry shows status complete and links to all 6 PLAN.md files"
    - "Full pytest suite GREEN at the close commit (cite passing/total ratio)"
    - "ROADMAP requirement IDs TMPL-01 and TMPL-02 marked complete (per project convention)"
  artifacts:
    - path: ".planning/phases/55-template-conditionals-connections/55-VERIFICATION.md"
      provides: "Final phase verification report with truths, artifact existence, key-link presence, test-citation map"
      contains: "TMPL-01"
    - path: ".planning/ROADMAP.md"
      provides: "Phase 55 marked complete, all 6 plan files enumerated, requirement IDs updated"
      contains: "Phase 55"
  key_links:
    - from: "55-VERIFICATION.md"
      to: "tests/test_templates.py and tests/test_profile.py and tests/test_docs_templates_examples.py"
      via: "explicit test name citations + grep gates"
      pattern: "test_"
    - from: "ROADMAP.md Phase 55 entry"
      to: "Plans 55-01 through 55-06"
      via: "markdown link list"
      pattern: "55-0[1-6]-PLAN\\.md"
---

<objective>
Wave 4 close-out. Author `55-VERIFICATION.md` per the goal-backward methodology with explicit citations to: (a) the new GREEN tests landed in 55-02/55-03/55-04/55-05; (b) the Phase 31 backward-compat sentinels — `test_nested_blocks_rejected_with_specific_error`, `test_nested_if_in_if_rejected`, `test_connections_empty_loop_renders_nothing`, `test_block_free_template_renders_byte_identical` — which constitute the TMPL-02 evidence per D-55.02. Update ROADMAP.md to mark Phase 55 complete with links to all 6 plan files.

Purpose: Final accounting. The phase is self-evidencing — every truth in the must_haves maps to a grep-verifiable artifact or pytest-verifiable behavior, and the verification document records that mapping for future audits.
Output: 55-VERIFICATION.md + updated ROADMAP.md; commit ready for `/gsd-verify-work`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/55-template-conditionals-connections/55-CONTEXT.md
@.planning/phases/55-template-conditionals-connections/55-RESEARCH.md
@.planning/phases/55-template-conditionals-connections/55-VALIDATION.md
@.planning/phases/55-template-conditionals-connections/55-01-SUMMARY.md
@.planning/phases/55-template-conditionals-connections/55-02-SUMMARY.md
@.planning/phases/55-template-conditionals-connections/55-03-SUMMARY.md
@.planning/phases/55-template-conditionals-connections/55-04-SUMMARY.md
@.planning/phases/55-template-conditionals-connections/55-05-SUMMARY.md

<interfaces>
ROADMAP.md Phase 55 success criteria (lines 573-585):
1. Conditional template sections + profile-controlled predicates + sanitization (TMPL-01)
2. {{#connections}} loop deterministic ordering + nested + empty-iterable (TMPL-02) — already satisfied by Phase 31 per D-55.02
3. validate_profile_preflight flags malformed blocks — already satisfied at wiring level per D-55.03

Phase 31 backward-compat tests to cite as TMPL-02 evidence (per D-55.02 + RESEARCH.md Q7):
- tests/test_templates.py::test_nested_blocks_rejected_with_specific_error (L2838)
- tests/test_templates.py::test_nested_if_in_if_rejected (L2852)
- tests/test_templates.py::test_connections_empty_loop_renders_nothing (L2767)
- tests/test_templates.py::test_block_free_template_renders_byte_identical (L3219)
- tests/test_templates.py::test_connections_loop_deterministic_order (L2780)
- tests/test_templates.py::test_connection_field_sanitization_blocks_label_injection (L3123)
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Author 55-VERIFICATION.md with truths / artifacts / key_links / test-citation map</name>
  <files>.planning/phases/55-template-conditionals-connections/55-VERIFICATION.md</files>
  <read_first>
    - .planning/phases/55-template-conditionals-connections/55-VALIDATION.md (existing Nyquist test map — verification expands on this)
    - .planning/phases/55-template-conditionals-connections/55-01-SUMMARY.md through 55-05-SUMMARY.md (per-plan outcomes)
    - 55-CONTEXT.md decisions (D-55.01..D-55.14) and goals
    - RESEARCH.md "Validation Architecture" + "Wave 0 Gaps"
  </read_first>
  <action>
    Create `55-VERIFICATION.md` with the following structure. Run the cited grep/pytest commands at write time to populate counts/numbers; do not stub them.

    ```markdown
    ---
    phase: 55
    slug: template-conditionals-connections
    status: complete
    completed: <YYYY-MM-DD>  # fill from `date +%Y-%m-%d`
    ---

    # Phase 55 Verification — Template Conditionals & Connection Loops

    Goal-backward verification per Phase 55 ROADMAP success criteria. All truths grep- or pytest-verifiable.

    ## Goal

    > Extend `string.Template`-based rendering with conditional sections and connection-iteration blocks, expanded **before** `${}` substitution and routed through existing sanitization sinks (no Jinja2 dependency).

    ## Observable Truths (TMPL-01)

    | # | Truth | Verification | Result |
    |---|-------|--------------|--------|
    | T1 | Authors can write `{{#if_note_type_thing}}…{{/if}}` and 5 siblings | `pytest tests/test_templates.py -k "if_note_type" -q` | ✅ GREEN (N tests) |
    | T2 | Unknown note-type suffix rejected at preflight (D-55.05) | `pytest tests/test_templates.py::test_if_note_type_unknown_suffix_rejected_at_validate_template -q` | ✅ GREEN |
    | T3 | `predicate_flags:` is a recognized top-level profile.yaml key | `grep -c '"predicate_flags"' graphify/profile.py` >=2 | ✅ |
    | T4 | predicate_flags rule shapes `{attr}` and `{attr, equals}` both work | `pytest tests/test_templates.py -k "if_flag" -q` | ✅ GREEN |
    | T5 | predicate_flags validation enforces D-55.08 rules | `pytest tests/test_profile.py -k "predicate_flags" -q` | ✅ GREEN |
    | T6 | preflight rejects undeclared `if_flag_*` references | `pytest tests/test_profile.py::test_preflight_rejects_undeclared_if_flag_in_template -q` | ✅ GREEN |
    | T7 | Block expansion runs BEFORE `${}` substitution (D-16) | `pytest tests/test_templates.py::test_block_expansion_runs_before_substitution -q` | ✅ GREEN |
    | T8 | New predicates emit boolean only — no new sanitization sink | `pytest tests/test_templates.py -k "sanitize or inject" -q` | ✅ GREEN |
    | T9 | `docs/TEMPLATES.md` exists with 8 locked sections | `grep -c '^## ' docs/TEMPLATES.md` >=8 | ✅ |
    | T10 | Every section has an annotated `<!-- test:<label> -->` fence | `for L in conditional-basic connections-loop ordering-invariant sanitization predicate-catalog predicate-flags validation backward-compat; do grep -q "<!-- test:$L -->" docs/TEMPLATES.md; done` | ✅ |
    | T11 | docs/TEMPLATES.md examples are runnable fixtures (D-55.12) | `pytest tests/test_docs_templates_examples.py -q` | ✅ GREEN (8 tests) |
    | T12 | `docs/PROFILE-CONFIGURATION.md` includes pointer to TEMPLATES.md | `grep -c "TEMPLATES.md" docs/PROFILE-CONFIGURATION.md` >=1 | ✅ |

    ## Backward-Compatibility Sentinels (TMPL-02 — D-55.02 evidence)

    Per D-55.02, the TMPL-02 deliverables (deterministic connections ordering, nested rejection, empty-iterable) are **already shipped** by Phase 31 and held green by Phase 55. Phase 55 does NOT re-implement these — it preserves them. Sentinels:

    | # | Test | Source Phase | Status |
    |---|------|--------------|--------|
    | S1 | `tests/test_templates.py::test_nested_blocks_rejected_with_specific_error` (L2838) | Phase 31 | ✅ GREEN |
    | S2 | `tests/test_templates.py::test_nested_if_in_if_rejected` (L2852) | Phase 31 | ✅ GREEN |
    | S3 | `tests/test_templates.py::test_connections_empty_loop_renders_nothing` (L2767) | Phase 31 | ✅ GREEN |
    | S4 | `tests/test_templates.py::test_block_free_template_renders_byte_identical` (L3219) | Phase 31 (updated by Plan 55-01 for new BlockContext fields) | ✅ GREEN |
    | S5 | `tests/test_templates.py::test_connections_loop_deterministic_order` (L2780) | Phase 31 | ✅ GREEN |
    | S6 | `tests/test_templates.py::test_connection_field_sanitization_blocks_label_injection` (L3123) | Phase 31 | ✅ GREEN |

    Verification command: `pytest tests/test_templates.py::test_nested_blocks_rejected_with_specific_error tests/test_templates.py::test_nested_if_in_if_rejected tests/test_templates.py::test_connections_empty_loop_renders_nothing tests/test_templates.py::test_block_free_template_renders_byte_identical -q` exits 0.

    ## Required Artifacts

    | Artifact | Provides | Contains | Verified |
    |----------|----------|----------|----------|
    | `graphify/templates.py` | `_IF_NOTE_TYPE_RE`, `_IF_FLAG_RE`, evaluator branches, `_compile_flag_predicates`, `BlockContext` plumbing | `_IF_NOTE_TYPE_RE = re.compile`, `_IF_FLAG_RE = re.compile`, `def _compile_flag_predicates`, `note_type=note_type`, `flag_predicates=_compile_flag_predicates` | grep gates above |
    | `graphify/profile.py` | `_validate_predicate_flags`, `_VALID_TOP_LEVEL_KEYS` extension, preflight cross-check | `def _validate_predicate_flags`, `"predicate_flags"`, `known_flag_predicates=` | grep gates above |
    | `tests/test_templates.py` | RED-then-GREEN tests for `if_note_type_*` and `if_flag_*` | `def test_if_note_type_`, `def test_if_flag_` | `grep -c` >=6 / >=3 |
    | `tests/test_profile.py` | predicate_flags validation tests | `def test_predicate_flags_` | `grep -c` >=8 |
    | `tests/test_docs_templates_examples.py` | Doc-fence-as-fixture loader + 8 section tests | `def _extract_fence`, `def test_doc_fence_` | `grep -c` >=8 |
    | `docs/TEMPLATES.md` | 8-section user reference | `## ` (8x), `<!-- test:` (8x) | grep gates above |
    | `docs/PROFILE-CONFIGURATION.md` | 1-line pointer | `TEMPLATES.md` | `grep -c` >=1 |

    ## Key Links

    - `_eval_predicate` → `_IF_NOTE_TYPE_RE`, `_IF_FLAG_RE` (templates.py)
    - `validate_profile_preflight` → `_validate_predicate_flags` → `validate_template(known_flag_predicates=)` (profile.py + templates.py)
    - `render_note` (L1408) → `BlockContext(note_type=note_type, flag_predicates=_compile_flag_predicates(profile))` → `_eval_predicate`
    - `_render_moc_like` (L1669) → `BlockContext(note_type=None, flag_predicates=_compile_flag_predicates(profile))`
    - `tests/test_docs_templates_examples.py` → `docs/TEMPLATES.md` via `<!-- test:<label> -->` annotation extraction

    ## Threat Model — Final State

    | Threat ID | Disposition | Verifying Test |
    |-----------|-------------|----------------|
    | T-55-INJ (rule values not rendered) | mitigate | label-injection tests + `test_doc_fence_sanitization_renders` |
    | T-55-FLAG-VAL (DoS via large predicate_flags) | mitigate | `test_predicate_flags_max_entries_cap` (Plan 55-03) — 64-entry cap enforced |
    | T-55-NAME-COLLISION (catalog shadowing) | mitigate | `test_predicate_flags_catalog_collision_rejected`, `test_predicate_flags_attr_prefix_rejected` |

    ## Test Suite Status

    Final command: `pytest tests/ -q` → **PASSED / TOTAL** (fill from actual run; baseline Phase 54 close = 1995 passed, 0 failed; Phase 55 adds an estimated +25–35 new tests).

    ## Sign-Off

    - [x] All TMPL-01 truths verified
    - [x] All TMPL-02 sentinels GREEN (D-55.02 evidence preserved)
    - [x] D-55.03 wiring confirmed via `test_preflight_rejects_undeclared_if_flag_in_template`
    - [x] No new mandatory dependencies (per CONTEXT.md constraints)
    - [x] No `docs/MIGRATION_V1_11.md` (D-55.13 honored)
    - [x] No abort/exit-code escalation (D-55.14 honored)

    Phase 55 ready for `/gsd-verify-work`.
    ```

    Replace `<YYYY-MM-DD>` with output of `date +%Y-%m-%d` and `(N tests)` / `PASSED / TOTAL` with actual numbers from running the cited commands. Run each grep/pytest command and verify the result before marking the row ✅.
  </action>
  <verify>
    <automated>test -f .planning/phases/55-template-conditionals-connections/55-VERIFICATION.md && grep -c "TMPL-01" .planning/phases/55-template-conditionals-connections/55-VERIFICATION.md</automated>
  </verify>
  <acceptance_criteria>
    - `test -f .planning/phases/55-template-conditionals-connections/55-VERIFICATION.md` exits 0
    - `grep -c "TMPL-01" .planning/phases/55-template-conditionals-connections/55-VERIFICATION.md` returns >=1
    - `grep -c "TMPL-02" .planning/phases/55-template-conditionals-connections/55-VERIFICATION.md` returns >=1
    - All 4 sentinel test names present: `for T in test_nested_blocks_rejected_with_specific_error test_nested_if_in_if_rejected test_connections_empty_loop_renders_nothing test_block_free_template_renders_byte_identical; do grep -q "$T" .planning/phases/55-template-conditionals-connections/55-VERIFICATION.md || exit 1; done` exits 0
    - `grep -c "✅" .planning/phases/55-template-conditionals-connections/55-VERIFICATION.md` returns >=12 (every truth row checked)
    - `pytest tests/ -q` exits 0 — full suite GREEN before this verification doc is committed
  </acceptance_criteria>
  <done>55-VERIFICATION.md committed with all citations and ✅ marks backed by real command output.</done>
</task>

<task type="auto">
  <name>Task 2: Update .planning/ROADMAP.md — mark Phase 55 complete + link the 6 plan files</name>
  <files>.planning/ROADMAP.md</files>
  <read_first>
    - .planning/ROADMAP.md lines 573-595 (Phase 55 entry)
    - 55-VERIFICATION.md (just created — confirm test status before marking complete)
  </read_first>
  <action>
    Open `.planning/ROADMAP.md`. Locate the Phase 55 entry (line ~573, heading `### Phase 55: Template conditionals & connection loops`). Apply two edits:

    1. **Mark Phase 55 complete.** If the project convention uses a status emoji (e.g., ✅) before the heading or a "Status: complete" line, follow the same style used for Phase 54 (find: `grep -n "Phase 54" .planning/ROADMAP.md` and mirror its post-completion style verbatim).

    2. **Replace the `**Plans:**` placeholder line.** Currently: `**Plans:** \`.planning/phases/55-template-conditionals-connections/\` (TBD during \`/gsd-plan-phase 55\`).`

       Replace with:
       ```markdown
       **Plans:** 6 plans
       - [x] [55-01-PLAN.md](phases/55-template-conditionals-connections/55-01-PLAN.md) — Wave 0 RED scaffolds (BlockContext extension + RED tests)
       - [x] [55-02-PLAN.md](phases/55-template-conditionals-connections/55-02-PLAN.md) — `if_note_type_<X>` GREEN
       - [x] [55-03-PLAN.md](phases/55-template-conditionals-connections/55-03-PLAN.md) — `predicate_flags:` + `if_flag_<name>` GREEN
       - [x] [55-04-PLAN.md](phases/55-template-conditionals-connections/55-04-PLAN.md) — Doc-fence-as-fixture loader
       - [x] [55-05-PLAN.md](phases/55-template-conditionals-connections/55-05-PLAN.md) — `docs/TEMPLATES.md` + PROFILE-CONFIGURATION.md pointer
       - [x] [55-06-PLAN.md](phases/55-template-conditionals-connections/55-06-PLAN.md) — Verification & close-out
       ```

    3. **If a global progress table near the top of ROADMAP.md exists** (e.g., a milestone status table like the one cited in STATE.md observation 3887 for v1.7), update Phase 55's row from "Unstarted/In progress" to "Complete". Mirror the style of the most recently-completed phase (Phase 54).

    Do not touch any other phase entries.
  </action>
  <verify>
    <automated>grep -c "55-0[1-6]-PLAN.md" .planning/ROADMAP.md</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "55-0[1-6]-PLAN.md" .planning/ROADMAP.md` returns >=6
    - `grep -c "TBD during" .planning/ROADMAP.md` returns 0 for the Phase 55 line specifically (other phases may legitimately keep their TBD placeholder): `grep -A1 "Phase 55:" .planning/ROADMAP.md | grep -c "TBD"` returns 0
    - The Phase 55 entry uses the same status indicator style as Phase 54 (verify by `grep -B1 -A1 "Phase 54:" .planning/ROADMAP.md` and confirming Phase 55 mirrors it)
    - `pytest tests/ -q` exits 0 (last full sanity run)
  </acceptance_criteria>
  <done>ROADMAP.md reflects Phase 55 complete with all 6 plan links.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Project planning files (.planning/) → human readers | Documentation only; no execution. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-55-INJ | Injection | n/a (planning docs only) | accept | No untrusted input crosses any boundary in this plan. |
| T-55-FLAG-VAL | DoS | n/a | accept | This plan ships zero new code paths. |
| T-55-NAME-COLLISION | Tampering (logic) | n/a | accept | This plan does not modify production code. |
</threat_model>

<verification>
- 55-VERIFICATION.md exists, references TMPL-01, TMPL-02, all 4 Phase 31 sentinels, and the 6 plan files
- ROADMAP.md Phase 55 entry shows complete status and links 6 plans
- `pytest tests/ -q` GREEN
</verification>

<success_criteria>
- 55-VERIFICATION.md committed with grep-verified truth rows and sentinel citations
- ROADMAP.md updated: Phase 55 complete, 6 plan links, no TBD placeholder
- Full pytest suite GREEN
- Phase ready for /gsd-verify-work
</success_criteria>

<output>
After completion, create `.planning/phases/55-template-conditionals-connections/55-06-SUMMARY.md`
</output>
