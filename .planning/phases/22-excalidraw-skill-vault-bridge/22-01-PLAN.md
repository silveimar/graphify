---
plan_id: 22-01
phase: 22
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - tests/test_excalidraw_layout.py
  - tests/test_profile.py
  - tests/test_install.py
  - graphify/profile.py
  - graphify/excalidraw.py
  - graphify/skill-excalidraw.md
autonomous: true
requirements: [SKILL-04, SKILL-05, SKILL-06]
nyquist_compliant: true
must_haves:
  truths:
    - "Pure-Python `write_diagram` produces a valid `.excalidraw.md` from a SeedDict + profile, deterministic and byte-identical across runs"
    - "`layout_for` covers all 6 valid `layout_type` values and falls back to `mind-map` for unknown types"
    - "`write_diagram` refuses to overwrite existing files unless `force=True` (raises `FileExistsError`) and rejects path-traversal in `output_path` (raises `ValueError`)"
    - "`profile.validate_profile` accepts `layout_type` + `output_path` keys and rejects non-string values with clear error messages"
    - "`graphify/skill-excalidraw.md` exists in the package, contains the 7-step pipeline, references `list_diagram_seeds` + `get_diagram_seed`, includes `.mcp.json` snippet, style rules (Excalifont 5, `#1e1e2e`, transparent, `compress: false`), and the guard list (no LZ-String, no label-derived IDs, no multi-seed, no `.mcp.json` edits, no direct frontmatter writes)"
    - "LZ-String denylist test (`tests/test_denylist.py::test_no_lzstring_import_anywhere`) still passes after all Phase 22 changes"
  artifacts:
    - path: "tests/test_excalidraw_layout.py"
      provides: "Wave 0 xfail stubs + final tests for layout_for and write_diagram"
      min_lines: 60
    - path: "graphify/profile.py"
      provides: "_VALID_DT_KEYS extension (layout_type, output_path) + per-key isinstance checks + _DEFAULT_PROFILE entries"
      contains: "layout_type"
    - path: "graphify/excalidraw.py"
      provides: "_VALID_LAYOUT_TYPES set, layout_for dispatch, _layout_grid/_layout_horizontal/_layout_radial/_layout_tree helpers, write_diagram"
      contains: "def layout_for"
    - path: "graphify/skill-excalidraw.md"
      provides: "Skill prompt with frontmatter (name/description/trigger), 7-step orchestration body, .mcp.json snippet, style rules, guard list"
      min_lines: 60
  key_links:
    - from: "graphify/excalidraw.py::write_diagram"
      to: "graphify/profile.py::validate_vault_path"
      via: "path confinement before mkdir/write_text"
      pattern: "validate_vault_path"
    - from: "graphify/excalidraw.py::write_diagram"
      to: "graphify/excalidraw.py::layout_for"
      via: "dispatch on profile diagram_type.layout_type then inject elements into deepcopy(SCENE_JSON_SKELETON)"
      pattern: "layout_for\\("
    - from: "graphify/skill-excalidraw.md (step 5b)"
      to: "graphify/excalidraw.py::write_diagram (pure-Python fallback)"
      via: "skill body references the fallback path explicitly; mcp_excalidraw is optional"
      pattern: "fallback|pure-Python"
---

<objective>
Deliver the pure-Python `.excalidraw.md` fallback path (SKILL-06) AND the orchestration skill prompt (SKILL-04, SKILL-05) for Phase 22. SKILL-06 ordering invariant: the deterministic Python `layout_for` + `write_diagram` are completed and tested BEFORE any `mcp_excalidraw` content is added to the skill body. `mcp_excalidraw` is referenced ONLY inside the skill markdown — graphify Python NEVER imports it.

Purpose: graphify must be able to produce a valid, deterministic `.excalidraw.md` from a SeedDict without any external Excalidraw runtime, and ship a Claude Code skill that orchestrates the full seeds → diagram → vault pipeline.

Output:
- Extended profile schema (`layout_type`, `output_path`)
- `layout_for(layout_type, nodes, edges) -> elements[]` deterministic dispatch
- `write_diagram(vault_dir, seed, profile, force=False) -> Path` with collision refusal + path confinement
- `graphify/skill-excalidraw.md` skill prompt
- Wave 0 xfail test stubs in 3 test files; concrete green tests at end of Wave 1
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/22-excalidraw-skill-vault-bridge/22-CONTEXT.md
@.planning/phases/22-excalidraw-skill-vault-bridge/22-RESEARCH.md
@.planning/phases/22-excalidraw-skill-vault-bridge/22-PATTERNS.md
@.planning/phases/22-excalidraw-skill-vault-bridge/22-VALIDATION.md
@graphify/excalidraw.py
@graphify/profile.py
@tests/test_init_templates.py
@tests/test_profile.py
@tests/test_denylist.py

<interfaces>
<!-- Extracted from PATTERNS.md + RESEARCH.md. Executor should use these directly — no codebase exploration needed. -->

From graphify/excalidraw.py (existing — extend, don't duplicate):
```python
from graphify.profile import safe_frontmatter_value, validate_vault_path

SCENE_JSON_SKELETON: dict = {
    "type": "excalidraw",
    "version": 2,
    "source": "graphify",
    "elements": [],
    "appState": {
        "viewBackgroundColor": "#ffffff",
        "gridSize": None,
        "currentItemFontFamily": 5,  # Excalifont
    },
    "files": {},
}

def render_stub(diagram_type: dict) -> str: ...   # produces full .excalidraw.md body
def write_stubs(vault_dir, diagram_types, force=False) -> list[Path]: ...
```

From graphify/profile.py (existing — extend `_VALID_DT_KEYS` + per-entry checks at lines 367–386):
```python
_VALID_DT_KEYS = {"name", "template_path", "trigger_node_types",
                  "trigger_tags", "min_main_nodes", "naming_pattern"}

# Per-entry validators currently look like:
#   if "name" in entry and not isinstance(entry["name"], str):
#       errors.append(f"diagram_types[{i}].name must be str")

def validate_vault_path(candidate: str | Path, vault_dir: str | Path) -> Path: ...
def safe_frontmatter_value(value) -> str: ...
```

New public API to add to graphify/excalidraw.py:
```python
_VALID_LAYOUT_TYPES = {
    "architecture", "workflow", "mind-map",
    "cuadro-sinoptico", "repository-components", "glossary-graph",
}

def layout_for(
    layout_type: str,
    nodes: list[dict],   # [{"id": str, "label": str, "element_id": str}, ...]
    edges: list[dict],   # [{"source": str, "target": str, "relation": str}, ...]
) -> list[dict]:
    """Deterministic dispatch to _layout_grid/_layout_horizontal/_layout_radial/_layout_tree.
    Unknown layout_type → _layout_radial (mind-map fallback)."""

def write_diagram(
    vault_dir: str | Path,
    seed: dict,        # SeedDict from get_diagram_seed
    profile: dict,     # parsed .graphify/profile.yaml
    force: bool = False,
) -> Path:
    """Resolves layout_type from seed → matches profile.diagram_types entry →
    builds elements via layout_for → injects into deepcopy(SCENE_JSON_SKELETON) →
    renders body via the same shape as render_stub → writes via validate_vault_path.
    Raises FileExistsError on collision unless force=True.
    Raises ValueError on path-traversal in output_path."""
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1 (Wave 0): Install xfail test stubs for layout, profile schema, and skill content</name>
  <read_first>
    - tests/test_init_templates.py (analog: test_write_stubs_path_traversal_blocked at L115–118; test_write_stubs_idempotent at L82–104)
    - tests/test_profile.py (analog: test_validate_profile_unknown_key at L137–141)
    - tests/test_install.py (analog: test_codex_skill_contains_spawn_agent at L142–146)
    - .planning/phases/22-excalidraw-skill-vault-bridge/22-VALIDATION.md (Per-Task Verification Map)
    - .planning/phases/22-excalidraw-skill-vault-bridge/22-RESEARCH.md lines 471–526 (canonical test bodies)
  </read_first>
  <behavior>
    Wave 0 deliverables — every test below is created with `@pytest.mark.xfail(reason="Wave 0 stub — implementation in Wave 1", strict=True)` so the suite stays green until the real implementation lands and the xfail markers are removed.
    - tests/test_excalidraw_layout.py (NEW): test_layout_for_all_six_layout_types, test_layout_for_is_deterministic, test_layout_for_unknown_falls_back_to_mind_map, test_write_diagram_collision_refuses, test_write_diagram_path_confined, test_write_diagram_compress_false
    - tests/test_profile.py (extend): test_diagram_types_layout_type_accepted, test_diagram_types_output_path_accepted, test_diagram_types_layout_type_must_be_str, test_diagram_types_output_path_must_be_str, test_diagram_types_output_path_traversal (validator-level optional; commented or xfail)
    - tests/test_install.py (extend): test_install_excalidraw, test_uninstall_excalidraw, test_install_excalidraw_idempotent, test_uninstall_excalidraw_idempotent, test_install_excalidraw_does_not_touch_claude_skill, test_excalidraw_skill_in_package, test_platform_config_has_excalidraw, test_excalidraw_skill_has_seven_steps, test_excalidraw_skill_calls_seed_tools, test_excalidraw_skill_has_mcp_json, test_excalidraw_skill_has_style_rules, test_excalidraw_skill_has_guard_list
  </behavior>
  <action>
    1. Create `tests/test_excalidraw_layout.py` using imports `from graphify.excalidraw import layout_for, write_diagram, _VALID_LAYOUT_TYPES` (these symbols don't exist yet — that's WHY each test carries `@pytest.mark.xfail(strict=True, reason="Wave 0 stub")`). Copy the 6 canonical test bodies verbatim from 22-RESEARCH.md lines 471–526.
    2. Append to `tests/test_profile.py` the 5 schema tests listed in `<behavior>`. Each calls `from graphify.profile import validate_profile`. Wrap each with `@pytest.mark.xfail(strict=True, reason="Wave 0 stub — schema extension in Task 2")` until Task 2 lands.
    3. Append to `tests/test_install.py` the 12 install/skill-content tests listed. Use the existing `_install` helper at L19–28. For skill-content tests, read `(Path(graphify.__file__).parent / "skill-excalidraw.md").read_text()` and grep for required strings (e.g., `"list_diagram_seeds"`, `"get_diagram_seed"`, `"```jsonc"` or `"mcpServers"`, `"fontFamily: 5"`, `"compress: false"`, `"#1e1e2e"`, `"lzstring"` — assert NOT present, etc.). Use `@pytest.mark.xfail(strict=True, reason="Wave 0 stub — implementation in Tasks 3–5")`.
    4. Run `pytest tests/test_excalidraw_layout.py tests/test_profile.py tests/test_install.py -q` and confirm all new tests show as `xfailed` (expected failure), suite still green.
  </action>
  <verify>
    <automated>pytest tests/test_excalidraw_layout.py tests/test_profile.py tests/test_install.py -q</automated>
  </verify>
  <acceptance_criteria>
    - File `tests/test_excalidraw_layout.py` exists and imports `layout_for, write_diagram, _VALID_LAYOUT_TYPES` from `graphify.excalidraw`.
    - `grep -c "@pytest.mark.xfail" tests/test_excalidraw_layout.py` >= 6
    - `grep -c "test_diagram_types_layout_type_accepted\|test_diagram_types_output_path_accepted\|test_diagram_types_layout_type_must_be_str" tests/test_profile.py` == 3
    - `grep -c "test_install_excalidraw\|test_uninstall_excalidraw\|test_excalidraw_skill_" tests/test_install.py` >= 10
    - `pytest tests/test_excalidraw_layout.py tests/test_profile.py tests/test_install.py -q` exits 0 with all new tests reported as xfailed (no unexpected pass, no real failure).
    - `pytest tests/test_denylist.py::test_no_lzstring_import_anywhere -x` still passes.
  </acceptance_criteria>
  <done>Wave 0 xfail stubs landed across 3 test files; full suite green; downstream tasks can flip xfail → real pass as they implement.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Extend profile schema with `layout_type` and `output_path` (PROF-style atomic)</name>
  <read_first>
    - graphify/profile.py lines 67–95 (`_DEFAULT_PROFILE`)
    - graphify/profile.py lines 361–388 (existing `diagram_types` validator block, current `_VALID_DT_KEYS`)
    - graphify/profile.py lines 407–422 (`validate_vault_path`)
    - .planning/phases/22-excalidraw-skill-vault-bridge/22-PATTERNS.md §"graphify/profile.py — `_VALID_DT_KEYS` + `_DEFAULT_PROFILE` extension"
  </read_first>
  <action>
    1. In `graphify/profile.py`, change `_VALID_DT_KEYS` from `{"name", "template_path", "trigger_node_types", "trigger_tags", "min_main_nodes", "naming_pattern"}` to add `"layout_type", "output_path"` (8 keys total, per D-05/RESEARCH Pitfall 3).
    2. In the `validate_profile` per-entry block (mirroring existing `if "name" in entry and not isinstance(entry["name"], str)` shape), add two NEW checks:
       ```python
       if "layout_type" in entry and not isinstance(entry["layout_type"], str):
           errors.append(f"diagram_types[{i}].layout_type must be str")
       if "output_path" in entry and not isinstance(entry["output_path"], str):
           errors.append(f"diagram_types[{i}].output_path must be str")
       ```
    3. Extend each of the 6 `_DEFAULT_PROFILE["diagram_types"]` entries to include `"layout_type": "<same-as-name>"` and `"output_path": "Excalidraw/Diagrams/"`. Note: per PATTERNS.md the 6 canonical layout_type values match the 6 name values 1:1 (architecture, workflow, mind-map, cuadro-sinoptico, repository-components, glossary-graph).
    4. Path-traversal: per CONTEXT D-08 + PATTERNS, validate at WRITE time (`validate_vault_path` in `write_diagram` Task 4), NOT in the schema validator. Schema-level traversal test stays xfail or is removed in Task 1.
    5. Remove the `@pytest.mark.xfail` markers from the 4 profile tests landed in Task 1 (`test_diagram_types_layout_type_accepted`, `test_diagram_types_output_path_accepted`, `test_diagram_types_layout_type_must_be_str`, `test_diagram_types_output_path_must_be_str`).
    6. Per D-XX (CLAUDE.md): per CLAUDE.md project rules — no new required deps. No new imports. Comment in the diff cites D-05 + T-22-V5.
  </action>
  <verify>
    <automated>pytest tests/test_profile.py -x -q</automated>
  </verify>
  <acceptance_criteria>
    - `grep -E "layout_type.*output_path|output_path.*layout_type" graphify/profile.py` matches `_VALID_DT_KEYS` line.
    - `grep -c "layout_type must be str" graphify/profile.py` >= 1
    - `grep -c "output_path must be str" graphify/profile.py` >= 1
    - `grep -c "\"layout_type\":" graphify/profile.py` >= 6 (one per default diagram_type entry)
    - `pytest tests/test_profile.py::test_diagram_types_layout_type_accepted tests/test_profile.py::test_diagram_types_output_path_accepted tests/test_profile.py::test_diagram_types_layout_type_must_be_str tests/test_profile.py::test_diagram_types_output_path_must_be_str -x` passes (no longer xfailed).
    - `pytest tests/test_profile.py -q` shows all profile tests passing.
    - `pytest tests/test_denylist.py::test_no_lzstring_import_anywhere -x` still passes.
  </acceptance_criteria>
  <done>Profile schema accepts and validates `layout_type` + `output_path`; default profile carries them on all 6 entries; per-key type errors land with clear messages; T-22-V5 surface mitigated.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Implement `layout_for` dispatch + 4 deterministic layout helpers</name>
  <read_first>
    - graphify/excalidraw.py full file (especially `SCENE_JSON_SKELETON` at L32–43, `render_stub` at L46–67, `write_stubs` at L70–106)
    - .planning/phases/22-excalidraw-skill-vault-bridge/22-RESEARCH.md lines 340–375 (canonical `layout_for` signature)
    - .planning/phases/22-excalidraw-skill-vault-bridge/22-PATTERNS.md §"graphify/excalidraw.py" + §"Element ID convention"
    - tests/test_excalidraw_layout.py (the 3 layout tests landed in Task 1)
  </read_first>
  <action>
    1. In `graphify/excalidraw.py`, add module-level constant:
       ```python
       _VALID_LAYOUT_TYPES = {
           "architecture", "workflow", "mind-map",
           "cuadro-sinoptico", "repository-components", "glossary-graph",
       }
       ```
    2. Implement 4 helper functions (deterministic geometry, integer-rounded coordinates per D-02):
       - `_layout_grid(nodes, edges) -> list[dict]` — square-ish grid, columns = ceil(sqrt(n)), spacing 200px X / 150px Y, top-left at (0, 0).
       - `_layout_horizontal(nodes, edges) -> list[dict]` — single row left→right, spacing 250px X, y=0 for all (workflow / pipeline).
       - `_layout_radial(nodes, edges) -> list[dict]` — first node at (0, 0); remaining placed on a circle of radius 250 at evenly-spaced angles, integer-rounded `x = round(250*cos(theta))`, `y = round(250*sin(theta))`.
       - `_layout_tree(nodes, edges) -> list[dict]` — first node at (0, 0); subsequent nodes ranked by index → row = i // 3, col = i % 3 anchored left, spacing 200/150 (deterministic stand-in; no networkx tree-shape work in v1.5).
       Each helper produces Excalidraw `rectangle` elements (one per node) with: `id` (deterministic counter `f"elem-{i:04d}"` per PATTERNS §"Element ID convention" — NEVER label-derived), `type: "rectangle"`, `x`, `y`, `width: 160`, `height: 60`, `strokeColor: "#1e1e2e"`, `backgroundColor: "transparent"`, `fontFamily: 5`, plus a paired `text` element rendering the node label. Edges become `arrow` elements binding `startBinding.elementId` / `endBinding.elementId` to the element_ids of source/target nodes. All numeric fields integer-rounded.
    3. Implement `layout_for(layout_type, nodes, edges)` exactly per RESEARCH lines 340–375:
       ```python
       def layout_for(layout_type, nodes, edges):
           dispatch = {
               "architecture":          _layout_grid,
               "workflow":              _layout_horizontal,
               "mind-map":              _layout_radial,
               "cuadro-sinoptico":      _layout_tree,
               "repository-components": _layout_grid,
               "glossary-graph":        _layout_radial,
           }
           return dispatch.get(layout_type, _layout_radial)(nodes, edges)
       ```
    4. Determinism guarantee: do NOT use `random`, `time`, `uuid`, or dict iteration order; iterate `nodes` in the order received; counter-based element ids only.
    5. Remove `@pytest.mark.xfail` markers from `test_layout_for_all_six_layout_types`, `test_layout_for_is_deterministic`, `test_layout_for_unknown_falls_back_to_mind_map`.
    6. Per CLAUDE.md: no new required dependencies; uses `math` from stdlib only.
  </action>
  <verify>
    <automated>pytest tests/test_excalidraw_layout.py::test_layout_for_all_six_layout_types tests/test_excalidraw_layout.py::test_layout_for_is_deterministic tests/test_excalidraw_layout.py::test_layout_for_unknown_falls_back_to_mind_map -x -q</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "_VALID_LAYOUT_TYPES" graphify/excalidraw.py` >= 2 (declaration + usage)
    - `grep -E "^def (_layout_grid|_layout_horizontal|_layout_radial|_layout_tree|layout_for)" graphify/excalidraw.py` returns 5 matches.
    - `grep -c "import lzstring\|from lzstring" graphify/excalidraw.py` == 0
    - `grep -c "import random\|from random\|uuid\.\|time\." graphify/excalidraw.py` (in new helpers) == 0 — no nondeterminism sources.
    - `pytest tests/test_excalidraw_layout.py::test_layout_for_all_six_layout_types tests/test_excalidraw_layout.py::test_layout_for_is_deterministic tests/test_excalidraw_layout.py::test_layout_for_unknown_falls_back_to_mind_map -x` passes.
    - `pytest tests/test_denylist.py::test_no_lzstring_import_anywhere -x` still passes.
  </acceptance_criteria>
  <done>`layout_for` covers 6 layout types, deterministic across runs, falls back to `_layout_radial` on unknown.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 4: Implement `write_diagram` with collision refusal + path confinement (T-22-V12)</name>
  <read_first>
    - graphify/excalidraw.py L70–106 (`write_stubs` — atomic write pattern; the analog)
    - graphify/profile.py L407–422 (`validate_vault_path`)
    - .planning/phases/22-excalidraw-skill-vault-bridge/22-RESEARCH.md lines 380–460 (canonical `write_diagram` signature + body)
    - .planning/phases/22-excalidraw-skill-vault-bridge/22-PATTERNS.md §"Atomic Write w/ Force Semantics"
    - tests/test_excalidraw_layout.py (collision/traversal/compress tests landed in Task 1)
  </read_first>
  <action>
    1. In `graphify/excalidraw.py`, add `import copy` at top of file (stdlib only).
    2. Implement `write_diagram(vault_dir, seed, profile, force=False) -> Path`:
       a. Resolve `layout_type = seed.get("suggested_layout_type", "mind-map")`. If not in `_VALID_LAYOUT_TYPES`, fall back to `"mind-map"`.
       b. Find matching profile entry: `dt = next((dt for dt in profile.get("diagram_types", []) if dt.get("layout_type") == layout_type or dt.get("name") == layout_type), {})`.
       c. Compute `output_dir = dt.get("output_path") or "Excalidraw/Diagrams/"`. Compute `topic = re.sub(r"[^a-z0-9-]+", "-", safe_frontmatter_value(seed.get("main_node_label", seed.get("seed_id", "diagram"))).lower()).strip("-") or "diagram"`. Filename: `f"{topic}-{layout_type}.excalidraw.md"`. Compose `rel = f"{output_dir.rstrip('/')}/{filename}"`.
       d. **T-22-V12 path confinement (MANDATORY before any mkdir/write):** `target = validate_vault_path(rel, vault_dir)` — raises `ValueError` with "escape vault directory" on traversal.
       e. Collision refusal: `if target.exists() and not force: raise FileExistsError(f"Refusing to overwrite existing diagram: {target}. Re-run with force=True to overwrite.")`.
       f. Build elements: `nodes = seed.get("main_nodes", []) + seed.get("supporting_nodes", [])`. For each node, ensure it has `element_id` (mint deterministically as `f"elem-{i:04d}"` if absent). Pass to `elements = layout_for(layout_type, nodes, seed.get("relations", []))`.
       g. Build scene: `scene = copy.deepcopy(SCENE_JSON_SKELETON); scene["elements"] = elements`.
       h. Render body using the same shape as `render_stub` (frontmatter with `excalidraw-plugin: parsed` + `compress: false` + `## Text Elements` block + `## Drawing` block + ` ```json ` fence with `json.dumps(scene, indent=2, sort_keys=True)`). Either factor a `_render_excalidraw_md(name, scene)` helper used by both `render_stub` and `write_diagram`, OR inline the same template here. Sort keys for byte-determinism.
       i. `target.parent.mkdir(parents=True, exist_ok=True); target.write_text(body, encoding="utf-8"); return target`.
    3. Remove `@pytest.mark.xfail` markers from `test_write_diagram_collision_refuses`, `test_write_diagram_path_confined`, `test_write_diagram_compress_false`.
    4. Per CLAUDE.md: no new required deps. `re`, `copy`, `json`, `pathlib` all stdlib.
  </action>
  <verify>
    <automated>pytest tests/test_excalidraw_layout.py -x -q</automated>
  </verify>
  <acceptance_criteria>
    - `grep -E "^def write_diagram\(" graphify/excalidraw.py` returns 1 match.
    - `grep -c "validate_vault_path" graphify/excalidraw.py` >= 2 (existing + new call in write_diagram).
    - `grep -c "FileExistsError" graphify/excalidraw.py` >= 1
    - `grep -c "compress: false" graphify/excalidraw.py` >= 1 (in render template path)
    - `pytest tests/test_excalidraw_layout.py::test_write_diagram_collision_refuses tests/test_excalidraw_layout.py::test_write_diagram_path_confined tests/test_excalidraw_layout.py::test_write_diagram_compress_false -x` passes.
    - `pytest tests/test_excalidraw_layout.py -q` shows ALL tests passing (no xfailed remaining).
    - `pytest tests/test_denylist.py::test_no_lzstring_import_anywhere -x` still passes.
  </acceptance_criteria>
  <done>Pure-Python `write_diagram` end-to-end: SeedDict + profile → byte-deterministic, path-confined, collision-safe `.excalidraw.md` file. SKILL-06 ordering invariant satisfied — fallback exists and is tested BEFORE skill body references mcp_excalidraw.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 5: Author `graphify/skill-excalidraw.md` (skill prompt + .mcp.json + style + guard list)</name>
  <read_first>
    - graphify/skill.md (frontmatter shape: name/description/trigger)
    - graphify/skill-codex.md (alt-platform variant; structural reference)
    - .planning/phases/22-excalidraw-skill-vault-bridge/22-RESEARCH.md lines 528–591 (canonical skill skeleton)
    - .planning/phases/22-excalidraw-skill-vault-bridge/22-CONTEXT.md §"Skill Orchestration" (D-13, D-14) and §"Vault Write Semantics" (D-08, D-11, D-12)
    - tests/test_install.py (5 skill-content tests landed in Task 1)
  </read_first>
  <behavior>
    Skill file MUST satisfy these grep-verifiable assertions:
    - 7 numbered steps in pipeline section (regex `^[1-7]\.` matches 7 lines OR section explicitly numbers 1..7)
    - References both `list_diagram_seeds` and `get_diagram_seed`
    - `.mcp.json` snippet present (fenced code block with `"mcpServers"` key)
    - Style rules: `fontFamily: 5`, `#1e1e2e`, `transparent`, `compress: false` all present
    - Guard list: forbids LZ-String, label-derived IDs, multi-seed (v1.5), `.mcp.json` editing, direct frontmatter writes
    - Step 5 explicitly mentions BOTH paths: `mcp_excalidraw` (preferred) AND pure-Python fallback (per SKILL-06 ordering: fallback came first in Tasks 3–4, skill body now references it)
  </behavior>
  <action>
    1. Create `graphify/skill-excalidraw.md` by copying the canonical skeleton from 22-RESEARCH.md lines 528–591 verbatim. Frontmatter: `name: excalidraw-diagram`, `description: Build an Excalidraw diagram from a graphify diagram seed and write it into the Obsidian vault.`, `trigger: /excalidraw-diagram`.
    2. Body sections (per D-13, D-14, D-11, D-12):
       - `# /excalidraw-diagram` heading.
       - Required MCP servers section with fenced `jsonc` `.mcp.json` snippet listing `graphify`, `obsidian`, `excalidraw` servers (per D-11; explicitly tells user to copy themselves — graphify never edits `.mcp.json`).
       - "What I do (7 steps)" numbered list — steps 1–7 per D-13, with step 5 splitting into 5a (`mcp_excalidraw`) and 5b (pure-Python fallback via `graphify.excalidraw.write_diagram`).
       - Vault conventions: templates at `Excalidraw/Templates/{name}.excalidraw.md`, diagrams at `Excalidraw/Diagrams/{topic}-{layout_type}.excalidraw.md`, `excalidraw-plugin: parsed`, `compress: false`.
       - Style rules (locked, per D-14): `fontFamily: 5` (Excalifont), `strokeColor: "#1e1e2e"`, `backgroundColor: "transparent"`, `compress: false`.
       - Do-not / guard list (per D-12): no `lzstring` / scene compression, no label-derived element IDs, no direct frontmatter writes (use the renderer), no editing user's `.mcp.json`, no multi-seed in v1.5.
       - Collision behavior: refer to skill-level `force: true` argument (D-08), NOT a graphify CLI flag.
    3. Remove `@pytest.mark.xfail` markers from the 5 skill-content grep tests in `tests/test_install.py` (`test_excalidraw_skill_has_seven_steps`, `test_excalidraw_skill_calls_seed_tools`, `test_excalidraw_skill_has_mcp_json`, `test_excalidraw_skill_has_style_rules`, `test_excalidraw_skill_has_guard_list`) and from `test_excalidraw_skill_in_package`.
    4. NOTE on cross-plan boundary: this task creates the skill FILE only. Plan 22-02 wires it into `_PLATFORM_CONFIG` and packaging. Skill-content tests pass even before 22-02 because they read the file directly from `graphify/skill-excalidraw.md` via `Path(graphify.__file__).parent`.
  </action>
  <verify>
    <automated>pytest tests/test_install.py::test_excalidraw_skill_in_package tests/test_install.py::test_excalidraw_skill_has_seven_steps tests/test_install.py::test_excalidraw_skill_calls_seed_tools tests/test_install.py::test_excalidraw_skill_has_mcp_json tests/test_install.py::test_excalidraw_skill_has_style_rules tests/test_install.py::test_excalidraw_skill_has_guard_list -x -q</automated>
  </verify>
  <acceptance_criteria>
    - File `graphify/skill-excalidraw.md` exists.
    - `grep -c "^name: excalidraw-diagram" graphify/skill-excalidraw.md` == 1
    - `grep -c "list_diagram_seeds\|get_diagram_seed" graphify/skill-excalidraw.md` >= 2
    - `grep -c "mcpServers" graphify/skill-excalidraw.md` >= 1
    - `grep -c "fontFamily: 5\|#1e1e2e\|compress: false\|transparent" graphify/skill-excalidraw.md` >= 4
    - `grep -ci "lzstring\|label-derived\|multi-seed\|frontmatter" graphify/skill-excalidraw.md` >= 4 (guard mentions)
    - `grep -c "^[1-7]\." graphify/skill-excalidraw.md` >= 7 OR an explicit "## What I do (7 steps)" section with 7 numbered items.
    - The 6 listed pytest commands all pass.
    - `pytest tests/test_denylist.py::test_no_lzstring_import_anywhere -x` still passes (the skill mentions the word "lzstring" but it's `.md` not `.py`, so denylist scope unaffected).
  </acceptance_criteria>
  <done>Skill file shipped with full 7-step orchestration, `.mcp.json` snippet (D-11), style rules (D-14), guard list (D-12). Step 5 explicitly references the pure-Python fallback delivered in Tasks 3–4 — SKILL-06 ordering invariant honored end-to-end.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| user vault → graphify writer | Untrusted profile values (`output_path`, `layout_type`) cross into Python file-write code. |
| user → `.mcp.json` | graphify never reads/writes user `.mcp.json` (D-11 boundary preserved). |
| graphify Python → mcp_excalidraw | None — graphify Python NEVER imports mcp_excalidraw. The boundary lives entirely inside the skill markdown body. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-22-V5  | Tampering / Input Validation (V5) | `graphify/profile.py::validate_profile` | mitigate | Task 2: extend `_VALID_DT_KEYS` with `layout_type`/`output_path`; add per-key `isinstance(..., str)` checks mirroring existing entry validators (PATTERNS profile.py:367–386). Non-string values produce `errors.append("diagram_types[i].<key> must be str")`. Verified by `test_diagram_types_layout_type_must_be_str` + `test_diagram_types_output_path_must_be_str`. |
| T-22-V12 | File Handling / Path Traversal (V12) | `graphify/excalidraw.py::write_diagram` | mitigate | Task 4: every write resolves through `validate_vault_path(rel, vault_dir)` BEFORE `mkdir`/`write_text`, raising `ValueError` with "escape vault directory" on traversal. Collision: `target.exists() and not force` raises `FileExistsError` (D-08). Verified by `test_write_diagram_path_confined` + `test_write_diagram_collision_refuses`. |
| T-22-V12-LZ | File Handling — `compress: false` one-way door | `graphify/excalidraw.py` (all new code) | mitigate | No `import lzstring` introduced. Existing `tests/test_denylist.py::test_no_lzstring_import_anywhere` enforces. Re-run as part of Task 1–5 verifies. |
| T-22-V5-MCPJSON | Input Validation — `.mcp.json` boundary | `graphify/skill-excalidraw.md` | accept | Skill body documents the `.mcp.json` snippet for the user to copy themselves (D-11). graphify never reads/writes `.mcp.json` — no Python code path to attack. Guard list explicitly forbids `.mcp.json` edits. |
</threat_model>

<verification>
- All Wave 0 xfail stubs flipped to passing tests by end of Wave 1.
- `pytest tests/test_excalidraw_layout.py tests/test_profile.py tests/test_install.py -x -q` exits 0.
- `pytest tests/test_denylist.py::test_no_lzstring_import_anywhere -x` exits 0.
- `pytest tests/ -q` exits 0 (full suite green).
- `grep -rn "import lzstring\|from lzstring" graphify/` returns nothing.
- SKILL-06 ordering: `git log --oneline graphify/excalidraw.py graphify/skill-excalidraw.md` shows excalidraw.py `layout_for`/`write_diagram` commits BEFORE the skill file commits OR the skill file's mcp_excalidraw mentions sit in a section that explicitly references the (already-committed) Python fallback.
</verification>

<success_criteria>
- SKILL-06: Pure-Python `write_diagram` produces byte-deterministic, path-confined, collision-safe `.excalidraw.md` from a SeedDict + profile, fully covered by 6 unit tests.
- SKILL-04: `graphify/skill-excalidraw.md` orchestrates the 7-step seeds → diagram → vault pipeline, references `list_diagram_seeds` + `get_diagram_seed`, calls out both `mcp_excalidraw` and pure-Python fallback paths.
- SKILL-05: Skill file ships `.mcp.json` snippet (D-11), Excalifont/`#1e1e2e`/transparent/`compress: false` style rules (D-14), and guard list (no LZ-String, no label-derived IDs, no direct frontmatter writes, no `.mcp.json` edits, no multi-seed) (D-12).
- Profile schema accepts `layout_type` + `output_path` with type validation (T-22-V5 mitigated).
- LZ-String denylist test still green (compress: false one-way door preserved).
- Full pytest suite green.
</success_criteria>

<output>
After completion, create `.planning/phases/22-excalidraw-skill-vault-bridge/22-01-SUMMARY.md` documenting:
- Files created/modified with line counts
- Test count delta (before/after)
- The 6 layout helper functions and their geometry choices
- Confirmation that `mcp_excalidraw` is referenced ONLY in the skill markdown body, never imported in Python
- Hand-off notes for 22-02: skill file exists at `graphify/skill-excalidraw.md`, ready to be wired into `_PLATFORM_CONFIG` + `pyproject.toml` package-data.
</output>
