---
phase: 19
plan: 04
type: execute
wave: 3
depends_on: [19-02, 19-03]
files_modified:
  - tests/test_vault_promote.py
  - tests/fixtures/vault_promote_graph.json
  - README.md
  - graphify/skill.md
  - .planning/REQUIREMENTS.md
autonomous: true
requirements: [VAULT-01, VAULT-02, VAULT-03, VAULT-04, VAULT-05, VAULT-06, VAULT-07]
must_haves:
  truths:
    - "Multi-run idempotency is proven by a pytest that runs promote() twice and asserts overwrite-self behavior without foreign file disturbance"
    - "Heuristic dispatchers for People, Quotes, Statements each have a dedicated unit test asserting the documented regex/predicate"
    - "README.md documents the `graphify vault-promote` subcommand with a usage example"
    - "skill.md references the vault-promote subcommand so the Claude skill orchestrator can invoke it"
    - "REQUIREMENTS.md traceability rows VAULT-01..07 all have `Plan` filled in (no more `TBD`)"
  artifacts:
    - path: "tests/fixtures/vault_promote_graph.json"
      provides: "Deterministic synthetic graph exercising all 7 folders + tech detection + EXTRACTED/INFERRED/AMBIGUOUS mix"
    - path: "tests/test_vault_promote.py"
      provides: "Integration + heuristic test coverage"
      contains: "test_end_to_end_all_seven_folders"
    - path: "README.md"
      provides: "Documented subcommand"
      contains: "vault-promote"
    - path: "graphify/skill.md"
      provides: "Skill-level vault-promote reference"
      contains: "vault-promote"
  key_links:
    - from: "tests/test_vault_promote.py"
      to: "tests/fixtures/vault_promote_graph.json"
      via: "json.loads + promote() invocation"
      pattern: "vault_promote_graph.json"
    - from: "README.md §CLI"
      to: "graphify vault-promote"
      via: "doc reference"
      pattern: "vault-promote"
---

<objective>
Prove end-to-end correctness with integration tests on a realistic synthetic graph, cover each heuristic dispatcher with focused unit tests, document the subcommand in README + skill.md, and close the REQUIREMENTS.md traceability table for Phase 19.

Purpose: Plans 02+03 produced the code and unit tests. This plan demonstrates the contract holds across a realistic multi-folder run and multi-run drift scenario, and ships user-facing documentation.
Output: 1 new fixture file, ≥6 new integration/heuristic tests, README + skill.md additions, and a fully-populated REQUIREMENTS.md traceability table.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/19-vault-promotion-script-layer-b/19-CONTEXT.md
@.planning/phases/19-vault-promotion-script-layer-b/19-RESEARCH.md
@.planning/phases/19-vault-promotion-script-layer-b/19-02-SUMMARY.md
@.planning/phases/19-vault-promotion-script-layer-b/19-03-SUMMARY.md
@graphify/vault_promote.py
@tests/test_vault_promote.py
@README.md
@graphify/skill.md

<interfaces>
From Plans 02 + 03:
```python
promote(graph_path: Path, vault_path: Path, threshold: int) -> dict
# Returns: {"promoted": {type: count}, "skipped": {reason: [paths]}, "writeback": str}
```

Fixture design — the synthetic graph should exercise:
- 2 code-type god_nodes (should NOT land in Things per D-10)
- 2 document-type god_nodes (SHOULD land in Things)
- 1 isolated non-file non-concept node (→ Questions via knowledge_gaps)
- 2 communities of ≥3 nodes each (→ Maps)
- 3 unique source_file paths with `.py`, `.ts`, `.md` extensions (→ Sources + Layer 3 tech/python, tech/typescript)
- 1 node with label `Alice Smith` matching People regex
- 1 document-type node with label `"Verbatim quote"` (U+201C/U+201D) → Quotes
- 2 edges with `relation="defines"` on one node → Statements
- Edge confidence mix: 3 EXTRACTED, 2 INFERRED, 1 AMBIGUOUS (so VAULT-04 filter can be verified)
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 4.1: Integration fixture + end-to-end tests + heuristic unit coverage</name>
  <files>tests/fixtures/vault_promote_graph.json, tests/test_vault_promote.py</files>
  <read_first>
    - tests/fixtures/extraction.json (existing fixture shape — for reference on graph.json field names)
    - graphify/vault_promote.py (final module state after Plans 02+03)
    - 19-RESEARCH.md section "graph.json Schema (Verified)" and "Heuristic Dispatch: Feasibility Analysis"
    - tests/test_vault_promote.py (existing Wave 0-2 tests to avoid duplication)
  </read_first>
  <behavior>
    - `test_end_to_end_all_seven_folders`: load fixture, run promote() to tmp_path vault, assert at least one file exists in each of the 7 target folders (Things, Questions, Maps, Sources, People, Quotes, Statements)
    - `test_multi_run_drift_overwrite_self`: run promote() twice in sequence with identical graph; second run reports all prior notes as `overwritten` (NOT `skipped_user_modified`), zero `skipped_foreign`; `import-log.md` has exactly 2 `## Run` blocks latest-first
    - `test_multi_run_preserves_foreign_file`: run promote() once, then hand-create `<vault>/Atlas/Dots/Things/Handmade.md`, then run promote() again; assert Handmade.md is UNCHANGED and appears in `skipped` count as `foreign`
    - `test_multi_run_preserves_user_edit`: run promote() once, modify one promoted file's bytes, run again; assert file is UNCHANGED and appears as `skipped_user_modified`
    - `test_heuristic_people_regex`: `_is_person` returns True for `"Alice Smith"`, `"John Doe"`; False for `"alice smith"`, `"ALICE SMITH"`, `"Alice"`, `"José García"` (documented ASCII-only limitation per RESEARCH.md)
    - `test_heuristic_quote_marks`: `_has_quote_marks` returns True for labels containing each of `"`, `“`, `”`, `«`, `»`; False for plain labels
    - `test_heuristic_defines_edge`: `_has_defines_edge` returns True when any adjacent edge has `relation="defines"`; False otherwise
    - `test_tech_layer3_detection_persists_via_writeback`: after promote() on a `.py` + `.ts` fixture, `.graphify/profile.yaml` now contains `tech/python` AND `tech/typescript` in `tag_taxonomy.tech`
  </behavior>
  <action>
    1. Create `tests/fixtures/vault_promote_graph.json` as a valid NetworkX `node_link_graph` JSON (directed=False, multigraph=False). Include the 11+ nodes and 6 edges spec'd in the interfaces block. Ensure `file_type` values match `{"code", "document", "paper"}` strictly. Ensure at least one node has `source_file` as a LIST (Phase 10 D-12 shape) to exercise `_iter_sources`.
    2. Add 8 tests to `tests/test_vault_promote.py` per the behavior block above. Use `tmp_path` exclusively; load the fixture via `Path(__file__).parent / "fixtures" / "vault_promote_graph.json"`.
    3. Remove any remaining `pytest.mark.skip` markers left over from Wave 0 stubs. Every one of the 13 original tests + 8 new tests must be active.
    4. Ensure `pytest tests/test_vault_promote.py -q` exits 0 with 21 tests passing (13 original Wave 0-2 + 8 new here).
    5. Run full suite `pytest tests/ -q` and confirm zero regressions.
  </action>
  <verify>
    <automated>pytest tests/test_vault_promote.py -q && pytest tests/ -q</automated>
  </verify>
  <acceptance_criteria>
    - `test -f tests/fixtures/vault_promote_graph.json`
    - `python -c "import json; d=json.load(open('tests/fixtures/vault_promote_graph.json')); assert len(d['nodes']) >= 11 and len(d['links']) >= 6; print('OK')"` prints `OK`
    - `grep -cE "^def test_" tests/test_vault_promote.py` ≥ 21
    - `grep -c "pytest.mark.skip" tests/test_vault_promote.py` = 0
    - `pytest tests/test_vault_promote.py -q` exits 0 with ≥21 passed, 0 skipped
    - `pytest tests/ -q` exits 0 (full suite)
    - New tests reference fixture: `grep -c "vault_promote_graph.json" tests/test_vault_promote.py` ≥ 1
  </acceptance_criteria>
  <done>Full integration coverage green; heuristic dispatchers unit-tested; no skipped tests remain.</done>
</task>

<task type="auto">
  <name>Task 4.2: Documentation (README + skill.md) and REQUIREMENTS.md traceability closure</name>
  <files>README.md, graphify/skill.md, .planning/REQUIREMENTS.md</files>
  <read_first>
    - README.md (locate existing CLI/usage section; find the pattern for subcommand documentation — search for `graphify harness` and `graphify approve` as reference examples)
    - graphify/skill.md (skim — locate the section that lists available commands or capabilities; search for `approve` and `harness` references)
    - .planning/REQUIREMENTS.md (the Traceability table rows for VAULT-01..07 — Plan 01 added 06 and 07; all 7 currently say `TBD` for the Plan column)
    - 19-01-SUMMARY.md, 19-02-SUMMARY.md, 19-03-SUMMARY.md (confirm which REQ-IDs each plan closed)
  </read_first>
  <action>
    1. Edit `README.md`:
       - Locate the CLI/Commands section (near `graphify harness` / `graphify approve` if documented there; else under the top-level usage section).
       - Append a new subsection:
         ```markdown
         ### Vault Promotion — `graphify vault-promote`

         Write-only promotion of `graphify-out/graph.json` into an Obsidian vault.

         ```bash
         graphify vault-promote --vault /path/to/vault --threshold 3
         ```

         Reads `graphify-out/graph.json` and `GRAPH_REPORT.md`, classifies nodes
         into Ideaverse Pro 2.5 folders (`Atlas/Dots/{Things,Questions,Statements,People,Quotes}`,
         `Atlas/Maps`, `Atlas/Sources/Clippings`), and writes promoted notes with
         full frontmatter and a 4-namespace tag taxonomy (`garden/`, `source/`,
         `graph/`, `tech/`).

         **Write semantics:** never overwrites foreign files; overwrites self-created
         files only when `graphify-out/vault-manifest.json` hash matches disk.
         User-modified notes are preserved and recorded in `graphify-out/import-log.md`.

         **Coexists with `graphify approve`:** pick `approve` for merge-with-review,
         `vault-promote` for direct write-only. See `.planning/notes/layer-b-vault-promotion-design.md`.
         ```
    2. Edit `graphify/skill.md`:
       - Locate the section that lists callable subcommands (search for `approve` or `harness`).
       - Add a bullet / entry for `vault-promote`:
         ```markdown
         - **`graphify vault-promote --vault PATH --threshold N`** — Promote graph to Obsidian
           vault. Write-only; never overwrites foreign files; idempotent via
           `graphify-out/vault-manifest.json`. See README §Vault Promotion.
         ```
       - If skill.md has a workflow/decision section on when to use which command, add a short note: "Use `vault-promote` when the target vault is clean or only contains prior graphify output; use `approve` when manual review of merges is required."
    3. Edit `.planning/REQUIREMENTS.md` traceability table — replace the `TBD` values:
       ```
       | VAULT-01 | 19 | 19-03 | — |
       | VAULT-02 | 19 | 19-02 | — |
       | VAULT-03 | 19 | 19-02 | — |
       | VAULT-04 | 19 | 19-02 | — |
       | VAULT-05 | 19 | 19-03 | — |
       | VAULT-06 | 19 | 19-03 | — |
       | VAULT-07 | 19 | 19-01, 19-02 | — |
       ```
  </action>
  <verify>
    <automated>grep -q "graphify vault-promote" README.md && grep -q "vault-promote" graphify/skill.md && ! grep -E "^\| VAULT-0[1-7] \| 19 \| TBD \|" .planning/REQUIREMENTS.md</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "vault-promote" README.md` ≥ 3 (heading + code block + coexists note)
    - `grep -q "vault-promote" graphify/skill.md`
    - `grep -cE "^\| VAULT-0[1-7] \| 19 \| TBD \|" .planning/REQUIREMENTS.md` = 0 (no more TBD rows for VAULT-01..07)
    - `grep -cE "^\| VAULT-0[1-7] \| 19 \| 19-0[1-3]" .planning/REQUIREMENTS.md` = 7 (every row populated)
    - `grep -q "Write semantics" README.md`
  </acceptance_criteria>
  <done>README and skill.md document the subcommand; REQUIREMENTS.md traceability fully populated for Phase 19.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| test fixtures → pytest | Controlled input; tests run in tmp_path sandbox |
| docs → user | Static content; no executable paths |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-19-04-01 | Tampering | Integration test writing outside tmp_path | mitigate | All writes use `tmp_path` fixture; `validate_vault_path` enforced by `promote()` re-validates |
| T-19-04-02 | Information Disclosure | README exposing security-sensitive flags | accept | Documentation lists public CLI surface only; no secret/internal flags |
| T-19-04-03 | Denial of Service | Fixture graph too large slowing CI | accept | Fixture kept to ~12 nodes / ~6 edges — runs in < 100ms |
</threat_model>

<verification>
- `pytest tests/test_vault_promote.py -q` exits 0 with 21+ passed, 0 skipped
- `pytest tests/ -q` exits 0 full suite
- `grep -q "vault-promote" README.md` AND `grep -q "vault-promote" graphify/skill.md`
- `grep -cE "^\| VAULT-0[1-7] \| 19 \| 19-0" .planning/REQUIREMENTS.md` = 7
- Manual spot-check: `python -m graphify vault-promote --vault /tmp/spot_vault --threshold 2` on a real `graphify-out/graph.json` writes expected files and `import-log.md` with correct counts
</verification>

<success_criteria>
- All 7 VAULT REQ-IDs verified by at least one passing automated test
- `pytest tests/ -q` full suite green
- README + skill.md document the subcommand
- REQUIREMENTS.md traceability table has plan numbers for every VAULT-0* row
</success_criteria>

<output>
Create `.planning/phases/19-vault-promotion-script-layer-b/19-04-SUMMARY.md` at task completion.
</output>
