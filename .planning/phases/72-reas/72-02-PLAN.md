---
phase: 72-reas
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - graphify/skill.md
  - graphify/skill-aider.md
  - graphify/skill-claw.md
  - graphify/skill-codex.md
  - graphify/skill-copilot.md
  - graphify/skill-droid.md
  - graphify/skill-excalidraw.md
  - graphify/skill-opencode.md
  - graphify/skill-trae.md
  - graphify/skill-windows.md
  - graphify/prompts.py
  - tests/test_skill_prompt_drift.py
autonomous: true
requirements: [REAS-02]

must_haves:
  truths:
    - "All 10 skill*.md files contain identical reasoning-relation taxonomy block (5 relations + ADR exemplars)"
    - "ADR-0042 supersedes ADR-0028 worked example present in every skill file"
    - "ADR contradiction worked example present in every skill file"
    - "PROMPT_VERSION bumped from 1.13.0 to 1.14.0 (or 2.0.0)"
    - "tests/test_skill_prompt_drift.py asserts ADR exemplar parity across all 10 skill files"
  artifacts:
    - path: "graphify/skill.md"
      provides: "Step 3 Part B reasoning taxonomy block"
      contains: "ADR-0042 supersedes ADR-0028"
    - path: "graphify/prompts.py"
      provides: "PROMPT_VERSION bump"
      contains: "1.14.0"
  key_links:
    - from: "graphify/skill.md Step 3 Part B"
      to: "graphify/skill-{aider,claw,codex,copilot,droid,excalidraw,opencode,trae,windows}.md"
      via: "skill-prompt-drift gate"
      pattern: "ADR-0042 supersedes ADR-0028"
---

<objective>
Extend the semantic-extraction prompt across all 10 skill files (D-01) with the 5-relation taxonomy and two ADR worked exemplars (D-02: supersession + contradiction). Bump `PROMPT_VERSION` so the confidence cache key invalidates and the new prompt re-extracts on the next run. Extend the drift gate test to cover the new ADR exemplar phrases so future drift fails CI.

Purpose: REAS-02 requires the LLM to emit reasoning-relation edges with `confidence` and `confidence_score`. The prompt body lives in skill files (NOT extract.py); skill-prompt-drift gate enforces parity across 10 platform variants.
Output: 10 skill files updated in lockstep, prompts.py PROMPT_VERSION bumped, drift test extended.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/72-reas/72-CONTEXT.md
@.planning/phases/72-reas/72-RESEARCH.md
@graphify/skill.md
@graphify/prompts.py
@tests/test_skill_prompt_drift.py
@CLAUDE.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Author the canonical reasoning-relation prompt block in graphify/skill.md Step 3 Part B</name>
  <read_first>
    - graphify/skill.md (Step 3 Part B, approximately lines 260-440 — locate the relation taxonomy / extraction instructions block)
    - .planning/phases/72-reas/72-CONTEXT.md (D-01, D-02, D-03)
    - .planning/phases/72-reas/72-RESEARCH.md (the "non-obvious finding" about all 10 skill files; Pattern 4 supersedes orientation)
    - docs/RELATIONS.md (Phase 72 section authored in Plan 01 — for definitional consistency)
  </read_first>
  <files>graphify/skill.md, graphify/prompts.py</files>
  <action>
    1. Locate the existing relation-taxonomy / edge-emission instructions in `graphify/skill.md` Step 3 Part B (the subagent prompt that extracts nodes/edges from documents/papers/rationales — approximately lines 260-440).
    2. Add a new subsection titled `### Reasoning relations (documents/papers/rationales only)` containing:
       - One-line definitions for the 5 relations (verbatim from docs/RELATIONS.md):
         - `supports` — Source asserts evidence in favor of target.
         - `contradicts` — Source asserts target is false or incompatible.
         - `supersedes` — Source replaces target as canonical. **Orientation: newer -> older. Target is the superseded (deprecated) node.**
         - `evolved_into` — Source is an earlier version that became target. Direction: older -> newer.
         - `depends_on` — Source's claim relies on target's claim.
       - Endpoint constraint: "Both `source` and `target` MUST be document/paper/rationale nodes (or document-derived concept nodes). DO NOT emit reasoning relations on code-typed nodes — they will be rejected by validate.py."
       - Confidence rules (CCONF v1.13): "Emit `confidence` (`EXTRACTED` / `INFERRED` / `AMBIGUOUS`). For `INFERRED`, emit `confidence_score` as a float in `[0.0, 1.0]`. EXTRACTED reasoning edges may omit `confidence_score`."
       - Worked example 1 — ADR supersession (D-02):
         ```
         Input fragment (in docs/adr/0042-new-auth.md):
           "This ADR supersedes ADR-0028 (token-based auth)."
         Emit:
           {"source": "adr_0042", "target": "adr_0028", "relation": "supersedes",
            "confidence": "EXTRACTED", "source_file": "docs/adr/0042-new-auth.md"}
         ```
       - Worked example 2 — ADR contradiction (D-02):
         ```
         Input fragment (in docs/adr/0050-revisit.md):
           "This proposal directly contradicts ADR-0042's claim that JWTs cannot be revoked."
         Emit:
           {"source": "adr_0050", "target": "adr_0042", "relation": "contradicts",
            "confidence": "INFERRED", "confidence_score": 0.85,
            "source_file": "docs/adr/0050-revisit.md"}
         ```
       - Resolution note: "If the textual reference (`ADR-0028`) does not match a node id, the build layer will resolve it by label or substring. Emit the human-readable label or partial id as `target` — do not invent ids."
    3. In `graphify/prompts.py`, bump `PROMPT_VERSION` from `"1.13.0"` to `"1.14.0"`. Single-line change.

    Mark the new prompt block with HTML comments as bookends so Task 2 can copy it deterministically:
    ```
    <!-- BEGIN: phase-72-reas reasoning-relations block -->
    ...content...
    <!-- END: phase-72-reas reasoning-relations block -->
    ```
  </action>
  <verify>
    <automated>grep -q "Reasoning relations (documents/papers/rationales only)" graphify/skill.md &amp;&amp; grep -q "ADR-0042" graphify/skill.md &amp;&amp; grep -q "adr_0050" graphify/skill.md &amp;&amp; grep -q '"1.14.0"' graphify/prompts.py</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "BEGIN: phase-72-reas reasoning-relations block" graphify/skill.md` == 1
    - `grep -c "END: phase-72-reas reasoning-relations block" graphify/skill.md` == 1
    - `grep -q "newer -> older" graphify/skill.md` matches
    - `grep -q "ADR-0042.*supersedes.*ADR-0028" graphify/skill.md` OR ADR-0042 and ADR-0028 both appear within the block
    - `grep -q "contradicts" graphify/skill.md` AND `grep -q "evolved_into" graphify/skill.md` AND `grep -q "depends_on" graphify/skill.md`
    - `grep -q '"1.14.0"' graphify/prompts.py` matches; `grep -c '"1.13.0"' graphify/prompts.py` == 0
    - `python -c "from graphify.prompts import PROMPT_VERSION; assert PROMPT_VERSION == '1.14.0'"` exits 0
  </acceptance_criteria>
  <done>skill.md contains the canonical reasoning-relation block bounded by BEGIN/END markers, and PROMPT_VERSION is bumped to 1.14.0.</done>
</task>

<task type="auto">
  <name>Task 2: Replicate the reasoning-relation block to the 9 platform skill variants and extend drift gate</name>
  <read_first>
    - graphify/skill.md (the BEGIN/END block authored in Task 1 — this is the source of truth)
    - graphify/skill-codex.md (reference for variant insertion location — find equivalent Step 3 Part B section)
    - tests/test_skill_prompt_drift.py (full file — understand current parity assertions and how the gate compares files)
    - .planning/phases/72-reas/72-RESEARCH.md (Anti-pattern: "Hand-editing one skill*.md and forgetting the other 9")
  </read_first>
  <files>graphify/skill-aider.md, graphify/skill-claw.md, graphify/skill-codex.md, graphify/skill-copilot.md, graphify/skill-droid.md, graphify/skill-excalidraw.md, graphify/skill-opencode.md, graphify/skill-trae.md, graphify/skill-windows.md, tests/test_skill_prompt_drift.py</files>
  <action>
    1. For each of the 9 platform variants (`skill-aider.md`, `skill-claw.md`, `skill-codex.md`, `skill-copilot.md`, `skill-droid.md`, `skill-excalidraw.md`, `skill-opencode.md`, `skill-trae.md`, `skill-windows.md`):
       - Locate the equivalent Step 3 Part B section (the section that mirrors `graphify/skill.md` Step 3 Part B in that platform's variant).
       - Insert the IDENTICAL `<!-- BEGIN: phase-72-reas reasoning-relations block --> ... <!-- END: phase-72-reas reasoning-relations block -->` content from `skill.md`, character-for-character within the block bounds. The surrounding platform-specific framing may differ; the block content must NOT.
    2. Run `pytest tests/test_skill_prompt_drift.py -q` to confirm the existing drift gate still passes after the inserts.
    3. Extend `tests/test_skill_prompt_drift.py`:
       - Add a test `test_reasoning_relations_block_parity` that reads the block content from `skill.md` between the BEGIN/END markers and asserts each of the 9 variant files contains the exact same block content between matching markers.
       - Add a test `test_prompt_version_bumped` that asserts `from graphify.prompts import PROMPT_VERSION; PROMPT_VERSION == "1.14.0"`.
       - Add a test `test_adr_supersession_exemplar_present` that asserts every skill*.md file contains both "ADR-0042" and "ADR-0028" within the BEGIN/END block.
  </action>
  <verify>
    <automated>pytest tests/test_skill_prompt_drift.py -x -q</automated>
  </verify>
  <acceptance_criteria>
    - For each of the 10 skill*.md files: `grep -c "BEGIN: phase-72-reas reasoning-relations block" {file}` == 1 AND `grep -c "END: phase-72-reas reasoning-relations block" {file}` == 1
    - For each of the 10 skill*.md files: file contains "ADR-0042" AND "ADR-0028" AND "newer -> older"
    - `pytest tests/test_skill_prompt_drift.py::test_reasoning_relations_block_parity tests/test_skill_prompt_drift.py::test_prompt_version_bumped tests/test_skill_prompt_drift.py::test_adr_supersession_exemplar_present -x -q` passes
    - `pytest tests/test_skill_prompt_drift.py -x -q` passes overall (no regression)
    - Inducing a one-character diff in any single variant's block causes `test_reasoning_relations_block_parity` to fail (manual gate verification — A4 mitigation)
  </acceptance_criteria>
  <done>All 10 skill files have identical reasoning-relation blocks; drift gate extended with 3 new tests asserting parity, exemplar presence, and PROMPT_VERSION bump.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| skill prompt -> LLM extractor | Prompt body shapes LLM emission; drift between platforms causes silent extraction divergence. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-72-04 | Tampering / Repudiation | skill*.md drift | mitigate | BEGIN/END markers + test_skill_prompt_drift parity assertion across all 10 files. |
| T-72-05 | Information disclosure (stale cache) | confidence cache | mitigate | PROMPT_VERSION bump invalidates `sha256(PROMPT_VERSION || model_id || file_hash)` cache key. |
</threat_model>

<verification>
- All 10 skill files contain the BEGIN/END-bounded reasoning block with identical content.
- PROMPT_VERSION is "1.14.0".
- `pytest tests/test_skill_prompt_drift.py -x -q` passes including 3 new tests.
</verification>

<success_criteria>
- REAS-02 prompt extension shipped consistently across all platform skill variants.
- Cache invalidation guaranteed via PROMPT_VERSION bump.
- Drift gate hardened with new parity assertions for the reasoning block.
</success_criteria>

<output>
After completion, create `.planning/phases/72-reas/72-02-SUMMARY.md`.
</output>
