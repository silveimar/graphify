---
phase: 72-reas
plan: 04
type: execute
wave: 3
depends_on: ["72-01", "72-02", "72-03"]
files_modified:
  - graphify/analyze.py
  - graphify/report.py
  - graphify/wiki.py
  - graphify/export.py
  - tests/test_analyze.py
  - tests/test_report.py
  - tests/test_wiki.py
  - tests/test_export.py
  - tests/fixtures/adr_supersession.md
  - tests/fixtures/adr_contradiction.md
autonomous: true
requirements: [REAS-03, REAS-04]

must_haves:
  truths:
    - "analyze.py knowledge_gaps does NOT classify isolated reasoning-edge endpoints as gaps"
    - "analyze.py has a contradictions_and_chains() producer returning {contradictions, supersession_chains}"
    - "Confidence gate is: EXTRACTED always included; INFERRED requires confidence_score >= 0.5"
    - "Longest supersession chains rendered first; contradictions sorted by confidence_score desc"
    - "GRAPH_REPORT.md has a top-level '## Contradictions and Supersession Chains' section"
    - "wiki.py renders a '## Reasoning Relations' subsection above '## Relationships' and above '## Historical relations'; subsection omitted when empty"
    - "Obsidian export note frontmatter contains a 'reasoning_relations' YAML list with [{type, target, confidence_score}] items"
  artifacts:
    - path: "graphify/analyze.py"
      provides: "contradictions_and_chains() + knowledge_gaps reasoning-edge filter"
      contains: "contradictions_and_chains"
    - path: "graphify/report.py"
      provides: "Contradictions and Supersession Chains section"
      contains: "Contradictions and Supersession Chains"
    - path: "graphify/wiki.py"
      provides: "Reasoning Relations subsection"
      contains: "Reasoning Relations"
    - path: "graphify/export.py"
      provides: "Obsidian reasoning_relations frontmatter"
      contains: "reasoning_relations"
  key_links:
    - from: "graphify/report.py"
      to: "graphify/analyze.py contradictions_and_chains"
      via: "import + call"
      pattern: "contradictions_and_chains"
    - from: "graphify/wiki.py _community_article"
      to: "graphify/validate.py REASONING_RELATIONS"
      via: "import + filter on edge.relation"
      pattern: "REASONING_RELATIONS"
    - from: "graphify/export.py classify"
      to: "Obsidian frontmatter reasoning_relations key"
      via: "per-node ctx injection + template render"
      pattern: "reasoning_relations"
---

<objective>
Land the rendering and analysis surface for reasoning relations:

- **REAS-03 (analyze.py):** Fix `knowledge_gaps` so isolated reasoning-edge endpoints are NOT misclassified as gaps (D-13 — predicate at lines 680-684). Add a new `contradictions_and_chains()` producer with confidence gate `EXTRACTED OR confidence_score >= 0.5` (D-11), longest-chains-first sorting (D-12), and cycle handling.
- **REAS-04 (report.py / wiki.py / export.py):**
  - `report.py` — new top-level "## Contradictions and Supersession Chains" section in GRAPH_REPORT.md (D-16).
  - `wiki.py` — new `## Reasoning Relations` subsection in per-community articles, placed above `## Relationships` and above `## Historical relations`, omitted when empty (D-14).
  - `export.py` — Obsidian export note frontmatter gains `reasoning_relations: [{type, target, confidence_score}]` YAML list (D-15).

Purpose: Surface reasoning relations distinguishably from structural relations across all output formats. Without this, the schema/build work in Plans 01-03 is invisible to users.
Output: 4 source files updated, 7 unit tests added, 2 ADR markdown fixtures.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/72-reas/72-CONTEXT.md
@.planning/phases/72-reas/72-RESEARCH.md
@graphify/analyze.py
@graphify/report.py
@graphify/wiki.py
@graphify/export.py
@graphify/validate.py
@CLAUDE.md
</context>

<interfaces>
From graphify/validate.py (Plan 01):
```python
REASONING_RELATIONS: frozenset[str]
```

From graphify/build.py (Plan 03): every edge carries `_src` and `_tgt` attributes for direction recovery on the undirected `nx.Graph` (Phase 53 W2 invariant verified at build.py:395-396).

Existing landmarks:
- `analyze.py:140-164` — `_is_concept_node`, `_is_file_node`
- `analyze.py:480-520` — knowledge_gaps "thin community" logic
- `analyze.py:661-685` — knowledge_gaps with isolated filter at 680-684 (predicate to extend)
- `wiki.py:89-110` — Phase 71-05 `## Historical relations` subsection (precedent shape)
- `wiki.py:112` — `html.escape(...)[:64]` precedent for label sanitization
- `report.py:434-447` — Temporal Health top-level section (precedent shape)
- `export.py:571+` — `to_obsidian` profile pipeline (`load_profile -> classify -> render_all -> split_rendered_note -> apply_merge_plan`)
- `export.py:821` — `split_rendered_note` public helper (handles arbitrary frontmatter keys)
</interfaces>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: analyze.py — fix knowledge_gaps reasoning-edge filter and add contradictions_and_chains()</name>
  <read_first>
    - graphify/analyze.py (lines 140-164 for `_is_concept_node`/`_is_file_node`; 480-520 for thin-community logic; 661-685 for the knowledge_gaps function and the exact predicate at 680-684)
    - graphify/validate.py (REASONING_RELATIONS)
    - .planning/phases/72-reas/72-RESEARCH.md (Code Examples §contradictions_and_chains; Pitfalls 5, 6, 7; Open Question 3 on contradicts dedup)
    - tests/test_analyze.py (existing knowledge_gaps test patterns)
  </read_first>
  <files>graphify/analyze.py, tests/test_analyze.py</files>
  <behavior>
    - Test: `knowledge_gaps` does NOT include a doc node with degree 1 whose only edge is `supports` (REAS-03 fix)
    - Test: `contradictions_and_chains` returns dict with keys `"contradictions"` and `"supersession_chains"`
    - Test: chains sorted longest-first; contradictions sorted by confidence_score descending
    - Test: confidence gate excludes INFERRED edges with `confidence_score=0.3`; includes EXTRACTED edges that omit `confidence_score`; includes INFERRED with `confidence_score=0.5`
    - Test: cycle in supersedes (A->B and B->A) is handled without crashing — emits stderr warning, returns truncated chains
    - Test: contradicts edges deduplicated by `frozenset({source,target})`, keeping the higher confidence_score (Open Question 3 recommendation)
  </behavior>
  <action>
    1. In `graphify/analyze.py`, add module-level helper after `_is_concept_node`/`_is_file_node`:
       ```python
       def _has_reasoning_edge(G, n) -> bool:
           from .validate import REASONING_RELATIONS
           return any(
               G.edges[n, m].get("relation") in REASONING_RELATIONS
               for m in G.neighbors(n)
           )
       ```
    2. Modify `knowledge_gaps` at lines 680-684. The current isolated-filter predicate is `G.degree(n) <= 1 and not _is_file_node(...) and not _is_concept_node(...)`. Add a third negated disjunct:
       ```python
       if (
           G.degree(n) <= 1
           and not _is_file_node(G, n)
           and not _is_concept_node(G, n)
           and not _has_reasoning_edge(G, n)
       ):
           ...
       ```
       (Match the exact existing structure; do not refactor the surrounding code.)
    3. Add a new public function `contradictions_and_chains(G)` per RESEARCH.md Code Examples §contradictions_and_chains (verbatim, with the Open-Question-3 contradicts-dedup recommendation applied):
       - Filter edges to `REASONING_RELATIONS` only.
       - Confidence gate: `confidence == "EXTRACTED" OR (confidence_score is not None AND float(confidence_score) >= 0.5)` — EXTRACTED edges always included regardless of score (Pitfall 6).
       - Honor `_src`/`_tgt` attrs to recover direction on the undirected G.
       - Build a `nx.DiGraph` for `supersedes`-only edges; enumerate maximal directed paths from in-degree-0 heads via DFS with `if s in path: continue` cycle guard. Wrap in `try/except nx.NetworkXUnfeasible:` (Pitfall 7) — on cycle, emit stderr `[graphify] supersession cycle detected — chain output truncated` and return the chains accumulated so far.
       - Collect `contradicts` edges into a list of dicts `{a, b, confidence_score, source_file}`. Deduplicate by `frozenset({a, b})` keeping higher `confidence_score` (Open Question 3).
       - Output shape per RESEARCH.md.
       - Sort: chains by `-length`; contradictions by `-confidence_score`. No top-N cap (D-12).
    4. Add the 6 tests in `tests/test_analyze.py`:
       - `test_knowledge_gaps_excludes_reasoning_endpoints`
       - `test_contradictions_and_chains_shape`
       - `test_chain_sort_longest_first`
       - `test_confidence_gate_excludes_low_inferred`
       - `test_supersession_cycle_handled` (use `capsys` for stderr)
       - `test_contradicts_dedup_keeps_higher_score`
    5. Create test fixtures `tests/fixtures/adr_supersession.md` and `tests/fixtures/adr_contradiction.md` — minimal markdown ADR snippets used by Tasks 2-4 below. Each <= 30 lines. Content per RESEARCH.md exemplars:
       - `adr_supersession.md`: front matter + body referencing "This ADR supersedes ADR-0028"
       - `adr_contradiction.md`: front matter + body containing "This proposal directly contradicts ADR-0042's claim..."
  </action>
  <verify>
    <automated>pytest tests/test_analyze.py::test_knowledge_gaps_excludes_reasoning_endpoints tests/test_analyze.py::test_contradictions_and_chains_shape tests/test_analyze.py::test_chain_sort_longest_first tests/test_analyze.py::test_confidence_gate_excludes_low_inferred tests/test_analyze.py::test_supersession_cycle_handled tests/test_analyze.py::test_contradicts_dedup_keeps_higher_score -x -q</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "_has_reasoning_edge" graphify/analyze.py` >= 2 (definition + use in knowledge_gaps)
    - `grep -c "def contradictions_and_chains" graphify/analyze.py` == 1
    - `grep -q "supersession cycle detected" graphify/analyze.py` matches (cycle warning string)
    - `grep -q "REASONING_RELATIONS" graphify/analyze.py` matches (import from validate)
    - All 6 new tests pass
    - `pytest tests/test_analyze.py -x -q` passes (no regression on existing knowledge_gaps tests)
    - Both fixture files exist: `tests/fixtures/adr_supersession.md` and `tests/fixtures/adr_contradiction.md`
  </acceptance_criteria>
  <done>analyze.py exports `contradictions_and_chains`, fixes the gaps filter, and 6 tests pass.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: report.py — emit "## Contradictions and Supersession Chains" top-level section</name>
  <read_first>
    - graphify/report.py (lines 420-460: Temporal Health section as precedent; locate the `generate(...)` function and its existing section ordering)
    - graphify/analyze.py (`contradictions_and_chains` from Task 1)
    - .planning/phases/72-reas/72-RESEARCH.md (Code Examples §GRAPH_REPORT.md section)
    - tests/test_report.py (existing test conventions; how it invokes `generate`)
  </read_first>
  <files>graphify/report.py, tests/test_report.py</files>
  <behavior>
    - Test: GRAPH_REPORT.md generated from a graph with one supersession chain contains "## Contradictions and Supersession Chains" and "### Supersession Chains (longest first)"
    - Test: GRAPH_REPORT.md from a graph with one contradiction contains "### Contradiction Pairs (highest confidence first)"
    - Test: Section is OMITTED entirely when neither contradictions nor chains exist
  </behavior>
  <action>
    1. In `graphify/report.py::generate`, INSERT the new section per RESEARCH.md Code Examples §GRAPH_REPORT.md (verbatim). Place it BEFORE or AFTER the existing "## Temporal Health" block (choose placement consistent with section ordering in the existing file). Use `_sanitize_md` (existing helper) on labels.
    2. Add the section ONLY when `_cc["contradictions"] or _cc["supersession_chains"]` is non-empty (omit-when-empty rule).
    3. Add the 1 test in `tests/test_report.py`:
       - `test_contradictions_section` — build a graph with both a 3-node supersession chain and one contradiction; assert the generated report contains both subsections in correct order; also test the omit-when-empty case (build a graph with no reasoning edges; assert "Contradictions and Supersession Chains" string is absent).
  </action>
  <verify>
    <automated>pytest tests/test_report.py::test_contradictions_section -x -q</automated>
  </verify>
  <acceptance_criteria>
    - `grep -q "Contradictions and Supersession Chains" graphify/report.py` matches
    - `grep -q "Supersession Chains (longest first)" graphify/report.py` matches
    - `grep -q "Contradiction Pairs (highest confidence first)" graphify/report.py` matches
    - `grep -q "from .analyze import contradictions_and_chains" graphify/report.py` OR similar import is present
    - `test_contradictions_section` passes including the omit-when-empty assertion
    - `pytest tests/test_report.py -x -q` passes (no regression)
  </acceptance_criteria>
  <done>GRAPH_REPORT.md gains the new section conditionally; test asserts both presence and omission cases.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: wiki.py — emit "## Reasoning Relations" subsection above Relationships and Historical relations</name>
  <read_first>
    - graphify/wiki.py (full file; especially lines 70-110 — `_community_article` shape, current section ordering, Phase 71-05 `## Historical relations` precedent at lines 89-110)
    - graphify/validate.py (REASONING_RELATIONS)
    - .planning/phases/72-reas/72-RESEARCH.md (Code Examples §wiki subsection — exact insertion code)
    - .planning/phases/72-reas/72-CONTEXT.md (D-14 placement: above ## Relationships AND above ## Historical relations)
    - tests/test_wiki.py (existing community-article test conventions)
  </read_first>
  <files>graphify/wiki.py, tests/test_wiki.py</files>
  <behavior>
    - Test: A community whose nodes have outbound `supports` edges produces an article containing "## Reasoning Relations" placed BEFORE both "## Relationships" and "## Historical relations"
    - Test: A community with NO reasoning edges produces an article that does NOT contain "## Reasoning Relations" (omit-when-empty per Phase 71-05 mirror)
    - Test: Neighbor labels in the subsection are HTML-escaped (security mirror of T-71-15)
  </behavior>
  <action>
    1. In `graphify/wiki.py::_community_article`, INSERT the Reasoning Relations subsection per RESEARCH.md Code Examples §wiki subsection (verbatim). Insertion point: BEFORE the existing "## Relationships" header line. Critical: verify by reading current file — placement MUST be above both `## Relationships` and `## Historical relations`.
    2. Reuse `html.escape(neighbor_label)[:64]` (existing pattern at line 112) for label sanitization.
    3. Honor `_src`/`_tgt` to recover direction on undirected G.
    4. Format: `- {rel}: [[{safe_label}]]` with optional ` (confidence {score:.2f})` suffix when `confidence_score` is present.
    5. Omit the entire subsection (do not emit the `## Reasoning Relations` header) when the filtered list is empty.
    6. Add 2 tests in `tests/test_wiki.py`:
       - `test_reasoning_relations_subsection` — community with reasoning edges; assert presence + ordering relative to other sections (use string `.find()` indices)
       - `test_reasoning_relations_omit_when_empty` — community without reasoning edges; assert "## Reasoning Relations" NOT in article output
  </action>
  <verify>
    <automated>pytest tests/test_wiki.py::test_reasoning_relations_subsection tests/test_wiki.py::test_reasoning_relations_omit_when_empty -x -q</automated>
  </verify>
  <acceptance_criteria>
    - `grep -q "## Reasoning Relations" graphify/wiki.py` matches
    - `grep -q "REASONING_RELATIONS" graphify/wiki.py` matches (import + filter)
    - `grep -q "html.escape" graphify/wiki.py` matches (label sanitization preserved)
    - Both new tests pass; the ordering assertion proves Reasoning Relations appears before Relationships AND before Historical relations (`.find("## Reasoning") < .find("## Relationships") < .find("## Historical relations")`)
    - `pytest tests/test_wiki.py -x -q` passes (no regression on Phase 71-05 Historical relations tests)
  </acceptance_criteria>
  <done>wiki community articles render Reasoning Relations subsection above existing sections; omit-when-empty rule honored.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 4: export.py — emit `reasoning_relations` YAML list in Obsidian note frontmatter</name>
  <read_first>
    - graphify/export.py (lines 571-905: `to_obsidian`, profile pipeline, `classify`, `render_all`, `split_rendered_note`, `apply_merge_plan`)
    - graphify/builtin_templates/ (template files used by the default profile — locate the per-node note template that emits frontmatter)
    - graphify/validate.py (REASONING_RELATIONS)
    - .planning/phases/72-reas/72-RESEARCH.md (Pattern 5 with both classify-side and template-side recommendations)
    - .planning/phases/72-reas/72-CONTEXT.md (D-15 shape: `reasoning_relations: [{type, target, confidence_score}]`)
    - tests/test_export.py (existing Obsidian export test conventions; how `split_rendered_note` is exercised)
  </read_first>
  <files>graphify/export.py, tests/test_export.py</files>
  <behavior>
    - Test: A doc node with two outbound reasoning edges (`supports -> Concept X` confidence_score=0.8, `supersedes -> ADR-0028` EXTRACTED) renders an Obsidian note whose frontmatter contains a `reasoning_relations:` YAML list with both items
    - Test: Each item has keys `type`, `target`, `confidence_score`. `target` is the bare label (Open Question 2 recommendation: bare label, not wikilink-wrapped). `confidence_score` is omitted (or null) when not present on the source edge.
    - Test: A doc node with NO outbound reasoning edges produces a note WITHOUT a `reasoning_relations` key (or with an empty list, per implementation choice — assert chosen behavior)
    - Test: Frontmatter round-trips through `split_rendered_note` without loss
  </behavior>
  <action>
    1. In `graphify/export.py::classify` (the function that builds `per_node` ctx for template rendering, ~lines 780-900), inject `reasoning_relations` into each per-node ctx dict for doc/concept nodes:
       ```python
       from .validate import REASONING_RELATIONS
       # For each node in this community:
       reasoning_items = []
       for nb in G.neighbors(node_id):
           ed = G.edges[node_id, nb]
           rel = ed.get("relation")
           if rel not in REASONING_RELATIONS:
               continue
           # Honor direction: only emit when this node is the source
           src = ed.get("_src", node_id)
           if src != node_id:
               continue
           tgt = ed.get("_tgt", nb)
           tgt_label = G.nodes[tgt].get("label", tgt)
           item = {"type": rel, "target": tgt_label}
           score = ed.get("confidence_score")
           if isinstance(score, (int, float)):
               item["confidence_score"] = float(score)
           reasoning_items.append(item)
       if reasoning_items:
           per_node_ctx["reasoning_relations"] = reasoning_items
       ```
       (Adapt variable names to existing `classify` locals; the precise function shape is in export.py.)
    2. Update the default note template under `graphify/builtin_templates/` (the per-node template that renders frontmatter YAML) to emit the `reasoning_relations:` key when the ctx contains it. Use `yaml.safe_dump` (T-72 mitigation per RESEARCH.md security domain) on the list, OR emit hand-written YAML using only escaped string values. Omit the key entirely when the ctx list is empty.
    3. Verify there is no collision with existing vault-profile templates (`grep -r "reasoning_relations" graphify/builtin_templates/` should return 0 hits before edit).
    4. Add 1 test in `tests/test_export.py`:
       - `test_obsidian_reasoning_relations_frontmatter` — build a small graph with reasoning edges, run `to_obsidian` against `tmp_path`, parse the resulting note's frontmatter (use the existing `split_rendered_note` helper or `yaml.safe_load`), assert `reasoning_relations` is a list with the expected items including `type`, `target`, `confidence_score`. Also assert that a node WITHOUT reasoning edges has no `reasoning_relations` key.
  </action>
  <verify>
    <automated>pytest tests/test_export.py::test_obsidian_reasoning_relations_frontmatter -x -q</automated>
  </verify>
  <acceptance_criteria>
    - `grep -q "reasoning_relations" graphify/export.py` matches (classify-side injection)
    - `grep -q "REASONING_RELATIONS" graphify/export.py` matches (import + filter)
    - `grep -rq "reasoning_relations" graphify/builtin_templates/` matches (template renders the key)
    - The test asserts `yaml.safe_load(frontmatter)["reasoning_relations"]` is a list with the expected items
    - The test asserts an empty-reasoning node has NO `reasoning_relations` key (or empty list — match implementation)
    - `pytest tests/test_export.py -x -q` passes (no regression on existing Obsidian export tests)
  </acceptance_criteria>
  <done>Obsidian export carries reasoning relations distinguishably from structural relations via frontmatter YAML list; round-trip test passes.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| analyze.py / report.py / wiki.py / export.py -> rendered output | LLM-derived neighbor labels and target strings reach markdown / YAML / HTML output. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-72-10 | DoS | analyze.py contradictions_and_chains | mitigate | Cycle guard `if s in path: continue`; `try/except nx.NetworkXUnfeasible`. Tested by `test_supersession_cycle_handled`. |
| T-72-11 | Tampering | wiki.py reasoning subsection | mitigate | `html.escape(neighbor_label)[:64]` (T-71-15 precedent). Tested by `test_reasoning_relations_subsection`. |
| T-72-12 | Tampering | export.py YAML frontmatter injection | mitigate | `yaml.safe_dump` on the list; coerce values to `str` / `float` before serialization. No `yaml.dump`. |
| T-72-13 | Information disclosure | report.py source_file path leakage | accept | Source file paths are project-relative and intentional (existing pattern in Temporal Health section). |
</threat_model>

<verification>
- All 10 new tests across test_analyze.py / test_report.py / test_wiki.py / test_export.py pass.
- `pytest tests/ -q` green on Python 3.10 AND 3.12 (per CI matrix).
- Phase gate sample: `pytest tests/test_validate.py tests/test_build.py tests/test_analyze.py tests/test_report.py tests/test_wiki.py tests/test_export.py tests/test_skill_prompt_drift.py -q` passes.
</verification>

<success_criteria>
- REAS-03 knowledge_gaps fix in place; reasoning-edge endpoints not classified as gaps.
- `contradictions_and_chains` confidence-gated and longest-first sorted.
- REAS-04 distinct rendering in GRAPH_REPORT.md, wiki community articles, and Obsidian frontmatter.
- All omit-when-empty rules honored consistently with Phase 71-05 precedent.
</success_criteria>

<output>
After completion, create `.planning/phases/72-reas/72-04-SUMMARY.md`.
</output>
