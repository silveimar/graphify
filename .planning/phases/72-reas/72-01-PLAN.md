---
phase: 72-reas
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - graphify/validate.py
  - docs/RELATIONS.md
  - tests/test_validate.py
  - tests/fixtures/extraction_with_reasoning.json
autonomous: true
requirements: [REAS-01]

must_haves:
  truths:
    - "validate.py accepts the 5 reasoning relations on doc/concept-typed nodes"
    - "validate.py rejects reasoning relations when EITHER endpoint has file_type='code'"
    - "INFERRED reasoning edges require confidence_score in [0.0, 1.0]"
    - "Legacy graph.json without reasoning relations still loads via validate_extraction_for_read"
    - "docs/RELATIONS.md documents the 5 reasoning relations including supersedes orientation (newer -> older)"
  artifacts:
    - path: "graphify/validate.py"
      provides: "REASONING_RELATIONS frozenset + endpoint type rule"
      contains: "REASONING_RELATIONS"
    - path: "docs/RELATIONS.md"
      provides: "Reasoning relations taxonomy section"
      contains: "Reasoning relations"
    - path: "tests/fixtures/extraction_with_reasoning.json"
      provides: "Fixture for build/validate tests"
  key_links:
    - from: "graphify/validate.py REASONING_RELATIONS"
      to: "graphify/validate.py KNOWN_EDGE_RELATIONS"
      via: "frozenset union/extension"
      pattern: "REASONING_RELATIONS"
---

<objective>
Register the 5 reasoning relations (`supports`, `contradicts`, `supersedes`, `evolved_into`, `depends_on`) in `validate.py` per Phase 53 frozenset precedent, enforce the code-endpoint rejection rule (D-05), enforce the CCONF v1.13 confidence_score rule for INFERRED edges (D-03, D-06), and document the relations in `docs/RELATIONS.md` including the critical newer->older orientation for `supersedes`.

Purpose: REAS-01 is the foundation — all later plans (extraction prompt, build resolver, analyze/render) depend on the vocabulary being registered and validated.
Output: Updated validate.py, docs/RELATIONS.md, test fixture, and 4 unit tests.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/72-reas/72-CONTEXT.md
@.planning/phases/72-reas/72-RESEARCH.md
@graphify/validate.py
@docs/RELATIONS.md
@CLAUDE.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Register REASONING_RELATIONS frozenset and extend KNOWN_EDGE_RELATIONS in validate.py</name>
  <read_first>
    - graphify/validate.py (full file; especially lines 14-43 KNOWN_EDGE_RELATIONS, 52-69 NEW_CONCEPT_CODE_RELATIONS, 156-214 per-edge validation loop, 231-258 read/write split)
    - .planning/phases/72-reas/72-CONTEXT.md (D-03, D-05, D-06)
    - .planning/phases/72-reas/72-RESEARCH.md (Pattern 1, Pattern 2)
    - tests/test_validate.py (existing test conventions)
  </read_first>
  <files>graphify/validate.py, tests/test_validate.py, tests/fixtures/extraction_with_reasoning.json</files>
  <behavior>
    - Test: REASONING_RELATIONS is a frozenset containing exactly {"supports", "contradicts", "supersedes", "evolved_into", "depends_on"}
    - Test: Each of the 5 relation names is also present in KNOWN_EDGE_RELATIONS
    - Test: validate_extraction accepts a doc->doc edge with relation="supersedes", confidence="EXTRACTED" (returns no errors for that edge)
    - Test: validate_extraction rejects a code->doc edge with relation="supports" with an error mentioning "non-code endpoints"
    - Test: validate_extraction rejects a doc->doc edge with relation="contradicts", confidence="INFERRED" and missing/out-of-range confidence_score (error mentions "confidence_score")
    - Test: validate_extraction_for_read accepts a legacy extraction dict with no reasoning edges (no errors related to reasoning)
  </behavior>
  <action>
    1. In `graphify/validate.py`, add a new module-level frozenset directly after `NEW_CONCEPT_CODE_RELATIONS`:
       ```python
       REASONING_RELATIONS: frozenset[str] = frozenset({
           "supports", "contradicts", "supersedes", "evolved_into", "depends_on",
       })
       ```
    2. Extend `KNOWN_EDGE_RELATIONS` (lines 14-43) to include all 5 names. Add them at the end of the existing frozenset literal as a contiguous block with a comment `# Phase 72 (REAS): reasoning relations`.
    3. In `validate_extraction`, change the `node_ids` comprehension (line ~156) to also build a `node_types` dict:
       ```python
       node_types: dict[str, str] = {
           n["id"]: str(n.get("file_type", ""))
           for n in data.get("nodes", []) if isinstance(n, dict) and "id" in n
       }
       node_ids = set(node_types)
       ```
    4. Inside the per-edge validation loop, AFTER the existing concept↔code block (~line 214), append a new block:
       ```python
       if rel in REASONING_RELATIONS:
           src_t = node_types.get(edge.get("source", ""), "")
           tgt_t = node_types.get(edge.get("target", ""), "")
           if src_t == "code" or tgt_t == "code":
               errors.append(
                   f"Edge {i} relation={rel!r} requires non-code endpoints; "
                   f"got source.file_type={src_t!r}, target.file_type={tgt_t!r}"
               )
           if conf == "INFERRED":
               raw = edge.get("confidence_score")
               try:
                   score = float(raw) if raw is not None else None
               except (TypeError, ValueError):
                   score = None
               if score is None or not (0.0 <= score <= 1.0):
                   errors.append(
                       f"Edge {i} relation={rel!r} confidence=INFERRED requires "
                       f"'confidence_score' in [0.0, 1.0]"
                   )
       ```
       Note: `conf` and `i` are already established loop locals; verify naming against the surrounding loop and adapt if needed.
    5. Verify `validate_extraction_for_read` (read-mode path) does NOT enforce the new rule — read mode must remain permissive (legacy graphs load).
    6. Create `tests/fixtures/extraction_with_reasoning.json` with the ADR-0028 / ADR-0042 / Concept X shape from RESEARCH.md test fixture pattern (schema_version "2.0", 3 doc nodes, 2 edges: supersedes EXTRACTED + supports INFERRED 0.8). Bare label targets allowed (will be tested in Plan 03).
    7. Add the 4 tests in `tests/test_validate.py`:
       - `test_reasoning_relations_accepted` — doc->doc supersedes EXTRACTED
       - `test_reasoning_rejects_code_endpoint` — code->doc supports
       - `test_reasoning_inferred_score` — INFERRED without score in range
       - `test_legacy_no_reasoning_loads` — legacy graph via validate_extraction_for_read
  </action>
  <verify>
    <automated>pytest tests/test_validate.py::test_reasoning_relations_accepted tests/test_validate.py::test_reasoning_rejects_code_endpoint tests/test_validate.py::test_reasoning_inferred_score tests/test_validate.py::test_legacy_no_reasoning_loads -x -q</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "REASONING_RELATIONS" graphify/validate.py` >= 3 (definition + 2 usages)
    - `grep "supports.*contradicts.*supersedes.*evolved_into.*depends_on" graphify/validate.py` matches the frozenset literal (or each token present in KNOWN_EDGE_RELATIONS)
    - `grep -c "supports\|contradicts\|supersedes\|evolved_into\|depends_on" graphify/validate.py` >= 10 (registration in 2 frozensets)
    - All 4 new tests pass
    - `pytest tests/test_validate.py -x -q` passes (no regression)
    - `tests/fixtures/extraction_with_reasoning.json` exists and is valid JSON with `schema_version: "2.0"`
  </acceptance_criteria>
  <done>validate.py exports REASONING_RELATIONS, enforces D-05 code-endpoint rejection and D-03 confidence_score rule, and the 4 unit tests pass.</done>
</task>

<task type="auto">
  <name>Task 2: Document the 5 reasoning relations in docs/RELATIONS.md with supersedes orientation</name>
  <read_first>
    - docs/RELATIONS.md (full file — match section formatting conventions)
    - .planning/phases/72-reas/72-RESEARCH.md (Pattern 4 "Direction note" — newer->older)
    - .planning/phases/72-reas/72-CONTEXT.md (D-01..D-16)
  </read_first>
  <files>docs/RELATIONS.md</files>
  <action>
    Append a new top-level section `## Reasoning relations (Phase 72)` to `docs/RELATIONS.md`. Section MUST contain:

    1. One-sentence summary: "Reasoning relations encode claims about ideas/documents (not code). Both endpoints must be document- or concept-typed; code-typed endpoints are rejected by `validate.py`."
    2. A table with columns: `Relation | Definition | Direction | Confidence`. Rows for each of the 5 relations:
       - `supports` — "Source asserts evidence in favor of target." Direction: `supporter -> claim`.
       - `contradicts` — "Source asserts target is false or incompatible." Direction: symmetric (semantically); emitted with stable `(source, target)` ordering by extractor.
       - `supersedes` — "Source replaces target as canonical. **Orientation: newer -> older. The TARGET is the superseded (deprecated) node.**" Direction: `replacement -> deprecated`.
       - `evolved_into` — "Source is an earlier version that became target." Direction: `older -> newer` (note: opposite orientation from `supersedes`).
       - `depends_on` — "Source's claim relies on target's claim." Direction: `dependent -> prerequisite`.
       Confidence column: all 5 may be EXTRACTED, INFERRED (with confidence_score in [0.0, 1.0]), or AMBIGUOUS per CCONF v1.13.
    3. A subsection `### Auto-stamping interaction with Phase 71 temporal layer`:
       - "When `A supersedes B` lands, build.py stamps `valid_until = run_now` on every other outbound edge of B. Idempotent (skipped when `valid_until` is already set). Rationale: once B is superseded, all its outbound claims become historical and surface in the wiki's `## Historical relations` subsection."
    4. A worked example (ADR supersession): `ADR-0042 supersedes ADR-0028` -> auto-stamps outbound edges of `adr_0028`.
  </action>
  <verify>
    <automated>grep -q "Reasoning relations (Phase 72)" docs/RELATIONS.md &amp;&amp; grep -q "newer -> older" docs/RELATIONS.md &amp;&amp; grep -q "supersedes" docs/RELATIONS.md &amp;&amp; grep -q "evolved_into" docs/RELATIONS.md</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "Reasoning relations (Phase 72)" docs/RELATIONS.md` == 1
    - `grep -c "supports\|contradicts\|supersedes\|evolved_into\|depends_on" docs/RELATIONS.md` >= 5 (each relation named at least once in new section)
    - `grep "newer -> older" docs/RELATIONS.md` matches (orientation explicitly documented)
    - `grep -q "Auto-stamping" docs/RELATIONS.md` matches (Phase 71 interaction subsection present)
    - `grep "ADR-0042 supersedes ADR-0028" docs/RELATIONS.md` matches (worked example present)
  </acceptance_criteria>
  <done>RELATIONS.md has a complete Phase 72 section documenting all 5 relations, the supersedes orientation, and the auto-stamp interaction.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| extractor (LLM) -> validate.py | Untrusted reasoning edges with arbitrary `relation`, `confidence`, `confidence_score`, endpoint ids. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-72-01 | Tampering | validate.py per-edge loop | mitigate | Endpoint type lookup is read-only; rejects `code` endpoints; appends to errors list (no raise). Phase 53 precedent. |
| T-72-02 | Tampering | validate.py confidence_score parse | mitigate | `float()` in try/except; non-numeric drops to error path (existing pattern at validate.py:194-198). |
| T-72-03 | Information disclosure | docs/RELATIONS.md orientation | mitigate | Direction explicitly documented as "newer -> older"; downstream code asserted by unit test in Plan 03. |
</threat_model>

<verification>
- All 4 new tests in test_validate.py pass.
- `pytest tests/test_validate.py -x -q` green.
- docs/RELATIONS.md acceptance grep checks pass.
- No regression in existing validate tests.
</verification>

<success_criteria>
- REAS-01 vocabulary registered and enforced in validate.py.
- Phase 72 section in docs/RELATIONS.md documents all 5 relations with orientation.
- Test fixture `extraction_with_reasoning.json` available for downstream plans.
</success_criteria>

<output>
After completion, create `.planning/phases/72-reas/72-01-SUMMARY.md`.
</output>
