---
phase: 13-agent-capability-manifest
plan: 02
type: execute
wave: 1
depends_on:
  - 13-01
files_modified:
  - .github/workflows/ci.yml
  - graphify/capability.py
  - graphify/mcp_tool_registry.py
  - tests/test_capability.py
autonomous: true
requirements:
  - MANIFEST-09
  - MANIFEST-10
threat_refs:
  - T-13-08
tags: [mcp, manifest, ci, docstrings]
must_haves:
  truths:
    - "CI runs `graphify capability --validate` on every push/PR to main/v* and fails the job when the committed server.json content-hash does not match canonical_manifest_hash(build_manifest_dict()) (MANIFEST-09)"
    - "Developer who adds a new MCP tool to mcp_tool_registry.build_mcp_tools without regenerating server.json sees a failing CI job whose stderr prints expected-hash, actual-hash, path to server.json, and the exact regenerate command `graphify capability --stdout > server.json` (D-03, MANIFEST-09)"
    - "The CI drift gate is NOT bypassable by a GRAPHIFY_SKIP_MANIFEST_VALIDATE or similar env-var toggle — the workflow step always runs; suppression is only possible by editing ci.yml (T-13-08)"
    - "Each tool entry in CAPABILITY_TOOLS carries an `_meta.examples: list[str]` field auto-extracted from the handler's docstring `Examples:` section (MANIFEST-10)"
    - "Tools with no docstring `Examples:` block receive `_meta.examples: []` (empty list, not omitted) so the manifest schema stays uniform"
    - "Adding an example line to a handler docstring and re-running `graphify capability --stdout` deterministically produces the same manifest (examples list is order-preserving and whitespace-normalized)"
  artifacts:
    - path: ".github/workflows/ci.yml"
      provides: "CI job step invoking `graphify capability --validate` after `pip install -e .[mcp,pdf,watch]` (MANIFEST-09)"
    - path: "graphify/capability.py"
      provides: "extract_tool_examples(handler_or_docstring) helper; _tool_to_manifest_entry augmented to populate `_meta.examples` (MANIFEST-10)"
    - path: "graphify/mcp_tool_registry.py"
      provides: "build_mcp_tools returns tools whose description/docstring carries an `Examples:` block; handler reference accessible to capability.py for docstring lookup"
    - path: "tests/test_capability.py"
      provides: "test_validate_cli_drift_detected, test_meta_examples_populated, test_meta_examples_empty_when_no_docstring_block"
---

<objective>
**Wave A continuation (P2):** Close Phase 13 P2 items. Wire **`graphify capability --validate`** into GitHub Actions CI as a **drift gate** that fails the build when the committed `server.json` hash diverges from live introspection (**MANIFEST-09**). Auto-extract per-tool **usage examples from docstrings** into `_meta.examples` so agents can show worked examples without hand-maintenance (**MANIFEST-10**).

**Boundary:** Do NOT touch files owned by Plan 01 beyond (a) augmenting `capability.py::_tool_to_manifest_entry` to populate `_meta.examples` and (b) exposing handler docstrings from `mcp_tool_registry.py` to `capability.py`. Do NOT re-create `server.json`, `capability_manifest.schema.json`, or the MCP tool definitions — Plan 01 owns them.

**Locked constraints honored:** D-01 (no placeholder tools — examples only for tools actually registered), D-02 (CI failure messaging stable per D-03), D-04 (no hash-semantics change), project rule `tests pure unit — no network calls`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/13-agent-capability-manifest/13-CONTEXT.md
@.planning/phases/13-agent-capability-manifest/13-RESEARCH.md
@.planning/phases/13-agent-capability-manifest/13-01-SUMMARY.md
@.planning/REQUIREMENTS.md
@graphify/capability.py
@graphify/mcp_tool_registry.py
@.github/workflows/ci.yml
</context>

<interfaces>
<!-- Existing exports from Plan 01 that this plan consumes -->

From `graphify/capability.py`:
```python
def build_manifest_dict() -> dict[str, Any]: ...
def canonical_manifest_hash(manifest_dict: dict[str, Any]) -> str: ...
def validate_cli(*, repo_root: Path | None = None) -> tuple[int, str]: ...  # returns (exit_code, message)
def _tool_to_manifest_entry(tool: Any, meta_defaults: dict[str, Any]) -> dict[str, Any]: ...
```

From `graphify/mcp_tool_registry.py`:
```python
def build_mcp_tools() -> list[Tool]: ...   # types.Tool objects (mcp.types)
def tool_names_ordered() -> list[str]: ...
```

Tool `description` field already carries first-line summary; full handler docstrings (in `serve.py::_tool_*`) are the canonical source for `Examples:` blocks.
</interfaces>

<tasks>

<task type="execute" tdd="true">
  <name>Task 1: CI drift gate step in ci.yml + validate_cli stderr contract test</name>
  <files>.github/workflows/ci.yml, tests/test_capability.py</files>
  <read_first>
    - .github/workflows/ci.yml (full — pattern for adding a step after `pip install` and before/after `python -m pytest`)
    - graphify/capability.py lines 122–165 (validate_cli + print_manifest_stdout)
    - .planning/phases/13-agent-capability-manifest/13-CONTEXT.md D-03 (stderr contract)
  </read_first>
  <behavior>
    - Test 1 (`test_validate_cli_drift_detected`): Monkeypatch `load_committed_server_json` to return a dict whose `_meta.manifest_content_hash` differs from `canonical_manifest_hash(build_manifest_dict())`; call `validate_cli()`; assert exit_code != 0 AND returned message contains all of: the literal substring `expected`, the literal substring `actual`, the literal substring `server.json`, the literal substring `graphify capability --stdout > server.json` (D-03 stability).
    - Test 2 (`test_validate_cli_clean_tree`): With unmodified working tree (monkeypatch `load_committed_server_json` to return a dict whose `_meta.manifest_content_hash` equals the live hash), `validate_cli()` returns exit_code == 0.
    - Test 3 (`test_validate_cli_no_huge_diff_by_default`): In the drift case the returned message length is bounded (e.g. < 2000 chars) — D-03 says "no huge unified diffs by default".
  </behavior>
  <action>
    **CI step (MANIFEST-09):** Edit `.github/workflows/ci.yml`. After the existing `Run tests` step and the existing `Verify install works end-to-end` step, insert a new step named `Verify MCP capability manifest has not drifted` that runs:

    ```
    graphify capability --validate
    ```

    The step must run on BOTH matrix Python versions (3.10 and 3.12). No `continue-on-error: true`. No `if:` guard keyed to env vars. The step inherits the `mcp` extra from the already-installed `pip install -e ".[mcp,pdf,watch]"` — do not alter the install line.

    **Stderr contract test (T-13-08 + D-03):** In `tests/test_capability.py` add the three tests described in `<behavior>`. Use `monkeypatch.setattr("graphify.capability.load_committed_server_json", ...)` to inject drift without touching the real `server.json` on disk. Do NOT use subprocess or network.

    **Stability assertions:** Test 1 must assert each of the four literal substrings independently (4 assertions). Do NOT accept matches via regex catch-alls.

    Do NOT modify `validate_cli()` itself — Plan 01 already implements D-03. If the tests fail because the existing stderr is missing one of the four tokens, patch the message template in `capability.py::validate_cli` minimally (add the missing token); do NOT rewrite the function.
  </action>
  <verify>
    <automated>pytest tests/test_capability.py::test_validate_cli_drift_detected tests/test_capability.py::test_validate_cli_clean_tree tests/test_capability.py::test_validate_cli_no_huge_diff_by_default -q</automated>
  </verify>
  <done>CI workflow step added; three unit tests pass; D-03 stderr contract enforced.</done>
  <acceptance_criteria>
    - `grep -q 'graphify capability --validate' .github/workflows/ci.yml` exits 0
    - `grep -c 'continue-on-error' .github/workflows/ci.yml` unchanged from before (no bypass added)
    - `grep -E 'if:.*GRAPHIFY_SKIP' .github/workflows/ci.yml` returns no match (no env bypass)
    - `pytest tests/test_capability.py -k validate_cli -q` exits 0
    - `pytest tests/test_capability.py::test_validate_cli_drift_detected -q` exits 0
    - `pytest tests/test_capability.py::test_validate_cli_no_huge_diff_by_default -q` exits 0
  </acceptance_criteria>
</task>

<task type="execute" tdd="true">
  <name>Task 2: Docstring example extraction into _meta.examples (MANIFEST-10)</name>
  <files>graphify/capability.py, graphify/mcp_tool_registry.py, tests/test_capability.py</files>
  <read_first>
    - graphify/capability.py (full — focus on `_tool_to_manifest_entry` line 39, `build_manifest_dict` line 66, `_load_yaml_meta` line 26)
    - graphify/mcp_tool_registry.py (full — focus on `build_mcp_tools` line 57; does it carry handler references?)
    - graphify/serve.py lines 1718–2050 (one-shot — look at existing `_tool_*` docstrings; confirm at least one has a docstring already and survey shape)
  </read_first>
  <behavior>
    - Test 4 (`test_extract_tool_examples_parses_examples_block`): Given docstring: `"""Query the graph.\n\nExamples:\n  query_graph(query='transformer')\n  query_graph(query='attention', limit=5)\n"""`, `extract_tool_examples(docstring)` returns `["query_graph(query='transformer')", "query_graph(query='attention', limit=5)"]` — order preserved, whitespace stripped, no trailing blanks.
    - Test 5 (`test_extract_tool_examples_empty_when_no_block`): Docstring without an `Examples:` header → returns `[]` (empty list, not None).
    - Test 6 (`test_extract_tool_examples_empty_on_none_docstring`): `extract_tool_examples(None)` returns `[]`.
    - Test 7 (`test_meta_examples_populated_in_manifest`): Build manifest; find at least one tool entry whose handler docstring contains `Examples:` (or patch one for the test via `monkeypatch`); assert `entry["_meta"]["examples"]` is a non-empty list.
    - Test 8 (`test_meta_examples_uniform_when_absent`): For a tool whose docstring has no `Examples:` block, `entry["_meta"]["examples"] == []` (present, uniform, empty).
    - Test 9 (`test_manifest_hash_stable_after_examples_added`): `canonical_manifest_hash` over `build_manifest_dict()` is the same across two calls (determinism — examples list ordering stable).
  </behavior>
  <action>
    **Expose handlers to capability.py (MANIFEST-10 introspection):** Extend `graphify/mcp_tool_registry.py`. Add a module-level dict `_HANDLER_DOCSTRINGS: dict[str, str | None]` populated when `build_mcp_tools()` runs. The dict maps tool name → handler `__doc__`. Source handlers from the same closure that `serve.py` builds (`_handlers`) — if `build_mcp_tools` does not currently have handler access, add a sibling function `build_handler_docstrings() -> dict[str, str | None]` that imports `serve._handlers_snapshot()` (a new 2-line helper inside `serve.py` that returns `dict((name, fn.__doc__) for name, fn in _handlers.items())` when `_handlers` is populated; return `{}` if serve not initialized). `capability.py` calls `build_handler_docstrings()` with a fallback to `{}` — no crash when serve hasn't been bootstrapped.

    **Add `extract_tool_examples(docstring: str | None) -> list[str]` to `graphify/capability.py`:** Parse docstrings using the exact grammar:
    1. Split on lines.
    2. Locate the first line whose stripped value is `Examples:` (case-sensitive, no trailing text).
    3. From the next line onward, collect lines until a blank line OR another `^[A-Z][A-Za-z ]*:$` header (e.g. `Args:`, `Returns:`) OR EOF.
    4. For each collected line: `line.strip()`; drop empties; keep order.
    5. Return the list. If `docstring is None` or no header found → `[]`.

    Reject non-string input safely: `if not isinstance(docstring, str): return []`. No regex engines beyond simple splits. Use `string.ascii_uppercase` check for header detection — no `re` import unless already present in module.

    **Wire into `_tool_to_manifest_entry`:** Accept a new kwarg `handler_docstring: str | None = None`. In `build_manifest_dict`, after loading `build_mcp_tools()`, also call `build_handler_docstrings()`; pass the matching docstring by name into `_tool_to_manifest_entry`. Inside the function, set `entry["_meta"]["examples"] = extract_tool_examples(handler_docstring)`. Preserve any existing `_meta` keys (merge, don't overwrite).

    **Determinism:** Because `build_mcp_tools()` returns tools in a stable order (Plan 01 guarantee via `tool_names_ordered`), and `extract_tool_examples` preserves input order, `canonical_manifest_hash` remains deterministic across runs. Add a test asserting two successive calls return identical hashes (Test 9).

    **Do NOT** write new examples into handler docstrings in this plan — MANIFEST-10 only requires the extraction machinery; content-population is a docstring-grooming task owned by whoever adds a new tool. Existing docstrings in `serve.py` pass through as-is.
  </action>
  <verify>
    <automated>pytest tests/test_capability.py -q</automated>
  </verify>
  <done>`_meta.examples` populated from docstrings for all tools; hash stability preserved; six new tests pass.</done>
  <acceptance_criteria>
    - `grep -n "def extract_tool_examples" graphify/capability.py` shows the function
    - `grep -n "_meta" graphify/capability.py | grep -q examples` (wiring present)
    - `grep -n "_handlers_snapshot\|build_handler_docstrings" graphify/mcp_tool_registry.py graphify/serve.py` shows the helper in BOTH files
    - `pytest tests/test_capability.py::test_extract_tool_examples_parses_examples_block tests/test_capability.py::test_extract_tool_examples_empty_when_no_block tests/test_capability.py::test_extract_tool_examples_empty_on_none_docstring tests/test_capability.py::test_meta_examples_populated_in_manifest tests/test_capability.py::test_meta_examples_uniform_when_absent tests/test_capability.py::test_manifest_hash_stable_after_examples_added -q` exits 0
    - `python -c "from graphify.capability import build_manifest_dict; d=build_manifest_dict(); assert all(isinstance(t.get('_meta',{}).get('examples'), list) for t in d['tools'])"` exits 0
  </acceptance_criteria>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| CI runner → committed `server.json` | Untrusted commits must not silently ship a hash-mismatched manifest |
| Developer docstring → published manifest | Docstring text becomes agent-visible metadata; must not exfiltrate secrets |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-13-08 | Tampering (CI bypass) | `.github/workflows/ci.yml` drift-gate step | mitigate | Step runs unconditionally on every push/PR; no `continue-on-error`, no env-var guard. Suppression requires editing ci.yml (visible in code review). Test asserts no bypass tokens present in workflow file. |
| T-13-09 | Information disclosure (docstring leak) | `_meta.examples` extraction | accept | Docstrings are already public (visible in source + MCP `description` field); elevating `Examples:` blocks to `_meta.examples` surfaces no net-new information. Developer discipline: do not put secrets in docstrings — same rule as today. |
| T-13-10 | Integrity (non-deterministic manifest) | `build_manifest_dict` + examples ordering | mitigate | `extract_tool_examples` preserves input order; `build_mcp_tools` returns stable order (Plan 01). Test 9 asserts hash stability. |
</threat_model>

<verification>
- `pytest tests/test_capability.py -q` passes
- `grep -q 'graphify capability --validate' .github/workflows/ci.yml`
- `python -c "from graphify.capability import build_manifest_dict; d=build_manifest_dict(); assert all('examples' in t.get('_meta',{}) for t in d['tools'])"` exits 0
</verification>

<success_criteria>
- [ ] MANIFEST-09 closed: CI drift gate active on 3.10 and 3.12 matrix; non-zero exit on drift; D-03 stderr contract enforced by unit test
- [ ] MANIFEST-10 closed: all tool entries carry `_meta.examples` (list), populated from handler docstrings via `extract_tool_examples`
- [ ] Hash determinism preserved (Test 9)
- [ ] No regressions: `pytest tests/ -q` passes
</success_criteria>

<output>
After completion, create `.planning/phases/13-agent-capability-manifest/13-02-SUMMARY.md`
</output>
