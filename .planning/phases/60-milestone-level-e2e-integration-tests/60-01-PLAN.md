---
phase: 60-milestone-level-e2e-integration-tests
plan: 01
type: tdd
wave: 1
depends_on: []
files_modified:
  - tests/test_e2e_integration.py
autonomous: true
requirements:
  - E2E-01
tags:
  - testing
  - e2e
  - subprocess
  - vault-adapter
  - profile-composition
  - override-ladder

must_haves:
  truths:
    - "tests/test_e2e_integration.py exists and is collected by pytest"
    - "test_e2e_compose_override_ladder runs end-to-end via two subprocess calls (preview, then --apply --plan-id)"
    - "Notes are materialized on disk under <vault>/Atlas/Sources/Graphify/ after the apply call"
    - "Override-ladder ordering invariant is observable: block expansion runs BEFORE ${} substitution"
    - "mapping_rule_templates → note_type_templates ladder resolves with first-match-wins"
    - "Frontmatter assertions pin the contract: type, tags, community fields are emitted as documented"
    - "Test passes deterministically on Python 3.10 and 3.12"
  artifacts:
    - path: tests/test_e2e_integration.py
      provides: "E2E-01 subprocess test plus local _graphify, _write_vault, _read_frontmatter helpers"
      contains: "def test_e2e_compose_override_ladder"
      min_lines: 120
  key_links:
    - from: tests/test_e2e_integration.py::test_e2e_compose_override_ladder
      to: graphify/__main__.py update-vault subcommand
      via: subprocess.run([sys.executable, "-m", "graphify", "update-vault", ...])
      pattern: "update-vault.*--input.*--vault"
    - from: tests/test_e2e_integration.py::_graphify
      to: PYTHONPATH worktree-root injection
      via: env["PYTHONPATH"] = worktree_root + os.pathsep + existing
      pattern: "PYTHONPATH"
    - from: tests/test_e2e_integration.py override template
      to: graphify/templates.py block-expand-then-substitute pipeline (D-16)
      via: "{{#connections}}…{{/connections}} block + ${label} substitution + OVERRIDE_NTT_THING_MARKER sentinel"
      pattern: "OVERRIDE_NTT_THING_MARKER"
---

<objective>
Add `tests/test_e2e_integration.py` containing the E2E-01 subprocess integration test that closes the v1.11 audit's Flow 2 gap.

Purpose: Lock down the Phase 55 (`note_type_templates` block expansion) + Phase 56 (`mapping_rule_templates` override ladder) composition contract end-to-end. Today these layers are unit-tested in isolation but never co-exercised through the real CLI subprocess pipeline. E2E-01 makes their interaction a regression target.

Output: One new test file with one passing test (`test_e2e_compose_override_ladder`), plus the three local helpers (`_graphify`, `_write_vault`, `_read_frontmatter`) that Plan 02 will reuse.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/60-milestone-level-e2e-integration-tests/60-CONTEXT.md
@.planning/phases/60-milestone-level-e2e-integration-tests/60-RESEARCH.md
@.planning/phases/60-milestone-level-e2e-integration-tests/60-VALIDATION.md
@tests/test_main_flags.py
@tests/test_integration.py
@tests/test_profile_composition.py
@graphify/__main__.py
@graphify/profile.py
@./CLAUDE.md

<interfaces>
<!-- Verbatim helper to copy from tests/test_main_flags.py:15-39 (verified line range) -->

```python
def _graphify(args: list[str], cwd: Path, env: dict | None = None) -> subprocess.CompletedProcess:
    full_env = os.environ.copy()
    worktree_root = Path(__file__).resolve().parent.parent  # tests/ -> repo root
    existing_pp = full_env.get("PYTHONPATH", "")
    full_env["PYTHONPATH"] = (
        f"{worktree_root}{os.pathsep}{existing_pp}" if existing_pp else str(worktree_root)
    )
    full_env.pop("GRAPHIFY_ELICIT_LLM", None)  # keep maybe_deepen_session deterministic (RESEARCH Pitfall env)
    if env:
        full_env.update(env)
    return subprocess.run(
        [sys.executable, "-m", "graphify", *args],
        cwd=str(cwd), env=full_env,
        capture_output=True, text=True, timeout=60,
    )
```

<!-- update-vault CLI surface (verified at graphify/__main__.py:3080-3124, migration.py:166-265) -->

Required preconditions enforced inside `run_update_vault`:
1. `<vault>/.obsidian/` MUST be a directory (else exits 1: "target vault must contain .obsidian").
2. `<vault>/.graphify/profile.yaml` MUST exist with an `output:` block containing both `mode` and `path` (else exits 1: "no 'output:' block").

Two-step preview→apply contract (RESEARCH Pattern 2 + Pitfall 6):
- Call 1: `graphify update-vault --input <corpus> --vault <vault>` → preview only; writes `<artifacts_dir>/migrations/migration-plan-<id>.json`; does NOT write notes to disk.
- Call 2: `graphify update-vault --input <corpus> --vault <vault> --apply --plan-id <id>` → materializes notes under `<vault>/<output.path>/`.
- `<artifacts_dir>` for a vault run = `<vault>.parent / graphify-out` (sibling-of-vault, D-11).

<!-- Frontmatter contract (verified at graphify/templates.py:803-849 _build_frontmatter_fields) -->

Field emission order:
`up, related, collections, created, tags, type, file_type, source_file, source_location, community, cohesion`

Distinguishing assertions for E2E-01:
- `type == "thing"` for the ntt-targeted note
- `tags` list contains a `community/<slug>` entry
- `community` is a non-empty string (membership assigned)
- `cohesion` is ABSENT for `type=thing` notes (only present for moc/community)

<!-- Override-ladder ordering invariant (D-16, RESEARCH Pitfall 5) -->

D-16: block expansion (`_expand_blocks`) runs BEFORE `_BlockTemplate.safe_substitute` (verified call sites templates.py:1504, 1520, 1867, 1886). To prove ordering at the test level, the override template must contain BOTH a `{{#…}}…{{/…}}` block AND a `${label}` substitution AND a unique sentinel literal — assertions verify the sentinel appears together with substituted label content AFTER block expansion.

<!-- Tier-2 shadowing avoidance (RESEARCH Pitfall 4) -->

The 4-tier ladder (mapping_rule_templates → community_templates → note_type_templates → base) means OMITTING `community_templates` from the inline profile is required to keep the test isolated to the mrt → ntt → base path Phase 56 actually composes.

<!-- min_community_size override (RESEARCH Pitfall 7) -->

`_DEFAULT_PROFILE.mapping.min_community_size = 6`; for a 2-class synthetic corpus this filters out all communities. Inline profile MUST set `mapping: {min_community_size: 1}`.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true" gate="RED">
  <name>Task 1 (RED): Author failing E2E-01 test with helpers and inline vault fixture</name>

  <read_first>
    - tests/test_main_flags.py (lines 1-40 — verbatim source for `_graphify` helper, line 15)
    - tests/test_integration.py (existing in-process integration patterns)
    - tests/test_profile_composition.py (valid `note_type_templates` + `mapping_rule_templates` shapes)
    - .planning/phases/60-milestone-level-e2e-integration-tests/60-CONTEXT.md (LOCKED USER DECISIONS — file count, helper reuse, fixture strategy, assertion granularity)
    - .planning/phases/60-milestone-level-e2e-integration-tests/60-RESEARCH.md (Patterns 1-3, Pitfalls 1-7, Code Examples sections)
    - graphify/__main__.py update-vault subcommand handler (lines 3080-3124)
    - graphify/profile.py `_DEFAULT_PROFILE` for layout reference
  </read_first>

  <files>tests/test_e2e_integration.py</files>

  <behavior>
    - Test 1: `test_e2e_compose_override_ladder` — given a tmp_path vault with an inline profile defining BOTH `mapping_rule_templates` AND `note_type_templates` (and crucially NO `community_templates`), running `graphify update-vault` (preview + apply) materializes at least one rendered note whose body contains the sentinel `OVERRIDE_NTT_THING_MARKER`, the substituted label, and the expanded `{{#connections}}` block content. The note's frontmatter parses as YAML and satisfies: `type == "thing"`, `tags` contains a `community/...` tag, `community` is a non-empty string, `cohesion` key is absent.
    - Subprocess invariants: each `graphify` call has `result.returncode == 0` (asserted with stderr in failure message); preview JSON exists between calls at `<vault>.parent/graphify-out/migrations/migration-plan-*.json` from which `plan_id` is harvested.
    - Helpers: file exposes `_graphify(args, cwd, env=None)`, `_write_vault(tmp_path, profile_yaml, *, templates=None)`, and `_read_frontmatter(path)` for reuse by Plan 02.
    - Determinism: `GRAPHIFY_ELICIT_LLM` is popped from env; corpus is fixed text; clustering is seed-stable (Leiden seed=42, owned by graphify).
  </behavior>

  <action>
Create the new test file `tests/test_e2e_integration.py` with the structure documented in 60-RESEARCH.md "Recommended Test File Structure". Use the Write tool — never heredoc.

**1. File header / imports:**
```python
"""E2E subprocess integration tests (Phase 60). Closes v1.11 audit Flow 2/3 gaps."""
from __future__ import annotations

import json
import os
import subprocess
import sys
import textwrap
from pathlib import Path

import yaml
import pytest
```

**2. `_graphify` helper:** Copy verbatim from `tests/test_main_flags.py:15-39` (signature `_graphify(args: list[str], cwd: Path, env: dict | None = None) -> subprocess.CompletedProcess`). Add ONE addition before the `if env:` line: `full_env.pop("GRAPHIFY_ELICIT_LLM", None)` (per RESEARCH "Gotchas the planner must encode" + Runtime State Inventory). Keep `timeout=60`. Keep `capture_output=True, text=True`.

**3. `_write_vault(tmp_path, profile_yaml: str, *, templates: dict[str, str] | None = None) -> Path` helper:**
   - Creates `<tmp_path>/vault/.obsidian/` (mkdir parents=True, exist_ok=True) — MUST run before profile write (RESEARCH Pitfall 1).
   - Creates `<tmp_path>/vault/.graphify/` and writes `profile.yaml` with `profile_yaml` content (use `textwrap.dedent` at the call site, not inside helper).
   - If `templates` dict provided, creates `<tmp_path>/vault/.graphify/templates/` and writes each `<key>` filename with `<value>` content.
   - Returns the vault Path.

**4. `_write_corpus(tmp_path) -> Path` helper:**
   - Creates `<tmp_path>/corpus/sample.py` with the verbatim 2-class fixture from 60-RESEARCH.md "Smallest valid corpus":
     ```python
     class TransformerLayer:
         def __init__(self, dim):
             self.dim = dim
         def forward(self, x):
             return x

     class AttentionHead:
         def attend(self, x):
             return x
     ```
   - Returns the corpus Path.

**5. `_read_frontmatter(p: Path) -> dict` helper:** Verbatim from 60-RESEARCH.md "Frontmatter parser helper":
```python
def _read_frontmatter(p: Path) -> dict:
    text = p.read_text(encoding="utf-8")
    if not text.startswith("---\n"):
        return {}
    _, fm, _ = text.split("---\n", 2)
    return yaml.safe_load(fm) or {}
```

**6. `_run_update_vault_preview_then_apply(corpus, vault) -> Path` helper (local to file, used by both tests):**
   - Calls `_graphify(["update-vault", "--input", str(corpus), "--vault", str(vault)], cwd=vault.parent)` and asserts `returncode == 0`.
   - Globs `<vault>.parent / "graphify-out" / "migrations" / "migration-plan-*.json"`, asserts exactly one match, loads JSON, extracts `plan_id` (top-level key).
   - Calls `_graphify(["update-vault", "--input", str(corpus), "--vault", str(vault), "--apply", "--plan-id", plan_id], cwd=vault.parent)` and asserts `returncode == 0`.
   - Returns `<vault> / "Atlas" / "Sources" / "Graphify"` (the resolved output root from the inline profile below).

**7. `test_e2e_compose_override_ladder(tmp_path)` test body:**

Inline profile YAML (verbatim per 60-RESEARCH.md "Code Examples"):
```yaml
output:
  mode: vault-relative
  path: Atlas/Sources/Graphify
taxonomy:
  version: v1.8
  root: Atlas/Sources/Graphify
  folders:
    moc: MOCs
    thing: Things
    statement: Statements
    person: People
    source: Sources
    default: Things
    unclassified: MOCs
mapping:
  min_community_size: 1
naming:
  convention: kebab-case
mapping_rule_templates:
  - match: rule_id
    pattern: e2e-test-rule
    template: templates/mrt-rule.md
note_type_templates:
  - match: note_type
    pattern: thing
    template: templates/ntt-thing.md
```

Templates dict written via `_write_vault(..., templates={...})`:
- `"templates/ntt-thing.md"` content (verbatim per 60-RESEARCH.md to prove block-then-substitute ordering):
  ```
  ${frontmatter}
  # OVERRIDE_NTT_THING_MARKER: ${label}

  {{#if_note_type_thing}}NTT_BLOCK_RAN{{/if}}

  {{#connections}}
  - ${conn.label} via ${conn.relation}
  {{/connections}}
  ```
- `"templates/mrt-rule.md"` content (minimal placeholder so the file resolves; mrt won't match for this corpus since no `rule_id == e2e-test-rule` is produced — this is INTENTIONAL: it proves mrt does not shadow when its match fails, so ntt fires):
  ```
  ${frontmatter}
  # MRT_RULE_MATCHED: ${label}
  ```

Test body sequence:
1. `vault = _write_vault(tmp_path, profile_yaml, templates={...})`
2. `corpus = _write_corpus(tmp_path)` (sibling of vault, NOT inside vault)
3. `output_root = _run_update_vault_preview_then_apply(corpus, vault)`
4. `assert output_root.exists()` — proves apply call wrote files under the resolved `output.path`.
5. Glob `output_root.rglob("*.md")` and select notes whose frontmatter parses with `type == "thing"`. Bind `thing_notes`. Assert `len(thing_notes) >= 1`.
6. Pick `note = thing_notes[0]`; load `body = note.read_text(encoding="utf-8")`.
7. Assertions on body (proves D-16 ordering and ntt match):
   - `assert "OVERRIDE_NTT_THING_MARKER" in body` (sentinel — proves ntt template was selected over base)
   - `assert "NTT_BLOCK_RAN" in body` (block expanded — `{{#if_note_type_thing}}` resolved before substitution)
   - `assert " via " in body` (connections block expanded with relation substituted)
   - `assert "${label}" not in body` and `assert "${conn.label}" not in body` (substitution completed, no raw placeholders)
   - `assert "MRT_RULE_MATCHED" not in body` (mrt did NOT shadow — confirms ladder fell through to ntt for this corpus)
8. Assertions on frontmatter:
   - `fm = _read_frontmatter(note)`
   - `assert fm["type"] == "thing"`
   - `assert any(t.startswith("community/") for t in fm.get("tags", []))`
   - `assert isinstance(fm.get("community"), str) and fm["community"]`
   - `assert "cohesion" not in fm` (cohesion is moc/community-only per templates.py:848)

After writing, run the test and confirm it FAILS (RED gate). On a fresh worktree with the inline profile correct, the test SHOULD pass first try IF Phases 55/56/57 are implemented correctly — which is the point: this is regression coverage. If it fails for an actual bug reason, STOP and surface as a checkpoint per TDD reference (do not silently fix prod code in this plan).

If the test passes immediately, that satisfies the GREEN gate too — confirm by re-running and proceed to REFACTOR.
  </action>

  <verify>
    <automated>cd /Users/silveimar/Documents/silogia-repos/graphify && python -c "import ast; ast.parse(open('tests/test_e2e_integration.py').read())" && grep -q "def test_e2e_compose_override_ladder" tests/test_e2e_integration.py && grep -q "def _graphify(" tests/test_e2e_integration.py && grep -q "def _write_vault(" tests/test_e2e_integration.py && grep -q "def _read_frontmatter(" tests/test_e2e_integration.py && grep -q "OVERRIDE_NTT_THING_MARKER" tests/test_e2e_integration.py && grep -q "min_community_size: 1" tests/test_e2e_integration.py && grep -q "mapping_rule_templates" tests/test_e2e_integration.py && grep -q "note_type_templates" tests/test_e2e_integration.py && ! grep -q "community_templates:" tests/test_e2e_integration.py && grep -q "{{#connections}}" tests/test_e2e_integration.py && grep -q "\${label}" tests/test_e2e_integration.py && grep -q "GRAPHIFY_ELICIT_LLM" tests/test_e2e_integration.py && grep -q "\.obsidian" tests/test_e2e_integration.py && grep -q "\\-\\-apply" tests/test_e2e_integration.py && grep -q "\\-\\-plan-id" tests/test_e2e_integration.py</automated>
  </verify>

  <acceptance_criteria>
    - File `tests/test_e2e_integration.py` exists and parses as valid Python
    - Contains `def test_e2e_compose_override_ladder(`
    - Contains `def _graphify(` (locally defined; not imported)
    - Contains `def _write_vault(`
    - Contains `def _read_frontmatter(`
    - Contains the literal `OVERRIDE_NTT_THING_MARKER` (sentinel proves ntt template selection)
    - Contains the literal `min_community_size: 1` (Pitfall 7 override)
    - Contains both `mapping_rule_templates` and `note_type_templates` keys
    - Does NOT contain a `community_templates:` key in the inline profile (Pitfall 4 — must not shadow ntt)
    - Contains `{{#connections}}` (block) AND `${label}` (substitution) (proves D-16 ordering observable)
    - Contains `GRAPHIFY_ELICIT_LLM` (defensive env pop)
    - Contains `.obsidian` (vault precondition mkdir)
    - Contains both `--apply` and `--plan-id` (two-step preview→apply pattern explicit)
  </acceptance_criteria>

  <done>
    Test file written, all helpers defined, inline profile + templates encode the override-ladder + block-expansion contract, frontmatter assertions pin the type/tags/community/cohesion shape. Either the test fails (RED — proceed to GREEN) or passes immediately because Phases 55/56/57 already satisfy the contract (GREEN gate auto-satisfied — proceed to REFACTOR).
  </done>
</task>

<task type="auto" tdd="true" gate="GREEN">
  <name>Task 2 (GREEN): Run the test, confirm pass; if real bug surfaces, escalate</name>

  <read_first>
    - tests/test_e2e_integration.py (just written in Task 1)
    - .planning/phases/60-milestone-level-e2e-integration-tests/60-RESEARCH.md (Pitfalls 1-7 — failure-mode lookup table)
    - .planning/phases/60-milestone-level-e2e-integration-tests/60-CONTEXT.md (decision boundaries — do NOT alter prod code in this phase)
  </read_first>

  <files>tests/test_e2e_integration.py</files>

  <behavior>
    - `pytest tests/test_e2e_integration.py::test_e2e_compose_override_ladder -q` exits 0.
    - If it exits non-zero, the failure is diagnosed against RESEARCH.md Pitfalls 1-7 (vault precondition order, missing output: block, sidecar artifacts_dir mismatch [N/A here], community_templates shadowing, weak ordering assertions, dry_run-no-write, min_community_size).
    - Failures attributable to the test (helper bug, profile typo, assertion brittleness) are fixed in this task.
    - Failures attributable to a real Phase 55/56 prod-code bug trigger a checkpoint (per CONTEXT.md scope boundary: this phase is test infra only).
  </behavior>

  <action>
1. Run the test:
   ```
   pytest tests/test_e2e_integration.py::test_e2e_compose_override_ladder -q
   ```

2. If the run is GREEN (returncode 0), proceed directly to Task 3 (REFACTOR).

3. If RED, classify the failure using RESEARCH.md Pitfalls table:
   - stderr contains `must contain .obsidian` → Pitfall 1 → fix `_write_vault` ordering (mkdir `.obsidian` BEFORE writing profile.yaml).
   - stderr contains `no 'output:' block` → Pitfall 2 → ensure inline profile YAML literal contains `output: { mode: vault-relative, path: Atlas/Sources/Graphify }`.
   - assertion failure on rendered notes glob (zero `.md` found) → Pitfall 6 → confirm second `_graphify` call uses `--apply --plan-id <id>`; verify `plan_id` extraction from preview JSON (`json.loads(...)["plan_id"]`).
   - assertion `len(thing_notes) >= 1` fails → Pitfall 7 → re-confirm `min_community_size: 1` is in the inline profile; if still failing, bump corpus to 3 classes.
   - assertion `"OVERRIDE_NTT_THING_MARKER" in body` fails → Pitfall 4 → re-verify NO `community_templates` key in inline profile; check note_type_templates pattern `thing` matches the classified note's `note_type`.
   - assertion `"${label}" not in body` fails → Pitfall 5 → block expansion or substitution did not run; this is a REAL prod bug and must be escalated as a checkpoint, not silently fixed.
   - assertion on frontmatter shape (`type`, `tags`, `community`, `cohesion`) fails → check `_build_frontmatter_fields` order at templates.py:803-849; if the field set has drifted from RESEARCH expectations, surface as checkpoint.

4. Apply the test-side fix only if the failure class falls under (a) helper bug, (b) profile YAML typo, or (c) assertion targeting the wrong note. Re-run.

5. If after one fix attempt the failure persists OR the failure class indicates a prod-code regression (substitution not running, frontmatter contract broken), STOP and surface as a checkpoint to the user with: failure mode, RESEARCH pitfall reference, and the reason this exceeds Phase 60 scope (test-infra-only per CONTEXT.md).
  </action>

  <verify>
    <automated>cd /Users/silveimar/Documents/silogia-repos/graphify && pytest tests/test_e2e_integration.py::test_e2e_compose_override_ladder -q 2>&1 | tail -20</automated>
  </verify>

  <acceptance_criteria>
    - `pytest tests/test_e2e_integration.py::test_e2e_compose_override_ladder -q` exits with code 0
    - Test output shows `1 passed` (no skips, no xfails)
  </acceptance_criteria>

  <done>
    The E2E-01 test passes deterministically. The Phase 55+56 composition contract is now regression-locked at the subprocess boundary.
  </done>
</task>

<task type="auto" tdd="true" gate="REFACTOR">
  <name>Task 3 (REFACTOR): Tighten helpers and dedupe inline YAML, keep test green</name>

  <read_first>
    - tests/test_e2e_integration.py (post-GREEN state)
    - .planning/phases/60-milestone-level-e2e-integration-tests/60-CONTEXT.md ("Deferred ideas" section — DO NOT promote `_graphify` to conftest.py in this phase)
  </read_first>

  <files>tests/test_e2e_integration.py</files>

  <behavior>
    - Helpers (`_graphify`, `_write_vault`, `_write_corpus`, `_read_frontmatter`, `_run_update_vault_preview_then_apply`) are clearly separated at module top.
    - Inline YAML strings use `textwrap.dedent("""…""")` consistently with no leading-whitespace artifacts.
    - Repeated literal strings (vault output path `"Atlas/Sources/Graphify"`, sentinel `"OVERRIDE_NTT_THING_MARKER"`) are module-level constants if used in 3+ places — otherwise inline.
    - All assertion failure messages include captured stderr where applicable (`assert result.returncode == 0, f"stderr={result.stderr}"`).
    - Test still exits 0 after each refactor edit (verify after every change).
    - `_graphify` remains LOCAL to this file (per CONTEXT decision; do not move to `tests/conftest.py`).
  </behavior>

  <action>
1. Re-read the file. Identify cleanup opportunities WITHIN the file only:
   - Pull repeated string literals to module-level constants if used 3+ times.
   - Ensure all `_graphify` call sites use the `assert result.returncode == 0, f"stderr={result.stderr}"` pattern (RESEARCH "Gotchas").
   - Confirm `textwrap.dedent` is used for any multi-line YAML/template literal.
   - Confirm helpers are ordered: small/leaf first (`_read_frontmatter`), composite last (`_run_update_vault_preview_then_apply`).

2. After EACH structural edit, re-run `pytest tests/test_e2e_integration.py::test_e2e_compose_override_ladder -q` to keep green. Do not batch edits without re-running.

3. Do NOT:
   - Move `_graphify` to `tests/conftest.py` (deferred per CONTEXT.md).
   - Promote any helper to a shared fixtures module.
   - Add new tests (E2E-02 lives in Plan 02).
   - Touch any file outside `tests/test_e2e_integration.py`.

4. Final pass: confirm the file still satisfies every Task 1 grep gate (acceptance_criteria from RED stage).
  </action>

  <verify>
    <automated>cd /Users/silveimar/Documents/silogia-repos/graphify && pytest tests/test_e2e_integration.py::test_e2e_compose_override_ladder -q 2>&1 | tail -10 && python -c "import ast; ast.parse(open('tests/test_e2e_integration.py').read())" && grep -c "def test_e2e_compose_override_ladder" tests/test_e2e_integration.py</automated>
  </verify>

  <acceptance_criteria>
    - `pytest tests/test_e2e_integration.py::test_e2e_compose_override_ladder -q` exits 0 (green maintained)
    - File still parses as valid Python
    - `def test_e2e_compose_override_ladder` appears exactly once
    - All `_graphify` call sites are followed by an `assert result.returncode == 0` with f-string stderr context
    - `_graphify` is defined locally in the file (not imported from conftest)
  </acceptance_criteria>

  <done>
    Helpers and test body are clean and idiomatic; every grep gate from Task 1 still holds; test remains green. The file is ready for Plan 02 to extend with E2E-02.
  </done>
</task>

</tasks>

<verification>
- `pytest tests/test_e2e_integration.py::test_e2e_compose_override_ladder -q` exits 0
- `pytest tests/ -q` exits 0 (no regressions)
- File satisfies every grep gate listed in Task 1's acceptance_criteria
- `_graphify` is defined locally (not in conftest.py — deferred per CONTEXT.md)
</verification>

<success_criteria>
- E2E-01 requirement closed: subprocess test asserting profile composition + override ladder end-to-end through the real CLI pipeline
- Helpers (`_graphify`, `_write_vault`, `_write_corpus`, `_read_frontmatter`, `_run_update_vault_preview_then_apply`) live in `tests/test_e2e_integration.py` ready for Plan 02 reuse
- D-16 ordering invariant (block expansion before substitution) is regression-locked at the subprocess boundary
- Phase 55 + Phase 56 composition path is now exercised end-to-end on every CI run
</success_criteria>

<output>
After completion, create `.planning/phases/60-milestone-level-e2e-integration-tests/60-01-SUMMARY.md` per template.
</output>
