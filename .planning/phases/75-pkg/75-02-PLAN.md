---
phase: 75-pkg
plan: 02
type: execute
wave: 2
depends_on: [75-01]
files_modified:
  - .planning/phases/75-pkg/75-02-SUMMARY.md
  - .planning/STATE.md
  - .planning/ROADMAP.md
autonomous: true
requirements: [PKG-02]
must_haves:
  truths:
    - "Full pytest suite passes locally on the developer's default Python (3.10 or 3.12) post-bump"
    - "CI workflow run on the 3.10 + 3.12 matrix is green and linked in the phase summary"
    - "Two atomic commits exist in history: chore(75) for the bump, docs(75) for the release-stamp record"
  artifacts:
    - path: ".planning/phases/75-pkg/75-02-SUMMARY.md"
      provides: "Phase 75 release-stamp record including linked green CI run URL"
      contains: "CI run"
  key_links:
    - from: "pytest tests/ -q"
      to: "no regression introduced by version bump"
      via: "single-Python local smoke + GitHub Actions matrix"
      pattern: "passed"
    - from: "git log"
      to: "two atomic commits per D6"
      via: "chore(75) precedes docs(75); no squash"
      pattern: "chore\\(75\\)|docs\\(75\\)"
---

<objective>
Prove the 2.0.0 bump introduced no regressions by running the full pytest suite locally as a smoke filter (D3), then capture the authoritative green signal from the GitHub Actions matrix on Python 3.10 AND 3.12. Commit the bump and the phase record as two atomic commits per D6.

Purpose: Close PKG-02 — the test-matrix half — and produce the auditable release-stamp record so Phase 75 can be marked Complete in ROADMAP.md / STATE.md.

Output: Local pytest green log, linked CI run URL covering both Python versions, two commits (`chore(75): bump graphifyy to 2.0.0` and `docs(75): record v2.0.0 release stamp`), and `75-02-SUMMARY.md` recording it all.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/75-pkg/75-CONTEXT.md
@.planning/phases/75-pkg/75-01-SUMMARY.md
@CLAUDE.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Local full-suite pytest smoke (D3 — single Python is sufficient locally)</name>
  <files>(read-only test execution; no source mutation)</files>
  <action>
    From repo root, run `pytest tests/ -q` on the developer's default Python interpreter. Per D3, do NOT block on locally executing both 3.10 and 3.12 — CI is the authoritative matrix gate. Local execution is a pre-CI smoke filter only.

    If any test fails: per scope_guardrails, fixing tests is allowed ONLY when the bump itself exposes a regression in Phase 71/72 schema work. In that case: STOP this plan, do NOT commit, and surface the failure so a follow-up plan can be created (and note the originating phase had insufficient coverage). Do NOT add features, change schemas, or modify extraction/build logic to make tests pass.

    Capture the pytest output (last 30 lines minimum, including the summary line `N passed in Ms`) for inclusion in the SUMMARY artifact.
  </action>
  <verify>
    <automated>pytest tests/ -q</automated>
  </verify>
  <done>Local `pytest tests/ -q` exits 0 on the developer's default Python. Summary line captured for the phase record.</done>
</task>

<task type="auto">
  <name>Task 2: Commit the bump (D6 commit 1 of 2) — chore(75)</name>
  <files>pyproject.toml, server.json, graphify/skill*.md (and any in-repo `.graphify_version` stamps), .planning/STATE.md, .planning/ROADMAP.md</files>
  <action>
    Stage ONLY the bump-related files mutated by 75-01:
    - `pyproject.toml`
    - `server.json`
    - The 7 platform SKILL.md variants in `graphify/` if `graphify install` rewrote any of them
    - Any in-repo `.graphify_version` stamps (note: per D5, most platform stamps live OUTSIDE the repo at user-config paths; only stage `.graphify_version` files that are actually tracked under the repo tree — do NOT `git add -A`)

    Also update `.planning/ROADMAP.md` and `.planning/STATE.md` to mark Phase 75 as Complete with the current date — these planning-doc edits ride with the chore commit because the release-stamp coherence INCLUDES the roadmap status row.

    Stage by exact filename (never `git add .` or `git add -A`, per safety protocol). Create the commit per D6:

    ```
    chore(75): bump graphifyy to 2.0.0

    Coordinated v2.0.0 release stamp per PKG-01/PKG-02:
    - pyproject.toml: 1.0.0 → 2.0.0
    - server.json: regenerated via sync_mcp_server_json.py (manifest hash recomputed with graphify_version=2.0.0)
    - .graphify_version stamps refreshed across 7 platforms via `graphify install`
    - Phase 75 marked Complete in ROADMAP.md / STATE.md

    Co-Authored-By: Claude Opus 4.7 (1M context) <noreply@anthropic.com>
    ```

    Do NOT amend; do NOT skip hooks; do NOT create a git tag (D7).
  </action>
  <verify>
    <automated>git log -1 --pretty=%s | grep -E "^chore\(75\): bump graphifyy to 2\.0\.0$"</automated>
  </verify>
  <done>Single atomic chore(75) commit exists at HEAD with bump artifacts + roadmap/state status update. No tag created.</done>
</task>

<task type="auto">
  <name>Task 3: Push, capture green CI matrix run, then commit phase record (D6 commit 2 of 2) — docs(75)</name>
  <files>.planning/phases/75-pkg/75-02-SUMMARY.md</files>
  <action>
    Push the chore(75) commit to the current branch's remote (do NOT force-push). Wait for the GitHub Actions workflow run to complete on both Python 3.10 and 3.12. Use `gh run list --branch $(git rev-parse --abbrev-ref HEAD) --limit 1` then `gh run watch <run-id>` to track to completion. The phase's PKG-02 acceptance is "full pytest suite green on Python 3.10 AND 3.12" — both matrix legs must be green. Capture the run URL via `gh run view <run-id> --json url -q .url`.

    If CI is red on either leg: STOP. Do not write the docs commit. Surface the failing leg's logs and treat as a regression-exposing-insufficient-coverage scenario (scope_guardrails) — a follow-up plan will be needed.

    Once CI is green on both 3.10 and 3.12, write `.planning/phases/75-pkg/75-02-SUMMARY.md` capturing:
    - PKG-01 completion evidence (pyproject version, graphify --version, reinstall result from 75-01)
    - PKG-02 completion evidence (local pytest summary line + linked CI run URL with both matrix legs green)
    - The two commit SHAs (chore + docs)
    - Confirmation that D7-deferred items (git tag v2.0.0, PyPI publish) are NOT done and remain for human-supervised action
    - Confirmation that D5-deferred CLAUDE.md doc fix is NOT done (out of scope)

    Then create the second atomic commit per D6:

    ```
    docs(75): record v2.0.0 release stamp

    Phase 75 (PKG) Complete: release-stamp coherence verified across pyproject.toml,
    server.json, and the 7 platform .graphify_version stamps; full pytest suite green
    on Python 3.10 and 3.12 via CI run <URL>.

    Deferred (per CONTEXT.md): git tag v2.0.0, PyPI publish (D7); CLAUDE.md mcp/server.json
    doc reference fix (D5).

    Co-Authored-By: Claude Opus 4.7 (1M context) <noreply@anthropic.com>
    ```

    Stage only `.planning/phases/75-pkg/75-02-SUMMARY.md` (and 75-01-SUMMARY.md if not already committed in chore step). Do NOT push automatically after this commit — let the developer review the two-commit topology before pushing the docs commit.
  </action>
  <verify>
    <automated>git log -2 --pretty=%s | head -1 | grep -E "^docs\(75\): record v2\.0\.0 release stamp$" && test -f .planning/phases/75-pkg/75-02-SUMMARY.md && grep -E "https://github\.com/.*/actions/runs/[0-9]+" .planning/phases/75-pkg/75-02-SUMMARY.md</automated>
  </verify>
  <done>
    - CI run on Python 3.10 AND 3.12 is green; URL captured in SUMMARY
    - `75-02-SUMMARY.md` records both requirements' acceptance evidence
    - Two atomic commits in order: chore(75) then docs(75); no squash
    - No git tag, no PyPI publish, no CLAUDE.md edit
  </done>
</task>

</tasks>

<verification>
- PKG-01: pyproject 2.0.0, bump script clean, reinstall clean, `graphify --version` == 2.0.0 (delivered by 75-01, recorded here)
- PKG-02: server.json regenerated with new manifest hash, all 7 `.graphify_version` stamps == 2.0.0 (delivered by 75-01), pytest green locally + green on CI 3.10 + 3.12 matrix (delivered by this plan)
- D6 commit topology preserved: chore(75) precedes docs(75); each commit reversible without touching the other
- D7 scope respected: no `git tag v2.0.0`, no PyPI publish in this plan
</verification>

<success_criteria>
Phase 75 is Complete. Both PKG-01 and PKG-02 acceptance text from REQUIREMENTS.md is satisfied with linked evidence. ROADMAP.md and STATE.md reflect Complete status. Two atomic commits exist in git history. The repository sits in a coherent v2.0.0 release-stamp state, ready for a human-supervised `git tag v2.0.0` + PyPI publish as separate actions outside Phase 75.
</success_criteria>

<output>
Final artifact: `.planning/phases/75-pkg/75-02-SUMMARY.md` containing PKG-01/PKG-02 evidence, both commit SHAs, the green CI run URL covering both matrix legs, and the explicit deferred-items confirmation list.
</output>
