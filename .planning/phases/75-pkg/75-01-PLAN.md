---
phase: 75-pkg
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - pyproject.toml
  - server.json
  - graphify/skill.md
  - graphify/skill-codex.md
  - graphify/skill-opencode.md
  - graphify/skill-openclaw.md
  - graphify/skill-droid.md
  - graphify/skill-trae.md
  - graphify/skill-trae-cn.md
autonomous: true
requirements: [PKG-01]
must_haves:
  truths:
    - "Phases 71, 72, 73, 74 are all marked Complete in ROADMAP.md and STATE.md before any mutation"
    - "pyproject.toml records version = \"2.0.0\""
    - "server.json is regenerated by sync_mcp_server_json.py with a manifest hash incorporating graphify_version = 2.0.0"
    - "Each of the 7 platform SKILL.md install locations has a sibling .graphify_version file whose contents read exactly 2.0.0"
    - "graphify --version prints 2.0.0 from the reinstalled package"
  artifacts:
    - path: "pyproject.toml"
      provides: "version = \"2.0.0\""
      contains: "version = \"2.0.0\""
    - path: "server.json"
      provides: "MCP manifest carrying graphify_version 2.0.0 in its hashed content"
  key_links:
    - from: "scripts/bump_version.py"
      to: "pyproject.toml"
      via: "regex-strict X.Y.Z rewrite"
      pattern: "version = \"2.0.0\""
    - from: "scripts/sync_mcp_server_json.py"
      to: "server.json"
      via: "importlib.metadata.version(\"graphifyy\") read after reinstall"
      pattern: "graphify_version.*2\\.0\\.0"
    - from: "graphify install"
      to: ".graphify_version (per platform)"
      via: "_PLATFORM_CONFIG iteration in graphify/__main__.py"
      pattern: "2.0.0"
---

<objective>
Execute the canonical bump dance (D2) — steps 1 through 6 — to move graphifyy from 1.0.0 to 2.0.0 across pyproject.toml, server.json, and the 7 platform `.graphify_version` install stamps. This plan stops just before the full test sweep; testing + commits live in 75-02.

Purpose: Deliver a coherent in-repo v2.0.0 stamp surface so that the Phase 75 release-engineering goal (PKG-01) is satisfied and PKG-02 can be proven by 75-02.

Output: Mutated `pyproject.toml`, regenerated `server.json`, refreshed `.graphify_version` stamps in all 7 platform skill install locations, and a verified `graphify --version` == `2.0.0` sanity result.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/75-pkg/75-CONTEXT.md
@CLAUDE.md
@scripts/bump_version.py
@scripts/sync_mcp_server_json.py
@pyproject.toml
@server.json

<interfaces>
<!-- Operational contracts the executor relies on. -->

From scripts/bump_version.py:
- Accepts a single positional X.Y.Z argument (regex-strict)
- Supports `--dry-run` which prints the diff and exits 0 without mutating files
- Mutates `pyproject.toml` `version = "..."` line

From scripts/sync_mcp_server_json.py:
- Reads `importlib.metadata.version("graphifyy")` (the INSTALLED version, not the file)
- Exits non-zero if installed != pyproject — forcing a `pip install -e ...` between bump and sync
- Writes `_REPO_ROOT / "server.json"` (NOT `mcp/server.json`; see D5)
- Recomputes `_meta.manifest_content_hash` over content that includes `graphify_version`

From graphify/__main__.py (`graphify install`):
- Iterates `_PLATFORM_CONFIG` (7 platforms: Claude Code, Codex, OpenCode, OpenClaw, Factory Droid, Trae, Trae CN)
- For each, writes the platform's `SKILL.md` variant AND a sibling `.graphify_version` file containing the current package version
- Absence of `.graphify_version` suppresses drift warnings — so a partial install bug is silent unless we structurally verify content
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Pre-bump dependency gate (D1) — verify 71/72/73/74 all Complete</name>
  <files>(read-only) .planning/ROADMAP.md, .planning/STATE.md</files>
  <action>
    Per D1, this is the HARD precondition: do NOT touch any file in this plan until phases 71, 72, 73, 74 are all marked Complete in BOTH `.planning/ROADMAP.md` AND `.planning/STATE.md`. Read each file and confirm the phase status rows for 71 (TEMP), 72 (REAS), 73 (DEDUP), 74 (VBUG) all show Complete/✅/done. If any phase is still In Progress, Blocked, or otherwise non-Complete, ABORT the entire phase: print `[75-01] ABORT: Phase {N} not Complete — bump dance gated per D1` to stderr and stop. Do not invoke `bump_version.py`. Do not modify any file. Surface the gating phase clearly so the human can close it before retrying Phase 75.
  </action>
  <verify>
    <automated>grep -E "Phase 7[1-4]" .planning/ROADMAP.md | grep -v -iE "complete|✅|done" | wc -l | grep -q "^[[:space:]]*0$"</automated>
  </verify>
  <done>Both ROADMAP.md and STATE.md show Complete status for all of 71, 72, 73, 74. Gate explicitly passed before any subsequent task runs.</done>
</task>

<task type="auto">
  <name>Task 2: Execute bump dance steps 1–6 (D2) — bump, reinstall, sync, install, sanity</name>
  <files>pyproject.toml, server.json, graphify/skill.md, graphify/skill-codex.md, graphify/skill-opencode.md, graphify/skill-openclaw.md, graphify/skill-droid.md, graphify/skill-trae.md, graphify/skill-trae-cn.md (and sibling `.graphify_version` files per platform install location)</files>
  <action>
    Run the canonical 6-step sequence per D2 from CWD = repo root. Fail-fast: if any step returns non-zero, ABORT — do NOT continue to the next step, do NOT attempt repair. Report which step failed and its stderr.

    Step 1 (preview, no mutation): `python scripts/bump_version.py 2.0.0 --dry-run` — confirm the diff shows `version = "1.0.0"` → `version = "2.0.0"` and nothing else.

    Step 2 (mutate pyproject): `python scripts/bump_version.py 2.0.0` — writes `pyproject.toml`. Verify post-condition: `grep -E '^version = "2\\.0\\.0"$' pyproject.toml` returns exactly one match.

    Step 3 (reinstall with CI-matching extras): `pip install -e ".[mcp,pdf,watch]"` — refreshes `importlib.metadata` so step 4's gate passes. Per D2, sync MUST follow reinstall or it fails by design.

    Step 4 (regenerate manifest): `python scripts/sync_mcp_server_json.py` — rewrites `server.json` at repo root (D5: NOT `mcp/server.json`). Verify post-condition: `grep -E '"graphify_version"\\s*:\\s*"2\\.0\\.0"' server.json` returns at least one match, and the file's `_meta.manifest_content_hash` changed from its pre-bump value.

    Step 5 (refresh skill stamps): `graphify install` — iterates `_PLATFORM_CONFIG` and writes `.graphify_version` next to each of the 7 platform `SKILL.md` files. Do NOT hand-edit any `.graphify_version` file; only the install command may write them (scope guardrail).

    Step 6 (sanity): `graphify --version` — output MUST equal `2.0.0` exactly. If it prints `1.0.0`, the reinstall in step 3 did not refresh the active interpreter's metadata — ABORT and surface the discrepancy.

    Skill-stamp structural check (D4): after step 5, for EVERY platform SKILL.md install location enumerated by `_PLATFORM_CONFIG` in `graphify/__main__.py`, assert the sibling `.graphify_version` exists AND its contents read exactly `2.0.0`. Existence alone is insufficient — a stale `1.0.0` stamp suppresses drift warnings in field (per CLAUDE.md "missing stamp = no warning" semantics, an outdated stamp is the silent-failure mode this check catches). Use this one-liner from repo root after `graphify install`:

    `python -c "from graphify.__main__ import _PLATFORM_CONFIG; from pathlib import Path; import sys; bad = [str(Path(cfg['skill_dest']).expanduser().parent / '.graphify_version') for cfg in _PLATFORM_CONFIG.values() if not (Path(cfg['skill_dest']).expanduser().parent / '.graphify_version').exists() or (Path(cfg['skill_dest']).expanduser().parent / '.graphify_version').read_text().strip() != '2.0.0']; sys.exit('STAMP DRIFT: ' + ', '.join(bad)) if bad else print('all 7 stamps == 2.0.0')"`

    If the executor finds `_PLATFORM_CONFIG` uses a different key name for the skill destination (e.g., `dest`, `skill_path`), adapt the attribute name accordingly — the contract is "for each platform entry, read the sibling `.graphify_version` of that platform's SKILL.md path." Do not skip the check.

    Per D7: do NOT run `git tag v2.0.0` and do NOT run any PyPI publish command in this task.
  </action>
  <verify>
    <automated>grep -E '^version = "2\.0\.0"$' pyproject.toml && grep -E '"graphify_version"[[:space:]]*:[[:space:]]*"2\.0\.0"' server.json && [ "$(graphify --version 2>&1 | tr -d '[:space:]')" = "2.0.0" ] && python -c "from graphify.__main__ import _PLATFORM_CONFIG; from pathlib import Path; import sys; bad = [k for k, cfg in _PLATFORM_CONFIG.items() if not any((Path(cfg['skill_dest']).expanduser().parent / '.graphify_version').read_text().strip() == '2.0.0' for _ in [0])]; sys.exit(1 if bad else 0)"</automated>
  </verify>
  <done>
    - `pyproject.toml` shows `version = "2.0.0"` (exactly one match)
    - `server.json` regenerated; contains `graphify_version: "2.0.0"`; `_meta.manifest_content_hash` differs from pre-bump
    - `graphify --version` prints `2.0.0`
    - All 7 platform `.graphify_version` stamps exist AND contain exactly `2.0.0`
    - No PyPI publish, no git tag created (D7)
    - No edits to CLAUDE.md (D5 deferred)
  </done>
</task>

</tasks>

<verification>
- Phase 75 dependency gate passed (D1) before any mutation
- 6-step bump dance completed in order with no skipped step (D2)
- All 7 skill stamps structurally verified to read `2.0.0`, not merely exist (D4)
- `server.json` mutated only via `sync_mcp_server_json.py` — no hand-edits (scope guardrail)
- CLAUDE.md untouched (D5)
- No git tag, no PyPI publish (D7)
</verification>

<success_criteria>
PKG-01 satisfied: `pyproject.toml` at 2.0.0; `bump_version.py 2.0.0` ran cleanly; `pip install -e ".[mcp,pdf,watch]"` succeeded; `graphify --version` reports `2.0.0`.

Partial PKG-02 satisfied (the in-repo half): `sync_mcp_server_json.py` regenerated `server.json` with the new manifest hash incorporating `graphify_version = 2.0.0`; `graphify install` refreshed the `.graphify_version` stamps for all 7 platforms. The pytest-matrix half of PKG-02 is delivered by 75-02.
</success_criteria>

<output>
After completion, create `.planning/phases/75-pkg/75-01-SUMMARY.md` summarizing: gate result, each bump step's exit status, pre/post `_meta.manifest_content_hash` values for `server.json`, and the per-platform `.graphify_version` content check results.
</output>
