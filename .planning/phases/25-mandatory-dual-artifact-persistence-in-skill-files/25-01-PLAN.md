---
phase: 25-mandatory-dual-artifact-persistence-in-skill-files
plan: 01
type: tdd
wave: 1
depends_on: []
files_modified:
  - tests/test_skill_persistence.py
  - graphify/skill.md
  - graphify/skill-aider.md
  - graphify/skill-claw.md
  - graphify/skill-codex.md
  - graphify/skill-copilot.md
  - graphify/skill-droid.md
  - graphify/skill-opencode.md
  - graphify/skill-trae.md
  - graphify/skill-windows.md
autonomous: true
requirements: [SKILLMEM-01, SKILLMEM-02, SKILLMEM-03, SKILLMEM-04]
tags: [skill-files, persistence-contract, dual-artifact, tdd, markdown-content]

must_haves:
  truths:
    - "Every in-scope skill file (9 variants) contains a single canonical persistence contract block delimited by the sentinel `<!-- graphify:persistence-contract:v1 -->`."
    - "The body of the contract block is byte-identical across all 9 in-scope skill variants — no drift permitted."
    - "Running `graphify install` for any of the 11 in-scope `_PLATFORM_CONFIG` entries copies a skill file whose installed bytes contain the sentinel canary. The 11 in-scope keys are: claude, codex, opencode, aider, copilot, claw, droid, trae, trae-cn, antigravity, windows (excalidraw is OUT OF SCOPE per CONTEXT.md D-04)."
    - "A pytest test suite exists that fails if any in-scope skill file is missing the sentinel, OR if any two in-scope variants have a non-byte-equal block."
    - "Existing pytest suite continues to pass — no regressions in `tests/test_install.py` or any other test module."
  artifacts:
    - path: "tests/test_skill_persistence.py"
      provides: "Canary + byte-equality drift lock for the persistence contract block"
      contains: "test_install_emits_persistence_canary, test_persistence_block_byte_equal_across_variants"
    - path: "graphify/skill.md"
      provides: "Master skill file with verbatim persistence contract block inserted between `## Usage` and `## Available slash commands`"
      contains: "<!-- graphify:persistence-contract:v1 -->"
    - path: "graphify/skill-aider.md"
      contains: "<!-- graphify:persistence-contract:v1 -->"
    - path: "graphify/skill-claw.md"
      contains: "<!-- graphify:persistence-contract:v1 -->"
    - path: "graphify/skill-codex.md"
      contains: "<!-- graphify:persistence-contract:v1 -->"
    - path: "graphify/skill-copilot.md"
      contains: "<!-- graphify:persistence-contract:v1 -->"
    - path: "graphify/skill-droid.md"
      contains: "<!-- graphify:persistence-contract:v1 -->"
    - path: "graphify/skill-opencode.md"
      contains: "<!-- graphify:persistence-contract:v1 -->"
    - path: "graphify/skill-trae.md"
      contains: "<!-- graphify:persistence-contract:v1 -->"
    - path: "graphify/skill-windows.md"
      contains: "<!-- graphify:persistence-contract:v1 -->"
  key_links:
    - from: "tests/test_skill_persistence.py::test_install_emits_persistence_canary"
      to: "graphify/__main__.py:install"
      via: "_install helper from tests/test_install.py + Path.home() mock; IN_SCOPE_PLATFORMS derived at runtime from _PLATFORM_CONFIG (excluding excalidraw)"
      pattern: "<!-- graphify:persistence-contract:v1 -->"
    - from: "tests/test_skill_persistence.py::test_persistence_block_byte_equal_across_variants"
      to: "graphify/skill*.md (9 in-scope files)"
      via: "slice from sentinel to next `## ` heading; assert mutual byte-equality"
      pattern: "<!-- graphify:persistence-contract:v1 -->"
---

<objective>
Insert a mandatory dual-artifact persistence contract block into all 9 in-scope graphify skill files (master `skill.md` + 8 platform variants), guarded by a new pytest module that enforces both presence (canary) and byte-equality (drift lock) across variants.

Purpose: Skill files are the prompt seam between graphify and host AI assistants. Without an enforced contract, response-only outputs and graph mutations drift across platforms. The sentinel-delimited block standardizes "what the skill must persist on every invocation" across all 11 supported install targets, with tests that fail loudly on any future drift.

Output: 1 new test file, 9 modified skill markdown files, all wired into existing `graphify install` byte-copy flow without any Python production-code changes.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/25-mandatory-dual-artifact-persistence-in-skill-files/25-CONTEXT.md
@.planning/phases/25-mandatory-dual-artifact-persistence-in-skill-files/25-RESEARCH.md
@.planning/phases/25-mandatory-dual-artifact-persistence-in-skill-files/25-PATTERNS.md
@.planning/phases/25-mandatory-dual-artifact-persistence-in-skill-files/25-VALIDATION.md
@CLAUDE.md
@tests/test_install.py
@graphify/__main__.py
@graphify/skill.md

<interfaces>
<!-- Key contracts the executor needs. No exploration required. -->

The verbatim contract block is defined in `.planning/phases/25-mandatory-dual-artifact-persistence-in-skill-files/25-RESEARCH.md` under the proposed-contract section. It MUST be copied byte-for-byte; do not paraphrase, reformat, or "clean up" whitespace.

Required structure of the block (per RESEARCH.md and CONTEXT.md D-01..D-06):
- Opens with HTML sentinel comment: `<!-- graphify:persistence-contract:v1 -->`
- Followed by `## Mandatory response persistence` H2 heading
- Body (~50 lines) describing the dual-artifact requirement
- Terminates implicitly at the next `## ` heading (used by the byte-equality test slicer)

In-scope skill files (9 total — `skill-excalidraw.md` is OUT OF SCOPE per CONTEXT.md D-04):
  graphify/skill.md            (master — installed by `claude` and `antigravity` per `_PLATFORM_CONFIG`)
  graphify/skill-aider.md
  graphify/skill-claw.md
  graphify/skill-codex.md
  graphify/skill-copilot.md
  graphify/skill-droid.md
  graphify/skill-opencode.md
  graphify/skill-trae.md
  graphify/skill-windows.md

`_PLATFORM_CONFIG` keys covered by the canary test — 11 in-scope keys (the canonical set, derived at runtime from `_PLATFORM_CONFIG` minus `excalidraw`):
  claude, codex, opencode, aider, copilot, claw, droid, trae, trae-cn, antigravity, windows

DO NOT use any other names anywhere in the plan, tests, or comments. The keys `agent` and `cursor` are NOT entries in `_PLATFORM_CONFIG` and must never appear.

Platform → skill file mapping (relevant to Task 2 partial-GREEN checkpoint):
  - `claude` → `skill_file: "skill.md"` (per `__main__.py` ~L51)
  - `antigravity` → `skill_file: "skill.md"` (per `__main__.py` ~L132)
  - `codex` → `skill_file: "skill-codex.md"`
  - `opencode` → `skill_file: "skill-opencode.md"`
  - `aider` → `skill_file: "skill-aider.md"`
  - `copilot` → `skill_file: "skill-copilot.md"`
  - `claw` → `skill_file: "skill-claw.md"`
  - `droid` → `skill_file: "skill-droid.md"`
  - `trae` → `skill_file: "skill-trae.md"`
  - `trae-cn` → `skill_file: "skill-trae.md"` (shares the trae variant)
  - `windows` → `skill_file: "skill-windows.md"`

Insertion anchor (heading-based, NOT line-number-based — line numbers vary per variant):
  Insert AFTER the `## Usage` section, BEFORE the `## Available slash commands` section.
  If a variant lacks `## Available slash commands`, insert before the next `## ` heading following `## Usage`.

Test helper to reuse (from `tests/test_install.py:18-23`):
  ```python
  def _install(tmp_path, platform):
      with mock.patch.object(Path, "home", return_value=tmp_path):
          # invoke graphify install <platform>
          ...
  ```
  Use `Path.home()` mocking to redirect installs into `tmp_path`.

`graphify install` already byte-copies `skill_file` -> `skill_dst` via `shutil.copy` in `graphify/__main__.py`. NO Python production changes required — adding the block to source skill files makes it ride along automatically.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1 (RED): Create failing pytest module enforcing canary + byte-equality drift lock</name>
  <files>tests/test_skill_persistence.py</files>
  <read_first>
    - .planning/phases/25-mandatory-dual-artifact-persistence-in-skill-files/25-RESEARCH.md (test design + verbatim contract block reference)
    - .planning/phases/25-mandatory-dual-artifact-persistence-in-skill-files/25-VALIDATION.md (Nyquist mapping for both tests)
    - tests/test_install.py (lines 1-50; reuse `_install` helper pattern + `Path.home()` mock idiom from lines 18-23)
    - graphify/__main__.py (locate `_PLATFORM_CONFIG` dict; confirm 12 entries and `skill_file` field per entry — the 12 keys are: claude, codex, opencode, aider, copilot, claw, droid, trae, trae-cn, antigravity, excalidraw, windows)
    - CLAUDE.md (testing conventions: pytest, `tmp_path`, `from __future__ import annotations`, no network/FS side effects outside `tmp_path`)
  </read_first>
  <behavior>
    - test_install_emits_persistence_canary[platform]: parametrized over the 11 in-scope `_PLATFORM_CONFIG` keys (all except `excalidraw`), DERIVED AT RUNTIME from the source dict so the test cannot drift from production. For each platform:
        1. Mock `Path.home()` to `tmp_path`.
        2. Invoke the install codepath for that platform (mirror `tests/test_install.py:_install`).
        3. Read the installed skill destination file.
        4. Assert the bytes `<!-- graphify:persistence-contract:v1 -->` appear in the installed file.
    - test_persistence_block_byte_equal_across_variants: no parametrization.
        1. For each of the 9 in-scope source files (`graphify/skill.md` and the 8 `skill-<platform>.md` variants except `skill-excalidraw.md`), read raw bytes.
        2. Slice each file from the first occurrence of `<!-- graphify:persistence-contract:v1 -->` (inclusive) up to (but not including) the next line that starts with `## ` after that sentinel.
        3. Assert all 9 slices are byte-identical to the slice from `graphify/skill.md` (use `skill.md` as the reference). Failure message must name the divergent file(s).
        4. Assert each slice is non-empty (guards against the sentinel existing without a body).
    - Both tests MUST fail in this task (no contract block has been added to any skill file yet). Failure mode: missing sentinel = AssertionError with file path; this is the expected RED state.
  </behavior>
  <action>
    Create `tests/test_skill_persistence.py` with:
      - `from __future__ import annotations` as the first import (project convention).
      - Stdlib imports: `pathlib.Path`, `unittest.mock`, `pytest`, `re` (for the heading-boundary slice).
      - Module-level constants:
          `SENTINEL = "<!-- graphify:persistence-contract:v1 -->"`
          `# IN_SCOPE_PLATFORMS is derived at runtime from graphify._PLATFORM_CONFIG`
          `# so the test cannot drift if a new platform is added or renamed.`
          `from graphify.__main__ import _PLATFORM_CONFIG`
          `IN_SCOPE_PLATFORMS = sorted(k for k in _PLATFORM_CONFIG if k != "excalidraw")`
          `IN_SCOPE_SKILL_FILES = ["skill.md", "skill-aider.md", "skill-claw.md", "skill-codex.md", "skill-copilot.md", "skill-droid.md", "skill-opencode.md", "skill-trae.md", "skill-windows.md"]`
      - At test-collection time, assert that `IN_SCOPE_PLATFORMS` has exactly 11 entries (sanity check that `_PLATFORM_CONFIG` still contains the expected 12 keys minus `excalidraw`). If this assertion fails, the test setup itself fails loudly with a clear message — preventing silent under-coverage.
      - A helper `_extract_block(text: str) -> str` that finds `SENTINEL`, then uses a regex like `re.search(r"\n## ", text[start_after_sentinel:])` to find the next H2 boundary, returning the slice from sentinel through (but not including) that next `## ` line. Returns empty string if sentinel absent.
      - `test_install_emits_persistence_canary` parametrized via `@pytest.mark.parametrize("platform", IN_SCOPE_PLATFORMS)`. Reuse the install-helper pattern from `tests/test_install.py:18-23` (mock `Path.home()` to `tmp_path`, run `graphify install <platform>`, locate the installed skill destination via that platform's `_PLATFORM_CONFIG[platform]["skill_dst"]` resolved relative to mocked home). Read installed file; assert `SENTINEL.encode() in installed_bytes` (byte-level containment, not substring on decoded text — content may have encoding subtleties).
      - `test_persistence_block_byte_equal_across_variants` reads each in-scope source file from `graphify/`, calls `_extract_block` on each, asserts every slice equals the slice from `skill.md`, with a per-file diagnostic on mismatch.
    Run `pytest tests/test_skill_persistence.py -q` and confirm BOTH tests fail (this is the RED state). Commit message: `test(25-01): add failing pytest module for persistence contract canary + drift lock (SKILLMEM-04)`.
  </action>
  <verify>
    <automated>pytest tests/test_skill_persistence.py -q; [ $? -ne 0 ] && pytest tests/ --ignore=tests/test_skill_persistence.py -q</automated>
  </verify>
  <acceptance_criteria>
    - File `tests/test_skill_persistence.py` exists.
    - `grep -F "SENTINEL = \"<!-- graphify:persistence-contract:v1 -->\"" tests/test_skill_persistence.py` returns the line.
    - `grep -F "from graphify.__main__ import _PLATFORM_CONFIG" tests/test_skill_persistence.py` returns the line (runtime derivation, not a hardcoded literal).
    - `grep -F "test_install_emits_persistence_canary" tests/test_skill_persistence.py` returns the def line.
    - `grep -F "test_persistence_block_byte_equal_across_variants" tests/test_skill_persistence.py` returns the def line.
    - `grep -E "\bagent\b|\bcursor\b" tests/test_skill_persistence.py` returns NOTHING (these are not real `_PLATFORM_CONFIG` keys and must not appear anywhere in the test).
    - `pytest tests/test_skill_persistence.py -q` exits NON-ZERO (RED state) — both tests fail because no skill file contains the sentinel yet. Use the explicit exit-code gate `pytest ...; [ $? -ne 0 ]` rather than grepping output text.
    - `pytest tests/ --ignore=tests/test_skill_persistence.py -q` exits 0 (no regressions to existing suite — the new test file does not break anything else by its mere presence).
  </acceptance_criteria>
  <done>The new test module exists, both tests are visible to pytest, both fail with assertion errors complaining about the missing sentinel — confirming the test correctly detects the absence of the contract block. The IN_SCOPE_PLATFORMS list is derived from `_PLATFORM_CONFIG` at runtime (no hardcoded literal), and the existing test suite is still green.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2 (GREEN, master): Insert verbatim contract block into graphify/skill.md</name>
  <files>graphify/skill.md</files>
  <read_first>
    - .planning/phases/25-mandatory-dual-artifact-persistence-in-skill-files/25-RESEARCH.md (the FULL ~50-line verbatim contract block — copy bytes exactly; do NOT paraphrase or reflow whitespace)
    - .planning/phases/25-mandatory-dual-artifact-persistence-in-skill-files/25-CONTEXT.md (D-01..D-06 locked decisions about block content)
    - graphify/skill.md (locate the `## Usage` heading and the next `## ` heading after it, typically `## Available slash commands` — these are the insertion anchors)
    - graphify/__main__.py (confirm `claude` and `antigravity` both reference `skill_file: "skill.md"` — these are the two platforms expected to pass the canary test after this task)
  </read_first>
  <action>
    Open `graphify/skill.md`. Locate the line that starts with `## Usage`. Find the NEXT line that starts with `## ` after that. Insert the verbatim contract block from RESEARCH.md between those two `## ` headings, separated by exactly one blank line on each side.

    The block to insert begins with the literal line `<!-- graphify:persistence-contract:v1 -->`, followed by a blank line, followed by `## Mandatory response persistence`, followed by the body specified in RESEARCH.md, ending with a single trailing newline before the next existing `## ` heading.

    DO NOT modify any other content in `skill.md`. DO NOT alter line endings or whitespace within the block — the byte-equality test in Task 1 compares raw bytes across variants.

    Run `pytest tests/test_skill_persistence.py -q`. Expected partial-GREEN state:
      - `test_install_emits_persistence_canary[claude]` and `test_install_emits_persistence_canary[antigravity]` PASS (these two platforms install `skill.md` per `_PLATFORM_CONFIG` — see `__main__.py:51,132`).
      - `test_install_emits_persistence_canary[codex|opencode|aider|copilot|claw|droid|trae|trae-cn|windows]` still FAIL (variants don't have the block yet — 9 platforms still red).
      - `test_persistence_block_byte_equal_across_variants` FAILS (only 1 of 9 source files has the block).

    This partial-pass state is the expected mid-GREEN checkpoint. Commit message: `feat(25-01): insert verbatim persistence contract block into master skill.md (SKILLMEM-01)`.
  </action>
  <verify>
    <automated>grep -cF "<!-- graphify:persistence-contract:v1 -->" graphify/skill.md && grep -cF "## Mandatory response persistence" graphify/skill.md && pytest tests/test_skill_persistence.py::test_install_emits_persistence_canary -q -k "claude or antigravity"</automated>
  </verify>
  <acceptance_criteria>
    - `grep -cF "<!-- graphify:persistence-contract:v1 -->" graphify/skill.md` returns `1` (sentinel present exactly once).
    - `grep -cF "## Mandatory response persistence" graphify/skill.md` returns `1`.
    - `awk '/<!-- graphify:persistence-contract:v1 -->/,/^## /' graphify/skill.md | wc -l` returns a value &gt; 10 (block has substantive body, not just sentinel + heading).
    - `pytest tests/test_skill_persistence.py::test_install_emits_persistence_canary -q -k "claude or antigravity"` exits 0 — both platforms that consume `skill.md` (per `_PLATFORM_CONFIG`) now pass the canary. This is the partial-GREEN checkpoint: 2 of 11 platforms covered.
    - No content outside the inserted block has been modified: `git diff graphify/skill.md` should show ONLY additions (no deletions, no re-flowed surrounding text).
  </acceptance_criteria>
  <done>Master `skill.md` contains the verbatim contract block at the correct anchor; the 2 platforms that install `skill.md` (claude, antigravity) now pass the canary test. The remaining 9 platforms remain red until Task 3 fans out the block.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3 (GREEN, fan-out): Copy verbatim contract block into 8 platform variants</name>
  <files>graphify/skill-aider.md, graphify/skill-claw.md, graphify/skill-codex.md, graphify/skill-copilot.md, graphify/skill-droid.md, graphify/skill-opencode.md, graphify/skill-trae.md, graphify/skill-windows.md</files>
  <read_first>
    - graphify/skill.md (the now-canonical block — slice from `<!-- graphify:persistence-contract:v1 -->` up to but not including the next `## ` heading; this is the SOURCE OF TRUTH for byte-for-byte copying into the 8 variants)
    - Each of the 8 target variants — locate each variant's `## Usage` heading and the next `## ` heading after it. Anchor placement is heading-based; line numbers differ per variant.
    - .planning/phases/25-mandatory-dual-artifact-persistence-in-skill-files/25-PATTERNS.md (variant heading-structure analogs)
  </read_first>
  <action>
    For each of the 8 in-scope variant files in `<files>` above, repeat the same insertion done in Task 2:

      1. Locate the `## Usage` heading.
      2. Locate the NEXT `## ` heading after `## Usage` (most variants will have `## Available slash commands`; if a variant uses a different heading, use whatever next `## ` heading comes first — heading anchors, not line numbers).
      3. Paste the EXACT byte slice extracted from `graphify/skill.md` between sentinel and next `## ` heading. Same blank-line padding (one blank line above sentinel, one blank line before the trailing `## ` heading) as in `skill.md`.
      4. Do not modify any other content in the variant.

    Critical: copy from `graphify/skill.md` byte-for-byte. If any variant uses different line endings (CRLF) than `skill.md`, that would break byte-equality — verify all 9 files use `\n` (LF) line endings via `file graphify/skill*.md` or `od -c | head` checks. The repo is unix; LF is expected.

    After all 8 variants are updated, run the full test:
      `pytest tests/test_skill_persistence.py -q`
    Expected: ALL tests PASS — 11 parametrized canary tests + the 1 byte-equality test = 12 passing.

    Then run the full suite to confirm no regressions:
      `pytest tests/ -q`

    Commit message: `feat(25-01): replicate persistence contract block across 8 skill variants (SKILLMEM-02, SKILLMEM-03)`.
  </action>
  <verify>
    <automated>pytest tests/test_skill_persistence.py -q && pytest tests/ -q</automated>
  </verify>
  <acceptance_criteria>
    - For each variant V in {skill-aider.md, skill-claw.md, skill-codex.md, skill-copilot.md, skill-droid.md, skill-opencode.md, skill-trae.md, skill-windows.md}: `grep -cF "<!-- graphify:persistence-contract:v1 -->" graphify/$V` returns `1`.
    - `for f in graphify/skill.md graphify/skill-aider.md graphify/skill-claw.md graphify/skill-codex.md graphify/skill-copilot.md graphify/skill-droid.md graphify/skill-opencode.md graphify/skill-trae.md graphify/skill-windows.md; do awk '/<!-- graphify:persistence-contract:v1 -->/,/^## /' "$f" | head -n -1 | md5sum; done | sort -u | wc -l` returns `1` (all 9 block slices are byte-identical, hash to the same digest).
    - `grep -cF "<!-- graphify:persistence-contract:v1 -->" graphify/skill-excalidraw.md` returns `0` (out-of-scope variant correctly excluded).
    - `pytest tests/test_skill_persistence.py -q` exits 0 with all 12 tests (11 parametrized canary covering claude, codex, opencode, aider, copilot, claw, droid, trae, trae-cn, antigravity, windows + 1 drift lock) passing.
    - `pytest tests/ -q` exits 0 — no regressions in any existing test module.
  </acceptance_criteria>
  <done>All 9 in-scope skill files contain the byte-identical contract block, the excalidraw variant is correctly untouched, both new tests pass for all 11 platforms (claude, codex, opencode, aider, copilot, claw, droid, trae, trae-cn, antigravity, windows), and the full pytest suite is green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

No new trust boundaries introduced — this phase modifies only static markdown content shipped in the package. No runtime input handling, no path manipulation, no external IO.

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-25-00 | n/a | n/a | accept | No new threats — content addition only. The change is markdown text inserted into 9 source files that are byte-copied by the existing, unchanged `graphify install` codepath. No new attack surface, no new dependencies, no new code paths exercised at runtime. The byte-equality test acts as a tampering detector for accidental drift in CI but is not a security control. |
</threat_model>

<verification>
Phase-level verification (run after Task 3 completes):

1. `pytest tests/ -q` — full suite green, including the 12 new test cases in `tests/test_skill_persistence.py`.
2. `grep -lF "<!-- graphify:persistence-contract:v1 -->" graphify/skill*.md | sort` — returns exactly 9 paths (the in-scope set), excluding `skill-excalidraw.md`.
3. Byte-equality spot check across 3 variants:
     `diff <(awk '/<!-- graphify:persistence-contract:v1 -->/,/^## /' graphify/skill.md | head -n -1) \
           <(awk '/<!-- graphify:persistence-contract:v1 -->/,/^## /' graphify/skill-codex.md | head -n -1)` returns no output.
4. Manual smoke: `python -c "import graphify; ..."` — package still imports cleanly (no Python changes were expected, but confirms no accidental damage to the package).
5. Confirm `graphify install` still works for one platform end-to-end via the standard install test in `tests/test_install.py` (already covered by step 1).
</verification>

<success_criteria>
- All 4 SKILLMEM requirement IDs satisfied:
    - SKILLMEM-01: master skill.md contains the verbatim block at the correct anchor.
    - SKILLMEM-02: all 8 in-scope variants contain the block.
    - SKILLMEM-03: all 9 blocks are byte-identical across variants.
    - SKILLMEM-04: enforcement tests exist in `tests/test_skill_persistence.py` and pass.
- `pytest tests/ -q` exits 0.
- 12 new test cases in `tests/test_skill_persistence.py` all pass (11 parametrized canary covering claude, codex, opencode, aider, copilot, claw, droid, trae, trae-cn, antigravity, windows + 1 drift-lock).
- `skill-excalidraw.md` is untouched (out-of-scope per CONTEXT.md D-04).
- No Python production code (`graphify/*.py`) was modified.
- 3 atomic commits land on the branch corresponding to Tasks 1, 2, 3.
</success_criteria>

<output>
After completion, create `.planning/phases/25-mandatory-dual-artifact-persistence-in-skill-files/25-01-SUMMARY.md` per the GSD summary template, including:
  - `requirements_completed: [SKILLMEM-01, SKILLMEM-02, SKILLMEM-03, SKILLMEM-04]` in frontmatter
  - The 3 commit SHAs (RED, GREEN-master, GREEN-fanout)
  - Confirmation that `skill-excalidraw.md` was correctly left untouched
  - Note that no Python production code was modified
</output>
