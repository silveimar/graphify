---
id: 64-02
phase: 64-audit-a-stderr-format-snapshot-lock-sweep
plan: 02
title: AUDIT-02 stderr Outlier Sweep — grep-and-migrate to error:/info:/hint:
type: execute
wave: 2
depends_on: ["64-01"]
files_modified:
  - graphify/__main__.py
  - graphify/serve.py
  - graphify/profile.py
  - graphify/naming.py
  - graphify/vault_promote.py
  - graphify/watch.py
  - graphify/seed.py
  - graphify/dedup.py
  - tests/test_stderr_contract.py
autonomous: true
requirements: [AUDIT-02]
must_haves:
  truths:
    - "Every print(..., file=sys.stderr) call site under graphify/ (excluding skill-*.md docs and test fixtures) starts with `[graphify] (error|info|hint): ` or `  hint: ` (D-04)"
    - "The snapshot test from Plan 01 still passes after sweep (no regression to locked emitters)"
    - "A grep-based invariant test asserts zero non-compliant prefixes remain"
  artifacts:
    - path: "graphify/__main__.py"
      provides: "Migrated outliers (lines ~316, 323, 546, 1826, 1832, 1834, 1965, 2008, 2009, 2012, 2023, 2066, 2121, ~2745) to compliant prefixes"
      contains: "[graphify] error:"
    - path: "graphify/serve.py"
      provides: "Migrated lines 160, 520 to [graphify] error:/info: prefix"
    - path: "tests/test_stderr_contract.py"
      provides: "New test_no_outlier_stderr_prefixes_in_source enforcing grep invariant"
      contains: "def test_no_outlier_stderr_prefixes_in_source"
  key_links:
    - from: "tests/test_stderr_contract.py::test_no_outlier_stderr_prefixes_in_source"
      to: "graphify/*.py"
      via: "subprocess.check_output(['grep','-rEn','print\\([^)]*file=sys\\.stderr', 'graphify/'])"
      pattern: "file=sys\\.stderr"
---

<objective>
Sweep every `print(..., file=sys.stderr)` call site under `graphify/` (excluding `skill-*.md` skill docs which are templates, not runtime code) and migrate non-compliant prefixes to `[graphify] error: ` / `[graphify] info: ` / `  hint: ` per D-02 (grep-and-migrate-all, no exceptions list) and D-04 (strict whitelist). Add an invariant test that fails CI if a new outlier is introduced.

Purpose: Phase 65 extractor refactor will add stderr lines; without sweep + invariant, skill regexes break silently.
Output: Migrated source files + new invariant test in tests/test_stderr_contract.py.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/phases/64-audit-a-stderr-format-snapshot-lock-sweep/64-CONTEXT.md
@.planning/phases/64-audit-a-stderr-format-snapshot-lock-sweep/64-01-PLAN.md
@graphify/__main__.py
@graphify/serve.py
@graphify/profile.py
@graphify/naming.py
@graphify/vault_promote.py
@graphify/watch.py
@graphify/seed.py
@graphify/dedup.py

<interfaces>
<!-- Compliant prefix shapes (D-04 whitelist): -->
<!--   "[graphify] error: <msg>"        — fatal/exit conditions -->
<!--   "[graphify] info: <msg>"         — informational notices -->
<!--   "  hint: <msg>"                  — secondary line following error: or info: -->
<!-- Non-compliant outliers found by initial grep (representative, NOT exhaustive — task MUST re-grep): -->
<!--   __main__.py:316,323  "error: --repo-identity requires a value"        → "[graphify] error: --repo-identity requires a value" -->
<!--   __main__.py:546      "error: {skill_file} not found ..."              → "[graphify] error: {skill_file} not found ..." -->
<!--   __main__.py:1826     "Usage: graphify --validate-profile <vault-path>" → "[graphify] error: usage: graphify --validate-profile <vault-path>" -->
<!--   __main__.py:1832     "error: {err}"                                   → "[graphify] error: {err}" -->
<!--   __main__.py:1834     "warning: {warn}"                                → "[graphify] info: {warn}" (warning re-categorized as info per D-04 — only 3 prefixes allowed) -->
<!--   __main__.py:1965,1965,2008,2012,2023,2066,2121  "error: ..."          → "[graphify] error: ..." -->
<!--   __main__.py:2009     "hint: run /graphify ..."                        → "  hint: run /graphify ..." (note 2-space indent per _emit_vault_error shape) -->
<!--   __main__.py:~2745    (one-liner outlier per CONTEXT.md)               → migrate to "[graphify] error:" or "info:" + "  hint:" -->
<!--   serve.py:520         "error: {exc}"                                   → "[graphify] error: {exc}" -->
<!-- Compliance map (verify each before editing):
     - output.py emitters: ALREADY COMPLIANT — DO NOT TOUCH.
     - profile.py:665,672 ([graphify] profile error:): ALREADY COMPLIANT.
     - naming.py:117,122 ([graphify] ... warning ...): REQUIRES MIGRATION — "warning:" → "info:" per D-04.
     - serve.py:120,160 ([graphify] warning:/serve:): REQUIRES MIGRATION — "warning:" → "info:" per D-04; bare "serve:" → "info:" per D-04.
     - vault_promote.py:1399, watch.py:89, seed.py:498, dedup.py:228: VERIFY via grep, migrate any non-whitelist prefix to error/info/hint per D-04.
     - __main__.py: known one-liner outlier near ~2745 plus full grep sweep. -->
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Enumerate and migrate every non-compliant stderr call site</name>
  <read_first>
    - All files listed in `files_modified` above (one read per file, search for `file=sys.stderr`)
    - .planning/phases/64-audit-a-stderr-format-snapshot-lock-sweep/64-CONTEXT.md (D-02, D-04)
    - graphify/output.py lines 70–135 (compliant emitter shapes — match these exactly)
  </read_first>
  <files>graphify/__main__.py, graphify/serve.py, graphify/profile.py, graphify/naming.py, graphify/vault_promote.py, graphify/watch.py, graphify/seed.py, graphify/dedup.py</files>
  <action>
    1. Run: `grep -rEn 'print\([^)]*file=sys\.stderr' graphify/ --include="*.py"` to enumerate ALL Python call sites. Record each line.
    2. For each call site, classify the existing message into one of three buckets:
       - Fatal/exit-causing or raises after print → `[graphify] error: <msg>`
       - Informational/progress → `[graphify] info: <msg>`
       - Secondary line directly following an error/info print → `  hint: <msg>` (TWO leading spaces, lowercase 'hint:')
    3. Migrate every non-compliant call site to the compliant form. SPECIFIC RULES:
       - "error: ..." → "[graphify] error: ..."
       - "Usage: ..." → "[graphify] error: usage: ..." (lowercased)
       - "warning: ..." → "[graphify] info: ..." (D-04 whitelist allows ONLY error/info/hint — warning is folded into info)
       - "[graphify] warning: ..." → "[graphify] info: ..."
       - "[graphify] serve: ..." (or any other namespace prefix like "migrate-legacy:", "watch:", "diagram-seeds:", "Loading embedding model") → "[graphify] info: <namespace>: ..." preserving the namespace as part of the message body, e.g. "[graphify] info: serve: enrichment.json unreadable: {exc}"
       - bare "hint: ..." → "  hint: ..." (note 2-space indent)
       - "[graphify] Invalid choice; try again." (no prefix word) → "[graphify] error: invalid choice; try again."
       - "[graphify] {msg}" (line 77 of output.py is internal — leave alone since wrapped by emit functions; verify it's not a user-facing path)
    4. DO NOT modify graphify/output.py emitters (locked by Plan 01 snapshot).
    5. DO NOT modify graphify/skill*.md files (documentation, not runtime code).
    6. Re-run: `grep -rEn 'print\([^)]*file=sys\.stderr' graphify/ --include="*.py" | grep -v -E '(\[graphify\] (error|info)|^[^:]+: *[0-9]+: *print\(f?"  hint:)' | grep -v 'graphify/output.py'`. Output MUST be empty.
  </action>
  <verify>
    <automated>pytest tests/test_stderr_contract.py -x && python3 -c "import subprocess,sys; out=subprocess.check_output(['grep','-rEn','print\\([^)]*file=sys\\.stderr','graphify/','--include=*.py'],text=True); bad=[l for l in out.splitlines() if 'graphify/output.py' not in l and not (('\"[graphify] error:' in l) or ('\"[graphify] info:' in l) or (\"'  hint:\" in l) or ('\"  hint:' in l) or ('f\"[graphify] error:' in l) or ('f\"[graphify] info:' in l) or ('f\"  hint:' in l))]; sys.exit(0 if not bad else (print('\\n'.join(bad)) or 1))"</automated>
  </verify>
  <done>Zero non-compliant stderr prefixes remain in graphify/*.py (excluding output.py emitters); Plan 01 snapshot test still passes; commit: `chore(64-02): migrate stderr outliers to [graphify] error/info/hint (AUDIT-02)`.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Add grep-invariant test that fails CI on new outliers</name>
  <read_first>
    - tests/test_stderr_contract.py (Plan 01 module — append new test)
    - graphify/output.py (the only file allowed to emit raw "[graphify] {msg}" without prefix word; whitelist it)
  </read_first>
  <files>tests/test_stderr_contract.py</files>
  <behavior>
    - test_no_outlier_stderr_prefixes_in_source: subprocess greps for `print\([^)]*file=sys\.stderr` under graphify/ (Python files only). For each match line, the literal print argument MUST start with one of: `"[graphify] error:`, `"[graphify] info:`, `"  hint:`, `f"[graphify] error:`, `f"[graphify] info:`, `f"  hint:`. The single allowed exception is graphify/output.py (whitelisted via path filter) since its raw `[graphify] {msg}` form is wrapped by tested emit_* functions. Any non-compliant line fails the test with a list of offenders.
  </behavior>
  <action>
    Append `test_no_outlier_stderr_prefixes_in_source` to tests/test_stderr_contract.py. Implementation:
    ```python
    import subprocess, re
    from pathlib import Path
    def test_no_outlier_stderr_prefixes_in_source():
        repo = Path(__file__).resolve().parents[1]
        out = subprocess.check_output(
            ["grep", "-rEn", r"print\([^)]*file=sys\.stderr", "graphify/", "--include=*.py"],
            cwd=repo, text=True,
        )
        allowed = re.compile(r'(f?"\[graphify\] (error|info): |f?"  hint: )')
        offenders = []
        for line in out.splitlines():
            path = line.split(":", 1)[0]
            if path == "graphify/output.py":
                continue
            if not allowed.search(line):
                offenders.append(line)
        assert not offenders, "Non-compliant stderr prefixes found:\n" + "\n".join(offenders)
    ```
    Run pytest — MUST pass after Task 1 sweep. Commit: `test(64-02): grep-invariant — no outlier stderr prefixes (AUDIT-02)`.
  </action>
  <verify>
    <automated>pytest tests/test_stderr_contract.py::test_no_outlier_stderr_prefixes_in_source -x</automated>
  </verify>
  <done>Test exists and passes; introducing any non-compliant `print(..., file=sys.stderr)` under graphify/ (other than output.py) causes pytest failure.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| (none new) | Pure source migration — no new external input, no new FS/network surface. Messages already shown to operator on stderr. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-64-03 | I (Info disclosure) | Migrated stderr messages | accept | f-string interpolated values (e.g., `{exc}`, `{warn}`) already exposed pre-migration; prefix change does not alter content. |
| T-64-04 | T (Tampering) | grep-invariant test | mitigate | Test is in tests/, fails CI if disabled or skipped. Removing it requires PR review. |

Non-applicable: S/R/D/E — pure refactor of stderr formatting, no auth boundary, no privileged ops, no DoS surface. Injection/SSRF: N/A (no URL/shell evaluation introduced).
</threat_model>

<verification>
- `grep -rEn 'print\([^)]*file=sys\.stderr' graphify/ --include='*.py' | grep -v graphify/output.py | grep -vE '\[graphify\] (error|info):|"  hint:|f"  hint:'` returns zero lines
- pytest tests/test_stderr_contract.py passes (all 5 tests: 4 from Plan 01 + 1 invariant from this plan)
- pytest tests/ -q overall green (no regression)
</verification>

<success_criteria>
- All non-compliant stderr prefixes migrated under graphify/ Python source
- Invariant test enforces D-04 whitelist going forward
- Plan 01 snapshot still GREEN
</success_criteria>

<output>
After completion, create `.planning/phases/64-audit-a-stderr-format-snapshot-lock-sweep/64-02-SUMMARY.md`
</output>
