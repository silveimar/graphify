---
id: 64-03
phase: 64-audit-a-stderr-format-snapshot-lock-sweep
plan: 03
title: AUDIT-02 Skill Regex Fixture — 7 Platform SKILL.md Contracts (TDD)
type: tdd
wave: 2
depends_on: ["64-01"]
files_modified:
  - tests/fixtures/skill_stderr_regexes.yaml
  - tests/test_skill_regex_fixture.py
autonomous: true
requirements: [AUDIT-02]
must_haves:
  truths:
    - "A hand-curated YAML fixture lists one regex per platform skill file, totaling exactly 7 entries (claude-code, codex, opencode, openclaw, factory-droid, trae, trae-cn)"
    - "Each regex matches at least one line of the locked stderr_contract.txt fixture from Plan 01"
    - "Parser/validator test loads the YAML and asserts every regex (a) compiles, (b) matches >=1 fixture line"
    - "If any platform's SKILL.md contract pattern changes, the test fails until the fixture is updated"
  artifacts:
    - path: "tests/fixtures/skill_stderr_regexes.yaml"
      provides: "Hand-curated map: platform → regex extracted from that platform's SKILL.md"
      contains: "claude-code:"
    - path: "tests/test_skill_regex_fixture.py"
      provides: "Pytest module that compiles each regex and matches against tests/fixtures/stderr_contract.txt"
      exports: ["test_all_seven_platforms_present", "test_each_regex_compiles", "test_each_regex_matches_locked_contract"]
  key_links:
    - from: "tests/test_skill_regex_fixture.py"
      to: "tests/fixtures/skill_stderr_regexes.yaml"
      via: "yaml.safe_load"
      pattern: "skill_stderr_regexes.yaml"
    - from: "tests/test_skill_regex_fixture.py"
      to: "tests/fixtures/stderr_contract.txt"
      via: "Path read + per-line regex.search"
      pattern: "stderr_contract.txt"
---

<objective>
Hand-curate a YAML fixture mapping each of the 7 platform skill files to the regex pattern that platform uses (or implicitly relies on) to parse graphify's stderr output. Add a pytest module that compiles each regex and matches it against the locked snapshot from Plan 01. This implements D-03 (hand-curated platform/regex fixture) from 64-CONTEXT.md.

Purpose: Each platform's SKILL.md (claude-code, codex, opencode, openclaw, factory-droid, trae, trae-cn) tells the agent how to parse graphify stderr. If a future emitter change breaks any of those parsing contracts, this fixture catches it.
Output: tests/fixtures/skill_stderr_regexes.yaml (7 entries) + tests/test_skill_regex_fixture.py (parser/validator).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/phases/64-audit-a-stderr-format-snapshot-lock-sweep/64-CONTEXT.md
@graphify/skill.md
@graphify/skill-codex.md
@graphify/skill-opencode.md
@graphify/skill-claw.md
@graphify/skill-trae.md
@CLAUDE.md

<interfaces>
<!-- D-03: hand-curate one regex per platform SKILL.md by reading each file. -->
<!-- Platform list (7, exact slugs as fixture keys): -->
<!--   claude-code     → graphify/skill.md -->
<!--   codex           → graphify/skill-codex.md -->
<!--   opencode        → graphify/skill-opencode.md -->
<!--   openclaw        → graphify/skill-claw.md -->
<!--   factory-droid   → graphify/skill-droid.md  (verify exact filename via ls graphify/skill-*.md) -->
<!--   trae            → graphify/skill-trae.md -->
<!--   trae-cn         → graphify/skill-trae-cn.md  (verify exact filename) -->
<!-- For each SKILL.md, search for stderr-parsing instructions: regex literals, "[graphify]" mentions, "error:"/"info:"/"hint:" prose. Distill a single regex that captures the contract that file relies on. -->
<!-- Minimum acceptable regex for ALL platforms (default if no platform-specific pattern asserted): -->
<!--   ^\[graphify\] (error|info): .+ -->
<!-- Platforms whose SKILL.md mentions hints explicitly should also include `  hint:` matching: -->
<!--   ^(\[graphify\] (error|info): |  hint: ).+ -->
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: RED — Test scaffold + empty fixture skeleton</name>
  <read_first>
    - .planning/phases/64-audit-a-stderr-format-snapshot-lock-sweep/64-CONTEXT.md (D-03)
    - tests/fixtures/stderr_contract.txt (from Plan 01 — the lines we will match against)
    - Run `ls graphify/skill-*.md graphify/skill.md` to confirm exact filenames for all 7 platforms
  </read_first>
  <files>tests/test_skill_regex_fixture.py, tests/fixtures/skill_stderr_regexes.yaml</files>
  <behavior>
    - test_all_seven_platforms_present: yaml.safe_load(fixture) returns a dict whose keys equal exactly {"claude-code", "codex", "opencode", "openclaw", "factory-droid", "trae", "trae-cn"} (7 entries, no more, no less)
    - test_each_regex_compiles: for each entry, re.compile(value) does not raise
    - test_each_regex_matches_locked_contract: for each entry, at least one line in tests/fixtures/stderr_contract.txt satisfies regex.search(line); if zero lines match, test fails naming the platform
  </behavior>
  <action>
    Create tests/fixtures/skill_stderr_regexes.yaml with 7 keys mapped to empty strings (placeholder so YAML is parseable but tests fail):
    ```yaml
    claude-code: ""
    codex: ""
    opencode: ""
    openclaw: ""
    factory-droid: ""
    trae: ""
    trae-cn: ""
    ```
    Create tests/test_skill_regex_fixture.py with 3 tests above. Use `yaml.safe_load` (PyYAML is already optional dep — add `pytest.importorskip("yaml")` at top to skip cleanly if missing). Run pytest — MUST fail on test_each_regex_compiles (empty string compiles to match-everything but matches zero lines via search? Empty regex matches every position, so test_each_regex_matches_locked_contract would PASS spuriously). To force RED: in test_each_regex_compiles assert `len(value) > 0` — empty strings fail.
    Commit: `test(64-03): RED skill regex fixture scaffold (AUDIT-02)`.
  </action>
  <verify>
    <automated>pytest tests/test_skill_regex_fixture.py -x 2>&1 | grep -qE "FAILED|AssertionError" && echo RED-OK</automated>
  </verify>
  <done>Both files exist; pytest fails on at least one assertion.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: GREEN — Hand-curate 7 platform regexes by reading each SKILL.md</name>
  <read_first>
    - graphify/skill.md (claude-code) — search for stderr / regex / "[graphify]" mentions
    - graphify/skill-codex.md
    - graphify/skill-opencode.md
    - graphify/skill-claw.md (openclaw)
    - graphify/skill-droid.md (factory-droid) — confirm filename first via `ls graphify/skill-*.md`
    - graphify/skill-trae.md
    - graphify/skill-trae-cn.md (trae-cn) — confirm filename
    - tests/fixtures/stderr_contract.txt (Plan 01 — every regex must match ≥1 line here)
  </read_first>
  <files>tests/fixtures/skill_stderr_regexes.yaml</files>
  <behavior>
    - For each of the 7 platform SKILL.md files: grep for `\[graphify\]`, `stderr`, `error:`, `info:`, `hint:` mentions. Identify the strictest pattern that platform's SKILL.md asserts. Distill into a single Python-compatible regex.
    - If a SKILL.md does not assert a specific stderr pattern, fall back to the default contract regex: `^\[graphify\] (error|info): .+`
    - Each chosen regex MUST match at least one line in tests/fixtures/stderr_contract.txt (verified by Task 1's third test).
  </behavior>
  <action>
    1. Run `ls graphify/skill-*.md graphify/skill.md` to confirm all 7 filenames (resolve factory-droid + trae-cn exact names).
    2. For each SKILL.md, run: `grep -nE '\[graphify\]|stderr|error:|info:|hint:' <file> | head -40`. Read the surrounding context. Distill one regex per platform.
    3. Populate tests/fixtures/skill_stderr_regexes.yaml with 7 non-empty regex strings. Each regex must:
       - Be a valid Python re pattern (verify with `python -c "import re; re.compile(<pat>)"`)
       - Match >=1 line of tests/fixtures/stderr_contract.txt (verify with `python -c "import re; lines=open('tests/fixtures/stderr_contract.txt').read().splitlines(); print(any(re.search(<pat>, l) for l in lines))"` returns True)
    4. Default for any platform whose SKILL.md does not assert a specific pattern: `^\[graphify\] (error|info): .+` — DOCUMENT in YAML comment which platforms used the default.
    5. Run pytest tests/test_skill_regex_fixture.py — MUST pass all 3 tests.
    Commit: `test(64-03): GREEN — hand-curated 7 platform skill regex fixture (AUDIT-02)`.
  </action>
  <verify>
    <automated>pytest tests/test_skill_regex_fixture.py -x</automated>
  </verify>
  <done>All 3 tests pass; fixture has exactly 7 non-empty entries; `python -c "import yaml; d=yaml.safe_load(open('tests/fixtures/skill_stderr_regexes.yaml')); assert set(d.keys())=={'claude-code','codex','opencode','openclaw','factory-droid','trae','trae-cn'} and all(d.values())"` exits 0.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| (none new) | YAML fixture + test module read local files only; no external input. PyYAML parses local committed file. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-64-05 | T (Tampering) | tests/fixtures/skill_stderr_regexes.yaml | accept | Committed source; PR review catches tampering. |
| T-64-06 | D (DoS) | re.compile of fixture regex | accept | Hand-curated by maintainer; no untrusted regex source. Catastrophic-backtracking patterns avoidable in review. |

Non-applicable: S/R/I/E — no auth, no PII, no privileged ops, no network. Injection/SSRF: N/A (fixture files local-only, no shell or URL evaluation). YAML loaded with `yaml.safe_load` — no arbitrary object instantiation.
</threat_model>

<verification>
- pytest tests/test_skill_regex_fixture.py passes all 3 tests
- `python -c "import yaml; d=yaml.safe_load(open('tests/fixtures/skill_stderr_regexes.yaml')); assert len(d)==7"` exits 0
- Each regex demonstrably distilled from a real SKILL.md (commit message references which file)
</verification>

<success_criteria>
- 7 platforms covered, no missing or extra keys
- Every regex compiles and matches the locked contract from Plan 01
- Future drift in any SKILL.md's stderr contract surfaces via fixture-update requirement
</success_criteria>

<output>
After completion, create `.planning/phases/64-audit-a-stderr-format-snapshot-lock-sweep/64-03-SUMMARY.md`
</output>
