---
phase: 32-profile-contract-defaults
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - .planning/ROADMAP.md
  - .planning/REQUIREMENTS.md
autonomous: true
requirements:
  - TAX-01
  - TAX-02
  - TAX-03
  - TAX-04
  - COMM-03
  - CLUST-01
  - CLUST-04
must_haves:
  truths:
    - "Later v1.8 phases read `mapping.min_community_size`, not `clustering.min_community_size`."
    - "Later v1.8 phases treat `mapping.moc_threshold` as invalid immediately."
    - "Later v1.8 phases inherit taxonomy-over-folder-mapping precedence and the Graphify-owned default subtree."
  artifacts:
    - path: ".planning/REQUIREMENTS.md"
      provides: "Canonical v1.8 requirement wording for Phase 32 and traceability"
      contains: "mapping.min_community_size"
    - path: ".planning/ROADMAP.md"
      provides: "Phase 32 plan list and revised success criteria"
      contains: "mapping.moc_threshold` is invalid"
  key_links:
    - from: ".planning/phases/32-profile-contract-defaults/32-CONTEXT.md"
      to: ".planning/REQUIREMENTS.md"
      via: "D-07, D-14, D-18 contract corrections"
      pattern: "mapping\\.min_community_size"
    - from: ".planning/REQUIREMENTS.md"
      to: ".planning/ROADMAP.md"
      via: "matching CLUST-01/CLUST-04 wording"
      pattern: "mapping\\.moc_threshold"
---

<objective>
Reconcile the v1.8 planning contract so Phase 32 and downstream phases inherit the locked user decisions instead of the older roadmap wording.

Purpose: prevent executors for Phases 32-36 from implementing `clustering.min_community_size` or soft `mapping.moc_threshold` compatibility.
Output: updated `.planning/ROADMAP.md` and `.planning/REQUIREMENTS.md` with the Phase 32 plan list and corrected v1.8 contract.
</objective>

<execution_context>
@$HOME/.cursor/get-shit-done/workflows/execute-plan.md
@$HOME/.cursor/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/32-profile-contract-defaults/32-CONTEXT.md
@.planning/phases/32-profile-contract-defaults/32-RESEARCH.md
@.planning/phases/32-profile-contract-defaults/32-VALIDATION.md
@.planning/phases/32-profile-contract-defaults/32-PATTERNS.md
</context>

<source_audit>
GOAL: Users get a stable v1.8 default vault taxonomy and actionable profile validation before downstream export behavior changes. Covered by Plans 01-04.
REQ: TAX-01, TAX-02, TAX-03, TAX-04, COMM-03, CLUST-01, and CLUST-04 are all assigned across Plans 01-04.
RESEARCH: Contract drift, taxonomy schema, `mapping.min_community_size`, immediate `mapping.moc_threshold` invalidation, shared preflight, and doctor drift are planned.
CONTEXT: D-01 through D-20 are covered; there are no deferred ideas to exclude.
</source_audit>

<tasks>

<task type="auto">
  <name>Task 1: Correct requirement wording for the locked v1.8 contract</name>
  <files>.planning/REQUIREMENTS.md</files>
  <read_first>
    - .planning/REQUIREMENTS.md
    - .planning/phases/32-profile-contract-defaults/32-CONTEXT.md
    - .planning/phases/32-profile-contract-defaults/32-RESEARCH.md
  </read_first>
  <action>Update the Phase 32 requirement text per D-06, D-07, D-13, and D-14. Replace `clustering.min_community_size` with `mapping.min_community_size`. Rewrite CLUST-04 so it says user receives deterministic validation failure when `mapping.moc_threshold` is present, including when `mapping.min_community_size` is also present. Keep CLUST-02 and CLUST-03 assigned to Phase 34. Adjust TAX-03 wording so valid v1.8 user profiles can override default folder placement through `taxonomy:`, while profiles missing `taxonomy:` or `mapping.min_community_size` fail validation per D-17.</action>
  <verify>
    <automated>python - <<'PY'
from pathlib import Path
text = Path('.planning/REQUIREMENTS.md').read_text()
assert 'clustering.min_community_size' not in text
assert 'mapping.min_community_size' in text
assert 'mapping.moc_threshold' in text
assert 'taking precedence' not in text
PY</automated>
  </verify>
  <acceptance_criteria>
    - `.planning/REQUIREMENTS.md` contains `mapping.min_community_size`.
    - `.planning/REQUIREMENTS.md` does not contain `clustering.min_community_size`.
    - CLUST-04 describes invalidation of `mapping.moc_threshold`, not aliasing or precedence.
    - TAX-03 mentions valid v1.8 profile override semantics and does not promise old-profile compatibility.
  </acceptance_criteria>
  <done>The requirements file is the source of truth for the locked Phase 32 contract and no longer contradicts CONTEXT.md.</done>
</task>

<task type="auto">
  <name>Task 2: Update the roadmap phase entry and plan list</name>
  <files>.planning/ROADMAP.md</files>
  <read_first>
    - .planning/ROADMAP.md
    - .planning/REQUIREMENTS.md
    - .planning/phases/32-profile-contract-defaults/32-CONTEXT.md
  </read_first>
  <action>Update only the Phase 32 entry and the progress row. Change success criterion 2 to say valid v1.8 profiles override folder placement through `taxonomy:` and invalid/missing v1.8 keys fail validation. Change success criterion 4 to say `mapping.min_community_size` controls standalone MOC generation and `mapping.moc_threshold` is invalid immediately. Set `**Plans:** 4 plans` and add the exact plan list: `32-01-PLAN.md — Reconcile v1.8 planning contract`, `32-02-PLAN.md — Add profile taxonomy defaults and validation`, `32-03-PLAN.md — Consume taxonomy in mapping/export paths`, `32-04-PLAN.md — Share preflight findings with doctor`. Update the Phase 32 progress table cell from `0/TBD` to `0/4`.</action>
  <verify>
    <automated>python - <<'PY'
from pathlib import Path
text = Path('.planning/ROADMAP.md').read_text()
assert '**Plans:** 4 plans' in text
assert '32-01-PLAN.md' in text
assert '32-04-PLAN.md' in text
assert 'mapping.min_community_size' in text
assert 'clustering.min_community_size' not in text
assert '| 32. Profile Contract & Defaults | v1.8 | 0/4 | Not started | - |' in text
PY</automated>
  </verify>
  <acceptance_criteria>
    - `.planning/ROADMAP.md` Phase 32 lists exactly four plan files using `32-NN-PLAN.md` names.
    - `.planning/ROADMAP.md` contains `mapping.min_community_size`.
    - `.planning/ROADMAP.md` does not contain `clustering.min_community_size`.
    - The Phase 32 progress row says `0/4`.
  </acceptance_criteria>
  <done>ROADMAP.md points executors and future planners at the same v1.8 contract as REQUIREMENTS.md.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| planning docs -> executor prompts | Incorrect wording can cause executors to implement unsafe or deprecated profile behavior. |
| CONTEXT decisions -> roadmap requirements | Locked user decisions must not be weakened by older planning artifacts. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-32-01 | Tampering | `.planning/REQUIREMENTS.md` | mitigate | Replace obsolete `clustering.min_community_size` and precedence wording with the locked `mapping.min_community_size` and invalid-legacy-key contract. |
| T-32-02 | Repudiation | `.planning/ROADMAP.md` | mitigate | Add explicit plan list and success criteria so later phases cannot claim the old CLUST wording was still authoritative. |
| T-32-03 | Information Disclosure | Planning output paths | accept | This plan edits repo-local planning docs only and does not touch vault contents or generated note paths. |
</threat_model>

<verification>
Run:

```bash
python - <<'PY'
from pathlib import Path
req = Path('.planning/REQUIREMENTS.md').read_text()
roadmap = Path('.planning/ROADMAP.md').read_text()
assert 'clustering.min_community_size' not in req + roadmap
assert 'mapping.min_community_size' in req + roadmap
assert '32-04-PLAN.md' in roadmap
PY
```
</verification>

<success_criteria>
Phase 32 planning artifacts no longer contain the obsolete `clustering.min_community_size` contract, and they explicitly direct implementation toward taxonomy precedence, Graphify-owned default folders, immediate `mapping.moc_threshold` invalidation, and the four-plan execution structure.
</success_criteria>

<output>
After completion, create `.planning/phases/32-profile-contract-defaults/32-01-SUMMARY.md`.
</output>
