---
phase: 32-profile-contract-defaults
plan: 03
type: execute
wave: 2
depends_on:
  - 32-02
files_modified:
  - graphify/mapping.py
  - tests/test_mapping.py
  - tests/test_export.py
autonomous: true
requirements:
  - TAX-01
  - TAX-02
  - TAX-03
  - CLUST-01
must_haves:
  truths:
    - "Mapping classification uses taxonomy-resolved folders before export renders target paths."
    - "`mapping.min_community_size` controls standalone MOC classification."
    - "The `_Unclassified` bucket uses the Graphify MOC subtree."
    - "No-profile dry-run Obsidian output paths begin with `Atlas/Sources/Graphify/`."
  artifacts:
    - path: "graphify/mapping.py"
      provides: "Taxonomy-aware folder resolution and community threshold routing"
      contains: "min_community_size"
    - path: "tests/test_mapping.py"
      provides: "Pure unit coverage for taxonomy precedence and community floor"
      contains: "_Unclassified"
    - path: "tests/test_export.py"
      provides: "Dry-run path assertion for default no-profile Obsidian output"
      contains: "Atlas/Sources/Graphify"
  key_links:
    - from: "graphify/profile.py"
      to: "graphify/mapping.py"
      via: "profile taxonomy contract consumed during classify()"
      pattern: "taxonomy"
    - from: "graphify/mapping.py"
      to: "graphify/export.py"
      via: "ClassificationContext.folder consumed by to_obsidian()"
      pattern: "ctx\\.get\\(\"folder\"\\)"
---

<objective>
Consume the v1.8 profile contract in mapping and export-facing tests without moving routing decisions into rendering.

Purpose: `to_obsidian()` should receive already-resolved `ClassificationContext.folder` values so later rendering phases can rely on stable folder semantics.
Output: taxonomy-aware classification, `mapping.min_community_size` community routing, `_Unclassified` bucket naming, and no-profile dry-run path tests.
</objective>

<execution_context>
@$HOME/.cursor/get-shit-done/workflows/execute-plan.md
@$HOME/.cursor/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/32-profile-contract-defaults/32-CONTEXT.md
@.planning/phases/32-profile-contract-defaults/32-RESEARCH.md
@.planning/phases/32-profile-contract-defaults/32-VALIDATION.md
@.planning/phases/32-profile-contract-defaults/32-PATTERNS.md
@graphify/profile.py
@graphify/mapping.py
@graphify/export.py
@tests/test_mapping.py
@tests/test_export.py

<interfaces>
From Plan 02, the profile contract provides:
```python
profile["taxonomy"]["root"] == "Atlas/Sources/Graphify"
profile["taxonomy"]["folders"]["moc"] == "MOCs"
profile["mapping"]["min_community_size"] == 3
```

From `graphify/templates.py`, keep this contract unchanged:
```python
class ClassificationContext(TypedDict, total=False):
    note_type: str
    folder: str
    parent_moc_label: str
    community_tag: str
    members_by_type: dict
    sub_communities: list
    sibling_labels: list
    community_name: str
```
</interfaces>
</context>

<source_audit>
GOAL: Runtime note classification honors the stable v1.8 default taxonomy.
REQ: Covers TAX-01, TAX-02, TAX-03, and CLUST-01. TAX-04/COMM-03/CLUST-04 validation surfaces are covered by Plans 02 and 04.
RESEARCH: Implements taxonomy precedence in mapping, not templates/export; replaces `moc_threshold`; adds export dry-run path evidence.
CONTEXT: Implements D-01, D-02, D-03, D-05, D-06, D-16, D-19, and D-20. Does not implement deferred work.
</source_audit>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add mapping and export tests for taxonomy routing</name>
  <files>tests/test_mapping.py, tests/test_export.py</files>
  <read_first>
    - tests/test_mapping.py
    - tests/test_export.py
    - graphify/mapping.py
    - graphify/export.py
    - graphify/profile.py
    - .planning/phases/32-profile-contract-defaults/32-VALIDATION.md
  </read_first>
  <behavior>
    - A profile with `taxonomy.root: Custom/Graphify`, `taxonomy.folders.moc: Concept MOCs`, and conflicting `folder_mapping.moc: Old/Maps/` classifies MOCs into `Custom/Graphify/Concept MOCs/`.
    - `mapping.min_community_size: 2` makes a two-node community a standalone MOC; `mapping.min_community_size: 3` sends the same community below the standalone threshold.
    - Hostless below-threshold communities create the synthetic `_Unclassified` MOC with `community_name == "_Unclassified"` and a safe lower-case tag derived from that name.
    - `to_obsidian(..., dry_run=True)` with no explicit profile returns planned note paths under `Atlas/Sources/Graphify/` and MOC paths under `Atlas/Sources/Graphify/MOCs/`.
  </behavior>
  <action>Update `tests/test_mapping.py` and `tests/test_export.py` before changing production code. Replace test helper defaults from `Atlas/Maps/`, `Atlas/Dots/...`, and `mapping.moc_threshold` with the v1.8 defaults from Plan 02. Add tests for taxonomy-over-`folder_mapping` precedence, `mapping.min_community_size`, `_Unclassified`, and no-profile dry-run export paths. Confirm the new tests fail before editing `graphify/mapping.py`.</action>
  <verify>
    <automated>pytest tests/test_mapping.py tests/test_export.py -q</automated>
  </verify>
  <acceptance_criteria>
    - `tests/test_mapping.py` contains `taxonomy` and `min_community_size`.
    - `tests/test_mapping.py` contains `_Unclassified`.
    - `tests/test_export.py` contains `Atlas/Sources/Graphify/MOCs/`.
    - The tests do not create files outside `tmp_path`.
  </acceptance_criteria>
  <done>Runtime routing expectations are captured in failing tests before implementation.</done>
</task>

<task type="auto">
  <name>Task 2: Resolve folders from taxonomy and consume min community size</name>
  <files>graphify/mapping.py, tests/test_mapping.py, tests/test_export.py</files>
  <read_first>
    - graphify/mapping.py
    - graphify/profile.py
    - graphify/export.py
    - tests/test_mapping.py
    - tests/test_export.py
    - .planning/phases/32-profile-contract-defaults/32-PATTERNS.md
  </read_first>
  <action>In `graphify/mapping.py`, compute an effective folder mapping at the start of `classify()` from `profile["taxonomy"]` when present. Taxonomy must win over `folder_mapping` for `moc`, `thing`, `statement`, `person`, `source`, `default`, and `unclassified`; only fall back to `folder_mapping` if taxonomy is absent. Keep `_resolve_folder()` and `ClassificationContext.folder` as the downstream contract. Replace every `profile.get("mapping", {}).get("moc_threshold", 3)` read with `profile.get("mapping", {}).get("min_community_size", 3)` and preserve the bool/non-int defensive fallback to `3`. Change the synthetic bucket context from `Uncategorized`/`uncategorized` to `_Unclassified` with its folder set to the effective `moc` or `unclassified` Graphify folder. Do not add handling for `mapping.moc_threshold`; validation already rejects it in Plan 02.</action>
  <verify>
    <automated>pytest tests/test_profile.py tests/test_mapping.py tests/test_export.py -q</automated>
  </verify>
  <acceptance_criteria>
    - `graphify/mapping.py` contains `min_community_size`.
    - `graphify/mapping.py` does not contain `moc_threshold`.
    - `classify()` returns MOC folders under taxonomy paths when taxonomy and folder_mapping conflict.
    - `_Unclassified` appears in per-community classification for the synthetic bucket.
    - `pytest tests/test_profile.py tests/test_mapping.py tests/test_export.py -q` passes.
  </acceptance_criteria>
  <done>Mapping produces taxonomy-resolved folders and min-community-size behavior that export can consume unchanged.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| validated profile -> classification | Profile-derived folder and threshold values influence generated vault paths. |
| classification -> export path construction | `ClassificationContext.folder` is joined with output dir to create target note paths. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-32-09 | Tampering | `graphify/mapping.py` folder resolution | mitigate | Use the already-validated taxonomy contract from Plan 02 and centralize effective folder resolution before classification contexts are created. |
| T-32-10 | Tampering | taxonomy vs `folder_mapping` precedence | mitigate | Add tests where both define MOC folder placement and assert taxonomy wins. |
| T-32-11 | Denial of Service | `mapping.min_community_size` | mitigate | Preserve defensive fallback for bool/non-int values and rely on profile validation for strict user-facing errors. |
| T-32-12 | Repudiation | `_Unclassified` bucket | mitigate | Use explicit `_Unclassified` naming in tests and mapping output so future phases can identify migration/routing behavior deterministically. |
</threat_model>

<verification>
Run:

```bash
pytest tests/test_profile.py tests/test_mapping.py tests/test_export.py -q
```

If failures mention `moc_threshold`, remove runtime fallback code rather than adding compatibility aliases.
</verification>

<success_criteria>
Classification, community thresholding, and dry-run export evidence all show the v1.8 taxonomy: Graphify-owned default subtree, MOC folder under `Atlas/Sources/Graphify/MOCs/`, taxonomy precedence over `folder_mapping`, and canonical `mapping.min_community_size`.
</success_criteria>

<output>
After completion, create `.planning/phases/32-profile-contract-defaults/32-03-SUMMARY.md`.
</output>
