---
phase: 24-manifest-writer-audit-atomic-read-merge-write-hardening
plan: 02
type: execute
wave: 2
depends_on: ["24-01"]
files_modified:
  - .planning/phases/24-manifest-writer-audit-atomic-read-merge-write-hardening/AUDIT.md
autonomous: true
requirements: [MANIFEST-12]
requirements_addressed: [MANIFEST-12]
tags: [manifest, audit, documentation]

must_haves:
  truths:
    - "AUDIT.md exists at .planning/phases/24-manifest-writer-audit-atomic-read-merge-write-hardening/AUDIT.md"
    - "AUDIT.md preamble (1 paragraph) documents the read-merge-write contract and the D-08 recovery path for pre-v1.6 manifests"
    - "AUDIT.md contains a single table with the D-06 column shape (manifest filename | writer | invocation site | row identity key | pre-fix read? | pre-fix atomic? | post-fix read? | post-fix atomic? | Phase 24 action)"
    - "Table has exactly 5 rows: vault-manifest.json (LOCKED), seeds-manifest.json (LOCKED), routing.json (PATCHED), capability.json (PATCHED), graphify-out/manifest.json (DEFERRED)"
    - "DEFERRED row for detect.py:save_manifest documents the rationale and that the future phase wiring detect_incremental MUST include this fix"
  artifacts:
    - path: ".planning/phases/24-manifest-writer-audit-atomic-read-merge-write-hardening/AUDIT.md"
      provides: "MANIFEST-12 audit document"
      contains: "manifest filename"
  key_links:
    - from: "AUDIT.md preamble"
      to: "D-08 migration policy"
      via: "explicit recovery instructions for pre-v1.6 manifests"
      pattern: "re-run on the missing subpaths"
---

<objective>
Ship `AUDIT.md` enumerating every on-disk manifest writer in graphify with pre-v1.6 vs post-fix policy, in the table-shape locked by D-06. Anchors future manifest additions and surfaces the deferred `detect.save_manifest` known-bad writer for whichever future phase wires up `detect_incremental`.

Purpose: MANIFEST-12 acceptance — auditable record of every writer's atomicity + read-merge-write status, plus migration guidance per D-08.

Output: One markdown file in the phase directory, ~1 page.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/REQUIREMENTS.md
@.planning/phases/24-manifest-writer-audit-atomic-read-merge-write-hardening/24-CONTEXT.md
@.planning/phases/24-manifest-writer-audit-atomic-read-merge-write-hardening/24-RESEARCH.md
@.planning/phases/24-manifest-writer-audit-atomic-read-merge-write-hardening/24-01-PLAN.md

@graphify/routing_audit.py
@graphify/capability.py
@graphify/vault_promote.py
@graphify/seed.py
@graphify/merge.py
@graphify/detect.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create AUDIT.md with preamble + 5-row writer table per D-06</name>
  <files>.planning/phases/24-manifest-writer-audit-atomic-read-merge-write-hardening/AUDIT.md</files>
  <read_first>
    - .planning/phases/24-manifest-writer-audit-atomic-read-merge-write-hardening/24-CONTEXT.md (D-01 through D-08, esp. D-06 column shape and D-08 migration policy)
    - .planning/phases/24-manifest-writer-audit-atomic-read-merge-write-hardening/24-RESEARCH.md (section "AUDIT.md Content Specification" — verbatim row content)
    - .planning/phases/24-manifest-writer-audit-atomic-read-merge-write-hardening/24-01-PLAN.md (confirm Plan 01 has shipped both patches before writing the post-fix columns)
    - graphify/routing_audit.py (re-confirm post-fix line numbers if shifted by Plan 01 patch)
    - graphify/capability.py (re-confirm post-fix line numbers if shifted by Plan 01 patch)
    - graphify/vault_promote.py:651-682 (confirm `_save_manifest` line)
    - graphify/seed.py:96-131 (confirm `_save_seeds_manifest` line)
    - graphify/merge.py:1085 (confirm `_save_manifest` sibling line)
    - graphify/detect.py:447-465 (confirm `save_manifest` line and `detect_incremental` invocation site at :467)
  </read_first>
  <action>
    Create `.planning/phases/24-manifest-writer-audit-atomic-read-merge-write-hardening/AUDIT.md` with the EXACT structure below. After Plan 01 commits, line numbers in `routing_audit.py` and `capability.py` may shift by a few lines — re-grep and adjust the writer column accordingly. All other rows have stable line refs (no Phase 24 modifications).

    ```markdown
    # Manifest Writer Audit (Phase 24)

    **Phase:** 24 — Manifest Writer Audit + Atomic Read-Merge-Write Hardening
    **Closes:** MANIFEST-12
    **Date:** 2026-04-27

    ## Contract

    Every on-disk manifest writer in graphify MUST follow the read-merge-write contract: (1) **read** the existing file, returning `{}` or `[]` on missing/corrupt (catch `json.JSONDecodeError` and `OSError`, log to stderr if intent is to surface the recovery to users); (2) **merge** new-run rows on top of existing rows keyed by **row identity** (path / id / tool name), last-write-wins on conflict; (3) **write** to `<dest>.tmp` then `os.replace(<dest>.tmp, <dest>)` for crash-safe atomic commit. The reference implementation lives in `graphify/vault_promote.py:_save_manifest` (lines 651-682). Phase 24 brings `routing.json` and `capability.json` into compliance; `vault-manifest.json` and `seeds-manifest.json` were already compliant; `graphify-out/manifest.json` (detect.py) is documented here as DEFERRED because it is unreachable from any active CLI flow today.

    **Migration policy (D-08):** No migration code. On first read after the v1.6 upgrade, the patched writers treat whatever's on disk as authoritative and merge the new run's rows on top. Users with pre-v1.6 manifests missing sibling-subpath rows recover by **re-running on the missing subpaths**; the union accumulates after each run. There is no `graphify manifest rebuild` CLI and no auto-detection / stderr warning for stale state — both are deferred as new capabilities.

    ## Writer Inventory

    | manifest filename | writer (file:function:line) | invocation site | row identity key | pre-fix read? | pre-fix atomic? | post-fix read? | post-fix atomic? | Phase 24 action |
    |---|---|---|---|---|---|---|---|---|
    | `vault-manifest.json` | `graphify/vault_promote.py:_save_manifest:665` (also `graphify/merge.py:_save_manifest:1085`) | `vault_promote.promote()` and `merge.apply_merge_plan()` | `path` (note path; dict key) | YES (caller loads via `_load_manifest`) | YES (`.tmp` + `os.replace` + `fsync`) | YES (unchanged) | YES (unchanged) | LOCKED — already compliant; regression test added (`tests/test_vault_promote.py::test_subpath_isolation_vault_manifest`) |
    | `seeds-manifest.json` | `graphify/seed.py:_save_seeds_manifest:114` | `seed.build_all_seeds()` | `seed_id` (caller deduplicates before save) | YES (caller loads via `_load_seeds_manifest`) | YES (`.tmp` + `os.replace` + `fsync`) | YES (unchanged) | YES (unchanged) | LOCKED — already compliant; documented for completeness |
    | `routing.json` | `graphify/routing_audit.py:RoutingAudit.flush:34` | `pipeline.py:run()` (after extract phase) | file path string (dict key in `files`) | NO (blind overwrite of `self._files`) | YES (`.tmp` + `os.replace`) | YES (read existing, merge by file path, last-write-wins) | YES (unchanged) | PATCHED — Phase 24 Plan 01 inserted read-merge step before payload construction |
    | `capability.json` | `graphify/capability.py:write_manifest_atomic:225` | `export.py:to_json():304` via `capability.write_runtime_manifest:245` | tool name (`entry["name"]` in `CAPABILITY_TOOLS`) | NO (blind overwrite) | YES (`.tmp` + `os.replace`) | YES (read existing, index by tool name, layer incoming, last-write-wins) | YES (unchanged) | PATCHED — Phase 24 Plan 01 inserted read-merge step before payload construction. This is "the MCP `manifest.json`" in ROADMAP success criterion #2 (renamed from `manifest.json` in `quick-260422-jdj`, per D-01) |
    | `graphify-out/manifest.json` | `graphify/detect.py:save_manifest:447` | `graphify/detect.py:detect_incremental:467` (NOT WIRED — not invoked from any active CLI flow) | file path string (dict key) | NO | NO (direct `Path.write_text`, no `.tmp`, no `os.replace`) | NO (unchanged in Phase 24) | NO (unchanged in Phase 24) | DEFERRED — known-bad writer (blind overwrite + non-atomic). Fix is REQUIRED scope of whichever future phase wires `detect_incremental` to an active CLI flow (e.g., a `--update` mode or watch-mode hookup). DO NOT ship that wiring without first bringing this writer into the contract. |

    ## Test Coverage (MANIFEST-11)

    | Writer | Test | File:Function |
    |---|---|---|
    | `vault-manifest.json` | locks existing contract | `tests/test_vault_promote.py::test_subpath_isolation_vault_manifest` |
    | `routing.json` | exercises new read-merge-write | `tests/test_routing_audit.py::test_subpath_isolation_routing` |
    | `capability.json` | exercises new read-merge-write | `tests/test_capability.py::test_subpath_isolation_capability_manifest` |

    `seeds-manifest.json` and `graphify-out/manifest.json` (detect) are not covered by Phase 24 tests — the former is already locked by Phase 14 tests, the latter is unreachable from CLI today (and will be tested when re-wired in a future phase).
    ```

    Verify the file is exactly the structure above (preamble + Contract + Writer Inventory table + Test Coverage table). Re-grep `routing_audit.py:flush` and `capability.py:write_manifest_atomic` to confirm the line numbers in the inventory match the post-Plan-01 line numbers; adjust `:34` and `:225` if Plan 01's patch shifted them. (The function start line typically does not move because patches add lines inside the body, not above the `def`.)

    Commit: `docs(24-02): add AUDIT.md enumerating manifest writers and policy diff (MANIFEST-12)`
  </action>
  <verify>
    <automated>cd /Users/silveimar/Documents/silogia-repos/graphify &amp;&amp; test -f .planning/phases/24-manifest-writer-audit-atomic-read-merge-write-hardening/AUDIT.md &amp;&amp; grep -c "^|" .planning/phases/24-manifest-writer-audit-atomic-read-merge-write-hardening/AUDIT.md | awk '$1 &gt;= 11 { exit 0 } { exit 1 }'</automated>
  </verify>
  <acceptance_criteria>
    - File `.planning/phases/24-manifest-writer-audit-atomic-read-merge-write-hardening/AUDIT.md` exists
    - `grep -c "vault-manifest.json" AUDIT.md` returns ≥ 2 (table row + test coverage)
    - `grep -c "seeds-manifest.json" AUDIT.md` returns ≥ 1
    - `grep -c "routing.json" AUDIT.md` returns ≥ 2
    - `grep -c "capability.json" AUDIT.md` returns ≥ 2
    - `grep -c "graphify-out/manifest.json" AUDIT.md` returns ≥ 1
    - `grep -E "PATCHED|LOCKED|DEFERRED" AUDIT.md` matches all three labels at least once
    - `grep -E "row identity key" AUDIT.md` matches (D-06 column header present)
    - `grep -E "re-run on the missing subpaths" AUDIT.md` matches (D-08 migration text present)
    - `wc -l AUDIT.md` returns roughly 30-60 lines (≈1 page per D-06 target)
    - Commit message contains `docs(24-02)` and `MANIFEST-12`
  </acceptance_criteria>
  <done>AUDIT.md exists with the D-06 single-table shape, all 5 writers enumerated with PATCHED/LOCKED/DEFERRED disposition, preamble documents the contract and D-08 recovery path, test coverage table cross-references the three subpath-isolation tests from Plan 01.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| (none — documentation artifact) | No code or runtime trust boundary; AUDIT.md is a planning document |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-24-06 | Information Disclosure | AUDIT.md surfaces all writer paths and invocation sites | accept | Same trust scope as the rest of `.planning/`; no new secrets disclosed; this is the explicit purpose of the audit per MANIFEST-12 |
| T-24-07 | Repudiation | AUDIT.md becomes stale as new writers are added | mitigate | Document anchored in phase directory; future manifest writer additions MUST update this table (called out in preamble + DEFERRED row for detect.py) |
</threat_model>

<verification>
- File exists at `.planning/phases/24-manifest-writer-audit-atomic-read-merge-write-hardening/AUDIT.md`
- Manual review confirms the table matches D-06 column shape and the 5 writers from RESEARCH.md
- All 3 dispositions (PATCHED, LOCKED, DEFERRED) appear in the `Phase 24 action` column
</verification>

<success_criteria>
- MANIFEST-12 acceptance text satisfied: AUDIT.md enumerates every manifest writer, pre-v1.6 policy, post-fix policy
- D-06 column shape preserved verbatim
- D-08 migration policy documented in preamble
- DEFERRED row for `detect.save_manifest` flags the known-bad writer for future phases
</success_criteria>

<output>
After completion, create `.planning/phases/24-manifest-writer-audit-atomic-read-merge-write-hardening/24-02-SUMMARY.md` listing:
- AUDIT.md path
- Commit hash
- Confirmation: 5 writers enumerated (2 LOCKED, 2 PATCHED, 1 DEFERRED)
- requirements_completed: [MANIFEST-12]
</output>
