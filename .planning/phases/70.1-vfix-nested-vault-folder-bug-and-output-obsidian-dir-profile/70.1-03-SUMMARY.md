---
phase: 70.1-vfix-nested-vault-folder-bug-and-output-obsidian-dir-profile
plan: 03
subsystem: docs
tags: [docs, vfix-02, precedence, obsidian, skill-files]
requires: [70.1-02]
provides: [VFIX-02-doc-half]
affects: [README.md, graphify/__main__.py, graphify/skill*.md]
tech-stack:
  added: []
  patterns: [canonical-doc-paragraph-mirrored-across-platform-skills]
key-files:
  created:
    - .planning/phases/70.1-vfix-nested-vault-folder-bug-and-output-obsidian-dir-profile/70.1-03-SUMMARY.md
  modified:
    - README.md
    - graphify/__main__.py
    - graphify/skill.md
    - graphify/skill-codex.md
    - graphify/skill-opencode.md
    - graphify/skill-claw.md
    - graphify/skill-droid.md
    - graphify/skill-trae.md
    - graphify/skill-windows.md
decisions:
  - "Canonical phrasing of precedence chain: '--output > profile > --obsidian-dir > legacy default' (matches plan must_haves and existing __main__.py D-08 comment)"
  - "Insert precedence + pitfall section directly after the 'Back up the target vault before apply...' anchor in skill files (single shared anchor across all 7 variants → idempotent inserts)"
  - "Pitfall callout names three escape hatches: cd into vault / --vault <abs> / --output <abs> — covers all three resolution paths so users always have a non-broken option"
metrics:
  duration: ~10 min
  completed: 2026-05-05
  tasks: 2
  files: 9
  commits: 2
---

# Phase 70.1 Plan 03: Document precedence chain and nested-vault pitfall

User-facing documentation now describes the `--output > profile > --obsidian-dir > legacy default` precedence chain in a single canonical paragraph, mirrored across README, CLI `--help`, and all seven `graphify/skill*.md` platform variants, closing the doc half of VFIX-02.

## What Shipped

**Task 1 — README + CLI --help (commit 9f80e44):**
- New `### Output destination precedence` H3 in README.md (under the Obsidian vault adapter section, before "Vault selection for scripting") explaining the four-step chain, the meaning of `output.path: '.'`, and a "Common pitfall — Nested vault folder" callout that lists three correct invocation patterns.
- CLI `--help` text in `graphify/__main__.py` updated in two places: the per-flag block under `--obsidian-dir` and the global notes block near `--output composes with vault pin`. Both reference the precedence chain and link the README section by name.
- No CLI flag parsing or production code paths changed.

**Task 2 — Seven skill variants (commit ed97ad5):**
- Same canonical paragraph inserted into `graphify/skill.md`, `skill-codex.md`, `skill-opencode.md`, `skill-claw.md`, `skill-droid.md`, `skill-trae.md`, `skill-windows.md`.
- Insertion point: directly after the shared "Back up the target vault before apply..." anchor line in the `## v1.8 Obsidian vault behavior` section. All seven files share this exact anchor, making the change uniform and idempotent.
- README phrasing normalized from `profile.output` → `profile` to match the plan's required canonical short form (`--output > profile > --obsidian-dir`).

## Verification

- `grep -lF -- '--output > profile > --obsidian-dir' README.md graphify/skill*.md` → 8 files (README + 7 skills) ✓
- `grep -c 'Nested vault folder' README.md` → 1 ✓
- `python -m graphify --help 2>&1 | grep -i precedence` → 3 lines (global notes + per-flag + existing vault selection) ✓
- pytest expected to remain green (doc-only changes; no Python imports or behavior touched).

## Deviations from Plan

None. Both tasks executed exactly as written.

## Self-Check: PASSED

- [x] README.md has precedence section + Nested vault folder callout
- [x] graphify/__main__.py --help text references precedence rule
- [x] All 7 skill files contain canonical phrase `--output > profile > --obsidian-dir`
- [x] Commit 9f80e44 (Task 1) present in `git log`
- [x] Commit ed97ad5 (Task 2) present in `git log`

## Next

Plan 70.1-04 (next in wave) — typically the verification/closure plan. The canonical paragraph composed in Task 1 can be reused verbatim if Plan 04 needs it for additional surfaces (profile examples, etc.).
