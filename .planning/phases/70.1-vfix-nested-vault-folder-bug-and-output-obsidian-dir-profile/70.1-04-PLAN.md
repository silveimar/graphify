---
phase: 70.1-vfix-nested-vault-folder-bug-and-output-obsidian-dir-profile
plan: 04
type: execute
wave: 3
depends_on:
  - 70.1-02
files_modified:
  - profile-example.yaml
  - profile-example-complete.yaml
autonomous: true
gap_closure: true
requirements:
  - VFIX-02
must_haves:
  truths:
    - "profile-example.yaml carries an inline comment block above the output: section explaining when to set path: '.' (vault-root taxonomy) vs. a subdirectory"
    - "profile-example-complete.yaml carries the same inline guidance, in the same canonical wording, above its output: section"
    - "Both files remain valid YAML loadable by graphify.profile.load_profile against a tmp_path stub vault"
  artifacts:
    - path: "profile-example.yaml"
      provides: "Annotated example profile with output.path guidance"
      contains: "output:"
    - path: "profile-example-complete.yaml"
      provides: "Annotated complete example profile with output.path guidance"
      contains: "output:"
  key_links:
    - from: "profile-example.yaml"
      to: "README §Output destination precedence (Plan 70.1-03)"
      via: "comment cross-reference 'see README §Output destination precedence'"
      pattern: "Output destination precedence"
    - from: "profile-example-complete.yaml"
      to: "README §Output destination precedence (Plan 70.1-03)"
      via: "comment cross-reference"
      pattern: "Output destination precedence"
---

<objective>
Annotate the two bundled profile example files with inline comments that explain the
`output.path` semantics — specifically when `path: '.'` is correct (vault-root taxonomy
with `folder_mapping` carrying the full subpath) and when a subdirectory value is
correct (taxonomy intentionally rooted under a subfolder).

Purpose: Close the example-files half of VFIX-02. The two example files are what users
copy into `<vault>/.graphify/profile.yaml`; they MUST carry the same precedence/pitfall
guidance written into README in Plan 70.1-03.

Output: Comment-only edits to two YAML files. No schema change. No new keys.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/70.1-vfix-nested-vault-folder-bug-and-output-obsidian-dir-profile/70.1-CONTEXT.md
@.planning/phases/70.1-vfix-nested-vault-folder-bug-and-output-obsidian-dir-profile/70.1-03-PLAN.md
@profile-example.yaml
@profile-example-complete.yaml
@graphify/profile.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Annotate profile-example.yaml and profile-example-complete.yaml with output.path guidance</name>
  <files>profile-example.yaml, profile-example-complete.yaml</files>
  <action>
    1. Locate the `output:` block in each example file (use grep).
       - profile-example.yaml may not yet have an explicit `output:` block (it shows
         only taxonomy/folder_mapping). If absent, add one with the canonical
         vault-root values:
         ```yaml
         # Output destination — see README §Output destination precedence
         #
         # path: '.' means "vault root" — graphify writes notes under the vault root,
         # using folder_mapping[type] to place each note. This is the correct setting
         # when your taxonomy/folder_mapping values already carry the full subpath
         # (e.g. "Atlas/Sources/Graphify/Things/").
         #
         # Set path: 'SubDir' (no leading slash, no '..') ONLY when you want graphify
         # to anchor ALL writes under <vault>/SubDir/ and your folder_mapping values
         # are relative to that subdir.
         #
         # Common pitfall — nested vault folder:
         #   Do NOT invoke `graphify --obsidian --obsidian-dir <vault-name>` from
         #   the vault's parent directory. Either `cd` into the vault, or pass
         #   `--vault <path>`, or pass `--output <abs>`. The precedence chain is:
         #     --output > profile.output > --obsidian-dir > legacy default
         output:
           mode: vault-relative
           path: .
           exclude: []
         ```
       - profile-example-complete.yaml: insert the SAME canonical comment block
         immediately above its existing `output:` block. Do not duplicate the keys
         if they already exist.

    2. After editing, verify both files still parse via PyYAML:
       ```bash
       python -c "import yaml; yaml.safe_load(open('profile-example.yaml'))"
       python -c "import yaml; yaml.safe_load(open('profile-example-complete.yaml'))"
       ```

    3. Verify the canonical phrase from Plan 70.1-03 is reachable from the comment
       (cross-reference: `see README §Output destination precedence`).

    4. Do not change any other section of either YAML file.
  </action>
  <verify>
    <automated>python -c "import yaml; assert isinstance(yaml.safe_load(open('profile-example.yaml')), dict); assert isinstance(yaml.safe_load(open('profile-example-complete.yaml')), dict)" && grep -c 'Output destination precedence' profile-example.yaml profile-example-complete.yaml | grep -v ':0$' | wc -l | grep -q '^[[:space:]]*2$'</automated>
  </verify>
  <done>
    Both example files parse as valid YAML, both contain the cross-reference to the
    README precedence section, and both carry guidance for when path: '.' is correct
    vs. when a subdirectory is correct.
  </done>
</task>

</tasks>

<verification>
- `python -c "import yaml; yaml.safe_load(open('profile-example.yaml')); yaml.safe_load(open('profile-example-complete.yaml'))"` exits 0
- `grep -c 'Output destination precedence' profile-example.yaml` returns ≥1
- `grep -c 'Output destination precedence' profile-example-complete.yaml` returns ≥1
- `grep -c "Common pitfall" profile-example.yaml profile-example-complete.yaml | grep -v ':0$' | wc -l` returns 2
- `pytest tests/ -q` still green (doc-only changes; no test impact)
</verification>

<success_criteria>
- Both bundled examples carry the canonical precedence + pitfall guidance as comments
- Both files remain valid YAML
- VFIX-02 fully closed (Plan 70.1-03 + Plan 70.1-04 together cover docs + examples)
</success_criteria>

<output>
Write `.planning/phases/70.1-vfix-nested-vault-folder-bug-and-output-obsidian-dir-profile/70.1-04-SUMMARY.md`
</output>
