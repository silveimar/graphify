---
phase: 70.1-vfix-nested-vault-folder-bug-and-output-obsidian-dir-profile
plan: 02
type: tdd
wave: 2
depends_on:
  - 70.1-01
files_modified:
  - graphify/output.py
  - graphify/__main__.py
autonomous: true
gap_closure: true
requirements:
  - VFIX-01
must_haves:
  truths:
    - "All tests in tests/test_output_path_matrix.py turn GREEN without modifying the test file"
    - "Existing tests/test_output.py and tests/test_profile.py remain green (no regression)"
    - "When CWD == vault root and profile.output.path == '.', notes_dir == vault.resolve() exactly — no doubled segment"
    - "When CWD == vault parent and --vault <vault> is passed, notes_dir is identical to the in-vault-CWD case"
    - "When CWD == vault parent, no --vault, and --obsidian-dir <vault-name> is passed, the obsidian-dir branch produces a path with no doubled vault-name segment"
    - "D-08 precedence (--output > profile > --obsidian-dir > legacy default) is preserved end-to-end"
  artifacts:
    - path: "graphify/output.py"
      provides: "resolve_output / resolve_execution_paths fixed for cwd × pin × profile matrix"
      contains: "def resolve_output"
    - path: "graphify/__main__.py"
      provides: "--obsidian command obsidian_dir precedence resolves to a single absolute path with no double-vault join"
      contains: "user_passed_obsidian_dir"
  key_links:
    - from: "graphify/output.py::resolve_output"
      to: "graphify/profile.py::validate_vault_path"
      via: "vault-relative branch with path: '.' returning vault root"
      pattern: "validate_vault_path\\(path_val, cwd_resolved\\)"
    - from: "graphify/__main__.py::--obsidian dispatch"
      to: "graphify.export.to_obsidian"
      via: "obsidian_dir = str(resolved.notes_dir) under cli_output OR profile-driven branches"
      pattern: "obsidian_dir = str\\(resolved\\.notes_dir\\)"
---

<objective>
Make Plan 70.1-01's RED tests turn GREEN by fixing the path resolution chain so that
cwd × --obsidian-dir × profile.output.path produces a single canonical absolute path.

Purpose: Eliminate the nested `<vault>/<vault>/Atlas/...` folder reproduced in the
Phase 70 UAT. Restore the invariant that the same logical invocation, expressed three
ways (cwd-in-vault / cwd-parent-with-pin / cwd-parent-with-obsidian-dir), produces
byte-identical write paths.

Output: Minimal patches to `graphify/output.py` and `graphify/__main__.py` (`--obsidian`
dispatch). No public API signature changes. No new dependencies.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/70.1-vfix-nested-vault-folder-bug-and-output-obsidian-dir-profile/70.1-CONTEXT.md
@.planning/phases/70.1-vfix-nested-vault-folder-bug-and-output-obsidian-dir-profile/70.1-01-PLAN.md
@graphify/output.py
@graphify/profile.py
@graphify/__main__.py
@graphify/export.py
@tests/test_output.py
@tests/test_output_path_matrix.py
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Diagnose + fix resolve_output / resolve_execution_paths to honor `output.path: '.'` and parent-cwd pin without doubling</name>
  <files>graphify/output.py</files>
  <behavior>
    After this task:
    - `resolve_output(vault).notes_dir == vault.resolve()` when profile has
      `output: {mode: vault-relative, path: .}`. (Already nominally true via
      `validate_vault_path('.', vault) == vault.resolve()`. If a regression here is
      what causes the bug, fix it inline.)
    - `resolve_execution_paths(parent_cwd, explicit_vault=vault).notes_dir == vault.resolve()`
      i.e. the parent-cwd code path does NOT prepend `parent_cwd` onto an already-resolved
      vault-relative path. Inspect `resolve_execution_paths` → `resolve_output(effective_root, ...)`
      to confirm `effective_root` is the resolved vault root, not `cwd`.
    - No regression in `default` source (no vault detected) — still emits
      `Path("graphify-out/obsidian")` per D-12.
    - No regression in `cli-flag` source — still emits `flag_path.resolve()`.
  </behavior>
  <action>
    1. Run `pytest tests/test_output_path_matrix.py -q` and read each failing test's
       traceback. Identify the precise call chain that yields the wrong notes_dir.
    2. Apply the minimum necessary patch in `graphify/output.py`. Likely sites:
       - `resolve_execution_paths`: ensure `effective_root` (used as cwd for inner
         `resolve_output`) is the resolved vault root, NOT the caller's cwd
       - `resolve_output` vault-relative branch: if `path_val in (".", "", None)`,
         short-circuit to `cwd_resolved` (the vault root) without re-joining
       - Confirm `validate_vault_path(".", vault)` returns `vault.resolve()` exactly
         (no trailing `vault.name` doubling).
    3. Do NOT change `ResolvedOutput`'s field order or types. Do NOT change stderr
       message formats (those are locked by tests/test_output.py).
    4. Re-run `pytest tests/test_output_path_matrix.py tests/test_output.py tests/test_profile.py -q`.
       All must pass.
  </action>
  <verify>
    <automated>pytest tests/test_output_path_matrix.py tests/test_output.py tests/test_profile.py -q</automated>
  </verify>
  <done>
    All tests in those three files pass. `git diff graphify/output.py` shows a focused
    change (no drive-by refactors). Commit: `fix(70.1-02): resolve_output honors path: '.' and parent-cwd pin without double-vault join`.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Fix --obsidian dispatch precedence so user_passed_obsidian_dir branch produces an absolute path that does not double the vault name</name>
  <files>graphify/__main__.py</files>
  <behavior>
    For the `--obsidian` command (the dispatch block starting near line 1800):
    - When `cli_output` is set: `obsidian_dir = str(resolved.notes_dir)` — already correct.
    - When `resolved.vault_detected` AND `resolved.source in _PROFILE_DRIVEN_SOURCES`:
      `obsidian_dir = str(resolved.notes_dir)` — already correct.
    - When `user_passed_obsidian_dir` is true AND no vault was detected at cwd AND no
      `--vault` pin was passed: resolve `obsidian_dir` to an ABSOLUTE path
      (`Path(obsidian_dir).resolve()`) BEFORE handing it to `to_obsidian`. If that
      path turns out to itself be an Obsidian vault, treat it as such — load its profile
      explicitly and pass to `to_obsidian(profile=...)` so `to_obsidian` does NOT
      re-discover and re-join. This is the fix for the parent-cwd+`--obsidian-dir uat70-vault`
      reproducer.
    - Legacy default (none of the above) preserved.

    Plan 70.1-01 Test 7 (`test_obsidian_dispatch_parent_cwd_with_obsidian_dir_no_doubled_segment`)
    is the locked sentinel for this dispatch fix. This task MUST NOT modify
    `tests/test_output_path_matrix.py` — that file is owned by Plan 70.1-01 and adding
    tests here mid-cycle would violate the RED → GREEN discipline (truth #1: "without
    modifying the test file"). Verification of the dispatch precedence is via Test 7
    flipping from RED to GREEN.
  </behavior>
  <action>
    1. Read the precedence block in `graphify/__main__.py` (the `if cli_output is not None:`
       chain near the `--obsidian` dispatch — search for `# Precedence (D-08):`).
    2. In the `elif user_passed_obsidian_dir:` branch (currently `pass`), upgrade to:
       ```python
       elif user_passed_obsidian_dir:
           # VFIX-01: Resolve to absolute now so downstream to_obsidian does not
           # re-join against cwd or load_profile inside an unrelated tree, and so
           # we never double the vault-name segment in the final write path.
           _od = Path(obsidian_dir).resolve()
           obsidian_dir = str(_od)
           # If the resolved obsidian_dir is itself a vault with a profile, load
           # its profile explicitly so to_obsidian receives the correct mapping
           # and does NOT re-resolve relative to a stale cwd.
           if (_od / ".obsidian").is_dir() and (_od / ".graphify" / "profile.yaml").exists():
               from graphify.profile import load_profile as _lp
               profile = _lp(_od)  # type: ignore[name-defined]
       ```
       (Adapt to the surrounding name `profile` — it is set lower in the dispatch.
       If `profile` is not yet bound at this point, set a local `_obsidian_dir_vault`
       hint and consume it where `profile = None` is currently initialized below.)
    3. Where `to_obsidian(... profile=profile ...)` is called, ensure the profile
       set in step 2 is what gets passed.
    4. Re-run the matrix tests + all output/obsidian tests.
  </action>
  <verify>
    <automated>pytest tests/test_output_path_matrix.py tests/test_output.py tests/test_export.py tests/test_profile.py -q 2>&1 | tail -20</automated>
  </verify>
  <done>
    All matrix tests green. No regression in test_output / test_export / test_profile.
    `git diff graphify/__main__.py` shows a focused patch limited to the precedence
    block (no drive-by changes). Commit: `fix(70.1-02): --obsidian-dir resolves absolute, loads vault profile when applicable`.
  </done>
</task>

</tasks>

<verification>
- `pytest tests/test_output_path_matrix.py -q` — all GREEN
- `pytest tests/ -q` — full suite green
- `grep -n "user_passed_obsidian_dir" graphify/__main__.py | grep -v '^#'` — branch is no longer a bare `pass`
- `grep -nE "Path\\(obsidian_dir\\)\\.resolve\\(\\)" graphify/__main__.py` — at least one hit in the new branch
- Manual repro: from `/Users/silveimar/temp-testgph/tmp/`, `graphify --obsidian --obsidian-dir uat70-vault --graph uat70-vault/.graphify/graph.json` writes notes under `uat70-vault/Atlas/Sources/Graphify/...` with no nested `uat70-vault/uat70-vault/` segment.
</verification>

<success_criteria>
- VFIX-01 closed: matrix tests GREEN, full suite GREEN, no doubled vault-name segment in any resolution
- D-08 precedence preserved (existing test_output.py tests still green)
- Patch surface focused to two files; no API changes
</success_criteria>

<output>
Write `.planning/phases/70.1-vfix-nested-vault-folder-bug-and-output-obsidian-dir-profile/70.1-02-SUMMARY.md`
</output>
