---
phase: 70.1-vfix-nested-vault-folder-bug-and-output-obsidian-dir-profile
plan: 04
subsystem: docs
tags: [docs, vfix-02, profile-examples, precedence, obsidian]
requires: [70.1-02, 70.1-03]
provides: [VFIX-02-examples-half, VFIX-02-closed]
affects: [profile-example.yaml, profile-example-complete.yaml]
tech-stack:
  added: []
  patterns: [canonical-doc-paragraph-mirrored-into-bundled-yaml-examples]
key-files:
  created:
    - .planning/phases/70.1-vfix-nested-vault-folder-bug-and-output-obsidian-dir-profile/70.1-04-SUMMARY.md
  modified:
    - profile-example.yaml
    - profile-example-complete.yaml
decisions:
  - "Mirror Plan 70.1-03 canonical paragraph verbatim into both YAML examples (single source of truth — README §Output destination precedence)"
  - "profile-example.yaml gains an explicit output: block (mode/path/exclude) since it had none — chosen values match graphify built-in defaults so behavior is unchanged for users who copy this file"
  - "profile-example-complete.yaml keeps its existing optional/commented output: stub but gains the canonical comment block above it, so the guidance is visible whether the user uncomments the block or not"
metrics:
  duration: ~5 min
  completed: 2026-05-05
  tasks: 1
  files: 2
  commits: 1
---

# Phase 70.1 Plan 04: Annotate Profile Examples with output.path Guidance Summary

Bundled profile examples (`profile-example.yaml`, `profile-example-complete.yaml`) now carry the canonical `--output > profile.output > --obsidian-dir > legacy default` precedence chain and the "nested vault folder" pitfall callout as inline YAML comments, closing the examples half of VFIX-02 (Plan 70.1-03 covered the README/CLI/skill half).

## What Shipped

**Task 1 — Annotate both example files (commit eace03b):**

- `profile-example.yaml`: added an explicit `output:` block with the default values (`mode: vault-relative`, `path: .`, `exclude: []`) plus the canonical comment block above it. The block explains when `path: '.'` is correct (taxonomy/folder_mapping carries the full subpath) vs. when a `SubDir` value is correct (taxonomy rooted under a subfolder), and lists the three escape hatches for the nested-vault-folder pitfall (`cd` / `--vault` / `--output`).
- `profile-example-complete.yaml`: added the same canonical comment block immediately above the existing optional/commented `output:` stub, with phrasing tuned for this example (notes that this file's `folder_mapping` already carries the full subpath, so `path: '.'` is the correct default for it).
- Both files cross-reference README §Output destination precedence so users can find the long-form explanation.

## Verification

- `python -c "import yaml; yaml.safe_load(open('profile-example.yaml')); yaml.safe_load(open('profile-example-complete.yaml'))"` exits 0 ✓
- `grep -c 'Output destination precedence' profile-example.yaml` → 1 ✓
- `grep -c 'Output destination precedence' profile-example-complete.yaml` → 1 ✓
- `grep -c 'Common pitfall' profile-example.yaml profile-example-complete.yaml` → 1 each (2 total) ✓
- Loaded `output` value for `profile-example.yaml`: `{'mode': 'vault-relative', 'path': '.', 'exclude': []}` (matches built-in defaults) ✓
- `profile-example-complete.yaml` keeps `output` commented out (existing optional behavior preserved) ✓
- Doc-only YAML changes; pytest impact: none expected.

## Deviations from Plan

None. Task executed exactly as written. Plan permitted adding the `output:` block to `profile-example.yaml` (it was absent) and inserting the comment block into `profile-example-complete.yaml` above its existing `output:` stub — both done as specified.

## Self-Check: PASSED

- [x] `profile-example.yaml` parses as YAML and contains "Output destination precedence" + "Common pitfall"
- [x] `profile-example-complete.yaml` parses as YAML and contains "Output destination precedence" + "Common pitfall"
- [x] `profile-example.yaml` carries an explicit `output:` block with vault-relative defaults
- [x] Commit eace03b present in `git log`
- [x] No other YAML sections modified

## Next

VFIX-02 is now closed across all four user-visible surfaces (README, CLI `--help`, 7 skill files, 2 profile examples). Plan 70.1-05 — if present in the wave — would be the verification/closure plan. Phase 70.1 plans 02–04 together fully resolve the nested-vault-folder bug and output/obsidian-dir profile precedence ambiguity.
