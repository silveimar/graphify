---
phase: 70.1-vfix-nested-vault-folder-bug-and-output-obsidian-dir-profile
plan: 03
type: execute
wave: 3
depends_on:
  - 70.1-02
files_modified:
  - README.md
  - graphify/__main__.py
  - graphify/skill.md
  - graphify/skill-codex.md
  - graphify/skill-opencode.md
  - graphify/skill-claw.md
  - graphify/skill-droid.md
  - graphify/skill-trae.md
  - graphify/skill-windows.md
autonomous: true
gap_closure: true
requirements:
  - VFIX-02
must_haves:
  truths:
    - "README.md describes the precedence chain --output > profile > --obsidian-dir > legacy default in a single dedicated section"
    - "README.md contains a 'Common pitfalls' note explaining when output.path: '.' is correct and how to avoid the nested-vault folder"
    - "CLI --help text for --obsidian-dir mentions the precedence rule and links the pitfall by name"
    - "All seven graphify/skill*.md variants in files_modified contain the same precedence + pitfall paragraph (canonical text reused verbatim)"
  artifacts:
    - path: "README.md"
      provides: "Public-facing precedence doc + pitfall callout"
      contains: "--output > profile"
    - path: "graphify/skill.md"
      provides: "Claude Code skill: precedence + pitfall section"
      contains: "Nested vault folder"
  key_links:
    - from: "graphify/__main__.py (--help text)"
      to: "README.md precedence section"
      via: "shared canonical phrase"
      pattern: "--output > profile"
    - from: "graphify/skill.md"
      to: "README.md"
      via: "identical precedence paragraph"
      pattern: "--output > profile > --obsidian-dir"
---

<objective>
Update user-facing documentation so the precedence chain and the nested-vault-folder
pitfall are documented exactly once, in a single canonical phrasing, and that phrasing
appears in (a) README.md, (b) the CLI `--help` text for `--obsidian-dir` and `--output`,
and (c) all seven `graphify/skill*.md` platform variants.

Purpose: Close VFIX-02. Prevent future repro of the UAT bug by giving the user (and
agent skills that orchestrate graphify) accurate precedence rules and an explicit
"do not pass --obsidian-dir <vault-name> from the parent directory" pitfall.

Output: Doc edits only. No production behavior change.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/70.1-vfix-nested-vault-folder-bug-and-output-obsidian-dir-profile/70.1-CONTEXT.md
@.planning/phases/70.1-vfix-nested-vault-folder-bug-and-output-obsidian-dir-profile/70.1-02-PLAN.md
@README.md
@graphify/__main__.py
@graphify/skill.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Define canonical precedence + pitfall paragraph and update README + --help</name>
  <files>README.md, graphify/__main__.py</files>
  <action>
    1. Compose ONE canonical paragraph of ~6-10 lines that contains:
       - The precedence chain: `--output > profile.output > --obsidian-dir > legacy default`
       - A short example showing `output.path: '.'` means "vault root" (taxonomy +
         folder_mapping then place notes underneath)
       - A "Common pitfall — Nested vault folder" callout: do NOT invoke
         `--obsidian-dir <vault-name>` from the vault's parent directory; either
         `cd` into the vault, or pass `--vault <path>`, or pass `--output <abs>`.
       Save the canonical text as a code-fenced block in your task notes (it will be
       reused verbatim in Plan 70.1-03 Task 2 and in Plan 70.1-04).
    2. Insert the canonical paragraph into README.md under the Obsidian/vault output
       section (find the existing `--obsidian-dir` mention with grep; create a
       `### Output destination precedence` H3 if absent).
    3. Update CLI `--help` strings in `graphify/__main__.py` for `--obsidian-dir` and
       `--output`. Look for the help/usage text emitted by the `--help` handler
       (search for `--obsidian-dir` literal in __main__.py). Append a one-line
       reference: "see README §Output destination precedence (precedence: --output >
       profile > --obsidian-dir > default)."
    4. Do NOT change CLI flag parsing logic or behavior — text only.
  </action>
  <verify>
    <automated>grep -c -- '--output > profile' README.md && grep -c -- 'Nested vault folder' README.md && python -c "import subprocess; out = subprocess.run(['python','-m','graphify','--help'], capture_output=True, text=True).stdout + subprocess.run(['python','-m','graphify','--help'], capture_output=True, text=True).stderr; assert 'precedence' in out.lower() or '--output > profile' in out, out[:500]"</automated>
  </verify>
  <done>
    README has the precedence section and the pitfall callout. `python -m graphify --help`
    text references the precedence rule. No code path changed.
  </done>
</task>

<task type="auto">
  <name>Task 2: Mirror the canonical precedence + pitfall paragraph into all seven skill*.md variants</name>
  <files>graphify/skill.md, graphify/skill-codex.md, graphify/skill-opencode.md, graphify/skill-claw.md, graphify/skill-droid.md, graphify/skill-trae.md, graphify/skill-windows.md</files>
  <action>
    For each of the seven skill files in files_modified:
    1. Find the section that mentions `--obsidian-dir`, `--output`, or "vault output"
       (use grep on each file).
    2. Insert the SAME canonical paragraph composed in Task 1, verbatim, under a
       `### Output destination precedence` H3 (or whatever the file's existing
       heading style is — match it).
    3. Do NOT alter unrelated skill content. Verify the insertion is idempotent
       (a second run would no-op because the canonical phrase is already present).

    Note on skill-excalidraw.md, skill-aider.md, skill-copilot.md: those exist but
    the user instruction listed seven skill files. Apply only to the seven listed.
    If any of those seven do not exist, log to stderr and continue — do not invent files.
  </action>
  <verify>
    <automated>for f in graphify/skill.md graphify/skill-codex.md graphify/skill-opencode.md graphify/skill-claw.md graphify/skill-droid.md graphify/skill-trae.md graphify/skill-windows.md; do test -f "$f" || continue; grep -q -- '--output > profile > --obsidian-dir' "$f" || { echo "MISSING in $f"; exit 1; }; done; echo OK</automated>
  </verify>
  <done>
    Every existing skill file in files_modified contains the canonical precedence phrase
    `--output > profile > --obsidian-dir`. No skill file's body is otherwise mutated
    beyond the inserted paragraph.
  </done>
</task>

</tasks>

<verification>
- `grep -l -- '--output > profile > --obsidian-dir' README.md graphify/skill*.md` returns ≥8 files (README + 7 skills present)
- `grep -c 'Nested vault folder' README.md` returns ≥1
- `python -m graphify --help 2>&1 | grep -i precedence` returns ≥1 line
- `pytest tests/ -q` still green (no test impact expected; doc-only changes)
</verification>

<success_criteria>
- VFIX-02 doc half closed: README, --help, and seven skills carry the same canonical precedence + pitfall text
- No code behavior change
- Idempotent: re-running edits would no-op
</success_criteria>

<output>
Write `.planning/phases/70.1-vfix-nested-vault-folder-bug-and-output-obsidian-dir-profile/70.1-03-SUMMARY.md`
</output>
