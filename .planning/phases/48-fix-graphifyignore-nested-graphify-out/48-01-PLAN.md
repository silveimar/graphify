---
phase: 48
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified:
  - graphify/detect.py
  - graphify/doctor.py
  - graphify/corpus_prune.py
autonomous: true
requirements:
  - HYG-04
must_haves:
  truths:
    - "Shared helper (or reuse of _is_ignored + roots) determines whether nested self-output paths are already excluded by effective ignore patterns"
    - "Doctor does not emit the WOULD_SELF_INGEST .graphifyignore fix line when that predicate is true for the resolved destinations under cwd"
    - "pytest covers at least one .graphifyignore pattern that already excludes nested graphify-out (e.g. **/graphify-out/**)"
---

<objective>
Stop duplicate **“add graphify-out/** to .graphifyignore”** guidance when patterns already exclude nested output (**HYG-04**, **D-48.01–02**, **D-48.05**).
</objective>

<threat_model>
| Threat | Mitigation |
|--------|------------|
| Hint suppressed while files would still ingest | Use same combined patterns as `detect` / `collect_files`; fixture asserts ingest vs skip |
| Doctor/detect drift | Single module-level or `corpus_prune` helper imported by both |
</threat_model>

<tasks>

<task type="execute">
  <name>48-01-01 — Shared “nested output excluded?” predicate</name>
  <read_first>
    - graphify/detect.py
    - graphify/doctor.py
    - graphify/corpus_prune.py
    - .planning/phases/48-fix-graphifyignore-nested-graphify-out/48-CONTEXT.md
  </read_first>
  <acceptance_criteria>
    - `pytest tests/test_doctor.py -q -k graphifyignore` (or new test file path chosen by implementer) exits 0 for the new duplicate-suppression case
    - `grep -n "WOULD_SELF_INGEST" graphify/doctor.py` shows conditional guard, not unconditional append of ignore fix
  </acceptance_criteria>
  <action>
    Add a predicate that, given `cwd`, `resolved`, and optional concrete `Path`s under cwd (notes_dir / artifacts_dir), returns whether nested self-output would already be ignored by `_is_ignored` semantics using the same pattern list as `detect` (`_load_graphifyignore` + `exclude_globs`). Wire `doctor._build_recommended_fixes` so the graphifyignore branch of the WOULD_SELF_INGEST fix is skipped when the predicate is true. Document the function in a one-line docstring referencing Phase 48 / HYG-04.
  </action>
</task>

</tasks>
