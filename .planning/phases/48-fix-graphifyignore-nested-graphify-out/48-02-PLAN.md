---
phase: 48
plan: "02"
type: execute
wave: 2
depends_on:
  - "01"
files_modified:
  - graphify/__main__.py
  - graphify/output.py
  - tests/test_detect.py
  - tests/test_extract.py
  - CLAUDE.md
autonomous: true
requirements:
  - HYG-05
must_haves:
  truths:
    - "No code path creates a new nested graphify-out tree under the corpus scan root when ResolvedOutput points to a canonical artifacts_dir outside that subtree (document any intentional exception)"
    - "At least one pytest (tmp_path) locks canonical-root vs nested sprawl behavior"
    - "Docs or CLAUDE.md mention operator expectations for legacy nested directories (warn-only)"
---

<objective>
Enforce **canonical per-run output root** behavior and document legacy nested **`graphify-out/`** on disk (**HYG-05**, **D-48.03–04**, **D-48.06**).
</objective>

<threat_model>
| Threat | Mitigation |
|--------|------------|
| Breaking vault-relative or sibling-of-vault layouts | Only change paths that incorrectly mkdir nested `graphify-out` under scan root; run full `pytest tests/ -q` before merge |
| Deleting user artifacts | Per CONTEXT: warn/document only — no auto-delete |
</threat_model>

<tasks>

<task type="execute">
  <name>48-02-01 — Audit mkdir / default output paths</name>
  <read_first>
    - graphify/__main__.py
    - graphify/output.py
    - graphify/pipeline.py
    - .planning/phases/48-fix-graphifyignore-nested-graphify-out/48-CONTEXT.md
  </read_first>
  <acceptance_criteria>
    - `rg "graphify-out" graphify/__main__.py graphify/pipeline.py graphify/export.py` reviewed; each mkdir/write root documented or aligned with `resolved.artifacts_dir`
    - `pytest tests/ -q` passes
  </acceptance_criteria>
  <action>
    Trace all creation of `graphify-out` directories. Ensure resolved canonical `artifacts_dir` is used consistently so new runs do not sprawl nested trees under arbitrary corpus folders. Add stderr or doctor-adjacent warning text for pre-existing nested trees only if low-noise and covered by tests.
  </action>
</task>

<task type="execute">
  <name>48-02-02 — Regression tests + doc touch</name>
  <read_first>
    - tests/test_detect.py
    - tests/test_extract.py
    - CLAUDE.md
  </read_first>
  <acceptance_criteria>
    - New test(s) in `tests/test_detect.py` and/or `tests/test_extract.py` (or focused new file) prove no nested sprawl under the scenario from 48-CONTEXT
    - `CLAUDE.md` or `docs/` has a short bullet on canonical `graphify-out` vs legacy nested folders
  </acceptance_criteria>
  <action>
    Add tmp_path fixtures mirroring roadmap success criterion 2; update CLAUDE.md “Architecture” or “Security/path” notes minimally.
  </action>
</task>

</tasks>
