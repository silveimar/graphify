---
phase: 34-mapping-cluster-quality-note-classes
plan: 4
type: execute
wave: 4
depends_on: [34-02, 34-03]
files_modified:
  - graphify/export.py
  - graphify/templates.py
  - tests/test_export.py
  - tests/test_templates.py
autonomous: true
requirements: [COMM-01, CLUST-02, CLUST-03, GOD-02, GOD-03, GOD-04]
user_setup: []
must_haves:
  truths:
    - "CODE notes link upward to their final rendered concept MOC title through frontmatter and body wikilinks."
    - "Concept MOCs list important CODE notes through frontmatter/body links."
    - "Concept-name overrides are reflected in per-node parent links before CODE notes render."
    - "Bucketed isolates and hostless below-floor nodes point to `_Unclassified`."
    - "The relevant Phase 34 test surface passes together."
  artifacts:
    - path: "graphify/export.py"
      provides: "Final concept-label propagation into node contexts before rendering."
      contains: ["parent_moc_label", "merged_labels", "filename_stem"]
    - path: "graphify/templates.py"
      provides: "CODE frontmatter/body links and MOC CODE member links using existing sanitizers."
      contains: ["code_member", "related", "_emit_wikilink"]
    - path: "tests/test_templates.py"
      provides: "Rendering proof for CODE up links and MOC CODE members."
      contains: ["type: code", "related", "code"]
    - path: "tests/test_export.py"
      provides: "End-to-end dry-run proof for CODE/MOC output and final labels."
      contains: ["CODE_", "_Unclassified"]
  key_links:
    - from: "graphify/export.py"
      to: "graphify/templates.render_note"
      via: "final parent_moc_label supplied to CODE contexts"
      pattern: "parent_moc_label"
    - from: "graphify/templates.render_moc"
      to: "ClassificationContext.code_members"
      via: "context-only rendering"
      pattern: "code_member"
---

<objective>
Wire Phase 34 classification and filename data into minimal rendering proof for CODE-to-concept navigation.

Purpose: The user-facing contract is not complete until CODE notes and concept MOCs can navigate to each other using final rendered names and safe links.
Output: CODE `up:` links, MOC CODE-member links, final concept-name propagation, and a focused phase test gate.
</objective>

<execution_context>
@$HOME/.cursor/get-shit-done/workflows/execute-plan.md
@$HOME/.cursor/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/34-mapping-cluster-quality-note-classes/34-CONTEXT.md
@.planning/phases/34-mapping-cluster-quality-note-classes/34-RESEARCH.md
@.planning/phases/34-mapping-cluster-quality-note-classes/34-PATTERNS.md
@graphify/export.py
@graphify/templates.py
@tests/test_export.py
@tests/test_templates.py

<interfaces>
Rendering interfaces to preserve:
- `render_note()` already builds `up:` from `classification_context["parent_moc_label"]`.
- `render_note()` already builds `related:` from `classification_context["sibling_labels"]`.
- `render_moc()` must render from `ClassificationContext`; do not graph-walk inside templates to discover CODE members.
- Phase 33 concept naming can replace community labels after `classify()`, so export must propagate final labels into per-node contexts before rendering.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add CODE navigation rendering tests</name>
  <files>tests/test_templates.py, tests/test_export.py</files>
  <read_first>
    - graphify/templates.py
    - graphify/export.py
    - tests/test_templates.py
    - tests/test_export.py
    - .planning/phases/34-mapping-cluster-quality-note-classes/34-CONTEXT.md
  </read_first>
  <behavior>
    - Rendering a CODE note with `parent_moc_label: "Auth Concepts"` emits `type: code`, an `up:` frontmatter wikilink to `Auth Concepts`, and a body/Wayfinder link to the same MOC.
    - Rendering a MOC context with `code_member_labels: ["CODE_graphify_Auth_Service"]` or equivalent `code_members` emits CODE links in `related:` or a dedicated CODE member section.
    - Export with concept label override updates CODE node `parent_moc_label` to the final rendered MOC title before rendering.
    - Bucketed isolates point to `_Unclassified` rather than an old community label.
  </behavior>
  <action>
    In `tests/test_templates.py`, add CODE rendering tests that call `render_note()` with `note_type="code"` and a `ClassificationContext` containing `parent_moc_label: "Auth Concepts"`, `filename_stem: "CODE_graphify_Auth_Service"`, and safe source metadata. Assert the rendered text contains `type: code`, `up:`, and a wikilink to `Auth Concepts`.

    Add a MOC rendering test with context fields from Plan 02: `code_member_labels` and/or `code_members`. Assert the MOC text contains a CODE member wikilink and that related/frontmatter output uses existing wikilink format rather than raw labels. Use a label containing `]]` or `|` in one test value to prove existing wikilink alias sanitization is used.

    In `tests/test_export.py`, add an end-to-end dry-run test where `community_labels={0: "Auth Concepts"}` or the existing Phase 33 concept naming override path changes the final concept MOC title. Assert the CODE note content or merge plan preview, if accessible in current tests, links to `Auth Concepts` and not the pre-override topology label. If dry-run actions expose only paths, add the assertion at the smallest export helper level that mutates `per_node` before rendering.
  </action>
  <verify>
    <automated>pytest tests/test_templates.py tests/test_export.py -q</automated>
  </verify>
  <acceptance_criteria>
    - `rg 'type: code|Auth Concepts|code_member|_Unclassified' tests/test_templates.py tests/test_export.py` finds explicit navigation assertions.
    - `pytest tests/test_templates.py tests/test_export.py -q` passes after production edits.
  </acceptance_criteria>
  <done>Tests prove CODE notes point up to final concept MOCs and MOCs list important CODE notes safely.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Render CODE and MOC bidirectional links</name>
  <files>graphify/export.py, graphify/templates.py, tests/test_templates.py, tests/test_export.py</files>
  <read_first>
    - graphify/export.py
    - graphify/templates.py
    - graphify/mapping.py
    - graphify/naming.py
    - tests/test_templates.py
    - tests/test_export.py
  </read_first>
  <behavior>
    - After concept naming/label overrides are merged, every `per_node` context with a `community_id`, `parent_community_id`, `host_community_id`, or equivalent Phase 34 routing key receives the final `parent_moc_label`.
    - CODE notes use the same `up:` frontmatter and Wayfinder/body behavior as other non-MOC notes, targeting their final concept MOC.
    - MOC notes include CODE member links from `code_member_labels`/`code_members` using `_emit_wikilink()` and include those labels in `related:` unless a dedicated field is already cleaner in the existing template structure.
    - Collision metadata renders only when `filename_collision` is true and is written through the existing frontmatter/metadata callout path.
  </behavior>
  <action>
    In `graphify/export.py`, after applying `merged_labels`/concept-name titles to `per_community`, also update `per_node` contexts. For each node context, determine the rendered parent community using existing context keys (`community_id`, `parent_community_id`, `host_community_id`, or `bucket_moc_label`). Set `parent_moc_label` to the final `per_community[cid]["label"]` or `community_name`; set `_Unclassified` for bucketed isolate contexts. This fixes the Phase 34 research pitfall where CODE `up:` links could point to stale topology names.

    In `graphify/templates.py`, preserve `render_note()` as the CODE renderer. If `classification_context["filename_stem"]` is present, use it for the returned filename stem before applying `.md`, so CODE files keep `CODE_<repo>_<node>` names from Plan 03. Add optional frontmatter/metadata fields for `filename_collision` and `filename_collision_hash` only when collision occurred; write them through `_build_frontmatter_fields()` extension or the existing metadata callout machinery, not as raw YAML strings.

    Extend `_render_moc_like()` to consume `code_member_labels`/`code_members` from `classification_context`. Add CODE wikilinks to `related:` or a dedicated rendered section named "Important CODE Notes"; use `_emit_wikilink()` for every link. Do not inspect `G` to rediscover CODE members; the context is the contract per 34-PATTERNS.md.
  </action>
  <verify>
    <automated>pytest tests/test_mapping.py tests/test_templates.py tests/test_export.py tests/test_profile.py tests/test_naming.py -q</automated>
  </verify>
  <acceptance_criteria>
    - `rg 'filename_stem|filename_collision|code_member|parent_moc_label' graphify/templates.py graphify/export.py` finds the rendering and label propagation code.
    - `rg 'render_community_overview' graphify/export.py` does not show a normal output fallback to legacy community overview rendering.
    - `pytest tests/test_mapping.py tests/test_templates.py tests/test_export.py tests/test_profile.py tests/test_naming.py -q` passes.
  </acceptance_criteria>
  <done>Rendered CODE notes and concept MOCs have safe bidirectional navigation, final parent labels, and collision provenance.</done>
</task>

<task type="auto">
  <name>Task 3: Run phase gate and record remaining boundaries</name>
  <files>tests/test_mapping.py, tests/test_templates.py, tests/test_export.py, tests/test_profile.py, tests/test_naming.py</files>
  <read_first>
    - .planning/ROADMAP.md
    - .planning/REQUIREMENTS.md
    - .planning/phases/34-mapping-cluster-quality-note-classes/34-CONTEXT.md
    - tests/test_mapping.py
    - tests/test_templates.py
    - tests/test_export.py
    - tests/test_profile.py
    - tests/test_naming.py
  </read_first>
  <action>
    Run the Phase 34 focused gate and then the full suite if the focused gate passes. Do not fix the known deferred baseline failures `tests/test_detect.py::test_detect_skips_dotfiles` or `tests/test_extract.py::test_collect_files_from_dir` in this phase; if they still fail in the full suite, record them in the plan summary as pre-existing deferred baseline failures from `.planning/STATE.md`.
  </action>
  <verify>
    <automated>pytest tests/test_mapping.py tests/test_templates.py tests/test_export.py tests/test_profile.py tests/test_naming.py -q</automated>
    <automated>pytest tests/ -q</automated>
  </verify>
  <acceptance_criteria>
    - Focused Phase 34 test command passes.
    - Full suite is run; any failure is either fixed if introduced by Phase 34 or recorded as the known deferred baseline failures from STATE.md.
    - No source file contains new logic that scans or deletes existing `_COMMUNITY_*` vault files.
  </acceptance_criteria>
  <done>Phase 34 focused behavior is verified and any residual non-Phase-34 baseline failures are documented.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| final concept labels -> CODE up links | LLM/cache/override concept names can contain unsafe wiki/frontmatter characters. |
| CODE filename metadata -> frontmatter/body | Collision provenance and filenames are rendered into Markdown. |
| MOC code member context -> MOC links | CODE member labels become body/frontmatter wikilinks. |
| export completion -> test gate | Full-suite failures must not hide introduced regressions or unrelated deferred baseline failures. |
| legacy vault notes -> Phase 34 scope | Legacy `_COMMUNITY_*` files must remain untouched. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-34-04-01 | Tampering | CODE `up:` and MOC CODE links | mitigate | Use `_emit_wikilink()` and `_sanitize_wikilink_alias()` for all body/frontmatter wikilinks; add tests with `]]` or `|` in labels. |
| T-34-04-02 | Tampering | collision frontmatter metadata | mitigate | Render collision fields through existing frontmatter/metadata helpers, never raw YAML strings. |
| T-34-04-03 | Spoofing | concept label propagation | mitigate | Update per-node `parent_moc_label` after final concept labels are known, preventing stale topology names from spoofing navigation targets. |
| T-34-04-04 | Repudiation | verification results | mitigate | Run focused Phase 34 tests and full suite; distinguish introduced failures from documented deferred baseline failures. |
| T-34-04-05 | Tampering / Data loss | legacy `_COMMUNITY_*` files | mitigate | Keep Phase 34 limited to generated output behavior; do not scan, edit, or delete existing vault legacy files. |
</threat_model>

<verification>
Run the focused Phase 34 gate:

```bash
pytest tests/test_mapping.py tests/test_templates.py tests/test_export.py tests/test_profile.py tests/test_naming.py -q
```

Then run the full suite:

```bash
pytest tests/ -q
```
</verification>

<success_criteria>
- CODE notes include safe `up:`/body links to the final rendered concept MOC title.
- Concept MOCs include safe links to capped important CODE member notes.
- Bucketed isolate nodes point to `_Unclassified`.
- CODE collision provenance renders only for collisions.
- The focused Phase 34 test gate passes.
</success_criteria>

<output>
After completion, create `.planning/phases/34-mapping-cluster-quality-note-classes/34-04-SUMMARY.md`.
</output>
