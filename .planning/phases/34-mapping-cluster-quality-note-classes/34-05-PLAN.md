---
phase: 34-mapping-cluster-quality-note-classes
plan: 5
type: execute
wave: 5
depends_on: [34-04]
files_modified:
  - graphify/templates.py
  - tests/test_templates.py
autonomous: true
gap_closure: true
requirements: [GOD-03, GOD-04]
user_setup: []
must_haves:
  truths:
    - "Concept MOCs list important CODE member notes with wikilink targets that exactly match generated CODE filename stems."
    - "MOC CODE link aliases remain sanitized and readable even when the preserved target is a CODE filename stem."
    - "CODE filename identity remains collision-safe and is not rewritten through title-case wikilink normalization."
  artifacts:
    - path: "graphify/templates.py"
      provides: "MOC CODE member link rendering that preserves code_members[].filename_stem as the wikilink target."
      contains: ["_build_code_members_section", "filename_stem", "_sanitize_wikilink_alias"]
    - path: "tests/test_templates.py"
      provides: "Regression coverage for exact MOC-to-CODE wikilink targets."
      contains: ["CODE_graphify_Auth_Session", "[[CODE_graphify_Auth_Session|"]
  key_links:
    - from: "graphify/export.py"
      to: "graphify/templates._build_code_members_section"
      via: "ClassificationContext.code_members[].filename_stem populated by export before MOC rendering"
      pattern: "filename_stem"
    - from: "graphify/templates._build_code_members_section"
      to: "generated MOC markdown"
      via: "exact wikilink target plus sanitized alias"
      pattern: "\\[\\[CODE_graphify_Auth_Session\\|"
---

<objective>
Close the Phase 34 blocker where concept MOCs link to rewritten CODE note targets instead of the generated CODE filenames.

Purpose: GOD-03 is only satisfied when concept MOC CODE member links navigate to the actual CODE note files, and GOD-04 depends on preserving the collision-safe CODE filename identity produced upstream.
Output: A narrow template fix plus a regression test proving `[[CODE_graphify_Auth_Session|...]]` is rendered instead of `[[Code_Graphify_Auth_Session|...]]`.
</objective>

<execution_context>
@$HOME/.cursor/get-shit-done/workflows/execute-plan.md
@$HOME/.cursor/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/34-mapping-cluster-quality-note-classes/34-CONTEXT.md
@.planning/phases/34-mapping-cluster-quality-note-classes/34-VERIFICATION.md
@.planning/phases/34-mapping-cluster-quality-note-classes/34-04-SUMMARY.md
@graphify/templates.py
@tests/test_templates.py

<interfaces>
Existing rendering contract to preserve:
- `graphify/export.py` already enriches MOC `code_members` with `filename_stem`; do not rework export unless the regression proves that field is absent.
- `graphify/templates.py` currently has `_build_code_members_section(code_members, code_member_labels, convention)` and `_code_member_display_labels(...)`.
- `_emit_wikilink(label, convention)` is for label-to-target generation. It must not be used when the caller already has an exact filename stem target.
- Use the existing alias sanitizer for display text. Do not render raw labels, raw YAML, or unsanitized wikilink aliases.
</interfaces>
</context>

<source_audit>
## Multi-Source Coverage Audit

| Source | Item | Coverage |
|--------|------|----------|
| GOAL | Phase 34 requires code-derived hubs separated from concept MOCs with working bidirectional navigation. | Covered by Tasks 1-2 preserving exact CODE targets in concept MOC links. |
| REQ | GOD-03: Concept MOCs list important CODE member notes. | Covered by the regression test and `_build_code_members_section()` fix. |
| REQ | GOD-04: Filename collisions between CODE notes and concept MOCs are prevented. | Covered by preserving upstream `filename_stem` instead of re-normalizing it. |
| CONTEXT | D-14: CODE and concept MOC navigation is bidirectional. | Covered by exact MOC-to-CODE wikilink target assertion. |
| CONTEXT | D-16: Concept MOCs list important CODE notes. | Covered by `_build_code_members_section()` behavior. |
| CONTEXT | D-17: CODE member listings use context-owned important members. | Covered by consuming `code_members` only; no graph rediscovery. |
| VERIFICATION | Failed truth: MOC link target `Code_Graphify_Auth_Session` does not match file `CODE_graphify_Auth_Session.md`. | Covered by asserting `[[CODE_graphify_Auth_Session|...]]`. |
</source_audit>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add exact CODE target regression</name>
  <files>tests/test_templates.py</files>
  <read_first>
    - graphify/templates.py
    - tests/test_templates.py
    - .planning/phases/34-mapping-cluster-quality-note-classes/34-VERIFICATION.md
    - .planning/phases/34-mapping-cluster-quality-note-classes/34-04-SUMMARY.md
  </read_first>
  <behavior>
    - Rendering a MOC with `code_members: [{"filename_stem": "CODE_graphify_Auth_Session", "label": "Auth Session"}]` emits a CODE member link whose target is exactly `CODE_graphify_Auth_Session`.
    - The rendered alias is sanitized/readable and does not need to equal the filename stem.
    - The old broken target `[[Code_Graphify_Auth_Session|...]]` does not appear.
  </behavior>
  <action>
    In `tests/test_templates.py`, add a focused regression near the existing MOC/CODE rendering tests. Build a small in-memory graph and call `render_moc()` or `_render_moc_like()` with a classification context that mirrors export-enriched MOC context:
    `note_type: "moc"`, `community_name: "Auth Concepts"`, `code_members: [{"id": "n_auth_session", "label": "Auth Session", "filename_stem": "CODE_graphify_Auth_Session"}]`, plus the minimal required MOC fields.

    Assert the rendered MOC text contains `[[CODE_graphify_Auth_Session|` and does not contain `[[Code_Graphify_Auth_Session|`. Also assert the alias side of the link remains readable/sanitized, for example by checking the link contains `|Auth Session]]` or another sanitized alias chosen by the implementation. This test should fail before the production change because `_emit_wikilink()` title-cases the target.
  </action>
  <verify>
    <automated>pytest tests/test_templates.py -q</automated>
  </verify>
  <acceptance_criteria>
    - `tests/test_templates.py` contains an assertion for `[[CODE_graphify_Auth_Session|`.
    - The regression fails before the template implementation is changed and passes after Task 2.
  </acceptance_criteria>
  <done>A regression test locks the exact MOC-to-CODE target that the verifier identified as broken.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Preserve filename_stem for MOC CODE links</name>
  <files>graphify/templates.py, tests/test_templates.py</files>
  <read_first>
    - graphify/templates.py
    - tests/test_templates.py
    - .planning/phases/34-mapping-cluster-quality-note-classes/34-CONTEXT.md
    - .planning/phases/34-mapping-cluster-quality-note-classes/34-VERIFICATION.md
  </read_first>
  <behavior>
    - Structured `code_members` entries with `filename_stem` use that stem, sanitized as a filename stem only, as the wikilink target.
    - Display aliases come from member `label` when available, then `filename_stem`, and are sanitized with the existing wikilink alias sanitizer.
    - Legacy `code_member_labels` fallback keeps the existing `_emit_wikilink()` behavior because those entries do not carry an exact generated target.
    - Invalid or empty structured entries are skipped rather than rendered as raw reprs.
  </behavior>
  <action>
    In `graphify/templates.py`, split structured CODE member rendering from generic label rendering. Do not pass `filename_stem` through `_emit_wikilink()` or `resolve_filename()`, because those helpers are for deriving targets from display labels and will rewrite `CODE_graphify_Auth_Session` to `Code_Graphify_Auth_Session`.

    Implement a small helper or local logic in `_build_code_members_section()` that, for each dict in `code_members`, reads `filename_stem`, validates it is a non-empty string, applies `safe_filename()` to keep the target path-safe while preserving its case/underscores, and builds `[[{target}|{alias}]]` with `alias` sanitized through `_sanitize_wikilink_alias(str(member.get("label") or filename_stem))`. Keep `code_member_labels` as a fallback only when no structured links are produced.

    Do not touch `graphify/export.py` unless the Task 1 regression proves `filename_stem` is not present in the MOC context. Do not add migration behavior, legacy `_COMMUNITY_*` scans, or broad template polish from Phase 35.
  </action>
  <verify>
    <automated>pytest tests/test_templates.py -q</automated>
    <automated>pytest tests/test_mapping.py tests/test_templates.py tests/test_export.py tests/test_profile.py tests/test_naming.py -q</automated>
  </verify>
  <acceptance_criteria>
    - MOC CODE member links target `[[CODE_graphify_Auth_Session|...]]`.
    - The old broken target `[[Code_Graphify_Auth_Session|...]]` is absent from the new regression output.
    - Existing CODE/MOC navigation tests still pass.
    - No files outside `graphify/templates.py` and `tests/test_templates.py` are changed unless the executor documents a verifier-relevant reason in the summary.
  </acceptance_criteria>
  <done>Concept MOCs preserve exact CODE filename stems as link targets while aliases remain sanitized and readable.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| export-enriched context -> MOC renderer | `code_members[].filename_stem` and labels enter generated Markdown. |
| CODE member label -> wikilink alias | Human-readable labels may contain `]]`, `|`, control characters, or template-like text. |
| CODE filename stem -> wikilink target | Collision-safe filename identity must remain path-safe without being semantically rewritten. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-34-05-01 | Tampering | `_build_code_members_section()` target rendering | mitigate | Preserve `filename_stem` as the target after `safe_filename()` only; do not pass it through label-based title-case normalization. |
| T-34-05-02 | Tampering | CODE member wikilink aliases | mitigate | Sanitize aliases with the existing wikilink alias sanitizer before building `[[target|alias]]`. |
| T-34-05-03 | Spoofing | CODE/MOC collision identity | mitigate | Continue using export-provided `CODE_<repo>_<node>` stems and collision suffixes; do not derive new CODE targets in templates. |
| T-34-05-04 | Information Disclosure | Generated Markdown labels | accept | The renderer only emits labels already present in graph/export context; no new data source is introduced. |
</threat_model>

<verification>
Run the focused template test:

```bash
pytest tests/test_templates.py -q
```

Run the Phase 34 focused gate:

```bash
pytest tests/test_mapping.py tests/test_templates.py tests/test_export.py tests/test_profile.py tests/test_naming.py -q
```
</verification>

<success_criteria>
- The rendered MOC CODE link target is `CODE_graphify_Auth_Session`, matching the generated `CODE_graphify_Auth_Session.md` file stem.
- The rendered alias is sanitized and readable.
- Phase 34 GOD-03 and GOD-04 are no longer blocked by MOC-to-CODE target drift.
- The change stays narrow to `graphify/templates.py` and `tests/test_templates.py`.
</success_criteria>

<output>
After completion, create `.planning/phases/34-mapping-cluster-quality-note-classes/34-05-SUMMARY.md`.
</output>
