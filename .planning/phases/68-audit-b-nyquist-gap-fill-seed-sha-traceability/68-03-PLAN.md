---
phase: 68
plan: 03
type: execute
wave: 2
depends_on: [01, 02]
files_modified:
  - .planning/PROJECT.md
  - .planning/REQUIREMENTS.md
  - .planning/MILESTONES.md
autonomous: true
requirements: [AUDIT-01, AUDIT-03]
tags: [audit, seed-traceability, milestones]
must_haves:
  truths:
    - "PROJECT.md SEED bullets reflect closure of v1.13 by milestone (v1.13 added to SEED-vault-root-aware-cli and SEED-bidirectional-concept-code-links lines)."
    - "REQUIREMENTS.md AUDIT-01 and AUDIT-03 checkboxes are flipped from [ ] to [x]."
    - "MILESTONES.md has a new ## v1.12 section prepended after # Milestones, before ## v1.11."
    - "The v1.12 MILESTONES.md section names Phase 68 as the audit-closure agent for the Nyquist gap-fill and SEED traceability deferred items."
    - "scripts/audit_b_closure.py exits 0 as the final phase gate."
  artifacts:
    - path: ".planning/PROJECT.md"
      contains: "v1.13 (Option B silent reroute Phase 63"
    - path: ".planning/REQUIREMENTS.md"
      contains: "[x] **AUDIT-01**"
    - path: ".planning/MILESTONES.md"
      contains: "## v1.12 Vault Awareness, Pipeline Integration"
  key_links:
    - from: ".planning/MILESTONES.md (v1.12 section)"
      to: ".planning/milestones/v1.12-VALIDATION.md"
      via: "Audit closure cross-reference"
      pattern: "v1.12-VALIDATION\\.md"
    - from: ".planning/MILESTONES.md (v1.12 section)"
      to: "scripts/audit_b_closure.py"
      via: "Re-run command line"
      pattern: "audit_b_closure\\.py"
---

<objective>
Apply the SEED-SHA traceability annotations per locked decision D-03, flip the AUDIT-01 / AUDIT-03 checkboxes in REQUIREMENTS.md, and create the missing `## v1.12` section in MILESTONES.md per locked decision D-05. Final phase gate is `python scripts/audit_b_closure.py` exiting 0.

Purpose: This plan closes AUDIT-03 (seed traceability) and the AUDIT-01 documentation surface. After this plan, no v1.12-deferred audit item within Phase 68's scope remains open.
Output: Three planning files patched (PROJECT.md, REQUIREMENTS.md, MILESTONES.md). No code changes.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/68-audit-b-nyquist-gap-fill-seed-sha-traceability/68-CONTEXT.md
@.planning/phases/68-audit-b-nyquist-gap-fill-seed-sha-traceability/68-RESEARCH.md
@.planning/PROJECT.md
@.planning/REQUIREMENTS.md
@.planning/MILESTONES.md
@.planning/milestones/v1.12-MILESTONE-AUDIT.md
@.planning/milestones/v1.12-VALIDATION.md

<interfaces>
<!-- D-03 locked SEED annotation patches (per RESEARCH §SEED Annotation Targets) -->

# PROJECT.md line 52 — current:
- **SEED-vault-root-aware-cli** — 80% CONSUMED by **v1.12** (VCWD-01..05 Phase 59; argparse fix Phase 62.1). Remaining: silent reroute Option B → scoped into v1.13.

# PROJECT.md line 52 — patched:
- **SEED-vault-root-aware-cli** — CONSUMED by **v1.12** (VCWD-01..05 Phase 59; argparse fix Phase 62.1) + **v1.13** (Option B silent reroute Phase 63, closes remaining 20%).

# PROJECT.md line 53 — current:
- **SEED-bidirectional-concept-code-links** — 65% CONSUMED by **v1.10/v1.11** (Phase 46 CCODE schema; Phase 53/54 CGRAPH typed edges + `/trace` integration). Remaining 35% (cross-repo identity, per-edge confidence, drift, parameterized queries) → scoped into v1.13.

# PROJECT.md line 53 — patched:
- **SEED-bidirectional-concept-code-links** — CONSUMED by **v1.10/v1.11** (Phase 46 CCODE schema; Phase 53/54 CGRAPH typed edges + `/trace` integration) + **v1.13** (CCONF per-edge confidence Phase 65, CFED cross-repo federation Phase 66, CDRIFT/CQUERY drift detection + parameterized queries Phase 67 — closes remaining 35%).

# SEED-001 (line 50) and SEED-002 (line 51) — D-03 says these already match. Leave unchanged.

# REQUIREMENTS.md — flip checkboxes:
#   line 56:  - [ ] **AUDIT-01**:  →  - [x] **AUDIT-01**:
#   line 58:  - [ ] **AUDIT-03**:  →  - [x] **AUDIT-03**:

<!-- D-05: MILESTONES.md needs a new ## v1.12 section prepended after `# Milestones`, before `## v1.11`. -->
<!-- Style follows v1.11 (RESEARCH §MILESTONES.md). Voice: as-shipped (2026-05-04) with audit-closure subsection naming Phase 68. -->
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Patch SEED annotations in PROJECT.md and flip AUDIT checkboxes in REQUIREMENTS.md</name>
  <files>.planning/PROJECT.md, .planning/REQUIREMENTS.md</files>
  <action>
    Per D-03 (locked) and the verbatim diffs in `<interfaces>` above:

    **PROJECT.md edits** (use Edit tool; preserve all surrounding text and voice):
    1. Line 52 (`SEED-vault-root-aware-cli` bullet) — replace with patched form. Net effect: removes "80% CONSUMED ... Remaining: ... → scoped into v1.13" and writes the v1.13-closure form naming Phase 63.
    2. Line 53 (`SEED-bidirectional-concept-code-links` bullet) — replace with patched form. Net effect: removes "65% CONSUMED ... Remaining 35% (...)→ scoped into v1.13" and writes the v1.13-closure form naming Phases 65, 66, 67.

    Do NOT modify lines 50 (SEED-001) or 51 (SEED-002) — D-03 confirmed these already reflect their full consumption.
    Do NOT modify lines 17 / 23 (the "remaining" parenthetical headers above the bullets) — those describe v1.13 scope at start; rewriting them would invent state. The audit traceability lives in the SEED bullets per D-03.

    **REQUIREMENTS.md edits**:
    1. Line 56: `- [ ] **AUDIT-01**:` → `- [x] **AUDIT-01**:` (no other change to that line)
    2. Line 58: `- [ ] **AUDIT-03**:` → `- [x] **AUDIT-03**:` (no other change to that line)

    The AUDIT-03 line itself (line 58) already contains all 4 SEED→milestone mappings inline; per D-03 the checkbox flip is the sufficient annotation in REQUIREMENTS.md (no new traceability table). Confirmed in RESEARCH §SEED Annotation Targets.

    Pitfalls:
    - Multiple SEED references exist in PROJECT.md (lines 17, 23, 50–53, 69, 89, 92–93, 158). ONLY edit lines 52 and 53. Other lines are historical milestone narrative or future-milestone seed audit notes (D-03 boundary).
    - Use Edit with `old_string` long enough to be unique (e.g., include the leading `- **SEED-vault-root-aware-cli**` portion).
  </action>
  <verify>
    <automated>grep -q "SEED-vault-root-aware-cli\*\* — CONSUMED by \*\*v1.12\*\*.*v1.13.*Option B silent reroute Phase 63" .planning/PROJECT.md &amp;&amp; grep -q "SEED-bidirectional-concept-code-links\*\* — CONSUMED by \*\*v1.10/v1.11\*\*.*v1.13.*CCONF.*Phase 65.*CFED.*Phase 66.*CDRIFT/CQUERY.*Phase 67" .planning/PROJECT.md &amp;&amp; grep -q "^- \[x\] \*\*AUDIT-01\*\*" .planning/REQUIREMENTS.md &amp;&amp; grep -q "^- \[x\] \*\*AUDIT-03\*\*" .planning/REQUIREMENTS.md</automated>
  </verify>
  <done>
    PROJECT.md lines 52 and 53 patched with v1.13-closure annotations naming Phases 63 and 65–67; REQUIREMENTS.md AUDIT-01 and AUDIT-03 checkboxes flipped to `[x]`. No other lines modified in either file.
  </done>
</task>

<task type="auto">
  <name>Task 2: Prepend ## v1.12 section to MILESTONES.md and run final closure-script gate</name>
  <files>.planning/MILESTONES.md</files>
  <action>
    Per D-05 (locked) and RESEARCH §MILESTONES.md — v1.12 Deferred Items and D-05 Scope:

    The current MILESTONES.md has NO `## v1.12` section. Top entry today is `## v1.11 Templates, Graph Semantics & Vault Depth`. Insert a new `## v1.12 Vault Awareness, Pipeline Integration & Error Hygiene (Shipped: 2026-05-04)` section AFTER the `# Milestones` heading and BEFORE the v1.11 section.

    Style: match v1.11 voice (as-shipped) per RESEARCH Open Question recommendation. Section MUST contain:

    ```markdown
    ## v1.12 Vault Awareness, Pipeline Integration & Error Hygiene (Shipped: 2026-05-04)

    **Phases completed:** 5 phases + 1 insertion (59, 59.1, 60, 60.1, 61; insertion 62.1)
    **Audit status:** passed (13/13 requirements satisfied; see `.planning/milestones/v1.12-MILESTONE-AUDIT.md`)

    **Key accomplishments:**

    - **VCWD (59):** Vault-CWD-aware CLI default with `_resolve_cli_paths(explicit_vault=cwd)` auto-adopt across 14 commands; two-line `[graphify] error:` + `  hint:` refusal at exit 2; `doctor` `[vault-cwd]` parity section. (VCWD-01..05; SHA 5eb2c17.)
    - **VSYNC (59.1):** Silent skill-stamp self-heal on drift; multi-line `graphify --version`; `doctor` `version sync` block. (VSYNC-01..04; SHA 671045f.)
    - **E2E (60):** Milestone-level subprocess integration tests for compose+override-ladder and elicit→update-vault flows. (E2E-01/02; SHA b6378b9.)
    - **APPLY-DET (60.1):** First-run determinism for `update-vault --apply` via `random_seed=42` on graspologic Leiden plus topology-fallback respecting `note_type_templates.thing`. (APPLY-DET-01; root-cause SHA e110ead, criterion-3 closure SHA a96435a.)
    - **HARN-FMT (61):** Harness vault-write refusal migrated to two-line `_emit_vault_error` format. (HARN-FMT-01; SHA 2413f18.)
    - **Audit cleanup (62 / 62.1):** REQUIREMENTS-SYNC, EXIT-CODE-CONST, E2E-AUTO-ADOPT findings closed; insertion phase 62.1 fixed `--vault required=True` argparse defect that bypassed auto-adopt for `update-vault` and `vault-promote`.

    **Audit Closure (Phase 68 / AUDIT-B, 2026-05-06):**

    Two v1.12 audit-deferred items closed by Phase 68:

    1. **Nyquist VALIDATION gap-fill (AUDIT-01):** Retroactive entries for Phases 59, 59.1, 60, 60.1, 61 written to `.planning/milestones/v1.12-VALIDATION.md`. Each entry cites implementing SHA + asserting test path; re-run via `python scripts/audit_b_closure.py` (exit 0 with all 5 marked tests green).
    2. **Seed-SHA traceability (AUDIT-03):** REQUIREMENTS.md and PROJECT.md SEED bullets annotated with consuming milestone(s) — SEED-001 → v1.9, SEED-002 → v1.4, SEED-vault-root-aware-cli → v1.12 + v1.13 (Phase 63 closes Option B), SEED-bidirectional-concept-code-links → v1.10 / v1.11 / v1.13 (Phases 65–67 close remainder).

    **Items remaining outside Phase 68 scope** (deferred to a future audit pass; tracked in `.planning/milestones/v1.12-MILESTONE-AUDIT.md`):
    - Adjacent `print(f"[graphify] {exc}")` outlier at `__main__.py:~2745` — partially addressed by Phase 64 (AUDIT-02 sweep); residual scan deferred.
    - HARN-FMT-01 second E2E flow — single-flow regression-locked; second-flow expansion deferred.

    Archives: `.planning/milestones/v1.12-ROADMAP.md`, `.planning/milestones/v1.12-REQUIREMENTS.md`, `.planning/milestones/v1.12-MILESTONE-AUDIT.md`, `.planning/milestones/v1.12-VALIDATION.md`.

    ---
    ```

    Insertion mechanics:
    - Use Edit. `old_string` = the literal `# Milestones\n\n## v1.11 Templates, Graph Semantics & Vault Depth (Shipped: 2026-05-03)`.
    - `new_string` = `# Milestones\n\n` + the v1.12 block above (ending with `---`) + `\n\n## v1.11 Templates, Graph Semantics & Vault Depth (Shipped: 2026-05-03)`.
    - Do NOT touch the v1.11 section content or any older milestone section.

    After the insertion, run the final phase gate:
    ```
    python scripts/audit_b_closure.py
    ```
    Must exit 0. If non-zero, the phase has not closed AUDIT-01 — rerun the gate with `-v` to diagnose.
  </action>
  <verify>
    <automated>grep -q "^## v1.12 Vault Awareness, Pipeline Integration &amp; Error Hygiene" .planning/MILESTONES.md &amp;&amp; grep -q "Audit Closure (Phase 68 / AUDIT-B" .planning/MILESTONES.md &amp;&amp; grep -q "v1.12-VALIDATION.md" .planning/MILESTONES.md &amp;&amp; awk '/^## v1\./{print NR": "$0}' .planning/MILESTONES.md | head -3 | grep -q "v1.12.*Vault Awareness" &amp;&amp; python scripts/audit_b_closure.py</automated>
  </verify>
  <done>
    `## v1.12 Vault Awareness, Pipeline Integration & Error Hygiene` section exists in MILESTONES.md (positioned between `# Milestones` and `## v1.11`); the section names Phase 68 as the audit-closure agent for AUDIT-01 and AUDIT-03 and references `v1.12-VALIDATION.md`; `python scripts/audit_b_closure.py` exits 0 as the final phase gate.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| editor ↔ planning archive | This plan rewrites historical milestone narrative (MILESTONES.md v1.12 entry created retroactively). The text must accurately reflect what shipped, not what we wish had shipped. |
| editor ↔ AUDIT-03 checkbox | Flipping `[ ]` → `[x]` is an audit assertion; must only happen after Tasks 1 and 2 of this plan are complete and the closure script exits 0. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-68-07 | Repudiation | MILESTONES.md v1.12 audit-closure claim | mitigate | Audit-closure subsection cross-references `.planning/milestones/v1.12-VALIDATION.md` and `scripts/audit_b_closure.py`; both are checked-in artifacts the closure script verifies on every run. |
| T-68-08 | Tampering | SEED bullet patches in PROJECT.md | mitigate | Verify command in Task 1 greps for the v1.13-closure phrasing on lines 52 and 53. Phase gate rejects any edit that fails the grep. |
| T-68-09 | Information Disclosure | None | accept | All edited files are public planning docs. No PII / secrets. |
</threat_model>

<verification>
- PROJECT.md SEED-vault-root-aware-cli bullet contains "v1.13" + "Phase 63".
- PROJECT.md SEED-bidirectional-concept-code-links bullet contains "v1.13" + "Phase 65" + "Phase 66" + "Phase 67".
- REQUIREMENTS.md AUDIT-01 and AUDIT-03 lines start with `- [x]`.
- MILESTONES.md has `## v1.12` section with `Audit Closure (Phase 68 / AUDIT-B` subsection naming.
- The v1.12 section is positioned BEFORE the v1.11 section.
- `python scripts/audit_b_closure.py` exits 0.
- `pytest tests/ -q` full suite remains green.
</verification>

<success_criteria>
1. SEED annotations in PROJECT.md updated per D-03 (lines 52 and 53 only).
2. AUDIT-01 and AUDIT-03 checkboxes in REQUIREMENTS.md flipped to `[x]`.
3. New `## v1.12` section prepended to MILESTONES.md per D-05, naming Phase 68 as the audit-closure agent and referencing `v1.12-VALIDATION.md`.
4. `scripts/audit_b_closure.py` exits 0 — proving the v1.12 Nyquist gap-fill is re-executable from a clean checkout.
5. No v1.12-deferred audit item within Phase 68 scope remains open in MILESTONES.md.
</success_criteria>

<output>
After completion, create `.planning/phases/68-audit-b-nyquist-gap-fill-seed-sha-traceability/68-03-SUMMARY.md` recording the SEED bullet diffs, the checkbox flips, the MILESTONES.md insertion, and the final closure-script exit code.
</output>
