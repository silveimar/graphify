---
phase: 69
plan: 03
type: tdd
wave: 3
depends_on: [02]
files_modified:
  - graphify/vault_promote.py
  - tests/test_vault_promote.py
autonomous: true
requirements: [VPROF-03]
must_haves:
  truths:
    - "Pre-flight pass collects ALL user-namespace violations and aborts atomically with zero partial writes (D-09)."
    - "Every vault write in promote() flows through _write_record() chokepoint, which calls _assert_under_pinned_subtree() before write_note()."
    - "Pre-flight runs BEFORE the manifest-hash overwrite guard at vault_promote.py:702-732; manifest-hash guard remains untouched and still fires on within-pinned-subtree name collisions (D-11)."
    - "Refusal stderr matches the two-line `[graphify] error:` + `  hint:` format (D-10)."
    - "Path.resolve() is called on each target before user_only_folders check (symlink bypass mitigation, T-69-V4)."
  artifacts:
    - path: "graphify/vault_promote.py"
      provides: "_assert_under_pinned_subtree(); _write_record() chokepoint; _preflight_check_user_only_folders(); ordering preserved before manifest-hash guard"
      contains: "_assert_under_pinned_subtree"
  key_links:
    - from: "graphify/vault_promote.py:promote"
      to: "_preflight_check_user_only_folders"
      via: "called BEFORE any write_note invocation; raises on violation"
      pattern: "_preflight_check_user_only_folders\\("
    - from: "graphify/vault_promote.py:_write_record"
      to: "_assert_under_pinned_subtree"
      via: "called per-write as defense-in-depth chokepoint"
      pattern: "_assert_under_pinned_subtree\\("
---

<objective>
Implement the user-namespace refusal contract from D-08..D-11: pre-flight pass + per-write chokepoint, atomic batch refusal with two-line stderr, and a regression test proving the manifest-hash overwrite guard at lines 702-732 is preserved and still fires.

Purpose: Make it structurally impossible for graphify to write outside the pinned subtree, even via future code paths.
Output: Refactored vault_promote.py with chokepoint guard + 6 new tests.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/69-vprof-vault-profile-driven-folder-resolution-user-namespace-guard/69-CONTEXT.md
@.planning/phases/69-vprof-vault-profile-driven-folder-resolution-user-namespace-guard/69-RESEARCH.md
@.planning/phases/69-vprof-vault-profile-driven-folder-resolution-user-namespace-guard/69-PATTERNS.md
@.planning/phases/69-vprof-vault-profile-driven-folder-resolution-user-namespace-guard/69-VALIDATION.md
@.planning/phases/69-vprof-vault-profile-driven-folder-resolution-user-namespace-guard/69-02-SUMMARY.md
@graphify/vault_promote.py
@graphify/security.py
@graphify/profile.py
</context>

<interfaces>
From Plan 02:
- `_resolve_folder_prefix(bucket_key, merged_profile) -> str`
- `_BUCKET_TO_PROFILE_KEY: dict[str, str]`

Existing (PRESERVE — DO NOT MODIFY):
- `write_note(vault_dir, rel_path, content, manifest) -> str` at line 702 — manifest-hash overwrite guard at lines 702-732 stays intact.
- `validate_vault_path(candidate, vault_dir)` in profile.py — reuse pattern for resolve+containment.

New surface this plan adds:
```python
def _assert_under_pinned_subtree(rel_path: str, merged_profile: dict, vault_dir: Path) -> None
def _preflight_check_user_only_folders(planned_targets: list[tuple[str, str]], merged_profile: dict, vault_dir: Path) -> None
def _write_record(vault_dir: Path, rel_path: str, content: str, manifest: dict, merged_profile: dict) -> str
```
where `planned_targets` is `[(bucket_key, rel_path), ...]`.
</interfaces>

<tasks>

<task type="tdd" tdd="true">
  <name>Task 1: RED — six failing tests covering pre-flight, chokepoint, stderr format, manifest-hash regression, ordering, symlink resolve</name>
  <read_first>
    - graphify/vault_promote.py (lines 702-732 manifest-hash guard — DO NOT modify; lines 893-980 promote)
    - .planning/phases/69-vprof-vault-profile-driven-folder-resolution-user-namespace-guard/69-VALIDATION.md (rows 69-03-01..06)
    - .planning/phases/69-vprof-vault-profile-driven-folder-resolution-user-namespace-guard/69-CONTEXT.md (D-08..D-11)
    - .planning/phases/69-vprof-vault-profile-driven-folder-resolution-user-namespace-guard/69-PATTERNS.md (chokepoint + pre-flight blocks)
  </read_first>
  <behavior>
    - test_preflight_refusal_atomic: build a vault and a profile with `user_only_folders: ["Atlas/Maps/", "Calendar/"]` AND a `graphify_folder_mapping.map: "Atlas/Maps/Bad/"` (mapping points into a user-only folder). Run promote() — must raise SystemExit non-zero (or specific RefusalError); count files under vault_dir afterwards == 0 (zero partial writes).
    - test_write_record_chokepoint_guard: call `_write_record(vault, "Calendar/evil.md", "x", {}, profile_with_user_only=["Calendar/"])` directly — must raise ValueError matching "user-owned" / "not under any graphify-owned folder".
    - test_refusal_stderr_format: capture stderr from a refused promote(); assert first line matches `r"^\[graphify\] error: refused \d+ write\(s\) targeting user-owned folders"` and second line starts with `"  hint:"`.
    - test_manifest_hash_guard_regression: write a note via write_note() at `Atlas/Sources/Graphify/Maps/Foo.md` with manifest hash A; call write_note() again at the same rel_path with content yielding manifest hash B (collision under pinned subtree) — must raise / refuse via the existing line 702-732 guard. Confirms guard untouched.
    - test_preflight_before_manifest_guard: configure both a user-only violation AND a manifest-hash collision in the same run; assert the user-only refusal fires first (the manifest-hash code path is never reached — assert via mock/spy that write_note is never called).
    - test_user_only_symlink_resolved: create `vault/Atlas/Sources/Graphify/Maps -> ../../../Calendar` symlink; profile.user_only_folders=["Calendar/"]; attempting to write `Atlas/Sources/Graphify/Maps/x.md` must refuse (Path.resolve() resolves the symlink before the user_only check).
  </behavior>
  <action>
    Add the six tests to tests/test_vault_promote.py per VALIDATION rows 69-03-01..06. Use tmp_path only; use `pytest.raises` for refusal cases; use `capsys` for stderr capture. All six must FAIL at this commit because pre-flight + chokepoint not yet implemented.
  </action>
  <verify>
    <automated>pytest tests/test_vault_promote.py::test_preflight_refusal_atomic tests/test_vault_promote.py::test_write_record_chokepoint_guard tests/test_vault_promote.py::test_refusal_stderr_format tests/test_vault_promote.py::test_manifest_hash_guard_regression tests/test_vault_promote.py::test_preflight_before_manifest_guard tests/test_vault_promote.py::test_user_only_symlink_resolved -x 2>&1 | grep -E "FAILED|ERROR" | head -6</automated>
  </verify>
  <acceptance_criteria>
    - 6 new test functions present (`grep -c "def test_preflight_refusal_atomic\|def test_write_record_chokepoint_guard\|def test_refusal_stderr_format\|def test_manifest_hash_guard_regression\|def test_preflight_before_manifest_guard\|def test_user_only_symlink_resolved" tests/test_vault_promote.py` returns 6)
    - All 6 tests fail (RED)
    - Commit: `test(69-03): add failing tests for user-namespace refusal + manifest-hash regression`
  </acceptance_criteria>
  <done>Six failing tests in place; production untouched.</done>
</task>

<task type="tdd" tdd="true">
  <name>Task 2: GREEN — implement chokepoint, pre-flight pass, two-line stderr, ordering invariant</name>
  <read_first>
    - graphify/vault_promote.py (lines 702-732 — PRESERVE EXACTLY; lines 893-980 promote)
    - graphify/profile.py validate_vault_path (lines 1480-1494) — reuse resolve+containment pattern
    - .planning/phases/69-vprof-vault-profile-driven-folder-resolution-user-namespace-guard/69-PATTERNS.md (vault_promote.py chokepoint + pre-flight code blocks)
    - .planning/phases/69-vprof-vault-profile-driven-folder-resolution-user-namespace-guard/69-CONTEXT.md (D-08..D-11 verbatim)
  </read_first>
  <action>
    Step 1 — Implement `_assert_under_pinned_subtree(rel_path: str, merged_profile: dict, vault_dir: Path) -> None`:
      - Compute `resolved = (vault_dir / rel_path).resolve()` (resolves symlinks per T-69-V4).
      - Build `pinned_prefixes = tuple((vault_dir / v.rstrip("/")).resolve() for v in merged_profile["graphify_folder_mapping"].values())`.
      - If `not any(resolved == p or p in resolved.parents for p in pinned_prefixes)`: raise ValueError(f"Target path {rel_path!r} is not under any graphify-owned folder. Check graphify_folder_mapping in .graphify/profile.yaml.").
      - Additionally check `user_only_folders`: for each `uof` in `merged_profile.get("user_only_folders", [])`, compute `uof_resolved = (vault_dir / uof.rstrip("/")).resolve()`; if `resolved == uof_resolved or uof_resolved in resolved.parents`: raise ValueError("...user-owned folder {uof!r}...").
    Step 2 — Implement `_preflight_check_user_only_folders(planned_targets: list[tuple[str, str]], merged_profile: dict, vault_dir: Path) -> None`:
      - Iterate ALL planned (bucket_key, rel_path) tuples; collect every violation into a list (do NOT short-circuit).
      - If list non-empty: print to stderr exactly two lines per D-10:
        ```
        [graphify] error: refused {N} write(s) targeting user-owned folders
          hint: violations: {comma-separated list of "rel_path (record_type=bucket_key, violates user_only_folders)"}; edit graphify_folder_mapping in .graphify/profile.yaml to retarget under Atlas/Sources/Graphify/.
        ```
      - Then raise SystemExit(1) (atomic batch refusal per D-09; zero writes have happened because this runs before the write loop).
    Step 3 — Implement `_write_record(vault_dir, rel_path, content, manifest, merged_profile) -> str`:
      - Call `_assert_under_pinned_subtree(rel_path, merged_profile, vault_dir)`.
      - Then call existing `write_note(vault_dir, rel_path, content, manifest)` (manifest-hash guard fires inside write_note at lines 702-732 — UNCHANGED).
    Step 4 — Wire into `promote()`:
      - After `merged_profile` is built and BEFORE any write_note() call, build `planned_targets` list from the classified records and call `_preflight_check_user_only_folders(planned_targets, merged_profile, vault_dir)`.
      - Replace EVERY direct `write_note(vault_dir, rel_path, ...)` call inside promote() with `_write_record(vault_dir, rel_path, ..., merged_profile)`. (Use grep to find them; expected count >= 1 within promote scope.)
      - DO NOT modify lines 702-732 (manifest-hash guard inside write_note).
    Step 5 — Verify ordering: pre-flight runs first; manifest-hash guard inside write_note fires only for within-pinned-subtree collisions (D-11).
  </action>
  <verify>
    <automated>pytest tests/test_vault_promote.py -x -q</automated>
  </verify>
  <acceptance_criteria>
    - All 6 RED tests now PASS
    - `grep -c "def _assert_under_pinned_subtree" graphify/vault_promote.py` returns 1
    - `grep -c "def _preflight_check_user_only_folders" graphify/vault_promote.py` returns 1
    - `grep -c "def _write_record" graphify/vault_promote.py` returns 1
    - `sed -n '702,732p' graphify/vault_promote.py | wc -l` returns 31 (range still occupied; manifest-hash guard preserved structurally)
    - `grep -c "manifest" graphify/vault_promote.py | head -1` (manifest-hash guard logic still references manifest dict — no zero)
    - `grep -cE "^\[graphify\] error: refused" graphify/vault_promote.py` returns >= 1 (literal stderr template present)
    - `grep -c "  hint:" graphify/vault_promote.py` returns >= 1
    - Inside promote(), every write_note call replaced. Verify with: `grep -n "write_note(" graphify/vault_promote.py` — every match must be either (a) the `def write_note(` definition line, (b) inside the body of `def write_note(`, or (c) inside `def _write_record(`. ZERO `write_note(` call sites may exist inside `def promote(` itself. Manual visual check on the grep output is the verification (avoids brittle `awk` range addressing if `promote()` is the last function in the file).
    - `pytest tests/ -q` green
    - Commit: `feat(69-03): pre-flight refusal + chokepoint guard; preserve manifest-hash guard`
  </acceptance_criteria>
  <done>Defense-in-depth landed; manifest-hash guard preserved; all 6 tests + full suite green.</done>
</task>

</tasks>

<verification>
- `pytest tests/test_vault_promote.py -q` green
- `pytest tests/ -q` green
- Manual stderr inspection: trigger a refusal, confirm two-line format renders correctly in a real terminal
- Manual: `sed -n '702,732p' graphify/vault_promote.py` shows manifest-hash guard intact
</verification>

<success_criteria>
- VPROF-03 (refusal half) satisfied: pre-flight refuses; zero partial writes; two-line stderr.
- Phase 69 SC#3 satisfied (refusal pre-flight, no partial writes).
- Phase 69 SC#4 satisfied (manifest-hash guard preserved with regression test).
</success_criteria>

<output>
After completion, create `69-03-SUMMARY.md` summarizing: new function signatures, exact stderr template, ordering invariant proof, list of write_note → _write_record replacement sites.
</output>
