---
phase: 69
plan: 04
type: tdd
wave: 4
depends_on: [02]
files_modified:
  - graphify/vault_promote.py
  - graphify/doctor.py
  - graphify/__main__.py
  - tests/test_vault_promote.py
  - tests/test_doctor.py
autonomous: true
requirements: [VPROF-04]
must_haves:
  truths:
    - "graphify doctor surfaces a non-blocking 'Legacy Artifacts' section listing legacy graphify-shaped notes outside the pinned subtree (D-15)."
    - "graphify update-vault --migrate-legacy is dry-run by default (prints move plan to stdout, zero file moves)."
    - "graphify update-vault --migrate-legacy-apply moves legacy artifacts AND updates manifest paths atomically; rollback on failure (D-13, D-14)."
    - "Detection uses ownership marker `graphifyProject` (per RESEARCH Open Q1) plus the hardcoded glob list per D-12: `_COMM*.md` at vault root, `Community*.md` under `Atlas/Maps/`, files outside pinned subtree carrying `graphifyProject` frontmatter."
  artifacts:
    - path: "graphify/vault_promote.py"
      provides: "_LEGACY_GLOB_PATTERNS constant; detect_legacy_artifacts(); migrate_legacy(vault_path, profile, apply: bool)"
      contains: "def detect_legacy_artifacts"
    - path: "graphify/doctor.py"
      provides: "DoctorReport.legacy_artifact_paths field; format_report Legacy Artifacts section; run_doctor() populates legacy paths"
      contains: "legacy_artifact_paths"
    - path: "graphify/__main__.py"
      provides: "--migrate-legacy and --migrate-legacy-apply flags wired into update-vault subcommand"
      contains: "--migrate-legacy"
  key_links:
    - from: "graphify/__main__.py:update-vault dispatcher"
      to: "graphify.vault_promote.migrate_legacy"
      via: "opts.migrate_legacy_apply or opts.migrate_legacy → call migrate_legacy(..., apply=opts.migrate_legacy_apply)"
      pattern: "migrate_legacy\\("
    - from: "graphify/doctor.py:run_doctor"
      to: "graphify.vault_promote.detect_legacy_artifacts"
      via: "called when vault_path known; populates DoctorReport.legacy_artifact_paths"
      pattern: "detect_legacy_artifacts\\("
---

<objective>
Implement legacy artifact detection (doctor) and migration (`update-vault --migrate-legacy[-apply]`) per D-12..D-15 and RESEARCH Open Q1/Q3 resolutions.

Purpose: Give users a one-shot path to clean up regression-bug debris (community MOCs in Atlas/Maps, etc.) without manual file shuffling.
Output: doctor section + dry-run + apply mode + atomic rollback.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/69-vprof-vault-profile-driven-folder-resolution-user-namespace-guard/69-CONTEXT.md
@.planning/phases/69-vprof-vault-profile-driven-folder-resolution-user-namespace-guard/69-RESEARCH.md
@.planning/phases/69-vprof-vault-profile-driven-folder-resolution-user-namespace-guard/69-PATTERNS.md
@.planning/phases/69-vprof-vault-profile-driven-folder-resolution-user-namespace-guard/69-VALIDATION.md
@.planning/phases/69-vprof-vault-profile-driven-folder-resolution-user-namespace-guard/69-02-SUMMARY.md
@graphify/vault_promote.py
@graphify/doctor.py
@graphify/__main__.py
@tests/test_vault_promote.py
</context>

<interfaces>
From Plan 02:
- `_resolve_folder_prefix(bucket_key, merged_profile) -> str`
- merged_profile structure with `graphify_folder_mapping`

New surface added by this plan:
```python
# In graphify/vault_promote.py
_LEGACY_GLOB_PATTERNS: list[tuple[str, str]] = [
    ("",            "_COMM*.md"),         # vault root
    ("Atlas/Maps",  "Community*.md"),
]
_OWNERSHIP_MARKER_KEY: str = "graphifyProject"  # frontmatter key indicating graphify ownership

def detect_legacy_artifacts(vault_dir: Path, merged_profile: dict) -> list[str]:
    """Return vault-relative paths of legacy graphify-shaped notes outside the pinned subtree."""

def migrate_legacy(vault_dir: Path, merged_profile: dict, manifest_path: Path, apply: bool) -> dict:
    """Dry-run (apply=False): print move plan and return {'planned': [...], 'moved': 0}.
    Apply (apply=True): perform atomic move + manifest update with rollback on failure.
    Returns {'planned': [...], 'moved': N, 'failed': [...]}."""

# In graphify/doctor.py
@dataclass
class DoctorReport:
    # existing fields...
    legacy_artifact_paths: list[str] = field(default_factory=list)
```

CLI flags (per D-13 + RESEARCH Open Q3 — recommend two distinct flags to avoid colliding with existing `--apply`):
- `graphify update-vault --migrate-legacy` → dry-run
- `graphify update-vault --migrate-legacy-apply` → apply
</interfaces>

<tasks>

<task type="tdd" tdd="true">
  <name>Task 1: RED — six failing tests for detection + dry-run + apply + rollback</name>
  <read_first>
    - .planning/phases/69-vprof-vault-profile-driven-folder-resolution-user-namespace-guard/69-VALIDATION.md (rows 69-04-01..06)
    - .planning/phases/69-vprof-vault-profile-driven-folder-resolution-user-namespace-guard/69-CONTEXT.md (D-12..D-15)
    - tests/test_vault_promote.py (existing patterns)
    - graphify/doctor.py (DoctorReport dataclass + format_report)
  </read_first>
  <behavior>
    - test_detect_legacy_comm_at_root: create `vault/_COMM_alpha.md` and `vault/_COMM_beta.md`; call detect_legacy_artifacts(vault, default_profile); result contains both paths.
    - test_detect_legacy_community_maps: create `vault/Atlas/Maps/Community_X.md`; detect_legacy_artifacts must surface it.
    - test_detect_manifest_outside_pinned: create `vault/Notes/Random.md` with frontmatter `---\ngraphifyProject: true\n---\n`; detect_legacy_artifacts must surface it (outside pinned subtree + carries ownership marker).
    - test_migrate_legacy_dry_run: setup as above; call `migrate_legacy(vault, profile, manifest, apply=False)`; result['moved'] == 0; result['planned'] non-empty; original files still present on disk; stdout contains "old_path → new_path" lines.
    - test_migrate_legacy_apply: same setup + minimal manifest dict; call `migrate_legacy(..., apply=True)`; legacy files moved to mapped locations under `Atlas/Sources/Graphify/Maps/`; manifest dict updated with new paths; result['moved'] >= 3.
    - test_migrate_legacy_rollback: monkeypatch the manifest write to raise OSError on a specific file; call apply=True; assert that file's source location is restored (file moved back); result['failed'] contains the offending path.
  </behavior>
  <action>
    Add six tests to tests/test_vault_promote.py per VALIDATION rows 69-04-01..06. Use tmp_path; use `monkeypatch` for the rollback test. All six must FAIL at this commit (functions not implemented).
  </action>
  <verify>
    <automated>pytest tests/test_vault_promote.py::test_detect_legacy_comm_at_root tests/test_vault_promote.py::test_detect_legacy_community_maps tests/test_vault_promote.py::test_detect_manifest_outside_pinned tests/test_vault_promote.py::test_migrate_legacy_dry_run tests/test_vault_promote.py::test_migrate_legacy_apply tests/test_vault_promote.py::test_migrate_legacy_rollback -x 2>&1 | grep -E "FAILED|ERROR" | head -6</automated>
  </verify>
  <acceptance_criteria>
    - 6 new test functions present (grep -c each name returns 1; sum = 6)
    - All 6 fail (RED)
    - Commit: `test(69-04): add failing tests for legacy detection + migration`
  </acceptance_criteria>
  <done>Six failing tests; production untouched.</done>
</task>

<task type="tdd" tdd="true">
  <name>Task 2: GREEN — detect_legacy_artifacts, migrate_legacy with atomic rollback, doctor section, CLI wiring</name>
  <read_first>
    - graphify/vault_promote.py (lines 641-660 _write_atomic; lines 702-732 manifest-hash guard — preserve)
    - graphify/doctor.py (DoctorReport dataclass ~lines 155-175; format_report() ~lines 545-620; run_doctor())
    - graphify/__main__.py (update-vault subcommand argparse block — locate via `grep -n "update-vault\|update_vault" graphify/__main__.py`)
    - .planning/phases/69-vprof-vault-profile-driven-folder-resolution-user-namespace-guard/69-PATTERNS.md (doctor.py + __main__.py + vault_promote.py legacy blocks)
    - .planning/phases/69-vprof-vault-profile-driven-folder-resolution-user-namespace-guard/69-RESEARCH.md (Open Q1 ownership marker = `graphifyProject`; Open Q3 flag naming)
  </read_first>
  <action>
    Step 1 — In `graphify/vault_promote.py` add module-level constants:
    ```python
    _LEGACY_GLOB_PATTERNS: list[tuple[str, str]] = [
        ("", "_COMM*.md"),
        ("Atlas/Maps", "Community*.md"),
    ]
    _OWNERSHIP_MARKER_KEY: str = "graphifyProject"
    ```
    Step 2 — Implement `detect_legacy_artifacts(vault_dir: Path, merged_profile: dict) -> list[str]`:
      - Walk hardcoded globs; collect matches as vault-relative posix paths.
      - Additionally walk vault tree (excluding pinned subtree directories from `merged_profile['graphify_folder_mapping'].values()` and `.graphify/`); for each `*.md` file, parse frontmatter (use existing helper if any, else simple `--- ... ---` regex) and if `_OWNERSHIP_MARKER_KEY` key present and truthy, add path.
      - Deduplicate; return sorted list.
    Step 3 — Implement `migrate_legacy(vault_dir: Path, merged_profile: dict, manifest_path: Path, apply: bool) -> dict`:
      - Call detect_legacy_artifacts. For each legacy path, compute `new_rel_path` by mapping its bucket-of-origin to the profile-mapped folder (e.g., `_COMM*.md` and `Community*.md` → bucket `maps` → `_resolve_folder_prefix("maps", merged_profile)`; outside-pinned manifest-tagged files → preserve filename, place under the bucket inferred from frontmatter `type` if present, else under fallback `Atlas/Sources/Graphify/Misc/`).
      - Build planned list `[{"old": str, "new": str}, ...]`.
      - If apply=False: print each `f"{old} → {new}"` to stdout; return `{"planned": planned, "moved": 0, "failed": []}`.
      - If apply=True: load manifest via existing helper (or `json.loads(manifest_path.read_text())` if manifest is JSON; consult vault_promote.py for the existing manifest I/O pattern). For each (old, new):
        - Save `manifest_snapshot = dict(manifest)` BEFORE the move.
        - `shutil.move(vault_dir/old, vault_dir/new)` (creating parent dirs).
        - Update manifest entry: replace any reference to `old` with `new` (search by value or key as appropriate to existing manifest schema).
        - Try to atomic-write manifest via `_write_atomic(manifest_path, json.dumps(manifest, indent=2))`.
        - On any OSError during manifest write: rollback — `shutil.move(vault_dir/new, vault_dir/old)`, restore `manifest = manifest_snapshot`, append `old` to `failed` list, continue.
      - Return `{"planned": planned, "moved": len(planned) - len(failed), "failed": failed}`.
    Step 4 — In `graphify/doctor.py`:
      - Add `legacy_artifact_paths: list[str] = field(default_factory=list)` to `DoctorReport`.
      - In `run_doctor()`, after report.vault_path is set: `if report.vault_path and report.vault_path.is_dir(): from graphify.vault_promote import detect_legacy_artifacts; report.legacy_artifact_paths = detect_legacy_artifacts(report.vault_path, merged_profile)` (load merged_profile via existing profile loader).
      - In `format_report()`, append section per PATTERNS.md doctor.py block: `[graphify] === Legacy Artifacts ===` header, list up to 5 paths + overflow line, FIX hint pointing to `graphify update-vault --migrate-legacy-apply`. Section is non-blocking (does NOT influence exit code per D-15).
    Step 5 — In `graphify/__main__.py`, locate the `update-vault` subcommand argparse parser (e.g., `_p_uv` or via grep) and add two flags:
    ```python
    _p_uv.add_argument("--migrate-legacy", action="store_true",
                       help="Detect legacy graphify artifacts and print a dry-run move plan; performs no file changes.")
    _p_uv.add_argument("--migrate-legacy-apply", action="store_true",
                       help="Apply the migrate-legacy move plan (moves files + updates manifest atomically).")
    ```
    Add dispatch (before normal update-vault flow): if either flag set, load profile + manifest + call `migrate_legacy(vault_path, merged_profile, manifest_path, apply=opts.migrate_legacy_apply)`. Print `[graphify] migrate-legacy: planned {N}, moved {M}, failed {F}`. Exit 0 on success, 1 if `failed` non-empty.
    Use type hints, `from __future__ import annotations`, no new dependencies beyond stdlib (`shutil`, `json`, `re`, `pathlib`).
  </action>
  <verify>
    <automated>pytest tests/test_vault_promote.py tests/test_doctor.py -x -q</automated>
  </verify>
  <acceptance_criteria>
    - All 6 RED tests now PASS
    - `grep -c "def detect_legacy_artifacts" graphify/vault_promote.py` returns 1
    - `grep -c "def migrate_legacy" graphify/vault_promote.py` returns 1
    - `grep -c "_LEGACY_GLOB_PATTERNS" graphify/vault_promote.py` returns >= 2
    - `grep -c "_OWNERSHIP_MARKER_KEY\|graphifyProject" graphify/vault_promote.py` returns >= 2
    - `grep -c "legacy_artifact_paths" graphify/doctor.py` returns >= 2
    - `grep -c "Legacy Artifacts" graphify/doctor.py` returns >= 1
    - `grep -c "\\-\\-migrate-legacy" graphify/__main__.py` returns >= 2
    - `grep -c "\\-\\-migrate-legacy-apply" graphify/__main__.py` returns >= 1
    - `pytest tests/ -q` green (no regressions across full suite)
    - `graphify update-vault --help 2>&1 | grep -c "migrate-legacy"` returns >= 2
    - Commit: `feat(69-04): legacy artifact detection in doctor; --migrate-legacy[-apply] in update-vault`
  </acceptance_criteria>
  <done>Detection in doctor; dry-run + apply + atomic rollback in update-vault; full suite green.</done>
</task>

</tasks>

<verification>
- `pytest tests/ -q` green
- `graphify doctor` against a tmp vault containing `_COMM_*.md` produces a Legacy Artifacts section
- `graphify update-vault --migrate-legacy` prints plan, performs zero moves
- `graphify update-vault --migrate-legacy-apply` performs moves and updates manifest
- Manifest-hash guard at vault_promote.py:702-732 confirmed unchanged (`sed -n '702,732p'` matches pre-Plan-04 contents)
</verification>

<success_criteria>
- VPROF-04 satisfied: detection in doctor (D-15), `--migrate-legacy` dry-run default (D-13), `--migrate-legacy-apply` performs atomic move + manifest update with rollback (D-14).
- Phase 69 SC#6 satisfied (doctor reports legacy artifacts; update-vault relocates them).
- Manifest-hash guard preserved (no modifications to lines 702-732 in this plan either).
</success_criteria>

<output>
After completion, create `69-04-SUMMARY.md` summarizing: detection patterns + ownership marker, migrate_legacy contract (dry-run vs apply), rollback semantics, CLI flag names + help text, doctor section format.
</output>
