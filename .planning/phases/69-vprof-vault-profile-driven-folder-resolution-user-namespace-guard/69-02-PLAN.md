---
phase: 69
plan: 02
type: tdd
wave: 2
depends_on: [01]
files_modified:
  - graphify/vault_promote.py
  - tests/test_vault_promote.py
autonomous: true
requirements: [VPROF-02]
must_haves:
  truths:
    - "Every record-type write in promote() resolves its folder via _resolve_folder_prefix(bucket_key, merged_profile) — never via a hardcoded literal."
    - "No community MOC notes land in Atlas/Maps/ when the default profile is in use; all map-bucket writes go to Atlas/Sources/Graphify/Maps/."
    - "Unknown record types fall back to Atlas/Sources/Graphify/<Type>/ with a one-line INFO breadcrumb (D-04)."
  artifacts:
    - path: "graphify/vault_promote.py"
      provides: "_resolve_folder_prefix(); _FOLDER_PATH_PREFIX dict removed; classify_nodes() folder literals removed; promote() consults profile mapping"
      contains: "_resolve_folder_prefix"
    - path: "tests/test_vault_promote.py"
      provides: "Profile-driven routing tests + negative grep test for hardcoded literals"
      contains: "test_profile_folder_routing"
  key_links:
    - from: "graphify/vault_promote.py:promote"
      to: "_resolve_folder_prefix"
      via: "called per bucket_key with merged profile"
      pattern: "_resolve_folder_prefix\\("
    - from: "tests/test_vault_promote.py:test_end_to_end_all_seven_folders"
      to: "Atlas/Sources/Graphify/<Type>/ paths"
      via: "asserts every promoted note lives under the new pinned subtree"
      pattern: "Atlas/Sources/Graphify"
---

<objective>
Replace the hardcoded `_FOLDER_PATH_PREFIX` dict (vault_promote.py:872-880) and the per-record `folder = "Atlas/..."` literals inside `classify_nodes()` (lines 195-300) with profile-driven resolution via `_resolve_folder_prefix(bucket_key, merged_profile)`.

Purpose: Fix the regression-bug epicenter — community MOC notes leaking into user-curated `Atlas/Maps/` instead of `Atlas/Sources/Graphify/Maps/`.
Output: Refactored vault_promote.py + updated tests; full backward compatibility via _DEFAULT_PROFILE fallback.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/69-vprof-vault-profile-driven-folder-resolution-user-namespace-guard/69-CONTEXT.md
@.planning/phases/69-vprof-vault-profile-driven-folder-resolution-user-namespace-guard/69-RESEARCH.md
@.planning/phases/69-vprof-vault-profile-driven-folder-resolution-user-namespace-guard/69-PATTERNS.md
@.planning/phases/69-vprof-vault-profile-driven-folder-resolution-user-namespace-guard/69-VALIDATION.md
@.planning/phases/69-vprof-vault-profile-driven-folder-resolution-user-namespace-guard/69-01-SUMMARY.md
@graphify/vault_promote.py
@graphify/profile.py
@tests/test_vault_promote.py
</context>

<interfaces>
From graphify/profile.py (created by Plan 01):
```python
_DEFAULT_PROFILE["graphify_folder_mapping"]: dict[str, str]  # singular keys → vault-relative folder string ending with "/"
# Keys: "thing", "question", "map", "person", "quote", "statement", "source"
```

Existing vault_promote.py symbols to be modified:
- `classify_nodes(G, communities, profile, threshold)` at line 153 — currently sets `"folder"` field per record from hardcoded strings
- `promote(...)` at line 893 — currently iterates `_FOLDER_PATH_PREFIX` dict
- `_FOLDER_PATH_PREFIX` dict at line 872 — to be DELETED
- `write_note(vault_dir, rel_path, content, manifest)` at line 702 — unchanged (chokepoint extension is Plan 03)

Bucket-key translation (internal plural → singular config key):
```python
_BUCKET_TO_PROFILE_KEY = {
    "things": "thing", "questions": "question", "maps": "map",
    "people": "person", "quotes": "quote", "statements": "statement", "sources": "source",
}
```
</interfaces>

<tasks>

<task type="tdd" tdd="true">
  <name>Task 1: RED — failing tests for profile-driven routing + negative literal grep</name>
  <read_first>
    - tests/test_vault_promote.py (existing _make_graph_with_nodes, _make_communities helpers; test_end_to_end_all_seven_folders pattern)
    - .planning/phases/69-vprof-vault-profile-driven-folder-resolution-user-namespace-guard/69-VALIDATION.md (rows 69-02-01..04)
    - .planning/phases/69-vprof-vault-profile-driven-folder-resolution-user-namespace-guard/69-PATTERNS.md (test_vault_promote.py section)
  </read_first>
  <behavior>
    - test_profile_folder_routing: build a graph with one Thing community; pass a profile with `graphify_folder_mapping.thing = "MyVault/Custom/Things/"`; call classify_nodes → assert every result["things"] entry's `folder` field starts with "MyVault/Custom/Things".
    - test_unknown_type_fallback: pass a profile mapping that omits one key; classify_nodes for that bucket must produce `folder` starting with "Atlas/Sources/Graphify/<Type>/" (capitalized first letter, plural form per D-04). One stderr line emitted matching `[graphify] profile: unknown record type`.
    - test_end_to_end_all_seven_folders: UPDATE existing test to write a default profile to tmp vault, run full promote(), assert every produced rel_path begins with one of the 7 `Atlas/Sources/Graphify/<Type>/` prefixes (per D-01).
    - test_no_hardcoded_atlas_literals: scan `graphify/vault_promote.py` source as text; assert `_FOLDER_PATH_PREFIX` symbol not present AND no string literal matching `r'"Atlas/Maps"|"Atlas/Dots/'` outside comments. Use:
      ```python
      src = Path("graphify/vault_promote.py").read_text()
      # strip lines starting with # to avoid commenting-out evasion
      code = "\n".join(line for line in src.splitlines() if not line.lstrip().startswith("#"))
      assert "_FOLDER_PATH_PREFIX" not in code
      assert '"Atlas/Maps"' not in code
      assert '"Atlas/Dots/' not in code
      ```
  </behavior>
  <action>
    Add the four tests to tests/test_vault_promote.py. The existing `test_end_to_end_all_seven_folders` is RENAMED in place (kept name, updated assertions) — see PATTERNS.md "End-to-end test update pattern". All four tests must fail at this commit (literals still in source; _resolve_folder_prefix not defined).
  </action>
  <verify>
    <automated>pytest tests/test_vault_promote.py::test_profile_folder_routing tests/test_vault_promote.py::test_unknown_type_fallback tests/test_vault_promote.py::test_no_hardcoded_atlas_literals -x 2>&1 | grep -E "FAILED|ERROR" | head -3</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "def test_profile_folder_routing" tests/test_vault_promote.py` returns 1
    - `grep -c "def test_unknown_type_fallback" tests/test_vault_promote.py` returns 1
    - `grep -c "def test_no_hardcoded_atlas_literals" tests/test_vault_promote.py` returns 1
    - `grep -c "Atlas/Sources/Graphify" tests/test_vault_promote.py` returns >= 3
    - All four tests fail (RED)
    - Commit message: `test(69-02): add failing tests for profile-driven folder resolution`
  </acceptance_criteria>
  <done>Four failing tests exist; production code untouched.</done>
</task>

<task type="tdd" tdd="true">
  <name>Task 2: GREEN — implement _resolve_folder_prefix + remove all hardcoded literals</name>
  <read_first>
    - graphify/vault_promote.py (lines 153-300 classify_nodes, 702-732 manifest-hash guard, 872-880 _FOLDER_PATH_PREFIX, 893-980 promote)
    - graphify/profile.py (_DEFAULT_PROFILE.graphify_folder_mapping from Plan 01)
    - .planning/phases/69-vprof-vault-profile-driven-folder-resolution-user-namespace-guard/69-PATTERNS.md (vault_promote.py section)
    - .planning/phases/69-vprof-vault-profile-driven-folder-resolution-user-namespace-guard/69-CONTEXT.md (D-01..D-04)
  </read_first>
  <action>
    Step 1 — Add module-level `_BUCKET_TO_PROFILE_KEY` dict mapping internal plural bucket keys to singular profile keys (see <interfaces> above).
    Step 2 — Add `_resolve_folder_prefix(bucket_key: str, merged_profile: dict) -> str`:
      - Translate bucket_key (plural) → profile_key (singular) via `_BUCKET_TO_PROFILE_KEY`.
      - Read `merged_profile.get("graphify_folder_mapping") or {}`.
      - If profile_key in mapping: return `mapping[profile_key].rstrip("/")`.
      - Else if profile_key in `_DEFAULT_PROFILE["graphify_folder_mapping"]`: return that default rstripped.
      - Else (truly unknown record type per D-04): emit `print(f"[graphify] profile: unknown record type {bucket_key!r}; using fallback Atlas/Sources/Graphify/{bucket_key.capitalize()}/", file=sys.stderr)` and return `f"Atlas/Sources/Graphify/{bucket_key.capitalize()}"`.
    Step 3 — DELETE the entire `_FOLDER_PATH_PREFIX` dict block at lines 872-880.
    Step 4 — In `classify_nodes()` (lines 195-300), remove every hardcoded `"folder": "Atlas/..."` literal. Replace with `"folder": _resolve_folder_prefix(<bucket_plural>, profile)` for each of the 7 record types (things, questions, maps, people, quotes, statements, sources). The `profile` parameter is already available — confirm it threads through and pass it in if missing.
    Step 5 — In `promote()` at line 949, replace `for k in _FOLDER_PATH_PREFIX` with `for k in _BUCKET_TO_PROFILE_KEY`. At line 958, replace `prefix = _FOLDER_PATH_PREFIX[bucket_key]` with `prefix = _resolve_folder_prefix(bucket_key, merged_profile)`.
    Step 6 — Ensure `merged_profile` is built early in `promote()` via `_deep_merge(_DEFAULT_PROFILE, user_profile_dict)` (already present — verify path).
    Use `from __future__ import annotations`, type hints, no new dependencies.
  </action>
  <verify>
    <automated>pytest tests/test_vault_promote.py::test_profile_folder_routing tests/test_vault_promote.py::test_unknown_type_fallback tests/test_vault_promote.py::test_end_to_end_all_seven_folders tests/test_vault_promote.py::test_no_hardcoded_atlas_literals -x</automated>
  </verify>
  <acceptance_criteria>
    - All 4 RED tests now PASS
    - `grep -c "_FOLDER_PATH_PREFIX" graphify/vault_promote.py` returns 0
    - `grep -v '^[[:space:]]*#' graphify/vault_promote.py | grep -c '"Atlas/Maps"'` returns 0
    - `grep -v '^[[:space:]]*#' graphify/vault_promote.py | grep -c '"Atlas/Dots/'` returns 0
    - `grep -c "def _resolve_folder_prefix" graphify/vault_promote.py` returns 1
    - `grep -c "_BUCKET_TO_PROFILE_KEY" graphify/vault_promote.py` returns >= 2
    - `pytest tests/test_vault_promote.py -q` passes (full module green)
    - `pytest tests/ -q` passes (no cross-module regressions)
    - Commit: `feat(69-02): profile-driven folder resolution; remove hardcoded Atlas literals`
  </acceptance_criteria>
  <done>All literals gone, profile drives folders, full suite green.</done>
</task>

</tasks>

<verification>
- `pytest tests/test_vault_promote.py -q` green
- `pytest tests/ -q` green
- Negative grep for literals returns zero matches
- Manual: run `graphify update-vault` against a tmp vault with default profile; verify all 7 folders appear under `Atlas/Sources/Graphify/`.
</verification>

<success_criteria>
- VPROF-02 success criteria #1 and #2 satisfied (no MOCs in Atlas/Maps; literals removed).
- Backward compat preserved: profiles without graphify_folder_mapping fall back to _DEFAULT_PROFILE values.
</success_criteria>

<output>
After completion, create `69-02-SUMMARY.md` summarizing: removed symbols, added function, bucket-key translation table, list of touched line ranges in classify_nodes/promote.
</output>
