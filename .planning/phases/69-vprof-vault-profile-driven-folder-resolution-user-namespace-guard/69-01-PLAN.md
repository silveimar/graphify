---
phase: 69
plan: 01
type: tdd
wave: 1
depends_on: []
files_modified:
  - graphify/profile.py
  - tests/test_profile.py
autonomous: true
requirements: [VPROF-01]
must_haves:
  truths:
    - "On first read of a v1 profile.yaml, graphify silently rewrites it to v2 (folder_mapping → graphify_folder_mapping) and writes a single profile.yaml.bak."
    - "Re-running the migrator on a v2 profile is a no-op (idempotent); .bak is not rewritten."
    - "_DEFAULT_PROFILE exposes graphify_folder_mapping with the 7 singular-noun keys per D-01, plus user_only_folders, augment.allow_community=False, and an empty reverse_sync placeholder."
  artifacts:
    - path: "graphify/profile.py"
      provides: "_DEFAULT_PROFILE schema v2 keys + migrate_profile_v1_to_v2() + load_profile() wiring"
      contains: "graphify_folder_mapping"
    - path: "tests/test_profile.py"
      provides: "Migrator tests (renames key, idempotent, .bak written)"
      contains: "test_migrator_renames_key"
  key_links:
    - from: "graphify/profile.py:load_profile"
      to: "migrate_profile_v1_to_v2"
      via: "call immediately after YAML safe_load when folder_mapping detected"
      pattern: "migrate_profile_v1_to_v2\\("
---

<objective>
Land profile schema v2 (per D-01..D-07) and the silent in-place v1→v2 migrator. This is the foundation for Plan 02 (folder resolution) and Plan 04 (legacy detection).

Purpose: Establish profile.graphify_folder_mapping as the single source of truth for vault write folders, without breaking existing user profiles.
Output: Updated graphify/profile.py + tests/test_profile.py. No CLI surface change.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/69-vprof-vault-profile-driven-folder-resolution-user-namespace-guard/69-CONTEXT.md
@.planning/phases/69-vprof-vault-profile-driven-folder-resolution-user-namespace-guard/69-RESEARCH.md
@.planning/phases/69-vprof-vault-profile-driven-folder-resolution-user-namespace-guard/69-PATTERNS.md
@.planning/phases/69-vprof-vault-profile-driven-folder-resolution-user-namespace-guard/69-VALIDATION.md
@graphify/profile.py
@tests/test_profile.py
</context>

<tasks>

<task type="tdd" tdd="true">
  <name>Task 1: RED — write failing migrator tests</name>
  <read_first>
    - tests/test_profile.py (existing patterns, _write_profile helper, importorskip("yaml") guard)
    - .planning/phases/69-vprof-vault-profile-driven-folder-resolution-user-namespace-guard/69-VALIDATION.md (rows 69-01-01..03)
    - .planning/phases/69-vprof-vault-profile-driven-folder-resolution-user-namespace-guard/69-PATTERNS.md (test_profile.py pattern section)
  </read_first>
  <behavior>
    - test_migrator_renames_key: writing a v1 profile with top-level `folder_mapping: {...}` then calling `migrate_profile_v1_to_v2(profile_path, vault_dir)` rewrites profile.yaml so the top-level key is `graphify_folder_mapping` with the same nested values; original `folder_mapping` key absent.
    - test_migrator_idempotent: calling migrator twice — first returns "migrated", second returns "already_v2"; .bak mtime unchanged after second call.
    - test_migrator_writes_bak: after migration, `.graphify/profile.yaml.bak` exists and its bytes equal the pre-migration profile.yaml bytes.
    - All tests use `pytest.importorskip("yaml")` and only touch tmp_path.
  </behavior>
  <action>
    Add three new test functions to tests/test_profile.py per D-05/D-06 (Plan PATTERNS test_profile.py section). Use the `_write_profile(tmp_path, content)` helper pattern shown in PATTERNS.md. Import `migrate_profile_v1_to_v2` from `graphify.profile` (does not exist yet — import will fail, that is the RED). Tests must be runnable with `pytest tests/test_profile.py::test_migrator_renames_key tests/test_profile.py::test_migrator_idempotent tests/test_profile.py::test_migrator_writes_bak -x` and ALL THREE must fail/error (ImportError acceptable for RED). Per D-10 stderr format and D-06 single-overwriting-.bak rule.
  </action>
  <verify>
    <automated>pytest tests/test_profile.py::test_migrator_renames_key tests/test_profile.py::test_migrator_idempotent tests/test_profile.py::test_migrator_writes_bak -x 2>&1 | grep -E "ImportError|FAILED|ERROR" | head -3</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "def test_migrator_renames_key" tests/test_profile.py` returns 1
    - `grep -c "def test_migrator_idempotent" tests/test_profile.py` returns 1
    - `grep -c "def test_migrator_writes_bak" tests/test_profile.py` returns 1
    - `grep -c "migrate_profile_v1_to_v2" tests/test_profile.py` returns >= 3
    - Running the three tests produces ImportError or FAILED (RED gate)
    - Commit message: `test(69-01): add failing migrator tests`
  </acceptance_criteria>
  <done>Three tests exist, all fail because migrate_profile_v1_to_v2 is not yet defined.</done>
</task>

<task type="tdd" tdd="true">
  <name>Task 2: GREEN — implement migrator + schema v2 keys + load_profile wiring</name>
  <read_first>
    - graphify/profile.py (lines 1-180 for _DEFAULT_PROFILE and _VALID_TOP_LEVEL_KEYS, lines 1480-1494 for validate_vault_path, lines 1547-1557 for atomic YAML write)
    - .planning/phases/69-vprof-vault-profile-driven-folder-resolution-user-namespace-guard/69-PATTERNS.md (profile.py section for exact code shapes)
    - .planning/phases/69-vprof-vault-profile-driven-folder-resolution-user-namespace-guard/69-CONTEXT.md (D-01..D-07)
  </read_first>
  <action>
    Step 1 — Extend `_DEFAULT_PROFILE` in graphify/profile.py with these exact v2 keys (per D-01, singular noun keys):
    ```python
    "graphify_folder_mapping": {
        "thing":     "Atlas/Sources/Graphify/Things/",
        "question":  "Atlas/Sources/Graphify/Questions/",
        "map":       "Atlas/Sources/Graphify/Maps/",
        "person":    "Atlas/Sources/Graphify/People/",
        "quote":     "Atlas/Sources/Graphify/Quotes/",
        "statement": "Atlas/Sources/Graphify/Statements/",
        "source":    "Atlas/Sources/Graphify/Sources/",
    },
    "user_only_folders": [],
    "augment": {"allow_community": False},
    "reverse_sync": {},
    ```
    Step 2 — Add the four new keys to `_VALID_TOP_LEVEL_KEYS`: `graphify_folder_mapping`, `user_only_folders`, `augment`, `reverse_sync`.
    Step 3 — Implement `migrate_profile_v1_to_v2(profile_path: Path, vault_dir: Path) -> str`:
      - If profile_path does not exist: return "skipped_no_yaml".
      - Load YAML via `yaml.safe_load`.
      - If `graphify_folder_mapping` already present in dict: return "already_v2" (no .bak write, no rewrite).
      - Else if `folder_mapping` present (a real migration is about to occur): write `.graphify/profile.yaml.bak` with the original pre-migration bytes. If `.bak` already exists from a prior migration, OVERWRITE it — the most recent pre-migration state wins (per D-06: single overwriting `.bak`, no `.bak.bak` accumulation). Then rename key to `graphify_folder_mapping` preserving values. Atomic-write back via `.tmp` + `os.replace` per PATTERNS atomic write block. Emit one-line stderr `[graphify] profile: migrated folder_mapping → graphify_folder_mapping (backup at .graphify/profile.yaml.bak)`. Return "migrated".
      - **Critical .bak invariant:** the `.bak` write happens ONLY in this `folder_mapping`-detected branch (when an actual migration occurs). Do NOT touch `.bak` when returning `"already_v2"` or `"skipped_no_yaml"` — idempotent re-invocation must leave `.bak` mtime unchanged. This is what `test_migrator_idempotent` verifies.
      - Else (neither key present): return "already_v2" (no rewrite — nothing to migrate).
    Step 4 — Wire into `load_profile()`: immediately after YAML safe_load, if the loaded dict contains the legacy `folder_mapping` key without `graphify_folder_mapping`, call `migrate_profile_v1_to_v2(profile_path, vault_dir)` and re-load.
    Use `from __future__ import annotations`, `str | None` typing, type hints on all functions.
  </action>
  <verify>
    <automated>pytest tests/test_profile.py::test_migrator_renames_key tests/test_profile.py::test_migrator_idempotent tests/test_profile.py::test_migrator_writes_bak -x</automated>
  </verify>
  <acceptance_criteria>
    - All three RED tests now PASS (GREEN gate)
    - `grep -c "def migrate_profile_v1_to_v2" graphify/profile.py` returns 1
    - `grep -c "graphify_folder_mapping" graphify/profile.py` returns >= 3
    - `grep -c "Atlas/Sources/Graphify/Maps/" graphify/profile.py` returns >= 1
    - `grep -c "user_only_folders" graphify/profile.py` returns >= 2
    - `grep -c "augment" graphify/profile.py` returns >= 2
    - `pytest tests/test_profile.py -q` passes (no regressions)
    - Commit message: `feat(69-01): implement profile schema v2 + silent v1→v2 migrator`
  </acceptance_criteria>
  <done>Migrator implemented, RED tests now GREEN, full test_profile.py suite passes.</done>
</task>

</tasks>

<verification>
- `pytest tests/test_profile.py -q` — full profile test module passes
- `pytest tests/ -q` — no regressions across full suite
- Manual: hand-edit a profile.yaml with `folder_mapping:` block; load via `python -c "from graphify.profile import load_profile; load_profile(Path('.graphify/profile.yaml'), Path('.'))"`; confirm in-place rewrite + .bak appears.
</verification>

<success_criteria>
- Schema v2 keys live in `_DEFAULT_PROFILE` with D-01 defaults exactly.
- Migrator is silent (single stderr breadcrumb), idempotent, writes one overwriting .bak.
- VPROF-01 success criterion #5 (silent upgrade, second invocation is no-op) is satisfied.
</success_criteria>

<output>
After completion, create `.planning/phases/69-vprof-vault-profile-driven-folder-resolution-user-namespace-guard/69-01-SUMMARY.md` summarizing: schema v2 key list, migrator function signature, .bak semantics, test count.
</output>
