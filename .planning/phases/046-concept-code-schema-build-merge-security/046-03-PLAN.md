---
phase: 46
plan: "03"
type: execute
wave: 2
depends_on:
  - "01"
  - "02"
files_modified:
  - graphify/report.py
autonomous: true
requirements:
  - CCODE-05
must_haves:
  truths:
    - "Surprising connections and ambiguous-edge report lines sanitize `relation` via `sanitize_label_md` before markdown (D-46.05/CCODE-05 alignment)"
---

<objective>
Markdown injection defense for relation tokens surfaced in GRAPH_REPORT.md (**CCODE-05**).
</objective>

<threat_model>
| Threat | Mitigation |
|--------|------------|
| Markdown/script injection via malicious relation label | `sanitize_label_md` strips control/HTML-ish payloads |
</threat_model>

<tasks>

<task type="execute">
  <name>46-03-01 — report.py relation sanitization</name>
  <read_first>
    - graphify/report.py
    - graphify/security.py (`sanitize_label_md`)
  </read_first>
  <acceptance_criteria>
    - `pytest tests/test_report.py -q` exits 0
    - `grep -n sanitize_label_md graphify/report.py` matches lines for surprise + ambiguous sections
  </acceptance_criteria>
  <action>
    Import `sanitize_label_md`; wrap relation display strings in surprising connections and ambiguous edge sections; keep semantic tag check on raw relation for `semantically_similar_to`.
  </action>
</task>

</tasks>
