---
phase: 46
plan: "02"
type: execute
wave: 1
depends_on: []
files_modified:
  - graphify/build.py
  - tests/test_concept_code_edges.py
autonomous: true
requirements:
  - CCODE-02
must_haves:
  truths:
    - "`implemented_by` normalizes to `implements` oriented code→concept when file_types permit (D-46.01–02)"
    - "Duplicate directed triples merge attributes; confidence ladder beats raw score (D-46.08–09)"
    - "`graph.json` round-trip preserves `implements` + scores (fixture test)"
---

<objective>
Deterministic merge for concept↔code edges before NetworkX assembly (**CCODE-02**, **D-46.08–10**).
</objective>

<threat_model>
| Threat | Mitigation |
|--------|------------|
| Merge drops evidence | Concatenate `source_file` / `source_location`; bump weight sum |
</threat_model>

<tasks>

<task type="execute">
  <name>46-02-01 — build.py normalization + tests</name>
  <read_first>
    - graphify/build.py
    - graphify/validate.py
    - tests/test_confidence.py
  </read_first>
  <acceptance_criteria>
    - `pytest tests/test_concept_code_edges.py -q` exits 0
    - `pytest tests/test_confidence.py -q` exits 0
  </acceptance_criteria>
  <action>
    Shallow-copy extraction in `build_from_json`; run `_normalize_concept_code_edges` before `validate_extraction`: flip `implemented_by`, orient `implements` using `file_type == code`, directed-key merge, then frozenset-bucket merge for reverse `implements` pairs. Use tuple `(confidence_rank, score)` merge policy. Add `tests/test_concept_code_edges.py`.
  </action>
</task>

</tasks>
