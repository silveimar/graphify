---
phase: 46
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified:
  - graphify/validate.py
  - docs/RELATIONS.md
  - .planning/codebase/ARCHITECTURE.md
autonomous: true
requirements:
  - CCODE-01
must_haves:
  truths:
    - "`KNOWN_EDGE_RELATIONS` and `KNOWN_HYPEREDGE_RELATIONS` exist in validate.py"
    - "Unknown edge/hyperedge relation emits `[graphify]` stderr pointing to docs/RELATIONS.md (D-46.11)"
    - "`docs/RELATIONS.md` documents implements / implemented_by and structural relations (D-46.13)"
---

<objective>
Schema acceptance + registry docs: relation warnings (**CCODE-01**, **D-46.11–13**) without breaking legacy extractions.
</objective>

<threat_model>
| Threat | Mitigation |
|--------|------------|
| Validation DoS via huge relation strings | No path — relation is a short label; same as today |
</threat_model>

<tasks>

<task type="execute">
  <name>46-01-01 — validate.py registries + warn_unknown_relations</name>
  <read_first>
    - graphify/validate.py
    - .planning/phases/046-concept-code-schema-build-merge-security/046-CONTEXT.md
  </read_first>
  <acceptance_criteria>
    - `grep -n KNOWN_EDGE_RELATIONS graphify/validate.py` matches
    - `pytest tests/test_concept_code_edges.py::test_unknown_edge_relation_warns_stderr -q` exits 0
    - `test -f docs/RELATIONS.md`
  </acceptance_criteria>
  <action>
    Implement `warn_unknown_relations()` called from end of `validate_extraction()`; add frozensets covering extract/analyze/MCP/test vocabulary; add module pointer to docs/RELATIONS.md.
  </action>
</task>

</tasks>
