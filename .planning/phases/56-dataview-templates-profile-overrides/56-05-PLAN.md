---
phase: 56
plan: 05
type: tdd
wave: 5
depends_on: [56-02, 56-03, 56-04]
files_modified:
  - graphify/templates.py
  - graphify/mapping.py
  - tests/test_template_overrides.py
autonomous: true
requirements: [CFG-01]
must_haves:
  truths:
    - "_resolve_note_template consults the three override lists in D-56.05 ladder order: mapping_rule_templates > community_templates > note_type_templates > base"
    - "ClassificationContext gains optional rule_id: str field; mapping.classify() populates it when matched rule has id:"
    - "Warn-and-fall-back semantics (D-56.13) preserved per-list: missing/unreadable/invalid override file → stderr warn naming the list + base template renders"
    - "_load_override_template extended with list_name: str = 'community_templates' kwarg so warn messages identify which list's override failed (no duplicated fall-back code)"
    - "Existing _pick_community_template behavior unchanged for callers that have not migrated"
    - "All Phase 30 community_templates: render-time tests stay GREEN"
  artifacts:
    - path: "graphify/templates.py"
      provides: "_resolve_note_template ladder + _load_override_template list_name extension + ClassificationContext.rule_id field + 2 call-site rewires (render_note + _render_moc_like)"
      contains: "_resolve_note_template"
    - path: "graphify/mapping.py"
      provides: "classify() per_node[node_id] = ClassificationContext(...) populates rule_id when matched rule has id:"
    - path: "tests/test_template_overrides.py"
      provides: "Ladder precedence tests (4 levels) + 3 warn-fallback tests (one per override list) + integration test composing all three lists"
  key_links:
    - from: "graphify/templates.py:_resolve_note_template"
      to: "graphify/templates.py:_load_override_template"
      via: "delegate per-list with list_name kwarg"
      pattern: "list_name=\"mapping_rule_templates\""
    - from: "graphify/mapping.py:classify"
      to: "graphify/templates.py:ClassificationContext.rule_id"
      via: "per_node ctx population at line ~408"
      pattern: "rule_id=rule\\.get\\(\"id\"\\)"
    - from: "graphify/templates.py:render_note"
      to: "graphify/templates.py:_resolve_note_template"
      via: "wraps templates[note_type] direct lookup"
      pattern: "_resolve_note_template\\(rule_id="
    - from: "graphify/templates.py:_render_moc_like"
      to: "graphify/templates.py:_resolve_note_template"
      via: "replaces _pick_community_template direct call"
      pattern: "_resolve_note_template\\(.*community_id="
---

<objective>
Wire the CFG-01 render-time half: refactor `_pick_community_template` (templates.py:1572-1610) into a single `_resolve_note_template(...)` consulting all three override lists in D-56.05 ladder order, extend `ClassificationContext` (templates.py:109-126) with optional `rule_id: str`, populate it in `mapping.classify()` (mapping.py:408), and rewire the two call sites (`render_note` at templates.py:1487-1494, `_render_moc_like` at templates.py:1748-1750). Per Phase 55 D-55.14 contract preserved: warn-and-fall-back on per-list override failures.

Purpose: This plan completes CFG-01 by connecting the schema additions from Plan 03 (`mapping_rule_templates:`, `note_type_templates:`, `mapping_rules.id:`) to the render path. Lands after both Plan 02 (collision detection — proves the schema is collision-clean) and Plan 03 (the schema validators themselves) so the render code can trust the validated profile shapes. Plan 06 (docs) runs parallel to this since it touches no code.
Output: `_resolve_note_template` ladder + `ClassificationContext.rule_id` + `_load_override_template` list_name extension + ladder/warn-fallback/integration tests.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/56-dataview-templates-profile-overrides/56-CONTEXT.md
@.planning/phases/56-dataview-templates-profile-overrides/56-RESEARCH.md
@.planning/phases/56-dataview-templates-profile-overrides/56-PATTERNS.md
@.planning/phases/56-dataview-templates-profile-overrides/56-VALIDATION.md
@.planning/phases/56-dataview-templates-profile-overrides/56-03-PLAN.md

<interfaces>
From graphify/templates.py:109-126 (ClassificationContext, EXISTING TypedDict — Phase 56 adds one optional field):
```python
class ClassificationContext(TypedDict, total=False):
    note_type: str
    folder: str
    filename_stem: str
    filename_collision: bool
    ...
    community_name: str
    # Phase 56 addition (total=False keeps it backward-compatible):
    # rule_id: str  # populated by classify() when matched rule has id:
```

From graphify/templates.py:1521-1568 (_load_override_template, EXISTING — extend with list_name kwarg):
```python
def _load_override_template(rel_path: str, vault_dir, default_template):
    # ... validation, load, fall-back. Hardcodes "community_templates" in 4 stderr lines.
    # Phase 56: add `, list_name: str = "community_templates"` parameter; replace
    # all hardcoded "community_templates" stderr literals with f-string {list_name}.
```

From graphify/templates.py:1572-1610 (_pick_community_template, EXISTING — keep as-is for backward compat; new _resolve_note_template wraps + delegates):
[See PATTERNS.md §"_resolve_note_template" for the full new function shape]

Call sites to rewire:
- templates.py:1487-1494 (render_note): currently `templates[note_type]` direct lookup → wrap with `_resolve_note_template(rule_id=ctx.get("rule_id"), community_id=None, community_name=None, note_type=note_type, profile=..., vault_dir=..., default_template=templates[note_type])`
- templates.py:1748-1750 (_render_moc_like): currently `_pick_community_template(...)` → replace with `_resolve_note_template(rule_id=None, community_id=community_id, community_name=community_name, note_type=template_key, profile=..., vault_dir=..., default_template=...)`

From graphify/mapping.py:408 (per_node ctx population in classify):
```python
per_node[node_id] = ClassificationContext(
    note_type=...,
    folder=...,
    # Phase 56: when rule.get("id") is a string, include rule_id=rule["id"]
)
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: RED — ladder precedence + warn-fallback + integration tests in tests/test_template_overrides.py</name>
  <files>tests/test_template_overrides.py</files>
  <read_first>
    - tests/test_template_overrides.py (the file Plan 02 created — extend it)
    - tests/test_profile_composition.py:385-590 (existing community_templates: render-time tests — pattern for tmp_path fixture vault + helper that builds a 1-node nx.Graph and calls _render_moc_like; also _copy_fixture pattern)
    - graphify/templates.py:1521-1568 (_load_override_template — current warn message format to mirror in fallback assertions)
    - graphify/templates.py:1487-1494, 1748-1750 (the two call sites the test indirectly exercises through render_note and _render_moc_like)
    - .planning/phases/56-dataview-templates-profile-overrides/56-PATTERNS.md (§"_resolve_note_template" — D-56.05 ladder order)
    - .planning/phases/56-dataview-templates-profile-overrides/56-CONTEXT.md (D-56.05 ladder, D-56.13 warn-fallback contract)
  </read_first>
  <behavior>
    Add to tests/test_template_overrides.py — all RED:

    Ladder precedence (uses tmp_path with fixture vault providing distinct OVERRIDE_*_MARKER strings per template):
    - test_ladder_mapping_rule_template_wins_over_community_and_note_type
    - test_ladder_community_template_wins_when_no_mapping_rule_match
    - test_ladder_note_type_template_wins_when_no_mapping_or_community_match
    - test_ladder_base_default_when_no_overrides_match

    Warn-fallback (one per list — uses capsys to capture stderr):
    - test_mapping_rule_template_missing_file_warns_and_falls_back (stderr contains `"[graphify] mapping_rule_templates override missing"`)
    - test_note_type_template_missing_file_warns_and_falls_back (stderr contains `"[graphify] note_type_templates override missing"`)
    - test_community_template_missing_file_still_warns_with_correct_list_name (regression — stderr contains `"[graphify] community_templates override missing"` after refactor)

    Integration:
    - test_classify_populates_rule_id_when_matched_rule_has_id (asserts `per_node[node_id]["rule_id"] == expected_id`; absence of id: → no rule_id key in ctx)
    - test_render_note_with_rule_id_picks_mapping_rule_template (end-to-end: profile with mapping_rules[0].id=foo + mapping_rule_templates targeting foo → render uses override marker)
  </behavior>
  <action>
    Extend tests/test_template_overrides.py. Build a fixture-vault helper at top of the file (or reuse one already established by Plan 02's tests if present) that writes `.graphify/templates/override_mr.md`, `override_ct.md`, `override_nt.md` each containing distinct `OVERRIDE_<KIND>_MARKER` strings, plus a base profile.yaml.

    For ladder tests, build a node where mapping_rule (with id=foo) matches AND community matches AND note_type matches; set up the override lists to provide each at the corresponding tier; assert the WINNING marker appears in rendered text. Then incrementally remove tiers and assert next-tier wins.

    For warn-fallback tests, set the override path to a non-existent file; assert (a) base content rendered (no OVERRIDE marker), (b) capsys.readouterr().err contains the exact "[graphify] <list_name> override missing" prefix.

    For classify integration test, call `mapping.classify(graph, profile)` directly and inspect the returned per_node dict.

    Run `pytest tests/test_template_overrides.py -q 2>&1 | tail -5` — all new tests RED. Commit: `test(56-05): RED — ladder + warn-fallback + classify rule_id population`.
  </action>
  <verify>
    <automated>pytest tests/test_template_overrides.py -q 2>&1 | grep -E "failed|passed" | tail -3</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "test_ladder_" tests/test_template_overrides.py` returns >= 4
    - `grep -c "test_.*_missing_file_warns_and_falls_back\|test_community_template_missing_file_still_warns" tests/test_template_overrides.py` returns >= 3
    - `grep -c "test_classify_populates_rule_id" tests/test_template_overrides.py` returns 1
    - `grep -c "test_render_note_with_rule_id_picks_mapping_rule_template" tests/test_template_overrides.py` returns 1
    - `grep -c "OVERRIDE_MR_MARKER\|OVERRIDE_CT_MARKER\|OVERRIDE_NT_MARKER" tests/test_template_overrides.py` returns >= 3
    - `pytest tests/test_template_overrides.py::test_ladder_mapping_rule_template_wins_over_community_and_note_type -x` exits non-zero (RED)
    - Commit message starts with `test(56-05): RED`
  </acceptance_criteria>
  <done>9 RED tests covering ladder + warn-fallback + integration committed.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: GREEN — extend ClassificationContext + populate rule_id in classify + extend _load_override_template with list_name kwarg</name>
  <files>graphify/templates.py, graphify/mapping.py</files>
  <read_first>
    - graphify/templates.py:109-126 (ClassificationContext — add one optional field)
    - graphify/templates.py:1521-1568 (_load_override_template — exact stderr lines that need the f-string swap; count occurrences)
    - graphify/mapping.py:303-420 (classify function — read from beginning of function through line 408 ctx population to understand `rule` variable scope)
    - .planning/phases/56-dataview-templates-profile-overrides/56-PATTERNS.md (§"ClassificationContext extension" + §"_resolve_note_template" — note RESEARCH was wrong, it's a TypedDict not dataclass)
    - tests/test_template_overrides.py (the 3 RED tests this task addresses: test_classify_populates_rule_id, test_*_missing_file_warns_and_falls_back warn tests)
  </read_first>
  <behavior>
    - test_classify_populates_rule_id_when_matched_rule_has_id turns GREEN
    - test_community_template_missing_file_still_warns_with_correct_list_name turns GREEN (regression — existing Phase 30 calls keep working with default list_name)
    - test_mapping_rule_template_missing_file_warns_and_falls_back, test_note_type_template_missing_file_warns_and_falls_back stay RED (wired in Task 3)
    - All ladder tests stay RED (wired in Task 3)
    - Full suite stays GREEN
  </behavior>
  <action>
    1. Edit `ClassificationContext` at templates.py:109-126 — add `rule_id: str` as a NEW optional field (TypedDict with `total=False`):
       ```python
       community_name: str
       rule_id: str  # Phase 56 (CFG-01, D-56.04) — populated by classify() when matched rule has id:
       ```

    2. Edit `mapping.classify()` ctx population at mapping.py:408. Identify the `rule` variable (the matched mapping rule for the current node). When `rule.get("id")` is a string, include `rule_id=rule["id"]` in the `ClassificationContext(...)` dict literal. Conditional construction is required since TypedDict missing keys are valid:
       ```python
       ctx_kwargs: dict = {"note_type": ..., "folder": ..., ...}
       if isinstance(rule, dict) and isinstance(rule.get("id"), str):
           ctx_kwargs["rule_id"] = rule["id"]
       per_node[node_id] = ClassificationContext(**ctx_kwargs)
       ```
       (If the existing call site uses dict-literal syntax not kwargs, adapt accordingly while preserving every existing key.)

    3. Edit `_load_override_template` at templates.py:1521-1568:
       - Add new keyword-only parameter: `, *, list_name: str = "community_templates"`
       - Replace EVERY hardcoded `"community_templates"` literal in the four stderr `print(...)` lines with f-string interpolation `{list_name}`. Concretely, each line becomes:
         ```python
         print(f"[graphify] {list_name} override <reason> ({rel_path}): {exc} — using default", file=sys.stderr)
         ```
       - Backward compat: existing callers (one site at templates.py:~1598-1603 inside `_pick_community_template`) work unchanged because default kwarg keeps wording identical.

    Run `pytest tests/test_template_overrides.py::test_classify_populates_rule_id_when_matched_rule_has_id tests/test_template_overrides.py::test_community_template_missing_file_still_warns_with_correct_list_name -x` — both GREEN. Run `pytest tests/ -q` — full suite GREEN. Commit: `feat(56-05): GREEN — ClassificationContext.rule_id + classify() population + _load_override_template list_name`.
  </action>
  <verify>
    <automated>pytest tests/test_template_overrides.py::test_classify_populates_rule_id_when_matched_rule_has_id tests/test_template_overrides.py::test_community_template_missing_file_still_warns_with_correct_list_name -x 2>&1 | tail -3 && pytest tests/ -q 2>&1 | tail -3</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "rule_id: str" graphify/templates.py` returns >= 1
    - `grep -c "list_name: str = \"community_templates\"" graphify/templates.py` returns 1
    - `grep -c '"community_templates" override' graphify/templates.py` returns 0 (all hardcoded literals removed)
    - `grep -c "{list_name} override" graphify/templates.py` returns >= 3 (the four stderr sites now use f-string)
    - `grep -c "rule_id" graphify/mapping.py` returns >= 1 (population site)
    - `pytest tests/test_template_overrides.py::test_classify_populates_rule_id_when_matched_rule_has_id -x` exits 0
    - `pytest tests/ -q` exits 0
    - Commit message starts with `feat(56-05): GREEN`
  </acceptance_criteria>
  <done>Two infrastructure changes landed; full suite green; ladder still RED awaiting Task 3.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: GREEN — implement _resolve_note_template and rewire render_note + _render_moc_like</name>
  <files>graphify/templates.py</files>
  <read_first>
    - graphify/templates.py:1521-1610 (_load_override_template + _pick_community_template — entire region; new function lives here too)
    - graphify/templates.py:1487-1494 (render_note call site — currently does `templates[note_type]` direct lookup)
    - graphify/templates.py:1748-1750 (_render_moc_like call site — currently calls _pick_community_template directly)
    - .planning/phases/56-dataview-templates-profile-overrides/56-PATTERNS.md (§"_resolve_note_template" — full new function body to port + insertion-point list)
    - .planning/phases/56-dataview-templates-profile-overrides/56-CONTEXT.md (D-56.05 ladder, D-56.13 warn-fallback)
    - tests/test_template_overrides.py (the 6 still-RED tests this task turns GREEN: 4 ladder + 2 mapping/note_type warn-fallback)
  </read_first>
  <behavior>
    All remaining RED tests in tests/test_template_overrides.py from Task 1 turn GREEN. Existing Phase 30 community_templates: render tests (`tests/test_profile_composition.py`) stay GREEN — _pick_community_template kept intact, _resolve_note_template delegates to it for the community tier so its behavior is identical.
  </behavior>
  <action>
    1. Add `_resolve_note_template` immediately after `_pick_community_template` (~templates.py:1611). Port the function body verbatim from PATTERNS.md §"_resolve_note_template" — D-56.05 ladder:
       1. mapping_rule_templates (NEW) — iterate `profile.get("mapping_rule_templates") or []`; on `match == "rule_id"` and `pattern == rule_id`, return `_load_override_template(rule.get("template"), vault_dir, default_template, list_name="mapping_rule_templates")`.
       2. community_templates (existing) — when `community_id` and `community_name` both provided, delegate to `_pick_community_template(...)`; if it returns anything other than `default_template`, return that.
       3. note_type_templates (NEW) — iterate `profile.get("note_type_templates") or []`; on `match == "note_type"` and `pattern == note_type`, return `_load_override_template(..., list_name="note_type_templates")`.
       4. base — return `default_template`.

       Keyword-only signature per PATTERNS.md:
       ```python
       def _resolve_note_template(
           *,
           rule_id: str | None,
           community_id: int | None,
           community_name: str | None,
           note_type: str,
           profile: dict,
           vault_dir,
           default_template,
       ):
       ```

    2. Rewire call site at templates.py:1487-1494 (`render_note`). The current direct lookup `templates[note_type]` must be wrapped with `_resolve_note_template(...)`:
       ```python
       template = _resolve_note_template(
           rule_id=ctx.get("rule_id") if isinstance(ctx, dict) else None,
           community_id=None,
           community_name=None,
           note_type=note_type,
           profile=profile,
           vault_dir=vault_dir,
           default_template=templates[note_type],
       )
       ```
       Read 30 lines around 1487-1494 to identify how `profile`, `vault_dir`, and `templates` are scoped at that call site; pass them through.

    3. Rewire call site at templates.py:1748-1750 (`_render_moc_like`). Replace `_pick_community_template(community_id, community_name, profile, vault_dir, default_template)` with:
       ```python
       template = _resolve_note_template(
           rule_id=None,  # _render_moc_like is community-scoped; no rule_id available
           community_id=community_id,
           community_name=community_name,
           note_type=template_key,  # the note_type / template key in scope at this call site
           profile=profile,
           vault_dir=vault_dir,
           default_template=default_template,
       )
       ```
       Confirm `template_key` (or whatever the local note_type variable is named at line 1748) is in scope.

    Run `pytest tests/test_template_overrides.py -q` — ALL ladder + warn-fallback tests GREEN. Run `pytest tests/test_profile_composition.py -q` — Phase 30 community_templates: tests stay GREEN. Run `pytest tests/ -q` — full suite GREEN. Commit: `feat(56-05): GREEN — _resolve_note_template ladder + rewire render_note + _render_moc_like`.
  </action>
  <verify>
    <automated>pytest tests/test_template_overrides.py -q 2>&1 | tail -3 && pytest tests/test_profile_composition.py -q 2>&1 | tail -3 && pytest tests/ -q 2>&1 | tail -3</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "def _resolve_note_template" graphify/templates.py` returns 1
    - `grep -c "list_name=\"mapping_rule_templates\"" graphify/templates.py` returns 1
    - `grep -c "list_name=\"note_type_templates\"" graphify/templates.py` returns 1
    - `grep -c "_resolve_note_template(" graphify/templates.py` returns >= 3 (definition + 2 call sites)
    - `grep -c "_pick_community_template(" graphify/templates.py` returns >= 1 (kept as helper, called from _resolve_note_template tier 2)
    - `pytest tests/test_template_overrides.py -q` exits 0
    - `pytest tests/test_profile_composition.py -q` exits 0 (Phase 30 regression-safe)
    - `pytest tests/ -q` exits 0
    - Commit message starts with `feat(56-05): GREEN`
  </acceptance_criteria>
  <done>Ladder wired end-to-end; CFG-01 render half complete; full suite green.</done>
</task>

</tasks>

<verification>
- `pytest tests/test_template_overrides.py -q` exits 0 (all ladder + warn-fallback + integration tests green)
- `pytest tests/test_profile_composition.py -q` exits 0 (Phase 30 regression-safe)
- `pytest tests/ -q` exits 0 (full suite green)
- `grep -n "_resolve_note_template" graphify/templates.py` shows 1 definition + 2 call sites (render_note, _render_moc_like)
- `grep -n "rule_id" graphify/templates.py graphify/mapping.py` shows ClassificationContext field + classify() population
- `grep -n "list_name" graphify/templates.py` shows kwarg in _load_override_template + 2 explicit pass-through calls in _resolve_note_template
- D-56.05 ladder order verified by reading _resolve_note_template top-down: mapping_rule → community → note_type → base
</verification>

<success_criteria>
1. `_resolve_note_template` consults the three override lists in D-56.05 ladder order and falls back to base.
2. `ClassificationContext.rule_id` populated by `classify()` when the matched mapping rule has an `id:` field.
3. Warn-and-fall-back per Phase 55 D-55.14 / Phase 56 D-56.13 contract: missing/unreadable/invalid override file → stderr warn naming the list + base template renders. No abort.
4. Phase 30 community_templates: behavior unchanged — _pick_community_template kept as the tier-2 delegate; existing tests green.
5. CFG-01 render half complete (Plan 03 = schema half + this plan = render half together close CFG-01).
</success_criteria>

<output>
After completion, create `.planning/phases/56-dataview-templates-profile-overrides/56-05-SUMMARY.md` documenting the new `_resolve_note_template` signature, the two call-site rewires (with line numbers post-edit), the `_load_override_template` list_name extension, and the `ClassificationContext.rule_id` population logic. Note: confirm Plan 03's `mapping_rule_templates:` validator is the producer side of the rule_id contract this plan consumes.
</output>
