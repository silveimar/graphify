---
phase: 56
plan: 03
type: tdd
wave: 3
depends_on: [56-01, 56-02]
files_modified:
  - graphify/profile.py
  - graphify/mapping.py
  - tests/test_profile.py
  - tests/test_mapping.py
autonomous: true
requirements: [CFG-01]
must_haves:
  truths:
    - "mapping_rule_templates: validator rejects bad shapes with deterministic per-index error wording (mirrors community_templates: validator)"
    - "note_type_templates: validator rejects unknown note_types using _KNOWN_NOTE_TYPES allowlist"
    - "Both new keys register in _VALID_TOP_LEVEL_KEYS so unknown-key check at profile.py:660 passes them through"
    - "mapping_rules.id: optional slug field validated (type, length, pattern, uniqueness) at validate_rules in mapping.py"
    - "Path-confinement checks (.., absolute, ~) port verbatim from community_templates: validator using substring style (NOT Path.parts)"
    - "Backward compat: profiles without mapping_rule_templates: / note_type_templates: / mapping_rules.id: continue to validate (treat absence as empty list / optional field)"
  artifacts:
    - path: "graphify/profile.py"
      provides: "mapping_rule_templates: validator block + note_type_templates: validator block + _VALID_TOP_LEVEL_KEYS extension"
      contains: "mapping_rule_templates"
    - path: "graphify/mapping.py"
      provides: "_RULE_ID_PATTERN, _RULE_ID_MAX_LEN constants + per-rule id: validation + uniqueness pass in validate_rules"
      contains: "_RULE_ID_PATTERN"
    - path: "tests/test_profile.py"
      provides: "Validator tests for mapping_rule_templates: + note_type_templates: (positive + each rejection class)"
    - path: "tests/test_mapping.py"
      provides: "Five new validate_rules tests for the optional id: field"
  key_links:
    - from: "graphify/profile.py:_VALID_TOP_LEVEL_KEYS"
      to: "graphify/profile.py:660 (unknown-key gate)"
      via: "set membership check"
      pattern: "mapping_rule_templates.*Phase 56"
    - from: "graphify/mapping.py:validate_rules"
      to: "graphify/profile.py:1041-1051 (mapping_rules entry point)"
      via: "delegate call"
      pattern: "validate_rules"
---

<objective>
Implement CFG-01 schema-only contributions: two new top-level profile keys (`mapping_rule_templates:`, `note_type_templates:`) with validators ported byte-for-byte from `community_templates:` (`profile.py:682-735`), and the optional `mapping_rules.id:` slug field per D-56.04 added to `validate_rules` in `mapping.py`. No render-time wiring (Plan 05 owns that).

Purpose: CFG-01 demands "scoped template overrides documented in schema." This plan provides the schema half — the two new override-list shapes and the prerequisite `id:` field that `mapping_rule_templates:` references. Plan 05 then wires the render-time ladder. Splitting schema (this plan) from runtime (Plan 05) keeps each plan within context budget and lets Plan 04 (TMPL-03 dead-rules) run in parallel since it only touches the dataview_queries: validator path.
Output: Two new validators + `_VALID_TOP_LEVEL_KEYS` extension + `mapping_rules.id:` validation + tests.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/56-dataview-templates-profile-overrides/56-CONTEXT.md
@.planning/phases/56-dataview-templates-profile-overrides/56-RESEARCH.md
@.planning/phases/56-dataview-templates-profile-overrides/56-PATTERNS.md
@.planning/phases/56-dataview-templates-profile-overrides/56-VALIDATION.md

<interfaces>
From graphify/profile.py:168-176 (_VALID_TOP_LEVEL_KEYS, EXISTING — Phase 56 adds two entries):
```python
_VALID_TOP_LEVEL_KEYS = {
    "folder_mapping", "naming", "merge", "mapping_rules", "obsidian",
    "topology", "mapping", "tag_taxonomy", "profile_sync", "diagram_types",
    "output", "taxonomy", "repo", "corpus",
    "extends", "includes", "community_templates",  # Phase 30
    "dataview_queries",  # Phase 31
    "predicate_flags",  # Phase 55
    # Phase 56 additions:
    # "mapping_rule_templates",  # Phase 56 (CFG-01, D-56.03)
    # "note_type_templates",     # Phase 56 (CFG-01, D-56.03)
}
```

From graphify/profile.py:682-735 (community_templates: validator — CANONICAL ANALOG to port):
[See PATTERNS.md §"mapping_rule_templates: validator" for the verbatim block + the swap list]

From graphify/profile.py:181+ (_KNOWN_NOTE_TYPES — allowlist that note_type_templates.pattern validates against)

From graphify/mapping.py:841-925 (validate_rules — per-rule loop where id: validation lands)
From graphify/mapping.py:910-913 (analog: existing optional `then.folder` field check)
From graphify/naming.py:34, 53-64 (slug pattern reference: _REPO_IDENTITY_MAX_LEN = 80)
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: RED — validator tests for mapping_rule_templates: and note_type_templates: (test_profile.py) + mapping_rules.id: tests (test_mapping.py)</name>
  <files>tests/test_profile.py, tests/test_mapping.py</files>
  <read_first>
    - tests/test_profile.py (read ~50 lines around the existing `community_templates:` validator tests — search by `community_templates` to locate; mirror the test style precisely)
    - tests/test_mapping.py (skim full file structure to understand how validate_rules is invoked; locate existing `then.folder` optional-field test pattern as the analog for id:)
    - .planning/phases/56-dataview-templates-profile-overrides/56-PATTERNS.md (§"mapping.py — Optional id: field" — exact slug regex, max-len constant, error wording)
    - .planning/phases/56-dataview-templates-profile-overrides/56-CONTEXT.md (D-56.04 — id: field rules; Claude's Discretion: slug regex `^[a-z][a-z0-9_-]*$`, max-len aligned with naming._REPO_IDENTITY_MAX_LEN)
    - graphify/profile.py:682-735 (community_templates: validator — to understand what positive/negative test cases must cover for the parallel new validators)
  </read_first>
  <behavior>
    In tests/test_profile.py (add adjacent to existing community_templates: validator tests):
    - test_mapping_rule_templates_validates_well_formed_list (positive)
    - test_mapping_rule_templates_rejects_non_list
    - test_mapping_rule_templates_rejects_bad_match_kind (only "rule_id" allowed; reject "label", "id", missing)
    - test_mapping_rule_templates_rejects_non_slug_pattern (uppercase, leading digit, contains `/`, contains `..`)
    - test_mapping_rule_templates_rejects_template_path_escapes (`..`, absolute, leading `~`)
    - test_mapping_rule_templates_rejects_unknown_keys (anything outside `{match, pattern, template}`)
    - test_note_type_templates_validates_well_formed_list (positive — pattern from _KNOWN_NOTE_TYPES)
    - test_note_type_templates_rejects_non_list
    - test_note_type_templates_rejects_unknown_note_type (pattern not in _KNOWN_NOTE_TYPES)
    - test_note_type_templates_rejects_template_path_escapes
    - test_note_type_templates_rejects_unknown_keys
    - test_unknown_top_level_key_no_longer_rejects_mapping_rule_templates (regression guard for _VALID_TOP_LEVEL_KEYS extension)
    - test_unknown_top_level_key_no_longer_rejects_note_type_templates

    In tests/test_mapping.py (add adjacent to existing validate_rules tests):
    - test_validate_rules_id_field_accepted_when_valid_slug (and accepts absence — backward compat)
    - test_validate_rules_id_field_rejects_non_string (int, list, None-via-explicit-null)
    - test_validate_rules_id_field_rejects_too_long (> 80 chars)
    - test_validate_rules_id_field_rejects_bad_pattern (leading digit, uppercase letters, contains `..`, contains `/`)
    - test_validate_rules_id_field_rejects_duplicate_across_rules (two rules same id → error citing both indices)

    All ~18 tests RED at this point — production code does not exist yet.
  </behavior>
  <action>
    Add the test functions described above. Use the EXISTING community_templates: validator tests in test_profile.py as the pattern template — direct `validate_profile(dict)` calls, single-assertion-per-test style, error-substring assertions like `assert any("mapping_rule_templates[0].pattern" in e for e in errors)`. For test_mapping.py id: tests, mirror the existing `then.folder` test pattern; use `validate_rules([{...rule}])` calls.

    Run `pytest tests/test_profile.py tests/test_mapping.py -q 2>&1 | tail -10` — expect ~18 failures. Commit: `test(56-03): RED — CFG-01 validator + mapping_rules.id: tests`.
  </action>
  <verify>
    <automated>pytest tests/test_profile.py tests/test_mapping.py -q 2>&1 | grep -E "failed" | tail -3</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "test_mapping_rule_templates_" tests/test_profile.py` returns >= 6
    - `grep -c "test_note_type_templates_" tests/test_profile.py` returns >= 5
    - `grep -c "test_validate_rules_id_field_" tests/test_mapping.py` returns >= 5
    - `pytest tests/test_profile.py::test_mapping_rule_templates_validates_well_formed_list -x 2>&1 | tail -3` shows non-zero exit (RED)
    - Commit message starts with `test(56-03): RED`
  </acceptance_criteria>
  <done>~18 RED tests across test_profile.py + test_mapping.py, committed.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: GREEN — port community_templates: validator to mapping_rule_templates: and note_type_templates: + register both in _VALID_TOP_LEVEL_KEYS</name>
  <files>graphify/profile.py</files>
  <read_first>
    - graphify/profile.py:168-176 (_VALID_TOP_LEVEL_KEYS — add two entries with comment style mirroring Phase 31/55 entries)
    - graphify/profile.py:682-735 (the verbatim block to mirror)
    - graphify/profile.py:181+ (_KNOWN_NOTE_TYPES — used in note_type_templates.pattern check)
    - .planning/phases/56-dataview-templates-profile-overrides/56-PATTERNS.md (§"mapping_rule_templates: validator" + §"note_type_templates: validator" — exact swap lists; CRITICAL note about substring-style path-confinement)
    - tests/test_profile.py (the RED tests from Task 1 that this code must turn GREEN)
  </read_first>
  <behavior>
    All ~13 mapping_rule_templates: + note_type_templates: + _VALID_TOP_LEVEL_KEYS tests turn GREEN. Existing community_templates: tests remain GREEN (no regression — the new validators are independent code blocks).
  </behavior>
  <action>
    1. Edit `_VALID_TOP_LEVEL_KEYS` at profile.py:168-176 to add two entries with comment style mirroring Phase 31/55:
       ```python
       "mapping_rule_templates",  # Phase 56 (CFG-01, D-56.03)
       "note_type_templates",     # Phase 56 (CFG-01, D-56.03)
       ```

    2. Add the `mapping_rule_templates:` validator block immediately after the `community_templates:` validator block (insert after line 735). Port lines 682-735 verbatim with these exact swaps per PATTERNS.md:
       - Variable: `ct` → `mrt`
       - Top-level key: `"community_templates"` → `"mapping_rule_templates"`
       - Prefix: `"community_templates[{idx}]"` → `"mapping_rule_templates[{idx}]"`
       - `match` allowlist: `{"label", "id"}` → `{"rule_id"}` (single value in v1.11)
       - Replace the `match in {label, id}` branches with: `if match != "rule_id": errors.append(f"{prefix}.match must be 'rule_id' (got {match!r})")`. Drop the int-pattern branch entirely.
       - `pattern` type rule: must be `str` matching slug regex `^[a-z][a-z0-9_-]*$` (use shared `_RULE_ID_PATTERN` constant from mapping.py — import or duplicate; planner discretion: duplicate locally as `_MAPPING_RULE_TEMPLATE_PATTERN_RE` to avoid cross-module import cycle; use `re.fullmatch`)
       - Path-confinement (`..`, absolute, `~`): port VERBATIM including error wording (substring style `".." in template`, NOT Path.parts).
       - Unknown-keys: port verbatim.

    3. Add the `note_type_templates:` validator block immediately after the new `mapping_rule_templates:` block. Same template, swaps:
       - Variable: `ct` → `ntt`
       - Top-level key: `"note_type_templates"`
       - Prefix: `"note_type_templates[{idx}]"`
       - `match` allowlist: `{"note_type"}` (single value)
       - `pattern` validation: must be `str` AND `pattern in _KNOWN_NOTE_TYPES`. Error wording: `f"{prefix}.pattern must be one of {sorted(_KNOWN_NOTE_TYPES)} (got {pattern!r})"`.
       - Same path-confinement + unknown-keys block.

    Run `pytest tests/test_profile.py -q` — all GREEN including existing community_templates: tests. Commit: `feat(56-03): GREEN — mapping_rule_templates: + note_type_templates: validators`.
  </action>
  <verify>
    <automated>pytest tests/test_profile.py -q 2>&1 | tail -3</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "mapping_rule_templates" graphify/profile.py` returns >= 5 (key in _VALID_TOP_LEVEL_KEYS, validator block references)
    - `grep -c "note_type_templates" graphify/profile.py` returns >= 5
    - `grep -c '"mapping_rule_templates",  # Phase 56' graphify/profile.py` returns 1
    - `grep -c '"note_type_templates",     # Phase 56' graphify/profile.py` returns 1
    - `grep -c "mrt = profile.get" graphify/profile.py` returns 1 (mapping_rule_templates: validator entry)
    - `grep -c "ntt = profile.get" graphify/profile.py` returns 1
    - `grep -c '".." in template' graphify/profile.py` returns >= 3 (community_templates: + 2 new validators all use substring-style)
    - `pytest tests/test_profile.py -q` exits 0
    - Commit message starts with `feat(56-03): GREEN`
  </acceptance_criteria>
  <done>Two new validators implemented; CFG-01 schema half complete; commit landed.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: GREEN — implement mapping_rules.id: optional slug field in validate_rules</name>
  <files>graphify/mapping.py</files>
  <read_first>
    - graphify/mapping.py:1-30 (imports + module-level constants — _MAX_PATTERN_LEN at line 26 is the analog for new constants)
    - graphify/mapping.py:841-925 (validate_rules function — full read; injection point is between line 871 and matcher dispatch at line 873; uniqueness pass goes before line 924's _detect_dead_rules call)
    - graphify/mapping.py:910-913 (analog optional-field pattern: `then.folder`)
    - graphify/naming.py:34, 53-64 (_REPO_IDENTITY_MAX_LEN = 80 — convention reference for max-len constant)
    - .planning/phases/56-dataview-templates-profile-overrides/56-PATTERNS.md (§"mapping.py — Optional id: field" — exact code shape including uniqueness pass)
    - tests/test_mapping.py (the five RED tests from Task 1)
  </read_first>
  <behavior>
    All five RED `test_validate_rules_id_field_*` tests turn GREEN. No regression in `pytest tests/test_mapping.py -q` or `pytest tests/ -q`. Backward compat: existing rules without `id:` field validate cleanly (no new error introduced for them).
  </behavior>
  <action>
    1. Add module-level constants near line 26 (after `_MAX_PATTERN_LEN`):
       ```python
       _RULE_ID_PATTERN = re.compile(r"^[a-z][a-z0-9_-]*$")  # Phase 56 (CFG-01, D-56.04)
       _RULE_ID_MAX_LEN = 80  # Phase 56 (CFG-01, D-56.04) — mirror naming._REPO_IDENTITY_MAX_LEN
       ```
       (Verify `import re` exists at top of mapping.py; if not, add it.)

    2. Inside `validate_rules` per-rule loop, before matcher dispatch at line 873, insert per-rule id: validation (mirror the `then.folder` optional-field pattern at lines 910-913):
       ```python
       rule_id = rule.get("id")
       if rule_id is not None:
           if not isinstance(rule_id, str):
               errors.append(f"{prefix}.id: must be a string (got {type(rule_id).__name__})")
           elif len(rule_id) > _RULE_ID_MAX_LEN:
               errors.append(f"{prefix}.id: length {len(rule_id)} exceeds cap {_RULE_ID_MAX_LEN}")
           elif not _RULE_ID_PATTERN.fullmatch(rule_id):
               errors.append(
                   f"{prefix}.id: must match pattern '^[a-z][a-z0-9_-]*$' (got {rule_id!r})"
               )
       ```

    3. After the per-rule loop completes, before line 924's `_detect_dead_rules` call, add uniqueness pass:
       ```python
       seen_ids: dict[str, int] = {}
       for idx, rule in enumerate(rules):
           if not isinstance(rule, dict):
               continue
           rid = rule.get("id")
           if isinstance(rid, str) and rid in seen_ids:
               errors.append(
                   f"mapping_rules[{idx}].id: duplicate id {rid!r} — also defined at "
                   f"mapping_rules[{seen_ids[rid]}]"
               )
           elif isinstance(rid, str):
               seen_ids[rid] = idx
       ```

    Run `pytest tests/test_mapping.py -q` — all five new tests GREEN. Run `pytest tests/ -q` — full suite GREEN. Commit: `feat(56-03): GREEN — mapping_rules.id: optional slug field with uniqueness validation`.
  </action>
  <verify>
    <automated>pytest tests/test_mapping.py -q 2>&1 | tail -3 && pytest tests/ -q 2>&1 | tail -3</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "_RULE_ID_PATTERN = re.compile" graphify/mapping.py` returns 1
    - `grep -c "_RULE_ID_MAX_LEN = 80" graphify/mapping.py` returns 1
    - `grep -c "rule.get(\"id\")" graphify/mapping.py` returns >= 2 (per-rule check + uniqueness pass)
    - `grep -c "duplicate id" graphify/mapping.py` returns 1
    - `pytest tests/test_mapping.py -q` exits 0
    - `pytest tests/ -q` exits 0
    - Commit message starts with `feat(56-03): GREEN`
  </acceptance_criteria>
  <done>Optional id: field validated with type + length + pattern + uniqueness; backward compat preserved; commit landed.</done>
</task>

</tasks>

<verification>
- `pytest tests/test_profile.py tests/test_mapping.py -q` exits 0 (all CFG-01 schema tests green)
- `pytest tests/ -q` exits 0 (full suite green — no regression in any existing validator)
- `grep -n "Phase 56" graphify/profile.py` shows entries for both new top-level keys + their validator blocks
- `grep -n "Phase 56" graphify/mapping.py` shows id: field constants + validation blocks
- All path-confinement checks in the two new validators use substring style (`".." in template`), matching community_templates: validator
</verification>

<success_criteria>
1. `mapping_rule_templates:` and `note_type_templates:` accepted by preflight when well-formed; rejected with deterministic per-index errors when malformed.
2. `mapping_rules.id:` optional field validated; duplicates raise preflight error citing colliding indices.
3. Existing profiles (no new keys, no id: fields) continue to validate without modification — backward compat preserved.
4. CFG-01 schema half complete; render-time half lands in Plan 05.
</success_criteria>

<output>
After completion, create `.planning/phases/56-dataview-templates-profile-overrides/56-03-SUMMARY.md` documenting the two new validator block locations in profile.py, the three additions in mapping.py (constants + per-rule check + uniqueness pass), and the swap-from-community_templates audit trail.
</output>
