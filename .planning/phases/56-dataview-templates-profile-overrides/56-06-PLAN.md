---
phase: 56
plan: 06
type: execute
wave: 5
depends_on: [56-02, 56-03, 56-04]
files_modified:
  - docs/PROFILE-CONFIGURATION.md
  - docs/TEMPLATES.md
autonomous: true
requirements: [TMPL-03, CFG-01, CFG-02]
must_haves:
  truths:
    - "docs/PROFILE-CONFIGURATION.md has a subsection per new key: mapping_rule_templates, note_type_templates, mapping_rules.id"
    - "docs/PROFILE-CONFIGURATION.md has a 'How overrides resolve' subsection containing the D-56.05 ladder as a numbered list (mapping_rule > community > note_type > base)"
    - "docs/PROFILE-CONFIGURATION.md has an 'Override collision validation' subsection cross-referencing all four CFG-02 collision classes (D-56.06)"
    - "docs/PROFILE-CONFIGURATION.md contains one worked example showing all three override types resolving for a single note"
    - "docs/TEMPLATES.md gains exactly one paragraph forward-pointer to docs/PROFILE-CONFIGURATION.md per D-56.11; no override semantics duplicated"
    - "No docs/MIGRATION_V1_11.md created (D-56.12 explicit)"
    - "No deprecation notice for community_templates: (it stays first-class per D-56.12)"
  artifacts:
    - path: "docs/PROFILE-CONFIGURATION.md"
      provides: "Major update: 3 new key subsections + ladder + collision classes + worked example"
      min_lines: 80
    - path: "docs/TEMPLATES.md"
      provides: "One-paragraph forward-pointer to PROFILE-CONFIGURATION.md (Phase 56 additions)"
  key_links:
    - from: "docs/TEMPLATES.md"
      to: "docs/PROFILE-CONFIGURATION.md"
      via: "single forward-pointer paragraph"
      pattern: "PROFILE-CONFIGURATION.md"
    - from: "docs/PROFILE-CONFIGURATION.md ladder section"
      to: "graphify/templates.py:_resolve_note_template (Plan 05)"
      via: "documents D-56.05 priority order users will observe at render time"
      pattern: "mapping_rule_templates"
---

<objective>
Implement D-56.10 (`docs/PROFILE-CONFIGURATION.md` major update) and D-56.11 (`docs/TEMPLATES.md` one-paragraph forward-pointer). No code, no tests — pure documentation per D-56.12 (no MIGRATION_V1_11.md, no deprecation notice for community_templates:).

Purpose: CFG-01 success criterion ("documented in profile schema") requires docs to land alongside code. Lands in Wave 3 parallel with Plan 05 since it touches no shared files. type: execute (not TDD) per planning_context: documentation tasks do not benefit from RED/GREEN cycles.
Output: Two documentation file edits.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/56-dataview-templates-profile-overrides/56-CONTEXT.md
@.planning/phases/56-dataview-templates-profile-overrides/56-PATTERNS.md
@.planning/phases/56-dataview-templates-profile-overrides/56-VALIDATION.md
@.planning/phases/56-dataview-templates-profile-overrides/56-03-PLAN.md
@.planning/phases/56-dataview-templates-profile-overrides/56-05-PLAN.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Update docs/PROFILE-CONFIGURATION.md with new key subsections + ladder + collision classes + worked example</name>
  <files>docs/PROFILE-CONFIGURATION.md</files>
  <read_first>
    - docs/PROFILE-CONFIGURATION.md (full file — locate existing community_templates: section + dataview_queries: section as the analog style for the three new subsections; identify a sensible insertion point after the community_templates: section)
    - .planning/phases/56-dataview-templates-profile-overrides/56-CONTEXT.md (D-56.10 — exact subsections required; D-56.05 ladder; D-56.06 four collision classes; D-56.04 mapping_rules.id: rules)
    - .planning/phases/56-dataview-templates-profile-overrides/56-03-PLAN.md (the schema validator details — slug pattern, max length, allowed match values, _KNOWN_NOTE_TYPES allowlist)
    - .planning/phases/56-dataview-templates-profile-overrides/56-05-PLAN.md (the render-time ladder behavior — mirror its description)
  </read_first>
  <action>
    Add the following sections to `docs/PROFILE-CONFIGURATION.md` (insert after the existing `community_templates:` section so the three sibling override-list sections cluster together):

    1. **`mapping_rules.id:` (optional)** subsection (D-56.04):
       - Why: makes a rule referenceable by `mapping_rule_templates:`.
       - Constraint: slug regex `^[a-z][a-z0-9_-]*$`, max 80 chars, unique within `mapping_rules:` list.
       - Backward compat: rules without `id:` continue to work; only opt-in if you intend to override their template.
       - Short YAML example.

    2. **`mapping_rule_templates:` (override list)** subsection (D-56.03):
       - Shape: `- {match: rule_id, pattern: <slug>, template: <fragment-path>}`.
       - `match:` allowlist is `{rule_id}` in v1.11 (single value).
       - `pattern:` is the rule slug; must match a `mapping_rules[].id`.
       - `template:` is a fragment path under `.graphify/`; path-confinement enforced (no `..`, no absolute, no leading `~`).
       - YAML example showing one rule + its override.

    3. **`note_type_templates:` (override list)** subsection (D-56.03):
       - Shape: `- {match: note_type, pattern: <one of _KNOWN_NOTE_TYPES>, template: <fragment-path>}`.
       - `match:` allowlist is `{note_type}` (single value).
       - `pattern:` must be a known note_type (list the current set: `code`, `community`, `moc`, `person`, `source`, `statement`, `thing`).
       - `template:` same path-confinement rules.
       - YAML example.

    4. **`How overrides resolve` (precedence ladder)** subsection (D-56.05) — numbered list:
       1. `mapping_rule_templates` — when the node was classified by a rule with `id:` and a matching entry exists.
       2. `community_templates` — when the node's community matches a rule's `match`/`pattern`.
       3. `note_type_templates` — when the node's note_type matches a rule's `pattern`.
       4. Base profile template for the note_type.
       Add narrative: "Most-specific scope wins. Cross-scope overlaps resolve silently via this ladder; no error is raised. Within a single list, first-matching-rule wins (only relevant when patterns overlap; intra-list pattern-overlap is not detected at preflight)."

    5. **`Override collision validation`** subsection (D-56.06) — cross-reference the four collision classes:
       1. Duplicate `pattern` within `mapping_rule_templates:` — preflight error citing both indices.
       2. Duplicate `pattern` within `community_templates:` or `note_type_templates:` — same.
       3. Duplicate `pattern` (= note_type) within `note_type_templates:` — same.
       4. Duplicate `dataview_queries.<note_type>` across `extends:` / `includes:` chain — preflight error enumerating ALL contributing source paths.
       Add narrative: "Pattern-overlap detection (e.g., `Auth*` vs `AuthService` matching the same node) is intentionally NOT performed; render-time list-order resolution applies."

    6. **Worked example** (D-56.10): one complete YAML snippet showing all three override lists used together for a single conceptual note (e.g., a "ServiceClass" classified by `mapping_rule[id=service-class]` in community `Authentication` with `note_type: thing`). Show which override wins per the ladder. Include the rendered template path so readers see the resolution explicitly.

    Use Markdown headings consistent with the rest of the document (e.g., if existing sections use `###`, use `###`). Mirror the YAML formatting style from existing `community_templates:` section. Cross-link with relative anchors where helpful (e.g., the ladder section can link to each per-key subsection above it).

    Commit: `docs(56-06): document mapping_rule_templates, note_type_templates, mapping_rules.id, ladder, and collision matrix`.
  </action>
  <verify>
    <automated>grep -c "mapping_rule_templates" docs/PROFILE-CONFIGURATION.md && grep -c "note_type_templates" docs/PROFILE-CONFIGURATION.md && grep -c "mapping_rules.id\|mapping_rules\[\].id\|^### .*\`id:\`\|id: (optional)" docs/PROFILE-CONFIGURATION.md && grep -ci "How overrides resolve\|precedence ladder" docs/PROFILE-CONFIGURATION.md && grep -ci "collision\|composition chain" docs/PROFILE-CONFIGURATION.md</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "mapping_rule_templates" docs/PROFILE-CONFIGURATION.md` returns >= 3
    - `grep -c "note_type_templates" docs/PROFILE-CONFIGURATION.md` returns >= 3
    - `grep -ci "How overrides resolve\|precedence ladder" docs/PROFILE-CONFIGURATION.md` returns >= 1
    - `grep -ci "collision\|composition chain" docs/PROFILE-CONFIGURATION.md` returns >= 4 (one per collision class)
    - `grep -c "rule_id\|^- match: rule_id" docs/PROFILE-CONFIGURATION.md` returns >= 1
    - `grep -c "^- match: note_type" docs/PROFILE-CONFIGURATION.md` returns >= 1
    - `grep -ci "worked example\|^## Example\|^### Example" docs/PROFILE-CONFIGURATION.md` returns >= 1 (worked example heading)
    - `wc -l docs/PROFILE-CONFIGURATION.md` shows the file grew by at least 80 lines vs pre-edit baseline (Phase 56 additions)
    - Commit message starts with `docs(56-06):`
  </acceptance_criteria>
  <done>PROFILE-CONFIGURATION.md updated; D-56.10 satisfied; commit landed.</done>
</task>

<task type="auto">
  <name>Task 2: Add one-paragraph forward-pointer to docs/TEMPLATES.md</name>
  <files>docs/TEMPLATES.md</files>
  <read_first>
    - docs/TEMPLATES.md (read full file structure — find the block-engine intro section; the new paragraph follows it per PATTERNS.md analog)
    - .planning/phases/56-dataview-templates-profile-overrides/56-PATTERNS.md (§"docs/TEMPLATES.md — one-paragraph forward-pointer" — exact suggested text)
    - .planning/phases/56-dataview-templates-profile-overrides/56-CONTEXT.md (D-56.11 — "one-paragraph forward-pointer", "no duplication of override semantics")
  </read_first>
  <action>
    Add exactly one paragraph to `docs/TEMPLATES.md`, immediately after the existing block-engine intro section. Suggested wording (per PATTERNS.md):

    > Profile-level template overrides (per-mapping-rule, per-community, per-note-type) and the precedence ladder are documented in [`docs/PROFILE-CONFIGURATION.md`](./PROFILE-CONFIGURATION.md) (Phase 56 additions). This document scopes to the block engine; override resolution lives in the profile composition layer.

    Adapt path/wording to match the document's house style (relative link form, capitalization, voice). Do NOT duplicate any override semantics into TEMPLATES.md; the cross-link is the entire contribution per D-56.11.

    Commit: `docs(56-06): add forward-pointer in TEMPLATES.md to PROFILE-CONFIGURATION.md override surface`.
  </action>
  <verify>
    <automated>grep -c "PROFILE-CONFIGURATION.md" docs/TEMPLATES.md && grep -ci "Phase 56\|profile-level template overrides\|override resolution" docs/TEMPLATES.md</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "PROFILE-CONFIGURATION.md" docs/TEMPLATES.md` returns >= 1
    - `grep -ci "profile-level template overrides\|profile composition layer" docs/TEMPLATES.md` returns >= 1
    - `grep -c "mapping_rule_templates\|note_type_templates" docs/TEMPLATES.md` returns 0 (no duplication of override semantics — only the cross-link)
    - The added text is exactly ONE paragraph (not a section, not multiple paragraphs)
    - Commit message starts with `docs(56-06):`
  </acceptance_criteria>
  <done>TEMPLATES.md forward-pointer added; D-56.11 satisfied; commit landed.</done>
</task>

</tasks>

<verification>
- `grep -c "mapping_rule_templates\|note_type_templates" docs/PROFILE-CONFIGURATION.md` returns >= 6
- `grep -c "PROFILE-CONFIGURATION.md" docs/TEMPLATES.md` returns >= 1
- `test -f docs/MIGRATION_V1_11.md` exits NON-ZERO (D-56.12 — file MUST NOT exist)
- `grep -ci "deprecat" docs/PROFILE-CONFIGURATION.md | head -1` shows no Phase-56-specific deprecation language for community_templates: (D-56.12)
- `pytest tests/ -q` exits 0 (no code changed; suite remains green)
</verification>

<success_criteria>
1. `docs/PROFILE-CONFIGURATION.md` documents all three new schema additions (mapping_rule_templates, note_type_templates, mapping_rules.id) + the D-56.05 ladder + the four CFG-02 collision classes + a worked example.
2. `docs/TEMPLATES.md` has the one-paragraph forward-pointer per D-56.11 with no override-semantics duplication.
3. No `docs/MIGRATION_V1_11.md` created (D-56.12).
4. `community_templates:` retains first-class status; no deprecation language added.
5. CFG-01 success criterion ("documented in profile schema") fully satisfied.
</success_criteria>

<output>
After completion, create `.planning/phases/56-dataview-templates-profile-overrides/56-06-SUMMARY.md` documenting the section structure added to PROFILE-CONFIGURATION.md (with section headings) and the exact paragraph text added to TEMPLATES.md.
</output>
