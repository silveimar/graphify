---
phase: 56
plan: 02
type: tdd
wave: 2
depends_on: [56-01]
files_modified:
  - graphify/profile.py
  - tests/test_template_overrides.py
autonomous: true
requirements: [CFG-02]
must_haves:
  truths:
    - "Four schema-only collision classes from D-56.06 each raise deterministic errors with stable wording"
    - "Cross-chain dataview_queries.<note_type> collisions enumerate ALL contributing source paths (consumer of Plan 01's list-shape provenance)"
    - "Each collision class has at least one positive test (collision detected) and one negative test (similar-but-non-colliding) per D-56.07"
    - "validate_profile_preflight aggregates all four detector outputs into its existing errors list (never raises)"
    - "tests/test_template_overrides.py exists as the canonical home for collision-matrix tests (Wave 0 file per VALIDATION.md)"
  artifacts:
    - path: "graphify/profile.py"
      provides: "_detect_mapping_rule_template_collisions, _detect_pattern_duplicate_collisions, _detect_note_type_template_collisions, _detect_dataview_collisions module-level helpers + wiring into validate_profile_preflight"
      contains: "_detect_dataview_collisions"
    - path: "tests/test_template_overrides.py"
      provides: "Wave 0 collision-matrix test module: 4 classes × 2 cases (positive + negative) parametric tests"
      contains: "test_collision"
  key_links:
    - from: "graphify/profile.py:validate_profile_preflight"
      to: "graphify/profile.py:_detect_dataview_collisions"
      via: "errors.extend(_detect_dataview_collisions(provenance))"
      pattern: "errors\\.extend\\(_detect_dataview_collisions"
    - from: "graphify/profile.py:_detect_dataview_collisions"
      to: "graphify/profile.py:_deep_merge_with_provenance (Plan 01 output)"
      via: "consumes provenance: dict[str, list[Path]] shape"
      pattern: "dict\\[str, list\\[Path\\]\\]"
---

<objective>
Implement the four CFG-02 schema-only collision detectors per D-56.06 as module-level helpers in `graphify/profile.py`, wire them into `validate_profile_preflight`, and create the new Wave 0 test file `tests/test_template_overrides.py` containing the canonical collision matrix (4 classes × ≥2 cases each per D-56.07).

Purpose: CFG-02 requires deterministic preflight errors when override precedence is ambiguous. The detection is schema-only (graph-blind, terminating, fast) per D-56.06. Cross-chain `dataview_queries.<note_type>` collisions consume Plan 01's new list-shape provenance to enumerate all contributing files in the error message. This plan also seeds the new `tests/test_template_overrides.py` module that Plans 04 and 05 will extend.
Output: Four detectors + wired errors + collision matrix test file.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/56-dataview-templates-profile-overrides/56-CONTEXT.md
@.planning/phases/56-dataview-templates-profile-overrides/56-RESEARCH.md
@.planning/phases/56-dataview-templates-profile-overrides/56-PATTERNS.md
@.planning/phases/56-dataview-templates-profile-overrides/56-VALIDATION.md
@.planning/phases/56-dataview-templates-profile-overrides/56-01-PLAN.md

<interfaces>
From graphify/profile.py:_deep_merge_with_provenance (POST PLAN 01 SHAPE):
```python
provenance: dict[str, list[Path]]   # Plan 01 output — list per leaf in merge order
```

From graphify/profile.py:682-735 (community_templates: validator — analog for collision style; CURRENTLY has NO duplicate-pattern check, Plan 02 adds one via §2 detector):
```python
ct = profile.get("community_templates")
# ... existing per-rule validation, no cross-rule duplicate check today
```

From graphify/profile.py:validate_profile_preflight (~line 1426+) — wiring point:
```python
# After existing schema validators (Layer 1):
errors.extend(_detect_mapping_rule_template_collisions(profile))
errors.extend(_detect_pattern_duplicate_collisions(profile))
errors.extend(_detect_note_type_template_collisions(profile))
errors.extend(_detect_dataview_collisions(provenance))
```

Detector contract (mirror every existing validator in the codebase):
```python
def _detect_*_collisions(...) -> list[str]:
    """Returns list of error strings; empty means no collisions; never raises."""
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: RED — create tests/test_template_overrides.py with full collision matrix</name>
  <files>tests/test_template_overrides.py</files>
  <read_first>
    - tests/test_profile.py (read ~50 lines around line 1612-1740 for existing dataview_queries: validator test style — one assertion per function, direct validate_profile() calls, no fixture vault for schema-only)
    - tests/test_profile_composition.py:385-590 (existing community_templates: render-time tests for tmp_path extends-chain pattern)
    - .planning/phases/56-dataview-templates-profile-overrides/56-PATTERNS.md (§"Four collision detectors" — exact code shape for all four)
    - .planning/phases/56-dataview-templates-profile-overrides/56-CONTEXT.md (D-56.06 — four collision classes, D-56.07 — positive + negative cases required)
    - .planning/phases/56-dataview-templates-profile-overrides/56-VALIDATION.md (Wave 0 — this file MUST be the new module created here)
  </read_first>
  <behavior>
    Eight tests (4 classes × 2 cases) — RED at this point because detectors do not exist yet:
    - test_collision_duplicate_rule_id_pattern_in_mapping_rule_templates_detected
    - test_collision_distinct_rule_ids_in_mapping_rule_templates_not_detected
    - test_collision_duplicate_pattern_in_community_templates_detected
    - test_collision_similar_but_distinct_patterns_in_community_templates_not_detected
    - test_collision_duplicate_note_type_in_note_type_templates_detected
    - test_collision_distinct_note_types_in_note_type_templates_not_detected
    - test_collision_dataview_queries_across_extends_chain_detected (uses tmp_path with parent + child YAML; asserts error message contains BOTH source paths)
    - test_collision_dataview_queries_single_source_not_detected
  </behavior>
  <action>
    Create `tests/test_template_overrides.py` from scratch. First line: `from __future__ import annotations`. Module docstring: `"""Phase 56 — CFG-01 (override surface) + CFG-02 (collision matrix) tests."""`. Imports per project convention: stdlib (Path, textwrap), third-party (pytest), then `from graphify.profile import validate_profile, validate_profile_preflight`.

    For schema-only tests (1-6) call `validate_profile(dict)` directly; for cross-chain test 7 use `tmp_path` to write parent.yaml + child.yaml (child has `extends: parent.yaml`), then call `validate_profile_preflight(child_path)` and assert the returned errors list contains exactly one error matching `"dataview_queries.code: collision across composition chain"` AND that BOTH source path strings appear in the error message.

    Use `pytest.mark.parametrize` for the 4-class × 2-case grid if it stays readable; otherwise one function per case (matching existing house style in test_profile.py). Each negative test asserts `not any("collision" in e for e in errors)` for the relevant scope.

    Run: `pytest tests/test_template_overrides.py -x` — ALL EIGHT must FAIL (production detectors not yet implemented). Commit: `test(56-02): RED — collision matrix for CFG-02 (4 classes × 2 cases)`.
  </action>
  <verify>
    <automated>test -f tests/test_template_overrides.py && pytest tests/test_template_overrides.py 2>&1 | grep -E "failed|error" | head -10</automated>
  </verify>
  <acceptance_criteria>
    - `test -f tests/test_template_overrides.py` exits 0
    - `head -1 tests/test_template_overrides.py` is `from __future__ import annotations`
    - `grep -c "test_collision" tests/test_template_overrides.py` returns >= 8
    - `grep -c "test_collision_dataview_queries_across_extends_chain_detected" tests/test_template_overrides.py` returns 1
    - `pytest tests/test_template_overrides.py -x 2>&1 | tail -1` shows non-zero exit (RED)
    - Commit message starts with `test(56-02): RED`
  </acceptance_criteria>
  <done>Eight collision tests RED, file in repo, committed.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: GREEN — implement four detectors + wire into validate_profile_preflight</name>
  <files>graphify/profile.py</files>
  <read_first>
    - graphify/profile.py:682-735 (community_templates: validator — analog for per-rule iteration + error wording style)
    - graphify/profile.py:737-763 (dataview_queries: validator — analog for per-key iteration)
    - graphify/profile.py:validate_profile_preflight (~line 1426+ — wiring location, read 30 lines around it)
    - .planning/phases/56-dataview-templates-profile-overrides/56-PATTERNS.md (§"Four collision detectors" — port verbatim)
    - tests/test_template_overrides.py (the eight RED tests this task must turn GREEN)
  </read_first>
  <behavior>
    All eight tests from Task 1 turn GREEN. No regression in `pytest tests/ -q`. Error wording exactly matches PATTERNS.md format (`"<scope>[<idx>]: duplicate pattern <value> — also defined at <scope>[<other_idx>]"` etc.).
  </behavior>
  <action>
    Add four module-level helpers in `graphify/profile.py` (place near existing validator helpers, after line 763 where dataview_queries: validator ends):

    1. `_detect_mapping_rule_template_collisions(profile: dict) -> list[str]` — per PATTERNS.md §1; iterates `profile.get("mapping_rule_templates") or []`, tracks seen-pattern-strings in a `dict[str, int]`, appends error citing first-seen index when duplicate found.

    2. `_detect_pattern_duplicate_collisions(profile: dict) -> list[str]` — D-56.06 §2; iterates `community_templates`, `mapping_rule_templates`, `note_type_templates` (each independently), detects same-pattern-twice within EACH list. Per-list seen-dict; error format: `"<list_name>[{idx}]: duplicate pattern {pattern!r} — also defined at <list_name>[{first_idx}]"`. NOTE for community_templates: extends Phase 30 validator coverage — currently no duplicate-pattern check exists there; this is additive.

    3. `_detect_note_type_template_collisions(profile: dict) -> list[str]` — D-56.06 §3; same shape as §1, scoped to `note_type_templates` keyed on `pattern` (which is a note_type string from `_KNOWN_NOTE_TYPES`).

    4. `_detect_dataview_collisions(provenance: dict[str, list[Path]]) -> list[str]` — D-56.06 §4 (consumes Plan 01 output); iterates `provenance.items()`, filters keys starting with `"dataview_queries."`, when `len(sources) > 1` appends error `"dataview_queries.{note_type}: collision across composition chain — defined in: {comma-joined paths}"`.

    Wire into `validate_profile_preflight` after the Layer-1 schema-validation section: append `errors.extend(_detect_*(...))` calls for all four detectors. The provenance arg comes from the same `_resolve_profile_chain` call that already produces `composed`; no new computation.

    Run `pytest tests/test_template_overrides.py -q` — all eight GREEN. Run `pytest tests/ -q` — full suite GREEN. Commit: `feat(56-02): GREEN — four CFG-02 collision detectors`.
  </action>
  <verify>
    <automated>pytest tests/test_template_overrides.py -q 2>&1 | tail -3 && pytest tests/ -q 2>&1 | tail -3</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "def _detect_mapping_rule_template_collisions" graphify/profile.py` returns 1
    - `grep -c "def _detect_pattern_duplicate_collisions" graphify/profile.py` returns 1
    - `grep -c "def _detect_note_type_template_collisions" graphify/profile.py` returns 1
    - `grep -c "def _detect_dataview_collisions" graphify/profile.py` returns 1
    - `grep -c "errors.extend(_detect_dataview_collisions" graphify/profile.py` returns >= 1
    - `pytest tests/test_template_overrides.py -q` exits 0
    - `pytest tests/ -q` exits 0
    - Error wording for cross-chain collision matches `"dataview_queries.{note_type}: collision across composition chain — defined in:"`
    - Commit message starts with `feat(56-02): GREEN`
  </acceptance_criteria>
  <done>Four detectors implemented + wired; collision matrix all green; commit landed.</done>
</task>

</tasks>

<verification>
- `pytest tests/test_template_overrides.py -q` — 8 tests pass
- `pytest tests/ -q` — full suite green
- `grep -c "def _detect_" graphify/profile.py` returns >= 4 (the four new detectors)
- All four detectors return `list[str]`; never raise (verified by reading function bodies — no `raise` statements except defensive isinstance early-returns via `continue`)
</verification>

<success_criteria>
1. CFG-02 §1-4 collision classes detected at preflight time, with deterministic error wording.
2. Cross-chain `dataview_queries.<note_type>` collisions enumerate ALL contributing source paths (consumer of Plan 01's list-shape provenance).
3. Wave 0 file `tests/test_template_overrides.py` exists with collision matrix coverage per D-56.07.
4. ROADMAP CFG-02 success criterion met: "collision matrix encoded in tests" — done.
</success_criteria>

<output>
After completion, create `.planning/phases/56-dataview-templates-profile-overrides/56-02-SUMMARY.md` documenting the four detector signatures, their wiring points in `validate_profile_preflight`, the exact error wording for each collision class, and the test parametrization shape.
</output>
