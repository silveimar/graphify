---
phase: 54-mcp-trace-obsidian-parity
plan: 1
type: tdd
wave: 1
depends_on: []
files_modified:
  - tests/test_concept_code_mcp.py
  - tests/test_serve.py
  - tests/test_capability.py
  - tests/test_concept_code_obsidian.py
  - tests/fixtures/concept_code/vault_parity/code_klass.md
  - tests/fixtures/concept_code/vault_parity/code_subklass.md
  - tests/fixtures/concept_code/vault_parity/code_test_klass.md
  - tests/fixtures/concept_code/vault_parity/moc_authservice.md
  - tests/fixtures/concept_code/vault_parity/moc_tokenstore.md
autonomous: true
requirements: [CGRAPH-03, CGRAPH-04]
tags: [mcp, obsidian, parity, tdd, red]

must_haves:
  truths:
    - "Failing tests committed for every CGRAPH-03/04 truth in the validation table (16 RED tests)"
    - "Phase 53 round_trip.json fixture is reused unchanged"
    - "vault_parity/ fixture directory exists with 5 expected per-relation section bodies"
    - "Running `pytest tests/test_concept_code_mcp.py tests/test_serve.py tests/test_capability.py tests/test_concept_code_obsidian.py -x` produces controlled failures that reference the missing production behavior (not import errors or fixture errors)"
  artifacts:
    - path: "tests/test_concept_code_obsidian.py"
      provides: "7 RED parity/section tests (forward, backward, count, canonical order, empty-suppress, idempotence, MOC inverse)"
      min_lines: 120
    - path: "tests/fixtures/concept_code/vault_parity/code_klass.md"
      provides: "Expected per-relation forward sections for k_klass (Implements + Realizes only)"
    - path: "tests/fixtures/concept_code/vault_parity/code_subklass.md"
      provides: "Expected per-relation forward section for k_subklass (Instantiates only)"
    - path: "tests/fixtures/concept_code/vault_parity/code_test_klass.md"
      provides: "Expected per-relation forward section for t_test_klass (Tests only)"
    - path: "tests/fixtures/concept_code/vault_parity/moc_authservice.md"
      provides: "Expected inverse sections for c_concept MOC (Implemented by / Documented by / Tested by / Instantiated by)"
    - path: "tests/fixtures/concept_code/vault_parity/moc_tokenstore.md"
      provides: "Expected inverse section for c_concept2 MOC (Realized by only)"
  key_links:
    - from: "tests/test_concept_code_obsidian.py"
      to: "tests/fixtures/concept_code/round_trip.json"
      via: "json.loads + build_from_json"
      pattern: "round_trip\\.json"
    - from: "tests/test_concept_code_obsidian.py"
      to: "tests/fixtures/concept_code/vault_parity/"
      via: "Path(__file__).parent / 'fixtures/concept_code/vault_parity'"
      pattern: "vault_parity"
    - from: "tests/test_concept_code_mcp.py"
      to: "graphify.serve._run_concept_code_hops"
      via: "import + call with relations kw"
      pattern: "relations="
---

<objective>
Wave 1 (RED): Land all failing tests + fixture artifacts for Phase 54 BEFORE any production code changes. This is the TDD anchor — every CGRAPH-03/04 truth in 54-RESEARCH.md §"Phase Requirements → Test Map" gets a named, executable test that fails on `main`. Production waves (Plans 02/03/04) only flip these to GREEN.

Purpose: Lock the contract surface and parity assertions in pytest BEFORE implementation, so the helper renames, payload shim, entity_trace extension, and Obsidian body-slot populators cannot regress silently.

Output: 16 new failing tests across 4 test files + 5 expected-content fixture files under `tests/fixtures/concept_code/vault_parity/`. Reuses Phase 53's `round_trip.json` (8 edges → 5 distinct concept↔code edges after orientation/merge per RESEARCH §"Test Corpus Suggestion").
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/54-mcp-trace-obsidian-parity/54-CONTEXT.md
@.planning/phases/54-mcp-trace-obsidian-parity/54-RESEARCH.md
@.planning/phases/53-concept-code-schema-build-merge/53-CONTEXT.md
@tests/fixtures/concept_code/round_trip.json
@tests/test_concept_code_mcp.py
@tests/test_serve.py
@CLAUDE.md

<interfaces>
<!-- Read these to align test idioms with existing patterns -->

From graphify/build.py (Phase 53 — locked):
- `build_from_json(extraction: dict) -> nx.Graph` — produces `_src`/`_tgt` orientation
- `CONCEPT_CODE_RELATIONS` tuple — canonical 5

From graphify/serve.py (target signatures AFTER Plans 02/03):
- `_run_concept_code_hops(G, alias_map, arguments)` — `arguments["relations"]: list[str] | None`,
  default behavior when key absent ⇔ `["implements"]`. Returns dict with keys
  `traversal_steps: int`, `steps_by_relation: dict[str,int]`, `relations: list[str]` (sorted),
  and CONDITIONAL `implements_traversal_steps: int` only when `requested_relations == frozenset({"implements"})`.
- `_run_entity_trace(G, out_dir, alias_map, arguments)` — `arguments["include_concept_code"]: bool` default False.
  When True: meta gains `concept_code_reachable: list[str]` and `concept_code_steps_by_relation: dict[str,int]`.

From graphify/export.py:
- `to_obsidian(G, communities, str(out_dir))` — entry point.

From tests/fixtures/concept_code/round_trip.json (Phase 53):
- 8 raw edges → after `_normalize_concept_code_edges` + merge: 5 distinct concept↔code edges
  (k_klass-implements-c_concept, k_subklass-instantiates-c_concept, k_klass-realizes-c_concept2,
   t_test_klass-tests-c_concept, d_readme-documents-c_concept) + 1 contains edge
- Nodes: k_klass, k_subklass (file_type=code), t_test_klass (file_type=code), d_readme (file_type=document), c_concept, c_concept2 (file_type=rationale)
</interfaces>

<read_first>
- 54-CONTEXT.md §"Implementation Decisions" (D-54.01..14) — every test name/assertion must trace to a decision
- 54-RESEARCH.md §"Validation Architecture > Phase Requirements → Test Map" — names exactly 16 tests
- 54-RESEARCH.md §"Test Corpus Suggestion" — fixture content guidance
- 54-RESEARCH.md §"Code Examples > Verified pattern: parity test idiom" — copy this idiom
- tests/test_concept_code_mcp.py existing patterns (assert envelope keys, alias_map shape)
- tests/test_serve.py existing entity_trace tests (so new ones don't duplicate)
- tests/fixtures/concept_code/round_trip.json — count edges per relation to derive expected counts
</read_first>
</context>

<tasks>

<task type="tdd" tdd="true">
  <name>Task 1: Add 9 failing MCP tests (concept_code_hops widening + entity_trace + capability schema)</name>
  <files>
    tests/test_concept_code_mcp.py,
    tests/test_serve.py,
    tests/test_capability.py
  </files>
  <read_first>
    - graphify/serve.py:1952-2308 (current entity_trace + concept_code_hops)
    - graphify/mcp_tool_registry.py:270-330 (tool inputSchema)
    - tests/test_concept_code_mcp.py (full — see existing test idioms for envelope assertions)
    - tests/test_serve.py (search for `entity_trace` to align with existing test scaffolding)
    - tests/test_capability.py (search for `concept_code_hops` and `entity_trace` schema assertions)
    - 54-RESEARCH.md §"Code Examples > Verified pattern: payload shim" — exact JSON shape
  </read_first>
  <behavior>
    Add these 9 named tests (RED — must fail with assertion errors, not import/syntax errors):

    **Return-shape contract (verified at serve.py:1952-2308):** Both `_run_entity_trace` and `_run_concept_code_hops` return a SENTINEL-WRAPPED STRING of the form `text_body + QUERY_GRAPH_META_SENTINEL + json.dumps(meta)`. Tests MUST parse the meta dict via the existing idiom (already used in tests/test_concept_code_mcp.py:31-33):

    ```python
    from graphify.serve import QUERY_GRAPH_META_SENTINEL
    body = _run_concept_code_hops(G, alias_map, arguments)        # returns str
    text_part, sep, meta_part = body.partition(QUERY_GRAPH_META_SENTINEL)
    assert sep                                                    # sentinel present
    meta = json.loads(meta_part)                                  # now meta is a dict
    # ...then assert on meta[...] keys
    ```

    Every `meta[...]` assertion in the 9 tests below MUST go through this parse step. Do NOT assume the helper returns a dict — it does not. The error envelopes in Plan 02 (`{"status": "error", ...}`) ALSO emit as sentinel-wrapped strings ("" + SENTINEL + json.dumps(meta)) — same parse idiom applies.

    **Argument-key contract:** Both helpers read `arguments.get("entity", "")` (NOT `start_id`, NOT `entity_id`). The MCP `inputSchema` for both tools declares the property name as `"entity"` (verified at mcp_tool_registry.py:282-323). All 9 tests use `arguments={"entity": "<value>", ...}`.


    In tests/test_concept_code_mcp.py (extend, don't create — file exists per Phase 47):
    1. `test_concept_code_hops_default_relations` — call `_run_concept_code_hops` with no `relations` key in arguments; assert `meta["relations"] == ["implements"]` AND `meta["traversal_steps"] >= 0` AND `meta["steps_by_relation"] == {"implements": meta["traversal_steps"]}` AND `meta["implements_traversal_steps"] == meta["traversal_steps"]` (shim active).
    2. `test_concept_code_hops_unknown_relation_errors` — pass `arguments={"relations": ["bogus"], "entity": "Klass"}`; assert returned envelope has `status == "error"` AND error string contains `"bogus"` AND lists allowed values.
    3. `test_concept_code_hops_empty_relations_errors` — pass `arguments={"relations": [], "entity": "Klass"}`; assert `status == "error"` AND error string mentions "must not be empty".
    4. `test_concept_code_hops_multi_relation_traversal` — load round_trip.json, build graph, call with `relations=["documents", "tests"]` from `c_concept`; assert `meta["steps_by_relation"]` has both keys with positive counts AND `"implements_traversal_steps" not in meta` (shim NOT active for multi-relation).
    5. `test_concept_code_hops_payload_steps_by_relation` — call with all 5 relations, assert `set(meta["steps_by_relation"].keys()) == {"implements","documents","tests","realizes","instantiates"}` AND `meta["traversal_steps"] == sum(meta["steps_by_relation"].values())`.
    6. `test_concept_code_hops_backward_compat_implements_steps_key` — call with `relations=["implements"]` (explicit single-element list, not omitted); assert `meta["implements_traversal_steps"]` present AND equal to `meta["traversal_steps"]`.

    In tests/test_serve.py (extend):
    7. `test_entity_trace_default_excludes_concept_code` — call `_run_entity_trace` with no `include_concept_code` key; assert `"concept_code_reachable" not in meta` AND `"concept_code_steps_by_relation" not in meta`.
    8. `test_entity_trace_includes_concept_code_when_requested` — call with `arguments={"entity": "Klass", "include_concept_code": True}`; assert `meta["concept_code_reachable"]` is list AND `meta["concept_code_steps_by_relation"]` is dict with subset of allowed-relation keys.

    In tests/test_capability.py (extend):
    9. `test_concept_code_hops_schema_includes_relations_and_entity_trace_includes_concept_code` — fetch tool schemas via `build_mcp_tools()`; assert `concept_code_hops` inputSchema properties has `"relations"` with `type: array, items.type: string` AND default `["implements"]`; assert `entity_trace` inputSchema properties has `"include_concept_code"` with `type: boolean, default: false`.
  </behavior>
  <action>
    1. Open `tests/test_concept_code_mcp.py`, add the 6 tests at the bottom (after the last existing test). Reuse the existing parse idiom at lines 31-33:
       ```python
       text_part, sep, meta_part = body.partition(QUERY_GRAPH_META_SENTINEL)
       assert sep
       meta = json.loads(meta_part)
       ```
       Use the existing helper for graph construction (look for `_load_round_trip_graph` or equivalent in the file; if absent, define a module-level helper `def _load_graph()` that loads round_trip.json + `build_from_json`). Use `_run_concept_code_hops` (already imported alongside `QUERY_GRAPH_META_SENTINEL` at the top of the file) and the existing alias_map fixture pattern. All `arguments` dicts use key `"entity"` (NOT `start_id`).
    2. Open `tests/test_serve.py`, add the 2 entity_trace tests. Use the existing `entity_trace` test scaffolding (search for `_run_entity_trace` to find the existing setup pattern). `_run_entity_trace` ALSO returns a sentinel-wrapped string — import `QUERY_GRAPH_META_SENTINEL` from `graphify.serve` and use the same `body.partition(...)` idiom. Argument key is `"entity"` (NOT `entity_id`). Test 8 must use an `entity` value that resolves into `round_trip.json` (e.g. `"Klass"` resolves to node `k_klass`); after parse, the resolved id appears as `meta["entity_id"]` (output key, distinct from the input arg key).
    3. Open `tests/test_capability.py`, add the schema test. Import `build_mcp_tools` from `graphify.mcp_tool_registry`. Walk the tool list, find `concept_code_hops` and `entity_trace` by name, assert their `inputSchema["properties"]` shape. Note: both tools' existing `entity` property name is preserved — Plan 02 only ADDS a `relations` property; Plan 03 only ADDS an `include_concept_code` property.
    4. Each test must reference the matching D-54.XX in a one-line `# D-54.XX:` comment above the assertion to satisfy traceability for plan-checker.
    5. DO NOT modify production code. Tests must FAIL when run on the current main branch.
  </action>
  <verify>
    <automated>pytest tests/test_concept_code_mcp.py::test_concept_code_hops_default_relations tests/test_concept_code_mcp.py::test_concept_code_hops_unknown_relation_errors tests/test_concept_code_mcp.py::test_concept_code_hops_empty_relations_errors tests/test_concept_code_mcp.py::test_concept_code_hops_multi_relation_traversal tests/test_concept_code_mcp.py::test_concept_code_hops_payload_steps_by_relation tests/test_concept_code_mcp.py::test_concept_code_hops_backward_compat_implements_steps_key tests/test_serve.py::test_entity_trace_default_excludes_concept_code tests/test_serve.py::test_entity_trace_includes_concept_code_when_requested tests/test_capability.py::test_concept_code_hops_schema_includes_relations_and_entity_trace_includes_concept_code 2>&1 | tail -40</automated>
  </verify>
  <acceptance_criteria>
    - `pytest --collect-only tests/test_concept_code_mcp.py 2>&1 | grep -c "test_concept_code_hops_default_relations\|test_concept_code_hops_unknown_relation_errors\|test_concept_code_hops_empty_relations_errors\|test_concept_code_hops_multi_relation_traversal\|test_concept_code_hops_payload_steps_by_relation\|test_concept_code_hops_backward_compat_implements_steps_key"` returns at least 6
    - `pytest --collect-only tests/test_serve.py 2>&1 | grep -c "test_entity_trace_default_excludes_concept_code\|test_entity_trace_includes_concept_code_when_requested"` returns at least 2
    - `pytest --collect-only tests/test_capability.py 2>&1 | grep -c "test_concept_code_hops_schema_includes_relations_and_entity_trace_includes_concept_code"` returns at least 1
    - `pytest tests/test_concept_code_mcp.py::test_concept_code_hops_default_relations -x` exits NON-ZERO (fails — RED)
    - `pytest tests/test_serve.py::test_entity_trace_includes_concept_code_when_requested -x` exits NON-ZERO (fails — RED)
    - `grep -c "D-54\\." tests/test_concept_code_mcp.py` returns at least 6 (decision-traceability comments)
    - `grep -c "D-54\\." tests/test_serve.py` returns at least 2
  </acceptance_criteria>
  <done>9 RED MCP/capability tests committed, all fail with semantic assertion failures (not import errors).</done>
</task>

<task type="tdd" tdd="true">
  <name>Task 2: Create vault_parity fixture directory + 7 failing Obsidian parity tests</name>
  <files>
    tests/test_concept_code_obsidian.py,
    tests/fixtures/concept_code/vault_parity/code_klass.md,
    tests/fixtures/concept_code/vault_parity/code_subklass.md,
    tests/fixtures/concept_code/vault_parity/code_test_klass.md,
    tests/fixtures/concept_code/vault_parity/moc_authservice.md,
    tests/fixtures/concept_code/vault_parity/moc_tokenstore.md
  </files>
  <read_first>
    - tests/fixtures/concept_code/round_trip.json — the 8 raw edges; map to 5 post-orient/merge edges per RESEARCH §"Test Corpus Suggestion"
    - tests/test_export.py:266-294 — existing `to_obsidian` test idioms (build_from_json + communities dict + tmp_path)
    - 54-RESEARCH.md §"Code Examples > Verified pattern: parity test idiom" — copy the regex extraction helper
    - 54-RESEARCH.md §"Expected per-relation section content" — exact section content per fixture file
    - 54-CONTEXT.md D-54.07 / D-54.08 — canonical section ordering (Implements/Documents/Tests/Realizes/Instantiates and inverses)
    - graphify/templates.py:60-100 (sentinel wrap helpers `_wrap_sentinel`)
  </read_first>
  <behavior>
    Add these 7 named tests in NEW file `tests/test_concept_code_obsidian.py`:

    1. `test_code_note_per_relation_sections_canonical_order` — run `to_obsidian` on round_trip graph; locate the CODE note for `k_klass`; extract H2 headers from body; assert observed forward headers, in their order of appearance, are a subsequence of the canonical list `["Implements","Documents","Tests","Realizes","Instantiates"]` (skips allowed for empty-suppression).
    2. `test_concept_moc_inverse_sections_canonical_order` — locate the concept MOC for community containing `c_concept`; extract H2 headers; assert subsequence of canonical inverse list `["Implemented by","Documented by","Tested by","Realized by","Instantiated by"]`.
    3. `test_empty_relation_section_suppressed` — `k_subklass` has only `instantiates` edges; assert its CODE note body contains `"## Instantiates"` AND does NOT contain `"## Implements"`, `"## Documents"`, `"## Tests"`, or `"## Realizes"`.
    4. `test_forward_parity_edges_to_wikilinks` — for each concept↔code edge in the graph where `_src` is a code/test/document node, verify that node's note contains a `[[...]]` wikilink under the matching forward H2 section pointing at the target concept's MOC.
    5. `test_backward_parity_wikilinks_to_edges` — for each `## Implements` / `## Documents` / `## Tests` / `## Realizes` / `## Instantiates` H2 section in any vault file, every wikilink under it must correspond to a graph edge with the matching `relation` attribute (active form on code-side files, passive form on MOC-side files).
    6. `test_per_relation_count_parity` — implement `_count_graph_edges_by_relation(G)` and `_count_vault_wikilinks_by_relation(vault_dir, sections)` per RESEARCH §"Verified pattern: parity test idiom"; assert `forward_counts[rel] == graph_counts[rel]` AND `inverse_counts[rel] == graph_counts[rel]` for each of the 5 relations.
    7. `test_round_trip_per_relation_sections_idempotent` — run `to_obsidian` to `tmp_path/v1`; run again to `tmp_path/v2`; for each MD file in both, assert byte-equal content within the `concept_code_relations` sentinel block (use `_wrap_sentinel` start/end markers as boundaries).

    The 5 fixture files contain JUST the per-relation section content (no frontmatter, no other body) as plain markdown — see RESEARCH §"Expected per-relation section content" for exact lines. Tests load them with `Path(__file__).parent / "fixtures/concept_code/vault_parity/<name>.md"` and compare structurally against extracted sections from the actual export, treating wikilink targets as set-equal (filenames vary by `convention`).
  </behavior>
  <action>
    1. Create `tests/test_concept_code_obsidian.py` starting with `from __future__ import annotations`. Module imports: `import json, re; from pathlib import Path; import pytest; from graphify.build import build_from_json; from graphify.export import to_obsidian`.
    2. Add module-level constants:
       ```python
       _ALL_RELATIONS = ("implements", "documents", "tests", "realizes", "instantiates")
       _FORWARD_SECTIONS = {"Implements":"implements","Documents":"documents","Tests":"tests","Realizes":"realizes","Instantiates":"instantiates"}
       _INVERSE_SECTIONS = {"Implemented by":"implements","Documented by":"documents","Tested by":"tests","Realized by":"realizes","Instantiated by":"instantiates"}
       _FIXTURE_ROOT = Path(__file__).parent / "fixtures" / "concept_code"
       _ROUND_TRIP = _FIXTURE_ROOT / "round_trip.json"
       _VAULT_PARITY = _FIXTURE_ROOT / "vault_parity"
       ```
    3. Add helper `def _build_test_vault(tmp_path) -> tuple[Path, nx.Graph]:` that loads round_trip.json, calls `build_from_json`, constructs a `communities` dict (use single community {0: [all node ids]} OR mirror what test_export.py does — pick whichever surfaces both rationale nodes as MOCs), calls `to_obsidian(G, communities, str(tmp_path/'vault'))`, returns `(vault_dir, G)`.
    4. Add helper `def _extract_section_wikilinks(text: str, header: str) -> list[str]:` using regex `rf"^## {re.escape(header)}\\n((?:- \\[\\[.+?\\]\\]\\n?)+)"` (multiline) returning the wikilink target strings (strip `[[` and `]]`, split on `|` taking the link target before alias).
    5. Add `_count_graph_edges_by_relation(G)` and `_count_vault_wikilinks_by_relation(vault_dir, sections)` per RESEARCH idiom.
    6. Implement the 7 tests per `<behavior>` above. Each test gets a `# D-54.XX:` comment referencing the governing decision (D-54.07, D-54.08, D-54.11, D-54.12, etc.).
    7. Create the 5 fixture files. Use the EXACT content from RESEARCH §"Expected per-relation section content" verbatim for `code_klass.md`, `code_subklass.md`, `code_test_klass.md`, `moc_authservice.md`, `moc_tokenstore.md`. Tests should NOT regex-match exact filenames in wikilinks (those depend on `convention`/`build_code_filename_stems`); instead match the H2 header presence/absence and aliases via `|alias]]` suffix, OR compare header set inclusion only.
    8. DO NOT modify production code. All 7 tests must FAIL on current main.
  </action>
  <verify>
    <automated>pytest tests/test_concept_code_obsidian.py -x 2>&1 | tail -30</automated>
  </verify>
  <acceptance_criteria>
    - `ls tests/fixtures/concept_code/vault_parity/ | wc -l | tr -d ' '` returns at least 5
    - `pytest --collect-only tests/test_concept_code_obsidian.py 2>&1 | grep -c "test_code_note_per_relation_sections_canonical_order\|test_concept_moc_inverse_sections_canonical_order\|test_empty_relation_section_suppressed\|test_forward_parity_edges_to_wikilinks\|test_backward_parity_wikilinks_to_edges\|test_per_relation_count_parity\|test_round_trip_per_relation_sections_idempotent"` returns at least 7
    - `pytest tests/test_concept_code_obsidian.py::test_per_relation_count_parity -x` exits NON-ZERO
    - `pytest tests/test_concept_code_obsidian.py::test_round_trip_per_relation_sections_idempotent -x` exits NON-ZERO
    - `grep -c "## Implements" tests/fixtures/concept_code/vault_parity/code_klass.md` returns at least 1
    - `grep -c "## Realized by" tests/fixtures/concept_code/vault_parity/moc_tokenstore.md` returns at least 1
    - `grep -c "from __future__ import annotations" tests/test_concept_code_obsidian.py` returns 1
    - `grep -c "D-54\\." tests/test_concept_code_obsidian.py` returns at least 7
    - Phase 53 fixture untouched: `git diff --name-only main HEAD -- tests/fixtures/concept_code/round_trip.json` returns empty
  </acceptance_criteria>
  <done>7 RED Obsidian parity tests committed; 5 expected-section fixtures land under vault_parity/; round_trip.json untouched.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| pytest fixture loader → graph builder | round_trip.json is a checked-in test fixture; treated as trusted input by `build_from_json`. No new boundary introduced by this plan. |
| Test code → tmp_path | Vault output confined to pytest `tmp_path`; matches `security.py` path-confinement convention. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-54-01 | Tampering | round_trip.json fixture | accept | Fixture is checked into repo + read-only at test time; no Plan 01 mutations (acceptance criterion enforces). |
| T-54-02 | Information Disclosure | tmp_path vault output | accept | pytest tmp_path is isolated, garbage-collected; no PII in fixtures (synthetic node ids only: k_klass, c_concept, etc.). |
| T-54-03 | Denial of Service | Test runtime explosion | mitigate | All tests use the 6-node round_trip.json fixture. No looping over real corpora. Single `to_obsidian` call per test (cached via tmp_path session scope where helpful). |
</threat_model>

<verification>
- All 16 new tests collect without import/syntax errors: `pytest --collect-only tests/test_concept_code_mcp.py tests/test_concept_code_obsidian.py tests/test_serve.py tests/test_capability.py` exits 0.
- All 16 new tests FAIL semantically (assertion / `KeyError` / `AttributeError`, not `ImportError` / `SyntaxError`): `pytest tests/test_concept_code_mcp.py::test_concept_code_hops_default_relations tests/test_concept_code_obsidian.py::test_per_relation_count_parity tests/test_serve.py::test_entity_trace_includes_concept_code_when_requested -x` produces failures referencing missing keys / wrong shapes.
- Existing Phase 53 tests UNAFFECTED: `pytest tests/test_concept_code_mcp.py -q --deselect <new tests>` passes (Phase 47 backward-compat preserved at test-collection level).
</verification>

<success_criteria>
- 16 named RED tests committed across 4 test files
- 5 vault_parity/ fixture files committed
- All RED tests fail for the right reasons (semantic assertions about missing prod behavior, not infrastructure errors)
- Each test has a `# D-54.XX:` traceability comment
- No production code modified by this plan
</success_criteria>

<output>
Create `.planning/phases/54-mcp-trace-obsidian-parity/54-01-SUMMARY.md` listing: each test name with its target D-54.XX, the 5 fixture files with their target relations, and the exact `pytest` command that surfaces all 16 RED failures.
</output>
