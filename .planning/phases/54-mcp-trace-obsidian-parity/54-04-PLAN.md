---
phase: 54-mcp-trace-obsidian-parity
plan: 4
type: tdd
wave: 4
depends_on: [54-01]
files_modified:
  - graphify/templates.py
  - graphify/builtin_templates/moc.md
autonomous: true
requirements: [CGRAPH-04]
tags: [obsidian, export, parity, green]

must_haves:
  truths:
    - "to_obsidian() emits per-relation H2 sections in CODE notes in canonical order: Implements, Documents, Tests, Realizes, Instantiates"
    - "to_obsidian() emits inverse per-relation H2 sections in concept MOC notes in canonical order: Implemented by, Documented by, Tested by, Realized by, Instantiated by"
    - "Empty per-relation sections are suppressed (no `## Tests` followed by zero items)"
    - "All wikilinks come from G.edges (single source of truth) — no recomputation from labels/text"
    - "Per-relation block is wrapped with _wrap_sentinel('concept_code_relations', ...) for round-trip preservation"
    - "moc.md builtin template gains a single ${body} slot between ${members_section} and ${sub_communities_callout} (A1 carve-out adopted)"
    - "Two consecutive to_obsidian runs on the same graph produce byte-identical body content (idempotent)"
    - "All 7 RED Obsidian parity tests from Plan 01 turn GREEN; existing test_export.py round-trip tests still pass"
    - "Forward parity, backward parity, AND per-relation count parity all hold (D-54.12 — three-assertion bar)"
  artifacts:
    - path: "graphify/templates.py"
      provides: "_build_concept_code_sections_for_code, _build_concept_code_sections_for_moc helpers"
      contains: "_build_concept_code_sections_for_code"
    - path: "graphify/templates.py"
      provides: "Body slot population in render_note (note_type=='code') and _render_moc_like (concept MOCs)"
      contains: 'substitution_ctx["body"]'
    - path: "graphify/builtin_templates/moc.md"
      provides: "Single-line ${body} slot addition (A1 carve-out)"
      contains: "${body}"
  key_links:
    - from: "graphify/templates.py:render_note"
      to: "G.edges (concept↔code edges with _src/_tgt)"
      via: "Edge iteration in _build_concept_code_sections_for_code"
      pattern: "G\\.edges\\("
    - from: "graphify/templates.py:_render_moc_like"
      to: "graphify/templates.py:_build_concept_code_sections_for_moc"
      via: "substitution_ctx body assignment"
      pattern: "concept_code_relations"
    - from: "graphify/templates.py per-relation builders"
      to: "graphify/templates.py:_emit_wikilink + _wrap_sentinel"
      via: "Reuse existing helpers (D-54.09 / D-54.11)"
      pattern: "_emit_wikilink"
---

<objective>
Wave 4 (GREEN, Obsidian export parity): Populate the `${body}` slot in `code.md` and `moc.md` builtin templates with per-relation H2 sections sourced directly from `G.edges`. Add the `${body}` slot to `moc.md` (A1 carve-out — single-line additive edit, NOT a new template per D-54.10's intent). Implement section builders that respect canonical ordering, empty-suppression, sentinel-wrapping, and deterministic intra-section sorting. Turn the 7 RED `test_concept_code_obsidian.py` tests GREEN without regressing any existing `test_export.py` round-trip test.

Purpose: CGRAPH-04 satisfaction — the vault is no longer a parallel data source that can drift from the graph; it is a render of the graph's typed concept↔code edges, and the parity test seals the contract bidirectionally.

Output: Two new helpers in `templates.py`; body-slot population at the two render sites; `${body}` slot added to `moc.md`; round-trip-safe via `_wrap_sentinel("concept_code_relations", ...)`.

A1 Carve-out (RESOLVED — ADOPT): D-54.10 forbids "new templates" and "profile changes" — adding a `${body}` slot to the existing `moc.md` builtin template is a 1-line additive edit (parallel to `code.md`), NOT a new template file or profile knob. Plan 04 adopts this carve-out and Plan 05 documents it in 54-VERIFICATION.md per D-54.06.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/54-mcp-trace-obsidian-parity/54-CONTEXT.md
@.planning/phases/54-mcp-trace-obsidian-parity/54-RESEARCH.md
@.planning/phases/54-mcp-trace-obsidian-parity/54-01-SUMMARY.md
@graphify/templates.py
@graphify/export.py
@graphify/builtin_templates/code.md
@graphify/builtin_templates/moc.md
@graphify/naming.py
@CLAUDE.md

<interfaces>
graphify/builtin_templates/code.md (current — has ${body} slot already):
```
${frontmatter}
# ${label}

${wayfinder_callout}

${body}

${connections_callout}

${metadata_callout}
```

graphify/builtin_templates/moc.md (current — NO ${body} slot):
```
${frontmatter}
# ${label}

${wayfinder_callout}

${members_section}

${sub_communities_callout}

${dataview_block}

${metadata_callout}
```

graphify/templates.py existing helpers (DO NOT recreate):
- `_wrap_sentinel(name: str, content: str) -> str` (line ~68) — wraps with start/end sentinel markers
- `_emit_wikilink(label: str, convention) -> str` (line ~663) — sanitised `[[fname|alias]]`
- `_sanitize_wikilink_alias(s: str) -> str` (line ~654)
- `render_note(...)` (line ~1043) with `substitution_ctx` dict including `"body": ""` (line ~1181)
- `_render_moc_like(...)` (line ~1380) with substitution_ctx (line ~1431) — currently no `body` key

graphify/naming.py:
- `resolve_concept_names(...) -> dict[community_id → ConceptName]`
- `ConceptName` NamedTuple: `(community_id, title, filename_stem, source, signature, reason)`

graphify/export.py:
- `to_obsidian(G, communities, str(out_dir))` (line ~547) — orchestrates everything; calls `resolve_concept_names` (line ~629), `build_code_filename_stems` (line ~709), and `render_note`/`_render_moc_like`.

Phase 53 edge orientation (DO NOT re-orient):
- `_src` is always the code/test/document side
- `_tgt` is always the concept side
- Edges iterate via `G.edges(data=True)` returning `(u, v, data)`; data has `relation`, `_src`, `_tgt`
</interfaces>

<read_first>
- graphify/templates.py:60-100 (sentinel helpers)
- graphify/templates.py:253-310 (existing edge-record canonical sort idiom — `_build_edge_records`)
- graphify/templates.py:660-695 (`_emit_wikilink`, `_sanitize_wikilink_alias`)
- graphify/templates.py:1043-1212 (`render_note` body composition; substitution_ctx around line 1167-1193)
- graphify/templates.py:1380-1465 (`_render_moc_like`; substitution_ctx around line 1431)
- graphify/builtin_templates/code.md (the existing `${body}` slot — already there, just empty)
- graphify/builtin_templates/moc.md (CURRENT — needs `${body}` added)
- graphify/export.py:540-840 (orchestration: `resolve_concept_names`, classification, render dispatch)
- graphify/naming.py:459-540 (`resolve_concept_names` + ConceptName NamedTuple)
- 54-RESEARCH.md §"Code Examples > Verified pattern: Body substitution in templates" (the section builder skeleton)
- 54-RESEARCH.md §"Common Pitfalls > Pitfalls 1, 5" (round-trip + non-determinism guidance)
- 54-RESEARCH.md §"Risks & Landmines > OQ3, OQ4" (where to render, label resolution)
- 54-CONTEXT.md D-54.07 / D-54.08 / D-54.09 / D-54.10 / D-54.11
</read_first>
</context>

<tasks>

<task type="tdd" tdd="true">
  <name>Task 1: Implement _build_concept_code_sections_for_code; populate ${body} in render_note for code notes</name>
  <files>graphify/templates.py</files>
  <read_first>
    - graphify/templates.py:60-100 (_wrap_sentinel)
    - graphify/templates.py:660-695 (_emit_wikilink, _sanitize_wikilink_alias)
    - graphify/templates.py:1043-1212 (render_note + substitution_ctx assembly)
    - graphify/builtin_templates/code.md (the ${body} slot is already present)
    - 54-RESEARCH.md §"Code Examples > Verified pattern: Body substitution"
    - 54-CONTEXT.md D-54.07 (canonical order: Implements, Documents, Tests, Realizes, Instantiates)
  </read_first>
  <action>
    1. In `graphify/templates.py`, add a module-level constant near the top (after existing constants):
       ```python
       _CONCEPT_CODE_FORWARD_SECTIONS: tuple[tuple[str, str], ...] = (
           ("implements",   "Implements"),
           ("documents",    "Documents"),
           ("tests",        "Tests"),
           ("realizes",     "Realizes"),
           ("instantiates", "Instantiates"),
       )
       _CONCEPT_CODE_INVERSE_SECTIONS: tuple[tuple[str, str], ...] = (
           ("implements",   "Implemented by"),
           ("documents",    "Documented by"),
           ("tests",        "Tested by"),
           ("realizes",     "Realized by"),
           ("instantiates", "Instantiated by"),
       )
       _CONCEPT_CODE_SENTINEL = "concept_code_relations"
       ```
    2. Implement `_build_concept_code_sections_for_code(G, node_id, classification_context, convention) -> str`. Algorithm (per RESEARCH §"Pattern 2"):
       - If `node_id not in G`: return `""` (no sections).
       - Initialize `by_relation: dict[str, list[str]] = {rel: [] for rel, _ in _CONCEPT_CODE_FORWARD_SECTIONS}`.
       - Iterate `G.edges(node_id, data=True)`; for each `(u, v, data)`:
         - `rel = data.get("relation")`; if `rel not in by_relation`: skip.
         - `src_m = data.get("_src"); tgt_m = data.get("_tgt")`
         - Forward section attribution: include only when `isinstance(src_m, str) and src_m == node_id` (this CODE note is the code-side endpoint per Phase 53 orientation).
         - `target = v if u == node_id else u`.
         - Resolve target label: prefer `classification_context` MOC name lookup if `target` is a rationale node living in a known community; otherwise fall back to `G.nodes[target].get("label", target)`. The `classification_context` dict already carries `community_name` per node id at this call site (verified at templates.py:1167+) — reuse, do not reinvent.
         - Append `_emit_wikilink(target_label, convention)` to `by_relation[rel]`.
       - Build output parts: for each `(rel, header)` in `_CONCEPT_CODE_FORWARD_SECTIONS`:
         - If `not by_relation[rel]`: continue (D-54.07 empty-suppression).
         - Append `f"## {header}"`.
         - Append `f"- {link}"` for `link in sorted(set(by_relation[rel]))` (deterministic — Pitfall 5).
         - Append `""` (blank line between sections).
       - If `parts` is empty: return `""`.
       - Return `_wrap_sentinel(_CONCEPT_CODE_SENTINEL, "\n".join(parts).rstrip())`.
    3. In `render_note` (templates.py:~1043), find the `substitution_ctx` assembly (around line 1167-1193). Find the line `"body": "",`. Replace with:
       ```python
       "body": (
           _build_concept_code_sections_for_code(G, node_id, classification_context, convention)
           if note_type == "code" else ""
       ),
       ```
       (G is in scope at render_note per RESEARCH §"Architectural Responsibility Map".)
    4. Do NOT modify `code.md` (it already has `${body}`). Do NOT touch `moc.md` in this task — Task 2 handles MOCs.
    5. Verify deterministic output: every `for ... in G.edges(node_id)` iteration must be followed by canonical sort. The `sorted(set(items))` pass inside the section loop guarantees idempotence (Pitfall 5).
  </action>
  <verify>
    <automated>pytest tests/test_concept_code_obsidian.py::test_code_note_per_relation_sections_canonical_order tests/test_concept_code_obsidian.py::test_empty_relation_section_suppressed tests/test_concept_code_obsidian.py::test_forward_parity_edges_to_wikilinks tests/test_export.py -x 2>&1 | tail -20</automated>
  </verify>
  <acceptance_criteria>
    - `pytest tests/test_concept_code_obsidian.py::test_code_note_per_relation_sections_canonical_order -x` exits 0
    - `pytest tests/test_concept_code_obsidian.py::test_empty_relation_section_suppressed -x` exits 0
    - `pytest tests/test_concept_code_obsidian.py::test_forward_parity_edges_to_wikilinks -x` exits 0
    - Existing test_export.py round-trip suite passes: `pytest tests/test_export.py -x` exits 0
    - `grep -c "def _build_concept_code_sections_for_code(" graphify/templates.py` returns 1
    - `grep -c "_CONCEPT_CODE_SENTINEL" graphify/templates.py` returns at least 2 (definition + use)
    - `grep -c "concept_code_relations" graphify/templates.py` returns at least 1 (sentinel name)
    - `grep -v '^#' graphify/templates.py | grep -c "_wrap_sentinel(_CONCEPT_CODE_SENTINEL"` returns at least 1 (sentinel actually applied)
    - `grep -c "Implements\\|Documents\\|Tests\\|Realizes\\|Instantiates" graphify/templates.py` returns at least 5 (one per canonical header)
  </acceptance_criteria>
  <done>CODE-note forward sections render via templates.py body slot; 3 of 7 Obsidian RED tests turn GREEN; existing test_export.py untouched.</done>
</task>

<task type="tdd" tdd="true">
  <name>Task 2: Add ${body} slot to moc.md; implement _build_concept_code_sections_for_moc; populate body in _render_moc_like for concept MOCs</name>
  <files>graphify/builtin_templates/moc.md, graphify/templates.py</files>
  <read_first>
    - graphify/builtin_templates/moc.md (full — 11 lines)
    - graphify/templates.py:1380-1465 (_render_moc_like + substitution_ctx)
    - graphify/naming.py:459-540 (ConceptName + resolve_concept_names) — for label resolution
    - graphify/export.py:540-840 (how communities + classification_context flow into _render_moc_like)
    - 54-CONTEXT.md D-54.08 (inverse canonical order: Implemented by → Documented by → Tested by → Realized by → Instantiated by)
    - 54-RESEARCH.md §"Risks & Landmines > OQ3, OQ4" (the moc.md edit, label-resolution path)
    - 54-RESEARCH.md §"Assumptions Log > A1" — the carve-out adoption rationale
  </read_first>
  <action>
    1. Edit `graphify/builtin_templates/moc.md` — insert `${body}` between `${members_section}` and `${sub_communities_callout}`. Result EXACTLY:
       ```
       ${frontmatter}
       # ${label}

       ${wayfinder_callout}

       ${members_section}

       ${body}

       ${sub_communities_callout}

       ${dataview_block}

       ${metadata_callout}
       ```
       This is the A1 carve-out. NO other template files are touched.
    2. Implement `_build_concept_code_sections_for_moc(G, community_id, community_members, classification_context, convention) -> str` in `graphify/templates.py`. Algorithm:
       - Initialize `by_relation: dict[str, list[str]] = {rel: [] for rel, _ in _CONCEPT_CODE_INVERSE_SECTIONS}`.
       - For each node_id in `community_members` whose `G.nodes[node_id].get("file_type") == "rationale"` (the concept-side nodes for this MOC):
         - Iterate `G.edges(node_id, data=True)`. For each edge:
           - `rel = data.get("relation")`; if `rel not in by_relation`: skip.
           - `src_m = data.get("_src"); tgt_m = data.get("_tgt")`
           - INVERSE attribution: include only when `isinstance(tgt_m, str) and tgt_m == node_id` (concept is the tgt-side of a code→concept edge per Phase 53 orientation).
           - `target = v if u == node_id else u` (the code/test/document node).
           - Resolve target label: prefer `classification_context.get(target, {}).get("filename_stem")` or the node's `label` attribute (`G.nodes[target].get("label", target)`). Use whatever convention sibling code-note rendering uses for cross-note wikilinks (look at how `connections_callout` is built in templates.py for an analogous pattern; reuse `_emit_wikilink`).
           - Append `_emit_wikilink(target_label, convention)` to `by_relation[rel]`.
       - Render parts in the canonical INVERSE order using `_CONCEPT_CODE_INVERSE_SECTIONS`. Empty-suppression and `sorted(set(items))` identical to the forward builder.
       - Wrap final output with `_wrap_sentinel(_CONCEPT_CODE_SENTINEL, ...)`. Return `""` if empty.
    3. In `_render_moc_like` (templates.py:~1380, substitution_ctx around line 1431), add a `"body"` key to the substitution_ctx dict:
       ```python
       "body": _build_concept_code_sections_for_moc(
           G, community_id, community_members, classification_context, convention,
       ),
       ```
       The dict currently has no `body` key; add it. Since `${body}` slot is now present in moc.md, `string.Template.safe_substitute` will pick it up.
    4. Concept-MOC detection: per RESEARCH §A3, the inverse section builder will naturally return `""` (empty) for any community that has no rationale nodes with concept↔code edges, so no extra `if note_type == "moc"` gate is needed beyond the empty-suppression already in the helper. Document this in the helper's docstring: "Returns empty string for non-concept MOCs by construction".
    5. Verify the merge engine respects the `concept_code_relations` sentinel — Pitfall 1: should be automatic since the merge engine handles all `_wrap_sentinel(...)`-tagged regions per D-67/D-68. Test `test_round_trip_per_relation_sections_idempotent` is the gate.
  </action>
  <verify>
    <automated>pytest tests/test_concept_code_obsidian.py tests/test_export.py -x 2>&1 | tail -25</automated>
  </verify>
  <acceptance_criteria>
    - `pytest tests/test_concept_code_obsidian.py -x` exits 0 (all 7 RED tests now GREEN)
    - `pytest tests/test_export.py -x` exits 0 (no round-trip regression)
    - `grep -c '${body}' graphify/builtin_templates/moc.md` returns 1
    - `grep -c "def _build_concept_code_sections_for_moc(" graphify/templates.py` returns 1
    - `grep -v '^#' graphify/templates.py | grep -c '"body": _build_concept_code_sections_for_moc'` returns at least 1
    - `grep -c "Implemented by\\|Documented by\\|Tested by\\|Realized by\\|Instantiated by" graphify/templates.py` returns at least 5
    - Phase gate (full suite): `pytest tests/ -q 2>&1 | tail -3` shows no new failures vs Phase 53 baseline
  </acceptance_criteria>
  <done>All 7 Obsidian RED tests GREEN; concept MOC inverse sections render via newly added ${body} slot in moc.md; round-trip idempotent; full pytest suite green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| `to_obsidian` → vault filesystem (tmp_path or user-configured out_dir) | Existing boundary; path-confined via security.py to graphify-out/ pattern. No new boundary. |
| `G.edges` → wikilink alias rendering | Untrusted node labels may appear in wikilinks; existing sanitization layer applies. |
| User-edited vault → next `to_obsidian` run | Round-trip: user edits OUTSIDE the sentinel must survive; per-relation block content is server-controlled and refreshed deterministically. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-54-12 | Tampering | Wikilink injection via concept label (`]] {{include}}` etc.) | mitigate | Reuse `_emit_wikilink` (templates.py:663) + `_sanitize_wikilink_alias` (templates.py:654) — strips `]`, `\|`, `\n`, control chars per CR-01 (D-54.09). Tests `test_forward_parity_edges_to_wikilinks` and `test_backward_parity_wikilinks_to_edges` verify wikilinks parse cleanly. |
| T-54-13 | Tampering | Round-trip overwrite of user edits | mitigate | Per-relation block wrapped in `_wrap_sentinel("concept_code_relations", ...)` (templates.py:68 helper). Merge engine D-67/D-68 sentinel-respect already in place; refreshes content INSIDE markers, preserves user content OUTSIDE (Pitfall 1). Test `test_round_trip_per_relation_sections_idempotent` enforces. |
| T-54-14 | Information Disclosure | Concept rationale node labels echoed into wikilinks | accept | Concept names are derived from extraction output; not secret in graphify's threat model. Same risk surface as existing concept-MOC names. |
| T-54-15 | Denial of Service | Section rendering O(deg(node)) — no bound | accept | Real-world `deg(node)` for concept↔code edges bounded by extraction output (typically <50 per node); no new attack vector vs Phase 47 graph rendering. Round_trip.json fixture (6 nodes, 5 edges) exercises the path; production graphs have similar density. |
| T-54-16 | Tampering | Section ordering non-determinism leading to round-trip churn | mitigate | Hardcoded canonical order constants `_CONCEPT_CODE_FORWARD_SECTIONS`, `_CONCEPT_CODE_INVERSE_SECTIONS`. Intra-section `sorted(set(items))`. Test `test_round_trip_per_relation_sections_idempotent` enforces byte-equality across two consecutive runs (Pitfall 5). |
</threat_model>

<verification>
- All 7 Obsidian parity tests GREEN: `pytest tests/test_concept_code_obsidian.py -x` exits 0
- Full export regression suite GREEN: `pytest tests/test_export.py -x` exits 0
- All Plan 01 tests now GREEN (combined with Plans 02/03): `pytest tests/test_concept_code_mcp.py tests/test_serve.py tests/test_capability.py tests/test_concept_code_obsidian.py -x` exits 0
- Full pytest suite green vs Phase 53 baseline (1979 passed): `pytest tests/ -q` matches baseline + 16 new passes
- No new external dependencies (composition only)
- moc.md edit is exactly 1 line addition (`${body}`); no other template/profile changes (D-54.10 honoured per A1 carve-out)
</verification>

<success_criteria>
- 7 RED Obsidian parity tests turn GREEN (forward, inverse, count, canonical order forward, canonical order inverse, empty-suppression, idempotence)
- Forward parity, backward parity, AND per-relation count parity all verified by tests (D-54.12 three-assertion bar satisfied)
- moc.md gains `${body}` slot (A1 carve-out — adopted)
- `_build_concept_code_sections_for_code` and `_build_concept_code_sections_for_moc` both present in `graphify/templates.py`
- Sentinel-wrapped output makes the per-relation block round-trip-safe
- No regression in `test_export.py`, no regression in Plan 02/03 MCP tests
</success_criteria>

<output>
Create `.planning/phases/54-mcp-trace-obsidian-parity/54-04-SUMMARY.md` documenting: (1) the two helpers' algorithms, (2) the moc.md `${body}` slot edit (A1 carve-out adoption), (3) the sentinel-wrap strategy for round-trip preservation, (4) which CGRAPH-04 truths are now verified, (5) confirmation that all 16 Plan 01 RED tests are now GREEN.
</output>
