---
phase: 54-mcp-trace-obsidian-parity
plan: 5
type: execute
wave: 5
depends_on: [54-02, 54-03, 54-04]
files_modified:
  - docs/RELATIONS.md
  - server.json
  - .planning/phases/54-mcp-trace-obsidian-parity/54-VERIFICATION.md
autonomous: true
requirements: [CGRAPH-03, CGRAPH-04]
tags: [docs, manifest, verification, close-out]

must_haves:
  truths:
    - "docs/RELATIONS.md gains an `## MCP traversal` section documenting the relations param, payload shape, and entity_trace include_concept_code extension"
    - "server.json regenerated by scripts/sync_mcp_server_json.py reflects new tool schemas"
    - "54-VERIFICATION.md contains the CGRAPH-03 mapping table per D-54.06 (sub-requirement → MCP tool/parameter/test)"
    - "54-VERIFICATION.md explicitly addresses A1 carve-out (moc.md ${body} addition) — adopted with justification"
    - "Full pytest suite passes; no regressions vs Phase 53 baseline"
    - "Both CGRAPH-03 and CGRAPH-04 are traceable from VERIFICATION.md to specific test cases and source lines"
  artifacts:
    - path: "docs/RELATIONS.md"
      provides: "MCP traversal documentation section"
      contains: "MCP traversal"
    - path: "server.json"
      provides: "Regenerated capability manifest with new tool schemas"
      contains: "concept_code_hops"
    - path: ".planning/phases/54-mcp-trace-obsidian-parity/54-VERIFICATION.md"
      provides: "CGRAPH-03/04 mapping table + A1 carve-out resolution + must-haves verification status"
      contains: "CGRAPH-03"
  key_links:
    - from: ".planning/phases/54-mcp-trace-obsidian-parity/54-VERIFICATION.md"
      to: "tests/test_concept_code_mcp.py + tests/test_concept_code_obsidian.py + tests/test_serve.py"
      via: "Mapping table column 'Test'"
      pattern: "test_concept_code"
    - from: "server.json"
      to: "graphify/mcp_tool_registry.py"
      via: "scripts/sync_mcp_server_json.py regeneration"
      pattern: "concept_code_hops"
---

<objective>
Wave 5 (close-out): Document the new MCP traversal surface in `docs/RELATIONS.md`, regenerate the capability manifest (`server.json`) so MCP clients see the updated tool schemas, and produce `54-VERIFICATION.md` containing the CGRAPH-03 sub-requirement mapping table mandated by D-54.06 and the A1 carve-out resolution.

Purpose: Close the phase. Without docs the new `relations` param is invisible to agent users; without manifest regen MCP clients see stale tool descriptions; without the mapping table CGRAPH-03's "documented mapping table in verification" success criterion fails.

Output: Three files updated/created. Phase-gate full pytest suite passes.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/54-mcp-trace-obsidian-parity/54-CONTEXT.md
@.planning/phases/54-mcp-trace-obsidian-parity/54-RESEARCH.md
@.planning/phases/54-mcp-trace-obsidian-parity/54-02-SUMMARY.md
@.planning/phases/54-mcp-trace-obsidian-parity/54-03-SUMMARY.md
@.planning/phases/54-mcp-trace-obsidian-parity/54-04-SUMMARY.md
@docs/RELATIONS.md
@scripts/sync_mcp_server_json.py
@server.json
@graphify/mcp_tool_registry.py
@CLAUDE.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Append MCP traversal section to docs/RELATIONS.md</name>
  <files>docs/RELATIONS.md</files>
  <read_first>
    - docs/RELATIONS.md (full — to align tone and section structure with existing Phase 53 content)
    - 54-CONTEXT.md D-54.01..04 (the contract)
    - 54-RESEARCH.md §"Common Pitfalls > Pitfall 3" (BFS perf + cap documentation requirement)
  </read_first>
  <action>
    Append a new top-level section `## MCP traversal` to `docs/RELATIONS.md`. Content must include:

    1. **`concept_code_hops` tool**:
       - Purpose: BFS-walk concept↔code edges from a starting node.
       - Parameters: `entity` (label or node id), `max_hops` (1-6, default 3), `direction` (`code_to_concept` / `concept_to_code` / `both`), `relations` (NEW — list of strings, default `["implements"]`, allowed values `implements / documents / tests / realizes / instantiates`).
       - Payload meta keys: `traversal_steps`, `steps_by_relation: {relation: count}`, `relations` (echo, sorted), `reachable_node_ids`, `depth_by_id`, `truncated`. Plus shim `implements_traversal_steps` retained when `relations == ["implements"]`.
       - Edge budget: `_IMPL_EDGE_BUDGET = 500` global cap (Pitfall 3) — `truncated=true` when hit.

    2. **`entity_trace` tool**:
       - Existing temporal trace surface (Phase 11), now with optional `include_concept_code: bool = false`.
       - When `true`: meta gains `concept_code_reachable: list[str]` and `concept_code_steps_by_relation: dict[str, int]`.
       - Default behaviour byte-identical to Phase 11.
       - Concept↔code merge uses fixed `max_hops=2` and all 5 relations (no per-call tunable).

    3. **Backward compatibility**: explicit subsection noting:
       - Phase 47 callers omitting `relations` continue to receive `implements_traversal_steps` (shim active).
       - Phase 11 callers omitting `include_concept_code` see no change in their trace envelope.

    4. **Example invocations** (small JSON examples) for both tools — one default-args call and one widened-relations call.

    Tone: terse, reference-style. ~80-120 lines added. Do NOT modify existing sections.
  </action>
  <verify>
    <automated>grep -c "^## MCP traversal" docs/RELATIONS.md</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "^## MCP traversal" docs/RELATIONS.md` returns 1
    - `grep -v '^#' docs/RELATIONS.md | grep -c "concept_code_hops"` returns at least 2
    - `grep -v '^#' docs/RELATIONS.md | grep -c "include_concept_code"` returns at least 1
    - `grep -v '^#' docs/RELATIONS.md | grep -c "steps_by_relation"` returns at least 1
    - `grep -v '^#' docs/RELATIONS.md | grep -c "implements_traversal_steps"` returns at least 1 (shim documented)
    - `grep -v '^#' docs/RELATIONS.md | grep -c "_IMPL_EDGE_BUDGET\\|edge budget\\|edge cap"` returns at least 1 (Pitfall 3)
    - Existing RELATIONS.md content untouched: `git diff docs/RELATIONS.md | grep -c "^-"` returns 0 (only `+` lines added)
  </acceptance_criteria>
  <done>docs/RELATIONS.md has a new `## MCP traversal` section covering both tools, parameters, payload shapes, and backward-compat guarantees.</done>
</task>

<task type="auto">
  <name>Task 2: Regenerate server.json via sync_mcp_server_json.py</name>
  <files>server.json</files>
  <read_first>
    - scripts/sync_mcp_server_json.py (full — confirm it's the right entry point and how it writes server.json)
    - server.json (current — note the manifest_content_hash to confirm it changes after regen)
    - 54-RESEARCH.md §"Risks & Landmines > OQ7" (manifest hash drift)
  </read_first>
  <action>
    1. Run: `python scripts/sync_mcp_server_json.py` from the repo root.
    2. Verify server.json regenerated (mtime changed; manifest_content_hash field updated).
    3. Confirm the new tool schemas appear in server.json:
       - `concept_code_hops` tool description mentions the 5 relations or `relations` parameter.
       - `entity_trace` tool description mentions `include_concept_code` or concept↔code merge.
    4. Run `pytest tests/test_capability.py -x` to confirm no manifest-hash assertions break (per RESEARCH §OQ7, test_capability assertions are non-hardcoded).
    5. DO NOT manually edit server.json — only commit the script-generated output.
  </action>
  <verify>
    <automated>python scripts/sync_mcp_server_json.py && pytest tests/test_capability.py -x 2>&1 | tail -10</automated>
  </verify>
  <acceptance_criteria>
    - `python scripts/sync_mcp_server_json.py` exits 0
    - `pytest tests/test_capability.py -x` exits 0
    - `grep -c "concept_code_hops" server.json` returns at least 1
    - `grep -c "entity_trace" server.json` returns at least 1
    - `grep -c '"relations"' server.json` returns at least 1 (new property visible in manifest)
    - `grep -c '"include_concept_code"' server.json` returns at least 1
    - `git diff server.json | grep -c "manifest_content_hash"` returns at least 1 (hash actually changed)
  </acceptance_criteria>
  <done>server.json regenerated; manifest hash updated; capability test green; new tool schemas visible.</done>
</task>

<task type="auto">
  <name>Task 3: Create 54-VERIFICATION.md with CGRAPH-03 mapping table, A1 resolution, must-haves status, and full-suite gate</name>
  <files>.planning/phases/54-mcp-trace-obsidian-parity/54-VERIFICATION.md</files>
  <read_first>
    - 54-CONTEXT.md D-54.06 (the mapping-table requirement)
    - 54-CONTEXT.md D-54.10 (no new templates)
    - 54-RESEARCH.md §"Assumptions Log > A1" (the carve-out)
    - 54-RESEARCH.md §"Validation Architecture > Phase Requirements → Test Map" (source of test names for the mapping table)
    - 54-01-SUMMARY.md, 54-02-SUMMARY.md, 54-03-SUMMARY.md, 54-04-SUMMARY.md (must-haves status reconciliation)
  </read_first>
  <action>
    Create `.planning/phases/54-mcp-trace-obsidian-parity/54-VERIFICATION.md` with the following sections:

    1. **Frontmatter**: `phase: 54-mcp-trace-obsidian-parity`, `requirements: [CGRAPH-03, CGRAPH-04]`, `status: complete`.

    2. **CGRAPH-03 Mapping Table** (D-54.06 — REQUIRED). Columns: `Sub-requirement | MCP tool / parameter | Source (file:line) | Test`. Rows must cover at minimum:
       - "MCP exposes typed concept↔code hop with 5-relation filter" → `concept_code_hops` / `relations` param → `graphify/serve.py:_run_concept_code_hops`, `graphify/mcp_tool_registry.py` → `tests/test_concept_code_mcp.py::test_concept_code_hops_default_relations` + `..._multi_relation_traversal` + `..._payload_steps_by_relation`.
       - "Backward-compat with v1.10 `concept_code_hops` (Phase 47)" → shim `implements_traversal_steps` → `serve.py:_run_concept_code_hops` payload conditional → `tests/test_concept_code_mcp.py::test_concept_code_hops_backward_compat_implements_steps_key`.
       - "Unknown / empty relations rejected" → `_validate_relations_arg` → `graphify/serve.py:_validate_relations_arg` → `tests/test_concept_code_mcp.py::test_concept_code_hops_unknown_relation_errors` + `..._empty_relations_errors`.
       - "/trace OR entity_trace covers concept↔code per D-54.05" → `entity_trace.include_concept_code` → `graphify/serve.py:_run_entity_trace` → `tests/test_serve.py::test_entity_trace_default_excludes_concept_code` + `test_entity_trace_includes_concept_code_when_requested`.
       - "MCP capability schema declares new params" → tool registry schemas → `graphify/mcp_tool_registry.py` + `server.json` → `tests/test_capability.py::test_concept_code_hops_schema_includes_relations_and_entity_trace_includes_concept_code`.

    3. **CGRAPH-04 Mapping Table**. Columns: `Sub-requirement | Vault location | Source (file:line) | Test`. Rows:
       - "CODE notes contain forward per-relation sections in canonical order" → `${body}` slot in `code.md` → `graphify/templates.py:_build_concept_code_sections_for_code` → `tests/test_concept_code_obsidian.py::test_code_note_per_relation_sections_canonical_order`.
       - "Concept MOCs contain inverse per-relation sections in canonical order" → `${body}` slot in `moc.md` → `graphify/templates.py:_build_concept_code_sections_for_moc` → `tests/test_concept_code_obsidian.py::test_concept_moc_inverse_sections_canonical_order`.
       - "Empty sections suppressed" → empty-list short-circuit in builders → templates.py builder loops → `test_empty_relation_section_suppressed`.
       - "Forward parity" → `G.edges` is single source → templates.py builders → `test_forward_parity_edges_to_wikilinks`.
       - "Backward parity" → vault wikilinks all map to graph edges → templates.py builders → `test_backward_parity_wikilinks_to_edges`.
       - "Per-relation count parity (5 separate counts)" → both builders + canonical sort → templates.py builders → `test_per_relation_count_parity`.
       - "Round-trip idempotence" → `_wrap_sentinel("concept_code_relations", ...)` → `graphify/templates.py:_wrap_sentinel` + builders → `test_round_trip_per_relation_sections_idempotent`.

    4. **A1 Carve-out Resolution** (RESEARCH §"Assumptions Log > A1"):
       - Question: Is adding `${body}` to `graphify/builtin_templates/moc.md` allowed under D-54.10 ("no new templates, no profile changes")?
       - Resolution: ADOPTED. Rationale: D-54.10's intent is no NEW template files and no profile-knob changes. Adding a `${body}` slot to an existing builtin moc.md is a 1-line additive edit, parallel to the slot already present in code.md, with zero impact on profile composition (Phase 30 inheritance). Vault overrides that omit `${body}` continue to win and naturally suppress per-relation sections — this is a feature for users who want to opt out without code changes.
       - Decision logged here; Plan 04 implemented the change.

    5. **Must-haves Verification Status** — reconciled across Plans 01-04:
       - Reproduce each plan's `must_haves.truths` list and mark `[x]` for verified by which test/grep / `[ ]` if still open. Goal: every truth `[x]`.

    6. **Full-suite gate**:
       - Command: `pytest tests/ -q`
       - Expected: ≥ Phase 53 baseline (1979 passed, 1 xfailed) + 16 new passes (Plan 01 tests now GREEN).
       - Record actual count from the verifying run.

    7. **Outstanding / Deferred** (for milestone close-out tracking):
       - `graphify_version` PyPI bump deferred to v1.11 close per RESEARCH §"Runtime State Inventory".
       - Profile-driven `concept_code_layout` deferred to CFG-01 / Phase 56.
       - `/trace` slash workflow concept↔code surfacing deferred (D-54.05).
  </action>
  <verify>
    <automated>pytest tests/ -q 2>&1 | tail -5 && grep -c "CGRAPH-03\|CGRAPH-04" .planning/phases/54-mcp-trace-obsidian-parity/54-VERIFICATION.md</automated>
  </verify>
  <acceptance_criteria>
    - `test -f .planning/phases/54-mcp-trace-obsidian-parity/54-VERIFICATION.md && echo OK` returns OK
    - `grep -c "^## CGRAPH-03 Mapping Table" .planning/phases/54-mcp-trace-obsidian-parity/54-VERIFICATION.md` returns 1
    - `grep -c "^## CGRAPH-04 Mapping Table" .planning/phases/54-mcp-trace-obsidian-parity/54-VERIFICATION.md` returns 1
    - `grep -c "A1 Carve-out\\|A1 carve-out" .planning/phases/54-mcp-trace-obsidian-parity/54-VERIFICATION.md` returns at least 1
    - `grep -c "ADOPTED\\|adopted" .planning/phases/54-mcp-trace-obsidian-parity/54-VERIFICATION.md` returns at least 1
    - `grep -c "test_concept_code_hops\\|test_concept_code_obsidian\\|test_entity_trace" .planning/phases/54-mcp-trace-obsidian-parity/54-VERIFICATION.md` returns at least 8
    - `pytest tests/ -q 2>&1 | tail -3 | grep -E "passed"` non-empty (full suite green)
    - Phase 53 baseline preserved: full-suite total ≥ 1979 passed
  </acceptance_criteria>
  <done>54-VERIFICATION.md complete with both mapping tables, A1 carve-out resolution, must-haves reconciled, full pytest suite green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| `scripts/sync_mcp_server_json.py` → `server.json` filesystem write | Internal build script; output is checked into repo. No external input. |
| `server.json` → MCP clients | Outbound; manifest_content_hash provides client-side cache invalidation. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-54-17 | Tampering | Manual edit of `server.json` instead of regeneration | mitigate | Action explicitly says "DO NOT manually edit server.json — only commit the script-generated output." Acceptance criteria check `manifest_content_hash` changed via diff (script-generated hash). |
| T-54-18 | Information Disclosure | docs/RELATIONS.md examples expose internal node IDs | accept | Docs use synthetic example IDs (`k_klass`, `c_concept`); no real corpus data. Already the project's docs convention. |
| T-54-19 | Denial of Service | sync_mcp_server_json.py runtime | accept | Existing mechanical script (already used in CI). Out of scope for new threat surface. |
</threat_model>

<verification>
- All previous waves' tests still GREEN: `pytest tests/test_concept_code_mcp.py tests/test_concept_code_obsidian.py tests/test_serve.py tests/test_capability.py tests/test_export.py -x` exits 0
- Full pytest suite green: `pytest tests/ -q` reports passed count ≥ Phase 53 baseline + 16
- docs/RELATIONS.md, server.json, 54-VERIFICATION.md all committed
- CGRAPH-03 and CGRAPH-04 traceable from VERIFICATION.md to specific lines and tests
- A1 carve-out documented as ADOPTED with rationale
</verification>

<success_criteria>
- docs/RELATIONS.md `## MCP traversal` section published
- server.json regenerated via sync script; manifest_content_hash updated
- 54-VERIFICATION.md exists with both mapping tables, A1 resolution, must-haves status
- Full pytest suite green
- Phase 54 ready to commit + close
</success_criteria>

<output>
Create `.planning/phases/54-mcp-trace-obsidian-parity/54-05-SUMMARY.md` listing: docs section added (line counts), server.json regen confirmation (old vs new manifest_content_hash), verification file path, A1 disposition (ADOPTED), and the final pytest pass count vs Phase 53 baseline.
</output>
