---
phase: 70.2
plan: 02
type: tdd
wave: 2
depends_on: ["70.2-01"]
files_modified:
  - graphify/build.py
  - graphify/export.py
  - tests/test_build.py
autonomous: true
requirements: [CCONF-05, CFED-03]
must_haves:
  truths:
    - "graphify/build.py defines a single SCHEMA_VERSION constant (source of truth)"
    - "graph.graph['schema_version'] == SCHEMA_VERSION after build()"
    - "graph.graph['schema_version'] == SCHEMA_VERSION after build_from_json()"
    - "graphify/export.py imports/uses SCHEMA_VERSION from build (no duplicate literal)"
    - "Existing graphs/exports continue to work (backward compatible)"
  artifacts:
    - path: "graphify/build.py"
      provides: "SCHEMA_VERSION constant and stamping in build()/build_from_json()"
      contains: "SCHEMA_VERSION"
    - path: "graphify/export.py"
      provides: "schema_version sourced from build.SCHEMA_VERSION"
      contains: "SCHEMA_VERSION"
    - path: "tests/test_build.py"
      provides: "Tests asserting in-memory schema_version stamping for both build paths"
      contains: "schema_version"
  key_links:
    - from: "graphify/export.py"
      to: "graphify/build.py"
      via: "from .build import SCHEMA_VERSION"
      pattern: "from \\.build import.*SCHEMA_VERSION"
---

<objective>
Close CCONF-05 / CFED-03: stamp `schema_version` on every in-memory graph (not just at export time) and unify the constant in `graphify/build.py`.

Purpose: MCP server, query tooling, and downstream callers using `build()` / `build_from_json()` directly currently never see `graph.graph["schema_version"]`. Moving the constant into build.py and stamping at construction time fixes that and removes the duplicate literal in export.py.

Output: SCHEMA_VERSION constant in build.py; both build entry points stamp it; export.py imports it; tests prove both paths.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/phases/70.2-address-v1-13-audit-tech-debt-stderr-regex-blind-spot-non-co/70.2-CONTEXT.md
@graphify/build.py
@graphify/export.py
@tests/test_build.py

<interfaces>
Current value being written by export.py:332:
```python
data["schema_version"] = getattr(G, "graph", {}).get("schema_version", "1.13")
```
SCHEMA_VERSION value to lock in build.py: `"1.13"` (must match the literal in export.py:332 verbatim for backward compat).
</interfaces>
</context>

<tasks>

<task type="tdd" tdd="true">
  <name>Task 1: RED — Add tests asserting schema_version stamping for build() and build_from_json()</name>
  <read_first>
    - graphify/build.py (current build/build_from_json signatures)
    - graphify/export.py:329-332 (current schema_version literal)
    - 70.2-CONTEXT.md (D-04..D-07)
    - tests/test_build.py (if exists; otherwise create)
  </read_first>
  <files>tests/test_build.py</files>
  <behavior>
    - Test A: `build({"nodes": [...], "edges": [...]})` returns a graph G with `G.graph["schema_version"] == graphify.build.SCHEMA_VERSION`
    - Test B: `build_from_json(<json path or dict>)` returns a graph G with `G.graph["schema_version"] == graphify.build.SCHEMA_VERSION`
    - Test C: `graphify.build.SCHEMA_VERSION == "1.13"` (lock the value)
    - Test D: `graphify.export` imports SCHEMA_VERSION from `graphify.build` (assert via `import graphify.export; assert graphify.export.SCHEMA_VERSION is graphify.build.SCHEMA_VERSION` — or via source grep `from .build import SCHEMA_VERSION`)
  </behavior>
  <action>
    Create or extend tests/test_build.py with the four tests above using minimal node/edge fixtures (single node + zero edges is fine). Use `tmp_path` for any JSON file. Run pytest — all four tests must FAIL (constant doesn't exist yet, stamping not in build()).
  </action>
  <verify>
    <automated>pytest tests/test_build.py -x 2>&1 | tail -20</automated>
  </verify>
  <done>Four new tests fail with clear messages indicating missing SCHEMA_VERSION / missing stamping.</done>
  <acceptance_criteria>
    - `grep -v '^#' tests/test_build.py | grep -c "schema_version"` >= 3
    - `pytest tests/test_build.py -x` exits non-zero
  </acceptance_criteria>
</task>

<task type="tdd" tdd="true">
  <name>Task 2: GREEN — Define SCHEMA_VERSION in build.py, stamp in build()/build_from_json(), refactor export.py</name>
  <read_first>
    - graphify/build.py (full file — locate build() and build_from_json())
    - graphify/export.py:325-340 (the literal to remove)
    - 70.2-CONTEXT.md (D-04, D-05, D-06, D-07)
    - tests/test_build.py (the failing tests from Task 1)
  </read_first>
  <files>graphify/build.py, graphify/export.py</files>
  <behavior>
    - `graphify.build.SCHEMA_VERSION = "1.13"` defined at module top
    - `build(...)` sets `G.graph["schema_version"] = SCHEMA_VERSION` before returning
    - `build_from_json(...)` does the same on its returned graph
    - `graphify/export.py` replaces its hard-coded `"1.13"` literal with a reference to `SCHEMA_VERSION` imported from `.build`. The export-time line preserves the existing fallback semantics: prefer the graph's own attribute if present, else use the imported constant
  </behavior>
  <action>
    1. In graphify/build.py, near the top imports add: `SCHEMA_VERSION = "1.13"  # CCONF-05 / CFED-03 single source of truth`. 2. In `build()` set `G.graph["schema_version"] = SCHEMA_VERSION` immediately before return. 3. Same in `build_from_json()`. 4. In graphify/export.py replace the literal at line ~332: `from .build import SCHEMA_VERSION` (top of file), then `data["schema_version"] = getattr(G, "graph", {}).get("schema_version", SCHEMA_VERSION)`. Run `pytest tests/test_build.py -x` then full `pytest tests/ -q`.
  </action>
  <verify>
    <automated>pytest tests/test_build.py -x && pytest tests/ -q</automated>
  </verify>
  <done>All four new tests pass; full suite passes; no duplicate `"1.13"` literal in export.py for schema_version.</done>
  <acceptance_criteria>
    - `grep -v '^#' graphify/build.py | grep -c "^SCHEMA_VERSION = \"1.13\""` == 1
    - `grep -v '^#' graphify/export.py | grep -c "from .build import.*SCHEMA_VERSION"` >= 1
    - `grep -v '^#' graphify/export.py | grep -c '"1\.13"'` == 0 (no duplicate literal for schema_version)
    - `pytest tests/ -q` exits zero
  </acceptance_criteria>
</task>

</tasks>

<verification>
- SCHEMA_VERSION single source of truth in build.py
- build() and build_from_json() both stamp it
- export.py imports it
- Backward compatible: value still "1.13"
- pytest tests/ -q passes
</verification>

<success_criteria>
CCONF-05 and CFED-03 closed: every constructed graph (in-memory or rehydrated from JSON) carries `schema_version`, sourced from a single constant.
</success_criteria>

<output>
After completion, create `.planning/phases/70.2-address-v1-13-audit-tech-debt-stderr-regex-blind-spot-non-co/70.2-02-SUMMARY.md`.
</output>
