# Phase 70.2: Address v1.13 Audit Tech Debt - Discussion Log

> **Audit trail only.** Do not use as input to planning, research, or execution agents.
> Decisions are captured in CONTEXT.md — this log preserves the alternatives considered.

**Date:** 2026-05-06
**Phase:** 70.2-address-v1-13-audit-tech-debt-stderr-regex-blind-spot-non-co
**Areas discussed:** Stderr fix approach, Schema stamp location, Docs scope, Test gate, Stderr prefix choice, Schema constant location

---

## Stderr fix approach (AUDIT-02 follow-up)

| Option | Description | Selected |
|--------|-------------|----------|
| Fix regex AND rewrite line | Tighten test_stderr_contract.py:108 regex AND rewrite build.py:251 with a compliant prefix | ✓ |
| Only rewrite build.py:251 | Leave regex; just change the line. Faster but blind spot remains. | |
| Only fix regex (TDD) | Tighten regex, watch test fail, fix line. Two commits. | |

**User's choice:** Fix regex AND rewrite line (Recommended)
**Notes:** Closes the structural blind spot AND the specific offender; future violations of similar shape will now be caught.

---

## Schema stamp location (CCONF-05 / CFED-03 gap)

| Option | Description | Selected |
|--------|-------------|----------|
| Stamp in build() + build_from_json() | Both constructors set G.graph['schema_version']; federation inherits naturally | ✓ |
| Stamp only in build_from_json() | Narrower fix; inconsistent between fresh builds and reads | |
| Lazy property on access | Add a get_schema_version(G) helper instead of touching constructors | |

**User's choice:** Stamp in build() + build_from_json() (Recommended)
**Notes:** Co-locate the SCHEMA_VERSION constant in build.py; export.py imports it. Single source of truth.

---

## Docs scope (VFIX-02)

| Option | Description | Selected |
|--------|-------------|----------|
| Single canonical wording everywhere | One verbatim string propagated to all 9 sites + one-line gloss | ✓ |
| Update text only, no shared source | Just fix the wording in each file | |
| Add an invariant test | Update + add regression grep test | |

**User's choice:** Single canonical wording everywhere (Recommended)
**Notes:** Combined in practice with an invariant grep test (see Test gate decision) — best of both: single wording + regression net.

---

## Test gate

| Option | Description | Selected |
|--------|-------------|----------|
| Targeted tests per item | Regex regression test + schema_version invariant + docs-precedence grep test | ✓ |
| Minimal — rely on existing suites | Tighten existing regex + single schema assertion | |
| Full Nyquist wave | Treat as a normal phase with VALIDATION.md | |

**User's choice:** Targeted tests per item (Recommended)
**Notes:** Three new/updated tests, no full VALIDATION.md sweep. Filling missing/partial Nyquist coverage for other phases is tracked separately.

---

## Stderr prefix choice for build.py:251

| Option | Description | Selected |
|--------|-------------|----------|
| `[graphify] hint:` | Non-fatal advisory; matches "actionable but not error" semantics | ✓ |
| `[graphify] error:` | Treat any extraction validation issue as error-level | |
| `[graphify] info:` | Treat as informational | |

**User's choice:** `[graphify] hint:` (Recommended)
**Notes:** Format: `[graphify] hint: extraction has {n} issues, first: {first_error}`.

---

## Schema constant location

| Option | Description | Selected |
|--------|-------------|----------|
| New constant in build.py | Co-locate SCHEMA_VERSION with the constructors that stamp it | ✓ |
| Promote to graphify/__init__.py | Top-level package constant; most discoverable | |
| New graphify/schema.py module | Dedicated module; cleanest separation | |

**User's choice:** New constant in build.py (Recommended)
**Notes:** export.py imports SCHEMA_VERSION from build for to_json stamp.

---

## Claude's Discretion

- Exact mechanism for the tightened stderr scanner (regex with balanced-paren handling vs Python AST scan) — planner picks based on robustness vs complexity.
- Whether to land the 3 items as 3 atomic commits or one bundled commit — planner decides; per repo convention atomic-per-item is safer.
- Whether `export.py:332` retains the defensive `getattr(...).get(..., "1.13")` fallback or switches to a direct `SCHEMA_VERSION` import once constructors guarantee the stamp.

## Deferred Ideas

- Nyquist VALIDATION.md gap-fill for phases 63, 64, 66, 67, 70, 70.1 (use `/gsd-validate-phase` per phase) — out of scope for 70.2.
- Phase 70 deferred-items.md review — separate task.
- Schema_version migration policy when v1.14 ships — beyond this phase.
