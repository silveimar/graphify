---
phase: 70.2
plan: 03
type: tdd
wave: 1
depends_on: []
files_modified:
  - tests/test_docs_precedence_invariant.py
  - README.md
  - graphify/__main__.py
  - graphify/skill.md
  - graphify/skill-codex.md
  - graphify/skill-opencode.md
  - graphify/skill-claw.md
  - graphify/skill-droid.md
  - graphify/skill-trae.md
  - graphify/skill-windows.md
autonomous: true
requirements: [VFIX-02]
must_haves:
  truths:
    - "Canonical precedence string appears verbatim in README.md"
    - "Canonical precedence string appears verbatim in --help text (graphify/__main__.py around lines 1746 and 1767)"
    - "Canonical precedence string appears verbatim in all 7 platform skill source files"
    - "Invariant test fails if any of the 9 doc sites omits or alters the canonical string"
  artifacts:
    - path: "tests/test_docs_precedence_invariant.py"
      provides: "CI gate enforcing canonical precedence string across 9 doc sites"
      contains: "--output > profile > option-b (vault) > --obsidian-dir > legacy default"
  key_links:
    - from: "tests/test_docs_precedence_invariant.py"
      to: "README.md / __main__.py / skill*.md"
      via: "substring scan"
      pattern: "option-b \\(vault\\)"
---

<objective>
Close VFIX-02: align all 9 documentation sites with the runtime precedence at graphify/__main__.py:2021 (which puts option-b vault between profile and --obsidian-dir), and lock that alignment with an invariant test.

Canonical precedence string (D-08, exact verbatim):
`--output > profile > option-b (vault) > --obsidian-dir > legacy default`

Purpose: Today docs say `--output > profile > --obsidian-dir > legacy default` (missing option-b). Drift between docs and runtime is exactly the kind of bug a regression test prevents.

Output: Invariant test (added first, RED), then 9 doc sites updated to make it pass (GREEN).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/70.2-address-v1-13-audit-tech-debt-stderr-regex-blind-spot-non-co/70.2-CONTEXT.md
@README.md
@graphify/__main__.py
@graphify/skill.md
</context>

<tasks>

<task type="tdd" tdd="true">
  <name>Task 1: RED — Add docs precedence invariant test (D-09)</name>
  <read_first>
    - 70.2-CONTEXT.md (D-08 canonical string, D-09 test design)
    - README.md (current precedence wording)
    - graphify/__main__.py around lines 1746 and 1767 (current --help text)
    - graphify/skill.md and 6 sibling skill files
  </read_first>
  <files>tests/test_docs_precedence_invariant.py</files>
  <behavior>
    - Test loads each of the 9 doc sites and asserts the canonical string `--output > profile > option-b (vault) > --obsidian-dir > legacy default` appears verbatim
    - Sites: README.md, graphify/__main__.py (full file scan; the string must occur >= 1 time covering both --help blocks), graphify/skill.md, graphify/skill-codex.md, graphify/skill-opencode.md, graphify/skill-claw.md, graphify/skill-droid.md, graphify/skill-trae.md, graphify/skill-windows.md
    - Test must FAIL initially (current docs lack `option-b (vault)`), and the failure message must list each missing site
  </behavior>
  <action>
    Create tests/test_docs_precedence_invariant.py. Define `CANONICAL = "--output > profile > option-b (vault) > --obsidian-dir > legacy default"` and `SITES = [list of 9 paths above]`. For each site, read the file (UTF-8) and assert `CANONICAL in text`. Collect all failures and assert at the end with a summary listing every missing site. Use `pathlib.Path` rooted at the repo root via `Path(__file__).resolve().parents[1]`. Run pytest — must fail listing all 9 (or at least the doc sites that omit it).
  </action>
  <verify>
    <automated>pytest tests/test_docs_precedence_invariant.py -x 2>&1 | tail -30</automated>
  </verify>
  <done>Test fails; failure output enumerates missing doc sites.</done>
  <acceptance_criteria>
    - `grep -c "option-b (vault)" tests/test_docs_precedence_invariant.py` >= 1
    - `pytest tests/test_docs_precedence_invariant.py -x` exits non-zero
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 2: GREEN — Propagate canonical precedence string to all 9 doc sites (D-08)</name>
  <read_first>
    - tests/test_docs_precedence_invariant.py (the failing test from Task 1 — list of 9 sites and the canonical string)
    - 70.2-CONTEXT.md (D-08)
    - Each of the 9 doc sites to locate the existing (incorrect) precedence wording
  </read_first>
  <files>README.md, graphify/__main__.py, graphify/skill.md, graphify/skill-codex.md, graphify/skill-opencode.md, graphify/skill-claw.md, graphify/skill-droid.md, graphify/skill-trae.md, graphify/skill-windows.md</files>
  <action>
    In each of the 9 files, find the existing precedence sentence (typically `--output > profile > --obsidian-dir > legacy default` or close variants) and replace with the canonical: `--output > profile > option-b (vault) > --obsidian-dir > legacy default`. For graphify/__main__.py both occurrences (around lines 1746 and 1767) must be updated. Preserve surrounding prose and indentation. Do NOT touch installed skill copies in user dirs — only the source files in graphify/. After edits, run the invariant test then full suite.
  </action>
  <verify>
    <automated>pytest tests/test_docs_precedence_invariant.py -x && pytest tests/ -q</automated>
  </verify>
  <done>Invariant test passes; full suite passes; all 9 sites contain the canonical string.</done>
  <acceptance_criteria>
    - `grep -l "option-b (vault)" README.md graphify/__main__.py graphify/skill.md graphify/skill-codex.md graphify/skill-opencode.md graphify/skill-claw.md graphify/skill-droid.md graphify/skill-trae.md graphify/skill-windows.md | wc -l` == 9
    - `grep -c "option-b (vault)" graphify/__main__.py` >= 2 (both --help blocks updated)
    - `pytest tests/ -q` exits zero
  </acceptance_criteria>
</task>

</tasks>

<verification>
- Invariant test passes
- All 9 doc sites contain canonical string
- Full suite green on Python 3.10/3.12
</verification>

<success_criteria>
VFIX-02 closed: docs match runtime precedence; future drift caught at CI time by the invariant test.
</success_criteria>

<output>
After completion, create `.planning/phases/70.2-address-v1-13-audit-tech-debt-stderr-regex-blind-spot-non-co/70.2-03-SUMMARY.md`. Final post-step (per CLAUDE.md graphify rule): run `python3 -c "from graphify.watch import _rebuild_code; from pathlib import Path; _rebuild_code(Path('.'))"` to refresh the project knowledge graph.
</output>
