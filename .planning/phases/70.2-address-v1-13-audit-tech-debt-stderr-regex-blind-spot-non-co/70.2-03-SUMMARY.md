---
phase: 70.2
plan: 03
subsystem: docs / cli-help / skill-files
tags: [docs, invariant-test, vfix-02, d-08, precedence]
requires: []
provides:
  - "CI gate: tests/test_docs_precedence_invariant.py enforces canonical D-08 string across 9 doc sites"
  - "Aligned docs across README, --help, and 7 platform skills"
affects:
  - "docs surface for output destination precedence"
tech_stack_added: []
tech_stack_patterns: ["substring-invariant test"]
key_files_created:
  - "tests/test_docs_precedence_invariant.py"
key_files_modified:
  - "README.md"
  - "graphify/__main__.py"
  - "graphify/skill.md"
  - "graphify/skill-codex.md"
  - "graphify/skill-opencode.md"
  - "graphify/skill-claw.md"
  - "graphify/skill-droid.md"
  - "graphify/skill-trae.md"
  - "graphify/skill-windows.md"
decisions:
  - "Docs follow runtime, not vice-versa — runtime precedence at graphify/__main__.py:2021 is the source of truth"
  - "Single canonical substring guarded verbatim; second assertion enforces both --help blocks (>= 2 occurrences) carry it"
metrics:
  tasks_completed: 2
  duration_minutes: 4
  completed: "2026-05-06"
requirements: [VFIX-02]
---

# Phase 70.2 Plan 03: Docs Precedence Invariant Summary

Invariant CI test plus verbatim alignment of 9 documentation sites with the runtime D-08 precedence chain `--output > profile > option-b (vault) > --obsidian-dir > legacy default`.

## What Shipped

- **RED gate** (commit `1cd747f`): `tests/test_docs_precedence_invariant.py` with two assertions — every site contains the canonical string verbatim, and `graphify/__main__.py` carries it at least twice (one per --help block). Initial run failed with 8 missing sites as designed.
- **GREEN gate** (commit `faf04ed`): inserted `option-b (vault)` segment into README.md, both --help blocks in `graphify/__main__.py` (lines 1746 and 1767), and all 7 platform skill source files. The same edit also normalized the trailing `default` token to `legacy default` in `__main__.py` so the canonical substring matched verbatim.

## Verification

- `pytest tests/test_docs_precedence_invariant.py -v` → 2 passed
- `pytest tests/ -q` → 2390 passed; 8 pre-existing failures (verified by stashing changes and re-running — all unrelated to docs edits: `test_capability`, `test_main_cli` profile-empty-vault, `test_main_flags` repo-identity / version-footer, `test_migration` risky-row expansion). Out of scope per Rule "Scope Boundary"; logged here for future phase pickup.

## Deviations from Plan

None. Plan executed exactly as written.

### Note on `__main__.py` wording

The pre-existing --help text used `> default` while the runtime D-08 comment uses `> legacy default`. Plan Task 2 explicitly mandated the canonical string verbatim, so both --help blocks were normalized to `legacy default`. This is alignment with D-08 source of truth, not a deviation.

## Commits

| Phase | Commit    | Type | Message                                                                       |
| ----- | --------- | ---- | ----------------------------------------------------------------------------- |
| RED   | `1cd747f` | test | add docs precedence invariant test (D-09, RED)                                |
| GREEN | `faf04ed` | docs | align 9 doc sites with D-08 canonical precedence (GREEN)                      |

## Self-Check: PASSED

- [x] `tests/test_docs_precedence_invariant.py` exists
- [x] Commit `1cd747f` present in git log
- [x] Commit `faf04ed` present in git log
- [x] All 9 doc sites contain `--output > profile > option-b (vault) > --obsidian-dir > legacy default` (verified by passing invariant test)
- [x] `graphify/__main__.py` contains canonical string >= 2 times
