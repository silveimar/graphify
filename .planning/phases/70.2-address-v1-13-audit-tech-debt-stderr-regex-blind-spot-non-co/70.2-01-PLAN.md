---
phase: 70.2
plan: 01
type: tdd
wave: 1
depends_on: []
files_modified:
  - tests/test_stderr_contract.py
  - graphify/build.py
autonomous: true
requirements: [AUDIT-02]
must_haves:
  truths:
    - "Stderr contract regex catches f-string violations with parenthesized interpolation"
    - "graphify/build.py:251 emits a contract-compliant [graphify] hint:/error:/info: prefixed message"
    - "Re-introducing the old non-compliant message at build.py:251 fails the tightened test"
  artifacts:
    - path: "tests/test_stderr_contract.py"
      provides: "Tightened regex catching nested-paren f-string violations"
      contains: "build.py"
    - path: "graphify/build.py"
      provides: "Contract-compliant stderr message at line ~251"
      contains: "[graphify]"
  key_links:
    - from: "tests/test_stderr_contract.py"
      to: "graphify/build.py"
      via: "regex scan over source files"
      pattern: "file=sys\\.stderr"
---

<objective>
Close AUDIT-02 (D-02 + D-10): tighten the stderr contract regex so f-strings with parenthesized interpolation cannot slip past it, then rewrite the non-compliant stderr line at graphify/build.py:251 to satisfy the contract.

Purpose: A regex that stops at the first `)` lets violations like `print(f"... ({n}): ...", file=sys.stderr)` evade the test. build.py:251 is exactly that case.

Output: Updated test file with stronger regex; build.py:251 message rewritten to use the `[graphify] hint:` (or error:/info:) prefix style; tests pass on Python 3.10/3.12.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/phases/70.2-address-v1-13-audit-tech-debt-stderr-regex-blind-spot-non-co/70.2-CONTEXT.md
@.planning/v1.13-MILESTONE-AUDIT.md
@./CLAUDE.md
@tests/test_stderr_contract.py
@graphify/build.py
</context>

<tasks>

<task type="tdd" tdd="true">
  <name>Task 1: RED — Tighten stderr contract regex to catch nested-paren f-strings</name>
  <read_first>
    - tests/test_stderr_contract.py (current regex around line 108)
    - graphify/build.py:251 (the violating call)
    - 70.2-CONTEXT.md (D-02 decision)
  </read_first>
  <files>tests/test_stderr_contract.py</files>
  <behavior>
    - The contract regex must match a `print(..., file=sys.stderr)` call whose first argument is an f-string containing parenthesized interpolation, e.g. `print(f"[graphify] Extraction warning ({n}): bad", file=sys.stderr)`
    - For each such call, the test must assert the first string literal begins with `[graphify] error:`, `[graphify] info:`, or `[graphify] hint:` (the contract prefixes)
    - When run against current build.py:251 (still non-compliant at this step), the test MUST FAIL listing build.py:251 as a violation
  </behavior>
  <action>
    Replace the current narrow regex (which uses `[^)]*file=sys\.stderr` and stops at the first `)`) with one that tolerates balanced parentheses inside the call. Recommended approach: do not rely solely on regex — use a small token/paren-depth scanner OR a regex like `print\s*\(\s*(?P<msg>(?:f?["'][^"']*["']))[\s\S]*?file=sys\.stderr` that matches across newlines and nested groups, then assert `msg` starts with one of the three contract prefixes (`[graphify] error:`, `[graphify] info:`, `[graphify] hint:`). Keep the existing test scope (scan all .py files under graphify/). Run pytest and confirm the test fails citing graphify/build.py:251 specifically.
  </action>
  <verify>
    <automated>pytest tests/test_stderr_contract.py -x 2>&1 | grep -i "build.py" && echo "RED OK: violation detected"</automated>
  </verify>
  <done>Test fails, failure output names graphify/build.py:251 (or its line) as the offending site.</done>
  <acceptance_criteria>
    - `grep -n "build.py" tests/test_stderr_contract.py` confirms the new regex/scanner code exists
    - `pytest tests/test_stderr_contract.py -x` exits non-zero before Task 2 runs
    - Failure message references the build.py line as a contract violation
  </acceptance_criteria>
</task>

<task type="tdd" tdd="true">
  <name>Task 2: GREEN — Rewrite graphify/build.py:251 to comply with the [graphify] contract</name>
  <read_first>
    - graphify/build.py (around line 251)
    - 70.2-CONTEXT.md (D-10 decision — exact prefix style)
    - tests/test_stderr_contract.py (the now-tightened regex)
  </read_first>
  <files>graphify/build.py</files>
  <behavior>
    - The stderr emission at build.py:251 begins its first message argument with `[graphify] hint:` (per D-10)
    - The semantic content of the message is preserved (parenthesized interpolation may stay; only the prefix changes)
    - Tightened test from Task 1 now passes
  </behavior>
  <action>
    Edit the `print(..., file=sys.stderr)` call at graphify/build.py:251 so the f-string starts with `[graphify] hint: ` followed by the existing descriptive content (preserving any `({n})`-style interpolation). Do not change call sites elsewhere. Run `pytest tests/test_stderr_contract.py -x` — must pass. Then run `pytest tests/ -q` — must pass.
  </action>
  <verify>
    <automated>pytest tests/test_stderr_contract.py -x && pytest tests/ -q</automated>
  </verify>
  <done>Stderr contract test passes; full suite passes; build.py:251 string starts with `[graphify] hint:`.</done>
  <acceptance_criteria>
    - `grep -v '^#' graphify/build.py | grep -c "\[graphify\] hint:"` returns >= 1
    - `pytest tests/test_stderr_contract.py -x` exits zero
    - `pytest tests/ -q` exits zero on Python 3.10 and 3.12
  </acceptance_criteria>
</task>

</tasks>

<verification>
- pytest tests/test_stderr_contract.py -x passes
- pytest tests/ -q passes
- Regex/scanner in test now spans nested parens
- build.py:251 emits `[graphify] hint: ...`
</verification>

<success_criteria>
AUDIT-02 closed: regex blind spot eliminated and the previously-uncaught violation at build.py:251 is fixed and contract-compliant.
</success_criteria>

<output>
After completion, create `.planning/phases/70.2-address-v1-13-audit-tech-debt-stderr-regex-blind-spot-non-co/70.2-01-SUMMARY.md`.
</output>
