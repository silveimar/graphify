---
phase: 53-concept-code-schema-build-merge
plan: 04
type: execute
wave: 4
depends_on: [53-02, 53-03]
files_modified:
  - docs/RELATIONS.md
autonomous: true
requirements: [CGRAPH-01, CGRAPH-02]
must_haves:
  truths:
    - "docs/RELATIONS.md describes the 4 new concept↔code relations with direction, confidence rules, and evidence semantics"
    - "docs/RELATIONS.md lists allowed `evidence` values: annotation, jsdoc, docstring, test_docstring, inheritance"
    - "docs/RELATIONS.md states the canonical sort and merge invariants from D-53.05/06"
  artifacts:
    - path: docs/RELATIONS.md
      provides: "Authoritative relation vocabulary documentation, updated for Phase 53"
      contains: "Phase 53 additions"
  key_links:
    - from: docs/RELATIONS.md
      to: graphify/validate.py::KNOWN_EDGE_RELATIONS
      via: "documentation mirrors module-level constant"
      pattern: "documents.*tests.*realizes.*instantiates"
---

<objective>
Update `docs/RELATIONS.md` so the relation vocabulary registry reflects the Phase 53 schema additions and merge invariants. Per D-53.03, this is the single source of truth for human-facing relation documentation.

Purpose: Keep documentation in sync with code; support future contributors and downstream phases (54 MCP/trace, 58 vault parity).
Output: Updated `docs/RELATIONS.md` with a Phase 53 sub-section.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/53-concept-code-schema-build-merge/53-CONTEXT.md
@.planning/phases/53-concept-code-schema-build-merge/53-RESEARCH.md
@.planning/phases/53-concept-code-schema-build-merge/53-02-SUMMARY.md
@.planning/phases/53-concept-code-schema-build-merge/53-03-SUMMARY.md
@docs/RELATIONS.md
@graphify/validate.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Append Phase 53 section to docs/RELATIONS.md</name>
  <files>docs/RELATIONS.md</files>
  <read_first>
    - docs/RELATIONS.md (entire file — 44 lines; understand existing section style and Phase 46 entries)
    - graphify/validate.py (KNOWN_EDGE_RELATIONS, NEW_CONCEPT_CODE_RELATIONS, KNOWN_EVIDENCE_VALUES — mirror exactly)
  </read_first>
  <action>
Append the following block to the end of `docs/RELATIONS.md`, separated from the existing content by a blank line. Match the existing markdown style (header level, table format) used for the Phase 46 concept↔code section — adjust the heading level to nest correctly under the existing concept↔code section if one exists, otherwise add a new top-level "Phase 53 additions (v1.11)" subsection.

```markdown
### Phase 53 additions (v1.11)

The four relations below extend the concept↔code vocabulary established by Phase 46's `implements` / `implemented_by`. All five concept↔code relations are canonically oriented **code → concept** at build time (`graphify/build.py::_normalize_concept_code_edges`).

| Relation | Direction | Default confidence | Notes |
|----------|-----------|--------------------|-------|
| `documents` | doc → concept | INFERRED + `confidence_score` | Docstring / comment / README references a concept |
| `tests` | test → concept | INFERRED + `confidence_score` | Test artifact asserts behavior of a concept |
| `realizes` | code → concept | INFERRED + `confidence_score` | Interface / abstract type realization |
| `instantiates` | code → concept | INFERRED + `confidence_score` | Concrete subtype instantiation |

#### Confidence rules (D-53.07-09)

For the four new relations only (`implements` retains Phase 46 semantics — D-53.10):

- `INFERRED` — requires `confidence_score ∈ [0.0, 1.0]`.
- `EXTRACTED` — requires an `evidence` field whose value is one of:
  - `annotation` — explicit code marker (e.g. `# graphify: implements MyConcept`)
  - `jsdoc` — JSDoc-style structured comment tag
  - `docstring` — Python / Ruby / Java structured docstring tag
  - `test_docstring` — test docstring naming the asserted concept
  - `inheritance` — AST-derived inheritance (`class X(Concept)` / `implements I` / Python ABC)
- `AMBIGUOUS` — permitted without `evidence` or `confidence_score`.

Unknown `evidence` values are rejected by `validate_extraction`. The allowed set is additive — extend `KNOWN_EVIDENCE_VALUES` in `graphify/validate.py` when extractors emit new evidence kinds.

#### Merge & ordering invariants (D-53.05, D-53.06)

When two edges share `(source, target, relation)`, `graphify/build.py::_merge_edge_fields` canonicalizes the merged edge:

- `source_file` — union of inputs, lex-sorted, deduplicated, joined with `"; "`.
- `source_location` — lex-minimum of non-empty values.
- `confidence` — highest tier (EXTRACTED > INFERRED > AMBIGUOUS).
- `confidence_score` — `max()` across present numeric values.
- `weight` — sum (preserves Phase 46 semantic).

After all merge passes, `_normalize_concept_code_edges` applies a final canonical sort by `(source, target, relation)` ascending across **all** edges (concept↔code AND structural). This guarantees `list(G.edges(data=True))` is identical across re-runs of `build_from_json` on the same input.
```
  </action>
  <verify>
    <automated>grep -E "documents|tests|realizes|instantiates" docs/RELATIONS.md | grep -c "concept" && grep -c "KNOWN_EVIDENCE_VALUES\|annotation.*jsdoc.*docstring.*test_docstring.*inheritance\|inheritance" docs/RELATIONS.md</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "Phase 53 additions" docs/RELATIONS.md` returns 1.
    - `grep -c "documents" docs/RELATIONS.md` returns at least 1.
    - `grep -c "tests" docs/RELATIONS.md` returns at least 1.
    - `grep -c "realizes" docs/RELATIONS.md` returns at least 1.
    - `grep -c "instantiates" docs/RELATIONS.md` returns at least 1.
    - `grep -c "test_docstring" docs/RELATIONS.md` returns at least 1.
    - `grep -c "inheritance" docs/RELATIONS.md` returns at least 1.
    - `grep -c "EXTRACTED" docs/RELATIONS.md` returns at least 2 (rules section + tier note).
    - `grep -c "canonical sort" docs/RELATIONS.md` returns at least 1.
    - `grep -c "max()" docs/RELATIONS.md` returns at least 1.
  </acceptance_criteria>
  <done>docs/RELATIONS.md updated with Phase 53 sub-section covering the 4 new relations, confidence rules, evidence values, and merge/sort invariants.</done>
</task>

<task type="auto">
  <name>Task 2: Phase-gate full test suite</name>
  <files>(verification only — no file writes)</files>
  <read_first>
    - .planning/phases/53-concept-code-schema-build-merge/53-01-SUMMARY.md
    - .planning/phases/53-concept-code-schema-build-merge/53-02-SUMMARY.md
    - .planning/phases/53-concept-code-schema-build-merge/53-03-SUMMARY.md
  </read_first>
  <action>
Run the full pytest suite from the repo root. This is the Phase 53 acceptance gate — both CGRAPH-01 (validate) and CGRAPH-02 (build/merge) must be green simultaneously.

```bash
pytest tests/ -q 2>&1 | tail -20
```

If any tests fail, do NOT modify them — open an investigation trail in the SUMMARY (or revert via `git status`/`git diff` and re-plan via `/gsd-plan-phase 53 --gaps`). The Phase 46 / 47 tests are explicit backward-compat gates: any regression in `test_concept_code_edges.py` outside the 13 new tests, or in `test_concept_code_mcp.py`, blocks the phase.
  </action>
  <verify>
    <automated>pytest tests/ -q 2>&1 | tail -20</automated>
  </verify>
  <acceptance_criteria>
    - `pytest tests/ -q` exits 0.
    - `pytest tests/test_concept_code_edges.py -q` exits 0 with at least 13 new tests passing (count the new tests added in Plan 01).
    - `pytest tests/test_concept_code_mcp.py -q` exits 0 (Phase 47 untouched).
    - `pytest tests/test_validate.py tests/test_build.py -q` exits 0.
    - `git diff --name-only HEAD` shows only the files declared in Phase 53 plans (`graphify/validate.py`, `graphify/build.py`, `tests/test_concept_code_edges.py`, `tests/fixtures/concept_code/round_trip.json`, `docs/RELATIONS.md`) — no scope creep.
  </acceptance_criteria>
  <done>Full pytest suite green; only Phase 53-declared files changed.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| docs/RELATIONS.md → human readers | Human-readable doc; no code execution path |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-53.04-01 | Tampering | docs/RELATIONS.md drift from `KNOWN_EDGE_RELATIONS` | mitigate | Acceptance criteria explicitly grep both files for the 4 new relation strings; future drift detected by reading the doc and the constant in tandem. |
| T-53.04-02 | Information Disclosure | Documenting `evidence` values exposes extractor capabilities | accept | These values are already public via the schema; documenting them is the intended outcome. |
| T-53.04-03 | Repudiation | Doc and code disagreeing on confidence rules | mitigate | Plan 02 establishes the code-side contract; this plan mirrors it verbatim with cross-references back to the source-of-truth file. |
</threat_model>

<verification>
```bash
pytest tests/ -q
grep -c "Phase 53 additions" docs/RELATIONS.md
git diff --stat
```

Expected: full suite green, doc updated, no unexpected file changes.
</verification>

<success_criteria>
- `docs/RELATIONS.md` documents the 4 new relations with direction, confidence rules, evidence values, and merge/sort invariants.
- `pytest tests/ -q` passes.
- `git diff --name-only` matches the Phase 53 file allow-list.
</success_criteria>

<output>
After completion, create `.planning/phases/53-concept-code-schema-build-merge/53-04-SUMMARY.md`. The phase as a whole is now ready for `/gsd-verify-phase 53`.
</output>
