---
phase: "60.1"
plan: "02"
subsystem: "verification"
tags: [verification, e2e, determinism, apply-det-01, gap-found]
type: verification
requires: ["60.1-01"]
provides: ["apply-det-01-residual-gap-evidence"]
affects: ["phase-60-e2e-resumption"]
tech-stack:
  added: []
  patterns: []
key-files:
  created:
    - .planning/phases/60.1-update-vault-apply-determinism-fix/60.1-02-SUMMARY.md
  modified: []
decisions:
  - "APPLY-DET-01 NOT closed: Phase 60 RED gate still fails after Wave 1's seed fix."
  - "Residual failure mode is taxonomy/classification, not clustering determinism — surfaces a second bug class."
  - "No source modified per scope discipline; gap routed to gap closure."
metrics:
  duration: "~3 min"
  completed: "2026-05-03"
  python_version: "3.10.19 (.pyenv)"
requirements-completed: []
---

# Phase 60.1 Plan 02: Verification — Phase 60 RED Gate Still RED

**Wave 1's `random_seed=42` fix did NOT close APPLY-DET-01: Phase 60's locked E2E test `test_e2e_compose_override_ladder` continues to fail; residual failure is taxonomy classification (notes emerge as `type=code`/`type=moc`, never `type=thing`), distinct from the clustering determinism bug Wave 1 addressed.**

## Performance

- **Duration:** ~3 min
- **Started:** 2026-05-04T05:36Z
- **Completed:** 2026-05-04T05:42Z
- **Tasks:** 2 (both verification-only)
- **Files modified:** 0 source / 1 SUMMARY

## Verification Steps Executed

### 1. E2E test untouched since lock commit `333d2da`

```
git diff 333d2da -- tests/test_e2e_integration.py
```

Result: empty diff. The Phase 60 RED gate is byte-identical to the locked version.

### 2. Wave 1 fix still in place (`graphify/cluster.py`)

```
grep -c "leiden(G, random_seed=42)" graphify/cluster.py    # → 1
grep -E "leiden\(G\)([^,]|$)" graphify/cluster.py | wc -l  # → 0
```

Both checks pass: the seeded Leiden call is present exactly once and no unseeded `leiden(G)` call remains.

### 3. Phase 60 locked E2E test — STILL FAILS

Command:
```
pytest tests/test_e2e_integration.py::test_e2e_compose_override_ladder -q
```

Result: **FAILED** (1 failed in 15.50s on Python 3.10.19).

Failure excerpt (first ~30 lines):

```
F                                                                        [100%]
=================================== FAILURES ===================================
_______________________ test_e2e_compose_override_ladder _______________________

tmp_path = PosixPath('/private/var/folders/.../test_e2e_compose_override_ladd0')

    def test_e2e_compose_override_ladder(tmp_path: Path) -> None:
        ...
        vault = _write_vault(tmp_path, _PROFILE_YAML_E2E_01, templates={...})
        corpus = _write_corpus(tmp_path)

        output_root = _run_update_vault_preview_then_apply(corpus, vault)
        assert output_root.exists(), f"output root not materialized: {output_root}"

        md_files = list(output_root.rglob("*.md"))
        assert md_files, f"no markdown files found under {output_root}"
        thing_notes = [p for p in md_files if _read_frontmatter(p).get("type") == "thing"]
>       assert len(thing_notes) >= 1, (
            f"expected >=1 note with type=thing, found {len(thing_notes)}; "
            f"all notes: {[(p.relative_to(output_root), _read_frontmatter(p).get('type')) for p in md_files]}"
        )
E       AssertionError: expected >=1 note with type=thing, found 0; all notes: [
E         (PosixPath('Atlas/Sources/Graphify/Things/CODE_corpus_AttentionHead.md'), 'code'),
E         (PosixPath('Atlas/Sources/Graphify/Things/CODE_corpus_TransformerLayer.md'), 'code'),
E         (PosixPath('Atlas/Sources/Graphify/MOCs/attentionhead-py-sample-c00b.md'), 'moc'),
E         (PosixPath('Atlas/Sources/Graphify/MOCs/transformerlayer-forward-init-c135.md'), 'moc')
E       ]
E       assert 0 >= 1
```

**Critical observation:** the failure is NOT a `plan_id mismatch` / "stale or mismatched migration plan" error — Wave 1 did fix the cluster determinism such that `update-vault preview` and `update-vault --apply --plan-id <id>` both run to completion across processes (the test reaches the post-apply assertions and `output_root.exists()` passes; markdown files are produced). The failure is now a **classification/taxonomy bug**: the profile composes `note_type_templates` for `type=thing` plus `mapping_rule_templates`, but the resulting notes are emitted as `type=code` (the CODE-note path) and `type=moc` instead. Zero notes ever land at `type=thing`.

### 4. Full pytest regression

Command:
```
pytest tests/ -q
```

Result: **1 failed, 2119 passed, 1 xfailed, 8 warnings** in 86.79s.

The single failure is the same `test_e2e_compose_override_ladder` above. Everything else (including Wave 1's `test_cluster_is_deterministic_across_runs`) is green; warnings are pre-existing third-party deprecations (scipy/numba/networkx) unrelated to this work.

### 5. No source modifications

```
git status --short
```

Pre-commit showed only the new SUMMARY file plus STATE/ROADMAP. No `graphify/*.py` or `tests/*.py` was touched by this plan.

## Verdict — APPLY-DET-01

**NOT CLOSED. Gap remains.**

Mapping APPLY-DET-01 success criteria to evidence:

| Criterion | Status | Evidence |
| --- | --- | --- |
| 1. `update-vault preview` → `update-vault --apply --plan-id <id>` succeeds across separate processes on identical input | Partially satisfied | Apply now runs to completion (output_root exists, notes materialize). Wave 1's `random_seed=42` fix removed the cross-process plan_id mismatch that had blocked apply in commit `333d2da`. |
| 2. Plan_id stable across two preview runs | Likely satisfied (not directly asserted by current failure point) | Unit test `test_cluster_is_deterministic_across_runs` passes; the E2E test no longer dies at the plan_id validation step. |
| 3. Output content stable across runs | Cannot verify here | Test fails before any cross-run content comparison. |
| 4. **Compose+override ladder produces a `type=thing` note** | **NOT satisfied** | All 4 emitted notes are `type=code` or `type=moc`; `note_type_templates` for `type=thing` is being shadowed/bypassed by the CODE-note path. |

The failure mode has shifted from the original "stale or mismatched migration plan" error (a clustering determinism bug, fixed in Wave 1) to a classification/taxonomy bug in the apply-time note-type resolution: `note_type_templates: thing` is not winning over the built-in CODE note path for code-backed god nodes from the corpus fixture. This is a **different bug class** from APPLY-DET-01's stated determinism scope.

## Recommended Next Step (gap closure scope, NOT this plan)

A new gap-closure plan (or Phase 60.2) should:

1. Trace why CODE-backed god nodes route through the CODE-note path before `note_type_templates` for `type=thing` can claim them — likely in `graphify/mapping.py` precedence ordering vs. `graphify/profile.py` template registration.
2. Decide whether the locked E2E fixture's intent is that `note_type_templates` should override CODE-note default emission, OR whether the test corpus needs a non-code-backed god node to trigger the `thing` template — review against `60.1-CONTEXT.md`'s `<deferred>` section and Phase 60 author intent.
3. Confirm whether this is in fact APPLY-DET-01's scope or a separate APPLY-CLASSIFY-* requirement that needs to be filed.

Per plan directive (`<critical_constraints>`), no source fix is attempted in this plan.

## Deviations from Plan

None — plan executed exactly as written. Verdict differs from the plan's hopeful expectation, which is exactly the case the plan's `<critical_constraints>` covered: "If Phase 60's E2E test STILL fails, do NOT attempt to fix it inside this plan — that is gap-closure scope. Document the failure clearly … and end with `## PLAN COMPLETE — GAPS FOUND`."

## Auth Gates

None.

## Known Stubs

None introduced by this plan.

## Threat Flags

None — verification-only plan, no surface added.

## Self-Check: PASSED

- E2E test exit code captured: FAILED (exit non-zero, 1 failed)
- Full suite results captured: 1 failed, 2119 passed, 1 xfailed
- grep checks confirm Wave 1 fix is still in place: ✓
- No source files modified: ✓ (git diff for `graphify/` and `tests/` is empty)
- SUMMARY.md created at `.planning/phases/60.1-update-vault-apply-determinism-fix/60.1-02-SUMMARY.md`: present
- Verdict and gap-closure routing documented: ✓

## Correction Note

An earlier draft of this SUMMARY (committed in `532b73b`) incorrectly reported the E2E test as PASSED. That draft conflated a different test run's tail output with this E2E run. This revision corrects the record: the actual E2E exit was FAILED, and the actual full-suite result was `1 failed, 2119 passed, 1 xfailed`. The mistake is logged here for audit and is itself a deviation from execution discipline (Rule 1 — verification reporting bug, fixed inline before plan close).

## PLAN COMPLETE — GAPS FOUND
