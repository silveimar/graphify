---
phase: 60.1-update-vault-apply-determinism-fix
verified: 2026-05-03T23:50:00Z
status: gaps_found
score: 2/4 must-haves verified
overrides_applied: 0
gaps:
  - truth: "Phase 60's RED test tests/test_e2e_integration.py::test_e2e_compose_override_ladder (commit 333d2da) turns green WITHOUT modification."
    status: failed
    reason: "Test still FAILS after Wave 1's seed fix. Failure mode shifted: original plan_id mismatch is gone (apply now runs end-to-end) but test asserts >=1 note with type=thing and 0/4 emitted notes are type=thing. Residual failure is a note-type classification/precedence bug in graphify/mapping.py / graphify/profile.py — the CODE-note default path shadows the profile's note_type_templates.thing mapping. This is a distinct bug class from the clustering determinism Wave 1 addressed."
    artifacts:
      - path: "graphify/mapping.py"
        issue: "CODE-note default path emits type=code instead of consulting profile.note_type_templates for type=thing classification."
      - path: "graphify/profile.py"
        issue: "note_type_templates: thing in vault profile is not honored when source node is classified as code; precedence rule appears to favor file_type over profile mapping."
    missing:
      - "Note-type precedence fix so vault profile's note_type_templates.thing overrides the default CODE-note classification when applicable."
      - "Re-run pytest tests/test_e2e_integration.py::test_e2e_compose_override_ladder after fix to witness GREEN."
  - truth: "Full test suite pytest tests/ -q passes on Python 3.10/3.12, no regressions."
    status: failed
    reason: "Wave 2 SUMMARY recorded 2119 passed, 1 failed, 1 xfailed on Python 3.10.19. The single failure is the locked Phase 60 E2E test above. Strict reading of ROADMAP success criterion #4 requires zero failures — gap is the same root cause as gap #1 (note-type classification)."
    artifacts:
      - path: "tests/test_e2e_integration.py::test_e2e_compose_override_ladder"
        issue: "Single failing test in suite; resolution coupled to gap #1."
    missing:
      - "Resolution of gap #1 closes this gap automatically."
human_verification: []
---

# Phase 60.1: update-vault-apply-determinism-fix Verification Report

**Phase Goal:** Make `graphify update-vault --apply --plan-id <id>` succeed on first run by ensuring preview and apply produce identical plan_ids on identical input. Root-cause and patch the non-determinism in community-id assignment between consecutive previews. Unblocks Phase 60 E2E-01 and E2E-02 GREEN gates (RED commit `333d2da` locked as regression test).
**Verified:** 2026-05-03T23:50:00Z
**Status:** gaps_found
**Re-verification:** No — initial verification

## Goal Achievement

### Observable Truths (ROADMAP Success Criteria)

| # | Truth | Status | Evidence |
|---|-------|--------|----------|
| 1 | Two consecutive `graphify update-vault` preview runs against same input produce identical `plan_id` values. | VERIFIED (indirect) | The locked E2E test reaches post-apply assertion stage, meaning apply did NOT abort with plan_id mismatch. Wave 2 SUMMARY confirms cross-process plan_id stability. Root-cause patch (`leiden(G, random_seed=42)` at `graphify/cluster.py:38`) plus tiebreaker sort (`graphify/cluster.py:103`) is in place. Unit test `test_cluster_is_deterministic_across_runs` (tests/test_cluster.py:79) passes 10/10. |
| 2 | `graphify update-vault --apply --plan-id <id>` succeeds on first attempt against fresh corpus. | VERIFIED (indirect) | E2E test progresses past `_run_update_vault_preview_then_apply()` and `output_root.exists()` assertion. Notes are emitted at `Atlas/Sources/Graphify/...`. Apply phase runs successfully end-to-end before the type-classification assertion failure. |
| 3 | Phase 60's RED test `tests/test_e2e_integration.py::test_e2e_compose_override_ladder` (commit `333d2da`) turns green WITHOUT modification. | FAILED | Test executed at verification time: **1 failed in 15.18s**. `git diff 333d2da -- tests/test_e2e_integration.py` returns empty (test file IS locked, unmodified). Failure: `AssertionError: expected >=1 note with type=thing, found 0` — all 4 emitted notes have type `code` or `moc`. Root cause is a separate note-type precedence bug, NOT clustering determinism. |
| 4 | Full test suite `pytest tests/ -q` passes on Python 3.10/3.12, no regressions. | FAILED | Wave 2 SUMMARY: 2119 passed, 1 failed (the locked E2E test from criterion #3), 1 xfailed on Python 3.10.19. Failure is coupled to criterion #3. |

**Score:** 2/4 truths verified

### Required Artifacts

| Artifact | Expected | Status | Details |
|----------|----------|--------|---------|
| `graphify/cluster.py` line 38 | `leiden(G, random_seed=42)` present | VERIFIED | `grep -c "leiden(G, random_seed=42)" graphify/cluster.py` → 1. No unseeded `leiden(G)` calls remain. |
| `graphify/cluster.py` line 103 | Deterministic tiebreaker sort | VERIFIED | `final_communities.sort(key=lambda c: (-len(c), sorted(c)))` present (1 occurrence). |
| `tests/test_cluster.py` | New `test_cluster_is_deterministic_across_runs` | VERIFIED | Defined at line 79. Full file `pytest tests/test_cluster.py -q` → 10 passed. |
| Wave 2 SUMMARY | Verification record | VERIFIED | `60.1-02-SUMMARY.md` honestly records the residual gap (`tags: [..., gap-found]`, `decisions: APPLY-DET-01 NOT closed`). |

### Key Link Verification

| From | To | Via | Status | Details |
|------|----|-----|--------|---------|
| `tests/test_cluster.py::test_cluster_is_deterministic_across_runs` | `graphify/cluster.py:_partition` | `cluster() -> _partition() -> leiden(G, random_seed=42)` | WIRED | Test passes; pattern confirmed at cluster.py:38. |
| `tests/test_e2e_integration.py::test_e2e_compose_override_ladder` | `graphify/cluster.py:_partition (seeded)` | subprocess invocation of `graphify update-vault` | PARTIAL | Determinism wiring functional (apply executes end-to-end); but downstream note-type pipeline (`graphify/mapping.py`, `graphify/profile.py`) misclassifies output, blocking the GREEN gate. |

### Requirements Coverage

| Requirement | Source Plan | Description | Status | Evidence |
|-------------|-------------|-------------|--------|----------|
| APPLY-DET-01 | 60.1-01, 60.1-02 | Determinism across separate Python processes; (1) plan_id stability, (2) first-run apply success, (3) Phase 60 E2E green unmodified, (4) full suite passes. | BLOCKED (partial) | Sub-criteria 1 & 2 satisfied (Leiden seeding closes plan_id divergence). Sub-criteria 3 & 4 BLOCKED by note-type precedence bug exposed once determinism unblocked the apply path. REQUIREMENTS.md marks `[x]` but the locked test still fails — the checkbox is premature. |

### Anti-Patterns Found

None in modified files. The fix is minimal (one kwarg + one sort key change). Wave 2 made zero source modifications, preserving scope discipline.

### Human Verification Required

None — all four success criteria are programmatically testable via existing pytest gates.

### Gaps Summary

Phase 60.1 **partially achieved its goal**. The root-cause clustering non-determinism is fixed and verifiable: same-process unit test passes, and the cross-process E2E now progresses past the apply phase that previously aborted on plan_id mismatch. Sub-criteria 1 (plan_id stability) and 2 (first-run apply success) of APPLY-DET-01 are demonstrably closed.

However, **the explicit ROADMAP success criteria #3 and #4 are not met**: the locked Phase 60 E2E test still FAILS, and the full suite still has 1 failure. The residual failure is in a different subsystem — note-type classification precedence, where the CODE-note default path shadows the vault profile's `note_type_templates.thing` mapping, so 0/4 emitted notes have `type=thing` against an `assert >=1` gate.

The Phase 60.1 charter (per ROADMAP and APPLY-DET-01) explicitly named the locked test as the GREEN gate. Strict reading: the gate is still RED, so the phase has not delivered. The Wave 2 SUMMARY itself acknowledges this honestly (`tags: [gap-found]`, `decisions: APPLY-DET-01 NOT closed`).

**Recommended remediation:**
- Open a sibling phase (e.g., Phase 60.2 — note-type precedence fix) covering `graphify/mapping.py` and `graphify/profile.py` to ensure profile `note_type_templates.thing` overrides the CODE-note default classification.
- Alternatively, scope a gap-closure plan within 60.1 narrowly to fix note-type precedence and re-run the locked E2E + full suite.
- Update `.planning/REQUIREMENTS.md` line 29: APPLY-DET-01 should NOT be marked `[x]` until the locked test is GREEN, since criterion (3) is part of the requirement contract.

---

_Verified: 2026-05-03T23:50:00Z_
_Verifier: Claude (gsd-verifier)_
