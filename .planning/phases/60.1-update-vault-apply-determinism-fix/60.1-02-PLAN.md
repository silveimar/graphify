---
phase: 60.1-update-vault-apply-determinism-fix
plan: 02
type: execute
wave: 2
depends_on: ["60.1-01"]
files_modified: []
autonomous: true
requirements: [APPLY-DET-01]
must_haves:
  truths:
    - "Phase 60's RED test test_e2e_compose_override_ladder turns green WITHOUT any modification to tests/test_e2e_integration.py."
    - "Full pytest tests/ -q passes locally on the developer's active Python interpreter."
    - "The cross-process subprocess scenario (two graphify update-vault preview runs from separate processes) produces identical plan_id values, proving APPLY-DET-01 success criteria 1, 2, and 3."
  artifacts:
    - path: ".planning/phases/60.1-update-vault-apply-determinism-fix/60.1-02-SUMMARY.md"
      provides: "Regression verification record (pytest output excerpts, E2E pass confirmation, no code diffs)."
      contains: "test_e2e_compose_override_ladder PASSED"
  key_links:
    - from: "tests/test_e2e_integration.py::test_e2e_compose_override_ladder"
      to: "graphify/cluster.py:_partition (seeded by Plan 01)"
      via: "subprocess invocation of graphify update-vault -> _partition() -> leiden(G, random_seed=42)"
      pattern: "test_e2e_compose_override_ladder"
---

<objective>
Verify (without modifying any code) that Plan 01's one-line fix closes APPLY-DET-01: Phase 60's locked RED gate turns green and the full test suite remains regression-free.

Purpose: Plan 01 proves same-process determinism via unit test. APPLY-DET-01 success criteria 1-3 require cross-process determinism, which is witnessed by Phase 60's E2E test `tests/test_e2e_integration.py::test_e2e_compose_override_ladder` (commit `333d2da`). That test launches two `graphify update-vault` subprocesses and asserts plan_id stability across them; it was written to fail before the seed fix and pass after. This plan exercises it and the full suite, then records the result.

Output: A SUMMARY.md capturing pytest output. No code changes. No test file changes. If anything fails, escalate via the failure path (do not patch).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/REQUIREMENTS.md
@.planning/phases/60.1-update-vault-apply-determinism-fix/60.1-CONTEXT.md
@.planning/phases/60.1-update-vault-apply-determinism-fix/60.1-RESEARCH.md
@.planning/phases/60.1-update-vault-apply-determinism-fix/60.1-VALIDATION.md
@.planning/phases/60.1-update-vault-apply-determinism-fix/60.1-01-PLAN.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Verify Phase 60 RED gate flips to GREEN unmodified</name>
  <files></files>
  <read_first>
    - .planning/phases/60-milestone-level-e2e-integration-tests/60-CONTEXT.md (the upstream E2E phase that authored the locked RED test)
    - tests/test_e2e_integration.py — read ONLY to confirm `test_e2e_compose_override_ladder` exists. Do NOT modify it. This file is the canonical cross-process witness for APPLY-DET-01.
  </read_first>
  <action>
    Run the locked Phase 60 E2E test:

    ```
    pytest tests/test_e2e_integration.py::test_e2e_compose_override_ladder -q --tb=short
    ```

    Expectations:
    - Exit code 0.
    - Output contains the line `1 passed`.
    - The test file `tests/test_e2e_integration.py` is byte-identical to commit `333d2da`. Confirm with: `git diff 333d2da -- tests/test_e2e_integration.py` returns no output.

    If the test fails:
    - Do NOT modify `tests/test_e2e_integration.py` (locked per CONTEXT.md / `<scope_constraints>` from the orchestrator).
    - Do NOT modify `graphify/cluster.py` beyond what Plan 01 produced.
    - Capture the full traceback, the diff of `graphify/cluster.py` vs `main` (`git diff main -- graphify/cluster.py`), and stop. Escalate to the user — likely cause: the seed value didn't propagate (e.g. graspologic version mismatch), or a second non-determinism source exists outside `_partition`. The CONTEXT.md `<deferred>` section lists candidate next steps (label canonicalization, `_digest_payload` audit).

    If graspologic is not installed in the active environment, the Leiden path is never taken — the test would still pass (Louvain has been seeded since v1.0). Note the situation in the SUMMARY but do NOT consider it a successful verification of the Leiden seed; flag for the user that they should `pip install -e ".[leiden]"` and re-run.
  </action>
  <verify>
    <automated>pytest tests/test_e2e_integration.py::test_e2e_compose_override_ladder -q 2>&1 | tail -5 | grep -E "1 passed"</automated>
  </verify>
  <acceptance_criteria>
    - `pytest tests/test_e2e_integration.py::test_e2e_compose_override_ladder -q` exits 0 and prints `1 passed`.
    - `git diff 333d2da -- tests/test_e2e_integration.py` is empty (test unchanged since commit `333d2da`).
    - If graspologic IS installed (verify with `python -c "import graspologic"` returning exit 0), the test result proves cross-process Leiden determinism.
  </acceptance_criteria>
  <done>
    Phase 60's E2E test passes unmodified, witnessing APPLY-DET-01 success criteria 1, 2, and 3.
  </done>
</task>

<task type="auto">
  <name>Task 2: Run full pytest suite + write regression verification SUMMARY</name>
  <files>.planning/phases/60.1-update-vault-apply-determinism-fix/60.1-02-SUMMARY.md</files>
  <read_first>
    - .planning/phases/60.1-update-vault-apply-determinism-fix/60.1-VALIDATION.md ("Sampling Rate" section: "Before /gsd-verify-work: Full suite + Phase 60 E2E test must be green")
    - $HOME/.claude/get-shit-done/templates/summary.md (SUMMARY.md template)
  </read_first>
  <action>
    Step 1: Run the full test suite:

    ```
    pytest tests/ -q
    ```

    Capture the final pytest summary line (e.g. `=== 247 passed in 28.42s ===`). Expectation: exit code 0, zero failures, zero errors. Skips are acceptable only if they're pre-existing (e.g. optional-dep skips for `mcp`, `pdf`, `office`); if any test that was passing before Plan 01 is now failing or skipped, treat as a regression and escalate.

    Step 2: Confirm the new determinism test from Plan 01 ran and passed:

    ```
    pytest tests/test_cluster.py::test_cluster_is_deterministic_across_runs -q
    ```

    Expectation: `1 passed`.

    Step 3: Confirm graspologic install status (informational, recorded in SUMMARY):

    ```
    python -c "import graspologic; print(graspologic.__version__)" 2>&1
    ```

    Step 4: Write `.planning/phases/60.1-update-vault-apply-determinism-fix/60.1-02-SUMMARY.md`. Use the standard SUMMARY template structure. Include:
    - Phase: 60.1; Plan: 02; Requirements: APPLY-DET-01.
    - Verification result table:
      | Check | Command | Result |
      | --- | --- | --- |
      | Phase 60 RED gate flips GREEN | `pytest tests/test_e2e_integration.py::test_e2e_compose_override_ladder -q` | passed |
      | Determinism unit test (Plan 01) | `pytest tests/test_cluster.py::test_cluster_is_deterministic_across_runs -q` | passed |
      | Full suite | `pytest tests/ -q` | <N> passed in <T>s |
      | E2E test unmodified | `git diff 333d2da -- tests/test_e2e_integration.py` | empty |
      | graspologic installed | `python -c "import graspologic"` | <version or "not installed"> |
    - APPLY-DET-01 closure: a one-paragraph statement mapping each success criterion (1-4 from ROADMAP Phase 60.1) to the specific test/command that witnesses it.
    - "Files modified: none" — this plan is pure verification.
    - Reference the Plan 01 RED and GREEN commit SHAs.

    Commit:
    ```
    git add .planning/phases/60.1-update-vault-apply-determinism-fix/60.1-02-SUMMARY.md
    git commit -m "docs(60.1-02): record APPLY-DET-01 regression verification"
    ```

    No code changes in this plan. If the full suite reports a regression unrelated to Phase 60.1's scope, halt and escalate — do NOT attempt fixes.
  </action>
  <verify>
    <automated>pytest tests/ -q 2>&1 | tail -3 | grep -E "passed" && test -f .planning/phases/60.1-update-vault-apply-determinism-fix/60.1-02-SUMMARY.md</automated>
  </verify>
  <acceptance_criteria>
    - `pytest tests/ -q` exits 0; final summary line contains `passed` and no `failed` / `error`.
    - `pytest tests/test_cluster.py::test_cluster_is_deterministic_across_runs -q` reports `1 passed`.
    - `.planning/phases/60.1-update-vault-apply-determinism-fix/60.1-02-SUMMARY.md` exists, contains the verification table with all rows marked passed, and references Plan 01's commit SHAs.
    - `git diff HEAD~1 -- graphify/ tests/` is empty (no code changes in this plan).
    - Commit subject is `docs(60.1-02): record APPLY-DET-01 regression verification`.
  </acceptance_criteria>
  <done>
    Full test suite is green, the SUMMARY records APPLY-DET-01 closure, and zero source/test files were modified by this plan.
  </done>
</task>

</tasks>

<verification>
- `pytest tests/test_e2e_integration.py::test_e2e_compose_override_ladder -q` -> 1 passed.
- `pytest tests/ -q` -> all passed (no new failures vs. main pre-Plan-01).
- `git diff 333d2da -- tests/test_e2e_integration.py` -> empty.
- `git diff HEAD~1 -- graphify/ tests/` -> empty (Plan 02 makes no code changes).
- SUMMARY.md exists with closure table.
</verification>

<success_criteria>
- All four APPLY-DET-01 ROADMAP success criteria witnessed by automated commands and recorded in SUMMARY.
- Phase 60.1 closes with no follow-on work and the deferred items in CONTEXT.md remain deferred.
</success_criteria>

<output>
After completion, the SUMMARY.md created by Task 2 IS the plan's output artifact. No additional file is produced.
</output>
