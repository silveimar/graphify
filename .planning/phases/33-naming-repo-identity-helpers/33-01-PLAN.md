---
phase: 33-naming-repo-identity-helpers
plan: 1
type: execute
wave: 0
depends_on: []
files_modified:
  - tests/test_naming.py
  - tests/test_profile.py
  - tests/test_export.py
  - tests/test_templates.py
  - tests/test_main_flags.py
autonomous: true
requirements:
  - NAME-01
  - NAME-02
  - NAME-03
  - NAME-04
  - NAME-05
  - REPO-01
  - REPO-02
  - REPO-03
must_haves:
  truths:
    - "Maintainer has failing or pending automated tests for every Phase 33 requirement before implementation begins."
    - "Tests use tmp_path/capsys/monkeypatch and do not perform network calls or writes outside tmp_path."
    - "Unsafe concept titles and repo identities are represented in tests before production code accepts them."
  artifacts:
    - path: "tests/test_naming.py"
      provides: "Repo identity, concept naming, cache, provenance, and unsafe-title unit coverage"
    - path: "tests/test_profile.py"
      provides: "Profile schema coverage for repo.identity and naming.concept_names controls"
    - path: "tests/test_export.py"
      provides: "to_obsidian naming integration and dry-run sidecar coverage"
    - path: "tests/test_templates.py"
      provides: "Combined MOC sink sanitization coverage for generated community titles"
    - path: "tests/test_main_flags.py"
      provides: "CLI --repo-identity parsing coverage for run and --obsidian"
  key_links:
    - from: "tests/test_naming.py"
      to: "graphify/naming.py"
      via: "imports planned helper symbols"
      pattern: "from graphify.naming import"
    - from: "tests/test_profile.py"
      to: "graphify/profile.py"
      via: "validate_profile assertions"
      pattern: "repo.identity"
    - from: "tests/test_export.py"
      to: "graphify/export.py"
      via: "to_obsidian dry-run assertions"
      pattern: "to_obsidian"
---

<objective>
Create the Wave 0 validation scaffold for Phase 33 so every requirement has an automated test target before implementation.

Purpose: Phase 33 changes naming and identity surfaces that feed filenames, tags, wikilinks, frontmatter, Dataview, sidecar cache paths, and manifest metadata. Tests must lock the security and stability contract before helpers are implemented.

Output: Focused pytest coverage for NAME-01..05 and REPO-01..03, following the pure unit-test and tmp_path conventions from the repository.
</objective>

<execution_context>
@$HOME/.cursor/get-shit-done/workflows/execute-plan.md
@$HOME/.cursor/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/33-naming-repo-identity-helpers/33-CONTEXT.md
@.planning/phases/33-naming-repo-identity-helpers/33-RESEARCH.md
@.planning/phases/33-naming-repo-identity-helpers/33-VALIDATION.md
@.planning/phases/33-naming-repo-identity-helpers/33-PATTERNS.md
@CLAUDE.md

<interfaces>
Planned helper contracts from research. Tests should import these exact names so downstream implementation has a concrete target:

```python
class ResolvedRepoIdentity(NamedTuple):
    identity: str
    source: Literal["cli-flag", "profile", "fallback-git-remote", "fallback-directory"]
    raw_value: str
    warnings: tuple[str, ...] = ()

class ConceptName(NamedTuple):
    community_id: int
    title: str
    filename_stem: str
    source: Literal["llm-cache", "llm-fresh", "fallback", "cache-tolerant"]
    signature: str
    reason: str
```

Expected public functions for tests:

```python
resolve_repo_identity(cwd: Path, *, cli_identity: str | None = None, profile: dict | None = None) -> ResolvedRepoIdentity
resolve_concept_names(G: nx.Graph, communities: dict[int, list[str]], profile: dict, artifacts_dir: Path, *, llm_namer: Callable | None = None, dry_run: bool = False) -> dict[int, ConceptName]
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add repo identity and concept naming unit tests</name>
  <files>tests/test_naming.py</files>
  <read_first>
    - tests/test_naming.py (create if absent)
    - tests/test_output.py
    - tests/test_cache.py
    - .planning/phases/33-naming-repo-identity-helpers/33-VALIDATION.md
    - .planning/phases/33-naming-repo-identity-helpers/33-PATTERNS.md
  </read_first>
  <action>
    Create `tests/test_naming.py` with pure pytest coverage for these exact test names from `33-VALIDATION.md`: `test_concept_name_uses_cached_llm_title`, `test_fallback_name_uses_terms_and_suffix`, `test_same_signature_reuses_filename`, `test_concept_name_provenance_records_source`, `test_unsafe_llm_title_rejected`, `test_repo_identity_cli_wins`, `test_repo_identity_profile_wins`, and `test_repo_identity_fallback_git_remote_then_cwd`.

    Use `tmp_path`, `capsys`, and `monkeypatch` only. Build a tiny NetworkX graph with community members whose labels/source_files make the expected fallback title deterministic. The repo fallback test must create a local `.git/config` file with an `origin` remote and must not shell out to `git`. Unsafe-title coverage must include at least `"]] | bad"`, `"{{#connections}}"`, `"../escape"`, an empty string, and an overly generic title such as `"Community"`. Per D-01..D-04 assert source precedence and emitted `[graphify] repo identity:` source text. Per D-05..D-11 assert cached LLM reuse, deterministic fallback, signature stability, sidecar provenance, and no writes when `dry_run=True`.
  </action>
  <verify>
    <automated>python3 -m pytest tests/test_naming.py -q</automated>
    Expected state after this Wave 0 task: tests are present and may fail until Plans 02 and 03 implement `graphify.naming`.
  </verify>
  <acceptance_criteria>
    - `rg "def test_concept_name_uses_cached_llm_title" tests/test_naming.py` finds one test.
    - `rg "def test_repo_identity_cli_wins" tests/test_naming.py` finds one test.
    - `rg "dry_run=True" tests/test_naming.py` finds a dry-run no-sidecar assertion.
    - `rg "origin" tests/test_naming.py` finds a `.git/config` fallback fixture.
    - `rg "subprocess|requests|urllib.request" tests/test_naming.py` returns no matches.
  </acceptance_criteria>
  <done>`tests/test_naming.py` contains all eight Phase 33 helper tests mapped to NAME-01..05 and REPO-01..03.</done>
</task>

<task type="auto">
  <name>Task 2: Add profile schema tests for repo and naming controls</name>
  <files>tests/test_profile.py</files>
  <read_first>
    - tests/test_profile.py
    - graphify/profile.py
    - .planning/phases/33-naming-repo-identity-helpers/33-CONTEXT.md
    - .planning/phases/33-naming-repo-identity-helpers/33-PATTERNS.md
  </read_first>
  <action>
    Extend `tests/test_profile.py` with tests named `test_validate_profile_accepts_repo_identity`, `test_validate_profile_rejects_invalid_repo_identity`, `test_validate_profile_accepts_concept_naming_controls`, and `test_validate_profile_rejects_invalid_concept_naming_controls`.

    Per D-03, valid config must use a top-level `repo:` block with `identity: "work-vault"`, not a nested `naming.repo`. Per D-06, concept naming controls must live under `naming.concept_names` with concrete keys `enabled`, `budget`, and `style`, where `enabled` is bool, `budget` is non-negative int or float, and `style` is a short string. Invalid tests must cover `repo` not being a mapping, `repo.identity` not being a string, empty `repo.identity`, path-like `repo.identity` containing `/`, `\`, or `..`, `naming.concept_names.enabled` not bool, and negative `budget`. Preserve validate_profile's collect-all-errors behavior.
  </action>
  <verify>
    <automated>python3 -m pytest tests/test_profile.py::test_validate_profile_accepts_repo_identity tests/test_profile.py::test_validate_profile_rejects_invalid_repo_identity tests/test_profile.py::test_validate_profile_accepts_concept_naming_controls tests/test_profile.py::test_validate_profile_rejects_invalid_concept_naming_controls -q</automated>
    Expected state after this Wave 0 task: tests are present and may fail until Plan 02 updates `graphify/profile.py`.
  </verify>
  <acceptance_criteria>
    - `rg "repo.identity" tests/test_profile.py` finds profile schema assertions.
    - `rg "naming.concept_names" tests/test_profile.py` finds concept naming control assertions.
    - `rg "naming.*repo|repo.*naming" tests/test_profile.py` does not introduce a valid nested repo identity example.
    - `rg "len\\(errors\\)|collect" tests/test_profile.py` finds a collect-all-errors assertion for invalid new fields.
  </acceptance_criteria>
  <done>`tests/test_profile.py` locks the exact profile schema required by D-03 and D-06.</done>
</task>

<task type="auto">
  <name>Task 3: Add export, template, and CLI integration tests</name>
  <files>tests/test_export.py, tests/test_templates.py, tests/test_main_flags.py</files>
  <read_first>
    - tests/test_export.py
    - tests/test_templates.py
    - tests/test_main_flags.py
    - graphify/export.py
    - graphify/templates.py
    - graphify/__main__.py
    - .planning/phases/33-naming-repo-identity-helpers/33-VALIDATION.md
    - .planning/phases/33-naming-repo-identity-helpers/33-PATTERNS.md
  </read_first>
  <action>
    Extend integration coverage without network calls. In `tests/test_export.py`, add `test_to_obsidian_resolves_concept_names_for_moc_paths` and `test_to_obsidian_dry_run_does_not_write_naming_sidecar`. Use a minimal graph and `dry_run=True`; assert the generated merge plan contains the resolved concept name in the MOC path or rendered content and that `concept-names.json` is absent after dry-run.

    In `tests/test_templates.py`, add `test_generated_moc_title_is_sanitized_across_sinks`. Render a MOC with `community_name` set to an unsafe generated label containing wikilink breakers, template braces, YAML-sensitive characters, and control characters. Assert output uses a safe filename stem, a safe tag, escaped/safe frontmatter, and no raw `]]` or `{{#connections}}` injection.

    In `tests/test_main_flags.py`, add CLI parsing tests named `test_run_repo_identity_flag_overrides_profile` and `test_obsidian_repo_identity_flag_overrides_profile`. Mirror the existing `--output` subprocess helper pattern, parse both `--repo-identity value` and `--repo-identity=value`, and assert the winning source text is emitted exactly once.
  </action>
  <verify>
    <automated>python3 -m pytest tests/test_export.py::test_to_obsidian_resolves_concept_names_for_moc_paths tests/test_export.py::test_to_obsidian_dry_run_does_not_write_naming_sidecar tests/test_templates.py::test_generated_moc_title_is_sanitized_across_sinks tests/test_main_flags.py::test_run_repo_identity_flag_overrides_profile tests/test_main_flags.py::test_obsidian_repo_identity_flag_overrides_profile -q</automated>
    Expected state after this Wave 0 task: tests are present and may fail until Plans 03 and 04 wire implementation.
  </verify>
  <acceptance_criteria>
    - `rg "test_to_obsidian_resolves_concept_names_for_moc_paths" tests/test_export.py` finds one test.
    - `rg "concept-names\\.json" tests/test_export.py` finds dry-run sidecar assertions.
    - `rg "test_generated_moc_title_is_sanitized_across_sinks" tests/test_templates.py` finds one test.
    - `rg "--repo-identity" tests/test_main_flags.py` finds CLI coverage.
    - `rg "requests|urllib.request|http" tests/test_export.py tests/test_templates.py tests/test_main_flags.py` returns no network-call matches.
  </acceptance_criteria>
  <done>Wave 0 integration tests cover NAME-04, NAME-05, REPO-01, and Phase 33 dry-run/cache safety expectations.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| test fixture -> planned helper API | Tests encode untrusted CLI/profile/LLM inputs before production validation exists. |
| generated title -> renderer assertions | Unsafe generated labels cross into filename, tag, wikilink, Dataview, and frontmatter sinks. |
| tmp_path artifact dir -> sidecar cache | Tests prove cache writes stay in tmp_path and dry-run does not persist sidecars. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-33-01 | Tampering | `tests/test_naming.py` unsafe title fixtures | mitigate | Include raw LLM candidates with path traversal, wikilink breakers, template braces, empty strings, and generic titles; assert rejection/fallback rather than acceptance. |
| T-33-02 | Tampering | `tests/test_profile.py` repo.identity schema tests | mitigate | Assert path-like `repo.identity` values are invalid before they can become filenames/tags/manifests. |
| T-33-03 | Denial of Service | generated labels | mitigate | Include oversized/generic candidate coverage and require deterministic fallback. |
| T-33-04 | Repudiation | provenance expectations | mitigate | Assert provenance includes source, signature, and reason so fallback/cache decisions are inspectable. |
| T-33-05 | Information Disclosure | test fixtures | accept | Tests use synthetic labels and tmp_path-only files; no secrets or real vault content are read. |
</threat_model>

<verification>
Run the focused Wave 0 commands after each task. Before moving to Plan 02, confirm all planned test names exist even if the tests fail because production symbols are not implemented yet.
</verification>

<success_criteria>
- Every Phase 33 requirement ID appears in at least one Wave 0 test.
- Every Wave 0 test is pure pytest with no network calls and no filesystem writes outside `tmp_path`.
- Tests encode CLI > profile > fallback precedence, cached/fallback concept naming, cache stability, provenance, and unsafe label rejection.
</success_criteria>

<output>
After completion, create `.planning/phases/33-naming-repo-identity-helpers/33-01-SUMMARY.md`
</output>
