---
phase: 40-multi-harness-memory-inverse-import-injection-defenses
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - graphify/harness_schemas/interchange_v1.schema.json
  - graphify/harness_interchange.py
  - graphify/harness_export.py
  - graphify/__main__.py
  - tests/test_harness_interchange.py
autonomous: true
requirements:
  - PORT-01
  - PORT-02
  - SEC-02
must_haves:
  truths:
    - "Bundled interchange schema v1 lives under graphify/harness_schemas/ and is referenced by code."
    - "Export path can emit a JSON interchange file with nodes/edges plus provenance (schema id, timestamp, graphify version)."
    - ".harness export (or equivalent) can request interchange output alongside or instead of markdown per D-01."
  artifacts:
    - path: "graphify/harness_schemas/interchange_v1.schema.json"
      provides: "Versioned interchange JSON Schema"
      contains: "interchange"
    - path: "graphify/harness_interchange.py"
      provides: "export_interchange_v1() building extraction dict + provenance from graph snapshot inputs"
      contains: "provenance"
    - path: "tests/test_harness_interchange.py"
      provides: "Tests for schema shape, provenance fields, validate_extraction on export output"
      contains: "test_"
  key_links:
    - from: "graphify/harness_interchange.py"
      to: "graphify.validate.validate_extraction"
      via: "export payload must validate before write"
      pattern: "validate_extraction"
---

<objective>
Deliver the **second first-class harness export target** as **canonical JSON interchange** (D-01), with **versioned schema artifacts under `harness_schemas/`** (PORT-02) and **provenance metadata** on every export (SEC-02).

Purpose: Grounds PORT-01/PORT-02/SEC-02 before import and MCP wiring; interchange must align conceptually with `export_claude_harness` data sources without adding Codex markdown emitters this phase.

Output: New module + JSON Schema + `harness export` flag wiring + tests.
</objective>

<execution_context>
@$HOME/.cursor/get-shit-done/workflows/execute-plan.md
@$HOME/.cursor/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/40-multi-harness-memory-inverse-import-injection-defenses/40-CONTEXT.md
@.planning/phases/40-multi-harness-memory-inverse-import-injection-defenses/40-RESEARCH.md
@.planning/phases/40-multi-harness-memory-inverse-import-injection-defenses/40-PATTERNS.md
@graphify/harness_export.py
@graphify/validate.py

<interfaces>
From `graphify.validate`:
- `validate_extraction(data: dict) -> list[str]`

From `graphify.harness_export` (executor: grep public entrypoints):
- Functions that assemble `graph_data` dict from `graph.json` + sidecars for harness rendering — reuse for interchange node/edge population per 40-PATTERNS.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Interchange schema + export produces valid extraction</name>
  <files>graphify/harness_schemas/interchange_v1.schema.json, graphify/harness_interchange.py, tests/test_harness_interchange.py</files>
  <behavior>
    - `export_interchange_v1(...)` returns (or writes) JSON matching `interchange_v1.schema.json` with `provenance.interchange_schema_id`, `generated_at`, `graphify_version`, and `extraction` key whose inner dict passes `validate_extraction` with [].
    - Provenance includes a stable `source_run_id` when tests pass a fixed UUID/seam (optional field OK when absent).
  </behavior>
  <action>
    Add bundled **JSON Schema** for interchange v1 (D-01, PORT-02). Implement `graphify/harness_interchange.py` with `export_interchange_v1(graph_data: dict, *, out_path: Path | None = None, clock=...) -> dict` per 40-RESEARCH envelope. Populate `extraction.nodes` / `extraction.edges` using the **same node/edge derivation strategy** as `export_claude_harness` where applicable (read `harness_export.py` and factor shared helper only if needed—avoid duplicating annotation merge logic). Per **SEC-02**, set `provenance.generated_at` via injectable clock (reuse `set_clock` pattern or accept `clock` kwarg). Use `importlib.metadata.version("graphifyy")` or existing project version helper if already present. Do **not** add Codex markdown emitters. Per **Phase 39 contract**, do not change `elicitation.json` merge semantics—export reads existing graph snapshot only.
  </action>
  <verify>
    <automated>pytest tests/test_harness_interchange.py -q</automated>
  </verify>
  <done>Schema on disk; tests pass; exported extraction validates.</done>
</task>

<task type="auto">
  <name>Task 2: Wire harness export CLI to emit interchange</name>
  <files>graphify/__main__.py, graphify/harness_export.py</files>
  <action>
    Extend **`graphify harness export`** (or shared dispatch) with a **`--format` / `--interchange`** flag (planner discretion: one UX) that writes `harness_memory.v1.json` (or name documented in `--help`) under the **same resolved directory** as markdown harness output, using **`resolve_output()` / `validate_graph_path`** patterns consistent with existing harness export. Reuse library from Task 1—**no** second implementation in `__main__.py` beyond argument parsing and path resolution.
  </action>
  <verify>
    <automated>pytest tests/test_harness_interchange.py tests/test_harness_export.py -q</automated>
  </verify>
  <done>CLI documents flag; interchange file appears under confined output root in tests.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Graph snapshot → interchange file | User-controlled labels already in graph; still run validation before write |
| Write path | Interchange output must stay inside `validate_graph_path`-approved roots |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-40-01 | T | interchange write | mitigate | `validate_graph_path` on `out_path`; atomic write via `.tmp` + `os.replace` like harness_export |
| T-40-02 | I | provenance spoofing in file | accept | Provenance is attest metadata only; graph content still validated by validate_extraction |
</threat_model>

<verification>
`pytest tests/test_harness_interchange.py tests/test_harness_export.py -q`
</verification>

<success_criteria>
Interchange v1 is a first-class export; schema versioned in-repo; SEC-02 fields present on emitted JSON; PORT-01/PORT-02 satisfied for export direction.
</success_criteria>

<output>
After completion, create `.planning/phases/40-multi-harness-memory-inverse-import-injection-defenses/40-01-SUMMARY.md`
</output>
