---
phase: 40-multi-harness-memory-inverse-import-injection-defenses
plan: 05
type: execute
wave: 4
depends_on:
  - "40-01"
  - "40-03"
  - "40-04"
files_modified:
  - tests/test_harness_interchange.py
  - tests/test_harness_import.py
autonomous: true
requirements:
  - PORT-04
must_haves:
  truths:
    - "Export→import round-trip preserves node ids, labels, and relations within documented normalization limits (D-06)."
    - "Test documents which fields may drift (whitespace, ordering) vs must be stable."
  artifacts:
    - path: "tests/test_harness_interchange.py"
      provides: "Round-trip semantic preservation tests"
      contains: "round_trip"
    - path: "tests/test_harness_import.py"
      provides: "Optional cross-fixture reinforcement"
      contains: "round_trip"
  key_links:
    - from: "tests/test_harness_interchange.py"
      to: "graphify.harness_interchange.export_interchange_v1"
      via: "build fixture → export → import → assert"
      pattern: "round_trip"
---

<objective>
Lock **PORT-04**: **fixture-based** tests demonstrate **export → import** semantic preservation for **ids, labels, relations** with **documented limits** (D-06); explicitly **not** default byte-identical requirement.

Purpose: Prevents regressions in interchange and import layers before release.

Output: Expanded automated tests only (no new production deps).
</objective>

<execution_context>
@$HOME/.cursor/get-shit-done/workflows/execute-plan.md
@$HOME/.cursor/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/40-multi-harness-memory-inverse-import-injection-defenses/40-RESEARCH.md
@graphify/harness_interchange.py
@graphify/harness_import.py
@graphify/harness_export.py
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Deterministic round-trip on golden graph_data fixture</name>
  <files>tests/test_harness_interchange.py</files>
  <behavior>
    - Build small in-memory `graph_data` fixture (nodes/edges lists) representative of harness-bound SOUL-like facts.
    - `export_interchange_v1` → write temp JSON → `import_harness_path(..., format=\"json\")` → **sets of** `(id, label)` and **sets of** `(source, target, relation)` match pre-export expectations after normalization documented in test comments.
    - Whitespace-only changes in optional description fields allowed if documented.
  </behavior>
  <action>
    Use **`set_clock`** or injected clock from Plan 01 for stable `generated_at`. Compare **semantic** tuples, not raw JSON strings. If `export` introduces sort order for edges, assert **canonical sort** in comparison helper. **Claude markdown** round-trip may be `@pytest.mark.skip` until parser complete—**JSON interchange must not skip** (D-01 primary).
  </action>
  <verify>
    <automated>pytest tests/test_harness_interchange.py::test_interchange_round_trip_semantic -q</automated>
  </verify>
  <done>Named test passes (executor may rename but keep one explicit semantic round-trip test).</done>
</task>

<task type="auto">
  <name>Task 2: Document limits block in test module docstring or README fragment</name>
  <files>tests/test_harness_interchange.py</files>
  <action>
    At top of test module or in `test_interchange_round_trip_semantic` docstring, bullet **what is guaranteed** vs **not guaranteed** (byte identity, ordering, optional fields)—per D-06 **documented limits** requirement.
  </action>
  <verify>
    <automated>pytest tests/test_harness_interchange.py -q</automated>
  </verify>
  <done>Limits visible to developers without reading planning chat.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Tests only | No new threat surface |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-40-RT | None | tests | accept | n/a |
</threat_model>

<verification>
`pytest tests/test_harness_interchange.py -q`
</verification>

<success_criteria>
PORT-04 demonstrably satisfied in CI with clear documentation of drift allowances.
</success_criteria>

<output>
After completion, create `.planning/phases/40-multi-harness-memory-inverse-import-injection-defenses/40-05-SUMMARY.md`
</output>
