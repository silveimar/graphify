---
phase: 40-multi-harness-memory-inverse-import-injection-defenses
plan: 04
type: execute
wave: 3
depends_on:
  - "40-02"
files_modified:
  - graphify/mcp_tool_registry.py
  - graphify/serve.py
  - SECURITY.md
  - tests/test_mcp_harness_io.py
autonomous: true
requirements:
  - SEC-03
  - SEC-04
must_haves:
  truths:
    - "MCP tools for harness import (and interchange export if exposed) call the same functions as CLI (D-05)."
    - "`build_mcp_tools()` tool names exactly match `serve._handlers` keys (MANIFEST-05)."
    - "SECURITY.md contains a dedicated harness import/export subsection with PORT/SEC traceability (D-07)."
  artifacts:
    - path: "graphify/mcp_tool_registry.py"
      provides: "Tool definitions import_harness + optional export_harness_interchange"
      contains: "import_harness"
    - path: "graphify/serve.py"
      provides: "_handlers entries delegating to harness_import / harness_interchange"
      contains: "_tool_import_harness"
    - path: "SECURITY.md"
      provides: "Harness I/O threat model rows"
      contains: "Harness import"
    - path: "tests/test_mcp_harness_io.py"
      provides: "Handler calls library with mocked paths / tmp_path"
      contains: "test_"
  key_links:
    - from: "graphify/serve.py"
      to: "graphify.harness_import.import_harness_path"
      via: "_handlers dispatcher"
      pattern: "import_harness_path"
---

<objective>
Achieve **MCP parity** with CLI for harness I/O (SEC / PORT-* D-05) and extend **SECURITY.md** with **full harness import/export** documentation (SEC-04 / D-07). **No stub tools** that imply parity without calling library code.

Purpose: Agents using MCP get the same validation/sanitization guarantees as shell users.

Output: Registry + serve handlers + SECURITY.md section + focused tests.
</objective>

<execution_context>
@$HOME/.cursor/get-shit-done/workflows/execute-plan.md
@$HOME/.cursor/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/40-multi-harness-memory-inverse-import-injection-defenses/40-CONTEXT.md
@graphify/serve.py
@graphify/mcp_tool_registry.py
@graphify/harness_import.py
@graphify/harness_interchange.py
@SECURITY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: MCP tools and handlers for import/export</name>
  <files>graphify/mcp_tool_registry.py, graphify/serve.py, tests/test_mcp_harness_io.py</files>
  <action>
    Add **`import_harness`** MCP tool: inputs include **`path` (string)** relative to project root or under `graphify-out/` per existing path rules, **`format`**, optional **`strict`** — mirror CLI. Handler validates with **`validate_graph_path`**, then calls **`import_harness_path`**. If interchange export is exposed, add **`export_harness_interchange`** tool calling **`export_interchange_v1`** / CLI-shared function from Plan 01—**same** validation stack. Register tools in **`build_mcp_tools()`** and add parallel **`_tool_*` functions** to `_handlers`; preserve **`RuntimeError` guard** on key set equality. **Never** duplicate parse/sanitize logic in serve.py (SEC-03). Optionally return JSON-excerpt string + sentinel pattern consistent with other tools—follow existing `QUERY_GRAPH_META_SENTINEL` only if appropriate (executor: match nearby tool style).
  </action>
  <verify>
    <automated>pytest tests/test_mcp_harness_io.py -q</automated>
  </verify>
  <done>Tests prove handler invokes import library; registry keys match _handlers.</done>
</task>

<task type="auto">
  <name>Task 2: SECURITY.md harness I/O subsection</name>
  <files>SECURITY.md</files>
  <action>
    Add **subsection** under Threat Surface (or new `## Harness memory import/export`): table covering **untrusted harness files**, **path traversal**, **prompt injection via imported text**, **MCP vs CLI trust parity**, **size limits**; each row maps to **mitigations** referencing **`security.py`**, **`validate_extraction`**, **`harness_import`**. Include **traceability line** listing **PORT-01–PORT-05** and **SEC-01–SEC-04** (D-07). Not a one-liner pointer only.
  </action>
  <verify>
    <automated>python -c "p=open('SECURITY.md',encoding='utf-8').read(); assert 'PORT-01' in p or 'PORT-0' in p; assert 'Harness' in p"</automated>
  </verify>
  <done>SECURITY.md contains multi-row harness I/O discussion + requirement IDs.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| MCP client → tool args | Strings may be hostile; treat like CLI paths |
| MCP → filesystem | Same graphify-out confinement as CLI |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-40-08 | T | MCP import path | mitigate | `validate_graph_path` before any read |
| T-40-09 | I | tool return text | mitigate | return sanitization summary / validated JSON only |
| T-40-10 | E | large payloads | mitigate | same read caps as Plan 02 |
</threat_model>

<verification>
`pytest tests/test_mcp_harness_io.py -q` and SECURITY.md grep gate in Task 2.
</verification>

<success_criteria>
SEC-03 and SEC-04 satisfied; no MANIFEST-05 regression; D-05 honored.
</success_criteria>

<output>
After completion, create `.planning/phases/40-multi-harness-memory-inverse-import-injection-defenses/40-04-SUMMARY.md`
</output>
