---
phase: 40-multi-harness-memory-inverse-import-injection-defenses
plan: 02
type: execute
wave: 2
depends_on:
  - "40-01"
files_modified:
  - graphify/security.py
  - graphify/harness_import.py
  - tests/test_harness_import.py
autonomous: true
requirements:
  - PORT-03
  - PORT-05
  - SEC-01
must_haves:
  truths:
    - "import_harness_bytes / import_harness_path returns extraction dict that passes validate_extraction after sanitization."
    - "Oversized inputs and forbidden paths fail closed with clear errors."
    - "Representative prompt-injection gadget strings are neutralized or rejected in strict mode (tests)."
  artifacts:
    - path: "graphify/harness_import.py"
      provides: "parse auto|json|claude, sanitize, validate pipeline"
      contains: "import_harness"
    - path: "graphify/security.py"
      provides: "shared size caps / injection-pattern helpers for harness text"
      contains: "harness"
    - path: "tests/test_harness_import.py"
      provides: "validation, path, size, sanitization tests"
      contains: "test_"
  key_links:
    - from: "graphify/harness_import.py"
      to: "graphify.validate.validate_extraction"
      via: "mandatory pre-build validation"
      pattern: "validate_extraction"
---

<objective>
Implement the **library import pipeline**: read harness memory files from disk, **sanitize against injection gadgets** (SEC-01, D-04), enforce **path and size policy** (PORT-05), and emit **`validate_extraction`-clean** dicts (PORT-03) **without** bypass.

Purpose: Shared core for CLI and MCP (SEC-03 later). Must **not** corrupt Phase 39 `elicitation.json` / harness markdown contracts—return extraction dicts only; merge ordering stays Phase 39/02 responsibility.

Output: `harness_import.py`, `security.py` extensions, `tests/test_harness_import.py`.
</objective>

<execution_context>
@$HOME/.cursor/get-shit-done/workflows/execute-plan.md
@$HOME/.cursor/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/40-multi-harness-memory-inverse-import-injection-defenses/40-CONTEXT.md
@.planning/phases/40-multi-harness-memory-inverse-import-injection-defenses/40-RESEARCH.md
@.planning/phases/40-multi-harness-memory-inverse-import-injection-defenses/40-PATTERNS.md
@graphify/security.py
@graphify/validate.py
@graphify/harness_schemas/claude.yaml

<interfaces>
From `graphify.validate`:
- `validate_extraction(data: dict) -> list[str]`, `assert_valid(data: dict) -> None`

From `graphify.security`:
- `sanitize_label`, `validate_graph_path` — exact signatures via grep
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Security helpers + import parse/sanitize/validate</name>
  <files>graphify/security.py, graphify/harness_import.py, tests/test_harness_import.py</files>
  <behavior>
    - JSON interchange v1: invalid schema → clear ValueError; valid → `validate_extraction(extraction)==[]` after sanitization.
    - File larger than cap → error; path outside `validate_graph_path` allowed root → error.
    - Injection fixture strings in labels/bodies become safe per tests (stripped markers or escaped), default **not** whole-file reject (D-04).
  </behavior>
  <action>
    Add **`sanitize_harness_text` / `guard_harness_injection_patterns`** (names discretionary) in `security.py`: apply **`sanitize_label`** to node labels and edge relation strings; bound length; neutralize common **prompt-injection** patterns in free-text **body** fields (executor: 3–5 concrete regexes + tests, e.g. fake system blocks, unbounded `---` directives). Add **read cap** constant consistent with local file policy (mirror similar caps in codebase). Implement **`graphify/harness_import.py`** with `import_harness_path(path: Path, *, format: str = "auto", strict: bool = False, artifacts_root: Path | None = None) -> dict` where `artifacts_root` is used with `validate_graph_path` for **input path** resolution under allowed roots (PORT-05). Parsers: **`json`** loads interchange v1; **`claude`** implements minimal markdown parser for SOUL/HEARTBEAT/USER sections per `claude.yaml` placeholders (Phase 39 shape compatibility); **`auto`** sniffs. **Stdin/URL deferred** (D-03). Optional **`strict=True`**: reject on first high-confidence injection match—must be tested. Populate **`source_file`** on nodes/edges as bounded synthetic strings (e.g. `harness:import:{path.name}`). Attach optional **`harness_import`** provenance flags on nodes only if `validate_extraction` still passes—executor verify optional keys do not break schema. **No** `validate_extraction` monkeypatch.
  </action>
  <verify>
    <automated>pytest tests/test_harness_import.py -q</automated>
  </verify>
  <done>All new tests pass; import returns build-ready dict.</done>
</task>

<task type="auto">
  <name>Task 2: Document import limits + Phase 39 compatibility</name>
  <files>graphify/harness_import.py</files>
  <action>
    In module docstring: document **max file size**, **supported formats**, **auto** sniff rules, **compatibility** with `elicitation.json` (import does not rewrite sidecar), and pointer to **40-RESEARCH** limits for round-trip (D-06).
  </action>
  <verify>
    <automated>python -c "import graphify.harness_import as h; assert h.__doc__ and 'elicitation' in h.__doc__.lower()"</automated>
  </verify>
  <done>Docstring names Phase 39 non-violation explicitly.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Imported harness file | Untrusted text crosses into graph merge — must sanitize (SEC-01) |
| Filesystem path | Must validate against artifacts / graphify-out roots (PORT-05) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-40-03 | I | harness markdown / JSON bodies | mitigate | layered `sanitize_label` + injection pattern guards; optional strict mode |
| T-40-04 | E | oversized file DoS | mitigate | byte cap before parse |
| T-40-05 | T | path traversal | mitigate | `validate_graph_path` + reject `..` components per security.py patterns |
</threat_model>

<verification>
`pytest tests/test_harness_import.py -q`
</verification>

<success_criteria>
PORT-03 library path complete; PORT-05 and SEC-01 enforced with automated tests; Phase 39 contracts preserved per docstring.
</success_criteria>

<output>
After completion, create `.planning/phases/40-multi-harness-memory-inverse-import-injection-defenses/40-02-SUMMARY.md`
</output>
