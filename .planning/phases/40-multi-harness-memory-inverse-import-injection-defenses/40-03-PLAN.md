---
phase: 40-multi-harness-memory-inverse-import-injection-defenses
plan: 03
type: execute
wave: 3
depends_on:
  - "40-02"
files_modified:
  - graphify/__main__.py
  - tests/test_harness_import.py
autonomous: true
requirements:
  - PORT-03
  - PORT-05
must_haves:
  truths:
    - "`graphify import-harness <path>` exists as a single top-level subcommand (D-02)."
    - "`--format auto|json|claude|...` dispatches to library import (D-02)."
    - "Output destination flags mirror harness export / elicit (`resolve_output`, `--out` or project-standard flag)."
  artifacts:
    - path: "graphify/__main__.py"
      provides: "import-harness CLI dispatch"
      contains: "import-harness"
    - path: "tests/test_harness_import.py"
      provides: "CLI integration tests via subprocess or CliRunner pattern used in repo"
      contains: "import-harness"
  key_links:
    - from: "graphify/__main__.py"
      to: "graphify.harness_import.import_harness_path"
      via: "single library call after argv parse"
      pattern: "import_harness_path"
---

<objective>
Expose **`graphify import-harness <path>`** on the CLI with **`--format`** matching D-02 and **path/output parity** with existing harness/elicit commands (D-03).

Purpose: Users and scripts ingest harness memory without MCP; library from Plan 02 remains the only implementation body.

Output: CLI wiring + tests invoking the subcommand.
</objective>

<execution_context>
@$HOME/.cursor/get-shit-done/workflows/execute-plan.md
@$HOME/.cursor/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/40-multi-harness-memory-inverse-import-injection-defenses/40-CONTEXT.md
@graphify/__main__.py
@graphify/harness_import.py

<interfaces>
Executor: locate existing `harness export` / `elicit` argument parsing for `--out`, `resolve_output`, and reuse **identical precedence** in help text.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add import-harness subcommand</name>
  <files>graphify/__main__.py</files>
  <action>
    Register **`import-harness`** (D-02 **single** command; no subcommand-per-format). Arguments: positional `path`; **`--format`** default `auto` with choices `auto`, `json`, `claude` (extend enum as parsers land); **`--strict`** if Plan 02 implemented strict mode; **output flags** mirroring `harness export` — write **validated extraction JSON** (pretty-printed) to resolved artifacts dir (e.g. `graphify-out/harness_import.json` or `--out` path). Call **`import_harness_path`** only—**no** duplicate sanitization. Wire **`resolve_output()`** / `validate_graph_path` for both **input path parent** allowance and **write** target (PORT-05). **Stdin/URL** rejected with message pointing to deferred scope (D-03). Help text references **40-RESEARCH** limits briefly in one line if appropriate.
  </action>
  <verify>
    <automated>pytest tests/test_harness_import.py -q</automated>
  </verify>
  <done>CLI `--help` lists import-harness; path confinement errors tested.</done>
</task>

<task type="auto">
  <name>Task 2: CLI smoke + subprocess contract</name>
  <files>tests/test_harness_import.py</files>
  <action>
    Add integration test: run `graphify import-harness` against **fixture interchange file** from Plan 01 golden path; assert exit code 0 and output file contains **valid** extraction (json.loads + `validate_extraction`). Use **`tmp_path`** only.
  </action>
  <verify>
    <automated>pytest tests/test_harness_import.py::test_cli_import_harness_smoke -q</automated>
  </verify>
  <done>Named test passes (adjust node id if test name differs—keep one focused CLI test).</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| CLI argv → filesystem | Path strings untrusted — must use same validation as library |
| CLI → stdout/stderr | No raw path leakage outside tests |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-40-06 | T | import-harness path | mitigate | delegate to `import_harness_path` + `validate_graph_path` |
| T-40-07 | I | echoed file content | accept | CLI may print summary only—avoid dumping unsanitized bodies |
</threat_model>

<verification>
`pytest tests/test_harness_import.py -q`
</verification>

<success_criteria>
PORT-03 user-facing entry exists; PORT-05 behaviors covered by tests; D-02/D-03 satisfied.
</success_criteria>

<output>
After completion, create `.planning/phases/40-multi-harness-memory-inverse-import-injection-defenses/40-03-SUMMARY.md`
</output>
