---
phase: 65
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - graphify/validate.py
  - graphify/export.py
  - tests/test_validate.py
  - tests/test_export.py
  - tests/fixtures/legacy_v1_12_graph.json
autonomous: true
requirements: [CCONF-05]
tags: [schema, validation, backward-compat, graphify]

must_haves:
  truths:
    - "Loading a frozen pre-1.13 graph.json (no schema_version) passes read validation with no errors"
    - "Writing a graph.json without schema_version fails write validation with an error mentioning schema_version"
    - "graphify/export.py:to_json emits schema_version='1.13' as a top-level JSON key on every new write"
    - "G.graph['schema_version'] round-trips through node_link_data / node_link_graph"
  artifacts:
    - path: "graphify/validate.py"
      provides: "validate_extraction_for_read + validate_extraction_for_write entry points"
      contains: "def validate_extraction_for_read"
    - path: "graphify/validate.py"
      provides: "validate_extraction_for_write requiring schema_version"
      contains: "def validate_extraction_for_write"
    - path: "graphify/export.py"
      provides: "schema_version emission in to_json"
      contains: "schema_version"
    - path: "tests/fixtures/legacy_v1_12_graph.json"
      provides: "frozen real v1.12 graph.json with NO schema_version key"
      contains: "\"nodes\""
    - path: "tests/test_validate.py"
      provides: "schema_version read/write split test cases"
      contains: "test_legacy_v1_12_passes_read"
    - path: "tests/test_export.py"
      provides: "to_json emits schema_version='1.13' test"
      contains: "test_to_json_emits_schema_version"
  key_links:
    - from: "graphify/validate.py:validate_extraction_for_read"
      to: "tests/fixtures/legacy_v1_12_graph.json"
      via: "json.loads + validate_extraction_for_read returns []"
      pattern: "validate_extraction_for_read"
    - from: "graphify/export.py:to_json"
      to: "data['schema_version']"
      via: "G.graph.get('schema_version', '1.13')"
      pattern: "schema_version"
---

<objective>
Land the schema_version read/write split (CCONF-05) and the frozen legacy v1.12 fixture FIRST so subsequent plans (confidence cache + scoring + calibration) emit graphs that pass write validation, while pre-1.13 graphs continue to pass read validation.

Purpose: Establish the read-validation contract that protects v1.10–v1.12 user graphs from breaking, and the write-validation contract that forces every new graph to carry an explicit version stamp.
Output: Two new validate.py entry points; updated export.py; frozen v1.12 fixture; 4 tests covering read accept / write reject / write emission / round-trip.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/65-cconf-per-edge-confidence-cache-split-schema-version/65-CONTEXT.md
@.planning/phases/65-cconf-per-edge-confidence-cache-split-schema-version/65-RESEARCH.md
@.planning/phases/65-cconf-per-edge-confidence-cache-split-schema-version/65-PATTERNS.md
@.planning/phases/65-cconf-per-edge-confidence-cache-split-schema-version/65-VALIDATION.md
@graphify/validate.py
@graphify/export.py

<interfaces>
From graphify/validate.py (existing entry point — extend, do NOT remove):
```python
def validate_extraction(data: dict) -> list[str]:
    """Returns list of error strings - empty list means valid."""
```
Phase 65 adds (per Open Question Q5 recommendation):
```python
def validate_extraction_for_read(data: dict) -> list[str]:
    # schema_version absent ⇒ OK (legacy)
def validate_extraction_for_write(data: dict) -> list[str]:
    # schema_version REQUIRED
```

From graphify/export.py:to_json (existing — line ~313-340):
- Builds `data = json_graph.node_link_data(G, edges="links")`
- Already pulls `data["hyperedges"] = getattr(G, "graph", {}).get("hyperedges", [])`
- Phase 65 inserts ONE LINE: `data["schema_version"] = G.graph.get("schema_version", "1.13")`
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: RED — write failing tests for schema_version read/write split + legacy fixture</name>
  <files>tests/test_validate.py, tests/test_export.py, tests/fixtures/legacy_v1_12_graph.json</files>
  <read_first>
    - tests/test_validate.py (existing test patterns; uses `validate_extraction(data) -> list[str]`, asserts `errors == []` or membership)
    - tests/test_export.py (existing to_json round-trip patterns; uses `tmp_path`)
    - graphify/validate.py (existing `validate_extraction` at line 117; REQUIRED_EDGE_FIELDS at line 10; Phase 53 conditional rules at lines 184-211)
    - graphify/export.py (existing `to_json` at lines 313-340)
    - .planning/phases/65-.../65-PATTERNS.md (analog: "Existing entry shape" lines 117-122 of validate.py; "to_json back-fill" lines 313-330 of export.py)
  </read_first>
  <behavior>
    - Test 1 (test_legacy_v1_12_passes_read): load tests/fixtures/legacy_v1_12_graph.json → validate_extraction_for_read(data) → returns [] (no errors). Fixture has NO schema_version key.
    - Test 2 (test_write_requires_schema_version): same fixture (no schema_version) → validate_extraction_for_write(data) → returns non-empty list, with at least one error string containing "schema_version".
    - Test 3 (test_write_accepts_with_schema_version): copy fixture, set data["schema_version"] = "1.13", → validate_extraction_for_write(data) → returns [] (no errors).
    - Test 4 (test_to_json_emits_schema_version): build a tiny nx.Graph G with one node + one edge (EXTRACTED), call to_json(G, {}, tmp_path / "out.json"), reload JSON, assert data["schema_version"] == "1.13".
    - Test 5 (test_to_json_round_trips_g_graph_attr): set G.graph["schema_version"] = "1.12", call to_json, reload, assert data["schema_version"] == "1.12" (not overwritten when explicitly set on G.graph).
  </behavior>
  <action>
    1. Create `tests/fixtures/legacy_v1_12_graph.json` per D-65.09. Procurement: run on a small fixture corpus and capture the resulting graph.json verbatim. Concrete steps:
       - From a clean git worktree at the v1.12 tag (or last v1.12 commit before phase 63 — `git log --oneline | grep -i 'v1.12'` to find SHA), run `python -m graphify run tests/fixtures/sample_corpus/` (or the smallest existing fixture corpus that produces a non-empty graph.json — prefer one already represented in `tests/fixtures/`).
       - Copy the resulting `graphify-out/graph.json` verbatim to `tests/fixtures/legacy_v1_12_graph.json`.
       - Verify the file does NOT contain the substring `"schema_version"` (grep `"schema_version" tests/fixtures/legacy_v1_12_graph.json` returns nothing).
       - If `git tag --list 'v1.12*'` returns no matching tag, escalate to the user before proceeding — do NOT hand-craft a substitute (D-65.09 explicitly rejects synthetic fixtures because they drift from what v1.10–v1.12 actually wrote).
    2. Add the 5 failing tests above to `tests/test_validate.py` and `tests/test_export.py`. Import `validate_extraction_for_read` and `validate_extraction_for_write` from `graphify.validate` (these don't exist yet — that's the RED state).
    3. Run `pytest tests/test_validate.py tests/test_export.py -x -q` and confirm the 5 new tests fail with `ImportError` or `AttributeError` (RED).
    4. Commit: `test(65-01): add failing tests for schema_version read/write split + legacy v1.12 fixture` per D-65.14 stderr contract not yet relevant (no stderr emission in this task).
  </action>
  <verify>
    <automated>pytest tests/test_validate.py::test_legacy_v1_12_passes_read tests/test_validate.py::test_write_requires_schema_version tests/test_validate.py::test_write_accepts_with_schema_version tests/test_export.py::test_to_json_emits_schema_version tests/test_export.py::test_to_json_round_trips_g_graph_attr -x 2>&1 | grep -E "(FAILED|ImportError|AttributeError)" | grep -v '^#' | wc -l | awk '$1 >= 5 {exit 0} {exit 1}'</automated>
  </verify>
  <acceptance_criteria>
    - `test -f tests/fixtures/legacy_v1_12_graph.json` exits 0
    - `grep -c '"schema_version"' tests/fixtures/legacy_v1_12_graph.json` returns 0 (legacy fixture must NOT contain schema_version)
    - `grep -c '"nodes"' tests/fixtures/legacy_v1_12_graph.json` returns ≥ 1
    - `grep -c "validate_extraction_for_read" tests/test_validate.py` returns ≥ 1
    - `grep -c "validate_extraction_for_write" tests/test_validate.py` returns ≥ 1
    - `grep -c "test_to_json_emits_schema_version" tests/test_export.py` returns ≥ 1
    - All 5 new tests FAIL when run (RED state confirmed)
  </acceptance_criteria>
  <done>5 RED tests committed; legacy v1.12 fixture committed with no schema_version key; tests fail because validate_extraction_for_read/write don't exist yet.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: GREEN — implement validate_extraction_for_read/_for_write + schema_version emission in to_json</name>
  <files>graphify/validate.py, graphify/export.py</files>
  <read_first>
    - graphify/validate.py (full — to extend `validate_extraction` with the two new entry points)
    - graphify/export.py lines 280-340 (to_json + _CONFIDENCE_SCORE_DEFAULTS)
    - .planning/phases/65-.../65-PATTERNS.md sections "graphify/validate.py" and "graphify/export.py" (analog code shapes)
  </read_first>
  <action>
    1. In `graphify/validate.py` (per Open Question Q5 recommendation: TWO entry points):
       - Add at the bottom of the module:
         ```python
         def validate_extraction_for_read(data: dict) -> list[str]:
             """Read-mode: schema_version absent is OK (pre-1.13 legacy graphs)."""
             return validate_extraction(data)

         def validate_extraction_for_write(data: dict) -> list[str]:
             """Write-mode: schema_version REQUIRED."""
             errors = validate_extraction(data)
             if not isinstance(data, dict):
                 return errors  # validate_extraction already reported this
             sv = data.get("schema_version")
             if not isinstance(sv, str) or not sv:
                 errors.append("Missing required key 'schema_version' (write-mode)")
             return errors
         ```
       - Do NOT modify `validate_extraction` itself — it remains the read-mode alias and preserves backward compat for existing callers (per D-65.08).
    2. In `graphify/export.py:to_json` (line ~313-340), add ONE line after `data = json_graph.node_link_data(...)` and before the `for node in data["nodes"]:` loop (mirrors the `data["hyperedges"] = ...` insertion pattern):
       ```python
       data["schema_version"] = G.graph.get("schema_version", "1.13")
       ```
       This ensures: if the caller has set `G.graph["schema_version"]` (round-trip path), use it; otherwise default to `"1.13"` (fresh write).
    3. Run the 5 RED tests from Task 1 — all 5 must now PASS (GREEN).
    4. Run full validate suite: `pytest tests/test_validate.py tests/test_export.py -x -q` — all green, no regressions.
    5. Commit: `feat(65-01): implement schema_version read/write split + emit in to_json (CCONF-05)`.
    6. Stderr contract check: this task introduces NO new stderr emissions. If a future patch adds a `[graphify] info: schema_version missing — read-validating as legacy` breadcrumb, it MUST conform to the two-line `[graphify] info: ...\n  hint: ...` contract from `tests/fixtures/stderr_contract.txt` (Phase 64 D-01) — DO NOT add it in this task; it is discretionary per D-65.08 and out of scope here.
  </action>
  <verify>
    <automated>pytest tests/test_validate.py tests/test_export.py -x -q</automated>
  </verify>
  <acceptance_criteria>
    - `grep -v '^#' graphify/validate.py | grep -c "def validate_extraction_for_read"` returns 1
    - `grep -v '^#' graphify/validate.py | grep -c "def validate_extraction_for_write"` returns 1
    - `grep -v '^#' graphify/validate.py | grep -c "schema_version"` returns ≥ 1
    - `grep -v '^#' graphify/export.py | grep -c 'data\["schema_version"\]'` returns ≥ 1
    - `pytest tests/test_validate.py tests/test_export.py -x -q` exits 0
    - The 5 tests added in Task 1 all pass (GREEN)
    - `pytest tests/ -q` shows zero new failures vs. main (no regression in legacy `validate_extraction` callers)
    - `pytest tests/test_stderr_contract.py -x -q` (if exists from Phase 64) still passes (no contract violation)
  </acceptance_criteria>
  <done>validate.py exposes validate_extraction_for_read + validate_extraction_for_write; export.py:to_json emits schema_version="1.13" by default and round-trips G.graph["schema_version"]; all 5 Task 1 tests pass; full suite stays green.</done>
</task>

</tasks>

<verification>
- All 5 schema_version tests pass.
- `tests/fixtures/legacy_v1_12_graph.json` exists, has no `schema_version` key, and parses as JSON.
- Existing `validate_extraction` callers continue to work (no signature change).
- `pytest tests/ -q` full suite green.
</verification>

<success_criteria>
- [ ] `validate_extraction_for_read` and `validate_extraction_for_write` exposed from `graphify/validate.py`
- [ ] `tests/fixtures/legacy_v1_12_graph.json` committed (no `schema_version` key)
- [ ] `to_json` emits `schema_version` at top level on every write
- [ ] `G.graph["schema_version"]` round-trips
- [ ] CCONF-05 success criteria 3 met: legacy fixture passes read, missing on write fails
- [ ] No regressions in stderr contract snapshot test from Phase 64
</success_criteria>

<output>
After completion, create `.planning/phases/65-cconf-per-edge-confidence-cache-split-schema-version/65-01-SUMMARY.md`.
</output>
