---
phase: 49
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified:
  - graphify/version.py
  - graphify/__main__.py
  - graphify/capability.py
  - graphify/harness_interchange.py
  - graphify/elicit.py
  - tests/test_main_cli.py
  - tests/test_main_flags.py
autonomous: true
requirements:
  - CLI-VER-01
  - CLI-VER-02
must_haves:
  truths:
    - "`graphify --version` and `graphify -V` print one stdout line and exit 0 without skill-version stderr checks"
    - "Successful non-install CLI exits emit `[graphify] version` on stderr once (except suppressed paths per 49-CONTEXT D-49.03)"
    - "`graphify.version.package_version()` is the single runtime reader for `graphifyy` metadata outside tests"
---

<objective>
Ship Phase 49: CLI `--version` / `-V`, stderr success version line, directional skill stamp warnings, consolidate `importlib.metadata.version("graphifyy")` (**D-49.01–D-49.08**).
</objective>

<threat_model>
| Threat | Mitigation |
|--------|------------|
| Circular imports | `graphify.version` imports stdlib only |
| Footer on error paths | `_cli_exit` only prints footer when `code == 0` |
| Install/uninstall noise | Suppress footer via argv heuristic per CONTEXT |
</threat_model>

<tasks>

<task type="execute">
  <name>49-01-01 — graphify.version + migrate readers</name>
  <read_first>
    - graphify/__main__.py (lines 1–30)
    - graphify/capability.py (head ~30)
    - graphify/harness_interchange.py (head ~30)
    - graphify/elicit.py (pkg_version usage ~340)
  </read_first>
  <acceptance_criteria>
    - `graphify/version.py` exists and defines `package_version() -> str`
    - `grep -R "importlib.metadata" graphify/capability.py graphify/harness_interchange.py graphify/elicit.py` shows no direct `version("graphifyy")` for package version (use `package_version` instead)
    - `pytest tests/test_capability.py tests/test_harness_interchange.py -q` exits 0
  </acceptance_criteria>
  <action>
    Add `graphify/version.py` with `package_version()` mirroring prior try/except semantics. Replace `_graphify_version` / `_package_version` / elicit inline metadata with imports from `graphify.version`. Set `graphify/__main__.__version__ = package_version()` at module init.
  </action>
</task>

<task type="execute">
  <name>49-01-02 — CLI --version, skill-check skip, _cli_exit footer</name>
  <read_first>
    - graphify/__main__.py (`main`, `_check_skill_version`, all `sys.exit(0)` sites)
    - .planning/phases/49-add-version-flag-to-graphify-command-and-also-print-current-/49-CONTEXT.md
  </read_first>
  <acceptance_criteria>
    - `python -m graphify --version` returns 0 and stdout matches `^graphify .+` (regex); stderr has no `warning: skill is from`
    - `python -m graphify -V` same as `--version`
    - `python -m graphify --validate-profile &lt;empty_tmp_dir&gt;` stderr contains `[graphify] version`
    - `pytest tests/test_main_cli.py tests/test_main_flags.py -q` exits 0
  </acceptance_criteria>
  <action>
    After `_strip_leading_vault_global_argv`, if `sys.argv[1]` is `--version` or `-V`, print `graphify {package_version()}` to stdout and `raise SystemExit(0)` before skill-version loop. Extend `_skip_skill_version_check` to include `--version`/`-V`. Implement `_suppress_success_version_footer(argv)` per CONTEXT (install + `&lt;platform&gt; install|uninstall` + `-h`/`--help`/short argv). Implement `_cli_exit(code: int) -> NoReturn` that on `code==0` and not suppressed prints `[graphify] version {package_version()}` to stderr, then `raise SystemExit(code)`. Replace every `sys.exit(0)` in `__main__.py` with `_cli_exit(0)` except intentionally omitted if any branch must bypass footer (none expected). Refine `_check_skill_version` messages for older vs newer stamp using numeric tuple comparison from digit runs (no new deps).
  </action>
</task>

</tasks>
