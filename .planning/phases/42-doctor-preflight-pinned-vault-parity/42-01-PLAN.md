---
phase: 42-doctor-preflight-pinned-vault-parity
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - graphify/doctor.py
  - tests/test_doctor.py
autonomous: true
requirements:
  - VCLI-03
must_haves:
  truths:
    - "validate_profile_preflight receives the same vault root as profile_home when Phase 41 pins apply."
    - "A unit test proves CWD without .graphify cannot mask invalid profile in pinned vault."
  artifacts:
    - path: "tests/test_doctor.py"
      provides: "Regression test for pinned vault preflight path."
      contains: "test_run_doctor_preflight_uses_pinned_vault_not_cwd"
---

<objective>
Close VCLI-03: doctor’s profile preflight must validate the pinned vault (`profile_home`), not the process CWD, when they differ.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
</execution_context>

<context>
@.planning/phases/42-doctor-preflight-pinned-vault-parity/42-CONTEXT.md
@.planning/phases/42-doctor-preflight-pinned-vault-parity/42-RESEARCH.md
</context>

<tasks>

<task type="execute">
  <name>Fix preflight path in run_doctor</name>
  <files>graphify/doctor.py</files>
  <read_first>graphify/doctor.py, graphify/profile.py (validate_profile_preflight docstring)</read_first>
  <action>In `run_doctor`, in the block that checks `profile_yaml` / `templates_dir` under `profile_home`, change the call from `validate_profile_preflight(cwd_resolved)` to `validate_profile_preflight(profile_home)`. Adjust the adjacent comment if it still implies CWD is the vault root for preflight.</action>
  <acceptance_criteria>`rg -n "validate_profile_preflight\\(profile_home\\)" graphify/doctor.py` matches exactly one line in the profile validation block; `rg "validate_profile_preflight\\(cwd_resolved\\)" graphify/doctor.py` returns no matches.</acceptance_criteria>
</task>

<task type="execute">
  <name>Add regression test for CWD vs pinned vault</name>
  <files>tests/test_doctor.py</files>
  <read_first>tests/test_doctor.py, graphify/output.py (ResolvedOutput)</read_first>
  <action>Add `test_run_doctor_preflight_uses_pinned_vault_not_cwd`: create a vault with an invalid profile (reuse `test_run_doctor_invalid_profile` style bad YAML). Use a separate `cwd_elsewhere` directory under tmp_path with **no** `.graphify/`. Build `ResolvedOutput(True, vault.resolve(), vault.resolve() / "Atlas" / "Generated", vault.resolve().parent / "graphify-out", "vault-cli", ())` (or equivalent valid notes/artifacts paths consistent with other tests). Call `run_doctor(cwd_elsewhere, resolved_output=resolved)`. Assert `report.profile_validation_errors` is non-empty (invalid mode or schema errors from the vault profile).</action>
  <acceptance_criteria>`pytest tests/test_doctor.py::test_run_doctor_preflight_uses_pinned_vault_not_cwd -q` exits 0.</acceptance_criteria>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| vault_dir argument | Must remain a resolved vault root passed to read-only preflight — no writes |

## STRIDE (summary)

| Threat | Mitigation |
|--------|------------|
| Wrong tree validated | Single source: `profile_home` matches profile file paths |

</threat_model>

<verification>
`pytest tests/test_doctor.py -q` then `pytest tests/ -q`
</verification>
