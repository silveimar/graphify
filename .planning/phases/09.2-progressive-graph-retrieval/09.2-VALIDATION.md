---
phase: 9.2
slug: progressive-graph-retrieval
status: draft
nyquist_compliant: false
wave_0_complete: false
created: 2026-04-16
---

# Phase 9.2 — Validation Strategy

> Per-phase validation contract for feedback sampling during execution.

---

## Test Infrastructure

| Property | Value |
|----------|-------|
| **Framework** | pytest (already present in graphify, see tests/ and pyproject.toml) |
| **Config file** | pyproject.toml `[tool.pytest]` (existing) |
| **Quick run command** | `pytest tests/test_serve.py -q` |
| **Full suite command** | `pytest tests/ -q` |
| **Estimated runtime** | ~15 seconds (test_serve.py subset); ~60 seconds full suite |

---

## Sampling Rate

- **After every task commit:** Run `pytest tests/test_serve.py -q`
- **After every plan wave:** Run `pytest tests/ -q`
- **Before `/gsd-verify-work`:** Full suite must be green
- **Max feedback latency:** 15 seconds (quick) / 60 seconds (full)

---

## Per-Task Verification Map

> Planner fills per-task rows during PLAN.md generation. Populated from RESEARCH.md §Validation Architecture (22 unit tests + 1 integration test mapped to TOKEN-01/02/03; TOKEN-04 marked N/A per D-09).

| Task ID | Plan | Wave | Requirement | Threat Ref | Secure Behavior | Test Type | Automated Command | File Exists | Status |
|---------|------|------|-------------|------------|-----------------|-----------|-------------------|-------------|--------|
| 09.2-XX-YY | TBD | TBD | TOKEN-01 | — | Layer 1 response ≤ budget tokens | unit | `pytest tests/test_serve.py::test_layer1_token_budget -q` | ❌ W0 | ⬜ pending |
| 09.2-XX-YY | TBD | TBD | TOKEN-02 | — | Cardinality estimate returned before traversal at depth ≥ 2 | unit | `pytest tests/test_serve.py::test_estimate_query_cost -q` | ❌ W0 | ⬜ pending |
| 09.2-XX-YY | TBD | TBD | TOKEN-03 | — | depth ≥ 3 query uses bidirectional BFS, `search_strategy` field set | unit | `pytest tests/test_serve.py::test_bidirectional_bfs_strategy -q` | ❌ W0 | ⬜ pending |

*Status: ⬜ pending · ✅ green · ❌ red · ⚠️ flaky*

*Planner MUST expand this table with one row per task, citing exact `pytest ...::test_name` commands from the 22 tests enumerated in 09.2-RESEARCH.md §Validation Architecture.*

---

## Wave 0 Requirements

- [ ] `tests/test_serve.py` — create new test file per CLAUDE.md "one test file per module" convention (pairs with graphify/serve.py). Seed with stubs for TOKEN-01/TOKEN-02/TOKEN-03 test names from RESEARCH.md §Validation Architecture.
- [ ] `tests/conftest.py` — add shared `tmp_graph_json` fixture if not already present (synthetic NetworkX graph → graphify-out/graph.json writer) so serve.py tests can load deterministic graphs without touching the live graph.
- [ ] No framework install needed — pytest is already a dev dependency.

*TOKEN-04 (Bloom filter + closure cache) is deferred per CONTEXT D-09 and gets no Wave 0 entries.*

---

## Manual-Only Verifications

| Behavior | Requirement | Why Manual | Test Instructions |
|----------|-------------|------------|-------------------|
| End-to-end agent consumption | TOKEN-01..03 | Validates real MCP client sees layered responses correctly — can't be automated without spinning up a stdio client | 1. Run `graphify run .` on a sample corpus with ≥500 nodes. 2. Connect Claude Code as MCP client. 3. Call `query_graph(budget=500, depth=2)`. 4. Verify response shape matches Layer 1 (node IDs + labels + community, no edges). 5. Call follow-up expansion. 6. Verify `search_strategy: "bidirectional_bfs"` in telemetry for depth=3 query. |

---

## Validation Sign-Off

- [ ] All tasks have `<automated>` verify or Wave 0 dependencies
- [ ] Sampling continuity: no 3 consecutive tasks without automated verify
- [ ] Wave 0 covers all MISSING references
- [ ] No watch-mode flags
- [ ] Feedback latency < 15s (quick) / 60s (full)
- [ ] `nyquist_compliant: true` set in frontmatter

**Approval:** pending
