---
phase: 43-elicitation-run-pipeline-elic02
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - graphify/watch.py
  - tests/test_watch.py
autonomous: true
requirements:
  - ELIC-02
must_haves:
  truths:
    - "`_rebuild_code` incorporates `<watch_path>/graphify-out/elicitation.json` into the merged extraction list before graph construction."
    - "When no sidecar exists, behavior matches prior `build_from_json(result)` semantics for the same `result` dict."
    - "A unit test proves an elicitation-only node id appears in the rebuilt `graph.json` when sidecar is present."
  artifacts:
    - path: "graphify/watch.py"
      provides: "Watch rebuild merges elicitation sidecar"
      contains: "merge_elicitation_into_build_inputs"
    - path: "tests/test_watch.py"
      provides: "Rebuild includes elicitation nodes"
      contains: "_rebuild_code"
  key_links:
    - from: "graphify/watch.py"
      to: "graphify.build.build"
      via: "_rebuild_code"
      pattern: "merge_elicitation_into_build_inputs"
---

<objective>
After AST extraction and optional semantic merge from existing `graph.json`, merge `elicitation.json` from `watch_path / \"graphify-out\"` using `merge_elicitation_into_build_inputs([result], out)` then call `build(...)` instead of `build_from_json(result)` alone. Ensures watch-mode incremental rebuilds stay consistent with ELIC-02 merge ordering.

Output: `watch.py` change + `tests/test_watch.py` coverage without requiring the `watchdog` dependency for the new test (call `_rebuild_code` directly).
</objective>

<execution_context>
@$HOME/.cursor/get-shit-done/workflows/execute-plan.md
</execution_context>

<context>
@.planning/phases/43-elicitation-run-pipeline-elic02/43-CONTEXT.md
@graphify/watch.py
@graphify/elicit.py
@graphify/build.py
@tests/test_watch.py

<interfaces>
- `_rebuild_code(watch_path: Path, *, follow_symlinks: bool = False) -> bool` — public for testing imports.
- Use `build(merge_elicitation_into_build_inputs([result], out), directed=False)` to mirror previous undirected default from `build_from_json`.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Merge elicitation in _rebuild_code</name>
  <read_first>
    <file>graphify/watch.py</file>
  </read_first>
  <files>graphify/watch.py</files>
  <behavior>
    Import `build` from `graphify.build` and `merge_elicitation_into_build_inputs` from `graphify.elicit` inside `_rebuild_code` (keep lazy imports consistent with the rest of the function).
    After computing `result` (including merge from existing graph.json), set `out = watch_path / \"graphify-out\"`, then `G = build(merge_elicitation_into_build_inputs([result], out))` instead of `G = build_from_json(result)`.
    Remove unused `build_from_json` import if no longer referenced.
  </behavior>
  <verify>
    <automated>pytest tests/test_watch.py -q</automated>
  </verify>
</task>

<task type="auto">
  <name>Task 2: Test elicitation node survives rebuild</name>
  <read_first>
    <file>tests/test_elicit.py</file>
  </read_first>
  <files>tests/test_watch.py</files>
  <behavior>
    Create `tmp_path` layout: minimal `x.py` with a trivial class (so `detect` finds code files), `graphify-out/` directory, write `elicitation.json` using `save_elicitation_sidecar` + scripted elicitation extraction (reuse `test_elicit` helpers).
    Optionally omit initial `graph.json` or seed empty nodes — goal is to ensure after `_rebuild_code(tmp_path)` the written `graphify-out/graph.json` contains at least one node id from elicitation (e.g. `elicitation_hub` from default session extraction).
    Assert JSON written contains that node `id`.
  </behavior>
  <verify>
    <automated>pytest tests/test_watch.py -q -k rebuild</automated>
  </verify>
</task>

</tasks>

<verification>
## Nyquist / acceptance

- [ ] With sidecar absent, rebuild still produces valid `graph.json` (merge helper returns single-element list behavior unchanged).
- [ ] `pytest tests/test_watch.py -q` passes full file.
- [ ] ELIC-02: merged payload validated inside `build` / `build_from_json` as existing pipeline.

</verification>
