---
phase: 43-elicitation-run-pipeline-elic02
plan: 03
type: execute
wave: 2
depends_on:
  - "43-01-PLAN.md"
  - "43-02-PLAN.md"
files_modified:
  - docs/ELICITATION.md
  - graphify/__main__.py
autonomous: true
requirements:
  - ELIC-02
  - ELIC-07
must_haves:
  truths:
    - "`docs/ELICITATION.md` explains which CLI paths call `build()` with sidecar merge (migration `update-vault`, `watch` rebuild) vs `graphify run` (extract-only unless documented otherwise)."
    - "Users can discover from docs how `elicitation.json` joins the graph without reading source."
    - "`graphify run` help or adjacent docstring clarifies that full graph persistence uses workflows that invoke `build` (not extract-only `run`)."
  artifacts:
    - path: "docs/ELICITATION.md"
      provides: "ELIC-07 discovery — merge surfaces and CLI mental model"
      contains: "build"
    - path: "graphify/__main__.py"
      provides: "CLI discoverability for run vs graph-producing commands"
      contains: "run"
  key_links:
    - from: "docs/ELICITATION.md"
      to: ".planning/REQUIREMENTS.md"
      via: "ELIC-07 traceability"
      pattern: "ELIC-07"
---

<objective>
Close **ELIC-07** alongside ELIC-02: documentation and CLI hints reflect the implemented merge wiring from Plans 01–02. No new merge logic — clarity only.

Output: Updated `docs/ELICITATION.md`, targeted `__main__.py` help text for `run` (and optionally `elicit`) sub-parser strings so `--help` surfaces the contract.
</objective>

<execution_context>
@$HOME/.cursor/get-shit-done/workflows/execute-plan.md
</execution_context>

<context>
@.planning/phases/43-elicitation-run-pipeline-elic02/43-CONTEXT.md
@docs/ELICITATION.md
@graphify/__main__.py

<interfaces>
- Preserve argparse structure — extend `help=` strings only unless a dedicated epilog is already used for `run`.
- Docs must stay accurate if `run` remains extract-only (state explicitly).
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Refresh ELICITATION.md merge surfaces</name>
  <read_first>
    <file>docs/ELICITATION.md</file>
  </read_first>
  <files>docs/ELICITATION.md</files>
  <behavior>
    Add a short subsection **“Where sidecar merge runs”** listing:
    - Vault migration preview (`run_update_vault` / `graphify update-vault`) — merges before graph build from `resolved.artifacts_dir`.
    - `graphify watch` incremental rebuild — merges from `<project>/graphify-out/elicitation.json`.
    - Explicitly note **`graphify run`** performs detect→extract only (no `graph.json` write today); elicitation joins the graph when using the above paths or any future command that calls `build()` with artifacts dir — align wording with post–Plan 01/02 code.
    - Keep requirement IDs ELIC-02 / ELIC-07 referenced.
  </behavior>
  <verify>
    <automated>grep -q "update-vault\\|watch\\|run" docs/ELICITATION.md</automated>
  </verify>
</task>

<task type="auto">
  <name>Task 2: Clarify `run` command help</name>
  <read_first>
    <file>graphify/__main__.py</file>
    <offset>2520</file>
    <limit>100</limit>
  </read_first>
  <files>graphify/__main__.py</files>
  <behavior>
    Locate the `run` command branch argparse / doc path (inline parser). Extend the help string by one sentence: extract-only; graph assembly + `elicitation.json` merge occur in `update-vault`, `watch` rebuild, and other pipelines that call `build()` — phrase as user-facing, not internal function names overload.
  </behavior>
  <verify>
    <automated>graphify run --help | head -40</automated>
  </verify>
</task>

</tasks>

<verification>
## Nyquist / acceptance

- [ ] Docs link requirement IDs and describe merge surfaces without contradicting code.
- [ ] `graphify run --help` mentions extract-only vs graph-producing workflows.
- [ ] No regression: `pytest tests/ -q` still passes after Plans 01–02 merge (run before doc-only commit if batching).

</verification>
