---
phase: 43-elicitation-run-pipeline-elic02
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - graphify/migration.py
  - tests/test_migration.py
autonomous: true
requirements:
  - ELIC-02
must_haves:
  truths:
    - "`run_update_vault` preview path merges `elicitation.json` from `resolved.artifacts_dir` before `build()` when the sidecar exists."
    - "Ordering is corpus extraction first, elicitation extraction last (duplicate `id` favors elicitation)."
    - "`tmp_path` tests prove `build()` receives a merged extraction list when a valid sidecar is present."
  artifacts:
    - path: "graphify/migration.py"
      provides: "Elicitation sidecar merged before graph assembly in vault migration workflow"
      contains: "merge_elicitation_into_build_inputs"
    - path: "tests/test_migration.py"
      provides: "Regression test for merged build inputs on preview path"
      contains: "elicitation"
  key_links:
    - from: "graphify/migration.py"
      to: "graphify.elicit.merge_elicitation_into_build_inputs"
      via: "run_update_vault preview"
      pattern: "merge_elicitation_into_build_inputs"
---

<objective>
Wire `merge_elicitation_into_build_inputs` into `run_update_vault` so vault migration previews include elicitation sidecar data in the graph before `cluster` / `to_obsidian`. Satisfies **ELIC-02** for the `update-vault` / migration code path.

Output: One-line code change plus focused tests; no behavior change when sidecar is absent.
</objective>

<execution_context>
@$HOME/.cursor/get-shit-done/workflows/execute-plan.md
</execution_context>

<context>
@.planning/phases/43-elicitation-run-pipeline-elic02/43-CONTEXT.md
@.planning/REQUIREMENTS.md
@graphify/migration.py
@graphify/elicit.py
@tests/test_migration.py

<interfaces>
- `run_update_vault(...)` return shape unchanged (still returns preview dict / apply dict).
- `merge_elicitation_into_build_inputs(extractions, artifacts_dir)` — second arg must be `resolved.artifacts_dir` (Path).
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Merge sidecar before build in migration</name>
  <read_first>
    <file>graphify/migration.py</file>
    <file>graphify/elicit.py</file>
  </read_first>
  <files>graphify/migration.py</files>
  <behavior>
    Replace `G = build([extraction])` with building from `merge_elicitation_into_build_inputs([extraction], resolved.artifacts_dir)`.
    Import `merge_elicitation_into_build_inputs` from `graphify.elicit` at function scope (match existing lazy imports in `run_update_vault`).
  </behavior>
  <verify>
    <automated>pytest tests/test_migration.py -q</automated>
  </verify>
</task>

<task type="auto">
  <name>Task 2: Test merged list reaches build when sidecar exists</name>
  <read_first>
    <file>tests/test_migration.py</file>
    <file>tests/test_elicit.py</file>
  </read_first>
  <files>tests/test_migration.py</files>
  <behavior>
    Add `tmp_path` test using `_make_update_vault_fixture` (or equivalent vault + raw corpus minimal layout).
    Before `run_update_vault`, ensure `resolve_output(vault).artifacts_dir` exists and write a valid `elicitation.json` via `save_elicitation_sidecar` + `run_scripted_elicitation` / `build_extraction_from_session` (same pattern as `tests/test_elicit.py`).
    Monkeypatch `graphify.build.build` to record the `extractions` argument; call `run_update_vault(input_dir=raw, vault_dir=vault)` with `apply=False`.
    Assert the first call passed a **list of length 2** when sidecar is present (corpus extraction + elicitation extraction).
    Add control case: no sidecar file → patched `build` receives a list of length **1** (merge helper returns single extraction only).
  </behavior>
  <verify>
    <automated>pytest tests/test_migration.py -q -k elicit</automated>
  </verify>
</task>

</tasks>

<verification>
## Nyquist / acceptance

- [ ] `rg -n "merge_elicitation_into_build_inputs" graphify/migration.py` shows import and single use before `build`.
- [ ] `pytest tests/test_migration.py -q` passes.
- [ ] ELIC-02 AC: elicitation-fed data reaches `build()` after merge (validated inside `build_from_json` as today).

</verification>
