---
phase: 67
plan: 03
type: execute
wave: 1
depends_on: []
files_modified:
  - tests/fixtures/cquery_v1_12/graph.json
  - tests/fixtures/cquery_v1_12/golden_concept_code_hops.json
  - tests/fixtures/cquery_v1_12/README.md
autonomous: true
requirements: [CQUERY-02]
tags: [cquery, fixture, regression]
must_haves:
  truths:
    - "Frozen v1.12-shaped graph.json fixture exists and is checked into the repo"
    - "Frozen golden output for concept_code_hops with no new params is checked into the repo"
    - "Both files are deterministic, hand-curated, and described in a README so future devs know not to regenerate"
  artifacts:
    - path: tests/fixtures/cquery_v1_12/graph.json
      provides: "Stable v1.12-shaped graph (nodes+edges with confidence/confidence_score) for byte-identity assertion"
    - path: tests/fixtures/cquery_v1_12/golden_concept_code_hops.json
      provides: "Expected concept_code_hops result with all CQUERY params omitted"
    - path: tests/fixtures/cquery_v1_12/README.md
      provides: "Contract: do-not-regenerate; what shape this guarantees; how to validate manually"
  key_links:
    - from: tests/fixtures/cquery_v1_12/golden_concept_code_hops.json
      to: graphify/serve.py::_run_concept_code_hops
      via: "Wave-2 plan 04 asserts deep_equal(result, golden) when CQUERY params are None"
      pattern: "golden_concept_code_hops"
---

<objective>
One-shot artifact plan: hand-curate a small, deterministic v1.12-shaped graph fixture plus the exact JSON output `concept_code_hops` should produce when called with NO new CQUERY params. This fixture is the byte-identity oracle Wave-2 Plan 04 will assert against (CQUERY-02).

Purpose: Lock the v1.12 contract in committed bytes so any future regression in `_run_concept_code_hops` (when all three new kwargs are None) is detected.
Output: Three files under `tests/fixtures/cquery_v1_12/`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/67-cdrift-cquery-edge-level-drift-drift-py-parameterized-concept-queries/67-CONTEXT.md
@graphify/serve.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create the frozen fixture pair + README</name>
  <files>tests/fixtures/cquery_v1_12/graph.json, tests/fixtures/cquery_v1_12/golden_concept_code_hops.json, tests/fixtures/cquery_v1_12/README.md</files>
  <read_first>
    - graphify/serve.py lines 2290-2566 (_run_concept_code_hops body — output shape)
    - graphify/serve.py line 3553 (_tool_concept_code_hops MCP entry — JSON envelope shape)
    - tests/test_serve.py (existing fixture conventions for this module)
  </read_first>
  <action>
    1. Build a small v1.12-shaped `graph.json` using `networkx.readwrite.json_graph.node_link_data`. The graph MUST contain:
       - At least 6 nodes mixing `file_type: "code"`, `file_type: "document"`, and one `file_type: "rationale"` (concept node).
       - At least one `implements` edge with `confidence: "EXTRACTED"`, one `documents` edge with `confidence: "INFERRED", confidence_score: 0.65`, one `tests` edge with `confidence: "INFERRED", confidence_score: 0.85`.
       - Stable, deterministic node IDs (lowercase_with_underscores per CONVENTIONS.md): e.g., `concept_auth`, `file_auth_py`, `file_auth_md`, `file_test_auth_py`.
       - All required node fields per validate.py: `id`, `label`, `file_type`, `source_file`, `source_location`.
       - All required edge fields: `source`, `target`, `relation`, `confidence`, `source_file`.
    2. Run `concept_code_hops` IN-PROCESS via `python -c "..."` against this graph with NO CQUERY params (all defaults), capture the JSON result, and write it verbatim to `golden_concept_code_hops.json` with `indent=2, sort_keys=True` for diff stability.
    3. Write `README.md` containing:
       - "Frozen v1.12 fixture for CQUERY-02 byte-identity. DO NOT regenerate."
       - The exact concept node and starting parameters used (e.g., `concept_id="concept_auth", max_hops=2`).
       - A note: "If `_run_concept_code_hops` legitimately changes its v1.12 path, this fixture must be updated by a human in the same commit, with rationale in the commit message."

    Validation step before commit: `python -c "import json; json.load(open('tests/fixtures/cquery_v1_12/graph.json')); json.load(open('tests/fixtures/cquery_v1_12/golden_concept_code_hops.json'))"` exits 0.

    Commit: `test(67-03): freeze v1.12 concept_code_hops fixture for CQUERY-02 byte-identity oracle`.
  </action>
  <verify>
    <automated>python -c "import json; assert isinstance(json.load(open('tests/fixtures/cquery_v1_12/graph.json')), dict); assert isinstance(json.load(open('tests/fixtures/cquery_v1_12/golden_concept_code_hops.json')), (dict, list))"</automated>
  </verify>
  <acceptance_criteria>
    - Three files exist under `tests/fixtures/cquery_v1_12/`
    - `graph.json` parses as JSON and contains keys consistent with `node_link_data` output (`nodes`, `links` or `edges`)
    - `golden_concept_code_hops.json` parses as JSON
    - `grep -c "DO NOT regenerate" tests/fixtures/cquery_v1_12/README.md` ≥ 1
    - At least one edge with `confidence_score` 0.85 and one with 0.65 present in graph.json (verifiable via `grep -c "0.85\|0.65" tests/fixtures/cquery_v1_12/graph.json` ≥ 2)
    - Files are committed
  </acceptance_criteria>
  <done>Fixture trio committed and parseable; ready for Wave-2 byte-identity assertion.</done>
</task>

</tasks>

<verification>
`python -m json.tool tests/fixtures/cquery_v1_12/graph.json > /dev/null`
`python -m json.tool tests/fixtures/cquery_v1_12/golden_concept_code_hops.json > /dev/null`
</verification>

<success_criteria>
- Three files exist and are valid JSON / markdown
- README documents the do-not-regenerate contract
</success_criteria>

<output>
After completion, create `.planning/phases/67-*/67-03-SUMMARY.md`
</output>
