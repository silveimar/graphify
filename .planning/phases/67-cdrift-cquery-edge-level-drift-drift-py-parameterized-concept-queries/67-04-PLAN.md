---
phase: 67
plan: 04
type: execute
wave: 2
depends_on: [67-02, 67-03]
files_modified:
  - graphify/serve.py
  - tests/test_concept_code_hops.py
autonomous: true
requirements: [CQUERY-01, CQUERY-02]
tags: [cquery, mcp, bfs, byte-identity]
must_haves:
  truths:
    - "concept_code_hops accepts three new optional kwargs: min_confidence, relations, confidence_band — all default None"
    - "BFS traversal applies the Wave-1 predicate; edges failing the predicate are not followed and not returned"
    - "When all three new kwargs are None, the result is deep-equal to tests/fixtures/cquery_v1_12/golden_concept_code_hops.json (CQUERY-02)"
    - "MCP tool schema in _tool_concept_code_hops advertises the three new optional fields with correct JSON Schema types"
  artifacts:
    - path: graphify/serve.py
      provides: "Updated _run_concept_code_hops signature + MCP schema in _tool_concept_code_hops"
    - path: tests/test_concept_code_hops.py
      provides: "Byte-identity test against golden fixture + integration tests for each new param"
  key_links:
    - from: graphify/serve.py::_run_concept_code_hops
      to: graphify/serve.py::_build_concept_hops_filter
      via: "kwargs threaded to predicate factory; predicate gates BFS edge traversal"
      pattern: "_build_concept_hops_filter"
---

<objective>
Wire CQUERY into the existing BFS path. Thread `min_confidence`, `relations`, `confidence_band` through `_run_concept_code_hops` (serve.py:2290-2566) and the MCP entry `_tool_concept_code_hops` (serve.py:3553). Apply the Plan-02 predicate inside the BFS edge-iteration loop. Add the byte-identity regression test against the Plan-03 golden fixture (CQUERY-02).

Purpose: CQUERY-01 integration + CQUERY-02 oracle assertion.
Output: Updated `serve.py` + new tests in `tests/test_concept_code_hops.py`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/67-cdrift-cquery-edge-level-drift-drift-py-parameterized-concept-queries/67-CONTEXT.md
@.planning/phases/67-*/67-02-SUMMARY.md
@.planning/phases/67-*/67-03-SUMMARY.md
@graphify/serve.py
@tests/fixtures/cquery_v1_12/README.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Thread CQUERY kwargs into _run_concept_code_hops + MCP schema</name>
  <files>graphify/serve.py</files>
  <read_first>
    - graphify/serve.py lines 2290-2566 (BFS body — find the edge-iteration loop)
    - graphify/serve.py around line 3553 (_tool_concept_code_hops MCP schema dict)
    - graphify/serve.py around line 2246 (Plan-02 helpers landed here)
  </read_first>
  <behavior>
    - `_run_concept_code_hops` accepts `min_confidence=None, relations=None, confidence_band=None` as new keyword-only args (with `*,` separator if appropriate)
    - When all three are None, control flow is byte-identical to v1.12 (predicate is identity, no extra branches taken outside a single guard)
    - When any is set, BFS skips edges where `predicate(edge_data)` is False — neighbor not enqueued, edge not included in returned result
    - MCP schema in `_tool_concept_code_hops` advertises the three new fields:
      - `min_confidence`: number, minimum 0.0, maximum 1.0, optional
      - `relations`: array of string, optional
      - `confidence_band`: enum ["high","medium","low"], optional
  </behavior>
  <action>
    1. Modify `_run_concept_code_hops` signature: append three keyword-only args `min_confidence: float | None = None, relations: list[str] | None = None, confidence_band: str | None = None`. Place them after existing optional kwargs.

    2. At the top of the function body (before the BFS loop), build the predicate exactly once:
       ```python
       _edge_filter = _build_concept_hops_filter(
           min_confidence=min_confidence,
           relations=relations,
           confidence_band=confidence_band,
       )
       ```
       Note: when all three args are None the resulting predicate is identity (returns True for every edge — proven in Plan 02 tests). The added branch cost is one boolean call per edge; no other branches change.

    3. Inside the existing BFS edge-iteration loop in `_run_concept_code_hops` (between lines 2290-2566 — locate via `grep -n "for .* in G.edges\|G\\[.*\\]\\|neighbors" graphify/serve.py`), wrap the body of the per-edge step:
       ```python
       if not _edge_filter(edge_data):
           continue
       ```
       Place this AS THE FIRST statement after `edge_data` is bound. This preserves enqueue/dedup semantics for edges that pass; failing edges are skipped before any side effect.

    4. Update `_tool_concept_code_hops` (around line 3553) JSON schema `inputSchema.properties` to advertise:
       ```python
       "min_confidence": {"type": "number", "minimum": 0.0, "maximum": 1.0, "description": "AND-combined with confidence_band; filters edges by per-edge confidence_score (Phase 67 CQUERY-01)."},
       "relations":      {"type": "array", "items": {"type": "string"}, "description": "Whitelist of edge relation strings; [] means zero-match (Phase 67 D-12)."},
       "confidence_band":{"type": "string", "enum": ["high", "medium", "low"], "description": "Cutpoints high>=0.8, medium 0.5<=x<0.8, low<0.5 (Phase 67 D-10)."}
       ```
       None of the three is added to `required`.

    5. In the MCP tool handler, validate inputs via the helpers from Plan 02 (`_validate_relations_filter_arg(args.get("relations"))`, `_resolve_confidence_band(args.get("confidence_band"))`, and a simple range check on `min_confidence` returning the Phase-64 two-line stderr `[graphify] error: ... \n  hint: ...` on failure).

    Commit: `feat(67-04): thread CQUERY filters through concept_code_hops BFS and MCP schema`.
  </action>
  <verify>
    <automated>pytest tests/test_concept_code_hops.py tests/test_serve.py -q</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "min_confidence" graphify/serve.py` ≥ 3 (signature + predicate call + schema)
    - `grep -c "confidence_band" graphify/serve.py` ≥ 3
    - `grep -c "_build_concept_hops_filter" graphify/serve.py` ≥ 1
    - `grep -c '"high", "medium", "low"\|\\["high", "medium", "low"\\]' graphify/serve.py` ≥ 1
    - Existing test suite still green (`pytest tests/ -q` reports no NEW failures relative to baseline; the pre-existing `test_migration.py` failure noted in 67-VALIDATION.md may remain)
  </acceptance_criteria>
  <done>BFS gates on predicate; MCP schema advertises three new fields; legacy tests still green.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Byte-identity regression test + integration tests for each new param</name>
  <files>tests/test_concept_code_hops.py</files>
  <read_first>
    - tests/fixtures/cquery_v1_12/README.md
    - tests/fixtures/cquery_v1_12/graph.json
    - tests/fixtures/cquery_v1_12/golden_concept_code_hops.json
    - graphify/serve.py (updated signature)
  </read_first>
  <behavior>
    - test_v1_12_byte_identity: load fixture graph + invoke `_run_concept_code_hops(...)` with NO new kwargs; assert `result == json.load(open(golden_path))` (deep equality, not subset) per D-13
    - test_min_confidence_filters: setting min_confidence=0.7 against fixture excludes the documents edge (score 0.65) but keeps the tests edge (score 0.85)
    - test_relations_empty_returns_zero_edges: relations=[] returns empty result_edges list
    - test_relations_whitelist: relations=["tests"] returns only the tests edge
    - test_confidence_band_high: confidence_band="high" returns only edges with score ≥ 0.8
    - test_AND_semantics: min_confidence=0.9 + confidence_band="high" excludes edge with score 0.85 (passes band but fails min_confidence)
  </behavior>
  <action>
    Append the six tests to `tests/test_concept_code_hops.py`. Use `from networkx.readwrite import json_graph` to load `tests/fixtures/cquery_v1_12/graph.json` into a `nx.Graph`. Use `from graphify.serve import _run_concept_code_hops`.

    The byte-identity test MUST be `assert result == golden` (deep equality). Sort lists in the result before comparison ONLY if `_run_concept_code_hops` does not already produce stable order; if instability is observed, the correct fix is to sort in the helper, NOT in the test.

    Run `pytest tests/test_concept_code_hops.py -q` until all green. Commit: `test(67-04): assert v1.12 byte-identity and CQUERY filter integration against frozen fixture`.
  </action>
  <verify>
    <automated>pytest tests/test_concept_code_hops.py::test_v1_12_byte_identity -xvs</automated>
  </verify>
  <acceptance_criteria>
    - `test_v1_12_byte_identity` passes
    - All 6 new tests pass
    - `grep -c "golden_concept_code_hops.json" tests/test_concept_code_hops.py` ≥ 1
    - `grep -c "assert result == " tests/test_concept_code_hops.py` ≥ 1
    - Commit starts with `test(67-04):`
  </acceptance_criteria>
  <done>Byte-identity oracle locked; AND semantics + zero-match + whitelist proven against real BFS path.</done>
</task>

</tasks>

<verification>
Quick: `pytest tests/test_concept_code_hops.py -q`
Full: `pytest tests/ -q` (no new failures vs pre-Phase-67 baseline)
</verification>

<success_criteria>
- v1.12 byte-identity asserted against frozen golden
- All three new params traverse end-to-end through BFS and through MCP schema
- AND semantics + empty-list zero-match enforced
</success_criteria>

<output>
After completion, create `.planning/phases/67-*/67-04-SUMMARY.md`
</output>
