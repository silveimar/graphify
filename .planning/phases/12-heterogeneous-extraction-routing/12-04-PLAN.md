---
phase: 12-heterogeneous-extraction-routing
plan: 04
type: execute
wave: 3
depends_on:
  - 12-03-PLAN.md
files_modified:
  - graphify/routing_audit.py
  - graphify/security.py
  - tests/test_routing_sidecar.py
autonomous: true
requirements:
  - ROUTE-05
threat_refs:
  - T-12-04
must_haves:
  truths:
    - "`graphify-out/routing.json` written with atomic replace"
    - "Per-file entries include class, model id, endpoint, tokens_used, ms (per ROADMAP success criteria)"
---

<objective>
Implement **`routing.json`** sidecar (ROUTE-05): per-run **per-file** audit trail `{file_path → {class, model, endpoint, tokens_used, ms}}`, **atomic** `.tmp` + `os.replace`, top-level **`version`** field. Optionally add **`routing_report.md`** **or** a **"Routing"** section hook in `report.py` / GRAPH_REPORT — pick **one** to avoid duplication (CONTEXT D-08). Route all output paths through **`validate_graph_path`** where applicable.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
</execution_context>

<context>
@graphify/dedup.py (write_dedup_reports pattern)
@graphify/security.py
@.planning/phases/12-heterogeneous-extraction-routing/12-CONTEXT.md
</context>

<tasks>

<task type="execute">
  <name>Task 1: routing_audit.py — append + flush API</name>
  <read_first>
    - graphify/dedup.py (`write_dedup_reports`)
    - graphify/security.py (`validate_graph_path`)
  </read_first>
  <action>
    Create `graphify/routing_audit.py` with `RoutingAudit` class: methods `record(path: Path, class_: str, model: str, endpoint: str, tokens_used: int, ms: float)`, `flush(out_dir: Path)`. Serialize JSON with `version: 1` (or semver string) at top level key `"version"`, `"files": { ... }`. Use temp file under `out_dir` then `os.replace`. Paths in JSON must be **relative or validated** strings from `str(path)` after `validate_graph_path` when rooted under project.
  </action>
  <acceptance_criteria>
    - `test_routing_sidecar.py::test_atomic_write` passes: interrupt mid-write does not leave corrupt final file
    - `grep -n "os.replace" graphify/routing_audit.py` returns ≥ 1
  </acceptance_criteria>
</task>

<task type="execute">
  <name>Task 2: Wire audit into extract/batch/skill callers</name>
  <read_first>
    - graphify/extract.py (extract loop)
  </read_first>
  <action>
    Pass optional `audit: RoutingAudit | None` into `extract()` or router context; when `--router` pipeline active, record each classified file after timing wrapper. **Batch calls**: record **per-file** tier + model used for audit even when single LLM call uses max tier (CONTEXT D-02).
  </action>
  <acceptance_criteria>
    - `pytest tests/test_routing_sidecar.py -q` exits 0
    - JSON file under `tmp_path / "graphify-out" / "routing.json"` parses with `version` key
  </acceptance_criteria>
</task>

</tasks>

<threat_model>
| Threat ID | Category | Mitigation |
|-----------|----------|------------|
| T-12-04 | Path traversal | `validate_graph_path` for writes under `graphify-out/` |
</threat_model>

<verification>
- `pytest tests/test_routing_sidecar.py -q`
</verification>

<success_criteria>
- [ ] ROUTE-05 satisfied
- [ ] D-07/D-08 atomic + reporting choice documented in code comment
</success_criteria>
