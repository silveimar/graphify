---
phase: 12-heterogeneous-extraction-routing
plan: 06
type: execute
wave: 4
depends_on:
  - 12-04-PLAN.md
  - 12-05-PLAN.md
files_modified:
  - graphify/__main__.py
  - graphify/skill.md
  - graphify/skill-codex.md
  - CLAUDE.md
  - tests/test_cli_run.py
autonomous: true
requirements:
  - ROUTE-03
threat_refs:
  - T-12-06
must_haves:
  truths:
    - "`graphify run --router` is opt-in; default `graphify` behavior without flag unchanged for existing commands"
    - "Help text lists `run` subcommand"
---

<objective>
Add **`graphify run`** CLI with **`--router`** flag (CONTEXT D-04, ROADMAP success criteria): loads detection + runs pipeline with router enabled — thin wrapper that invokes existing **`detect` → `extract(..., router=...)` → downstream stages** as used by skill, or delegates to a single new **`graphify.pipeline.run_corpus`** function if refactor reduces duplication. Update **`skill.md`** (and primary platform variants per repo convention) to pass `--router` only when user opts in. Update **`CLAUDE.md`** Build & Test line for new optional extra if any.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
</execution_context>

<context>
@graphify/__main__.py
@graphify/skill.md
@graphify/detect.py
</context>

<tasks>

<task type="execute">
  <name>Task 1: graphify run subcommand</name>
  <read_first>
    - graphify/__main__.py (`main()`, help banner)
    - graphify/skill.md (Steps 1–4 outline for parity)
  </read_first>
  <action>
    Add `run` command: `graphify run [path] [--router] [--batch-token-budget N] ...` mirroring flags skill already documents where applicable. Parsing: if `--router` absent, **do not** construct `Router` (D-04). If present, load `Router` from `graphify/routing_models.yaml`, run cost pre-flight (Plan 05), then extraction with audit flush. Reuse **absolute** path validation from `security.py` for input path. Print one-line summary including `routing.json` path when router on.
  </action>
  <acceptance_criteria>
    - `graphify run --help 2>&1 | grep -q router` exits 0
    - `python -m graphify run . --router --dry-run` or equivalent no-op mode if implemented — document; **minimal**: `tests/test_cli_run.py` invokes `main()` with argv patch and asserts exit 0 on empty corpus fixture
  </acceptance_criteria>
</task>

<task type="execute">
  <name>Task 2: Skill documentation parity</name>
  <read_first>
    - graphify/skill.md (header Usage)
  </read_first>
  <action>
    Add bullet: `/graphify <path> --router` → passes through to `graphify run --router` OR documents env/flag the skill sets so Claude invokes router path. Align with CONTEXT "skill-equivalent wiring".
  </action>
  <acceptance_criteria>
    - `grep -n "router" graphify/skill.md | head -5` returns ≥ 1 line
  </acceptance_criteria>
</task>

<task type="execute">
  <name>Task 3: Requirements coverage spot-check</name>
  <read_first>
    - .planning/REQUIREMENTS.md ROUTE rows
  </read_first>
  <action>
    Add `tests/test_route_requirements_smoke.py` that imports all new public symbols (`Router`, `file_hash` with model, `RoutingAudit`, cost functions) — **import smoke** to catch missing exports.
  </action>
  <acceptance_criteria>
    - `pytest tests/test_route_requirements_smoke.py -q` exits 0
  </acceptance_criteria>
</task>

</tasks>

<threat_model>
| Threat ID | Category | Mitigation |
|-----------|----------|------------|
| T-12-06 | Injection via CLI path | Reject non-filesystem-safe args; use `Path` resolve + `validate_graph_path` for outputs only |
</threat_model>

<verification>
- `pytest tests/test_cli_run.py tests/test_route_requirements_smoke.py -q`
- `pytest tests/ -q`
</verification>

<success_criteria>
- [ ] `graphify run --router` documented and callable
- [ ] Skill mentions router opt-in
- [ ] Full test suite green
</success_criteria>
