---
phase: 12-heterogeneous-extraction-routing
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - graphify/cache.py
  - tests/test_cache.py
autonomous: true
requirements:
  - ROUTE-04
threat_refs:
  - T-12-02
must_haves:
  truths:
    - "Empty `model_id` preserves legacy cache filename behavior OR documented migration rule — single chosen behavior in code + test"
    - "Distinct `model_id` values → distinct cache file paths or hash inputs"
---

<objective>
Extend **`cache.py::file_hash`** (and semantic cache helpers that derive filenames from it) with optional **`model_id: str = ""`** per ROUTE-04 so different routing decisions cannot share cache entries. Maintain **backward compatibility** for callers that omit `model_id`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
</execution_context>

<context>
@graphify/cache.py
@tests/test_cache.py
@.planning/phases/12-heterogeneous-extraction-routing/12-CONTEXT.md
</context>

<tasks>

<task type="execute">
  <name>Task 1: file_hash + cache path includes model_id</name>
  <read_first>
    - graphify/cache.py (full file)
    - .planning/REQUIREMENTS.md (ROUTE-04 exact formula)
  </read_first>
  <action>
    Change `file_hash(path: Path, model_id: str = "") -> str` to return `f"{hexdigest}:{model_id}"` where `hexdigest` is the **existing** SHA256 computation for content+path (unchanged inner hash). Update `load_cached`, `save_cached`, and `cached_files` to use the full string as cache key segment: e.g. cache file name `"{hexdigest}_{safe_model_id}.json"` OR embed model in hash input — **pick one** and document in module docstring; REQ cites `SHA256(bytes) + ":" + model_id` for the **returned** `file_hash` string, so filename pattern must incorporate the full `file_hash` return or a filesystem-safe encoding of it.
  </action>
  <acceptance_criteria>
    - `grep -n "model_id" graphify/cache.py | wc -l` is ≥ 8
    - `pytest tests/test_cache.py -q` exits 0 with tests proving: same file + different `model_id` → different cache paths; `model_id=""` matches prior behavior for existing tests (update golden expectations if filenames change — run full `pytest tests/ -q`)
  </acceptance_criteria>
</task>

<task type="execute">
  <name>Task 2: save_semantic_cache / check_semantic_cache alignment</name>
  <read_first>
    - graphify/cache.py (`save_semantic_cache`, `check_semantic_cache`)
    - graphify/skill.md (Step B0 — semantic cache)
  </read_first>
  <action>
    Thread optional `model_id` through `save_semantic_cache` and `check_semantic_cache` call chain **signature**; default `""` preserves behavior. Plans 03/06 wire callers — this task only adds parameters and updates internal `load_cached`/`save_cached` invocations to pass `model_id` when provided.
  </action>
  <acceptance_criteria>
    - `grep -n "def save_semantic_cache\|def check_semantic_cache" graphify/cache.py` shows new optional `model_id` parameter
    - `pytest tests/test_cache.py -q` covers semantic helpers with two model_ids
  </acceptance_criteria>
</task>

</tasks>

<threat_model>
| Threat ID | Category | Mitigation |
|-----------|----------|------------|
| T-12-02 | Cache poisoning | Paths confined to `graphify-out/cache/` via existing `cache_dir`; no user-controlled path in `model_id` beyond length — sanitize: reject `model_id` containing `/` or `..` (add unit test) |
</threat_model>

<verification>
- `pytest tests/test_cache.py tests/test_extract.py -q` (extract tests if they assert hash shapes)
</verification>

<success_criteria>
- [ ] ROUTE-04 satisfied with tests
- [ ] No regression on default `model_id`
</success_criteria>
