---
phase: 12-heterogeneous-extraction-routing
plan: 03
type: execute
wave: 2
depends_on:
  - 12-01-PLAN.md
  - 12-02-PLAN.md
files_modified:
  - graphify/extract.py
  - graphify/batch.py
  - tests/test_extract_router.py
autonomous: true
requirements:
  - ROUTE-03
  - ROUTE-07
threat_refs:
  - T-12-03
must_haves:
  truths:
    - "`extract(paths, router=None)` default matches pre-Phase-12 behavior"
    - "Global semaphore + 429 Event shared across worker threads (ROUTE-07)"
---

<objective>
Add **`router: Router | None = None`** to **`extract()`** (ROUTE-03): when set, per-file work uses router classification; parallel **ThreadPoolExecutor** (default 4 workers, **`GRAPHIFY_EXTRACT_WORKERS`** env override) runs per-file extraction tasks. Implement **stampede-safe** concurrency: **`threading.Semaphore`** for provider concurrency cap + **`threading.Event`** for global 429 backoff (ROUTE-07). Integrate **Phase 10 batch rule** (CONTEXT D-01–D-03): export helper **`max_tier_for_cluster(files, router) -> ResolvedRoute`** used by skill/CLI batch path — **one LLM tier per cluster** = max of member tiers.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
</execution_context>

<context>
@graphify/extract.py
@graphify/batch.py
@graphify/routing.py
@.planning/phases/12-heterogeneous-extraction-routing/12-CONTEXT.md
</context>

<tasks>

<task type="execute">
  <name>Task 1: extract() signature + executor wiring</name>
  <read_first>
    - graphify/extract.py (`extract` function ~2652+)
    - graphify/routing.py (`Router`)
  </read_first>
  <action>
    Add `router: Router | None = None` and `**kwargs` only if needed for semaphore injection in tests. Refactor the per-file loop into an inner function `_extract_one(path: Path) -> dict` that: (1) if `router` is None, behavior **identical** to current sequential loop (including cache checks); (2) if `router` is set, compute `model_id` from `router.resolve(path)` and pass to `load_cached`/`save_cached`. Use `concurrent.futures.ThreadPoolExecutor(max_workers=w)` where `w = int(os.environ.get("GRAPHIFY_EXTRACT_WORKERS", "4"))`. When `router` is None, **do not** spawn executor (preserve deterministic single-thread AST ordering).
  </action>
  <acceptance_criteria>
    - `grep -n "def extract(" graphify/extract.py` shows `router` parameter
    - `pytest tests/test_extract_router.py -q` exits 0: mock router + temp files → distinct model_ids hit different cache keys; `router=None` output matches baseline fixture count on tiny corpus
  </acceptance_criteria>
</task>

<task type="execute">
  <name>Task 2: ROUTE-07 rate limit primitives</name>
  <read_first>
    - graphify/routing.py
  </read_first>
  <action>
    Add module-level or `Router`-held **`threading.Semaphore`** (value from env e.g. `GRAPHIFY_EXTRACT_MAX_CONCURRENT` default 4) and **`threading.Event`** `_429_backoff`. Document: workers must `acquire` semaphore before any expensive call; on simulated 429, set Event and **all** workers sleep until clear. Expose `router.acquire_slot()` / `router.release_slot()` or context manager used from `_extract_one` hooks. Provide **test double** that simulates 10 parallel tasks with 8×429 to assert serialized backoff (no busy-loop).
  </action>
  <acceptance_criteria>
    - `grep -n "Semaphore\|Event" graphify/routing.py graphify/extract.py` returns ≥ 4 lines total
    - `pytest tests/test_extract_router.py::test_429_backoff_serializes` passes (name illustrative)
  </acceptance_criteria>
</task>

<task type="execute">
  <name>Task 3: batch max-tier helper</name>
  <read_first>
    - graphify/batch.py
    - .planning/milestones/v1.3-phases/10-cross-file-semantic-extraction/10-CONTEXT.md (batch scope)
  </read_first>
  <action>
    Add `max_tier_route(cluster_files: list[Path], router: Router) -> ResolvedRoute` in `batch.py` or `routing.py` that returns the **maximum** tier by deterministic ordering (trivial &lt; simple &lt; complex &lt; vision). Used by skill Step B1 when `--router` active — **document** in `skill.md` delta (Plan 06). No change to `cluster_files()` signature required.
  </action>
  <acceptance_criteria>
    - `grep -n "max_tier" graphify/batch.py graphify/routing.py` finds definition
    - Unit test: cluster with [trivial, complex] → complex wins
  </acceptance_criteria>
</task>

</tasks>

<threat_model>
| Threat ID | Category | Mitigation |
|-----------|----------|------------|
| T-12-03 | Denial of Service | Semaphore caps parallel requests; 429 Event prevents retry storms |
</threat_model>

<verification>
- `pytest tests/test_extract_router.py tests/test_extract.py -q`
</verification>

<success_criteria>
- [ ] ROUTE-03 executor + kwarg
- [ ] ROUTE-07 primitives tested
- [ ] CONTEXT D-01 helper exported and tested
</success_criteria>
