---
phase: 59.1-version-sync-hygiene-and-version-flag-fix-skill-stamp-drift-
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - .planning/REQUIREMENTS.md
  - .planning/ROADMAP.md
autonomous: true
requirements: [VSYNC-01, VSYNC-02, VSYNC-03, VSYNC-04]
must_haves:
  truths:
    - "VSYNC-01..04 exist in .planning/REQUIREMENTS.md under a v1.12 'Version sync hygiene (VSYNC)' section"
    - "v1.12 traceability table maps VSYNC-01..04 to Phase 59.1"
    - "ROADMAP.md Phase 59.1 row has a real Goal (not placeholder), real Requirements list (VSYNC-01..04), and a real Plans bullet list referencing 59.1-01/02/03 PLAN.md"
    - "REQUIREMENTS.md VSYNC block explicitly notes D-11 and D-14 as out-of-scope so future verification cannot flag them as missing"
  artifacts:
    - path: ".planning/REQUIREMENTS.md"
      contains: "VSYNC-01"
    - path: ".planning/ROADMAP.md"
      contains: "VSYNC-01"
  key_links:
    - from: ".planning/ROADMAP.md (Phase 59.1)"
      to: ".planning/REQUIREMENTS.md (VSYNC-01..04)"
      via: "REQ-ID cross-reference"
      pattern: "VSYNC-0[1-4]"
---

<objective>
Mint the four VSYNC-* requirement IDs in REQUIREMENTS.md and back-fill the Phase 59.1 row in ROADMAP.md so that downstream code-touching plans have a stable REQ-ID anchor and a proper goal/plans manifest. Explicitly mark D-11 and D-14 as out-of-scope to prevent future audit churn.

Purpose: Per CONTEXT.md D-15, the v1.12 traceability table must map VSYNC-01..04 to Phase 59.1. ROADMAP currently has a `[Urgent work - to be planned]` placeholder + `Requirements: TBD`; this plan replaces both.
Output: Updated REQUIREMENTS.md (new section + traceability rows + out-of-scope note) and ROADMAP.md (Phase 59.1 row fully populated).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/59.1-version-sync-hygiene-and-version-flag-fix-skill-stamp-drift-/59.1-CONTEXT.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Mint VSYNC-01..04 in REQUIREMENTS.md</name>
  <files>.planning/REQUIREMENTS.md</files>
  <read_first>
    - .planning/REQUIREMENTS.md (current v1.12 sections: VCWD, E2E, HARN-FMT, plus Traceability table at end)
    - .planning/phases/59.1-version-sync-hygiene-and-version-flag-fix-skill-stamp-drift-/59.1-CONTEXT.md (D-01..D-19 — source of truth for REQ wording; D-11, D-14 explicitly out-of-scope)
  </read_first>
  <action>
    Edit .planning/REQUIREMENTS.md to add a new H3 section after the existing `### Harness error format normalization (HARN-FMT)` section (and before `## Future`):

    ```
    ### Version sync hygiene and --version flag (VSYNC)

    > Out of scope (per CONTEXT.md): D-11 (no new top-level subcommand for stamp inspection — stamp surface is folded into `--version` and `doctor` only), D-14 (release-process tooling — `bump_version.py`, CI hooks, tag automation are deferred to a future milestone).

    - [ ] **VSYNC-01**: Silent auto-self-heal of `.graphify_version` skill stamp when stamp < package on regular CLI invocations. `_check_skill_version()` in `graphify/__main__.py:21` rewrites the stamp to the running `__version__` and continues with no stderr output. Skips: `--version`, `-V`, `-h`, `--help`, `install`, `uninstall`, and any case where `package_version()` cannot resolve. Heal applies to all `_PLATFORM_CONFIG` platforms whose install dir contains a stamp file; platforms with no stamp remain untouched. Fallback: if write fails (OSError/PermissionError), emit the existing two-line warning shape and continue. (D-01..D-06, D-13)
    - [ ] **VSYNC-02**: `graphify --version` and `-V` produce a multi-line block: line 1 `graphify <pkg-version>`; indented `skill stamps:` block with one row per `_PLATFORM_CONFIG` platform (`<name>: <stamp> (<install-dir-with-~>)` for installed; `<name>: — (not installed)` otherwise); `python: <interpreter-version>`; `install: <editable|site-packages> (<resolved-path>)`. Side-effect-free (skips self-heal). `_cli_exit()` success-footer at `__main__.py:1370` unchanged. (D-07..D-10, D-19)
    - [ ] **VSYNC-03**: `graphify doctor` gains a `version sync` section listing each platform with stamp value, package value, and status (`✓ in sync` / `! drifted-newer` / `— not installed`). Computed on the fly; mirrors the `--version` block. No heal-history sentinel file. (D-12)
    - [ ] **VSYNC-04**: When stamp > package (mixed/downgraded install), the existing two-line stderr warning at `__main__.py:46` is preserved verbatim. Stamp is NOT rewritten downward. (D-03)
    ```

    Then, in the existing `## Traceability` table at the end of the file, append four rows after the `HARN-FMT-01` row:

    ```
    | VSYNC-01 | Phase 59.1 | Silent self-heal in `_check_skill_version()` |
    | VSYNC-02 | Phase 59.1 | Multi-line `--version` / `-V` output block |
    | VSYNC-03 | Phase 59.1 | `doctor` `version sync` section |
    | VSYNC-04 | Phase 59.1 | Preserved warning for stamp > package |
    ```

    Do not modify other sections.
  </action>
  <verify>
    <automated>grep -c "VSYNC-0[1-4]" .planning/REQUIREMENTS.md | awk '$1 >= 8 {exit 0} {exit 1}'</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "^- \\[ \\] \\*\\*VSYNC-0[1-4]\\*\\*" .planning/REQUIREMENTS.md` returns `4`
    - `grep -c "| VSYNC-0[1-4] | Phase 59.1 |" .planning/REQUIREMENTS.md` returns `4`
    - `grep -c "### Version sync hygiene" .planning/REQUIREMENTS.md` returns `1`
    - Out-of-scope note for D-11/D-14 present: `grep -c "Out of scope (per CONTEXT.md): D-11" .planning/REQUIREMENTS.md` returns `>= 1` AND `grep -c "D-14" .planning/REQUIREMENTS.md` returns `>= 1`
    - File still contains pre-existing `VCWD-01`, `E2E-01`, `HARN-FMT-01` (no regressions): `grep -cE "VCWD-01|E2E-01|HARN-FMT-01" .planning/REQUIREMENTS.md` returns `>= 3`
  </acceptance_criteria>
  <done>VSYNC-01..04 minted with text traceable to D-01..D-19; traceability table maps all four to Phase 59.1; D-11 and D-14 explicitly noted as out-of-scope.</done>
</task>

<task type="auto">
  <name>Task 2: Back-fill Phase 59.1 row in ROADMAP.md</name>
  <files>.planning/ROADMAP.md</files>
  <read_first>
    - .planning/ROADMAP.md (Phase 59.1 row near line 438; surrounding Phase 59 row for shape reference at line ~424)
    - .planning/phases/59.1-version-sync-hygiene-and-version-flag-fix-skill-stamp-drift-/59.1-CONTEXT.md (Phase Boundary section — source of Goal text)
  </read_first>
  <action>
    Edit .planning/ROADMAP.md and replace the existing Phase 59.1 stub block:

    ```
    ### Phase 59.1: Version sync hygiene and --version flag (fix skill-stamp drift warning, expose graphify --version) (INSERTED)

    **Goal:** [Urgent work - to be planned]
    **Requirements**: TBD
    **Depends on:** Phase 59
    **Plans:** 0 plans

    Plans:
    - [ ] TBD (run /gsd-plan-phase 59.1 to break down)
    ```

    With:

    ```
    ### Phase 59.1: Version sync hygiene and --version flag (fix skill-stamp drift warning, expose graphify --version) (INSERTED)

    **Goal:** Silence the per-command `skill stamp ('X.Y.Z') is older than the installed package` warning by silently auto-self-healing the stamp when stamp < package, preserve the warning only for the unusual stamp > package case, and expand `graphify --version` / `-V` and `graphify doctor` to surface package vs per-platform skill-stamp state without grepping `.graphify_version` files.
    **Depends on:** Phase 59
    **Requirements:** **VSYNC-01**, **VSYNC-02**, **VSYNC-03**, **VSYNC-04**.

    **Success Criteria** (what must be TRUE):

    1. Running any non-skip `graphify` command (e.g. `graphify run`, `graphify query`) when an existing skill stamp is older than the installed package emits ZERO stderr lines and rewrites `.graphify_version` to the current `__version__` for every installed platform (**VSYNC-01**).
    2. `graphify --version` and `graphify -V` exit 0 with a multi-line block: package line, indented `skill stamps:` block (one row per `_PLATFORM_CONFIG` platform with `~`-shortened install dirs), `python:` line, and `install:` line; `--version` triggers no stamp writes (**VSYNC-02**).
    3. `graphify doctor` output includes a `version sync` section enumerating each platform with stamp / package / status (`✓ in sync` / `! drifted-newer` / `— not installed`) (**VSYNC-03**).
    4. When stamp > package, the original two-line stderr warning is emitted verbatim and the stamp file is NOT rewritten (**VSYNC-04**).
    5. CI tests on Python 3.10 and 3.12 cover: heal happy path, write-failure fallback to existing warning, stamp-newer warning preservation, and `--version` multi-line snapshot.

    **Plans:** 3 plans

    Plans:
    - [ ] 59.1-01-PLAN.md — Mint VSYNC-01..04 in REQUIREMENTS.md and back-fill the ROADMAP.md Phase 59.1 row
    - [ ] 59.1-02-PLAN.md — Rewrite `_check_skill_version()` for silent auto-self-heal (VSYNC-01, VSYNC-04)
    - [ ] 59.1-03-PLAN.md — Multi-line `--version` block + `doctor` `version sync` section (VSYNC-02, VSYNC-03)
    **UI hint:** partial — CLI `--version` and `doctor` output expand; previously-noisy stderr warning becomes silent.
    ```

    Preserve a blank line and the `---` separator (if present) between Phase 59.1 and Phase 60.
  </action>
  <verify>
    <automated>grep -c "VSYNC-01\|VSYNC-02\|VSYNC-03\|VSYNC-04" .planning/ROADMAP.md</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "Urgent work - to be planned" .planning/ROADMAP.md` returns `0` (placeholder gone)
    - `grep -c "Requirements: TBD" .planning/ROADMAP.md` returns `0`
    - `grep -c "59.1-0[1-3]-PLAN.md" .planning/ROADMAP.md` returns `3`
    - `grep -cE "VSYNC-0[1-4]" .planning/ROADMAP.md` returns `>= 4`
    - `grep -c "^### Phase 59.1:" .planning/ROADMAP.md` returns `1` (no duplicate insertion)
  </acceptance_criteria>
  <done>Phase 59.1 row has real Goal, Requirements list, Success Criteria, and Plans manifest pointing at 59.1-01/02/03 PLAN.md.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| (none new) | This plan is documentation-only; it touches `.planning/*.md` and introduces no new untrusted input. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-59.1.01-01 | Tampering | `.planning/REQUIREMENTS.md` traceability table | accept | Project planning artifacts are developer-authored; integrity guarded by git history. No new attack surface. |
| T-59.1.01-02 | Information disclosure | `.planning/ROADMAP.md` | accept | No secrets or PII; content is project-public. |
</threat_model>

<verification>
Both files validate against the four acceptance-criteria grep checks above. No code or test changes are required.
</verification>

<success_criteria>
- VSYNC-01..04 entries in REQUIREMENTS.md trace verbatim to D-01..D-19 in CONTEXT.md
- Traceability table maps all four to Phase 59.1
- D-11 and D-14 explicitly noted as out-of-scope in the VSYNC block (prevents future audit churn)
- ROADMAP.md Phase 59.1 row is no longer a stub: real Goal, real Requirements, real Plans list
</success_criteria>

<output>
After completion, create `.planning/phases/59.1-version-sync-hygiene-and-version-flag-fix-skill-stamp-drift-/59.1-01-SUMMARY.md`.
</output>
