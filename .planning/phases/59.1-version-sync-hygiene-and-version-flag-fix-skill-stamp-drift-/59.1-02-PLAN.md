---
phase: 59.1-version-sync-hygiene-and-version-flag-fix-skill-stamp-drift-
plan: 02
type: tdd
wave: 1
depends_on: []
files_modified:
  - graphify/__main__.py
  - tests/test_version_sync.py
autonomous: true
requirements: [VSYNC-01, VSYNC-04]
must_haves:
  truths:
    - "When stamp < package on any non-skip CLI command, .graphify_version is silently rewritten to the running __version__ and stderr is empty"
    - "When stamp > package, the existing two-line warning fires and the stamp file is NOT modified"
    - "If the stamp write fails (PermissionError/OSError), the existing two-line warning is emitted as a fallback and the user's command still runs"
    - "If importlib.metadata cannot resolve the package version (__version__ falsy), no stamp write happens (silent abort of heal)"
    - "If the stamp file exceeds 1024 bytes (corrupt/oversized), it is treated as corrupt: silent fallback (no stderr warning) and auto-heal rewrites it to __version__"
    - "--version, -V, -h, --help, install, uninstall continue to skip _check_skill_version() entirely (no heal side-effect)"
  artifacts:
    - path: "graphify/__main__.py"
      provides: "Rewritten _check_skill_version() with silent auto-self-heal + 1024-byte size guard"
      contains: "def _check_skill_version"
    - path: "tests/test_version_sync.py"
      provides: "Unit tests for D-16, D-17, D-18, D-05, D-02 and T-59.1.02-02 (heal happy path, write-failure fallback, stamp-newer preservation, package_version unresolvable, missing stamp, oversized stamp)"
  key_links:
    - from: "graphify/__main__.py main() loop iterating _PLATFORM_CONFIG"
      to: "_check_skill_version(skill_dst)"
      via: "existing call site at __main__.py:1349 (unchanged)"
      pattern: "_check_skill_version\\(skill_dst\\)"
    - from: "_check_skill_version()"
      to: "version_file.write_text(__version__, encoding=\"utf-8\")"
      via: "heal-path stamp rewrite mirroring install handlers at __main__.py:335,451"
      pattern: "\\.graphify_version"
---

<objective>
Replace the body of `_check_skill_version()` (graphify/__main__.py:21-50) with the silent auto-self-heal behavior specified in CONTEXT.md decisions D-01..D-06 and D-13. Preserve verbatim the existing two-line stderr warning shape for the stamp > package and write-failure fallback paths (D-03, D-04). Add a 1024-byte size guard at the read entry point per T-59.1.02-02 mitigation.

Purpose: Silences the `skill stamp ('0.4.7') is older than the installed package; run 'graphify install' to update the skill. (package is '1.0.0')` noise that fires on every CLI invocation after a `pip install -e .` upgrade.

Output: Modified `graphify/__main__.py` with rewritten `_check_skill_version()` (single-function diff per D-13 — no new helper, no new module) and a new `tests/test_version_sync.py` covering D-16, D-17, D-18, D-05, D-02, and T-59.1.02-02 with TDD-first tests.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
@$HOME/.claude/get-shit-done/references/tdd.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/phases/59.1-version-sync-hygiene-and-version-flag-fix-skill-stamp-drift-/59.1-CONTEXT.md
@graphify/__main__.py
@CLAUDE.md

<interfaces>
<!-- Existing contracts the executor MUST use directly. No codebase exploration needed. -->

From graphify/__main__.py (current state, lines 12-50):

```python
from graphify.version import package_version

__version__ = package_version()


def _numeric_version_prefix_tuple(s: str) -> tuple[int, ...]:
    """Best-effort version ordering from digit runs (no extra dependencies)."""
    nums = [int(m) for m in re.findall(r"\d+", s)]
    return tuple(nums) if nums else (0,)


def _check_skill_version(skill_dst: Path) -> None:
    """Warn when the installed skill stamp does not match the running package."""
    version_file = skill_dst.parent / ".graphify_version"
    if not version_file.exists():
        return
    installed = version_file.read_text(encoding="utf-8").strip()
    pkg = __version__
    if installed == pkg:
        return
    it = _numeric_version_prefix_tuple(installed)
    pt = _numeric_version_prefix_tuple(pkg)
    n = max(len(it), len(pt))
    it_pad = it + (0,) * (n - len(it))
    pt_pad = pt + (0,) * (n - len(pt))
    if it_pad < pt_pad:
        hint = "older than the installed package; run 'graphify install' to update the skill."
    elif it_pad > pt_pad:
        hint = (
            "newer than the installed package (unusual for an editable or mixed install); "
            "reinstall graphify if unintended."
        )
    else:
        hint = "differs from the installed package; run 'graphify install' to align versions."
    print(
        f"  warning: skill stamp ({installed!r}) is {hint} (package is {pkg!r}).",
        file=sys.stderr,
    )
```

Stamp-write pattern reused from install handlers at __main__.py:335 and :451:

```python
(skill_dst.parent / ".graphify_version").write_text(__version__, encoding="utf-8")
```

Call site (unchanged, __main__.py around line 1349):

```python
for skill_dst in {Path.home() / cfg["skill_dst"] for cfg in _PLATFORM_CONFIG.values()}:
    _check_skill_version(skill_dst)
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: RED — write failing tests in tests/test_version_sync.py</name>
  <files>tests/test_version_sync.py</files>
  <read_first>
    - graphify/__main__.py (current `_check_skill_version`, lines 21-50; current `__version__` import at line 12; `_PLATFORM_CONFIG` shape at line 115)
    - .planning/phases/59.1-version-sync-hygiene-and-version-flag-fix-skill-stamp-drift-/59.1-CONTEXT.md (D-16, D-17, D-18 — exact test specs; D-05; T-59.1.02-02 in this plan's threat model)
    - tests/test_main.py (existing test patterns and imports — no need to read full file, just confirm `from graphify.__main__ import ...` style)
  </read_first>
  <behavior>
    - Test 1 (D-16, heal happy path — `test_heal_happy_path_silent`): Given a `tmp_path` directory with a `.graphify_version` file containing `"0.4.7"` and a parent skill dir, when `_check_skill_version(skill_dst)` is called with the running `__version__` greater than `"0.4.7"`, then the file is rewritten to `__version__`, the function returns None, and `capsys.readouterr().err == ""`.
    - Test 2 (D-18, stamp newer preservation — `test_stamp_newer_warning_preserved`): Given a stamp file containing `"99.99.99"`, when `_check_skill_version(skill_dst)` is called, then stderr contains `"newer than the installed package"` AND the stamp file content is still `"99.99.99"` (NOT rewritten downward).
    - Test 3 (D-17, write-failure fallback — `test_write_failure_falls_back_to_warning`): Given a stamp file containing `"0.4.7"`, when `Path.write_text` is monkeypatched on the version_file path to raise `PermissionError`, then stderr contains the existing warning substring `"older than the installed package; run 'graphify install'"` AND the function does NOT raise.
    - Test 4 (D-05, package_version() unresolvable — `test_silent_abort_when_package_version_unresolvable`): Given a stamp file containing `"0.4.7"` and `monkeypatch.setattr("graphify.__main__.__version__", "", raising=False)`, when `_check_skill_version(skill_dst)` is called, then NO stamp file is written (the file content remains `"0.4.7"` byte-for-byte), `capsys.readouterr().err == ""`, and the function does not raise.
    - Test 5 (D-02, missing stamp file — `test_missing_stamp_is_noop`): Given a `tmp_path` skill dir with NO `.graphify_version` file, when `_check_skill_version(skill_dst)` is called, then no file is created and stderr is empty.
    - Test 6 (T-59.1.02-02, oversized stamp — `test_oversized_stamp_treated_as_corrupt`): Given a `.graphify_version` file written with 2048 bytes of arbitrary content (e.g. `("0.4.7\n" + "x" * 2048).encode("utf-8")` written via `version_file.write_bytes(...)`), when `_check_skill_version(skill_dst)` is called, then the function returns without raising, `capsys.readouterr().err == ""` (no stamp-older/newer warning emitted — corrupt is treated as silent fallback), and after the GREEN task the stamp file is auto-healed (its content equals `__version__` and its size is `<= 1024`).
  </behavior>
  <action>
    Create `tests/test_version_sync.py` with the SIX test functions described in `<behavior>`. Use these literal patterns:

    ```python
    from __future__ import annotations
    import pytest
    from pathlib import Path
    from graphify.__main__ import _check_skill_version, __version__
    ```

    Each test:
    - Creates `skill_dst = tmp_path / "skill.md"` (file path; parent is `tmp_path`)
    - Writes stamp via `(tmp_path / ".graphify_version").write_text("0.4.7", encoding="utf-8")` for heal tests
    - For stamp-newer test, write `"99.99.99"`
    - For write-failure test, monkeypatch via `monkeypatch.setattr("pathlib.Path.write_text", _raise)` where `_raise` raises `PermissionError("denied")`. Scope the monkeypatch only via a wrapper if needed.
    - For the D-05 test, use `monkeypatch.setattr("graphify.__main__.__version__", "", raising=False)` and assert stamp content is unchanged after the call (read it back).
    - For the oversized test, write the stamp with `version_file.write_bytes(("0.4.7\n" + "x" * 2048).encode("utf-8"))` so `version_file.stat().st_size > 1024`.
    - Uses `capsys.readouterr()` to assert stderr content (NOT capturing fd-level — Python's print to sys.stderr is captured by capsys by default)

    All tests must currently FAIL or skip against the existing `_check_skill_version` (which warns on heal path instead of healing, and never writes; the oversized test also fails because current code blindly reads + warns; the D-05 test fails because current code doesn't guard on falsy `__version__`).

    Run `pytest tests/test_version_sync.py -q` and confirm at least Test 1, Test 3, Test 4, and Test 6 fail.

    Commit: `test(59.1-02): add failing tests for silent auto-self-heal of skill stamp`
  </action>
  <verify>
    <automated>cd /Users/silveimar/Documents/silogia-repos/graphify && pytest tests/test_version_sync.py -q 2>&1 | grep -E "failed|passed" | head -3</automated>
  </verify>
  <acceptance_criteria>
    - `tests/test_version_sync.py` exists with at least 6 test functions: `grep -c "^def test_" tests/test_version_sync.py` returns `>= 6`
    - Required test names present:
      - `grep -c "^def test_heal_happy_path_silent" tests/test_version_sync.py` returns `1`
      - `grep -c "^def test_stamp_newer_warning_preserved" tests/test_version_sync.py` returns `1`
      - `grep -c "^def test_write_failure_falls_back_to_warning" tests/test_version_sync.py` returns `1`
      - `grep -c "^def test_silent_abort_when_package_version_unresolvable" tests/test_version_sync.py` returns `1`
      - `grep -c "^def test_missing_stamp_is_noop" tests/test_version_sync.py` returns `1`
      - `grep -c "^def test_oversized_stamp_treated_as_corrupt" tests/test_version_sync.py` returns `1`
    - At least one test FAILS against the current (un-modified) `_check_skill_version`: `pytest tests/test_version_sync.py -q 2>&1 | grep -E "^[0-9]+ failed"` returns a non-empty match
    - Tests import from `graphify.__main__`: `grep -c "from graphify.__main__ import" tests/test_version_sync.py` returns `>= 1`
    - Commit message matches `^test(59.1-02):` pattern: `git log -1 --pretty=%s | grep -E "^test\(59\.1-02\):"`
  </acceptance_criteria>
  <done>RED gate satisfied: failing tests committed before any source change.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: GREEN — rewrite _check_skill_version() to silent auto-self-heal</name>
  <files>graphify/__main__.py</files>
  <read_first>
    - graphify/__main__.py (lines 1-60 for current `_check_skill_version` and `__version__` import; lines 330-340 and 445-455 for the existing stamp-write pattern that this rewrite mirrors)
    - .planning/phases/59.1-version-sync-hygiene-and-version-flag-fix-skill-stamp-drift-/59.1-CONTEXT.md (D-01..D-06, D-13 — replace function body inline; no new helper)
    - tests/test_version_sync.py (the contract from Task 1)
  </read_first>
  <action>
    Replace the body of `_check_skill_version(skill_dst: Path) -> None` in graphify/__main__.py (currently lines 21-50). The new body MUST be implemented in this exact step order:

    1. Compute `version_file = skill_dst.parent / ".graphify_version"`. If `not version_file.exists()`, return (preserve current behavior — D-02).
    2. **Size guard (T-59.1.02-02):** Before reading, run `if version_file.stat().st_size > 1024: return  # treat as corrupt — silent fallback per D-04`. This caps the read at 1024 bytes worth of stamp content; oversized stamps are treated as corrupt and the function exits silently (no stderr) so the auto-heal write in step 5 never gets a chance to fire downward — but on the NEXT command invocation the install handler's normal write path (or a re-run of `graphify install`) will rewrite the stamp. (NOTE: if stricter "auto-heal corrupt-on-read" is desired by the executor, replace this `return` with a fall-through into step 5's heal write; the acceptance test `test_oversized_stamp_treated_as_corrupt` passes either way as long as stderr is empty and the function does not raise. Default behavior: `return`.)
    3. Read `installed = version_file.read_text(encoding="utf-8").strip()`. Compute `pkg = __version__`. If `not pkg` (D-05 guard for unresolvable `package_version()` — empty/falsy), return silently. Do NOT write the stamp in this branch.
    4. If `installed == pkg`, return.
    5. Compute `it_pad`, `pt_pad` as today using `_numeric_version_prefix_tuple` (preserve as-is).
    6. If `it_pad < pt_pad` (stamp older — heal path, D-01):
       - `try: version_file.write_text(pkg, encoding="utf-8")` and return on success (silent — D-01).
       - `except (OSError, PermissionError):` fall through to the existing two-line warning (D-04 fallback). Use the existing `older than the installed package; run 'graphify install' to update the skill.` hint.
    7. If `it_pad > pt_pad` (stamp newer — D-03): emit the existing `newer than the installed package (unusual for an editable or mixed install); reinstall graphify if unintended.` warning. Do NOT write the stamp.
    8. Else (equal numeric prefix but unequal strings — preserve existing `differs from` warning).

    The warning shape for D-03/D-04 MUST be the exact two-line print statement from the current code:

    ```python
    print(
        f"  warning: skill stamp ({installed!r}) is {hint} (package is {pkg!r}).",
        file=sys.stderr,
    )
    ```

    Do NOT log a `[graphify] info: refreshed stamp …` line on the heal path (silence is the desired UX — CONTEXT.md "specifics").

    Do NOT introduce a new helper function — replace the body inline per D-13.

    Run `pytest tests/test_version_sync.py -q` — all tests from Task 1 must pass. Also run `pytest tests/ -q` to confirm no regressions in the existing suite (especially any test asserting the old warning fires for stamp < package).

    If an existing test in `tests/test_main.py` asserts the old "older than the installed package" warning fires for a stamp-older case (regression), update that test to assert silent heal instead, and commit it as part of this task. Reference the exact test name in the commit body.

    Commit: `feat(59.1-02): silent auto-self-heal of skill stamp on drift (VSYNC-01, VSYNC-04)`
  </action>
  <verify>
    <automated>cd /Users/silveimar/Documents/silogia-repos/graphify && pytest tests/test_version_sync.py tests/ -q 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - `pytest tests/test_version_sync.py -q` exits 0 (all 6+ tests pass)
    - `pytest tests/ -q` exits 0 (no regressions across the full suite)
    - `_check_skill_version` body contains a `write_text` call: `grep -A 40 "def _check_skill_version" graphify/__main__.py | grep -c "write_text"` returns `>= 1`
    - Size guard present: `grep -A 40 "def _check_skill_version" graphify/__main__.py | grep -c "st_size"` returns `>= 1` (T-59.1.02-02 mitigation in place)
    - D-05 guard present: `grep -A 40 "def _check_skill_version" graphify/__main__.py | grep -cE "if not pkg|pkg == \"\"|not __version__"` returns `>= 1`
    - The function is still a single function (no new top-level helper added): `grep -cE "^def _check_skill_version|^def _heal_skill_stamp" graphify/__main__.py` returns `1` (D-13)
    - Heal path is silent: simulated stamp-older invocation produces empty stderr (covered by Task 1 Test 1)
    - Commit message matches `^feat(59.1-02):` pattern: `git log -1 --pretty=%s | grep -E "^feat\(59\.1-02\):"`
  </acceptance_criteria>
  <done>GREEN gate satisfied: all tests pass, _check_skill_version body rewritten with size guard + D-05 guard, no new helpers introduced.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| On-disk stamp file → `_check_skill_version()` | The `.graphify_version` file is written by graphify itself in install/uninstall handlers; trusted in principle, but a corrupted/malicious file could contain arbitrary bytes. Existing `read_text(encoding="utf-8").strip()` + numeric-prefix parser already tolerates non-semver content. Size guard added at entry per T-59.1.02-02. |
| `importlib.metadata` → `__version__` | Trusted (source: installed package metadata). D-05 adds a guard: if unresolvable (empty/falsy `__version__`), abort heal silently. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-59.1.02-01 | Tampering | `.graphify_version` stamp file | accept | File lives under user-owned `~/.claude/skills/...` etc. (per `_PLATFORM_CONFIG`); attacker with write access there has already escalated. Numeric-prefix tuple parser tolerates garbage (returns `(0,)`). |
| T-59.1.02-02 | Denial of service | Oversized stamp file | mitigate | At entry of `_check_skill_version`, before `read_text`, guard with `if version_file.stat().st_size > 1024: return  # treat as corrupt — silent fallback per D-04`. Bounds the read. Test `test_oversized_stamp_treated_as_corrupt` enforces this. |
| T-59.1.02-03 | Elevation of privilege | Stamp-write `version_file.write_text(pkg)` | accept | Writes to the same path the existing install handler writes to (`__main__.py:335,451`); no new path or permission expansion. PermissionError caught and falls back to warning per D-04. |
| T-59.1.02-04 | Information disclosure | stderr warning shape | accept | Warning text is identical to today; no new disclosure surface. |
</threat_model>

<verification>
- `pytest tests/test_version_sync.py -q` exits 0 with 6+ tests passing
- `pytest tests/ -q` exits 0 (no regressions in existing test suite)
- Manual smoke: `pip install -e ".[mcp,pdf,watch]" && echo 0.4.7 > ~/.claude/skills/graphify/.graphify_version && graphify --help 2>&1 1>/dev/null` produces empty stderr (heal path) and `cat ~/.claude/skills/graphify/.graphify_version` shows current `__version__`
</verification>

<success_criteria>
- VSYNC-01 truth: stderr is silent on stamp-older invocation; stamp file is rewritten
- VSYNC-04 truth: stamp-newer warning fires verbatim and stamp file is unchanged
- D-04 fallback: write failure falls through to the two-line warning without aborting the user's command
- D-05: empty/falsy `__version__` → silent abort (no write, no warning)
- D-13: function body replaced inline; no new top-level helper
- D-02 preserved: missing stamp file → no-op (no creation)
- T-59.1.02-02 mitigated: oversized stamp file (>1024 bytes) → silent fallback (no warning, no crash)
</success_criteria>

<output>
After completion, create `.planning/phases/59.1-version-sync-hygiene-and-version-flag-fix-skill-stamp-drift-/59.1-02-SUMMARY.md`.
</output>
