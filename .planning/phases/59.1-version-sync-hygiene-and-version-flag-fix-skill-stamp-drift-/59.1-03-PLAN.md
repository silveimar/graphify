---
phase: 59.1-version-sync-hygiene-and-version-flag-fix-skill-stamp-drift-
plan: 03
type: execute
wave: 2
depends_on: [59.1-02]
files_modified:
  - graphify/__main__.py
  - tests/test_version_sync.py
autonomous: true
requirements: [VSYNC-02, VSYNC-03]
must_haves:
  truths:
    - "graphify --version and graphify -V exit 0 and print a multi-line block: package line, indented 'skill stamps:' block (one row per _PLATFORM_CONFIG platform), python: line, install: line"
    - "graphify --version triggers no .graphify_version writes (side-effect-free, skip list preserved)"
    - "graphify --help still works post-flag-add (regression guard for D-10)"
    - "graphify doctor output contains a 'version sync' section listing each platform with stamp / package / status"
    - "Oversized/corrupt stamps render the literal string '<corrupt>' in --version and doctor output (T-59.1.03-02 mitigation)"
    - "_cli_exit() success-footer at __main__.py:1370 still prints the single line '[graphify] version <pkg>' unchanged"
  artifacts:
    - path: "graphify/__main__.py"
      provides: "Multi-line --version handler + doctor version-sync section + corrupt-stamp rendering"
      contains: "skill stamps:"
    - path: "tests/test_version_sync.py"
      provides: "Multi-line --version snapshot test (D-19) + doctor version-sync test (D-12) + corrupt-stamp render test"
  key_links:
    - from: "graphify --version handler at __main__.py:1335"
      to: "_PLATFORM_CONFIG iteration for stamp rendering"
      via: "shared _render_version_block() helper (Claude's discretion per CONTEXT.md)"
      pattern: "_render_version_block|skill stamps:"
    - from: "graphify doctor handler at __main__.py:2807 (`elif cmd == \"doctor\":`)"
      to: "version sync section in doctor output"
      via: "print(_render_doctor_version_sync()) inserted as the FIRST statement inside the doctor elif branch"
      pattern: "version sync"
---

<objective>
Expand `graphify --version` / `-V` from a single-line print to the multi-line block specified in CONTEXT.md D-07..D-10, and prepend a `version sync` section to `graphify doctor` per D-12. Both render-helpers gracefully handle oversized/corrupt stamp files by emitting the literal string `<corrupt>` instead of raising (T-59.1.03-02 mitigation).

Purpose: surface package vs per-platform skill-stamp state without users grepping `.graphify_version` files; keep `graphify --help` regression-safe.

Output: Modified `graphify/__main__.py`: (1) replaces the `--version` / `-V` early-exit at line 1335, (2) inserts a doctor sub-renderer at the START of the `elif cmd == "doctor":` block (line 2807), (3) introduces two private helpers `_render_version_block()` and `_render_doctor_version_sync()`. Tests in `tests/test_version_sync.py` extended with D-19 snapshot, doctor section, --help regression, and corrupt-stamp rendering.

This is an `execute`-mode integration plan (NOT TDD): it composes new helpers around the comparator/heal core that plan 02 already test-drove. Tests live in the same task as their implementation.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/phases/59.1-version-sync-hygiene-and-version-flag-fix-skill-stamp-drift-/59.1-CONTEXT.md
@.planning/phases/59.1-version-sync-hygiene-and-version-flag-fix-skill-stamp-drift-/59.1-02-SUMMARY.md
@graphify/__main__.py

<interfaces>
<!-- Existing contracts the executor will use directly. -->

From graphify/__main__.py (current --version handler around line 1335):

```python
argv_head = sys.argv[1:]
if len(argv_head) >= 1 and argv_head[0] in ("--version", "-V"):
    print(f"graphify {__version__}")
    raise SystemExit(0)
```

From graphify/__main__.py (skip list at line ~1342, MUST be preserved verbatim — D-10):

```python
_skip_skill_version_check = any(arg in ("install", "uninstall") for arg in sys.argv) or (
    len(argv_rest) >= 1 and argv_rest[0] in ("-h", "--help", "--version", "-V")
)
```

From graphify/__main__.py (success footer, line ~1370 — D-09 says LEAVE UNCHANGED):

```python
print(f"[graphify] version {__version__}")
```

From graphify/__main__.py (line 115): `_PLATFORM_CONFIG: dict[str, dict] = { ... }` with each entry having a `skill_dst` key (relative path under `Path.home()`).

From graphify/__main__.py (line 2807): `elif cmd == "doctor":` — extension point for D-12. Insertion strategy: `print(_render_doctor_version_sync())` becomes the FIRST statement inside this elif branch, before any other doctor logic.

`sys.version` (stdlib) — for the `python:` line.

`sys.executable` + heuristic on path — for `install: <editable|site-packages> (<resolved-path>)`. Editable install detection: presence of an `.egg-link`, `__editable__` in `__file__`, or `pip show graphifyy | grep -i editable`. Simplest: check if `Path(graphify.__file__).resolve()` is inside a `site-packages` directory; if not, treat as `editable`.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Implement multi-line --version block (VSYNC-02, D-07..D-10, D-19)</name>
  <files>graphify/__main__.py, tests/test_version_sync.py</files>
  <read_first>
    - graphify/__main__.py (lines 1330-1380 for --version handler, skip list, _cli_exit footer; lines 115-200 for `_PLATFORM_CONFIG` shape)
    - .planning/phases/59.1-version-sync-hygiene-and-version-flag-fix-skill-stamp-drift-/59.1-CONTEXT.md (D-07..D-10, D-19 — exact contract and snapshot expectations)
    - tests/test_version_sync.py (existing tests from plan 02 — append, do not duplicate imports)
  </read_first>
  <action>
    1. **Add a private helper** `_render_version_block(home: Path | None = None) -> str` near the top of `graphify/__main__.py` (after `_check_skill_version` is fine). Default `home` to `Path.home()` so tests can inject a `tmp_path`. The function returns a newline-joined string (no trailing newline). Field order per D-07:

       ```
       graphify <pkg-version>
         skill stamps:
           <platform-name>: <stamp> (<install-dir-with-~>)
           <platform-name>: — (not installed)
           ...
         python: <sys.version.split()[0]> (<sys.executable>)
         install: <editable|site-packages> (<resolved-path-of-graphify-package>)
       ```

       - Iterate `_PLATFORM_CONFIG.items()`. For each, compute `install_dir = home / cfg["skill_dst"]`. Stamp file is `install_dir.parent / ".graphify_version"`.
       - **Corrupt-rendering branch (T-59.1.03-02):** Before reading the stamp, guard with `if version_file.stat().st_size > 1024: stamp = "<corrupt>"`. If reading raises (UnicodeDecodeError, OSError) wrap in try/except and set `stamp = "<corrupt>"`. Render the row using this stamp value verbatim (do NOT raise).
       - For installed (stamp file exists): row = `f"    {name}: {stamp} ({_tilde(install_dir.parent, home)})"`.
       - For not installed: row = `f"    {name}: — (not installed)"`.
       - `_tilde(p, home)`: if `p` is under `home`, return `"~/" + p.relative_to(home).as_posix()`; else return `str(p)`.
       - `install` line: detect editable by `"site-packages" not in str(Path(graphify.__file__).resolve())` → emit `editable`, else `site-packages`. Resolve path to `Path(graphify.__file__).resolve().parent` (the package directory). Wrap in try/except; on any error, emit `install: unknown (<sys.executable>)`.
       - D-08: emit version numbers verbatim, NO `(drifted)` suffix.

    2. **Replace the --version handler** at __main__.py:1335:

       ```python
       if len(argv_head) >= 1 and argv_head[0] in ("--version", "-V"):
           print(_render_version_block())
           raise SystemExit(0)
       ```

    3. **Preserve verbatim** the skip list at line ~1342 — `--version` and `-V` already there per D-10. Do NOT modify.

    4. **Preserve verbatim** the success footer at __main__.py:1370 (`print(f"[graphify] version {__version__}")`) per D-09. Do NOT modify.

    5. **Append tests to tests/test_version_sync.py**:

       - `test_render_version_block_multi_line(tmp_path, monkeypatch)` (D-19): Fake two platform install dirs under `tmp_path` via `monkeypatch.setattr(gmain, "_PLATFORM_CONFIG", {...})`. Create one stamp at `tmp_path / "fake_claude" / ".graphify_version"` with `"0.4.7"`; leave `tmp_path / "fake_codex" / ".graphify_version"` missing. Call `gmain._render_version_block(home=tmp_path)`. Assert: starts with `f"graphify {gmain.__version__}"`, contains `"skill stamps:"`, `"0.4.7"`, `"(not installed)"`, `"python:"`, `"install:"`.

       - `test_version_flag_does_not_write_stamp(tmp_path, monkeypatch, capsys)`: Set up a stale stamp under `tmp_path`, monkeypatch `sys.argv = ["graphify", "--version"]` and `Path.home` to return `tmp_path`, call `main()`, catch `SystemExit(0)`, assert stamp content unchanged.

       - `test_render_version_block_handles_corrupt_stamp(tmp_path, monkeypatch)` (T-59.1.03-02): Write a 2048-byte oversized stamp via `version_file.write_bytes(b"x" * 2048)`. Call `gmain._render_version_block(home=tmp_path)`. Assert `"<corrupt>"` appears in the output for that platform's row, function does not raise.

       - `test_version_flag_help_regression(tmp_path)` (Warning 4 / D-10): Run `python -m graphify --help` via subprocess, assert `returncode == 0` and `"usage"` (case-insensitive) appears in stdout. Also run `python -m graphify --version` and assert it does NOT print a help-style usage banner (asserts `"usage:"` is NOT in stdout — proves --version did not become a help-output side effect).

    6. **Run** `pytest tests/test_version_sync.py -q` and `pytest tests/ -q`. Both must pass.

    Commit: `feat(59.1-03): multi-line graphify --version output (VSYNC-02)`
  </action>
  <verify>
    <automated>cd /Users/silveimar/Documents/silogia-repos/graphify && pytest tests/test_version_sync.py -q 2>&1 | tail -3 && python -c "import subprocess,sys; r=subprocess.run([sys.executable,'-m','graphify','--help'],capture_output=True); sys.exit(0 if r.returncode==0 and b'usage' in r.stdout.lower() else 1)" && python -c "from graphify.__main__ import _render_version_block; out = _render_version_block(); print(out); assert 'skill stamps:' in out and 'python:' in out and 'install:' in out"</automated>
  </verify>
  <acceptance_criteria>
    - `_render_version_block` exists: `grep -c "^def _render_version_block" graphify/__main__.py` returns `1`
    - `--version` handler now calls it: `grep -c "_render_version_block()" graphify/__main__.py` returns `>= 1`
    - Output contains all four sections — `python -c "from graphify.__main__ import _render_version_block; o=_render_version_block(); assert 'skill stamps:' in o and 'python:' in o and 'install:' in o"` exits 0
    - --help still works post-change (D-10 regression guard) — `python -c "import subprocess,sys; r=subprocess.run([sys.executable,'-m','graphify','--help'],capture_output=True); sys.exit(0 if r.returncode==0 and b'usage' in r.stdout.lower() else 1)"` exits 0
    - Corrupt-stamp rendering verifiable: `grep -c '<corrupt>' graphify/__main__.py` returns `>= 1` (literal token present in render code)
    - Footer at __main__.py:1370 unchanged: `grep -c '\[graphify\] version {__version__}' graphify/__main__.py` returns `>= 1` (D-09)
    - Skip list still includes `--version`/`-V`: `grep -A 3 "_skip_skill_version_check" graphify/__main__.py | grep -c -- "--version"` returns `>= 1` (D-10)
    - `pytest tests/test_version_sync.py -q` exits 0
    - Commit message matches `^feat(59.1-03):`: `git log -1 --pretty=%s | grep -E "^feat\(59\.1-03\):"`
  </acceptance_criteria>
  <done>--version emits the multi-line block; corrupt stamps render as `<corrupt>`; --help regression-safe; tests pass; footer and skip list untouched.</done>
</task>

<task type="auto">
  <name>Task 2: Prepend `version sync` section to `graphify doctor` (VSYNC-03, D-12)</name>
  <files>graphify/__main__.py, tests/test_version_sync.py</files>
  <read_first>
    - graphify/__main__.py (doctor handler starting at line 2807 — `elif cmd == "doctor":`)
    - .planning/phases/59.1-version-sync-hygiene-and-version-flag-fix-skill-stamp-drift-/59.1-CONTEXT.md (D-12 — exact section format and statuses)
  </read_first>
  <action>
    1. **Add a helper** `_render_doctor_version_sync(home: Path | None = None) -> str` near `_render_version_block`. Returns a string with this format:

       ```
       version sync:
         <platform-name>: stamp=<value-or-—-or-<corrupt>> package=<pkg-version> status=<✓ in sync | ! drifted-newer | ! drifted-older | — not installed>
         ...
       ```

       - Status logic per D-12:
         - No stamp file → `— not installed`
         - `stamp == pkg` → `✓ in sync`
         - `stamp > pkg` (using `_numeric_version_prefix_tuple`) → `! drifted-newer`
         - `stamp < pkg` → `! drifted-older` (in practice healed away by `_check_skill_version()`, but `doctor` is purely diagnostic and may run on read-only filesystems)
         - Stamp file exists but oversized (>1024 bytes) or unreadable → render stamp value as the literal string `<corrupt>` and status `! drifted-newer` (treat as drift; the heal write path will not run because the size guard returned early in plan 02)
       - **Corrupt-rendering branch (T-59.1.03-02):** Same guard as `_render_version_block`: `if version_file.stat().st_size > 1024: stamp = "<corrupt>"`. Wrap `read_text` in try/except; on failure set `stamp = "<corrupt>"`. Do NOT raise.
       - Use `_PLATFORM_CONFIG` iteration identical to `_render_version_block`.
       - Compute on the fly; no sentinel file.

    2. **Insert into the doctor handler.** Locate the doctor handler block (currently starting around line 2807 with `elif cmd == "doctor":`). Insert `print(_render_doctor_version_sync())` as the FIRST statement inside that elif branch, before any other doctor logic. Followed by `print()` (blank line separator) so subsequent doctor output is visually distinct.

       ```python
       elif cmd == "doctor":
           print(_render_doctor_version_sync())
           print()
           # ... existing doctor logic continues unchanged ...
       ```

       Do NOT refactor surrounding doctor logic. Do NOT change exit codes.

    3. **Append tests to tests/test_version_sync.py**:

       - `test_doctor_version_sync_section(tmp_path, monkeypatch)` (D-12): Build a minimal `_PLATFORM_CONFIG` fixture under `tmp_path` with three platforms — one in-sync (stamp == `__version__`), one drifted-newer (stamp `"99.99.99"`), one not-installed (no stamp file). Patch `_PLATFORM_CONFIG` and call `_render_doctor_version_sync(home=tmp_path)`. Assert the output contains: `"version sync"`, `"✓ in sync"`, `"! drifted-newer"`, `"— not installed"`.

       - `test_doctor_version_sync_renders_corrupt_stamp(tmp_path, monkeypatch)` (T-59.1.03-02): Build a single-platform `_PLATFORM_CONFIG` fixture with an oversized stamp (`version_file.write_bytes(b"x" * 2048)`). Call `_render_doctor_version_sync(home=tmp_path)`. Assert `"<corrupt>"` appears in the output.

       - End-to-end test: invoke `main()` (or subprocess `python -m graphify doctor`) with patched argv and assert `"version sync"` appears in stdout. Skip if doctor requires a vault context that can't be fixtured cheaply; in that case keep only the helper-level tests above.

    4. **Run** `pytest tests/test_version_sync.py -q` and `pytest tests/ -q`. Both must pass.

    Commit: `feat(59.1-03): add 'version sync' section to graphify doctor (VSYNC-03)`
  </action>
  <verify>
    <automated>cd /Users/silveimar/Documents/silogia-repos/graphify && pytest tests/test_version_sync.py -q 2>&1 | tail -3 && grep -n -A1 'elif cmd == "doctor":' graphify/__main__.py | grep -q '_render_doctor_version_sync'</automated>
  </verify>
  <acceptance_criteria>
    - `_render_doctor_version_sync` exists: `grep -c "^def _render_doctor_version_sync" graphify/__main__.py` returns `1`
    - Doctor handler invokes it as the first statement: `grep -n -A1 'elif cmd == "doctor":' graphify/__main__.py | grep -q '_render_doctor_version_sync'` exits 0
    - Status strings present: `grep -cE "in sync|drifted-newer|not installed" graphify/__main__.py` returns `>= 3`
    - Corrupt rendering present in doctor helper: `grep -A 40 "^def _render_doctor_version_sync" graphify/__main__.py | grep -c "<corrupt>"` returns `>= 1`
    - `pytest tests/test_version_sync.py -q` exits 0 (full plan-02 + plan-03 tests pass)
    - `pytest tests/ -q` exits 0 (no regressions in pre-existing test suite)
    - Commit message matches `^feat(59.1-03):`: `git log -1 --pretty=%s | grep -E "^feat\(59\.1-03\):"`
  </acceptance_criteria>
  <done>graphify doctor emits a `version sync` section as its first output; corrupt stamps render as `<corrupt>`; tests pass; existing doctor logic untouched.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| `.graphify_version` stamp files → `_render_version_block`, `_render_doctor_version_sync` | Read-only display path; same trust assumptions as plan 02. No new stamp writes added by this plan. Display helpers reuse plan 02's 1024-byte size guard pattern. |
| `Path(graphify.__file__).resolve()` → `install:` line | Trusted (Python import mechanism). Wrapped in try/except → falls back to `unknown` on any error. |
| `_PLATFORM_CONFIG` iteration → output strings | All values are static module-level constants; no user input. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-59.1.03-01 | Information disclosure | `--version` block exposes absolute install paths | accept | Paths are user-local (`~/...`-shortened where possible). No secrets. Pre-existing `pip show graphifyy` discloses the same path. |
| T-59.1.03-02 | Tampering | Oversized/corrupt stamp read by display helpers | mitigate | Both `_render_version_block` and `_render_doctor_version_sync` guard with `if version_file.stat().st_size > 1024: stamp = "<corrupt>"` and wrap `read_text` in try/except (set `stamp = "<corrupt>"` on failure). The string `<corrupt>` is rendered verbatim into output rows; helpers never raise. Tests `test_render_version_block_handles_corrupt_stamp` and `test_doctor_version_sync_renders_corrupt_stamp` enforce this. |
| T-59.1.03-03 | Denial of service | `--version` triggering heavy IO | accept | Iterates ≤ 7 paths (`_PLATFORM_CONFIG`), one stat + one ≤1024-byte read each. Bounded by size guard. |
| T-59.1.03-04 | Side-effects on read-only command | `--version` accidentally writing stamp | mitigate | D-10 contract: `--version`/`-V` already in skip list; explicit test `test_version_flag_does_not_write_stamp` asserts no stamp write. Helpers are pure (no `write_text` calls in `_render_version_block` or `_render_doctor_version_sync`). |
</threat_model>

<verification>
- `pytest tests/test_version_sync.py -q` exits 0 (plan 02 + plan 03 tests, all green)
- `pytest tests/ -q` exits 0 (no regressions across full suite)
- `python -m graphify --help` exits 0 and contains "usage" (D-10 regression guard)
- Manual smoke: `graphify --version` prints multi-line block with package, skill stamps, python, install lines; `graphify doctor` output STARTS with a `version sync:` block; `graphify` (bare command) on a stamp-older system emits zero stderr lines (plan 02 contract still holds)
</verification>

<success_criteria>
- VSYNC-02 truths satisfied: multi-line output, side-effect-free, footer at line 1370 unchanged
- VSYNC-03 truth satisfied: doctor `version sync` section emits per-platform status as the first line of doctor output
- D-09: `_cli_exit()` footer unchanged
- D-10: `--help` still works post-change; --version does not become a help-output side effect
- D-13 still holds from plan 02: `_check_skill_version` body remains a single inline function
- T-59.1.03-02 mitigated: oversized/unreadable stamps render as the literal `<corrupt>` in both --version and doctor output
- All 4 VSYNC requirements have at least one passing test in `tests/test_version_sync.py`
</success_criteria>

<output>
After completion, create `.planning/phases/59.1-version-sync-hygiene-and-version-flag-fix-skill-stamp-drift-/59.1-03-SUMMARY.md`.
</output>
