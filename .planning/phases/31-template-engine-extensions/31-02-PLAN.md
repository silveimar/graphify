---
phase: 31-template-engine-extensions
plan: 02
type: execute
wave: 2
depends_on: ["01"]
files_modified:
  - graphify/profile.py
  - graphify/templates.py
  - tests/test_profile.py
  - tests/test_templates.py
  - tests/fixtures/profiles/dataview_queries_valid.yaml
  - tests/fixtures/profiles/dataview_queries_unknown_key.yaml
  - tests/fixtures/profiles/dataview_queries_legacy_fallback.yaml
autonomous: true
requirements:
  - TMPL-03
tags: [profile, dataview, templates, validate-profile, provenance]

must_haves:
  truths:
    - "A profile declaring `dataview_queries: {moc: '<custom query>'}` produces a rendered MOC note whose Dataview block content matches the custom query (not the legacy `obsidian.dataview.moc_query`)"
    - "A profile with NO `dataview_queries` key falls back to the legacy `obsidian.dataview.moc_query` value — backward compatibility is preserved"
    - "A profile with `dataview_queries` keys outside `_KNOWN_NOTE_TYPES = {moc, community, thing, statement, person, source}` produces a `validate_profile` error at preflight (D-12)"
    - "`dataview_queries` is registered in `_VALID_TOP_LEVEL_KEYS` (D-11) and participates in `_deep_merge` for the Phase 30 `extends:`/`includes:` chain (D-14)"
    - "Phase 30's `--validate-profile` output shows per-key field provenance for each `dataview_queries.<note_type>` entry (D-14)"
    - "All six rendered note types (moc, community, thing, statement, person, source) consult `dataview_queries.<note_type>` BEFORE falling back to legacy `moc_query`"
    - "When the resolved Dataview query (after safe_substitute) is empty/whitespace, _build_dataview_block returns the empty string AND the rendered note's ${dataview_block} slot is empty (no fence emitted)"
  artifacts:
    - path: "graphify/profile.py"
      provides: "dataview_queries top-level key validation + deep-merge participation"
      contains: "dataview_queries"
    - path: "graphify/templates.py"
      provides: "_build_dataview_block extended with note_type lookup + legacy fallback + empty-query empty-output behavior"
      contains: "_build_dataview_block"
    - path: "graphify/templates.py"
      provides: "_render_moc_like signature includes note_type parameter; render_moc passes 'moc'; render_community_overview passes 'community'"
      contains: "_render_moc_like"
    - path: "tests/test_profile.py"
      provides: "validate_profile coverage for dataview_queries (valid, unknown key, non-string, deep-merge)"
      contains: "test_dataview_queries_validates_against_known_types"
  key_links:
    - from: "graphify/profile.py::_VALID_TOP_LEVEL_KEYS"
      to: "dataview_queries"
      via: "whitelist entry (D-11)"
      pattern: '"dataview_queries"'
    - from: "graphify/templates.py::_build_dataview_block"
      to: "profile['dataview_queries'][note_type]"
      via: "per-note-type lookup before legacy fallback (D-13)"
      pattern: 'dataview_queries'
    - from: "graphify/profile.py::validate_profile_preflight"
      to: "--validate-profile output"
      via: "provenance entries for dataview_queries.<note_type> (D-14)"
      pattern: "dataview_queries"
    - from: "graphify/templates.py::_render_moc_like"
      to: "graphify/templates.py::_build_dataview_block"
      via: "note_type parameter propagated from caller (render_moc → 'moc'; render_community_overview → 'community')"
      pattern: "_render_moc_like\\(.*note_type="
---

<objective>
Add per-note-type Dataview query strings (TMPL-03) declared via a new top-level `dataview_queries: {note_type: query_string}` profile key. `_build_dataview_block` consults the per-note-type override first and falls back to today's `obsidian.dataview.moc_query` when no entry is declared. `validate_profile` restricts keys to `_KNOWN_NOTE_TYPES = {moc, community, thing, statement, person, source}`. The new key composes cleanly with Phase 30's `extends:`/`includes:` chain via `_deep_merge`, and per-key provenance surfaces in `--validate-profile` output.

Purpose: Ship the third v1.7 template-engine extension while preserving backward compatibility (profiles without `dataview_queries` render exactly as today) and the Phase 30 graph-blind preflight discipline (unknown keys fail at load time, never mid-render).

Output: Extended `graphify/profile.py` (whitelist + validator + provenance) and `graphify/templates.py::_build_dataview_block` (per-note-type lookup) with locked `note_type` propagation through `_render_moc_like`, plus comprehensive tests in `tests/test_profile.py` and `tests/test_templates.py`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/31-template-engine-extensions/31-CONTEXT.md
@.planning/phases/31-template-engine-extensions/31-RESEARCH.md
@.planning/phases/31-template-engine-extensions/31-PATTERNS.md
@.planning/phases/30-profile-composition/30-CONTEXT.md
@graphify/profile.py
@graphify/templates.py
@tests/test_profile.py
@tests/test_templates.py

<interfaces>
<!-- Surfaces this plan extends (verbatim from grep + templates.py read). -->

graphify/profile.py:
  L85   "moc_query": "TABLE file.folder as Folder, type, source_file\nFROM #community/${community_tag}\nSORT file.name ASC"   # legacy default
  L130  _VALID_TOP_LEVEL_KEYS = { ... }                                                                                       # ADD "dataview_queries"
  L156  _deep_merge                                                                                                            # already handles per-key merge
  L447  validate_profile                                                                                                       # EXTEND with dataview_queries checks
  L990+ validate_profile_preflight (--validate-profile engine)                                                                 # EXTEND provenance output
  L1052 function-local import pattern (break templates ↔ profile cycle — replicate for _KNOWN_NOTE_TYPES)

graphify/templates.py:
  L517–L521  _DEFAULT_PROFILE-derived _FALLBACK_MOC_QUERY (single source of truth)
  L525  def _build_dataview_block(profile: dict, community_tag: str, folder: str) -> str:                                      # EXTEND: add note_type
  L540–L546 existing moc_query lookup (obsidian.dataview.moc_query → fallback)
  L553  string.Template(moc_query).safe_substitute(community_tag=..., folder=...)                                              # PRESERVE: two-phase substitution
  L570  render_note(node_id, G, profile, note_type, classification_context, *, vault_dir=None, created=None)
        # render_note ALREADY has `note_type` parameter — pass through verbatim
  L798  _render_moc_like(community_id, G, communities, profile, classification_context, template_key, vault_dir, created=None)
        # _render_moc_like ALREADY has `template_key: str` parameter carrying "moc" or "community"
  L881  dataview_block = _build_dataview_block(profile, community_tag, folder)                                                 # CALL SITE — pass note_type
  L917  render_moc       — calls _render_moc_like with template_key="moc"
  L938  render_community_overview — calls _render_moc_like with template_key="community"

LOCKED CONSTANTS (D-12):
  _KNOWN_NOTE_TYPES = {"moc", "community", "thing", "statement", "person", "source"}   # exactly six, no more

LOCKED note_type propagation (Warning 5 — confirmed by templates.py read):
  _build_dataview_block gains parameter `note_type: str` (positional, after `folder`).
  _render_moc_like adds parameter `note_type: str` (Plan 02 introduces this — `template_key` is also "moc"/"community" but is overloaded for template selection AND frontmatter; we add an explicit `note_type` parameter so the dataview lookup uses an unambiguous source even if `template_key` semantics drift later).
    NEW signature:
      def _render_moc_like(
          community_id, G, communities, profile, classification_context,
          template_key: str, vault_dir, created=None, *, note_type: str,
      ) -> tuple[str, str]
  render_moc passes `note_type="moc"`; render_community_overview passes `note_type="community"`.
  render_note already receives `note_type` — pass through unchanged into `_build_dataview_block(profile, community_tag, folder, note_type)`.

WAVE GATING (Warning 8):
  Plan 02 lands in wave 2 after Plan 01 completes. Both plans modify the four render entry points
  (render_note, _render_moc_like, render_moc, render_community_overview) AND tests/test_templates.py.
  Plan 01 wraps these entry points in _expand_blocks; Plan 02 adds note_type to the
  _build_dataview_block call inside the same functions. Sequential wave assignment eliminates the
  merge-conflict surface — no "sub-second offset" parallelism required or attempted.
</interfaces>
</context>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| profile YAML → render | User-authored profile YAML (vault-side `.graphify/profile.yaml`) declares Dataview query strings interpolated into rendered notes |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-31-02 | Tampering / Information Disclosure | `validate_profile` (profile.py L447) | mitigate | Unknown `dataview_queries.<note_type>` key produces a `validate_profile` error at preflight (D-12); whitelist enforced against `_KNOWN_NOTE_TYPES`; non-string / empty-string values rejected; test `test_dataview_queries_unknown_key_rejected` covers the threat |
| T-31-04 | Tampering | `_build_dataview_block` two-phase substitution | mitigate | Existing backtick/newline strip on the user-supplied query string is preserved (templates.py L553 `safe_substitute`); per-note-type query passes through the same sanitization gate before fence-wrap |
</threat_model>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Profile-side — register `dataview_queries` and validate against `_KNOWN_NOTE_TYPES`</name>
  <files>graphify/profile.py, tests/test_profile.py, tests/fixtures/profiles/dataview_queries_valid.yaml, tests/fixtures/profiles/dataview_queries_unknown_key.yaml, tests/fixtures/profiles/dataview_queries_legacy_fallback.yaml</files>
  <read_first>
    - graphify/profile.py (full module — `_VALID_TOP_LEVEL_KEYS` at L130, `_deep_merge` at L156, `validate_profile` at L447, `validate_profile_preflight` and `_REQUIRED_PER_TYPE` import pattern at L1052+)
    - .planning/phases/30-profile-composition/30-CONTEXT.md (precedent for adding top-level keys: `community_templates`; deep-merge per-key precedence; --validate-profile provenance output format)
    - .planning/phases/31-template-engine-extensions/31-RESEARCH.md (L595–L612 validate_profile extension code example; L690 validate_profile signature notes; L736 module-cycle note: function-local definition for `_KNOWN_NOTE_TYPES`)
    - .planning/phases/31-template-engine-extensions/31-CONTEXT.md (D-11, D-12, D-14 — locked decisions)
    - tests/test_profile.py (existing patterns — `validate_profile` invocation, fixture file usage, `--validate-profile` smoke tests)
  </read_first>
  <behavior>
    **profile.py changes:**
    - `_VALID_TOP_LEVEL_KEYS` (L130) gains the entry `"dataview_queries"` — exactly one new whitelist member (D-11)
    - `validate_profile` (L447) gains a block that validates `profile.get("dataview_queries", {})`:
      - Must be a dict (else: `f"dataview_queries must be a dict, got {type(value).__name__}"`)
      - Every key must be in `_KNOWN_NOTE_TYPES = frozenset({"moc","community","thing","statement","person","source"})` (D-12). Define `_KNOWN_NOTE_TYPES` as a module-private frozenset in `profile.py` itself (RESEARCH L736: function-local definition to avoid `templates.py` ↔ `profile.py` import cycle — duplicate the small set; this matches the `_REQUIRED_PER_TYPE` precedent at profile.py L1052)
      - Unknown key → `f"dataview_queries: unknown note_type '{key}' — valid types are: {sorted(_KNOWN_NOTE_TYPES)}"`
      - Every value must be a non-empty string → `f"dataview_queries.{key}: query must be a non-empty string"`
    - `_deep_merge` (L156) requires NO change — it already merges arbitrary top-level dicts per-key. Verify by adding a deep-merge composition test.
    - `validate_profile_preflight` (`--validate-profile` engine, L990+) emits per-key provenance for each `dataview_queries.<note_type>` entry following the Phase 30 D-15/D-16 pattern.

    **Test fixtures:**
    - `tests/fixtures/profiles/dataview_queries_valid.yaml` — minimal valid profile with `dataview_queries: {moc: "TABLE x FROM y", community: "TABLE z FROM w"}`
    - `tests/fixtures/profiles/dataview_queries_unknown_key.yaml` — profile with `dataview_queries: {mocs: "TABLE x"}` (typo) for the rejection test
    - `tests/fixtures/profiles/dataview_queries_legacy_fallback.yaml` — profile with NO `dataview_queries` key but a custom `obsidian.dataview.moc_query`

    **Tests in `tests/test_profile.py`:**
    - `test_dataview_queries_top_level_key_accepted` — valid profile loads with no errors
    - `test_dataview_queries_validates_against_known_types` — every key in `_KNOWN_NOTE_TYPES` accepted
    - `test_dataview_queries_unknown_key_rejected` — typo `mocs:` produces error containing `"unknown note_type 'mocs'"`
    - `test_dataview_queries_non_dict_rejected` — `dataview_queries: "not a dict"` produces error containing `"must be a dict"`
    - `test_dataview_queries_empty_string_rejected` — `dataview_queries: {moc: ""}` produces error containing `"non-empty string"`
    - `test_dataview_queries_non_string_value_rejected` — `dataview_queries: {moc: 123}` produces error containing `"non-empty string"`
    - `test_dataview_queries_deep_merge_per_key_precedence` — base + child override; resolved profile preserves base for non-overridden keys
    - `test_dataview_queries_provenance_in_validate_profile_output` — `--validate-profile` output shows correct provenance per resolved key
    - `test_dataview_queries_absent_key_is_legacy_compatible` — profile WITHOUT `dataview_queries` validates clean
  </behavior>
  <action>
    1. In `graphify/profile.py`:
       a. Add `"dataview_queries"` to `_VALID_TOP_LEVEL_KEYS` (L130).
       b. Add module-level constant: `_KNOWN_NOTE_TYPES: frozenset[str] = frozenset({"moc", "community", "thing", "statement", "person", "source"})`.
       c. In `validate_profile` (L447), add the dict/key/value validation block as specified.
       d. In `validate_profile_preflight` (L990+), add a parallel branch emitting `dataview_queries.<note_type>` provenance entries matching the Phase 30 format. Read the Phase 30 implementation first; do not invent format.

    2. Create the three fixture YAML files under `tests/fixtures/profiles/`. Each must be a complete minimal valid profile (copy minimal-valid template from existing fixture).

    3. Add the nine test functions to `tests/test_profile.py` under section comment `# --- TMPL-03: dataview_queries top-level key ---`. Pure unit tests using `tmp_path`.

    4. The provenance test should invoke the `--validate-profile` CLI surface (or its underlying function) and assert specific substrings — match Phase 30's `community_templates` provenance test style.
  </action>
  <verify>
    <automated>cd /Users/silveimar/Documents/silogia-repos/graphify && pytest tests/test_profile.py -q -x 2>&1 | tail -20</automated>
  </verify>
  <acceptance_criteria>
    - `grep -nE '"dataview_queries"' graphify/profile.py` returns ≥ 1 match
    - `grep -nE "_KNOWN_NOTE_TYPES.*frozenset" graphify/profile.py` returns exactly one match
    - `python3 -c "from graphify.profile import _VALID_TOP_LEVEL_KEYS; assert 'dataview_queries' in _VALID_TOP_LEVEL_KEYS"` exits 0
    - `python3 -c "from graphify.profile import _KNOWN_NOTE_TYPES; assert _KNOWN_NOTE_TYPES == frozenset({'moc','community','thing','statement','person','source'})"` exits 0
    - `pytest tests/test_profile.py -q -k "dataview_queries"` collects ≥ 9 tests and all pass
    - `pytest tests/test_profile.py -q -x` exits 0
    - `ls tests/fixtures/profiles/dataview_queries_*.yaml | wc -l` returns 3
    - `grep -F "unknown note_type 'mocs'" tests/test_profile.py | wc -l` returns ≥ 1
    - `grep -v '^#' tests/test_profile.py | grep -c "test_dataview_queries_"` returns >= 9
  </acceptance_criteria>
  <done>
    `dataview_queries` registered in `_VALID_TOP_LEVEL_KEYS`. `validate_profile` rejects unknown keys, non-dict, empty/non-string values. `_deep_merge` composition verified. `--validate-profile` provenance shows per-key entries matching Phase 30 format. All nine tests pass; no regressions.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Templates-side — `_build_dataview_block` per-note-type lookup with locked note_type propagation through `_render_moc_like`, empty-query empty-output behavior, render tests + Plan 01 cross-link</name>
  <files>graphify/templates.py, tests/test_templates.py</files>
  <read_first>
    - graphify/templates.py L517–L562 (existing `_FALLBACK_MOC_QUERY` and `_build_dataview_block` body — two-phase substitution pattern that MUST be preserved)
    - graphify/templates.py L570 (`render_note` — already has `note_type` param), L798 (`_render_moc_like` — already has `template_key: str` carrying "moc"/"community"; this task ADDS an explicit `note_type: str` parameter), L881 (current `_build_dataview_block` call site inside `_render_moc_like`), L917 (`render_moc` — passes `template_key="moc"`), L938 (`render_community_overview` — passes `template_key="community"`)
    - .planning/phases/31-template-engine-extensions/31-RESEARCH.md L561–L590 (exact code shape for per-note-type lookup with legacy fallback; preserved two-phase substitution)
    - .planning/phases/31-template-engine-extensions/31-CONTEXT.md (D-13 — _build_dataview_block extension contract)
    - tests/test_templates.py (existing render test patterns — graph fixtures, profile fixtures, full-pipeline render assertions)
  </read_first>
  <behavior>
    **templates.py changes:**
    - `_build_dataview_block(profile, community_tag, folder, note_type)` — `note_type: str` added as fourth positional argument.
    - Lookup order inside `_build_dataview_block` (D-13):
      1. `per_type = (profile.get("dataview_queries") or {}).get(note_type)` — if non-empty string after `.strip()`, use as `moc_query`
      2. Else fall back to existing `obsidian.dataview.moc_query` lookup
      3. Else fall back to `_FALLBACK_MOC_QUERY`
    - **Empty-query empty-output behavior (Warning 7 / VALIDATION TMPL-03)**: After two-phase substitution, if the resolved query string is empty or whitespace-only (i.e., `not query.strip()`), `_build_dataview_block` returns the empty string `""` (no fence emitted). The downstream `${dataview_block}` substitution slot will therefore be empty, and Plan 01's `BlockContext.dataview_nonempty` will be `False` (`bool(dataview_block.strip()) == False`), which makes `{{#if_has_dataview}}` blocks omit cleanly.
    - Two-phase substitution (existing) is preserved unchanged — the looked-up query string still goes through `string.Template(moc_query).safe_substitute(community_tag=..., folder=...)` before fence-wrap. The empty-output check runs AFTER substitution.
    - `_render_moc_like` (L798) gains an explicit `note_type: str` keyword-only parameter (added at the end of the existing signature). Reason: `template_key` is already overloaded for template selection + frontmatter — `note_type` is added as a sibling parameter so the dataview lookup is unambiguously sourced. Callers `render_moc` and `render_community_overview` pass `note_type="moc"` and `note_type="community"` respectively.
    - All four render entry points pass note_type to `_build_dataview_block`:
      - `render_note` (L570) — passes its existing `note_type` argument
      - `_render_moc_like` (L798) — passes its new `note_type` argument
      - `render_moc` (L917) — passes `note_type="moc"` to `_render_moc_like`
      - `render_community_overview` (L938) — passes `note_type="community"` to `_render_moc_like`
    - `_FALLBACK_MOC_QUERY` and the `_DEFAULT_PROFILE` single-source-of-truth pattern preserved.

    **Tests in `tests/test_templates.py`** (new section `# --- TMPL-03: per-note-type Dataview queries ---`):
    - `test_dataview_queries_per_note_type_overrides_default` — profile with `dataview_queries: {moc: "TABLE custom FROM #x"}` produces a rendered MOC note containing `TABLE custom FROM #x` inside the Dataview fence
    - `test_dataview_queries_moc_override` — verifies legacy `obsidian.dataview.moc_query` is NOT used when override present
    - `test_dataview_queries_legacy_fallback` — profile WITHOUT `dataview_queries` falls back to `obsidian.dataview.moc_query` (loads `dataview_queries_legacy_fallback.yaml`)
    - `test_dataview_queries_default_fallback` — neither key set → falls back to `_FALLBACK_MOC_QUERY`
    - `test_dataview_queries_each_note_type_routes_correctly` — parametric: profile defines all six per-note-type queries; render each note type and verify the correct query string in output
    - `test_dataview_queries_two_phase_substitution_preserved` — query string `"FROM #${community_tag} WHERE folder = ${folder}"` correctly substitutes both `community_tag` and `folder`
    - `test_dataview_block_omitted_when_resolved_query_empty` — profile with `dataview_queries: {moc: "   "}` (whitespace) — wait, validate_profile rejects empty/whitespace at preflight. Construct the empty-output case via the post-substitution path: a query `"${community_tag}"` rendered with `community_tag=""` and `folder=""` yields empty after `.strip()`. Assert `_build_dataview_block(...)` returns `""` AND a full render of a MOC note with this profile has an empty `${dataview_block}` slot (no fence emitted in the output).

    **Plan 01 cross-link test** (added to the existing `# --- TMPL-01: conditional blocks ---` section if not already present from Plan 01, otherwise verified-only):
    - `test_if_has_dataview_false_when_query_empty` — Plan 01 ships this test; this plan ensures the `_build_dataview_block` empty-output behavior makes it pass. If Plan 01's test asserts `BlockContext.dataview_nonempty=False` for an empty-query render, this plan's `_build_dataview_block` change must be the source of that empty value. Add an integration assertion in the Plan 02 test section if not covered: render a MOC with whitespace-resolving query, assert `{{#if_has_dataview}}HAS-DV{{/if}}` block omits.
  </behavior>
  <action>
    1. Modify `graphify/templates.py::_build_dataview_block` (L525):

    ```python
    def _build_dataview_block(
        profile: dict,
        community_tag: str,
        folder: str,
        note_type: str,
    ) -> str:
        """Render a ```dataview fence using the profile's per-note-type query.

        Lookup order (D-13):
          1. profile["dataview_queries"][note_type]   — Phase 31 per-note-type override
          2. profile["obsidian"]["dataview"]["moc_query"]  — legacy single-query
          3. _FALLBACK_MOC_QUERY                       — built-in default

        Empty-query empty-output (TMPL-03 Warning 7): when the resolved query
        string is empty or whitespace-only after two-phase substitution, return
        the empty string (no fence). This propagates to BlockContext.dataview_nonempty
        = False so {{#if_has_dataview}} blocks omit cleanly.
        """
        per_type = (profile.get("dataview_queries") or {}).get(note_type)
        if isinstance(per_type, str) and per_type.strip():
            moc_query = per_type
        else:
            moc_query = (
                profile.get("obsidian", {})
                .get("dataview", {})
                .get("moc_query")
            )
            if not moc_query:
                moc_query = _FALLBACK_MOC_QUERY

        # Sanitize substitution values (preserve existing WR-05 behavior).
        safe_community_tag = community_tag.replace("`", "").replace("\n", "").replace("\r", "")
        safe_folder = folder.replace("`", "").replace("\n", "").replace("\r", "")

        # Two-phase substitution (preserved).
        query = string.Template(moc_query).safe_substitute(
            community_tag=safe_community_tag,
            folder=safe_folder,
        )
        query = query.replace("```", "")

        # Empty-query empty-output (Warning 7 / TMPL-03).
        if not query.strip():
            return ""

        body = f"```dataview\n{query}\n```"
        return _wrap_sentinel("dataview", body)
    ```

    2. Update `_render_moc_like` (L798) signature to add `note_type: str` keyword-only parameter (positioned after `created`):

    ```python
    def _render_moc_like(
        community_id, G, communities, profile, classification_context,
        template_key: str, vault_dir, created: "datetime.date | None" = None,
        *, note_type: str,
    ) -> tuple[str, str]:
        ...
        dataview_block = _build_dataview_block(profile, community_tag, folder, note_type)
        ...
    ```

    Update `render_moc` (L917) to pass `note_type="moc"`. Update `render_community_overview` (L938) to pass `note_type="community"`.

    Update `render_note` (L570 — already has `note_type`) call site for `_build_dataview_block`: `_build_dataview_block(profile, community_tag, folder, note_type)`.

    3. Add the seven tests described in `<behavior>` to `tests/test_templates.py` under section comment `# --- TMPL-03: per-note-type Dataview queries ---`. Use Task 1 fixture YAML files. Use `tmp_path` for any output writes.

    4. The `test_dataview_queries_each_note_type_routes_correctly` test should use `pytest.mark.parametrize` over the six note types — match existing parametric test style if one exists.

    5. Verify backward compatibility via `test_dataview_queries_legacy_fallback`: profile WITHOUT `dataview_queries` and WITH `obsidian.dataview.moc_query: "TABLE legacy FROM #x"` must render `TABLE legacy FROM #x` (substituted).
  </action>
  <verify>
    <automated>cd /Users/silveimar/Documents/silogia-repos/graphify && pytest tests/test_templates.py tests/test_profile.py -q -x 2>&1 | tail -20</automated>
  </verify>
  <acceptance_criteria>
    - `grep -nE "def _build_dataview_block\(" graphify/templates.py` returns one match with signature ending in `note_type: str` (or equivalent positional)
    - `grep -v '^#' graphify/templates.py | grep -c "_build_dataview_block("` returns >= 5 (definition + 4 call sites: render_note, _render_moc_like body, +2 indirect via render_moc/render_community_overview if they call directly; otherwise 2 call sites + definition)
    - `grep -E "_render_moc_like\(.*note_type=" graphify/templates.py | wc -l` returns >= 2 (Warning 5 acceptance — render_moc passes note_type="moc"; render_community_overview passes note_type="community")
    - `grep -F 'profile.get("dataview_queries"' graphify/templates.py | wc -l` returns ≥ 1
    - `grep -F 'if not query.strip():' graphify/templates.py | wc -l` returns >= 1 (empty-output gate)
    - `pytest tests/test_templates.py -q -k "dataview_queries or dataview_block_omitted"` collects ≥ 7 tests and all pass
    - `pytest tests/test_templates.py tests/test_profile.py -q -x` exits 0
    - `pytest tests/ -q -x` exits 0
    - `grep -v '^#' tests/test_templates.py | grep -c "test_dataview_queries_"` returns >= 6
    - `grep -v '^#' tests/test_templates.py | grep -c "test_dataview_block_omitted_when_resolved_query_empty"` returns >= 1
    - The legacy-fallback test loads `tests/fixtures/profiles/dataview_queries_legacy_fallback.yaml` and asserts the legacy `obsidian.dataview.moc_query` value reaches the rendered output
    - No new required dependency: `grep -A 30 'dependencies = \[' pyproject.toml | grep -E 'jinja|mako|chevron'` returns empty
  </acceptance_criteria>
  <done>
    `_build_dataview_block` accepts `note_type` and consults `dataview_queries.<note_type>` first; legacy and default fallbacks preserved; empty resolved query → empty output (no fence). `_render_moc_like` carries a locked `note_type` parameter; `render_moc` passes `"moc"`; `render_community_overview` passes `"community"`. All four call sites pass the correct note_type. Seven new tests cover override, legacy fallback, default fallback, all-six routing, two-phase substitution preservation, and empty-output behavior. Plan 01 cross-link `test_if_has_dataview_false_when_query_empty` passes against this plan's `_build_dataview_block` empty-output behavior. Combined Plan 02 test run is green; no regressions.
  </done>
</task>

</tasks>

<verification>
- `pytest tests/test_profile.py -q -x` passes.
- `pytest tests/test_templates.py -q -x` passes.
- `pytest tests/ -q -x` passes.
- `python3 -c "from graphify.profile import _VALID_TOP_LEVEL_KEYS, _KNOWN_NOTE_TYPES; assert 'dataview_queries' in _VALID_TOP_LEVEL_KEYS and _KNOWN_NOTE_TYPES == frozenset({'moc','community','thing','statement','person','source'})"` succeeds.
- Round-trip: load `dataview_queries_valid.yaml`, render a MOC note, verify the custom query string appears inside the Dataview fence.
- `--validate-profile` CLI on `dataview_queries_valid.yaml` shows per-key provenance entries.
</verification>

<success_criteria>
- `dataview_queries` is a recognized top-level profile key validated against `_KNOWN_NOTE_TYPES` at preflight (D-11/D-12).
- Per-note-type override wins over legacy `obsidian.dataview.moc_query`; both fall back to `_FALLBACK_MOC_QUERY` (D-13).
- `_render_moc_like` carries `note_type` parameter; `render_moc` passes `"moc"`; `render_community_overview` passes `"community"` (Warning 5 locked).
- Empty resolved Dataview query → empty `_build_dataview_block` output → `${dataview_block}` slot empty → `{{#if_has_dataview}}` blocks omit cleanly (Warning 7 locked, Plan 01 cross-link verified).
- Phase 30 deep-merge composition works for `dataview_queries` per-key (D-14); provenance surfaces in `--validate-profile` output.
- Backward compatibility: profiles without `dataview_queries` render exactly as today.
- Two-phase substitution (`${community_tag}`/`${folder}` interpolation) preserved.
- T-31-02 mitigated: unknown keys cannot silently slip through (typo `mocs:` rejected with helpful message).
</success_criteria>

<output>
After completion, create `.planning/phases/31-template-engine-extensions/31-02-SUMMARY.md` summarizing: `dataview_queries` registration in `_VALID_TOP_LEVEL_KEYS`, the six locked `_KNOWN_NOTE_TYPES`, `_build_dataview_block` signature change with all four updated call sites, `_render_moc_like` `note_type` parameter addition, empty-output behavior locking the Plan 01 cross-link, deep-merge + provenance verification, test counts, and confirmation of backward compatibility for profiles lacking the new key.
</output>
