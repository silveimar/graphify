---
phase: 31-template-engine-extensions
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - graphify/templates.py
  - tests/test_templates.py
  - tests/fixtures/template_context.py
  - tests/fixtures/templates/with_if_block.md
  - tests/fixtures/templates/with_connections_block.md
  - tests/fixtures/templates/with_if_attr.md
  - tests/fixtures/templates/nested_blocks_invalid.md
autonomous: true
requirements:
  - TMPL-01
  - TMPL-02
tags: [templates, conditionals, loops, sanitization, string-template]

must_haves:
  truths:
    - "Template containing {{#if_god_node}}…{{/if}} renders the guarded section when predicate is true and omits it (with no leftover whitespace gaps) when predicate is false"
    - "Template containing {{#connections}}…{{/connections}} iterates each edge with namespaced ${conn.label}, ${conn.relation}, ${conn.target}, ${conn.confidence}, ${conn.community}, ${conn.source_file} (and parallel flat ${conn_label} forms)"
    - "validate_template rejects nested blocks at preflight time with the exact actionable message from D-08"
    - "validate_template rejects unknown predicate names (not in catalog and not prefixed with if_attr_) at preflight time"
    - "Node labels containing '{{', '}}', '#', '${', backticks, or newlines cannot inject conditional logic, smuggle a fake loop, or escape Dataview fences (block expansion runs BEFORE scalar substitution per D-16)"
  artifacts:
    - path: "graphify/templates.py"
      provides: "_BlockTemplate subclass, _PREDICATE_CATALOG, _IF_ATTR_RE, _CONN_FIELDS, block pre-processor, extended validate_template"
      contains: "_BlockTemplate"
    - path: "graphify/templates.py"
      provides: "predicate catalog with exactly four members"
      contains: "_PREDICATE_CATALOG"
    - path: "tests/test_templates.py"
      provides: "TMPL-01/TMPL-02/sanitization test coverage"
      contains: "test_if_god_node_true_renders"
  key_links:
    - from: "graphify/templates.py::render_note"
      to: "graphify/templates.py::_expand_blocks (new)"
      via: "called BEFORE safe_substitute (D-16 ordering invariant)"
      pattern: "_expand_blocks"
    - from: "graphify/templates.py::_expand_blocks"
      to: "graphify/templates.py::_sanitize_wikilink_alias"
      via: "loop iteration values pass through alias sanitizer (D-15)"
      pattern: "_sanitize_wikilink_alias"
    - from: "graphify/templates.py::_BlockTemplate"
      to: "string.Template"
      via: "subclass with idpattern only (no pattern override — D-05 / RESEARCH §Pitfall 3)"
      pattern: "class _BlockTemplate\\(string\\.Template\\)"
---

<objective>
Add `{{#if_X}}…{{/if}}` conditional sections (TMPL-01) and `{{#connections}}…{{/connections}}` iteration blocks (TMPL-02) to graphify's `string.Template`-based template engine, with sanitization hardening that prevents node-label-driven injection.

Purpose: Templates must support conditional rendering against node attributes/classification and per-edge iteration with namespaced scope, without leaving stdlib (no Jinja2). All block parsing happens preflight in `validate_template`; all expansion happens BEFORE `safe_substitute` so labels containing `{{`, `}}`, `#`, backticks, or newlines cannot inject syntax.

Output: Extended `graphify/templates.py` with `_BlockTemplate` subclass, `_PREDICATE_CATALOG` (exactly four entries), block pre-processor, hardened `validate_template`, and a comprehensive test suite covering both features plus the sanitization threat surface.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/31-template-engine-extensions/31-CONTEXT.md
@.planning/phases/31-template-engine-extensions/31-RESEARCH.md
@.planning/phases/31-template-engine-extensions/31-PATTERNS.md
@.planning/phases/31-template-engine-extensions/31-VALIDATION.md
@graphify/templates.py
@graphify/analyze.py
@tests/test_templates.py
@tests/fixtures/template_context.py

<interfaces>
<!-- Key surfaces in templates.py the executor must extend (verbatim from grep). -->

graphify/templates.py:
  L134  def validate_template(text: str, required: set[str]) -> list[str]:
  L195  def _load_builtin_template(...)
  L207  def load_templates(...)
  L284  def _sanitize_wikilink_alias(label: str) -> str:
  L388  _build_connections_callout (sanitization reference)
  L525  def _build_dataview_block(profile: dict, community_tag: str, folder: str) -> str:
  L570  render_note
  L713  _load_override_template
  L798  _render_moc_like
  L917  render_moc
  L938  render_community_overview

graphify/analyze.py:
  - god-node ranking (existing) — source of truth for `if_god_node`
  - isolated-node detection (existing) — source of truth for `if_isolated`

EXACT idpattern (RESEARCH.md L353, L434):
  idpattern = r"(?a:[_a-z][_a-z0-9]*(?:\.[_a-z][_a-z0-9]*)?)"

EXACT predicate catalog (D-02, locked, exactly four):
  if_god_node, if_isolated, if_has_connections, if_has_dataview

EXACT loop variables (D-04, locked, full set):
  conn.label, conn.relation, conn.target, conn.confidence, conn.community, conn.source_file
  (also emit parallel flat: conn_label, conn_relation, conn_target, conn_confidence, conn_community, conn_source_file)

EXACT block syntax (locked by ROADMAP success criteria 1 & 2):
  {{#if_X}} ... {{/if}}
  {{#if_attr_<name>}} ... {{/if}}
  {{#connections}} ... {{/connections}}

EXACT nested-block error message (D-08, verbatim):
  "validate_template: nested template blocks are not supported (found '{{#if_god_node}}' inside '{{#connections}}'). Flatten the template or pre-compute the predicate."
</interfaces>
</context>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| graph-data → template render | Node labels, relations, source_files (untrusted user content from extracted code/docs) flow into template substitution values |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-31-01 | Tampering / Elevation | `_expand_blocks` in templates.py | mitigate | Block expansion runs BEFORE `safe_substitute` (D-16); per-iteration `conn.label`/`conn.target` pass through `_sanitize_wikilink_alias` (templates.py L284); `conn.source_file` and other free-text fields pass through the same alias sanitizer; tests `test_label_injection_double_brace`, `test_label_injection_block_opener`, `test_label_injection_dataview_fence` cover the threat |
| T-31-03 | DoS | `_expand_blocks` regex / state machine | mitigate | Block parser is a single-pass FSM (no recursive regex); `validate_template` rejects nested blocks at preflight (D-07/D-08), bounding expansion depth to 1 |
</threat_model>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add _BlockTemplate subclass, predicate catalog, and block pre-processor to templates.py</name>
  <files>graphify/templates.py</files>
  <read_first>
    - graphify/templates.py (full module — must understand existing render_note / _build_connections_callout / _build_dataview_block / _sanitize_wikilink_alias surfaces)
    - .planning/phases/31-template-engine-extensions/31-RESEARCH.md (L139–L175 architecture; L347–L370 idpattern pitfalls; L415–L495 code examples for _BlockTemplate, _PREDICATE_CATALOG, _eval_predicate; L561–L590 _build_dataview_block extension)
    - .planning/phases/31-template-engine-extensions/31-CONTEXT.md (D-01 through D-16, all locked)
    - graphify/analyze.py (god-node ranking + isolated-node detection — predicate truth sources)
  </read_first>
  <behavior>
    - `_BlockTemplate(string.Template)` subclass exists with ONLY `idpattern = r"(?a:[_a-z][_a-z0-9]*(?:\.[_a-z][_a-z0-9]*)?)"` overridden (no `pattern` override — RESEARCH §Pitfall 3 forbids it; CPython `__init_subclass__` recompiles `pattern` when `idpattern` is set on the subclass)
    - `_PREDICATE_CATALOG` is a module-level `dict[str, Callable[[BlockContext], bool]]` with exactly four entries (D-02): `if_god_node`, `if_isolated`, `if_has_connections`, `if_has_dataview`. No more, no fewer. The catalog keys carry the `if_` prefix (RESEARCH L173).
    - `_IF_ATTR_RE` matches `if_attr_<name>` where `<name>` is `[a-z_][a-z0-9_]*` (D-01/D-03)
    - `_CONN_FIELDS` is a frozenset of exactly six members: `{"label","relation","target","confidence","community","source_file"}` (D-04)
    - `_expand_blocks(text, ctx)` is a pure function that performs a single-pass FSM over the template source, expanding `{{#…}}…{{/…}}` blocks. It must:
      1. Run BEFORE any `safe_substitute` call (D-16 ordering invariant)
      2. For `{{#if_X}}…{{/if}}`: dispatch to `_PREDICATE_CATALOG[name](ctx)`, then to `_IF_ATTR_RE` for `if_attr_<name>` (reads `G.nodes[node_id].get(<name>)` truthiness per D-03)
      3. For `{{#connections}}…{{/connections}}`: iterate `ctx.edges`, sanitize each iteration value via `_sanitize_wikilink_alias` (D-06/D-15), and emit BOTH `${conn.<field>}` and `${conn_<field>}` forms in the expanded body so stock `string.Template` and `_BlockTemplate` both render correctly (D-05)
      4. Reject nested blocks (D-07) — caller's preflight `validate_template` is the gate; `_expand_blocks` may assume preflight passed but should still raise `ValueError` defensively if it encounters a nested opener
    - `_eval_predicate(name, ctx)` dispatches: catalog hit → call; `if_attr_<name>` match → `bool(G.nodes[ctx.node_id].get(<name>))`; otherwise → raise `KeyError(name)` (preflight should have caught this — `_expand_blocks` re-raises as `ValueError` for render-path safety)
    - `_build_dataview_block` signature is unchanged in this task (Plan 02 extends it for per-note-type queries)
    - `BlockContext` (dataclass or NamedTuple) carries: `graph: nx.Graph`, `node_id: str`, `edges: list[dict]` (each dict has the six conn fields populated), `dataview_nonempty: bool`
    - `load_templates` and `_load_override_template` (templates.py L207, L713) ALWAYS wrap in `_BlockTemplate` (RESEARCH Open Question 1 recommendation: `_BlockTemplate`'s idpattern is a strict superset; back-compat by construction)
  </behavior>
  <action>
    Modify `graphify/templates.py`:

    1. Near the top of the module (after `_FALLBACK_MOC_QUERY` definition around L521 — BEFORE `_build_dataview_block` at L525), add:

    ```python
    class _BlockTemplate(string.Template):
        """string.Template subclass with one-segment dot-extended idpattern.

        Per CPython, ``string.Template.__init_subclass__`` recompiles ``pattern``
        when a subclass overrides ``idpattern``. Do NOT override ``pattern``.
        """
        idpattern = r"(?a:[_a-z][_a-z0-9]*(?:\.[_a-z][_a-z0-9]*)?)"


    _CONN_FIELDS: frozenset[str] = frozenset(
        {"label", "relation", "target", "confidence", "community", "source_file"}
    )

    _IF_ATTR_RE = re.compile(r"^if_attr_([a-z_][a-z0-9_]*)$")


    @dataclasses.dataclass(frozen=True)
    class BlockContext:
        graph: "nx.Graph"
        node_id: str
        edges: list[dict]
        dataview_nonempty: bool


    def _pred_god_node(ctx: BlockContext) -> bool:
        # god_nodes are computed by analyze.py and stored on the graph
        # (see analyze.py — node attribute 'is_god_node' OR membership in graph.graph.get('god_nodes', []))
        node = ctx.graph.nodes.get(ctx.node_id, {})
        if node.get("is_god_node"):
            return True
        return ctx.node_id in (ctx.graph.graph.get("god_nodes") or [])


    def _pred_isolated(ctx: BlockContext) -> bool:
        return ctx.graph.degree(ctx.node_id) == 0 if ctx.node_id in ctx.graph else False


    def _pred_has_connections(ctx: BlockContext) -> bool:
        return not _pred_isolated(ctx)


    def _pred_has_dataview(ctx: BlockContext) -> bool:
        return ctx.dataview_nonempty


    _PREDICATE_CATALOG: dict[str, "Callable[[BlockContext], bool]"] = {
        "if_god_node": _pred_god_node,
        "if_isolated": _pred_isolated,
        "if_has_connections": _pred_has_connections,
        "if_has_dataview": _pred_has_dataview,
    }


    def _eval_predicate(name: str, ctx: BlockContext) -> bool:
        if name in _PREDICATE_CATALOG:
            return _PREDICATE_CATALOG[name](ctx)
        m = _IF_ATTR_RE.match(name)
        if m:
            attr = m.group(1)
            node = ctx.graph.nodes.get(ctx.node_id, {})
            return bool(node.get(attr))
        raise KeyError(name)
    ```

    2. Add `_expand_blocks(text: str, ctx: BlockContext) -> str` as a single-pass FSM. Use `re.compile(r"\{\{#([a-z_][a-z0-9_]*)\}\}")` for openers and `r"\{\{/(if|connections)\}\}"` for closers. Walk the source linearly, maintain ONE level of block depth (any opener while depth>0 is a defensive `ValueError` since preflight should reject), expand each block into pure text. For `{{#connections}}` iteration, render both `${conn.<field>}` and `${conn_<field>}` substituted values per iteration, sanitizing each value via `_sanitize_wikilink_alias`.

    3. Extend `validate_template` (L134) to:
       - Detect `{{#…}}…{{/…}}` block syntax with the same regex pair above
       - Reject nested blocks with EXACT message (D-08): `"validate_template: nested template blocks are not supported (found '{{#<inner>}}' inside '{{#<outer>}}'). Flatten the template or pre-compute the predicate."` — substitute the actual inner/outer names captured during parsing
       - Reject unclosed `{{#…}}` with: `"validate_template: unclosed block '{{#<name>}}'"`
       - Reject mismatched closers (`{{/if}}` after `{{#connections}}` etc.) with: `"validate_template: block mismatch — '{{#<opener>}}' closed by '{{/<closer>}}'"`
       - Reject unknown predicate names with: `f"validate_template: unknown predicate '{{#{name}}}' — known: {sorted(_PREDICATE_CATALOG)} (or use {{#if_attr_<name>}} for raw node attributes)"`
       - Reject `{{#connections}}` references to unknown `conn.<field>` with: `f"validate_template: unknown connection field 'conn.{field}' — valid: {sorted(_CONN_FIELDS)}"`

    4. Modify `load_templates` (L207) and `_load_override_template` (L713) to wrap user-authored template text in `_BlockTemplate(text)` instead of `string.Template(text)`.

    5. Update `_load_builtin_template` (L195) the same way for symmetry.

    6. In `render_note` (L570), `_render_moc_like` (L798), `render_moc` (L917), `render_community_overview` (L938): construct a `BlockContext` from the graph + node_id + iterated edges + `bool(dataview_block.strip())`, call `_expand_blocks(template_source, ctx)` on the **template source string** BEFORE wrapping in `_BlockTemplate` for `safe_substitute`. (Plan 02 will additionally pass `note_type` to `_build_dataview_block`.)

    7. Imports at top of file: ensure `import dataclasses`, `import re` (already present), and `from typing import Callable, TYPE_CHECKING`. Add `if TYPE_CHECKING: import networkx as nx` if not already imported normally.

    Per D-15: every `conn.<field>` value passes through `_sanitize_wikilink_alias` BEFORE being inserted into the expanded loop body. Per D-16: block expansion produces a pure-text result; `safe_substitute` runs on that result.

    Use `_BlockTemplate` for render-time `safe_substitute` (Open Question 1: always, not conditionally — its idpattern is a strict superset).
  </action>
  <verify>
    <automated>cd /Users/silveimar/Documents/silogia-repos/graphify && pytest tests/test_templates.py -q -x 2>&1 | tail -20</automated>
  </verify>
  <acceptance_criteria>
    - `grep -nE "^class _BlockTemplate\(string\.Template\):" graphify/templates.py` returns exactly one match
    - `grep -n 'idpattern = r"(?a:\[_a-z\]\[_a-z0-9\]\*\(\?:\\\.\[_a-z\]\[_a-z0-9\]\*\)\?\)"' graphify/templates.py` returns exactly one match (the verbatim regex from RESEARCH L353/L434)
    - `grep -nE "^_PREDICATE_CATALOG: dict" graphify/templates.py` returns exactly one match
    - `python3 -c "from graphify.templates import _PREDICATE_CATALOG; print(sorted(_PREDICATE_CATALOG))"` outputs `['if_god_node', 'if_has_connections', 'if_has_dataview', 'if_isolated']` (exactly four, alphabetized)
    - `python3 -c "from graphify.templates import _CONN_FIELDS; print(sorted(_CONN_FIELDS))"` outputs `['community', 'confidence', 'label', 'relation', 'source_file', 'target']` (exactly six)
    - `grep -c "_expand_blocks" graphify/templates.py` returns ≥ 5 (definition + 4 render entry-point call sites)
    - `grep -nE "load_templates|_load_override_template|_load_builtin_template" graphify/templates.py | xargs -I{} echo {} | grep -c "_BlockTemplate"` ≥ 3 (all three loaders wrap in _BlockTemplate)
    - `pytest tests/test_templates.py -q -x` exits 0
    - No new required dependency added to `pyproject.toml` (PyYAML stays optional)
  </acceptance_criteria>
  <done>
    `_BlockTemplate`, `_PREDICATE_CATALOG` (exactly 4), `_CONN_FIELDS` (exactly 6), `_IF_ATTR_RE`, `BlockContext`, `_eval_predicate`, `_expand_blocks`, and the extended `validate_template` exist in `graphify/templates.py`. All four render entry points (`render_note`, `_render_moc_like`, `render_moc`, `render_community_overview`) call `_expand_blocks` BEFORE `safe_substitute`. All template loaders wrap text in `_BlockTemplate`. Existing test suite still passes (no regressions).
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Test fixtures + behavior tests for TMPL-01, TMPL-02, and sanitization (T-31-01)</name>
  <files>tests/test_templates.py, tests/fixtures/template_context.py, tests/fixtures/templates/with_if_block.md, tests/fixtures/templates/with_connections_block.md, tests/fixtures/templates/with_if_attr.md, tests/fixtures/templates/nested_blocks_invalid.md</files>
  <read_first>
    - tests/test_templates.py (existing patterns — `tmp_path`, NetworkX graph construction, `validate_template` invocation style)
    - tests/fixtures/template_context.py (existing graph fixture helpers — extend, do not replace)
    - .planning/phases/31-template-engine-extensions/31-VALIDATION.md (test categories: predicate truth/false, loop iteration, nested rejection, unknown predicate rejection, sanitization triplet)
    - .planning/phases/31-template-engine-extensions/31-PATTERNS.md (test analog excerpts)
    - graphify/templates.py (the symbols added in Task 1)
  </read_first>
  <behavior>
    Tests in `tests/test_templates.py` (extend the existing module; do not create a new file):

    **TMPL-01 conditional tests:**
    - `test_if_god_node_true_renders` — graph with node marked `is_god_node=True`; template `"X{{#if_god_node}}GOD{{/if}}Y"` renders `"XGODY"`
    - `test_if_god_node_false_omits_cleanly` — non-god node renders `"XY"` (no leftover whitespace gap; verify no double-newline if block was on its own line)
    - `test_if_isolated_true_renders` — isolated node (degree 0) renders the guarded section
    - `test_if_has_connections_renders_when_edges_exist` — node with ≥1 edge renders
    - `test_if_has_dataview_branches_on_dataview_nonempty` — `BlockContext.dataview_nonempty=False` omits, `True` renders
    - `test_if_attr_escape_hatch_reads_node_attribute` — node attr `is_published=True` → `{{#if_attr_is_published}}…{{/if}}` renders
    - `test_if_attr_falsy_value_omits` — node attr `is_published=False` → omits; missing attr → omits

    **TMPL-02 connection loop tests:**
    - `test_connections_loop_iterates` — template `"{{#connections}}- ${conn.label} (${conn.relation})\n{{/connections}}"` over a node with 3 edges produces 3 list items in correct order
    - `test_connections_loop_exposes_all_six_fields` — verify `${conn.label}`, `${conn.relation}`, `${conn.target}`, `${conn.confidence}`, `${conn.community}`, `${conn.source_file}` all substitute correctly
    - `test_connections_loop_flattened_form_works` — same template using `${conn_label}` (underscore, not dot) renders identically
    - `test_connections_target_renders_label_not_id` — per D-06, `${conn.target}` shows the target node's `label` attribute, not the raw node id
    - `test_connections_confidence_renders_string` — per D-06, value is the literal string `"EXTRACTED"` / `"INFERRED"` / `"AMBIGUOUS"`
    - `test_connections_empty_loop_renders_nothing` — node with no edges → block expands to empty string

    **Preflight rejection tests:**
    - `test_nested_blocks_rejected_with_specific_error` — template with `{{#connections}}…{{#if_god_node}}…{{/if}}…{{/connections}}` returns the EXACT D-08 error string from `validate_template`
    - `test_unknown_predicate_rejected` — `{{#if_foobar}}` returns error containing `"unknown predicate"` and listing catalog members
    - `test_unclosed_block_rejected` — `{{#if_god_node}}…` (no `{{/if}}`) returns error containing `"unclosed block"`
    - `test_mismatched_closer_rejected` — `{{#connections}}…{{/if}}` returns error containing `"block mismatch"`
    - `test_unknown_conn_field_rejected` — `{{#connections}}${conn.bogus}{{/connections}}` returns error containing `"unknown connection field 'conn.bogus'"`

    **Sanitization tests (T-31-01 — MANDATORY for success criterion 4):**
    - `test_label_injection_double_brace` — node label `"evil {{#if_god_node}}HIDDEN{{/if}}"` rendered through `{{#connections}}${conn.label}{{/connections}}` produces output where the literal `{{` is sanitized (NOT interpreted as a block opener)
    - `test_label_injection_block_opener` — node label `"{{#connections}}fake{{/connections}}"` cannot smuggle a fake nested loop into the rendered output (block expansion runs BEFORE substitution per D-16; verify the literal sequence does not appear in expanded result, OR appears as escaped/sanitized text)
    - `test_label_injection_dataview_fence` — node label containing backticks/newlines does not escape a Dataview fence in the rendered loop body (verify `_sanitize_wikilink_alias` strips them)

    Also extend `tests/fixtures/template_context.py` with helper builders:
    - `make_block_context(graph, node_id, *, dataview_nonempty=False)` returning a `BlockContext`
    - `make_graph_with_god_node(node_id="god1")`, `make_graph_with_isolated(node_id="iso1")`, `make_graph_with_edges(node_id, edge_specs)` where each edge_spec is a dict matching the six conn fields

    Fixture template files in `tests/fixtures/templates/` for end-to-end render tests:
    - `with_if_block.md` — minimal frontmatter + `{{#if_god_node}}This is a god node.{{/if}}` body
    - `with_connections_block.md` — frontmatter + `## Connections\n{{#connections}}- [[${conn.target}]] — ${conn.relation}\n{{/connections}}`
    - `with_if_attr.md` — `{{#if_attr_is_published}}Published{{/if}}`
    - `nested_blocks_invalid.md` — `{{#connections}}{{#if_god_node}}X{{/if}}{{/connections}}` (used by the negative test)
  </behavior>
  <action>
    1. Create the four fixture template files under `tests/fixtures/templates/` with the exact body content listed above (each must include minimal frontmatter `---\nid: ${id}\n---\n` to satisfy any pre-existing required-field check in `validate_template` — inspect existing tests for the required set and align).

    2. Extend `tests/fixtures/template_context.py` with the four helper builders described in `<behavior>`. Reuse existing helpers if they cover the same ground; do not duplicate.

    3. Add the test functions described in `<behavior>` to `tests/test_templates.py`. Group them with `# --- TMPL-01: conditional blocks ---`, `# --- TMPL-02: connection loops ---`, `# --- preflight rejection ---`, `# --- T-31-01: label injection sanitization ---` section comments for greppability.

    4. Pure unit tests only — `tmp_path`, no network, no filesystem outside `tmp_path`. Build `nx.Graph` instances inline in each test or via the new fixture helpers. Do not call `render_note` end-to-end unless asserting full-pipeline behavior; for predicate/loop unit tests, call `_expand_blocks(text, ctx)` directly.

    5. For the D-08 nested-block test: assert the error string equality is EXACT (use `assert errs == ["validate_template: nested template blocks are not supported (found '{{#if_god_node}}' inside '{{#connections}}'). Flatten the template or pre-compute the predicate."]`). This is the success-criterion-2 acceptance gate.
  </action>
  <verify>
    <automated>cd /Users/silveimar/Documents/silogia-repos/graphify && pytest tests/test_templates.py -q -x 2>&1 | tail -30</automated>
  </verify>
  <acceptance_criteria>
    - `pytest tests/test_templates.py -q -x` exits 0
    - `pytest tests/test_templates.py -q -k "if_god_node or if_isolated or if_has_connections or if_has_dataview or if_attr"` collects ≥ 7 tests and all pass
    - `pytest tests/test_templates.py -q -k "connections"` collects ≥ 6 tests and all pass
    - `pytest tests/test_templates.py -q -k "nested or unknown or unclosed or mismatched"` collects ≥ 5 tests and all pass
    - `pytest tests/test_templates.py -q -k "label_injection"` collects exactly 3 tests and all pass
    - `grep -c "test_label_injection_" tests/test_templates.py` returns exactly 3
    - `grep -c "test_if_" tests/test_templates.py` returns ≥ 7
    - `grep -c "test_connections_" tests/test_templates.py` returns ≥ 6
    - `ls tests/fixtures/templates/*.md | wc -l` returns ≥ 4
    - `grep -F "validate_template: nested template blocks are not supported (found '{{#if_god_node}}' inside '{{#connections}}'). Flatten the template or pre-compute the predicate." tests/test_templates.py | wc -l` returns ≥ 1 (verbatim D-08 message asserted)
    - All tests confined to `tmp_path` — `grep -nE "open\(|Path\(" tests/test_templates.py | grep -v "tmp_path\|fixtures" | wc -l` returns 0
  </acceptance_criteria>
  <done>
    Test suite covers TMPL-01 (predicate truth/false table for all four catalog members + `if_attr_` escape hatch), TMPL-02 (loop iteration with all six fields + flat parallel form + empty case), preflight rejection (nested D-08 verbatim, unknown predicate, unclosed, mismatched, unknown conn field), and T-31-01 sanitization triplet (double-brace, block opener, Dataview fence). All tests pass under Python 3.10+ with no network or non-tmp_path filesystem access.
  </done>
</task>

</tasks>

<verification>
- `pytest tests/test_templates.py -q -x` passes (existing suite + new TMPL-01/02/sanitization tests).
- `pytest tests/ -q` passes (no regressions in unrelated suites).
- `python3 -c "from graphify.templates import _BlockTemplate, _PREDICATE_CATALOG, _CONN_FIELDS, _expand_blocks, BlockContext"` succeeds.
- `python3 -c "from graphify.templates import _PREDICATE_CATALOG; assert sorted(_PREDICATE_CATALOG) == ['if_god_node','if_has_connections','if_has_dataview','if_isolated']"` succeeds.
- `pip show pyyaml` is still optional (no entry added to `dependencies` in `pyproject.toml`).
</verification>

<success_criteria>
- TMPL-01: Conditional sections render guarded body iff predicate true; absence is clean (no whitespace artifacts); preflight rejects unknown predicate names with helpful catalog listing.
- TMPL-02: Connection loop iterates per edge with all six namespaced fields populated; both `${conn.X}` and `${conn_X}` forms render identically; empty edge set produces empty output.
- Preflight: Nested blocks rejected with verbatim D-08 message; unclosed/mismatched/unknown-field cases produce specific actionable errors.
- Sanitization (T-31-01): Node labels containing `{{`, `}}`, `#`, `${`, backticks, or newlines cannot inject conditional logic, smuggle fake loops, or escape Dataview fences. Block expansion runs BEFORE scalar substitution (D-16 ordering invariant verified by tests).
- No new required dependencies; PyYAML stays optional; Python 3.10+ compatible.
</success_criteria>

<output>
After completion, create `.planning/phases/31-template-engine-extensions/31-01-SUMMARY.md` summarizing: symbols added/modified in `templates.py` with line refs, test counts per category, the verbatim D-08 message asserted, the four predicate catalog members (frozen), the six conn fields (frozen), and confirmation that block expansion order (D-16) is enforced by test.
</output>
