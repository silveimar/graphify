---
phase: 31-template-engine-extensions
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - graphify/templates.py
  - tests/test_templates.py
  - tests/fixtures/template_context.py
  - tests/fixtures/templates/with_if_block.md
  - tests/fixtures/templates/with_connections_block.md
  - tests/fixtures/templates/with_if_attr.md
  - tests/fixtures/templates/nested_blocks_invalid.md
autonomous: true
requirements:
  - TMPL-01
  - TMPL-02
tags: [templates, conditionals, loops, sanitization, string-template]

must_haves:
  truths:
    - "Template containing {{#if_god_node}}…{{/if}} renders the guarded section when predicate is true and omits it (with no leftover whitespace gaps) when predicate is false"
    - "Template containing {{#connections}}…{{/connections}} iterates each edge with namespaced ${conn.label}, ${conn.relation}, ${conn.target}, ${conn.confidence}, ${conn.community}, ${conn.source_file} (and parallel flat ${conn_label} forms) sorted by (relation, label) ascending"
    - "validate_template rejects nested blocks at preflight time with the exact actionable message from D-08"
    - "validate_template rejects unknown predicate names (not in catalog and not prefixed with if_attr_) at preflight time"
    - "Node labels containing '{{', '}}', '#', '${', backticks, or newlines cannot inject conditional logic, smuggle a fake loop, or escape Dataview fences (block expansion runs BEFORE scalar substitution per D-16)"
    - "A block-free template renders byte-identical to a stock string.Template.safe_substitute pass over the same input (backward compatibility gate per ROADMAP criterion 4 / D-16 / TMPL-01)"
    - "Each of the four render entry points (render_note, _render_moc_like, render_moc, render_community_overview) invokes _expand_blocks before safe_substitute (no forgotten call site)"
    - "_expand_blocks does NOT re-validate or re-parse blocks; it consumes a vetted template and never raises a syntax error for a template that passed validate_template (D-09/D-10 preflight-only invariant)"
  artifacts:
    - path: "graphify/templates.py"
      provides: "_BlockTemplate subclass, _PREDICATE_CATALOG, _IF_ATTR_RE, _CONN_FIELDS, _build_edge_records, block pre-processor, extended validate_template"
      contains: "_BlockTemplate"
    - path: "graphify/templates.py"
      provides: "predicate catalog with exactly four members"
      contains: "_PREDICATE_CATALOG"
    - path: "graphify/templates.py"
      provides: "_build_edge_records helper with explicit field provenance and (relation, label) ascending sort"
      contains: "_build_edge_records"
    - path: "tests/test_templates.py"
      provides: "TMPL-01/TMPL-02/sanitization/byte-identical/render-entry-point/ordering test coverage"
      contains: "test_if_god_node_true_renders"
  key_links:
    - from: "graphify/templates.py::render_note"
      to: "graphify/templates.py::_expand_blocks (new)"
      via: "called BEFORE safe_substitute (D-16 ordering invariant)"
      pattern: "_expand_blocks"
    - from: "graphify/templates.py::_expand_blocks"
      to: "graphify/templates.py::_sanitize_wikilink_alias"
      via: "loop iteration values pass through alias sanitizer (D-15)"
      pattern: "_sanitize_wikilink_alias"
    - from: "graphify/templates.py::_BlockTemplate"
      to: "string.Template"
      via: "subclass with idpattern only (no pattern override — D-05 / RESEARCH §Pitfall 3)"
      pattern: "class _BlockTemplate\\(string\\.Template\\)"
    - from: "graphify/templates.py::_expand_blocks"
      to: "graphify/templates.py::_build_edge_records"
      via: "edge iteration sorted by (relation, label) ascending (RESEARCH OQ3 / VALIDATION ordering)"
      pattern: "_build_edge_records"
---

<objective>
Add `{{#if_X}}…{{/if}}` conditional sections (TMPL-01) and `{{#connections}}…{{/connections}}` iteration blocks (TMPL-02) to graphify's `string.Template`-based template engine, with sanitization hardening that prevents node-label-driven injection.

Purpose: Templates must support conditional rendering against node attributes/classification and per-edge iteration with namespaced scope, without leaving stdlib (no Jinja2). All block parsing happens preflight in `validate_template`; all expansion happens BEFORE `safe_substitute` so labels containing `{{`, `}}`, `#`, backticks, or newlines cannot inject syntax. Block-free templates remain byte-identical to today.

Output: Extended `graphify/templates.py` with `_BlockTemplate` subclass, `_PREDICATE_CATALOG` (exactly four entries), `_build_edge_records` helper, block pre-processor, hardened `validate_template`, and a comprehensive test suite covering both features, sanitization threat surface, render-entry-point integration, deterministic loop ordering, and byte-identical backward compatibility.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/31-template-engine-extensions/31-CONTEXT.md
@.planning/phases/31-template-engine-extensions/31-RESEARCH.md
@.planning/phases/31-template-engine-extensions/31-PATTERNS.md
@.planning/phases/31-template-engine-extensions/31-VALIDATION.md
@graphify/templates.py
@graphify/analyze.py
@tests/test_templates.py
@tests/fixtures/template_context.py

<interfaces>
<!-- Key surfaces in templates.py the executor must extend (verbatim from grep). -->

graphify/templates.py:
  L134  def validate_template(text: str, required: set[str]) -> list[str]:
  L195  def _load_builtin_template(...)
  L207  def load_templates(...)
  L284  def _sanitize_wikilink_alias(label: str) -> str:
  L388  _build_connections_callout (sanitization reference)
  L525  def _build_dataview_block(profile: dict, community_tag: str, folder: str) -> str:
  L570  render_note(node_id, G, profile, note_type, classification_context, *, vault_dir=None, created=None)
  L713  _load_override_template
  L798  _render_moc_like(community_id, G, communities, profile, classification_context, template_key, vault_dir, created=None)
       # NOTE: `template_key` is already "moc" or "community" — Plan 02 uses this verbatim as note_type
  L917  render_moc       — calls _render_moc_like with template_key="moc"
  L938  render_community_overview — calls _render_moc_like with template_key="community"

graphify/analyze.py:
  - god-node ranking (existing) — source of truth for `if_god_node`
  - isolated-node detection (existing) — source of truth for `if_isolated`

EXACT idpattern (RESEARCH.md L353, L434):
  idpattern = r"(?a:[_a-z][_a-z0-9]*(?:\.[_a-z][_a-z0-9]*)?)"

EXACT predicate catalog (D-02, locked, exactly four):
  if_god_node, if_isolated, if_has_connections, if_has_dataview

EXACT loop variables (D-04, locked, full set):
  conn.label, conn.relation, conn.target, conn.confidence, conn.community, conn.source_file
  (also emit parallel flat: conn_label, conn_relation, conn_target, conn_confidence, conn_community, conn_source_file)

EXACT block syntax (locked by ROADMAP success criteria 1 & 2):
  {{#if_X}} ... {{/if}}
  {{#if_attr_<name>}} ... {{/if}}
  {{#connections}} ... {{/connections}}

EXACT loop iteration order (RESEARCH OQ3 / VALIDATION):
  sorted by (relation ASC, label ASC) — produced by _build_edge_records

EXACT nested-block error message (D-08, verbatim):
  "validate_template: nested template blocks are not supported (found '{{#if_god_node}}' inside '{{#connections}}'). Flatten the template or pre-compute the predicate."
</interfaces>
</context>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| graph-data → template render | Node labels, relations, source_files (untrusted user content from extracted code/docs) flow into template substitution values |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-31-01 | Tampering / Elevation | `_expand_blocks` in templates.py | mitigate | Block expansion runs BEFORE `safe_substitute` (D-16); per-iteration `conn.label`/`conn.target`/`conn.community`/`conn.source_file` pass through `_sanitize_wikilink_alias` (templates.py L284); tests `test_label_injection_double_brace_open`, `test_label_injection_double_brace_close`, `test_label_injection_block_opener`, `test_label_injection_dollar_brace`, `test_label_injection_backtick`, `test_label_injection_newline`, plus `test_block_expansion_runs_before_substitution` cover the threat |
| T-31-03 | DoS | `_expand_blocks` regex / state machine | mitigate | Block parser is a single-pass FSM (no recursive regex); `validate_template` rejects nested blocks at preflight (D-07/D-08), bounding expansion depth to 1 |
</threat_model>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add _BlockTemplate subclass, predicate catalog, _build_edge_records, and block pre-processor to templates.py</name>
  <files>graphify/templates.py</files>
  <read_first>
    - graphify/templates.py (full module — must understand existing render_note / _render_moc_like / _build_connections_callout / _build_dataview_block / _sanitize_wikilink_alias surfaces; note that `_render_moc_like` already has parameter `template_key: str` carrying "moc" or "community" — reuse, do not invent a new parameter)
    - .planning/phases/31-template-engine-extensions/31-RESEARCH.md (L139–L175 architecture; L347–L370 idpattern pitfalls; L415–L495 code examples for _BlockTemplate, _PREDICATE_CATALOG, _eval_predicate; L561–L590 _build_dataview_block extension; OQ3 ordering)
    - .planning/phases/31-template-engine-extensions/31-CONTEXT.md (D-01 through D-16, all locked)
    - graphify/analyze.py (god-node ranking + isolated-node detection — predicate truth sources)
  </read_first>
  <behavior>
    - `_BlockTemplate(string.Template)` subclass exists with ONLY `idpattern = r"(?a:[_a-z][_a-z0-9]*(?:\.[_a-z][_a-z0-9]*)?)"` overridden (no `pattern` override — RESEARCH §Pitfall 3 forbids it; CPython `__init_subclass__` recompiles `pattern` when `idpattern` is set on the subclass)
    - `_PREDICATE_CATALOG` is a module-level `dict[str, Callable[[BlockContext], bool]]` with exactly four entries (D-02): `if_god_node`, `if_isolated`, `if_has_connections`, `if_has_dataview`. No more, no fewer. The catalog keys carry the `if_` prefix (RESEARCH L173).
    - `_IF_ATTR_RE` matches `if_attr_<name>` where `<name>` is `[a-z_][a-z0-9_]*` (D-01/D-03)
    - `_CONN_FIELDS` is a frozenset of exactly six members: `{"label","relation","target","confidence","community","source_file"}` (D-04)
    - `_build_edge_records(graph, node_id) -> list[dict]` is a pure helper that returns a deterministic sorted list of edge records, each populated from explicit node/edge attribute sources (NetworkX edge data does NOT carry `label`/`community`/`source_file` reliably; those must be sourced from the TARGET node's attributes for label/community, and from edge data for relation/confidence/source_file). Sort key: `(relation ASC, label ASC)`.
    - `_expand_blocks(text, ctx)` is a pure function that performs a single-pass FSM over the template source, expanding `{{#…}}…{{/…}}` blocks. It must:
      1. Run BEFORE any `safe_substitute` call (D-16 ordering invariant)
      2. For `{{#if_X}}…{{/if}}`: dispatch to `_PREDICATE_CATALOG[name](ctx)`, then to `_IF_ATTR_RE` for `if_attr_<name>` (reads `G.nodes[node_id].get(<name>)` truthiness per D-03)
      3. For `{{#connections}}…{{/connections}}`: iterate `ctx.edges` (already sorted by `_build_edge_records`), and emit BOTH `${conn.<field>}` and `${conn_<field>}` substituted forms in the expanded body so stock `string.Template` and `_BlockTemplate` both render correctly (D-05). Each iteration value is already pre-sanitized by `_build_edge_records` via `_sanitize_wikilink_alias` (D-06/D-15).
      4. Be **preflight-only-trusting** (D-09/D-10): `_expand_blocks` consumes a vetted template (one already passing `validate_template`). It does NOT re-validate, does NOT re-parse for syntax errors, and does NOT surface block-syntax error messages. The single defensive `ValueError` for nested openers (depth > 0) is internal-invariant only and unreachable on valid input.
    - `_eval_predicate(name, ctx)` dispatches: catalog hit → call; `if_attr_<name>` match → `bool(G.nodes[ctx.node_id].get(<name>))`; otherwise → raise `KeyError(name)` (preflight should have caught this — `_expand_blocks` re-raises as `ValueError` for render-path safety only as a defensive invariant)
    - `_build_dataview_block` signature is unchanged in this task (Plan 02 extends it for per-note-type queries — Plan 02 is gated to wave 2)
    - `BlockContext` (dataclass) carries: `graph: nx.Graph`, `node_id: str`, `edges: list[dict]` (each dict has the six conn fields populated from `_build_edge_records`), `dataview_nonempty: bool`
    - `load_templates`, `_load_override_template`, and `_load_builtin_template` ALWAYS wrap in `_BlockTemplate` (RESEARCH Open Question 1 recommendation: `_BlockTemplate`'s idpattern is a strict superset; back-compat by construction)
  </behavior>
  <action>
    Modify `graphify/templates.py`:

    1. Near the top of the module (after `_FALLBACK_MOC_QUERY` definition around L521 — BEFORE `_build_dataview_block` at L525), add:

    ```python
    class _BlockTemplate(string.Template):
        """string.Template subclass with one-segment dot-extended idpattern.

        Per CPython, ``string.Template.__init_subclass__`` recompiles ``pattern``
        when a subclass overrides ``idpattern``. Do NOT override ``pattern``.
        """
        idpattern = r"(?a:[_a-z][_a-z0-9]*(?:\.[_a-z][_a-z0-9]*)?)"


    _CONN_FIELDS: frozenset[str] = frozenset(
        {"label", "relation", "target", "confidence", "community", "source_file"}
    )

    _IF_ATTR_RE = re.compile(r"^if_attr_([a-z_][a-z0-9_]*)$")


    @dataclasses.dataclass(frozen=True)
    class BlockContext:
        graph: "nx.Graph"
        node_id: str
        edges: list[dict]
        dataview_nonempty: bool


    def _pred_god_node(ctx: BlockContext) -> bool:
        node = ctx.graph.nodes.get(ctx.node_id, {})
        if node.get("is_god_node"):
            return True
        return ctx.node_id in (ctx.graph.graph.get("god_nodes") or [])


    def _pred_isolated(ctx: BlockContext) -> bool:
        return ctx.graph.degree(ctx.node_id) == 0 if ctx.node_id in ctx.graph else False


    def _pred_has_connections(ctx: BlockContext) -> bool:
        return not _pred_isolated(ctx)


    def _pred_has_dataview(ctx: BlockContext) -> bool:
        return ctx.dataview_nonempty


    _PREDICATE_CATALOG: dict[str, "Callable[[BlockContext], bool]"] = {
        "if_god_node": _pred_god_node,
        "if_isolated": _pred_isolated,
        "if_has_connections": _pred_has_connections,
        "if_has_dataview": _pred_has_dataview,
    }


    def _eval_predicate(name: str, ctx: BlockContext) -> bool:
        if name in _PREDICATE_CATALOG:
            return _PREDICATE_CATALOG[name](ctx)
        m = _IF_ATTR_RE.match(name)
        if m:
            attr = m.group(1)
            node = ctx.graph.nodes.get(ctx.node_id, {})
            return bool(node.get(attr))
        raise KeyError(name)


    def _build_edge_records(graph, node_id) -> list[dict]:
        """Build deterministic, sanitized edge records for {{#connections}} loops.

        Field provenance (NetworkX edge data does NOT carry label/community/source_file
        reliably — those must be sourced from the target node's attributes):
          - label:        target node attr 'label' (sanitized via _sanitize_wikilink_alias) — fallback to target node id
          - relation:     edge data 'relation' (default '')
          - target:       D-06 — target node 'label' (sanitized), NOT raw node id
          - confidence:   edge data 'confidence' (default '')
          - community:    target node attr 'community' (str-coerced, default '')
          - source_file:  edge data 'source_file' (default '')

        Sort: (relation ASC, label ASC) — RESEARCH OQ3 / VALIDATION ordering invariant.
        """
        records = []
        if node_id not in graph:
            return records
        for u, v, data in graph.edges(node_id, data=True):
            target_node = graph.nodes[v]
            label = _sanitize_wikilink_alias(target_node.get("label", v))
            target = _sanitize_wikilink_alias(target_node.get("label", v))  # D-06
            records.append({
                "label": label,
                "relation": data.get("relation", ""),
                "target": target,
                "confidence": data.get("confidence", ""),
                "community": str(target_node.get("community", "")),
                "source_file": data.get("source_file", ""),
            })
        return sorted(records, key=lambda e: (e["relation"], e["label"]))
    ```

    2. Add `_expand_blocks(text: str, ctx: BlockContext) -> str` as a single-pass FSM. Use `re.compile(r"\{\{#([a-z_][a-z0-9_]*)\}\}")` for openers and `r"\{\{/(if|connections)\}\}"` for closers. Walk the source linearly, maintain ONE level of block depth (any opener while depth>0 is a defensive `ValueError` per D-09/D-10 — unreachable on preflight-vetted input), expand each block into pure text.

    For `{{#connections}}` iteration: iterate `ctx.edges` (already sorted via `_build_edge_records`); for each edge dict, render the block body by substituting both `${conn.<field>}` and `${conn_<field>}` forms with the (already-sanitized) iteration values. Concatenate the per-iteration results to produce the expanded loop body.

    3. Extend `validate_template` (L134) to:
       - Detect `{{#…}}…{{/…}}` block syntax with the same regex pair above
       - Reject nested blocks with EXACT message (D-08): `"validate_template: nested template blocks are not supported (found '{{#<inner>}}' inside '{{#<outer>}}'). Flatten the template or pre-compute the predicate."` — substitute the actual inner/outer names captured during parsing
       - Reject unclosed `{{#…}}` with: `"validate_template: unclosed block '{{#<name>}}'"`
       - Reject mismatched closers (`{{/if}}` after `{{#connections}}` etc.) with: `"validate_template: block mismatch — '{{#<opener>}}' closed by '{{/<closer>}}'"`
       - Reject unknown predicate names with: `f"validate_template: unknown predicate '{{#{name}}}' — known: {sorted(_PREDICATE_CATALOG)} (or use {{#if_attr_<name>}} for raw node attributes)"`
       - Reject `{{#connections}}` references to unknown `conn.<field>` with: `f"validate_template: unknown connection field 'conn.{field}' — valid: {sorted(_CONN_FIELDS)}"`

    4. Modify `load_templates` (L207) and `_load_override_template` (L713) to wrap user-authored template text in `_BlockTemplate(text)` instead of `string.Template(text)`. Update `_load_builtin_template` (L195) the same way for symmetry.

    5. In `render_note` (L570), `_render_moc_like` (L798), `render_moc` (L917), `render_community_overview` (L938): construct a `BlockContext` from `graph` + `node_id` + `_build_edge_records(graph, node_id)` + `bool(dataview_block.strip())`, and call `_expand_blocks(template_source, ctx)` on the **template source string** BEFORE wrapping in `_BlockTemplate` for `safe_substitute`.

       For `_render_moc_like`, `render_moc`, `render_community_overview` (which operate on a community, not a single node), construct `BlockContext` with a synthetic `node_id` representing the community context (e.g., the community-MOC label) and pass `edges=[]` — `{{#connections}}` blocks in MOC templates are out of scope for these entry points but the wrapper must still call `_expand_blocks` so block-free MOC templates render unchanged AND any `{{#if_*}}` blocks evaluate correctly. (Plan 02 will additionally pass `note_type`/`template_key` to `_build_dataview_block`.)

    6. Imports at top of file: ensure `import dataclasses`, `import re` (already present), and `from typing import Callable, TYPE_CHECKING`. Add `if TYPE_CHECKING: import networkx as nx` if not already imported normally.

    Per D-15: every `conn.<field>` value is sanitized inside `_build_edge_records` via `_sanitize_wikilink_alias` BEFORE being inserted into the expanded loop body. Per D-16: block expansion produces a pure-text result; `safe_substitute` runs on that result.

    Use `_BlockTemplate` for render-time `safe_substitute` (Open Question 1: always, not conditionally — its idpattern is a strict superset).
  </action>
  <verify>
    <automated>cd /Users/silveimar/Documents/silogia-repos/graphify && pytest tests/test_templates.py -q -x 2>&1 | tail -20</automated>
  </verify>
  <acceptance_criteria>
    - `grep -nE "^class _BlockTemplate\(string\.Template\):" graphify/templates.py` returns exactly one match
    - `grep -n 'idpattern = r"(?a:\[_a-z\]\[_a-z0-9\]\*\(\?:\\\.\[_a-z\]\[_a-z0-9\]\*\)\?\)"' graphify/templates.py` returns exactly one match (verbatim regex from RESEARCH L353/L434)
    - `grep -nE "^_PREDICATE_CATALOG: dict" graphify/templates.py` returns exactly one match
    - `python3 -c "from graphify.templates import _PREDICATE_CATALOG; print(sorted(_PREDICATE_CATALOG))"` outputs `['if_god_node', 'if_has_connections', 'if_has_dataview', 'if_isolated']` (exactly four, alphabetized)
    - `python3 -c "from graphify.templates import _CONN_FIELDS; print(sorted(_CONN_FIELDS))"` outputs `['community', 'confidence', 'label', 'relation', 'source_file', 'target']` (exactly six)
    - `grep -nE "^def _build_edge_records\(" graphify/templates.py` returns exactly one match
    - `grep -v '^#' graphify/templates.py | grep -c "_expand_blocks"` returns >= 5 (definition + 4 render entry-point call sites)
    - `grep -v '^#' graphify/templates.py | grep -c "_build_edge_records"` returns >= 5 (definition + 4 render entry-point call sites)
    - All three loaders wrap in `_BlockTemplate`: `grep -nE "_BlockTemplate\(" graphify/templates.py | wc -l` returns >= 4 (three loaders + at least one render-time use)
    - `pytest tests/test_templates.py -q -x` exits 0
    - No new required dependency added to `pyproject.toml`
  </acceptance_criteria>
  <done>
    `_BlockTemplate`, `_PREDICATE_CATALOG` (exactly 4), `_CONN_FIELDS` (exactly 6), `_IF_ATTR_RE`, `BlockContext`, `_eval_predicate`, `_build_edge_records` (with explicit field provenance + (relation, label) sort), `_expand_blocks`, and the extended `validate_template` exist in `graphify/templates.py`. All four render entry points (`render_note`, `_render_moc_like`, `render_moc`, `render_community_overview`) call `_expand_blocks` BEFORE `safe_substitute`. All template loaders wrap text in `_BlockTemplate`. Existing test suite still passes (no regressions).
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Test fixtures + behavior tests for TMPL-01, TMPL-02, sanitization (T-31-01), render-entry-point integration, ordering, byte-identical backward compatibility, and preflight-only invariant</name>
  <files>tests/test_templates.py, tests/fixtures/template_context.py, tests/fixtures/templates/with_if_block.md, tests/fixtures/templates/with_connections_block.md, tests/fixtures/templates/with_if_attr.md, tests/fixtures/templates/nested_blocks_invalid.md</files>
  <read_first>
    - tests/test_templates.py (existing patterns — `tmp_path`, NetworkX graph construction, `validate_template` invocation style; existing render_note end-to-end test patterns)
    - tests/fixtures/template_context.py (existing graph fixture helpers — extend, do not replace)
    - graphify/builtin_templates/thing.md (used as the block-free byte-identical baseline)
    - .planning/phases/31-template-engine-extensions/31-VALIDATION.md (test categories: predicate truth/false, loop iteration ordering, nested rejection, unknown predicate rejection, sanitization sub-cases, render integration, byte-identical, preflight-only invariant)
    - .planning/phases/31-template-engine-extensions/31-PATTERNS.md (test analog excerpts)
    - graphify/templates.py (the symbols added in Task 1)
  </read_first>
  <behavior>
    Tests in `tests/test_templates.py` (extend the existing module; do not create a new file). Group with section comments for greppability:

    `# --- TMPL-01: conditional blocks ---`
    - `test_if_god_node_true_renders` — graph with node marked `is_god_node=True`; template `"X{{#if_god_node}}GOD{{/if}}Y"` renders `"XGODY"` via `_expand_blocks`
    - `test_if_god_node_false_omits_cleanly` — non-god node renders `"XY"` (no leftover whitespace; verify no double-newline if block was on its own line)
    - `test_if_isolated_true_renders` — isolated node (degree 0) renders the guarded section
    - `test_if_has_connections_renders_when_edges_exist` — node with ≥1 edge renders
    - `test_if_has_dataview_branches_on_dataview_nonempty` — `BlockContext.dataview_nonempty=False` omits, `True` renders
    - `test_if_has_dataview_false_when_query_empty` — cross-link to Plan 02: when `_build_dataview_block` returns empty (resolved query empty/whitespace), `BlockContext.dataview_nonempty` is False → `{{#if_has_dataview}}` omits the guarded section
    - `test_if_attr_escape_hatch_reads_node_attribute` — node attr `is_published=True` → `{{#if_attr_is_published}}…{{/if}}` renders
    - `test_if_attr_falsy_value_omits` — node attr `is_published=False` → omits; missing attr → omits

    `# --- TMPL-02: connection loops ---`
    - `test_connections_loop_iterates` — template `"{{#connections}}- ${conn.label} (${conn.relation})\n{{/connections}}"` over a node with 3 edges produces 3 list items
    - `test_connections_loop_exposes_all_six_fields` — verify `${conn.label}`, `${conn.relation}`, `${conn.target}`, `${conn.confidence}`, `${conn.community}`, `${conn.source_file}` all substitute correctly
    - `test_connections_loop_flattened_form_works` — same template using `${conn_label}` (underscore, not dot) renders identically
    - `test_connections_target_renders_label_not_id` — per D-06, `${conn.target}` shows the target node's `label` attribute, not the raw node id
    - `test_connections_confidence_renders_string` — value is the literal string `"EXTRACTED"` / `"INFERRED"` / `"AMBIGUOUS"`
    - `test_connections_empty_loop_renders_nothing` — node with no edges → block expands to empty string
    - `test_connections_loop_deterministic_order` — graph constructed with deliberately shuffled edge insertion order (e.g., relations `["zeta", "alpha", "mu"]` and labels reversed); assert the rendered loop output is sorted by `(relation ASC, label ASC)` exactly. This locks the RESEARCH OQ3 / VALIDATION ordering invariant.
    - `test_build_edge_records_field_provenance` — direct unit test of `_build_edge_records`: build a graph with explicit node attrs (`label`, `community`) and edge data (`relation`, `confidence`, `source_file`); assert each returned record has every field sourced correctly (label and target both = sanitized target node label; community = str(target node community); relation/confidence/source_file from edge data)

    `# --- preflight rejection ---`
    - `test_nested_blocks_rejected_with_specific_error` — template with `{{#connections}}…{{#if_god_node}}…{{/if}}…{{/connections}}` returns the EXACT D-08 error string from `validate_template`
    - `test_unknown_predicate_rejected` — `{{#if_foobar}}` returns error containing `"unknown predicate"` and listing catalog members
    - `test_unclosed_block_rejected` — `{{#if_god_node}}…` (no `{{/if}}`) returns error containing `"unclosed block"`
    - `test_mismatched_closer_rejected` — `{{#connections}}…{{/if}}` returns error containing `"block mismatch"`
    - `test_unknown_conn_field_rejected` — `{{#connections}}${conn.bogus}{{/connections}}` returns error containing `"unknown connection field 'conn.bogus'"`

    `# --- render-entry-point integration (no forgotten call site) ---`
    Each of the four render entry points must invoke block expansion. Each test passes a template containing AT LEAST ONE block, then grep-asserts (a) the literal block syntax (`{{#`) is gone from the rendered output AND (b) the guarded content rendered:
    - `test_render_note_invokes_block_expansion` — call `render_note` end-to-end with a Thing-like fixture using a vault override template containing `{{#if_god_node}}GUARDED-CONTENT{{/if}}`; node has `is_god_node=True`; assert `"GUARDED-CONTENT"` in output AND `"{{#"` NOT in output AND `"{{/"` NOT in output
    - `test_render_moc_like_invokes_block_expansion` — call `_render_moc_like` directly with a vault override template containing `{{#if_has_dataview}}HAS-DV{{/if}}`; assert behavior across both dataview-empty and dataview-populated cases
    - `test_render_moc_invokes_block_expansion` — call `render_moc` end-to-end with a vault override template containing `{{#if_god_node}}…{{/if}}` (predicate evaluates per the synthetic community context — verify the block is expanded, not left as literal syntax in output)
    - `test_render_community_overview_invokes_block_expansion` — call `render_community_overview` end-to-end with a vault override template containing `{{#if_has_dataview}}…{{/if}}`; assert block is expanded

    `# --- T-31-01: label injection sanitization ---`
    Ship at least 6 explicit label-injection tests (replaces the prior 3-test cap). Each test injects an adversarial label into a graph and renders through `{{#connections}}${conn.label}{{/connections}}` (or `${conn.target}` where appropriate), asserting the adversarial substring cannot inject template syntax into the output:
    - `test_label_injection_double_brace_open` — label contains `{{` → output contains the sanitized form (no live `{{` in rendered loop body that could be re-parsed)
    - `test_label_injection_double_brace_close` — label contains `}}` (standalone) → sanitized
    - `test_label_injection_block_opener` — label contains `#` at relevant position (`{{#`) → cannot smuggle a fake nested loop; literal sequence does not survive in expanded output as a parseable opener
    - `test_label_injection_dollar_brace` — label contains `${` → cannot inject a stock `string.Template` placeholder
    - `test_label_injection_backtick` — label contains backtick (Dataview fence break) → backtick stripped/escaped
    - `test_label_injection_newline` — label contains literal `\n` (callout/fence break) → newline stripped/escaped
    - `test_block_expansion_runs_before_substitution` — adversarial fixture: node label is the literal string `"{{#connections}}{{/connections}}"`. Render via `{{#connections}}${conn.label}{{/connections}}`. Assert (a) the rendered output does NOT contain a fake un-expanded `{{#connections}}` opener (would prove substitution-then-expansion ordering, violating D-16), (b) the literal sequence appears only as sanitized text. This locks the D-16 ordering invariant adversarially.

    `# --- backward compatibility (D-16 / TMPL-01 / ROADMAP criterion 4) ---`
    - `test_block_free_template_renders_byte_identical` — load a real block-free template (the existing built-in `graphify/builtin_templates/thing.md`); render it through the new pipeline (`_expand_blocks` → `_BlockTemplate.safe_substitute`); render the SAME source through stock `string.Template(text).safe_substitute(...)` (no _expand_blocks pass); assert the two outputs are `==` (byte-identical). This is the explicit ROADMAP criterion 4 / D-16 backward-compatibility gate. Use the same `substitution_ctx` mapping for both paths.

    `# --- preflight-only invariant (D-09/D-10) ---`
    - `test_render_does_not_revalidate_blocks` — pass a template that has already passed `validate_template` (returns no errors) directly to `_expand_blocks`; assert `_expand_blocks` consumes it without raising any block-syntax error and without surfacing any "unknown predicate" / "unclosed" / "block mismatch" message. Test docstring cites D-09/D-10: validation is preflight-only; render path trusts vetted input.

    Also extend `tests/fixtures/template_context.py` with helper builders:
    - `make_block_context(graph, node_id, *, dataview_nonempty=False)` returning a `BlockContext` (calls `_build_edge_records` internally)
    - `make_graph_with_god_node(node_id="god1")`, `make_graph_with_isolated(node_id="iso1")`, `make_graph_with_edges(node_id, edge_specs)` where each edge_spec is a dict matching the six conn fields

    Fixture template files in `tests/fixtures/templates/` for end-to-end render tests:
    - `with_if_block.md` — minimal frontmatter + `{{#if_god_node}}This is a god node.{{/if}}` body
    - `with_connections_block.md` — frontmatter + `## Connections\n{{#connections}}- [[${conn.target}]] — ${conn.relation}\n{{/connections}}`
    - `with_if_attr.md` — `{{#if_attr_is_published}}Published{{/if}}`
    - `nested_blocks_invalid.md` — `{{#connections}}{{#if_god_node}}X{{/if}}{{/connections}}` (used by the negative test)
  </behavior>
  <action>
    1. Create the four fixture template files under `tests/fixtures/templates/` with exact body content above. Each must include minimal frontmatter `---\nid: ${id}\n---\n` (or whatever fields existing `validate_template` requires for the corresponding note_type — inspect existing fixtures and align).

    2. Extend `tests/fixtures/template_context.py` with the four helper builders described in `<behavior>`. Reuse existing helpers if they cover the same ground; do not duplicate.

    3. Add the test functions described in `<behavior>` to `tests/test_templates.py`. Use the section comments listed above for greppability.

    4. Pure unit tests only — `tmp_path`, no network, no filesystem outside `tmp_path`. Build `nx.Graph` instances inline in each test or via the new fixture helpers.

    5. **Render-entry-point tests are end-to-end**: call `render_note` / `_render_moc_like` / `render_moc` / `render_community_overview` with a vault override template (use `tmp_path` to write the override file, pass `vault_dir=tmp_path`). These tests are MANDATORY for the "no forgotten call site" gate — do NOT replace them with isolated `_expand_blocks` calls.

    6. For predicate/loop/sanitization unit tests, calling `_expand_blocks(text, ctx)` directly is acceptable.

    7. For the D-08 nested-block test: assert the error string equality is EXACT (use `assert errs == ["validate_template: nested template blocks are not supported (found '{{#if_god_node}}' inside '{{#connections}}'). Flatten the template or pre-compute the predicate."]`).

    8. For `test_block_free_template_renders_byte_identical`: load `graphify/builtin_templates/thing.md` source via the same loader code path the production renderer uses, then perform two renders with identical substitution context — one through the new pipeline, one through stock `string.Template`. Assert byte-equality. This is the explicit backward-compatibility gate.
  </action>
  <verify>
    <automated>cd /Users/silveimar/Documents/silogia-repos/graphify && pytest tests/test_templates.py -q -x 2>&1 | tail -40</automated>
  </verify>
  <acceptance_criteria>
    - `pytest tests/test_templates.py -q -x` exits 0
    - `pytest tests/test_templates.py -q -k "if_god_node or if_isolated or if_has_connections or if_has_dataview or if_attr"` collects ≥ 8 tests and all pass
    - `pytest tests/test_templates.py -q -k "connections"` collects ≥ 7 tests and all pass (includes ordering + provenance)
    - `pytest tests/test_templates.py -q -k "nested or unknown or unclosed or mismatched"` collects ≥ 5 tests and all pass
    - `pytest tests/test_templates.py -q -k "label_injection or block_expansion_runs_before"` collects ≥ 7 tests and all pass
    - `grep -v '^#' tests/test_templates.py | grep -c "test_label_injection_"` returns >= 6
    - `grep -v '^#' tests/test_templates.py | grep -c "test_block_expansion_runs_before_substitution"` returns >= 1
    - `grep -v '^#' tests/test_templates.py | grep -c "test_block_free_template_renders_byte_identical"` returns >= 1
    - `grep -v '^#' tests/test_templates.py | grep -c "test_render_does_not_revalidate_blocks"` returns >= 1
    - `grep -v '^#' tests/test_templates.py | grep -c "test_connections_loop_deterministic_order"` returns >= 1
    - `grep -v '^#' tests/test_templates.py | grep -c "test_build_edge_records_field_provenance"` returns >= 1
    - `grep -v '^#' tests/test_templates.py | grep -c "test_render_note_invokes_block_expansion"` returns >= 1
    - `grep -v '^#' tests/test_templates.py | grep -c "test_render_moc_like_invokes_block_expansion"` returns >= 1
    - `grep -v '^#' tests/test_templates.py | grep -c "test_render_moc_invokes_block_expansion"` returns >= 1
    - `grep -v '^#' tests/test_templates.py | grep -c "test_render_community_overview_invokes_block_expansion"` returns >= 1
    - `grep -v '^#' tests/test_templates.py | grep -c "test_if_has_dataview_false_when_query_empty"` returns >= 1
    - `grep -v '^#' tests/test_templates.py | grep -c "test_if_"` returns >= 8
    - `grep -v '^#' tests/test_templates.py | grep -c "test_connections_"` returns >= 7
    - `ls tests/fixtures/templates/*.md | wc -l` returns >= 4
    - `grep -F "validate_template: nested template blocks are not supported (found '{{#if_god_node}}' inside '{{#connections}}'). Flatten the template or pre-compute the predicate." tests/test_templates.py | wc -l` returns >= 1 (verbatim D-08 message asserted)
    - All tests confined to `tmp_path` — `grep -nE "open\(|Path\(" tests/test_templates.py | grep -v "tmp_path\|fixtures" | wc -l` returns 0
  </acceptance_criteria>
  <done>
    Test suite covers TMPL-01 (predicate truth/false table for all four catalog members + `if_attr_` escape hatch + Plan 02 cross-link), TMPL-02 (loop iteration with all six fields + flat parallel form + empty case + deterministic ordering + edge-record field provenance), preflight rejection (nested D-08 verbatim, unknown predicate, unclosed, mismatched, unknown conn field), T-31-01 sanitization (≥6 explicit injection tests + adversarial ordering test), render-entry-point integration (all four call sites verified end-to-end — no forgotten call site possible), backward compatibility (byte-identical block-free render gate), and preflight-only invariant (D-09/D-10). All tests pass under Python 3.10+ with no network or non-tmp_path filesystem access.
  </done>
</task>

</tasks>

<verification>
- `pytest tests/test_templates.py -q -x` passes (existing suite + all new TMPL-01/02/sanitization/integration/ordering/byte-identical/preflight-invariant tests).
- `pytest tests/ -q` passes (no regressions in unrelated suites).
- `python3 -c "from graphify.templates import _BlockTemplate, _PREDICATE_CATALOG, _CONN_FIELDS, _expand_blocks, BlockContext, _build_edge_records"` succeeds.
- `python3 -c "from graphify.templates import _PREDICATE_CATALOG; assert sorted(_PREDICATE_CATALOG) == ['if_god_node','if_has_connections','if_has_dataview','if_isolated']"` succeeds.
- `pip show pyyaml` is still optional (no entry added to `dependencies` in `pyproject.toml`).
</verification>

<success_criteria>
- TMPL-01: Conditional sections render guarded body iff predicate true; absence is clean (no whitespace artifacts); preflight rejects unknown predicate names with helpful catalog listing.
- TMPL-02: Connection loop iterates per edge with all six namespaced fields populated, sorted by (relation, label) ascending; both `${conn.X}` and `${conn_X}` forms render identically; empty edge set produces empty output.
- Preflight: Nested blocks rejected with verbatim D-08 message; unclosed/mismatched/unknown-field cases produce specific actionable errors. Render path does not re-validate (D-09/D-10).
- Sanitization (T-31-01): Node labels containing `{{`, `}}`, `#`, `${`, backticks, or newlines cannot inject conditional logic, smuggle fake loops, or escape Dataview fences. Block expansion runs BEFORE scalar substitution (D-16 ordering invariant verified by adversarial test).
- Backward compatibility: Block-free templates render byte-identical to stock `string.Template.safe_substitute` (ROADMAP criterion 4).
- All four render entry points invoke `_expand_blocks` (verified end-to-end — no forgotten call site possible).
- No new required dependencies; PyYAML stays optional; Python 3.10+ compatible.
</success_criteria>

<output>
After completion, create `.planning/phases/31-template-engine-extensions/31-01-SUMMARY.md` summarizing: symbols added/modified in `templates.py` with line refs, test counts per category, the verbatim D-08 message asserted, the four predicate catalog members (frozen), the six conn fields (frozen), the (relation, label) sort order locked by `_build_edge_records`, the byte-identical backward-compatibility gate, and confirmation that block expansion order (D-16) is enforced by adversarial test.
</output>
