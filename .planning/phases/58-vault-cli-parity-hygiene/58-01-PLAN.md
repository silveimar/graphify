---
phase: 58-vault-cli-parity-hygiene
plan: 01
type: tdd
wave: 1
depends_on: []
files_modified:
  - graphify/output.py
  - tests/test_vault_parity.py
autonomous: true
requirements: [VAUX-01]
must_haves:
  truths:
    - "resolve_vault_for_parity() returns a dict with vault_path, source, profile_path, profile_mode, warnings"
    - "Parity helper agrees with run_doctor() on resolved vault_path and source label across resolution scenarios"
    - "Helper delegates resolution to resolve_execution_paths() and never duplicates resolution logic"
    - "Helper warnings dimension covers only stderr lines emitted by resolve_execution_paths() (not _merge_vault_pins overrides — see Q1 split)"
  artifacts:
    - path: "graphify/output.py"
      provides: "resolve_vault_for_parity() function returning structured parity dict"
      contains: "def resolve_vault_for_parity"
    - path: "tests/test_vault_parity.py"
      provides: "VAUX-01 parity tests asserting helper output equals run_doctor() resolution"
      contains: "def test_parity_vault_cli_matches_doctor"
  key_links:
    - from: "graphify/output.py:resolve_vault_for_parity"
      to: "graphify/output.py:resolve_execution_paths"
      via: "direct function call inside contextlib.redirect_stderr block"
      pattern: "resolve_execution_paths\\("
    - from: "tests/test_vault_parity.py"
      to: "graphify.doctor.run_doctor"
      via: "construct ResolvedOutput from parity dict, pass as resolved_output kwarg"
      pattern: "run_doctor\\(.*resolved_output="
---

<objective>
Implement the VAUX-01 structured parity helper. Add `resolve_vault_for_parity()` to `graphify/output.py` and create `tests/test_vault_parity.py` with field-level parity assertions between the helper and `run_doctor()`.

Purpose: Lock the four parity dimensions (vault_path, source, profile path/mode, warnings) so future CLI changes cannot silently desync `--vault` resolution from doctor's view (D-01, D-02, D-04).

Output: New helper function, new test module with parity coverage across explicit-vault, env-var, and vault-list resolution scenarios.

Note (Q1 resolution): The `warnings` dimension in the parity dict captures only stderr emitted by `resolve_execution_paths()`. `_merge_vault_pins` override warnings live in `__main__.py` and are tested separately via subprocess in Plan 02 — calling `_merge_vault_pins` from `output.py` would create a circular import.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/58-vault-cli-parity-hygiene/58-CONTEXT.md
@.planning/phases/58-vault-cli-parity-hygiene/58-RESEARCH.md
@.planning/phases/58-vault-cli-parity-hygiene/58-PATTERNS.md
@.planning/phases/58-vault-cli-parity-hygiene/58-VALIDATION.md
@graphify/output.py
@graphify/doctor.py
@tests/test_doctor.py
@tests/test_vault_cli.py
</context>

<tasks>

<task type="tdd" tdd="true">
  <name>Task 1: RED — write failing parity tests in tests/test_vault_parity.py</name>
  <files>tests/test_vault_parity.py</files>
  <read_first>
    - tests/test_doctor.py (lines 1-180 — `_make_vault`, `_VALID_PROFILE`, `test_run_doctor_preflight_uses_pinned_vault_not_cwd`)
    - tests/test_vault_cli.py (lines 1-100 — subprocess pattern for vault CLI invocation)
    - graphify/output.py (sections containing `resolve_execution_paths`, `ResolvedOutput`, `_refuse`)
    - .planning/phases/58-vault-cli-parity-hygiene/58-PATTERNS.md (test scaffolding section, lines 152-291)
    - .planning/phases/58-vault-cli-parity-hygiene/58-CONTEXT.md (D-01, D-02, D-04, D-08)
  </read_first>
  <behavior>
    - Test 1 `test_parity_helper_returns_dict_with_four_dimensions`: `resolve_vault_for_parity(tmp_path, explicit_vault=vault)` returns dict with keys {"vault_path", "source", "profile_path", "profile_mode", "warnings"}; vault_path equals constructed vault; source == "vault-cli"; profile_mode == "vault-relative"; warnings is a list (may be empty).
    - Test 2 `test_parity_vault_cli_matches_doctor`: parity dict's vault_path/source equals `run_doctor(tmp_path, resolved_output=ResolvedOutput(...constructed from parity...)).resolved_output.vault_path/source`.
    - Test 3 `test_parity_env_var_resolution`: with `env_vault` arg pointing at vault, parity["source"] reflects env-precedence label and vault_path matches.
    - Test 4 `test_parity_no_vault_returns_none`: with no explicit/env/list vault and cwd not under a vault, parity["vault_path"] is None and parity["source"] indicates default/cwd source.
    - Test 5 `test_parity_helper_does_not_duplicate_resolution`: assert (via inspecting parity["vault_path"]) that the helper produces the same Path object as a direct `resolve_execution_paths(...)` call would (call both and compare).
  </behavior>
  <action>
Create `tests/test_vault_parity.py`. Module docstring: `"""Unit + integration tests for VAUX-01 parity helper (Phase 58)."""`.

Imports:
```python
from __future__ import annotations
import subprocess
import sys
from pathlib import Path
import pytest
from graphify.doctor import run_doctor
from graphify.output import ResolvedOutput, resolve_execution_paths, resolve_vault_for_parity
```

Replicate `_make_vault` verbatim from `tests/test_doctor.py` (D-08 — do NOT cross-import; copy the body):
```python
def _make_vault(tmp_path: Path, *, profile_text: str | None = None) -> Path:
    vault = tmp_path / "vault"
    vault.mkdir()
    (vault / ".obsidian").mkdir()
    (vault / ".git").mkdir()
    (vault / ".graphify").mkdir()
    if profile_text is not None:
        (vault / ".graphify" / "profile.yaml").write_text(profile_text)
    return vault
```

Replicate `_VALID_PROFILE` constant verbatim from `tests/test_doctor.py` (the full taxonomy/mapping/output YAML block).

Write the 5 tests above. Each test that touches profile loading must call `pytest.importorskip("yaml")` first. Use `subprocess.run([sys.executable, "-m", "graphify", ...], capture_output=True, text=True, timeout=60)` only where CLI surface is required; prefer in-process calls to `resolve_vault_for_parity()`.

For test 2, construct ResolvedOutput per PATTERNS.md lines 215-234 and pass as `resolved_output=` to `run_doctor()`.

Run pytest after writing — all 5 tests MUST fail with `ImportError` or `AttributeError` (function doesn't exist yet). This is the RED gate.
  </action>
  <verify>
    <automated>pytest tests/test_vault_parity.py -x 2>&1 | grep -E "(ImportError|AttributeError|cannot import name 'resolve_vault_for_parity')"</automated>
  </verify>
  <acceptance_criteria>
    - File `tests/test_vault_parity.py` exists with 5 test functions named exactly as in `<behavior>`
    - File contains `_make_vault` function (replicated, not imported)
    - File contains `_VALID_PROFILE` constant
    - `pytest tests/test_vault_parity.py -x` exits non-zero with import-time failure on `resolve_vault_for_parity`
    - `grep -c "pytest.importorskip(\"yaml\")" tests/test_vault_parity.py` >= 3
    - No `from tests.test_doctor` import present (D-08): `grep -c "from tests.test_doctor" tests/test_vault_parity.py` == 0
  </acceptance_criteria>
  <done>RED gate met: tests exist and fail because `resolve_vault_for_parity` is not defined in `graphify/output.py`.</done>
</task>

<task type="tdd" tdd="true">
  <name>Task 2: GREEN — implement resolve_vault_for_parity() in graphify/output.py</name>
  <files>graphify/output.py</files>
  <read_first>
    - graphify/output.py (full file — locate `_refuse` at ~line 67, `resolve_execution_paths` at ~line 151, `ResolvedOutput` definition)
    - graphify/doctor.py (lines 417-430 — stderr-capture pattern using io.StringIO + contextlib.redirect_stderr)
    - graphify/profile.py (signature of `load_profile`)
    - .planning/phases/58-vault-cli-parity-hygiene/58-PATTERNS.md (lines 58-138 — exact function body to write)
  </read_first>
  <action>
Add module-level imports `import contextlib` and `import io` to `graphify/output.py` if not already present (place alongside existing `import sys`).

Add `resolve_vault_for_parity()` immediately after `resolve_execution_paths()` using the exact body from PATTERNS.md lines 60-121. Signature:
```python
def resolve_vault_for_parity(
    cwd: Path,
    *,
    explicit_vault: Path | None = None,
    env_vault: str | None = None,
    vault_list_file: Path | None = None,
) -> dict:
```

Returns dict with keys: `vault_path`, `source`, `profile_path`, `profile_mode`, `warnings`. Implementation MUST:
1. Use `contextlib.redirect_stderr(io.StringIO())` to capture warnings during resolution.
2. Delegate to `resolve_execution_paths(cwd, explicit_vault=..., env_vault=..., vault_list_file=...)` — NEVER duplicate resolution logic (anti-pattern #2 from PATTERNS.md).
3. On `SystemExit` from inner resolution, re-raise (do not swallow).
4. After resolution, if `resolved.vault_path is not None`, check for `.graphify/profile.yaml` and attempt `load_profile()` to extract `output.mode`. Wrap profile load in try/except (yaml may not be installed, profile may be invalid).
5. Strip and split captured stderr into a list of non-empty lines for the `warnings` field.

Do NOT touch `_refuse()` or any other existing function (per anti-pattern #4).

Run pytest — all 5 tests from Task 1 MUST pass. This is the GREEN gate.
  </action>
  <verify>
    <automated>pytest tests/test_vault_parity.py -x -v</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "^def resolve_vault_for_parity" graphify/output.py` == 1
    - `grep -c "resolve_execution_paths(" graphify/output.py` >= 2 (original definition + new helper call)
    - `grep -c "^import contextlib" graphify/output.py` >= 1
    - `grep -c "^import io" graphify/output.py` >= 1
    - `pytest tests/test_vault_parity.py -x` exits 0 with all 5 tests passing
    - `_refuse` function unchanged: `grep -A2 "^def _refuse" graphify/output.py` shows the same 3-line body (signature, docstring, print + return)
  </acceptance_criteria>
  <done>GREEN gate met: all 5 parity tests pass; helper delegates to resolve_execution_paths; profile mode extracted when present; stderr captured into warnings list.</done>
</task>

</tasks>

<threat_model>

## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| CLI args → output.py | User-supplied `--vault` path crosses into resolution helper |
| filesystem → profile loader | YAML profile content read from vault `.graphify/profile.yaml` |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-58-01 | T (Tampering) | resolve_vault_for_parity profile load | mitigate | Wrap `load_profile()` in try/except; do not crash parity helper on malformed YAML — leave profile_mode as None |
| T-58-02 | I (Info disclosure) | warnings list returned | accept | Warnings are existing stderr lines already shown to user; no new disclosure surface |
| T-58-03 | D (DoS) | profile_path .exists() check | accept | Single stat call inside vault dir already validated by `_ensure_vault_root()` |

</threat_model>

<verification>
- pytest tests/test_vault_parity.py -x passes (all 5 tests)
- pytest tests/ -q (full suite still green — no regressions in existing output.py callers)
- grep confirms `_refuse` body unchanged
</verification>

<success_criteria>
- VAUX-01 parity helper exists and is tested
- Helper agrees with `run_doctor()` on vault_path and source across resolution scenarios
- Helper does not duplicate `resolve_execution_paths()` logic
- All 5 RED tests turn GREEN after implementation
</success_criteria>

<output>
Create `.planning/phases/58-vault-cli-parity-hygiene/58-01-SUMMARY.md` after completion.
</output>
