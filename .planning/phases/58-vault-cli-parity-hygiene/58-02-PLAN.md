---
phase: 58-vault-cli-parity-hygiene
plan: 02
type: tdd
wave: 2
depends_on: [58-01]
files_modified:
  - graphify/output.py
  - tests/test_vault_parity.py
autonomous: true
requirements: [VAUX-02]
must_haves:
  truths:
    - "All vault CLI failures emit two-line stderr: '[graphify] error: <msg>' followed by '  hint: <fix>'"
    - "Vault config errors exit non-zero (1 for unknown/invalid vault, 2 for ambiguous vault-list)"
    - "Three D-07 failure scenarios are covered by subprocess tests asserting both lines and exit code"
    - "Existing _refuse() callers remain unchanged — only the 3 D-07-specific call sites are migrated to _emit_vault_error()"
    - "Global+local --vault override warning text from _merge_vault_pins is preserved (no precedence behavior change per D-09)"
  artifacts:
    - path: "graphify/output.py"
      provides: "_emit_vault_error(msg, hint, *, code=1) helper returning SystemExit"
      contains: "def _emit_vault_error"
    - path: "tests/test_vault_parity.py"
      provides: "VAUX-02 subprocess tests for 3 D-07 failure scenarios + override-warning preservation"
      contains: "def test_unknown_vault_nonexistent_path_error"
  key_links:
    - from: "graphify/output.py:_ensure_vault_root"
      to: "graphify/output.py:_emit_vault_error"
      via: "raise _emit_vault_error(msg, hint) at the 2 unknown-vault sites"
      pattern: "raise _emit_vault_error"
    - from: "graphify/output.py:_pick_vault_from_list_file"
      to: "stderr hint line + raise SystemExit(2)"
      via: "print hint line immediately before existing raise SystemExit(2)"
      pattern: "  hint:"
    - from: "tests/test_vault_parity.py"
      to: "graphify CLI subprocess"
      via: "subprocess.run([sys.executable, '-m', 'graphify', ...])"
      pattern: "subprocess.run"
---

<objective>
Implement VAUX-02 actionable error format. Add `_emit_vault_error()` companion to `_refuse()` in `graphify/output.py`. Migrate ONLY the three D-07 failure call sites (no broader behavior change per D-09). Add subprocess-level tests covering the three failure categories plus the global+local override-warning preservation (Q1 split).

Purpose: Ensure every vault CLI failure produces a machine-parseable two-line `[graphify] error: ... / hint: ...` format with non-zero exit (D-05/D-06). Catches the dry-run-mismatch scenario before it surprises users.

Output: Helper function, three failure-scenario subprocess tests, one override-warning preservation test, optional dry-run-mismatch test using Plan 01's parity helper.

Scope (Q2 resolution): Replace `raise _refuse(...)` at exactly 3 D-07 call sites — `_ensure_vault_root()` not-a-directory branch, `_ensure_vault_root()` no-`.obsidian/` branch, and add hint to `_pick_vault_from_list_file()` ambiguous exit-2 path. All other `_refuse()` callers stay as-is.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/58-vault-cli-parity-hygiene/58-CONTEXT.md
@.planning/phases/58-vault-cli-parity-hygiene/58-RESEARCH.md
@.planning/phases/58-vault-cli-parity-hygiene/58-PATTERNS.md
@.planning/phases/58-vault-cli-parity-hygiene/58-VALIDATION.md
@.planning/phases/58-vault-cli-parity-hygiene/58-01-SUMMARY.md
@graphify/output.py
@graphify/__main__.py
@graphify/doctor.py
@tests/test_vault_cli.py
</context>

<tasks>

<task type="tdd" tdd="true">
  <name>Task 1: RED — write failing VAUX-02 subprocess tests in tests/test_vault_parity.py</name>
  <files>tests/test_vault_parity.py</files>
  <read_first>
    - tests/test_vault_parity.py (current state from Plan 01)
    - tests/test_vault_cli.py (subprocess pattern lines 40-80)
    - graphify/output.py (`_refuse` callers and `_pick_vault_from_list_file` ambiguous branch)
    - graphify/__main__.py (`_merge_vault_pins` warning text — search for "command --vault / --vault-list overrides global pin")
    - .planning/phases/58-vault-cli-parity-hygiene/58-PATTERNS.md (lines 236-291 — VAUX-02 test patterns)
    - .planning/phases/58-vault-cli-parity-hygiene/58-CONTEXT.md (D-05, D-06, D-07, D-09)
  </read_first>
  <behavior>
    - Test 1 `test_unknown_vault_nonexistent_path_error`: `--vault /<tmp>/no-such-dir doctor` → returncode != 0, stderr contains `[graphify] error:` and `  hint:`.
    - Test 2 `test_unknown_vault_no_obsidian_marker_error`: `--vault <dir-without-.obsidian> doctor` → returncode != 0, stderr contains both lines, hint mentions `.obsidian/`.
    - Test 3 `test_ambiguous_vault_list_exit2`: vault-list file with two valid vaults, non-TTY → returncode == 2, stderr contains `  hint:`.
    - Test 4 `test_global_local_override_warning_preserved`: global `--vault X` + per-command `--vault X doctor` → stderr contains the existing `[graphify] command --vault / --vault-list overrides global pin` warning text (preserved per D-09; tested at CLI surface since `_merge_vault_pins` lives in `__main__.py`).
    - Test 5 `test_dry_run_mismatch_uses_parity_helper`: scenario where `--vault X doctor --dry-run` shows resolution; assert `resolve_vault_for_parity()` returns same vault_path as parsed from doctor's output (uses Plan 01 helper to lock parity at the dry-run boundary).
  </behavior>
  <action>
Append 5 new test functions to `tests/test_vault_parity.py` from Plan 01. Each uses `subprocess.run([sys.executable, "-m", "graphify", ...], cwd=str(tmp_path), capture_output=True, text=True, timeout=60)`.

Test 1 example:
```python
def test_unknown_vault_nonexistent_path_error(tmp_path):
    r = subprocess.run(
        [sys.executable, "-m", "graphify", "--vault", str(tmp_path / "no-such-dir"), "doctor"],
        cwd=str(tmp_path), capture_output=True, text=True, timeout=60,
    )
    assert r.returncode != 0
    assert "[graphify] error:" in r.stderr
    assert "  hint:" in r.stderr
```

Test 2: create empty dir without `.obsidian/`, point `--vault` at it. Assert hint string mentions `.obsidian` substring (e.g., `assert ".obsidian" in r.stderr`).

Test 3: create two `_make_vault()` instances under `tmp_path / "a"` and `tmp_path / "b"` (call `_make_vault` twice with different parents). Write a vault-list file containing both paths. Run `--vault-list <list>.txt doctor`. Assert `r.returncode == 2` and `"  hint:" in r.stderr`.

Test 4: create one valid vault. Run with both global and per-command `--vault` pointing at the same vault. Assert returncode == 0 (override is a warning, not error) AND `"command --vault / --vault-list overrides global pin" in r.stderr`. This locks the existing warning text (D-09 — no behavior change).

Test 5: create valid vault. Run `--vault <vault> doctor`. Capture stdout. Call `resolve_vault_for_parity(tmp_path, explicit_vault=vault)` in-process. Assert `parity["vault_path"] == vault` AND that doctor stdout contains `str(vault)` (or `parity["source"]`) — proving CLI and helper agree.

All 5 tests MUST initially fail because `_emit_vault_error()` is not yet implemented and the existing `_refuse()` output does not match `[graphify] error: ... / hint: ...` format.
  </action>
  <verify>
    <automated>pytest tests/test_vault_parity.py::test_unknown_vault_nonexistent_path_error tests/test_vault_parity.py::test_unknown_vault_no_obsidian_marker_error tests/test_vault_parity.py::test_ambiguous_vault_list_exit2 -x 2>&1 | grep -E "(FAILED|AssertionError)"</automated>
  </verify>
  <acceptance_criteria>
    - 5 new test functions exist in `tests/test_vault_parity.py` named exactly as in `<behavior>`
    - `grep -c "subprocess.run" tests/test_vault_parity.py` >= 5
    - `grep -c "\\[graphify\\] error:" tests/test_vault_parity.py` >= 2
    - `grep -c "  hint:" tests/test_vault_parity.py` >= 3
    - The 3 failure-category tests (Tests 1-3) FAIL before implementation
  </acceptance_criteria>
  <done>RED gate met: VAUX-02 tests exist and the 3 D-07 failure tests fail because output format does not yet match.</done>
</task>

<task type="tdd" tdd="true">
  <name>Task 2: GREEN — add _emit_vault_error() and migrate the 3 D-07 call sites</name>
  <files>graphify/output.py</files>
  <read_first>
    - graphify/output.py (locate `_refuse` ~line 67, `_ensure_vault_root` ~line 73-110, `_pick_vault_from_list_file` ambiguous branch)
    - .planning/phases/58-vault-cli-parity-hygiene/58-PATTERNS.md (lines 45-56 — exact `_emit_vault_error` body; lines 140-148 — call site mapping)
    - .planning/phases/58-vault-cli-parity-hygiene/58-CONTEXT.md (D-05, D-09 — no broader behavior change)
  </read_first>
  <action>
Step 1 — Add `_emit_vault_error()` immediately after `_refuse()` at ~line 70 in `graphify/output.py`:
```python
def _emit_vault_error(msg: str, hint: str, *, code: int = 1) -> SystemExit:
    """Emit [graphify] error: + hint: lines to stderr and return SystemExit(code).

    VAUX-02: two-line format mirrors doctor.py _FIX_HINTS pattern (D-05).
    Callers: raise _emit_vault_error(msg, hint, code=...)
    """
    print(f"[graphify] error: {msg}", file=sys.stderr)
    print(f"  hint: {hint}", file=sys.stderr)
    return SystemExit(code)
```

Step 2 — In `_ensure_vault_root()`, replace the two D-07 `raise _refuse(...)` sites:

Site A (not-a-directory branch, the `not p.is_dir()` check):
```python
raise _emit_vault_error(
    f"Vault path is not a directory: {p}",
    "Check the path exists and is a directory, not a file.",
)
```

Site B (no `.obsidian/` marker, the `not is_obsidian_vault(p)` check):
```python
raise _emit_vault_error(
    f"Not an Obsidian vault (missing .obsidian/): {p}",
    "Pass the root of an Obsidian vault (must contain .obsidian/).",
)
```

Step 3 — In `_pick_vault_from_list_file()` ambiguous multi-root non-TTY branch, immediately BEFORE the existing `raise SystemExit(2)`, add:
```python
print("  hint: Re-run from a TTY to pick interactively, or pass --vault explicitly.", file=sys.stderr)
```
(Do NOT change the existing `raise SystemExit(2)` — D-06 says exit 2 stays for ambiguity.)

Step 4 — Do NOT modify any other `_refuse()` call site. Do NOT change `_refuse()` itself. Do NOT touch `_merge_vault_pins` in `__main__.py` (D-09).

Run pytest. The 3 D-07 failure tests MUST pass. Test 4 (override-warning preservation) MUST pass without modification (warning text is unchanged in `__main__.py`). Test 5 (dry-run parity) MUST pass.
  </action>
  <verify>
    <automated>pytest tests/test_vault_parity.py -x -v && pytest tests/test_vault_cli.py tests/test_doctor.py -x</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "^def _emit_vault_error" graphify/output.py` == 1
    - `grep -c "raise _emit_vault_error" graphify/output.py` == 2 (the two `_ensure_vault_root` sites)
    - `grep -c "^def _refuse" graphify/output.py` == 1 (still exists, unchanged)
    - `grep -c "  hint:" graphify/output.py` >= 1 (the ambiguous-list branch hint print)
    - All tests in `tests/test_vault_parity.py` pass
    - `pytest tests/test_vault_cli.py tests/test_doctor.py` still passes (no regressions)
    - `grep -c "raise _refuse" graphify/output.py` == original count minus 2 (only 2 sites migrated)
  </acceptance_criteria>
  <done>GREEN gate met: 3 D-07 sites emit `[graphify] error:` + `  hint:` with non-zero exit; ambiguous case preserved at exit 2; override warning preserved verbatim; existing `_refuse` callers untouched; full suite green.</done>
</task>

</tasks>

<threat_model>

## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| CLI args → output.py error helpers | User-supplied paths formatted into stderr messages |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-58-04 | I (Info disclosure) | _emit_vault_error msg with user path | accept | Path is user-supplied; echoing back to user's own stderr is intended UX, not disclosure |
| T-58-05 | T (Tampering) | hint string injection via path | mitigate | Hint strings are static literals in code; only `msg` interpolates user path; printed to stderr (not parsed by anything trusted) |
| T-58-06 | E (Elevation) | exit code change behavior | mitigate | Exit codes preserved exactly per D-06 (1 for config error, 2 for ambiguous); test_ambiguous_vault_list_exit2 asserts ==2 to lock |

</threat_model>

<verification>
- pytest tests/test_vault_parity.py -x passes (all 10 tests across plans 01+02)
- pytest tests/ -q full suite green (no regressions in test_vault_cli.py, test_doctor.py, test_output.py)
- grep confirms only 2 `_refuse` sites migrated, `_refuse` body unchanged
- grep confirms `_merge_vault_pins` warning text in `__main__.py` unchanged
</verification>

<success_criteria>
- VAUX-02 helper exists and produces two-line error format
- 3 D-07 failure scenarios emit error+hint with correct exit codes
- Override-warning text preserved (D-09 no precedence change)
- Dry-run/parity helper agreement locked
</success_criteria>

<output>
Create `.planning/phases/58-vault-cli-parity-hygiene/58-02-SUMMARY.md` after completion.
</output>
