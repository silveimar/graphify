---
phase: 41-vault-cli-vault-flag-multi-vault-selector
plan: 03
type: execute
wave: 3
depends_on:
  - 02
files_modified:
  - graphify/doctor.py
  - graphify/__main__.py
  - tests/test_doctor.py
autonomous: true
requirements:
  - VCLI-03
  - VCLI-04
must_haves:
  truths:
    - "`graphify doctor` prints the same resolved vault root, notes_dir, artifacts_dir, and source as `run` / `--obsidian` for identical argv + env (D-05)."
    - "`doctor --dry-run` preview uses detect() against the effective corpus root and shows skip reasons aligned with non-doctor dry-run after vault resolution (D-06)."
    - "No phantom vault: dry-run never reports profile paths from the wrong vault before pin resolution."
  artifacts:
    - path: "graphify/doctor.py"
      provides: "run_doctor uses resolve_execution_paths or injected ResolvedOutput consistent with CLI"
      contains: "run_doctor"
    - path: "graphify/__main__.py"
      provides: "`doctor` subparser receives global vault flags; passes kwargs to run_doctor"
      contains: "doctor"
    - path: "tests/test_doctor.py"
      provides: "Assertions on vault_path / source strings with pinned --vault"
      contains: "test_"
  key_links:
    - from: "graphify/__main__.py (`doctor`)"
      to: "graphify.doctor.run_doctor"
      via: "same vault kwargs as run"
      pattern: "run_doctor\\("
    - from: "run_doctor"
      to: "resolve_execution_paths"
      via: "single resolution path for report.resolved_output"
      pattern: "resolved_output"
---

<objective>
Align **`graphify doctor`** and **`doctor --dry-run`** with the **same vault resolution** as the main pipeline (**VCLI-03**, **VCLI-04**). Refactor **`run_doctor`** so it does not assume **CWD == vault root** when **`--vault` / env / list** pins apply; **`format_report`** / **Vault Detection** section must show **pinned vault** and **`source`** including vault-pin kinds from Plan 01. Ensure **`detect()`** preview in dry-run runs with **effective cwd** consistent with **`run`** (document choice: scan root = `cwd` vs vault root — must match **D-06** and avoid duplicate resolver implementations).

Purpose: Operators see one truth for vault/output across **doctor**, **run**, and **export** preview.

Output: `doctor.py` + `__main__.py` doctor branch + pytest updates.
</objective>

<execution_context>
@$HOME/.cursor/get-shit-done/workflows/execute-plan.md
@$HOME/.cursor/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/41-vault-cli-vault-flag-multi-vault-selector/41-CONTEXT.md
@graphify/doctor.py
@graphify/detect.py
@graphify/__main__.py
@tests/test_doctor.py

<interfaces>
Current API:
```python
def run_doctor(cwd: Path, *, dry_run: bool = False) -> DoctorReport: ...
def format_report(report: DoctorReport) -> str: ...
```
`DoctorReport.resolved_output: ResolvedOutput | None` — populated via **`resolve_output(cwd_resolved)`** today (~L360–364). **Executor:** extend `run_doctor` to accept optional **`vault_cli` / `vault_list_path` / use env** mirroring **`resolve_execution_paths`** kwargs from Plan 01, OR accept **`resolved: ResolvedOutput | None`** precomputed by `__main__.py` (prefer **one** approach — avoid double resolution). **`format_report`** Vault Detection lines (**~461–466**) must print **explicit none** / **pinned path** / **`vault_detected`** consistent with **ResolvedOutput** from pin.

Dry-run preview (**~404–407**) calls **`detect(cwd_resolved)`** — if vault pin implies corpus cwd differs from `Path.cwd()`, align **`detect()` input** with **`run`** target rules so **VCLI-04** skip reasons match **`graphify run`** for the same tree.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: run_doctor + format_report vault parity</name>
  <files>graphify/doctor.py</files>
  <read_first>graphify/doctor.py (`run_doctor`, `format_report`, `DoctorReport`); graphify/output.py (`resolve_execution_paths`)</read_first>
  <action>
    **Per D-05 / VCLI-03:** Replace naive **`is_obsidian_vault(cwd)` + `resolve_output(cwd)`** sequence with **`resolve_execution_paths`** using the same parameters Plan 02 passes on CLI (or accept injected **`ResolvedOutput`** from caller — pick one pattern and document). Update **`format_report`** **Vault Detection** section: when vault pinned, print **`vault_path`** from **`ResolvedOutput`** and **detection source** (e.g. `source=vault-cli`); never claim **"no Obsidian vault detected in CWD"** when a valid pin exists. **Profile preflight** must run against **`resolved.vault_path`** when present (`validate_profile_preflight(vault_root)`). **`_compute_would_self_ingest`** / ignore-list: use **effective paths** from resolved output + same cwd semantics as **`run`**. **Per D-06 / VCLI-04:** Ensure **`dry_run=True`** preview attaches **after** successful resolution only (unchanged guard); **`PreviewSection`** notes/artifacts lines match **resolved** pins. Add **`tests/test_doctor.py`** (extend if exists) with **`tmp_path`** vault + **`run_doctor(..., )`** kwargs asserting stderr sections contain **`notes_dir:`** / **`source:`** matching pinned vault.
  </action>
  <acceptance_criteria>Doctor output strings stable except intentional vault-pin additions; existing doctor tests updated or preserved.</acceptance_criteria>
  <verify>
    <automated>pytest tests/test_doctor.py -q</automated>
  </verify>
  <done>VCLI-03 satisfied: doctor banner matches resolver for pinned vault.</done>
</task>

<task type="auto">
  <name>Task 2: __main__.py doctor argparse + dry-run alignment</name>
  <files>graphify/__main__.py</files>
  <read_first>graphify/__main__.py (`elif cmd == "doctor"` ~L2567+); Plan 02 global vault extraction helper</read_first>
  <action>
    Extend **`graphify doctor`** ArgumentParser (or pre-scan argv like Plan 02) so **`doctor`** receives **global `--vault` / `--vault-list` / env** the same way as **`run`**. Build **`resolved = resolve_execution_paths(...)`** once then call **`run_doctor(..., dry_run=opts.dry_run)`** with that resolution **without re-parsing vault inside doctor** if Task 1 uses injection pattern. **Print no second conflicting banner** — `format_report` remains single user-facing surface. **Closure checklist for VCLI-04:** (1) After Plan 02 merge, **`rg 'resolve_output\\(' graphify/__main__.py`** — every remaining call either stays on non-vault paths or is wrapped; **`doctor`**, **`--obsidian`**, **`elicit`**, **`import-harness`**, **`run`** must use **`resolve_execution_paths`** (or receive an injected **`ResolvedOutput`** from it). (2) Add regression test: **`graphify doctor --dry-run`** with **`GRAPHIFY_VAULT`** set shows **`notes_dir:`** / **`artifacts_dir:`** substrings equal to **`resolve_execution_paths`** for the same argv+env (golden strings from one helper in tests).
  </action>
  <acceptance_criteria>Doctor and dry-run preview paths share ResolvedOutput with run/--obsidian.</acceptance_criteria>
  <verify>
    <automated>pytest tests/test_doctor.py tests/test_vault_cli.py -q</automated>
  </verify>
  <done>VCLI-04 satisfied; no divergent phantom vault in preview sections.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| doctor ← user paths | Same `--vault` / `--vault-list` as Plan 02 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-41-06 | I | doctor misreports vault | mitigate | Single `ResolvedOutput` from validated resolver (Plans 01–02) |
| T-41-07 | E | dry-run leaks paths | accept | Preview already lists paths; same as today’s doctor dry-run |
</threat_model>

<verification>
`pytest tests/test_doctor.py tests/test_vault_cli.py -q`
</verification>

<success_criteria>
**VCLI-03** and **VCLI-04** verified by pytest; doctor visually consistent with resolver banners (spot-check optional).
</success_criteria>

<output>
After completion, create `.planning/phases/41-vault-cli-vault-flag-multi-vault-selector/41-03-SUMMARY.md`
</output>
