---
phase: 41-vault-cli-vault-flag-multi-vault-selector
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - graphify/output.py
  - tests/test_output.py
autonomous: true
requirements:
  - VCLI-01
  - VCLI-02
  - VCLI-05
must_haves:
  truths:
    - "A single public resolver composes explicit vault, GRAPHIFY_VAULT, --vault-list, and CWD auto-detect with documented precedence (D-01, D-03)."
    - "Invalid or traversal user paths for vault pins are rejected before load_profile: use graphify.profile.validate_vault_path (and the same path confinement tests as today’s profile output paths) so user-controlled `..` / symlink escapes fail closed."
    - "Unit tests in test_output.py cover precedence, list parsing, and tmp_path-only vault fixtures (D-07)."
  artifacts:
    - path: "graphify/output.py"
      provides: "resolve_execution_paths (or equivalent) delegating to resolve_output with effective vault root + extended source labels"
      contains: "resolve_execution_paths|ResolvedOutput"
    - path: "tests/test_output.py"
      provides: "Matrix tests for vault flag > env > list > CWD and refusal cases"
      contains: "test_"
  key_links:
    - from: "graphify/output.py"
      to: "graphify.profile.validate_vault_path"
      via: "validate user-supplied vault roots before treat-as-vault"
      pattern: "validate_vault_path"
    - from: "resolve_execution_paths"
      to: "resolve_output"
      via: "effective CWD = pinned vault when applicable; pass cli_output through unchanged (D-02)"
      pattern: "resolve_output\\("
---

<objective>
Add a **library-level execution path resolver** that implements **VCLI-01** (precedence: `--vault` / explicit pin > `GRAPHIFY_VAULT` > ordered `--vault-list` entries > existing `resolve_output(cwd, cli_output=...)` CWD behavior) and **VCLI-02** selection semantics for **non-interactive** resolution (list file, env). Keep **`--output` composable** with vault pin per **D-02**. Emit **one stderr precedence line** when a pin overrides auto-detect, matching **D-09** tone in `output.py`. Extend **`ResolvedOutput.source`** (or an adjacent field if required) so **doctor** can print `profile` / `cli-flag` / `default` / **vault pin kinds** per **D-05**.

Purpose: All CLI commands can call one API without forking Phase 27 resolution logic.

Output: `graphify/output.py` changes + **VCLI-05** coverage in `tests/test_output.py` (no `test_vault_cli` here — that is Plan 02).
</objective>

<execution_context>
@$HOME/.cursor/get-shit-done/workflows/execute-plan.md
@$HOME/.cursor/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/41-vault-cli-vault-flag-multi-vault-selector/41-CONTEXT.md
@.planning/phases/41-vault-cli-vault-flag-multi-vault-selector/41-RESEARCH.md
@.planning/phases/41-vault-cli-vault-flag-multi-vault-selector/41-PATTERNS.md
@graphify/output.py
@graphify/profile.py
@tests/test_output.py

<interfaces>
From `graphify/output.py` (baseline — extend, do not break callers unintentionally):
```python
class ResolvedOutput(NamedTuple):
    vault_detected: bool
    vault_path: Path | None
    notes_dir: Path
    artifacts_dir: Path
    source: Literal["profile", "cli-flag", "default"]
    exclude_globs: tuple[str, ...] = ()

def resolve_output(cwd: Path, *, cli_output: str | None = None) -> ResolvedOutput: ...
def is_obsidian_vault(path: Path) -> bool: ...
```
Executor: add **`resolve_execution_paths(...)`** (name exact per **41-RESEARCH** unless a clearer alias is documented in module docstring) with kwargs: `cwd`, `cli_output`, `explicit_vault` (`Path | None`), `env_vault` (`str | None` from `os.environ.get("GRAPHIFY_VAULT")`), `vault_list_file` (`Path | None`). **Interactive multi-candidate selection is NOT in this plan** — return a structured **NeedsSelection** or raise/return sentinel consumed by Plan 02 only if RESEARCH requires splitting; prefer **raising a dedicated exception** with candidate paths for CLI layer (document choice in `output.py` docstring).
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: resolve_execution_paths + ResolvedOutput source extension</name>
  <files>graphify/output.py</files>
  <read_first>graphify/output.py (module docstring D-*), graphify/profile.py (`validate_vault_path`)</read_first>
  <behavior>
    - Calling `resolve_execution_paths(tmp_path, explicit_vault=vault_a.resolve(), cli_output=None)` yields the same notes/artifacts semantics as `resolve_output(vault_a, ...)` for profile-driven vault layout when `vault_a` has `.obsidian` + valid profile (per D-01).
    - Precedence: resolved explicit vault path beats `GRAPHIFY_VAULT` beats first valid line from `--vault-list` file beats CWD `resolve_output` (VCLI-01).
    - When only `GRAPHIFY_VAULT` is set (valid vault), `source` reflects vault-env pin (VCLI-02).
    - Traversal / invalid vault paths for pins call existing validators and refuse consistently (SECURITY.md).
  </behavior>
  <action>
    Implement **`resolve_execution_paths`** per **41-RESEARCH** facade. **D-01:** Normalize paths with `Path.resolve()`; when explicit `--vault` (passed as `explicit_vault`) overrides CWD detection, print **exactly one** `[graphify]` stderr line mirroring **D-09** style. **D-02:** Thread `cli_output` into inner `resolve_output` so `--output` still wins over profile paths. **D-03 (non-TTY parts):** Parse `--vault-list` file as newline-separated roots; skip blanks and `#` comments if documented; take **first** path that `is_obsidian_vault` after validate (exact algorithm per **41-CONTEXT** bounded discovery). Extend **`ResolvedOutput.source`** `Literal` to include distinct vault-pin sources (e.g. `vault-cli`, `vault-env`, `vault-list` — names fixed in code + docstring) so **VCLI-03** can print them. **Do not** add interactive input here. Reuse lazy imports pattern from `resolve_output` to avoid cycles.
  </action>
  <acceptance_criteria>Public resolver exists; `resolve_output` callers unchanged or shimmed; source distinguishes vault pins.</acceptance_criteria>
  <verify>
    <automated>pytest tests/test_output.py -q</automated>
  </verify>
  <done>New API exported; module docstring lists precedence order verbatim for help/docs alignment (VCLI-01).</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: tmp_path matrix tests (VCLI-05)</name>
  <files>tests/test_output.py</files>
  <read_first>tests/test_output.py (`_setup_vault`, stderr assertion patterns)</read_first>
  <action>
    Add tests using **`tmp_path` only**: (1) two vaults — pin **vault B** via `explicit_vault` while CWD is non-vault proves **CWD does not govern** when pin wins (**VCLI-01**); (2) **`GRAPHIFY_VAULT`** alone selects vault when CWD is not a vault (**VCLI-02**); (3) **`vault_list_file`** with two lines picks first existing valid vault; (4) invalid pin path produces actionable stderr/refusal; (5) precedence when env + explicit vault both set — explicit wins (**D-01**). Follow **`capsys`** stderr patterns from `test_resolve_output_cli_flag_overrides_profile_emits_stderr`. Import `pytest.importorskip("yaml")` where profile loads apply.
  </action>
  <acceptance_criteria>All new tests pass without network or writes outside tmp_path.</acceptance_criteria>
  <verify>
    <automated>pytest tests/test_output.py -q</automated>
  </verify>
  <done>VCLI-05 satisfied at resolver layer; failures give clear messages for Plan 02 CLI wiring.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| User → resolver | `--vault`, list file paths, env strings are untrusted local paths |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-41-01 | I | `explicit_vault` / list paths | mitigate | `validate_vault_path` / profile validators; reject `..` escapes per SECURITY.md |
| T-41-02 | E | `vault_list_file` | mitigate | Open only after path confined to allowed roots; reject symlink tricks consistent with `profile` |
| T-41-03 | S | stderr precedence lines | accept | Informational only; no secrets |
</threat_model>

<verification>
`pytest tests/test_output.py -q`
</verification>

<success_criteria>
**VCLI-01**, **VCLI-02** (library subset), and **VCLI-05** resolver tests green; single resolver entry point ready for `__main__.py` (Plan 02) and `doctor` (Plan 03).
</success_criteria>

<output>
After completion, create `.planning/phases/41-vault-cli-vault-flag-multi-vault-selector/41-01-SUMMARY.md`
</output>
