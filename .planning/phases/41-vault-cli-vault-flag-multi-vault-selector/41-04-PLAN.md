---
phase: 41-vault-cli-vault-flag-multi-vault-selector
plan: 04
type: execute
wave: 4
depends_on:
  - 03
files_modified:
  - README.md
  - graphify/__main__.py
autonomous: true
requirements:
  - VCLI-06
must_haves:
  truths:
    - "README contains a short subsection **Vault selection for scripting** covering `--vault`, `GRAPHIFY_VAULT`, and `--vault-list` with precedence summary (D-08)."
    - "`graphify --help` (top-level help text) lists vault flags in one discoverable block/table per D-08."
    - "Help prose matches implemented precedence from `output.py` docstring (single source of truth)."
  artifacts:
    - path: "README.md"
      provides: "Scripting-oriented vault selection subsection"
      contains: "GRAPHIFY_VAULT|--vault"
    - path: "graphify/__main__.py"
      provides: "Updated `main()` help print strings for global vault flags"
      contains: "--vault|GRAPHIFY_VAULT|vault-list"
  key_links:
    - from: "README.md"
      to: "graphify/output.py"
      via: "Cross-link precedence to module docstring"
      pattern: "precedence|GRAPHIFY_VAULT"
---

<objective>
Document **vault selection for scripting users** (**VCLI-06**): update **`README.md`** with a concise subsection (**D-08**) and extend **top-level `graphify --help`** output in **`graphify/__main__.py`** so **`--vault`**, **`GRAPHIFY_VAULT`**, and **`--vault-list`** appear **in one table or grouped block** with **explicit precedence** (`--vault` > env > list > CWD auto-detect) matching **Plan 01** docstring.

Purpose: Users and CI authors discover flags without reading source.

Output: README + help strings only; no new runtime logic.
</objective>

<execution_context>
@$HOME/.cursor/get-shit-done/workflows/execute-plan.md
@$HOME/.cursor/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/41-vault-cli-vault-flag-multi-vault-selector/41-CONTEXT.md
@README.md
@graphify/__main__.py (`def main` help ~L1196+)
@graphify/output.py (final precedence text from Plans 01–03)

<interfaces>
Executor: copy **exact precedence bullet order** from **`graphify/output.py` module docstring** after Plan 03 merge — do not invent alternate ordering.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: README Vault selection for scripting</name>
  <files>README.md</files>
  <read_first>README.md (structure); SECURITY.md path notes if vault paths mentioned</read_first>
  <action>
    Add subsection **Vault selection for scripting** (or equivalent H2/H3 per README style): document **`--vault <path>`**, **`GRAPHIFY_VAULT`**, **`--vault-list <file>`** (newline-separated roots), **non-TTY multi-root exit code 2**, and **TTY interactive prompt** summary (**D-03**). Link **`docs/vault-adapter.md`** if present. State **`--output` composes with vault pin** (**D-02**). Keep subsection **under ~40 lines**.
  </action>
  <acceptance_criteria>README renders on GitHub; no broken internal links.</acceptance_criteria>
  <verify>
    <automated>python -c "from pathlib import Path; t=Path('README.md').read_text(encoding='utf-8'); assert 'GRAPHIFY_VAULT' in t and '--vault' in t and ('Vault selection' in t or 'vault selection' in t.lower())"</automated>
  </verify>
  <done>VCLI-06 README portion satisfied (**D-08**).</done>
</task>

<task type="auto">
  <name>Task 2: Top-level --help vault flag table</name>
  <files>graphify/__main__.py</files>
  <read_first>graphify/__main__.py (help prints ~L1196–1300); Plans 01–03 finalized flag names</read_first>
  <action>
    In **`main()`** early help branch (`-h` / `--help` / no args), insert a **single grouped block** (table or bullet list) documenting **global** vault options applying to **`run`**, **`--obsidian`**, **`doctor`**, **`elicit`**, **`import-harness`** (list actual wired commands from Plan 02). Include **`GRAPHIFY_VAULT`** env var name. Match **precedence** text with **`output.py`**. If **`run` subgroup** help exists separately, add cross-reference line only if already patterned in file — **do not** duplicate 200 lines of help.
  </action>
  <acceptance_criteria>Users running `graphify --help` see vault flags without reading README.</acceptance_criteria>
  <verify>
    <automated>python -m graphify --help 2>&1 | python -c "import sys; t=sys.stdin.read(); assert 'GRAPHIFY_VAULT' in t and '--vault' in t and ('vault-list' in t or '--vault-list' in t)"</automated>
  </verify>
  <done>VCLI-06 CLI help portion satisfied.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Help/README → user | Documentation accuracy |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-41-08 | I | misleading precedence | mitigate | Copy ordering verbatim from `output.py` / Plans 01 docstring |
| T-41-09 | I | env var name typo | mitigate | Grep `GRAPHIFY_VAULT` in code and help in same PR |
</threat_model>

<verification>
`python -m graphify --help 2>&1 | head -80` (manual spot OK) **and** README assertion one-liner from Task 1.
</verification>

<success_criteria>
**VCLI-06** complete; help and README agree with implementation.
</success_criteria>

<output>
After completion, create `.planning/phases/41-vault-cli-vault-flag-multi-vault-selector/41-04-SUMMARY.md`
</output>
