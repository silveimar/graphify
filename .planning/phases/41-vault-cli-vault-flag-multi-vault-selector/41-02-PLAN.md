---
phase: 41-vault-cli-vault-flag-multi-vault-selector
plan: 02
type: execute
wave: 2
depends_on:
  - 01
files_modified:
  - graphify/__main__.py
  - tests/test_vault_cli.py
autonomous: true
requirements:
  - VCLI-01
  - VCLI-02
  - VCLI-05
must_haves:
  truths:
    - "Global argv pre-scan (or parse_known_args) provides `--vault`, `--vault-list`, and reads `GRAPHIFY_VAULT` once for `run`, `--obsidian`, `elicit`, `import-harness`, and other resolve_output call sites."
    - "TTY shows interactive multi-vault choice only when required by D-03; non-TTY exits 2 with candidate list."
    - "Subcommand-local `--vault` (e.g. `approve`, `init-diagram-templates`) overrides global with one stderr note per 41-RESEARCH."
  artifacts:
    - path: "graphify/__main__.py"
      provides: "Early extraction of vault flags; threading into resolve_execution_paths"
      contains: "resolve_execution_paths|GRAPHIFY_VAULT"
    - path: "tests/test_vault_cli.py"
      provides: "CliRunner/subprocess tests for argv matrix and exit codes"
      contains: "test_"
  key_links:
    - from: "graphify/__main__.py"
      to: "graphify.output.resolve_execution_paths"
      via: "replace bare resolve_output(Path.cwd()) where vault-aware"
      pattern: "resolve_execution_paths"
    - from: "`run` command"
      to: "load_profile"
      via: "profile loads from resolved vault root when pinned, not blind Path.cwd()"
      pattern: "load_profile\\("
---

<objective>
Wire **global `--vault`**, **`--vault-list <file>`**, and **`GRAPHIFY_VAULT`** into **`graphify/__main__.py`** for every command that currently calls **`resolve_output(Path.cwd(), ...)`** — at minimum **`run`**, **`--obsidian`**, **`elicit`**, **`import-harness`** per **41-CONTEXT**. Implement **VCLI-02** interactive vs non-TTY behavior and **subcommand-local `--vault` overrides global** with documented precedence (41-RESEARCH). Add **`tests/test_vault_cli.py`** for integration-level **VCLI-05** coverage.

Purpose: Scriptable, CI-safe CLI with one consistent resolution path (**VCLI-01** end-to-end).

Output: Patched `__main__.py` + new test module; no changes to `doctor` dry-run banner here (Plan 03).
</objective>

<execution_context>
@$HOME/.cursor/get-shit-done/workflows/execute-plan.md
@$HOME/.cursor/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/41-vault-cli-vault-flag-multi-vault-selector/41-CONTEXT.md
@.planning/phases/41-vault-cli-vault-flag-multi-vault-selector/41-RESEARCH.md
@graphify/__main__.py
@graphify/output.py
@.planning/phases/41-vault-cli-vault-flag-multi-vault-selector/41-01-PLAN.md

<interfaces>
Baseline call sites to refactor (grep in executor session):
- `cmd == "--obsidian"`: `resolve_output(Path.cwd(), cli_output=cli_output)` (~L1435–1436).
- `cmd == "elicit"`: `resolve_output(Path.cwd(), cli_output=opts.output)` (~L2230).
- `cmd == "import-harness"`: `resolve_output(Path.cwd(), cli_output=opts.output)` (~L2366).
- `cmd == "run"`: `resolve_output(Path.cwd(), cli_output=cli_output)` (~L2422) + `load_profile(Path.cwd())` / corpus target rules (~L2425–2431) — **must** use **resolved vault root** for profile when pinned (**D-01**).

Add helper e.g. **`_extract_global_vault_args(argv: list[str]) -> tuple[VaultGlobals, list[str]]`** stripping `--vault`, `--vault=` , `--vault-list`, `--vault-list=` from argv copy before subcommand dispatch; **run before** `cmd = sys.argv[1]` cannot consume global flags after split — follow **41-RESEARCH** (`parse_known_args` / pre-scan). Document chosen approach in plan SUMMARY.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Global vault argv extraction + thread through resolve_execution_paths</name>
  <files>graphify/__main__.py</files>
  <read_first>graphify/__main__.py (`--obsidian`, `run`, `elicit`, `import-harness` branches); graphify/output.py (`resolve_execution_paths` from Plan 01)</read_first>
  <action>
    **Per D-01 / VCLI-01:** Implement early extraction of `--vault <path>`, `--vault-list <file>`, and read **`os.environ.get("GRAPHIFY_VAULT")`**. Call **`resolve_execution_paths`** with `cwd=Path.cwd()`, `cli_output` from existing parsers, `explicit_vault`, `env_vault`, `vault_list_file`. **Per D-02:** preserve `--output` / `--obsidian-dir` interaction exactly as today for `--obsidian`. **Per 41-RESEARCH:** when a subcommand also accepts `--vault` (e.g. `approve`, `init-diagram-templates`), **local `--vault` wins** over global; emit one `[graphify]` stderr line when override happens. **Per D-03:** if resolver signals multiple candidates and no pin, **`sys.stderr.isatty()`** true → prompt user with numbered paths; false → **`sys.exit(2)`** after printing all candidates (no prompt). **run** branch: replace **`load_profile(Path.cwd())`** with **`load_profile(resolved.vault_path or Path.cwd())`** so `.graphify/profile.yaml` is read from the **pinned vault root** whenever `resolved.vault_path` is set; **corpus file discovery** for `run` keeps using **`Path.cwd()`** as today unless a separate requirement says otherwise (do not regress **`tests/test_output.py`** / existing run tests). Add a **`test_vault_cli`** assertion that the **output block path/mode** in diagnostics matches the **pinned** vault’s profile, not CWD’s.
  </action>
  <acceptance_criteria>All listed commands use Plan 01 resolver; no duplicate resolution logic in __main__.</acceptance_criteria>
  <verify>
    <automated>pytest tests/test_vault_cli.py tests/test_output.py -q</automated>
  </verify>
  <done>VCLI-01 and VCLI-02 exercised through CLI entrypoints (non-doctor).</done>
</task>

<task type="auto">
  <name>Task 2: tests/test_vault_cli.py integration coverage</name>
  <files>tests/test_vault_cli.py</files>
  <read_first>tests/conftest.py (if any); tests/test_output.py subprocess patterns if present</read_first>
  <action>
    Create **`tests/test_vault_cli.py`** using **`tmp_path`**, building minimal vaults (`.obsidian/` + minimal `.graphify/profile.yaml` with `output:` block — copy from `test_output.py` fixtures). Use **`subprocess.run`** with `env={**os.environ, "GRAPHIFY_VAULT": ...}` and `-C` / `cwd=` to simulate CWD separate from vault. Cases: (1) **`graphify run`** (or lightest command invoking resolver) with **`--vault`** picks profile from pinned vault; (2) env pin without `--vault`; (3) non-TTY multi-candidate yields exit code **2** (monkeypatch `sys.stderr.isatty` return False in-process test or subprocess without pty); (4) help does not error (full assertions in Plan 04). Keep each test under ~60s.
  </action>
  <acceptance_criteria>VCLI-05 extended to CLI; tests pass on Python 3.10 and 3.12 CI matrix.</acceptance_criteria>
  <verify>
    <automated>pytest tests/test_vault_cli.py -q</automated>
  </verify>
  <done>Integration tests green; failures print stderr for debugging.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| argv → Path | Global `--vault` / `--vault-list` strings become paths crossing trust boundary |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-41-04 | I | argv path injection | mitigate | Resolver uses `validate_vault_path`; refuse traversal (Plan 01) |
| T-41-05 | T | env override | accept | Local user controls `GRAPHIFY_VAULT`; document in README (Plan 04) |
</threat_model>

<verification>
`pytest tests/test_vault_cli.py tests/test_output.py -q`
</verification>

<success_criteria>
Main CLI paths resolve vault identically to library tests; **VCLI-01**, **VCLI-02**, **VCLI-05** satisfied for executable wiring.
</success_criteria>

<output>
After completion, create `.planning/phases/41-vault-cli-vault-flag-multi-vault-selector/41-02-SUMMARY.md`
</output>
