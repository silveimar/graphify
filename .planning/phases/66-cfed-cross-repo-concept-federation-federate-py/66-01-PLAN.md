---
phase: 66-cfed-cross-repo-concept-federation-federate-py
plan: 01
type: tdd
wave: 1
depends_on: []
files_modified:
  - graphify/federate.py
  - tests/test_federate.py
  - tests/fixtures/peer_match/graph.json
  - tests/fixtures/peer_nomerge/graph.json
  - tests/fixtures/peer_collision_a/graph.json
  - tests/fixtures/peer_collision_b/graph.json
autonomous: true
requirements: [CFED-02, CFED-03]
must_haves:
  truths:
    - "federate.py exposes a deterministic pure function that takes a local extraction dict + list of peer graph.json paths and returns a merged extraction dict"
    - "All node ids (local AND peer) are namespaced as {repo}::{id} before merge logic runs"
    - "AND-gate: a merge happens iff label-equality (case-folded) AND neighborhood-label Jaccard ≥ 0.5 AND ≥1 shared source_file basename"
    - "When two candidates compete for the same target, tiebreaker = mean confidence_score across contributing INFERRED neighborhood edges"
    - "Canonical merged_id = lex-min of contributing namespaced ids (deterministic)"
    - "Two runs over identical inputs produce byte-identical output dicts (sorted keys, stable iteration)"
    - "Peer basename collision raises a ValueError carrying the Phase 64 two-line stderr message"
  artifacts:
    - path: "graphify/federate.py"
      provides: "federate(extraction, peers) merge engine + manifest builder"
      exports: ["federate", "build_manifest", "FederationCollisionError"]
    - path: "tests/test_federate.py"
      provides: "RED tests for namespace, AND-gate (4 cases), tiebreaker, canonical-id, collision, determinism"
    - path: "tests/fixtures/peer_match/graph.json"
      provides: "Peer artifact passing all three signals"
    - path: "tests/fixtures/peer_nomerge/graph.json"
      provides: "Peer with label match but Jaccard < 0.5"
    - path: "tests/fixtures/peer_collision_a/graph.json"
      provides: "Peer A under directory whose parent basename collides with peer B"
    - path: "tests/fixtures/peer_collision_b/graph.json"
      provides: "Peer B under colliding parent basename"
  key_links:
    - from: "tests/test_federate.py"
      to: "graphify/federate.py::federate"
      via: "direct import"
      pattern: "from graphify.federate import federate"
---

<objective>
Build the deterministic federation merge engine as a pure function with full test coverage.
Purpose: Lock the AND-gate, namespacing, tiebreaker, and canonical-id semantics from D-66.4 / D-66.7
behind tests before any pipeline wiring lands.
Output: graphify/federate.py + tests/test_federate.py + 4 peer fixture graph.json files.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/66-cfed-cross-repo-concept-federation-federate-py/66-CONTEXT.md
@.planning/phases/66-cfed-cross-repo-concept-federation-federate-py/66-RESEARCH.md
@.planning/phases/66-cfed-cross-repo-concept-federation-federate-py/66-VALIDATION.md
@graphify/validate.py
@graphify/export.py

<interfaces>
Peer artifact = `graphify-out/graph.json` (per D-66.2). Each peer file is the JSON dump produced
by `graphify.export.to_json` and conforms to the standard extraction schema:

```json
{
  "nodes": [{"id": "...", "label": "...", "file_type": "code|document|...", "source_file": "...", "source_location": "..."}],
  "edges": [{"source": "...", "target": "...", "relation": "...", "confidence": "EXTRACTED|INFERRED|AMBIGUOUS", "source_file": "...", "confidence_score": 0.0..1.0}]
}
```

Use `graphify.validate.validate_extraction_for_read` (legacy-tolerant) when reading peers.

Manifest entry schema (D-66.5, verbatim):

```json
{
  "merged_id": "<namespaced canonical id>",
  "contributing": [
    {"repo": "<label>", "original_id": "...", "label": "...", "source_files": ["..."]}
  ],
  "signals": {
    "label_match": "<the matched label>",
    "neighborhood_jaccard": 0.67,
    "shared_basenames": ["token.py", "auth.py"]
  },
  "tiebreaker_score": 0.82
}
```

`tiebreaker_score` present only when tiebreaker fired.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1 (Wave 0): Create peer fixtures and failing test scaffold</name>
  <files>tests/fixtures/peer_match/graph.json, tests/fixtures/peer_nomerge/graph.json, tests/fixtures/peer_collision_a/graph.json, tests/fixtures/peer_collision_b/graph.json, tests/test_federate.py</files>
  <read_first>
    - .planning/phases/66-cfed-cross-repo-concept-federation-federate-py/66-CONTEXT.md (D-66.2..D-66.5)
    - .planning/phases/66-cfed-cross-repo-concept-federation-federate-py/66-VALIDATION.md (per-task verification map rows 03-13)
    - tests/test_build.py (style reference for fixture-based tests)
    - graphify/export.py (to_json output shape)
  </read_first>
  <action>
    Create 4 peer `graph.json` fixtures (stdlib-json-dumpable dicts with `{"nodes": [...], "edges": [...]}`):

    1. `tests/fixtures/peer_match/graph.json` — peer with concept node label "TokenValidator" whose 1-hop neighbor labels overlap ≥0.5 with the local fixture's TokenValidator neighbors AND shares a source_file basename (e.g. `auth.py`). Include at least one INFERRED edge with `confidence_score: 0.85` to drive the tiebreaker test.
    2. `tests/fixtures/peer_nomerge/graph.json` — same label "TokenValidator" but disjoint neighborhood (Jaccard = 0). No basename overlap.
    3. `tests/fixtures/peer_collision_a/graph.json` and `tests/fixtures/peer_collision_b/graph.json` — both placed so their parent directory's basename collides (e.g. both under `tests/fixtures/peer_collision_a/<X>/graph.json` and `tests/fixtures/peer_collision_b/<X>/graph.json` where `<X>` resolves to identical basename when the federate caller computes `basename(parent_of_PATH)` per D-66.3).

    Then create `tests/test_federate.py` with the following RED tests (all expected to fail until Task 2 lands). Use `from graphify.federate import federate, build_manifest, FederationCollisionError`:

    - `test_namespace`: assert all node ids in returned dict are prefixed with `{repo}::` for both local and peer contributors.
    - `test_gate_all_pass`: peer_match fixture → exactly 1 merge, merged_id == lex-min of contributing namespaced ids.
    - `test_gate_label_fail`, `test_gate_jaccard_fail`, `test_gate_basename_fail`: each isolates one signal failing → 0 merges.
    - `test_tiebreaker`: two peer concepts both pass AND-gate against same local target; assert winner = higher mean confidence_score, manifest entry contains `tiebreaker_score`.
    - `test_canonical_id`: lex-min selection deterministic.
    - `test_collision_error`: `federate(local, [peer_collision_a/.../graph.json, peer_collision_b/.../graph.json])` raises `FederationCollisionError` whose `str()` matches Phase 64 two-line snapshot: `"[graphify] error: --federate-with paths share repo basename '{name}'\\n  hint: rename a peer directory or pass distinct parents"`.
    - `test_manifest_deterministic`: build_manifest twice on same merge → byte-identical JSON (sorted keys, separators=(",", ": ")).
    - `test_manifest_schema`: manifest entry has exactly the keys from D-66.5; `tiebreaker_score` absent when tiebreaker did not fire.
    - `test_no_new_deps`: AST-scan `graphify/federate.py` (use `ast.parse` + walk Import/ImportFrom) and assert imports are subset of {`__future__`, `json`, `pathlib`, `hashlib`, `os`, `tempfile`, `typing`, `graphify.validate`, `graphify.security`}.

    Run pytest. RED commit: `test(66-01): add failing federation engine tests + peer fixtures`.
  </action>
  <verify>
    <automated>pytest tests/test_federate.py -x 2>&1 | grep -E "(FAILED|ModuleNotFoundError|ImportError)" | head -1</automated>
  </verify>
  <acceptance_criteria>
    - `tests/test_federate.py` exists and contains at least 10 test functions matching VALIDATION map rows 03-13
    - `pytest tests/test_federate.py -x` exits non-zero (RED) with `ModuleNotFoundError: graphify.federate` or AttributeError on missing exports
    - `tests/fixtures/peer_match/graph.json`, `peer_nomerge/graph.json`, `peer_collision_a/graph.json`, `peer_collision_b/graph.json` all exist and parse via `python -c "import json; json.load(open('<path>'))"`
    - Each fixture validates via `python -c "from graphify.validate import validate_extraction_for_read; import json; assert validate_extraction_for_read(json.load(open('<path>'))) == []"`
    - Git commit message starts with `test(66-01):`
  </acceptance_criteria>
  <done>RED state committed; tests run and fail as expected.</done>
</task>

<task type="tdd" tdd="true">
  <name>Task 2 (GREEN): Implement federate.py merge engine</name>
  <files>graphify/federate.py</files>
  <read_first>
    - tests/test_federate.py (the contract we must satisfy)
    - .planning/phases/66-cfed-cross-repo-concept-federation-federate-py/66-CONTEXT.md (D-66.3, D-66.4, D-66.5, D-66.7)
    - graphify/validate.py (use validate_extraction_for_read)
    - graphify/security.py (path confinement helpers; do NOT introduce new deps)
  </read_first>
  <behavior>
    - `federate(extraction: dict, peers: list[Path], local_repo: str) -> dict` returns a NEW extraction dict (does not mutate input).
    - All node ids in the result are namespaced `{repo}::{original_id}`. All edges have `source`/`target` rewritten to namespaced ids.
    - For each (local_concept, peer_concept) candidate pair where both are concept-typed nodes, AND-gate:
        label_ok = local.label.casefold() == peer.label.casefold()
        nbrs_local = {n.label.casefold() for n in 1hop(local) on label edges}
        nbrs_peer  = {n.label.casefold() for n in 1hop(peer)  on label edges}
        jaccard_ok = (len(nbrs_local & nbrs_peer) / max(1, len(nbrs_local | nbrs_peer))) >= 0.5
        bnames_local = {basename(sf) for sf in local.source_files}
        bnames_peer  = {basename(sf) for sf in peer.source_files}
        basename_ok = len(bnames_local & bnames_peer) >= 1
        merge = label_ok and jaccard_ok and basename_ok
    - On competing candidates targeting the same local concept, tiebreaker = mean of `confidence_score` across contributing INFERRED neighborhood edges; record `tiebreaker_score` in manifest entry only when ≥2 candidates competed.
    - Canonical merged_id = `min(contributing_namespaced_ids)` (lex-min, deterministic).
    - `build_manifest(merges: list) -> list[dict]` returns sorted-by-merged_id entries matching D-66.5 schema verbatim.
    - `FederationCollisionError(ValueError)` raised when two peers' computed repo labels are equal; message uses the Phase 64 two-line format from Task 1's `test_collision_error`.
    - Module imports limited to: `__future__`, `json`, `pathlib`, `hashlib`, `os`, `tempfile`, `typing`, `graphify.validate`, `graphify.security`.
    - File header: `from __future__ import annotations` then module docstring.
  </behavior>
  <action>
    Implement `graphify/federate.py` until all RED tests turn GREEN. 4-space indent, type hints on every function, `from __future__ import annotations` first.

    Repo label derivation: `repo_label_for_peer(peer_path: Path) -> str` returns `peer_path.parent.name` (basename of parent of the `--federate-with` path, per D-66.3) when peer_path points at `<repo>/graphify-out/graph.json`; if peer_path is the directory `graphify-out/`, use `peer_path.parent.name`.

    Iterate refactor only as needed for clarity — keep AND-gate and tiebreaker as small pure helpers (`_label_ok`, `_jaccard_ok`, `_basename_ok`, `_select_winner`).

    Final pytest run: `pytest tests/test_federate.py -x` exits 0. Commit: `feat(66-01): implement deterministic federation merge engine`.
  </action>
  <verify>
    <automated>pytest tests/test_federate.py -x</automated>
  </verify>
  <acceptance_criteria>
    - `pytest tests/test_federate.py -x` exits 0 (all GREEN)
    - `grep -c "def federate" graphify/federate.py` ≥ 1
    - `grep -c "from __future__ import annotations" graphify/federate.py` == 1
    - `python -c "import ast,sys; t=ast.parse(open('graphify/federate.py').read()); imps={n.module for n in ast.walk(t) if isinstance(n,ast.ImportFrom)} | {a.name for n in ast.walk(t) if isinstance(n,ast.Import) for a in n.names}; allowed={'__future__','json','pathlib','hashlib','os','tempfile','typing','graphify.validate','graphify.security'}; bad=imps - allowed; print('OK' if not bad else f'BAD: {bad}'); sys.exit(0 if not bad else 1)"` prints `OK`
    - `pytest tests/test_federate.py::test_no_new_deps tests/test_federate.py::test_manifest_deterministic tests/test_federate.py::test_collision_error -x` exits 0
    - Commit message starts with `feat(66-01):`
  </acceptance_criteria>
  <done>federate.py shipped, all engine-level tests pass, no new deps introduced.</done>
</task>

</tasks>

<verification>
- `pytest tests/test_federate.py -q` exits 0
- `pytest tests/ -q` exits 0 (no regressions in other suites)
- AST scan confirms zero new dependencies
</verification>

<success_criteria>
- All RED→GREEN tests committed with conventional commits
- federate.py satisfies D-66.4 AND-gate, D-66.5 manifest schema, D-66.7 namespacing/canonical-id rules
- Two consecutive `federate(...)` calls return byte-identical JSON
</success_criteria>

<output>
After completion, create `.planning/phases/66-cfed-cross-repo-concept-federation-federate-py/66-01-SUMMARY.md`
</output>
