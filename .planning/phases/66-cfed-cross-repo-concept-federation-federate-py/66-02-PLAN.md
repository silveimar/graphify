---
phase: 66-cfed-cross-repo-concept-federation-federate-py
plan: 02
type: tdd
wave: 2
depends_on: [66-01]
files_modified:
  - graphify/build.py
  - graphify/federate.py
  - tests/test_federate.py
  - tests/test_build.py
autonomous: true
requirements: [CFED-01, CFED-04]
must_haves:
  truths:
    - "build_from_json invokes federate() ONLY when peer paths are passed (default: federation never runs)"
    - "Federation step runs AFTER _normalize_concept_code_edges and BEFORE the graph is returned to cluster.py"
    - "federation-manifest.json is written atomically (temp + os.replace) under default_graphify_artifacts_dir()"
    - "Manifest path resolves through the vault-aware resolver — never hardcoded graphify-out/"
    - "Existing test suite passes unchanged when no peer paths are supplied (CFED-01 default-off)"
    - "Phase 53 normalized concept↔code edge contract is preserved post-federation"
  artifacts:
    - path: "graphify/build.py"
      provides: "Federation invocation point in build_from_json"
      contains: "federate("
    - path: "graphify/federate.py"
      provides: "write_manifest(entries, target_dir, resolved=None) atomic JSON writer"
      exports: ["write_manifest"]
  key_links:
    - from: "graphify/build.py"
      to: "graphify/federate.py::federate"
      via: "direct call after _normalize_concept_code_edges"
      pattern: "federate\\("
    - from: "graphify/federate.py::write_manifest"
      to: "graphify/output.py::default_graphify_artifacts_dir"
      via: "import"
      pattern: "default_graphify_artifacts_dir"
---

<objective>
Wire the federation engine into the build pipeline and add the atomic, vault-aware manifest writer.
Purpose: Satisfy CFED-04 (correct pipeline placement) and CFED-01 (default-off invariant) while
respecting Phase 63 vault-aware output routing.
Output: Modified `build_from_json` + `write_manifest()` in `federate.py` + tests proving default-off,
atomic write, and vault-aware path resolution.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/66-cfed-cross-repo-concept-federation-federate-py/66-CONTEXT.md
@.planning/phases/66-cfed-cross-repo-concept-federation-federate-py/66-VALIDATION.md
@.planning/phases/66-cfed-cross-repo-concept-federation-federate-py/66-01-SUMMARY.md
@graphify/build.py
@graphify/output.py

<interfaces>
From graphify/build.py (~L213-215):

```python
nodes_for_norm = [n for n in extraction["nodes"] if isinstance(n, dict)]
_normalize_concept_code_edges(nodes_for_norm, extraction["edges"])
# <-- federation insertion point goes here, BEFORE validate_extraction
errors = validate_extraction(extraction)
```

From graphify/output.py (L54+):

```python
def default_graphify_artifacts_dir(
    target: Path,
    *,
    resolved: ResolvedOutput | None = None,
) -> Path: ...
```

Manifest entry schema = D-66.5 (already implemented in Plan 01).
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1 (RED): Add pipeline + manifest tests</name>
  <files>tests/test_federate.py, tests/test_build.py</files>
  <read_first>
    - tests/test_build.py (style for build pipeline tests)
    - graphify/build.py (lines 200-225, the insertion site)
    - graphify/output.py (default_graphify_artifacts_dir signature)
    - .planning/phases/66-cfed-cross-repo-concept-federation-federate-py/66-VALIDATION.md (rows 01, 11, 12, 13)
  </read_first>
  <action>
    Add the following failing tests:

    In `tests/test_federate.py`:
    - `test_manifest_vault_aware(tmp_path, monkeypatch)`: monkeypatch `Path.cwd` to a tmp dir; call `write_manifest([entry], tmp_path)` (or its default-resolver overload) and assert the file is written under `<resolved>/federation-manifest.json` where `<resolved>` matches `default_graphify_artifacts_dir(...)`.
    - `test_manifest_atomic(tmp_path)`: simulate failure mid-write by monkey-patching `os.replace` to raise on first call; assert no `federation-manifest.json` exists at target path (i.e. caller catches and tmp file is cleaned up) — or if write succeeds, assert no leftover `*.tmp` siblings exist.
    - `test_pipeline_invokes_federate(tmp_path)`: build a synthetic `extraction` dict, monkeypatch `graphify.federate.federate` to a spy; call `graphify.build.build_from_json(extraction, peers=[tmp_path/"peer/graphify-out/graph.json"])` (peer fixture inline) and assert the spy was called exactly once with arguments where every node id is already namespaced post-call.
    - `test_pipeline_preserves_concept_code_normalization`: feed a fixture with duplicate concept↔code edges; run with peers=[]; assert the post-merge edge count equals the pre-Phase-53-norm count (i.e., normalization still happened).

    In `tests/test_build.py`:
    - `test_build_from_json_default_off`: call `build_from_json(extraction)` (no `peers` kwarg) on the existing fixtures and assert the function runs unchanged — same node count, same edge count as a baseline captured BEFORE Phase 66 changes (snapshot the values inline).

    Commit RED: `test(66-02): add failing tests for federation pipeline integration + atomic manifest`.
  </action>
  <verify>
    <automated>pytest tests/test_federate.py::test_manifest_vault_aware tests/test_federate.py::test_manifest_atomic tests/test_federate.py::test_pipeline_invokes_federate tests/test_build.py::test_build_from_json_default_off 2>&1 | grep -E "FAILED|ERROR" | head -1</automated>
  </verify>
  <acceptance_criteria>
    - Four new test functions added, all currently failing
    - `pytest tests/test_federate.py::test_manifest_atomic -x` exits non-zero
    - `grep -c "test_pipeline_invokes_federate" tests/test_federate.py` == 1
    - Commit message starts with `test(66-02):`
  </acceptance_criteria>
  <done>RED state for pipeline integration + manifest writer committed.</done>
</task>

<task type="tdd" tdd="true">
  <name>Task 2 (GREEN): Wire federate into build_from_json + atomic vault-aware manifest writer</name>
  <files>graphify/build.py, graphify/federate.py</files>
  <read_first>
    - graphify/build.py (full `build_from_json` function, around L195-260)
    - graphify/output.py (default_graphify_artifacts_dir + ResolvedOutput dataclass)
    - graphify/federate.py (existing engine from Plan 01)
    - tests/test_federate.py (new tests from Task 1)
  </read_first>
  <behavior>
    - `build_from_json(extraction, *, peers: list[Path] | None = None, resolved_output=None, ...)` runs federation when `peers` is a non-empty list; otherwise behaves identically to today.
    - Federation runs strictly between `_normalize_concept_code_edges(...)` (L214) and `validate_extraction(...)` (L215) — so peer-imported nodes pass schema validation alongside local nodes.
    - Manifest is written via new `write_manifest(entries, target, *, resolved=None)` in `federate.py`:
        * resolves dir via `default_graphify_artifacts_dir(target, resolved=resolved)`
        * writes to `<dir>/federation-manifest.json.tmp` then `os.replace(tmp, final)` (atomic)
        * `json.dumps(entries, indent=2, sort_keys=True, separators=(",", ": "))`
        * cleans up `.tmp` if `os.replace` raises
    - Default-off invariant: when `peers` is None or `[]`, neither `federate()` nor `write_manifest()` is called.
  </behavior>
  <action>
    Modify `graphify/build.py`:
    - Add `peers: list[Path] | None = None, resolved_output: ResolvedOutput | None = None, target_dir: Path | None = None` to `build_from_json` signature (keyword-only, defaults preserve current behavior).
    - Insert federation block immediately after `_normalize_concept_code_edges(...)` and before `validate_extraction(...)`:
      ```python
      if peers:
          from .federate import federate, build_manifest, write_manifest
          extraction, merges = federate(extraction, peers, local_repo=...)
          manifest = build_manifest(merges)
          write_manifest(manifest, target_dir or Path.cwd(), resolved=resolved_output)
      ```
    - Update `federate()` (Plan 01) so it returns `(merged_extraction, merges_list)` — this is a small refactor; update Plan 01 tests if needed (keep their assertions intact by adapting the call site).

    Add to `graphify/federate.py`:
    - `write_manifest(entries: list[dict], target: Path, *, resolved=None) -> Path` implementing atomic write through `default_graphify_artifacts_dir`.

    Run `pytest tests/test_federate.py tests/test_build.py -x`. Commit GREEN: `feat(66-02): wire federation into build pipeline + atomic vault-aware manifest writer`.
  </action>
  <verify>
    <automated>pytest tests/test_federate.py tests/test_build.py -x</automated>
  </verify>
  <acceptance_criteria>
    - `pytest tests/test_federate.py tests/test_build.py -x` exits 0
    - `pytest tests/ -q` exits 0 (no regressions; default-off proven)
    - `grep -n "from .federate import" graphify/build.py` shows the import inside the `if peers:` block (lazy)
    - `grep -c "def write_manifest" graphify/federate.py` == 1
    - `grep -c "default_graphify_artifacts_dir" graphify/federate.py` ≥ 1
    - `grep -c "os.replace" graphify/federate.py` ≥ 1
    - Commit message starts with `feat(66-02):`
  </acceptance_criteria>
  <done>Pipeline wired, manifest written atomically + vault-aware, full suite green.</done>
</task>

</tasks>

<verification>
- `pytest tests/ -q` exits 0
- `grep -n "if peers:" graphify/build.py` matches a line between `_normalize_concept_code_edges` and `validate_extraction`
- Manifest file appears at the resolved artifacts dir (not hardcoded `graphify-out/`) when run from a vault CWD
</verification>

<success_criteria>
- CFED-01 default-off proven by full suite passing with no peers passed
- CFED-04 placement proven by line-order check in build_from_json
- Atomic write verified by failure-injection test
- Vault-aware path verified by monkeypatched `Path.cwd()` test
</success_criteria>

<output>
After completion, create `.planning/phases/66-cfed-cross-repo-concept-federation-federate-py/66-02-SUMMARY.md`
</output>
