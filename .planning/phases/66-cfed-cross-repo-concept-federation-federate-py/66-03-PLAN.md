---
phase: 66-cfed-cross-repo-concept-federation-federate-py
plan: 03
type: execute
wave: 2
depends_on: [66-01, 66-02]
files_modified:
  - graphify/__main__.py
  - tests/test_federate.py
  - tests/test_stderr_contract.py
autonomous: true
requirements: [CFED-01]
must_haves:
  truths:
    - "`graphify federate --federate-with PATH` is a registered subcommand with action='append' so the flag is repeatable"
    - "Missing peer artifact emits the Phase 64 two-line stderr breadcrumb and exits non-zero"
    - "Repo basename collision emits the Phase 64 two-line stderr breadcrumb and exits non-zero"
    - "`graphify run` and existing entrypoints are unmodified (CFED-01 default-off plumbing)"
  artifacts:
    - path: "graphify/__main__.py"
      provides: "federate subcommand parser + handler"
      contains: "federate"
    - path: "tests/test_stderr_contract.py"
      provides: "Snapshot test extension for federation breadcrumbs"
  key_links:
    - from: "graphify/__main__.py"
      to: "graphify/build.py::build_from_json"
      via: "subcommand handler invokes build_from_json with peers list"
      pattern: "build_from_json.*peers="
---

<objective>
Add the `graphify federate` CLI subcommand with repeatable `--federate-with` and Phase 64 stderr
contract for missing peer / collision errors.
Purpose: CFED-01 user-visible opt-in surface; integrates with the engine from Plan 01 and pipeline
from Plan 02 without modifying default behavior of `graphify run`.
Output: New subcommand + extended stderr snapshot tests.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/66-cfed-cross-repo-concept-federation-federate-py/66-CONTEXT.md
@graphify/__main__.py
@tests/test_stderr_contract.py
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add `federate` subcommand and extend stderr snapshot</name>
  <files>graphify/__main__.py, tests/test_federate.py, tests/test_stderr_contract.py</files>
  <read_first>
    - graphify/__main__.py (existing argparse subcommand registration pattern; locate `subparsers.add_parser(...)` calls for `run`, `query`, `watch`)
    - tests/test_stderr_contract.py (existing two-line snapshot harness)
    - .planning/phases/64-audit-a-stderr-format-snapshot-lock-sweep/ (Phase 64 stderr contract — two lines, line 1 = `[graphify] error: ...`, line 2 = `  hint: ...`)
    - .planning/phases/66-cfed-cross-repo-concept-federation-federate-py/66-CONTEXT.md (D-66.1, D-66.2, D-66.3)
  </read_first>
  <behavior>
    - `graphify federate --federate-with PATH [--federate-with PATH ...]` parses, resolves peer paths, invokes `build_from_json(extraction, peers=[...])`.
    - `--federate-with` uses `action="append"` so it is repeatable.
    - When a peer's `graph.json` is missing/unreadable: emit two-line stderr `"[graphify] error: peer export not found at {path}\n  hint: run `graphify run` in the peer repo first\n"` and exit code 2.
    - When two peers' computed repo labels collide: catch `FederationCollisionError` and re-emit on stderr in two-line form, exit 2.
    - `graphify run` argparse parser is NOT modified.
  </behavior>
  <action>
    In `graphify/__main__.py`:
    - Locate the subparsers block (search `subparsers.add_parser`).
    - Add: `fed = subparsers.add_parser("federate", help="Opt-in cross-repo concept federation (Phase 66)")`.
    - Add: `fed.add_argument("--federate-with", action="append", default=[], metavar="PATH", help="Path to a peer repo's graphify-out/ directory (repeatable)")`.
    - Add: any positional/options matching the `run` defaults so `federate` can drive `build_from_json` (corpus path / target dir).
    - Add a handler `_cmd_federate(args)` that:
        1. Resolves each `--federate-with` value to `<path>/graph.json` (or accepts the file directly).
        2. For each, if file missing → print Phase 64 two-line error to stderr, `sys.exit(2)`.
        3. Calls `build_from_json(extraction, peers=peer_paths, target_dir=...)`. Catches `FederationCollisionError` → two-line stderr → exit 2.
    - Wire `args.cmd == "federate"` to `_cmd_federate`.

    In `tests/test_federate.py` add:
    - `test_cli_repeatable`: invoke parser with two `--federate-with` flags and assert `args.federate_with == [path1, path2]`.
    - `test_cli_missing_peer`: subprocess-run `python -m graphify federate --federate-with /nonexistent/graphify-out` and assert stderr matches the exact two-line pattern via regex `^\[graphify\] error: peer export not found at /nonexistent/graphify-out/graph.json\n  hint: run `graphify run` in the peer repo first\n$`, returncode == 2.

    In `tests/test_stderr_contract.py` extend the snapshot to include both error breadcrumbs (missing peer, basename collision). Follow existing snapshot pattern in that file.

    Commit: `feat(66-03): add graphify federate subcommand with Phase 64 stderr contract`.
  </action>
  <verify>
    <automated>pytest tests/test_federate.py::test_cli_repeatable tests/test_federate.py::test_cli_missing_peer tests/test_stderr_contract.py -x</automated>
  </verify>
  <acceptance_criteria>
    - `python -m graphify federate --help` exits 0 and stdout contains `--federate-with`
    - `grep -c "action=\"append\"" graphify/__main__.py | grep -q .` (at least one append action exists for --federate-with)
    - `grep -n "subparsers.add_parser(\"federate\"" graphify/__main__.py` matches one line
    - `python -m graphify federate --federate-with /tmp/__definitely_missing__/graphify-out 2>&1 1>/dev/null | head -2` matches two-line pattern starting with `[graphify] error:` and second line starting with `  hint:`
    - `pytest tests/test_stderr_contract.py -x` exits 0
    - `grep -c "subparsers.add_parser(\"run\"" graphify/__main__.py` unchanged from baseline (run subcommand untouched)
    - Commit message starts with `feat(66-03):`
  </acceptance_criteria>
  <done>Subcommand registered, repeatable, and emits Phase-64-conformant stderr on both error paths.</done>
</task>

</tasks>

<verification>
- `pytest tests/test_federate.py tests/test_stderr_contract.py -q` exits 0
- `pytest tests/ -q` exits 0
</verification>

<success_criteria>
- CFED-01 user-facing opt-in surface present and tested
- Phase 64 two-line stderr contract preserved on both new error paths
- `graphify run` parser bytes unchanged
</success_criteria>

<output>
After completion, create `.planning/phases/66-cfed-cross-repo-concept-federation-federate-py/66-03-SUMMARY.md`
</output>
