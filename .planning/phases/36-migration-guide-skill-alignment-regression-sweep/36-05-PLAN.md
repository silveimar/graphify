---
phase: 36-migration-guide-skill-alignment-regression-sweep
plan: 05
type: execute
wave: 4
depends_on:
  - 36-03
files_modified:
  - graphify/__main__.py
  - tests/test_skill_files.py
autonomous: true
gap_closure: true
requirements:
  - VER-02
must_haves:
  truths:
    - "Maintainer can confirm skill files and install-time Claude/AGENTS sections use the same v1.8 Obsidian export behavior, with stale generated `_COMMUNITY_*` claims gone."
  artifacts:
    - path: "graphify/__main__.py"
      provides: "Install-time Claude/AGENTS guidance aligned to v1.8 MOC/wiki/GRAPH_REPORT behavior"
      contains: "_CLAUDE_MD_SECTION"
    - path: "tests/test_skill_files.py"
      provides: "Regression coverage for install-time graphify guidance embedded in the CLI"
      contains: "INSTALL_GUIDANCE_SECTIONS"
  key_links:
    - from: "tests/test_skill_files.py"
      to: "graphify/__main__.py"
      via: "imported install guidance section constants"
      pattern: "_CLAUDE_MD_SECTION|_AGENTS_MD_SECTION"
    - from: "graphify/__main__.py"
      to: "VER-02"
      via: "installed project guidance uses v1.8 Obsidian export contract"
      pattern: "GRAPH_REPORT.md|MOC|wiki/index.md"
---

<objective>
Close the remaining Phase 36 verification blocker by aligning installed Claude/AGENTS guidance with the v1.8 Obsidian export contract and adding regression tests for that install-time text.

Purpose: VER-02 remains blocked because packaged skill variants are aligned, but `_CLAUDE_MD_SECTION` and `_AGENTS_MD_SECTION` in `graphify/__main__.py` still tell installed agents to navigate obsolete `_COMMUNITY_*.md overview notes`.
Output: Updated embedded install guidance plus `tests/test_skill_files.py` coverage that fails on stale generated-output claims in either packaged skills or install-time sections.
</objective>

<execution_context>
@$HOME/.cursor/get-shit-done/workflows/execute-plan.md
@$HOME/.cursor/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/36-migration-guide-skill-alignment-regression-sweep/36-CONTEXT.md
@.planning/phases/36-migration-guide-skill-alignment-regression-sweep/36-RESEARCH.md
@.planning/phases/36-migration-guide-skill-alignment-regression-sweep/36-PATTERNS.md
@.planning/phases/36-migration-guide-skill-alignment-regression-sweep/36-VERIFICATION.md
@graphify/__main__.py
@tests/test_skill_files.py

<interfaces>
Existing source constants:

```python
_CLAUDE_MD_SECTION = """\
## graphify
...
"""

_AGENTS_MD_SECTION = """\
## graphify
...
"""
```

Existing test contract surfaces in `tests/test_skill_files.py`:

```python
REQUIRED_V18_OBSIDIAN_PHRASES = (
    "MOC-only community output",
    "Graphify-owned v1.8 subtree",
    "preview-first update-vault",
    "graphify update-vault --input work-vault/raw --vault ls-vault",
    "Back up the target vault before apply",
    "graphify-out/migrations/archive/",
    "no destructive deletion",
)

FORBIDDEN_V18_OBSIDIAN_PATTERNS = (
    re.compile(r"_COMMUNITY_\*.*overview notes", re.IGNORECASE),
    re.compile(r"_COMMUNITY_\*.*dataview queries", re.IGNORECASE),
    re.compile(r"print\(.*_COMMUNITY_", re.IGNORECASE),
)
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Align embedded Claude and AGENTS guidance</name>
  <read_first>
    <file>.planning/phases/36-migration-guide-skill-alignment-regression-sweep/36-VERIFICATION.md</file>
    <file>.planning/phases/36-migration-guide-skill-alignment-regression-sweep/36-CONTEXT.md</file>
    <file>graphify/__main__.py</file>
  </read_first>
  <files>graphify/__main__.py</files>
  <action>
    Replace the stale Obsidian rule line in both `_CLAUDE_MD_SECTION` and `_AGENTS_MD_SECTION` per D-06, D-07, D-09, and VER-02. The new guidance must tell installed agents to start from `graphify-out/GRAPH_REPORT.md`, use `graphify-out/obsidian/` MOC notes and wikilinks when an Obsidian export exists, and fall back to `graphify-out/wiki/index.md` when no Obsidian vault exists. Do not describe v1.8 output as generated `_COMMUNITY_*` overview notes, Dataview-query overview notes, or printed `_COMMUNITY_*` summaries. Preserve the surrounding install-section format and rebuild command.
  </action>
  <verify>
    <automated>pytest tests/test_main_flags.py -q</automated>
    <automated>! grep -E "_COMMUNITY_\\*.*overview notes|_COMMUNITY_\\*.*dataview queries|print\\(.*_COMMUNITY_" graphify/__main__.py</automated>
    <automated>grep -n "GRAPH_REPORT.md" graphify/__main__.py && grep -n "MOC" graphify/__main__.py && grep -n "wiki/index.md" graphify/__main__.py</automated>
  </verify>
  <acceptance_criteria>
    - `_CLAUDE_MD_SECTION` no longer contains `_COMMUNITY_*.md overview notes`.
    - `_AGENTS_MD_SECTION` no longer contains `_COMMUNITY_*.md overview notes`.
    - Both sections still direct agents to `graphify-out/GRAPH_REPORT.md`.
    - Both sections mention Obsidian MOC navigation and wikilinks without claiming legacy community overview files are generated output.
    - `pytest tests/test_main_flags.py -q` passes.
  </acceptance_criteria>
  <done>The failed install-time guidance artifact in `36-VERIFICATION.md` is aligned with the v1.8 MOC/wiki/GRAPH_REPORT contract.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Add install-time guidance drift tests</name>
  <read_first>
    <file>.planning/phases/36-migration-guide-skill-alignment-regression-sweep/36-VERIFICATION.md</file>
    <file>.planning/phases/36-migration-guide-skill-alignment-regression-sweep/36-CONTEXT.md</file>
    <file>tests/test_skill_files.py</file>
    <file>graphify/__main__.py</file>
  </read_first>
  <files>tests/test_skill_files.py</files>
  <behavior>
    - Tests inspect `_CLAUDE_MD_SECTION` and `_AGENTS_MD_SECTION`, not only `graphify/skill*.md`.
    - The install-time sections must contain v1.8-aligned graph navigation phrases.
    - The same forbidden stale generated-output phrases and patterns must fail tests for install-time sections.
  </behavior>
  <action>
    Extend `tests/test_skill_files.py` per D-07 and VER-02. Add a top-level import for `graphify.__main__` (for example `from graphify import __main__ as graphify_main`) and define an `INSTALL_GUIDANCE_SECTIONS` tuple mapping names such as `CLAUDE.md install section` and `AGENTS.md install section` to `graphify_main._CLAUDE_MD_SECTION` and `graphify_main._AGENTS_MD_SECTION`. Add required install guidance phrases that match the embedded section contract, including `GRAPH_REPORT.md`, `MOC`, `[[wikilinks]]`, and `wiki/index.md`. Add a test that asserts every install guidance section contains those phrases. Add a second test that reuses `FORBIDDEN_V18_OBSIDIAN_PHRASES` and `FORBIDDEN_V18_OBSIDIAN_PATTERNS` against every install guidance section. Keep imports at the top of the file and keep the existing skill-file variant tests unchanged except for sharing helpers/constants if useful.
  </action>
  <verify>
    <automated>pytest tests/test_skill_files.py tests/test_main_flags.py -q</automated>
    <automated>grep -n "INSTALL_GUIDANCE_SECTIONS" tests/test_skill_files.py</automated>
    <automated>grep -n "_CLAUDE_MD_SECTION\\|_AGENTS_MD_SECTION" tests/test_skill_files.py</automated>
    <automated>! grep -E "_COMMUNITY_\\*.*overview notes|_COMMUNITY_\\*.*dataview queries|print\\(.*_COMMUNITY_" graphify/__main__.py tests/test_skill_files.py</automated>
  </verify>
  <acceptance_criteria>
    - `tests/test_skill_files.py` imports the embedded install guidance constants from `graphify/__main__.py`.
    - A regression test fails if `_CLAUDE_MD_SECTION` reintroduces stale `_COMMUNITY_*` generated-output claims.
    - A regression test fails if `_AGENTS_MD_SECTION` reintroduces stale `_COMMUNITY_*` generated-output claims.
    - A regression test confirms both install sections describe the v1.8 MOC/wiki/GRAPH_REPORT navigation contract.
    - `pytest tests/test_skill_files.py tests/test_main_flags.py -q` passes.
  </acceptance_criteria>
  <done>VER-02 covers install-time CLAUDE/AGENTS sections as well as packaged skill files, closing the failed truth from `36-VERIFICATION.md`.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| CLI package source -> installed project instructions | Embedded strings in `graphify/__main__.py` become persistent guidance in user projects. |
| Generated graph output -> agent navigation | Installed agents use these rules to decide whether to read reports, Obsidian notes, wiki pages, or raw files. |
| Regression tests -> release confidence | VER-02 relies on tests catching drift across both packaged skill files and install-time guidance. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-36-17 | Tampering | `graphify/__main__.py` install guidance | mitigate | Replace obsolete `_COMMUNITY_*` generated-output guidance with explicit v1.8 MOC/wiki/GRAPH_REPORT navigation wording. |
| T-36-18 | Repudiation | `tests/test_skill_files.py` | mitigate | Add tests that prove install-time sections are covered, so future drift cannot be dismissed as outside the skill-file contract. |
| T-36-19 | Information Disclosure | Installed agent navigation rules | accept | Guidance points agents to local generated graph artifacts only; no network or secret-reading behavior is introduced. |
| T-36-20 | Denial of Service | `tests/test_skill_files.py` import of `graphify.__main__` | mitigate | Import only module constants and keep tests pure; verify with focused pytest to catch import side effects. |
</threat_model>

<verification>
Focused gap gate:

```bash
pytest tests/test_skill_files.py tests/test_main_flags.py -q
```

Required grep checks:

```bash
! grep -E "_COMMUNITY_\\*.*overview notes|_COMMUNITY_\\*.*dataview queries|print\\(.*_COMMUNITY_" graphify/__main__.py tests/test_skill_files.py
grep -n "INSTALL_GUIDANCE_SECTIONS" tests/test_skill_files.py
grep -n "GRAPH_REPORT.md" graphify/__main__.py
grep -n "wiki/index.md" graphify/__main__.py
```
</verification>

<success_criteria>
- `graphify/__main__.py` embedded `_CLAUDE_MD_SECTION` and `_AGENTS_MD_SECTION` no longer instruct installed agents to navigate `_COMMUNITY_*.md overview notes`.
- `tests/test_skill_files.py` covers both embedded install-time sections and packaged skill variants for v1.8 wording drift.
- VER-02 is no longer blocked by install-time guidance drift.
- The failed truth from `36-VERIFICATION.md` is directly addressed by code and regression tests.
- `pytest tests/test_skill_files.py tests/test_main_flags.py -q` passes.
</success_criteria>

<output>
After completion, create `.planning/phases/36-migration-guide-skill-alignment-regression-sweep/36-05-SUMMARY.md`
</output>
