---
phase: 47
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified:
  - graphify/serve.py
  - graphify/mcp_tool_registry.py
  - graphify/capability_tool_meta.yaml
  - tests/test_concept_code_mcp.py
autonomous: true
requirements:
  - CCODE-03
  - CCODE-04
must_haves:
  truths:
    - "New MCP tool `concept_code_hops` (or name locked in task) appears in `build_mcp_tools()` and `serve._handlers` (D-47.01)"
    - "`pytest tests/test_concept_code_mcp.py -q` exits 0 including golden-path hop test over `implements` (CCODE-04)"
    - "`capability_tool_meta.yaml` contains meta row for the new tool with cost_class/cacheable_until (D-47.01)"
---

<objective>
Add a dedicated MCP tool and pure helper that **traverse `implements` concept↔code hops** on the live graph, with automated golden-path tests — **CCODE-03** + **CCODE-04** core delivery. Does **not** rename temporal `entity_trace`.
</objective>

<threat_model>
| Threat | Mitigation |
|--------|------------|
| Injection via entity label | Reuse `sanitize_label` / existing graph tool input validation |
| Unbounded graph walk | Hard-cap `max_hops` (default 3, max 6) and edge budget in helper |
</threat_model>

<tasks>

<task type="execute">
  <name>47-01-01 — Pure helper + MCP wiring</name>
  <read_first>
    - graphify/serve.py
    - graphify/mcp_tool_registry.py
    - graphify/capability_tool_meta.yaml
    - .planning/phases/47-mcp-trace-integration/47-CONTEXT.md
    - tests/test_concept_code_edges.py
  </read_first>
  <acceptance_criteria>
    - `grep -n concept_code_hops graphify/mcp_tool_registry.py` returns at least one match
    - `grep -n _run_concept_code_hops graphify/serve.py` returns at least one match (helper name may match implementation choice)
    - `grep -n concept_code_hops graphify/capability_tool_meta.yaml` returns at least one match
    - `python -c "from graphify.mcp_tool_registry import build_mcp_tools; assert any(t.name=='concept_code_hops' for t in build_mcp_tools())"` exits 0
  </acceptance_criteria>
  <action>
    Implement `_run_concept_code_hops(G, alias_map, arguments)` (or equivalent) that resolves an entity, walks only edges with `relation == \"implements\"` respecting orientation, returns hybrid text+JSON meta. Register MCP tool `concept_code_hops` with JSON Schema: `entity` (required), `max_hops` (int default 3), optional `direction` enum. Wire `_handlers` in `serve.py`. Add YAML meta: `cost_class: cheap`, `deterministic: true`, `cacheable_until: graph_mtime`.
  </action>
</task>

<task type="execute">
  <name>47-01-02 — Automated golden path</name>
  <read_first>
    - tests/test_concept_code_edges.py
    - graphify/serve.py
  </read_first>
  <acceptance_criteria>
    - `test -f tests/test_concept_code_mcp.py`
    - `pytest tests/test_concept_code_mcp.py::test_concept_code_hops_golden_path -q` exits 0
  </acceptance_criteria>
  <action>
    Add `tests/test_concept_code_mcp.py` building a small graph via `build_from_json` with code+document nodes and one `implements` edge; call the pure helper (import from serve) with `entity` matching code node label; assert meta lists the concept hop or text body contains expected node labels.
  </action>
</task>

</tasks>
