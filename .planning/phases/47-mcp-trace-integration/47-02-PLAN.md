---
phase: 47
plan: "02"
type: execute
wave: 2
depends_on:
  - "47-01"
files_modified:
  - docs/RELATIONS.md
  - docs/ARCHITECTURE.md
  - server.json
  - graphify/skill.md
  - graphify/skill-codex.md
  - graphify/skill-opencode.md
  - graphify/skill-claw.md
  - graphify/skill-droid.md
  - graphify/skill-trae.md
  - graphify/skill-copilot.md
  - graphify/skill-aider.md
  - graphify/skill-windows.md
autonomous: true
requirements:
  - CCODE-03
must_haves:
  truths:
    - "`docs/RELATIONS.md` contains MCP↔`implements` pointer table or subsection (D-47.02)"
    - "`docs/ARCHITECTURE.md` (or `.planning/codebase/ARCHITECTURE.md`) mentions MCP concept↔code hop tool in pipeline section (D-47.02)"
    - "`server.json` `_meta.tool_count` matches `len(build_mcp_tools())` after regen (D-47.01)"
    - "`grep -c concept_code_hops graphify/skill.md` is >= 1 and same for each listed skill-*.md variant (D-47.03)"
---

<objective>
Documentation, manifest regeneration, and **skill/harness parity** after the new MCP tool ships — **D-47.01–03**, **CCODE-03** doc bullet.
</objective>

<threat_model>
| Threat | Mitigation |
|--------|------------|
| Stale server.json hash | Regenerate via documented `graphify capability` command; commit result |
| Skill drift | Grep checklist for tool name across all variants |
</threat_model>

<tasks>

<task type="execute">
  <name>47-02-01 — docs/RELATIONS + ARCHITECTURE</name>
  <read_first>
    - docs/RELATIONS.md
    - docs/ARCHITECTURE.md
    - .planning/phases/47-mcp-trace-integration/47-CONTEXT.md
  </read_first>
  <acceptance_criteria>
    - `grep -n concept_code_hops docs/RELATIONS.md` exits 0
    - `grep -n concept_code_hops docs/ARCHITECTURE.md` exits 0 OR `grep -n concept_code_hops .planning/codebase/ARCHITECTURE.md` exits 0
  </acceptance_criteria>
  <action>
    Add concise subsection: which MCP tool(s) list/traverse `implements`; link to `serve.py` helper. In ARCHITECTURE (repo `docs/ARCHITECTURE.md` if present, else `.planning/codebase/ARCHITECTURE.md`), add 1 short paragraph under MCP/export pipeline describing concept↔code hop surfacing.
  </action>
</task>

<task type="execute">
  <name>47-02-02 — Regenerate server.json</name>
  <read_first>
    - CLAUDE.md
    - server.json
    - graphify/capability.py
  </read_first>
  <acceptance_criteria>
    - `python -m graphify capability --stdout 2>/dev/null | head -1` starts with `{` OR documented equivalent updates `server.json`
    - `grep -q manifest_content_hash server.json`
  </acceptance_criteria>
  <action>
    Run project-documented capability export (e.g. `python -m graphify capability --stdout > server.json` from repo root with `[mcp]` extra installed) so `_meta.tool_count` and `manifest_content_hash` match current `build_mcp_tools()`.
  </action>
</task>

<task type="execute">
  <name>47-02-03 — Skill `/trace` + MCP enumerations</name>
  <read_first>
    - graphify/skill.md
    - graphify/mcp_tool_registry.py
    - .planning/phases/47-mcp-trace-integration/47-UI-SPEC.md
  </read_first>
  <acceptance_criteria>
    - Each of: `graphify/skill.md`, `graphify/skill-codex.md`, `graphify/skill-opencode.md`, `graphify/skill-claw.md`, `graphify/skill-droid.md`, `graphify/skill-trae.md`, `graphify/skill-copilot.md`, `graphify/skill-aider.md`, `graphify/skill-windows.md` contains the substring `concept_code_hops` OR updated MCP tool count table including that tool name
    - `grep -n /trace graphify/skill.md` exits 0 with line referencing concept↔code hop behavior (slash workflow per CCODE-04)
  </acceptance_criteria>
  <action>
    Update enumerated MCP tool lists and add `/trace` slash guidance pointing agents to `concept_code_hops` for concept↔code linkage (align with 47-UI-SPEC). Include `skill-excalidraw.md` only if it contains an MCP enumeration block; otherwise skip with comment in commit body.
  </action>
</task>

</tasks>
