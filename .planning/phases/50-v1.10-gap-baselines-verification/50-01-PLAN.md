---
phase: 50
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified:
  - .planning/phases/45-baselines-detect-self-ingestion/45-VERIFICATION.md
  - .planning/REQUIREMENTS.md
  - .planning/phases/45-baselines-detect-self-ingestion/45-VALIDATION.md
  - .planning/phases/50-v1.10-gap-baselines-verification/50-VALIDATION.md
  - .planning/phases/50-v1.10-gap-baselines-verification/50-01-SUMMARY.md
  - .planning/ROADMAP.md
autonomous: true
requirements: [HYG-01, HYG-02, HYG-03]
must_haves:
  truths:
    - "`45-VERIFICATION.md` exists under Phase 45 with Nyquist-style sections mirroring Phase 49 and cites grep + pytest evidence for HYG-01..03."
    - "Full `pytest tests/ -q` transcript (pass/xfail/skip summary) is recorded in `45-VERIFICATION.md` before any HYG checkbox flips."
    - "`REQUIREMENTS.md` rows **HYG-01**, **HYG-02**, **HYG-03** are `[x]` only after the verification doc’s **Automated** section references that fresh full-suite run."
    - "Optional: `45-VALIDATION.md` and `50-VALIDATION.md` frontmatter/task rows reflect executed sign-off (no stale ⬜ for completed 45 work when reconciliation is chosen)."
  artifacts:
    - path: .planning/phases/45-baselines-detect-self-ingestion/45-VERIFICATION.md
      provides: Canonical milestone verification for Phase 45 baselines (HYG-01..03)
      min_lines: 40
    - path: .planning/REQUIREMENTS.md
      provides: Ticked HYG-01..03 with traceability to 45-VERIFICATION evidence
      contains: "[x] **HYG-03:**"
  key_links:
    - from: .planning/REQUIREMENTS.md
      to: .planning/phases/45-baselines-detect-self-ingestion/45-VERIFICATION.md
      via: Checkbox sign-off after Automated section cites pytest
      pattern: "45-VERIFICATION"
---

<objective>
Close **v1.10-MILESTONE-AUDIT** gaps for **HYG-01**, **HYG-02**, **HYG-03** by authoring the missing **`45-VERIFICATION.md`** (canonical path under Phase **45**), recording **executed** pytest evidence (focused slice + full suite), and reconciling **REQUIREMENTS.md** checkboxes. Optionally align stale **Nyquist** validation metadata for Phase **45** and **50**.

Purpose: Phase 45 code shipped via **45-01..03**; audit debt is **traceability + proof**, not new runtime features.

Output: **`45-VERIFICATION.md`**, updated **`REQUIREMENTS.md`**, optional **`45-VALIDATION.md`** / **`50-VALIDATION.md`** / **ROADMAP** Phase 50 plan list.
</objective>

<execution_context>
@$HOME/.cursor/get-shit-done/workflows/execute-plan.md
@$HOME/.cursor/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/50-v1.10-gap-baselines-verification/50-RESEARCH.md
@.planning/phases/45-baselines-detect-self-ingestion/45-CONTEXT.md
@.planning/phases/49-add-version-flag-to-graphify-command-and-also-print-current-/49-VERIFICATION.md
@CLAUDE.md

**Template:** Mirror section order and tone of `49-VERIFICATION.md` — YAML frontmatter (`status`, `phase`, `phase_name`, `verified`), **Must-haves**, **Automated**, **Gaps**, **human_verification**.

**Canonical verification path (non-negotiable):** `.planning/phases/45-baselines-detect-self-ingestion/45-VERIFICATION.md` (not under phase 50 folder).
</context>

<tasks>

<task type="auto">
  <name>Task 1: Author 45-VERIFICATION.md with grep anchors + focused pytest</name>
  <files>.planning/phases/45-baselines-detect-self-ingestion/45-VERIFICATION.md</files>
  <read_first>
    - `.planning/phases/49-add-version-flag-to-graphify-command-and-also-print-current-/49-VERIFICATION.md` (structure)
    - `.planning/phases/45-baselines-detect-self-ingestion/45-CONTEXT.md` (D-45.01–D-45.09)
    - `.planning/phases/50-v1.10-gap-baselines-verification/50-RESEARCH.md` (outline §4)
    - `graphify/corpus_prune.py`, `graphify/detect.py`, `graphify/extract.py`, `graphify/profile.py` (spot-check symbols for table)
  </read_first>
  <action>
    1. **Create** `.planning/phases/45-baselines-detect-self-ingestion/45-VERIFICATION.md` with frontmatter: `status: passed` (only after focused pytest below is green), `phase: 45`, `phase_name: Baselines &amp; Detect Self-Ingestion`, `verified: &lt;ISO-8601 date of this run&gt;`.

    2. **Must-haves** table: rows for **HYG-01** (map to D-45.01–D-45.06, D-45.08 as in 50-RESEARCH), **HYG-02** (D-45.07), **HYG-03** (D-45.08). Evidence column MUST include **concrete grep targets** (executor runs these and pastes one representative line or `file:line` into the table or a following “Evidence details” subsection):
       - `rg -n "build_prior_files|dir_prune_reason|corpus_prune" graphify/corpus_prune.py graphify/detect.py graphify/extract.py`
       - `rg -n "skipped\\\[.\"manifest\"\\]|skipped\\\[\"manifest\"\\]|manifest" graphify/detect.py` (adjust for actual Python syntax — goal: show manifest skip telemetry / stderr path exists)
       - `rg -n "dot_graphify|corpus\\.dot_graphify" graphify/profile.py graphify/detect.py graphify/__main__.py graphify/doctor.py` (subset as applicable)
       - Test anchors: `tests/test_detect.py::test_detect_skips_dotfiles`, `tests/test_extract.py::test_collect_files_from_dir`, `tests/test_extract.py::test_collect_files_skips_nested_graphify_out`

    3. **Automated (focused slice):** From repo root, run and paste stdout (full last line with pass count):
       `pytest tests/test_detect.py::test_detect_skips_dotfiles tests/test_extract.py::test_collect_files_from_dir tests/test_extract.py::test_collect_files_skips_nested_graphify_out -q`

    4. **Gaps:** Note `N/A` or list any non-blocking manual items; reference D-45.03 operator manifest clear from `45-VALIDATION.md` only if still relevant.

    5. **human_verification:** Default `None.` unless product owner required `graphify doctor --dot-graphify-track` on `tests/fixtures/phase45-mini-vault/` — if not run, state `None (optional CLI smoke not required for HYG sign-off).`

    Do **not** yet paste full `pytest tests/ -q` output (Task 2).
  </action>
  <acceptance_criteria>
    - File exists at `.planning/phases/45-baselines-detect-self-ingestion/45-VERIFICATION.md`.
    - `grep -c "^## " .planning/phases/45-baselines-detect-self-ingestion/45-VERIFICATION.md` returns **≥ 4** (at least Must-haves, Automated, Gaps, human_verification section headings).
    - `grep -E "HYG-0[123]|D-45\\.0" .planning/phases/45-baselines-detect-self-ingestion/45-VERIFICATION.md` matches (non-zero lines) — HYG and D-45 references present.
    - Focused pytest command from Task 1 appears verbatim under **Automated** and output shows **passed** count &gt; 0.
  </acceptance_criteria>
  <verify>
    <automated>test -f .planning/phases/45-baselines-detect-self-ingestion/45-VERIFICATION.md && grep -q "Must-haves" .planning/phases/45-baselines-detect-self-ingestion/45-VERIFICATION.md && pytest tests/test_detect.py::test_detect_skips_dotfiles tests/test_extract.py::test_collect_files_from_dir tests/test_extract.py::test_collect_files_skips_nested_graphify_out -q</automated>
  </verify>
  <done>45-VERIFICATION.md created with 49-style structure, grep anchors, and focused pytest transcript; focused suite green.</done>
</task>

<task type="auto">
  <name>Task 2: Full pytest gate, append transcript, tick HYG-01..03 in REQUIREMENTS</name>
  <files>.planning/phases/45-baselines-detect-self-ingestion/45-VERIFICATION.md,.planning/REQUIREMENTS.md</files>
  <read_first>
    - `.planning/phases/45-baselines-detect-self-ingestion/45-VERIFICATION.md` (from Task 1)
    - `.planning/REQUIREMENTS.md` (HYG rows 8–10, traceability table lines 40–42)
  </read_first>
  <action>
    1. Run **full suite** from repo root: `pytest tests/ -q` (per `CLAUDE.md`). Capture the **summary line** (N passed, xfailed, skipped as emitted).

    2. Append to **`45-VERIFICATION.md`** under **Automated** a second bullet: **Full gate:** `pytest tests/ -q` — paste the **exact** summary line(s). Optionally add one line: `git rev-parse --short HEAD` for commit pointer.

    3. **Only if** full suite is green (no unexpected failures): In `.planning/REQUIREMENTS.md`, set checkboxes to `[x]` for **HYG-01**, **HYG-02**, **HYG-03** **only**. Do not modify HYG-04/05 or CCODE rows. Ensure each of those three rows still states Phase **45** delivery + pointer to **`45-VERIFICATION.md`** in the prose (adjust minimally if needed for grammar).

    4. If full suite is **not** green: do **not** tick boxes; document failures under **Gaps** in `45-VERIFICATION.md` and stop for fix — out of scope for this plan except recording state.
  </action>
  <acceptance_criteria>
    - `grep -n "pytest tests/ -q" .planning/phases/45-baselines-detect-self-ingestion/45-VERIFICATION.md` is non-empty.
    - `grep -E "passed|failed|error|skipped" .planning/phases/45-baselines-detect-self-ingestion/45-VERIFICATION.md` captures the full-suite transcript line.
    - `grep -E '^- \[x\] \*\*HYG-0[123]:\*\*' .planning/REQUIREMENTS.md | wc -l | tr -d ' '` outputs **3** after successful sign-off (matches live format `- [x] **HYG-01:**`).
    - `pytest tests/ -q` exits **0** immediately before checkbox edits.
  </acceptance_criteria>
  <verify>
    <automated>pytest tests/ -q && test "$(grep -E '^- \[x\] \*\*HYG-0[123]:\*\*' .planning/REQUIREMENTS.md | wc -l | tr -d ' ')" = "3"</automated>
  </verify>
  <done>Full pytest evidence in 45-VERIFICATION.md; HYG-01..03 checkboxes flipped with CI-local proof.</done>
</task>

<task type="auto">
  <name>Task 3: Optional validation doc + ROADMAP reconciliation + plan summary</name>
  <files>.planning/phases/45-baselines-detect-self-ingestion/45-VALIDATION.md,.planning/phases/50-v1.10-gap-baselines-verification/50-VALIDATION.md,.planning/ROADMAP.md,.planning/phases/50-v1.10-gap-baselines-verification/50-01-SUMMARY.md</files>
  <read_first>
    - `.planning/phases/45-baselines-detect-self-ingestion/45-VALIDATION.md`
    - `.planning/phases/50-v1.10-gap-baselines-verification/50-VALIDATION.md`
    - `.planning/ROADMAP.md` (search `Phase 50:` / `50-01-PLAN`)
  </read_first>
  <action>
    1. **`45-VALIDATION.md`:** If Task 2 completed sign-off, update frontmatter `status: complete` (or project convention), set `nyquist_compliant: true` when all Phase 45 automated rows are satisfied, replace ⬜ with ✅ in the Per-Task table for **45-01-01** through **45-03-02** where tests exist (align with 50-RESEARCH test map — do not invent passing tests). Update **Validation Sign-Off** checkboxes to match reality. If any row cannot be honestly marked green, leave a short note under **Gaps** in `45-VERIFICATION.md` explaining why validation doc stays partial.

    2. **`50-VALIDATION.md`:** Set row **50-01-01** Status to ✅ green; set `nyquist_compliant: true` and `status: complete` in frontmatter when Phase 50 plan is done; tick **Validation Sign-Off** items that apply.

    3. **`ROADMAP.md`:** In the Phase **50** details block, set **Plans:** `1 plan`, Plans list: `[x] 50-01-PLAN.md — Baselines verification artifact + REQ sign-off (HYG-01..03)`.

    4. Do not change Phase 45/49/48 narrative beyond Phase 50 checklist lines.

    5. **Create** `.planning/phases/50-v1.10-gap-baselines-verification/50-01-SUMMARY.md` per GSD summary template: bullets for each prior task outcome, pointer to `45-VERIFICATION.md`, pytest summary line repeated once.
  </action>
  <acceptance_criteria>
    - `test -f .planning/phases/50-v1.10-gap-baselines-verification/50-01-SUMMARY.md`
    - `grep -n "50-01-PLAN" .planning/ROADMAP.md` shows the plan line with `[x]`.
    - `grep "nyquist_compliant: true" .planning/phases/50-v1.10-gap-baselines-verification/50-VALIDATION.md` returns true after edits.
    - If `45-VALIDATION.md` was updated: `grep "nyquist_compliant" .planning/phases/45-baselines-detect-self-ingestion/45-VALIDATION.md` reflects post-sign-off state.
  </acceptance_criteria>
  <verify>
    <automated>test -f .planning/phases/50-v1.10-gap-baselines-verification/50-01-SUMMARY.md && grep -q "\[x\].*50-01-PLAN" .planning/ROADMAP.md && grep -q "nyquist_compliant: true" .planning/phases/50-v1.10-gap-baselines-verification/50-VALIDATION.md</automated>
  </verify>
  <done>ROADMAP Phase 50 plan checked; 50-VALIDATION reflects green; 45-VALIDATION optionally aligned with executed Phase 45 work.</done>
</task>

</tasks>

<!-- output: 50-01-SUMMARY.md is owned by Task 3 -->

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Docs ↔ repo | Planning markdown is trusted for process; **evidence** must come from **local pytest** and **repository file reads**, not assumed CI URLs unless pasted. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-50-01 | T | `45-VERIFICATION.md` / REQUIREMENTS | accept | Phase 50 adds **no new runtime**; false sign-off is a **process** risk only — mitigate by requiring **live** `pytest tests/ -q` exit 0 before `[x]` edits. |
| T-50-02 | I | N/A | accept | No new user-controlled parsers; manifest/path handling **shipped in Phase 45** — verification **references** existing mitigations from **45-01..03** threat tables, does not weaken `security.py`. |
| T-50-03 | E | N/A | accept | Documentation-only phase; no new deletion vectors. |

</threat_model>

<verification>
**Wave 1 (single wave):**

1. `pytest tests/ -q` — exit code 0.
2. `test -f .planning/phases/45-baselines-detect-self-ingestion/45-VERIFICATION.md`
3. `grep -E '^- \[x\] \*\*HYG-0[123]:\*\*' .planning/REQUIREMENTS.md` — three lines.
4. `grep "pytest tests/ -q" .planning/phases/45-baselines-detect-self-ingestion/45-VERIFICATION.md` — documents full suite.
5. Optional: ROADMAP + `50-VALIDATION.md` updates present per Task 3.
</verification>

<success_criteria>
- **`v1.10-MILESTONE-AUDIT`** can cite **`45-VERIFICATION.md`** for **HYG-01**, **HYG-02**, **HYG-03**.
- **REQUIREMENTS.md** traceability rows 40–42 align with **checked** boxes and evidence file path.
- No production code changes required for this phase unless full pytest exposes a **new** regression (then stop and fix outside this doc-only intent).
</success_criteria>

