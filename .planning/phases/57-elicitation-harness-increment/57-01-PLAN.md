---
phase: 57-elicitation-harness-increment
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - tests/test_elicit.py
autonomous: true
requirements: [ELIC-01]
must_haves:
  truths:
    - "Sidecar node-id collision: elicitation node attrs win over base extraction node attrs"
    - "Sidecar edge with conflicting relation on same (source,target): elicitation relation wins (last-write semantics in nx.Graph)"
    - "Confidence value is preserved across the sidecar merge"
    - "Malformed sidecar JSON: load_elicitation_sidecar returns None and emits a [graphify] stderr warning"
    - "Sidecar with edge missing required fields: rejected at save time via validate_extraction (raises ValueError)"
    - "Sidecar edge referencing absent node: build()'s resulting graph still contains the dangling endpoint as a bare node (no exception)"
  artifacts:
    - path: tests/test_elicit.py
      provides: "Six new sidecar-collision regression tests (ELIC-01)"
      contains: "test_sidecar_node_id_collision_elicitation_wins"
  key_links:
    - from: tests/test_elicit.py
      to: graphify/build.py:281-307
      via: "merge_elicitation_into_build_inputs + build()"
      pattern: "merge_elicitation_into_build_inputs"
---

<objective>
Lock down the elicitation sidecar collision merge contract (D-01..D-04) with six new regression tests in tests/test_elicit.py. These tests assert behavior that already holds in graphify/build.py:281-307 today; they prevent regression. No production code changes.

Purpose: Satisfy ELIC-01 — at least one additional scripted elicitation→extraction scenario covered by unit tests vs the v1.9 baseline, including failure modes and happy-path artifact shape.
Output: Six pytest functions appended to tests/test_elicit.py.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/57-elicitation-harness-increment/57-CONTEXT.md
@.planning/phases/57-elicitation-harness-increment/57-RESEARCH.md
@.planning/phases/57-elicitation-harness-increment/57-PATTERNS.md
@tests/test_elicit.py
@graphify/build.py
@graphify/elicit.py

<interfaces>
<!-- Key contracts from existing modules. Executor uses these directly. -->

From graphify/elicit.py (already imported by tests/test_elicit.py:1-18):
```python
ELICITATION_SIDECAR_FILENAME: str  # "elicitation.json"
def save_elicitation_sidecar(artifacts_dir: Path, extraction: dict, *, force: bool = False, filename: str | None = None) -> Path
def load_elicitation_sidecar(artifacts_dir: Path) -> dict | None
def merge_elicitation_into_build_inputs(extractions: list[dict], artifacts_dir: Path) -> list[dict]
```

From graphify/build.py:281-307:
```python
def build(extractions: Iterable[dict], *, directed: bool = False, elicitation: dict | None = None) -> nx.Graph
# Iterates extractions in order; later add_node/add_edge calls overwrite attrs (NetworkX semantics).
# Edge identity in nx.Graph is the (u, v) pair; conflicting relation on same pair → last-write wins on attrs.
```

From graphify/validate.py:
```python
def validate_extraction(data: dict) -> list[str]  # empty = valid; non-empty = errors
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add three node/edge collision regression tests (ELIC-01 happy-path)</name>
  <files>tests/test_elicit.py</files>
  <read_first>
    - tests/test_elicit.py (entire file, ~150 lines) — match existing imports, fixtures, and `_sample_answers()` helper. Pattern source: tests/test_elicit.py:102-124 `test_build_includes_elicitation_last_wins`.
    - graphify/build.py:281-307 — confirms `build()` iteration order and add_edge attr-overwrite semantics.
    - graphify/elicit.py:182-313 — `merge_elicitation_into_build_inputs` and `_merge_extractions_by_node_id` edge dedupe key `(source, target, relation, source_file)`.
    - 57-PATTERNS.md "Pattern 1: Sidecar collision merge contract" and "Code Examples: ELIC-01" sections.
    - 57-RESEARCH.md "Pitfall 1: Undirected vs directed graph semantics" — assertions must say "last-wins on attrs", not "both edges preserved".
  </read_first>
  <action>
Append three new test functions to tests/test_elicit.py (no new imports beyond what is already at lines 1-18 of the file; if a test needs `capsys`, the fixture is built into pytest). All tests use `tmp_path` only — no fs side effects elsewhere.

1. `test_sidecar_node_id_collision_elicitation_wins(tmp_path: Path) -> None` — per D-02. Construct base extraction with node id `"shared"` (`label="from code"`, `file_type="code"`, `source_file="x.py"`); construct elicitation extraction with same id `"shared"` (`label="from elicit"`, `file_type="rationale"`, `source_file=""`). Use `save_elicitation_sidecar(adir, elic, force=True)` to author the sidecar (per Pitfall 3 — never write raw `{"nodes":...,"edges":...}` to the file directly). Then `seq = merge_elicitation_into_build_inputs([base], adir)` and `G = build(seq)`. Assert `G.nodes["shared"]["label"] == "from elicit"` and `G.nodes["shared"]["file_type"] == "rationale"`.

2. `test_sidecar_edge_conflicting_relation_last_wins(tmp_path: Path) -> None` — per D-02. Base has nodes `a`, `b` and edge `{"source":"a","target":"b","relation":"calls","confidence":"EXTRACTED","source_file":"f.py"}`. Elicitation has same nodes plus edge with `relation="depends_on"`, `confidence="INFERRED"`, `source_file=""`. Save via `save_elicitation_sidecar`. Build. Assert `G.edges["a","b"]["relation"] == "depends_on"` AND `G.edges["a","b"]["confidence"] == "INFERRED"`. Use undirected `Graph` (build default `directed=False`). DO NOT assert "both edges preserved" — see Pitfall 1.

3. `test_sidecar_preserves_confidence_across_merge(tmp_path: Path) -> None` — per D-02. Base has zero edges. Elicitation has node `x` and edge `{"source":"x","target":"x","relation":"refs","confidence":"AMBIGUOUS","source_file":""}` (self-loop avoids dangling-endpoint complications). Save, merge, build. Assert `G.edges["x","x"]["confidence"] == "AMBIGUOUS"`.

For all three: use exact code shape from 57-PATTERNS.md "Code Examples" snippets. No new helpers. Reuse `tmp_path / "art"` as the artifacts dir convention from existing tests (lines 102-110).
  </action>
  <verify>
    <automated>pytest tests/test_elicit.py::test_sidecar_node_id_collision_elicitation_wins tests/test_elicit.py::test_sidecar_edge_conflicting_relation_last_wins tests/test_elicit.py::test_sidecar_preserves_confidence_across_merge -x -q</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "def test_sidecar_node_id_collision_elicitation_wins" tests/test_elicit.py` returns `1`
    - `grep -c "def test_sidecar_edge_conflicting_relation_last_wins" tests/test_elicit.py` returns `1`
    - `grep -c "def test_sidecar_preserves_confidence_across_merge" tests/test_elicit.py` returns `1`
    - All three tests pass: pytest exit code 0 for the verify command above.
    - `grep -c 'G\.edges\["a","b"\]\["relation"\] == "depends_on"' tests/test_elicit.py` returns `1` (no docstring false positive — exact assertion present).
    - No new top-level `import` statements added; `from __future__ import annotations` is already present at file head.
  </acceptance_criteria>
  <done>Three regression-lock tests covering D-02 (node-id collision, conflicting-relation edge, confidence preservation) appended to tests/test_elicit.py and passing.</done>
</task>

<task type="auto">
  <name>Task 2: Add three failure-mode regression tests (ELIC-01 D-03)</name>
  <files>tests/test_elicit.py</files>
  <read_first>
    - tests/test_elicit.py — same file as Task 1. Re-read only if more than 30 minutes have elapsed; otherwise the file is cached in working memory.
    - graphify/elicit.py — confirm `load_elicitation_sidecar` behavior on malformed JSON: catches `JSONDecodeError`, prints `[graphify] elicitation sidecar invalid JSON` to stderr, returns `None` (per 57-RESEARCH.md Pitfall 3).
    - graphify/validate.py — confirm `validate_extraction` rejects nodes/edges with missing required fields and `save_elicitation_sidecar` calls it pre-write (per 57-RESEARCH.md Assumption A4).
    - graphify/build.py docstring around line 310 — confirms "dangling edges are expected for stdlib imports" precedent for the absent-node test.
    - 57-RESEARCH.md "Code Examples" section, snippets 3 and 4.
  </read_first>
  <action>
Append three more test functions to tests/test_elicit.py.

1. `test_malformed_sidecar_loader_returns_none(tmp_path: Path, capsys) -> None` — per D-03 malformed JSON. Create `adir = tmp_path / "art"`, `adir.mkdir()`, write `(adir / "elicitation.json").write_bytes(b"{this is not json")`. Assert `load_elicitation_sidecar(adir) is None`. Read `capsys.readouterr().err` and assert the substring `"[graphify] elicitation sidecar invalid JSON"` is present.

2. `test_sidecar_missing_required_fields_rejected(tmp_path: Path) -> None` — per D-03 missing required fields. Construct an extraction dict with one node missing the required `file_type` field: `{"nodes": [{"id": "x", "label": "X", "source_file": "f.py"}], "edges": []}`. Use `pytest.raises(ValueError)` to assert that `save_elicitation_sidecar(tmp_path / "art", bad_extraction, force=True)` raises (because save calls validate_extraction internally per A4). If validate_extraction returns errors (non-raising path), invoke it directly and assert non-empty list — but try save first. Concrete shape: the `with pytest.raises(ValueError):` block wraps the `save_elicitation_sidecar(...)` call.

3. `test_sidecar_edge_referencing_absent_node(tmp_path: Path) -> None` — per D-03 absent node. Elicitation has node `x` and edge `{"source":"x","target":"ghost","relation":"refs","confidence":"AMBIGUOUS","source_file":""}` ("ghost" not declared). Save via `save_elicitation_sidecar`. Build with empty base: `G = build(merge_elicitation_into_build_inputs([{"nodes":[],"edges":[]}], adir))`. Assert `"ghost" in G.nodes` (NetworkX `add_edge` auto-creates the missing endpoint as a bare node — matches build.py docstring "dangling edges are expected for stdlib imports").

Use `from __future__ import annotations` (already at file head; do not duplicate). All paths under `tmp_path`.
  </action>
  <verify>
    <automated>pytest tests/test_elicit.py::test_malformed_sidecar_loader_returns_none tests/test_elicit.py::test_sidecar_missing_required_fields_rejected tests/test_elicit.py::test_sidecar_edge_referencing_absent_node -x -q</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "def test_malformed_sidecar_loader_returns_none" tests/test_elicit.py` returns `1`
    - `grep -c "def test_sidecar_missing_required_fields_rejected" tests/test_elicit.py` returns `1`
    - `grep -c "def test_sidecar_edge_referencing_absent_node" tests/test_elicit.py` returns `1`
    - `grep -c '\[graphify\] elicitation sidecar invalid JSON' tests/test_elicit.py` returns `1` (assertion string is exact and quoted).
    - All three tests pass: pytest exit code 0 for the verify command above.
    - Total ELIC-01 task count: `grep -c "^def test_sidecar_\|^def test_malformed_sidecar_" tests/test_elicit.py` returns at least `5` (three from Task 1, three from Task 2 — node/edge collision + confidence + malformed + missing-fields + absent-node = 6, but two share the `test_sidecar_` prefix; relax to `>= 5` exact-prefix matches).
  </acceptance_criteria>
  <done>Three failure-mode regression tests covering D-03 (malformed JSON, missing required fields, absent node) appended and passing. Combined with Task 1, all six ELIC-01 sidecar-collision scenarios from 57-VALIDATION.md "Phase Requirements → Test Map" rows 1-6 are covered.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| user disk → graphify build pipeline | Sidecar JSON authored by `graphify elicit` lives at `artifacts_dir/elicitation.json` and is read by every subsequent `graphify run`. Tampering with this file directly affects `build()` output. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-57-01 | Tampering | `merge_elicitation_into_build_inputs` (graphify/elicit.py) | mitigate | Regression test `test_sidecar_node_id_collision_elicitation_wins` locks merge precedence. Future change that silently swaps "elicitation wins" → "base wins" trips the test. |
| T-57-02 | Denial of Service | `load_elicitation_sidecar` (graphify/elicit.py) | mitigate | Regression test `test_malformed_sidecar_loader_returns_none` locks the swallow-and-warn behavior. Future change that lets `json.JSONDecodeError` bubble crashes the build pipeline; test catches it. |
| T-57-03 | Input Validation (V5) | `save_elicitation_sidecar` → `validate_extraction` | mitigate | Regression test `test_sidecar_missing_required_fields_rejected` locks the schema-enforcement layer between elicitation authoring and the build graph. |
| T-57-04 | Information Disclosure | `validate_extraction` permissiveness for dangling edges | accept | Dangling endpoints auto-create bare nodes in NetworkX — documented behavior ("dangling edges are expected for stdlib imports"). Test asserts the behavior so future tightening is deliberate. |
</threat_model>

<verification>
After all tasks: `pytest tests/test_elicit.py -q` exits 0. The six new tests appear in the verbose listing.
</verification>

<success_criteria>
- All six new test functions exist in tests/test_elicit.py (verifiable via grep).
- `pytest tests/test_elicit.py -q` is green.
- No new imports beyond stdlib (json, pathlib already in file).
- ELIC-01 requirement is testably closed.
</success_criteria>

<output>
After completion, create `.planning/phases/57-elicitation-harness-increment/57-01-SUMMARY.md` describing: tests added, surface tested (build.py:281-307 + elicit.py merge helpers), no production code changes, ELIC-01 closed.
</output>
