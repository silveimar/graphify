---
phase: 57-elicitation-harness-increment
plan: 02
type: execute
wave: 2
depends_on: [57-01]
files_modified:
  - docs/ELICITATION.md
  - tests/test_elicit.py
  - tests/test_harness_interchange.py
autonomous: true
requirements: [ELIC-02, HARN-01]
must_haves:
  truths:
    - "docs/ELICITATION.md contains a `## Trust Boundaries` H2 section addressing three surfaces (elicitation read/write contract, import-harness will/won't, LLM trust posture during elicit)"
    - "docs/ELICITATION.md contains a `## Canonical Harness Interchange (v1) Mapping` H2 section with the `graphify.harness.interchange/v1` schema id and a field-mapping table"
    - "docs/ELICITATION.md contains a `## Milestone Non-Goals (v1.11)` H2 section (replaces or supersedes the current `## Non-goals (other phases)` section)"
    - "INTERCHANGE_SCHEMA_ID constant value `graphify.harness.interchange/v1` is locked by a regression test"
    - "Doc canonical-mapping section references the same schema id constant value as the code (drift lock per Pitfall 5)"
  artifacts:
    - path: docs/ELICITATION.md
      provides: "Three new H2 sections (Trust Boundaries, Canonical Harness Interchange (v1) Mapping, Milestone Non-Goals (v1.11))"
      contains: "## Trust Boundaries"
    - path: tests/test_elicit.py
      provides: "Three doc-content regression tests (ELIC-02 + HARN-01)"
      contains: "test_doc_has_trust_boundaries_section"
    - path: tests/test_harness_interchange.py
      provides: "Schema-id constant lock test (HARN-01)"
      contains: "test_interchange_schema_id_locked"
  key_links:
    - from: tests/test_elicit.py
      to: docs/ELICITATION.md
      via: "Path(__file__).resolve().parents[1] / 'docs' / 'ELICITATION.md'"
      pattern: 'docs.*ELICITATION\.md'
    - from: tests/test_harness_interchange.py
      to: graphify/harness_interchange.py
      via: "from graphify.harness_interchange import INTERCHANGE_SCHEMA_ID"
      pattern: "INTERCHANGE_SCHEMA_ID"
---

<objective>
Update docs/ELICITATION.md in place with three new H2 sections (per D-10, D-11, Open Question #3 → sibling H2s) and lock the new doc content + the canonical schema-id constant with regression tests. Doc and tests point at the same invariants from prose and code.

Purpose: Satisfy ELIC-02 (Trust Boundaries + Milestone Non-Goals) and HARN-01 (canonical harness interchange v1 mapping + schema-id lock-in) via in-place doc edit and three regression-lock tests.
Output: Edited docs/ELICITATION.md, three new tests in tests/test_elicit.py, one new test in tests/test_harness_interchange.py.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/57-elicitation-harness-increment/57-CONTEXT.md
@.planning/phases/57-elicitation-harness-increment/57-RESEARCH.md
@.planning/phases/57-elicitation-harness-increment/57-PATTERNS.md
@docs/ELICITATION.md
@graphify/harness_interchange.py
@tests/test_elicit.py
@tests/test_harness_interchange.py

<interfaces>
From graphify/harness_interchange.py:
```python
INTERCHANGE_SCHEMA_ID: str = "graphify.harness.interchange/v1"
def graph_data_to_extraction(graph_data: dict) -> dict
def export_interchange_v1(graph_data: dict, out_path: Path | None) -> dict
```

From graphify/output.py:67:
```python
def is_obsidian_vault(path: Path) -> bool  # Strict CWD-only — presence of `.obsidian/`
def resolve_output(...) -> ResolvedOutput  # Single resolution path
```

From graphify/security.py:
```python
MAX_HARNESS_IMPORT_BYTES: int  # Phase 40 cap
def sanitize_harness_text(text: str) -> str  # Phase 40 — referenced, not re-tested
def validate_graph_path(path: Path, base: Path) -> None  # raises ValueError/OSError
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Edit docs/ELICITATION.md — add three H2 sections in place</name>
  <files>docs/ELICITATION.md</files>
  <read_first>
    - docs/ELICITATION.md (entire file, 52 lines) — match existing voice (backtick-bold for code identifiers, single-row tables for artifact mappings, stderr-warning prose). Existing section order: "When to use", "CLI", "Where artifacts land", "Merge order", "Where sidecar merge runs", "Non-goals (other phases)".
    - 57-PATTERNS.md "docs/ELICITATION.md (documentation, in-place edit)" section — section heading style, token style, voice, exact placement guidance.
    - 57-RESEARCH.md Open Question #3 — sibling H2s (NOT subsections of `## Trust Boundaries`).
    - 57-CONTEXT.md D-10 (in-place edit, two new sections plus the canonical-mapping section per HARN-01 D-07), D-11 (three surfaces in Trust Boundaries), D-12 (sidecar merge precedence NOT separately documented; reference test module).
    - graphify/output.py:40-200 — `resolve_output()` contract for the "where elicitation reads/writes" subsection.
    - graphify/harness_interchange.py — `INTERCHANGE_SCHEMA_ID = "graphify.harness.interchange/v1"` and `graph_data_to_extraction` for the canonical mapping table.
  </read_first>
  <action>
Edit docs/ELICITATION.md in place. Make exactly three edits:

**Edit A — Insert `## Trust Boundaries` section** between the existing `## Where sidecar merge runs` section and the existing `## Non-goals (other phases)` section. Content (use this exact prose, adapted to existing voice):

```markdown
## Trust Boundaries

This milestone (v1.11) clarifies what `elicit` and `import-harness` will and will not do, and where the trust line sits.

### Where elicitation reads / writes

`graphify elicit` resolves its artifacts directory through **`resolve_output()`** (see `graphify/output.py`). With a vault + profile, the sidecar lands at `<artifacts_dir>/elicitation.json` (typically the sibling `graphify-out/` next to the vault); without a vault, it lands at `./graphify-out/elicitation.json`. The pipeline never reads vault configuration without explicit user consent (the user must invoke `--vault` or `--profile`).

The sidecar merge precedence (elicitation overwrites base extraction on node-id collisions; conflicting edge attrs follow last-write-wins) is asserted by the regression suite at `tests/test_elicit.py` (Phase 57 ELIC-01 tests). That test module is the canonical record of the merge contract.

### What `import-harness` will and will not do

`graphify import-harness` is **off by default**: no other graphify command (`run`, `watch`, `update-vault`, `elicit`, `doctor`) invokes it transitively. It refuses to write reconstructed extractions under any vault root unless the user passes `--allow-vault-write`. The MCP `import_harness` tool requires an explicit `path` argument; missing or empty paths are rejected by `validate_graph_path`.

### LLM trust posture during `elicit`

Free-text answers from `--demo` and interactive elicitation are passed through `sanitize_harness_text` (`graphify/security.py`) before reaching downstream LLM calls or HTML / Obsidian export. Labels are HTML-escaped at export time (see `graphify/security.py::sanitize_label`). Size caps (`MAX_HARNESS_IMPORT_BYTES`) and prompt-injection guard patterns (`guard_harness_injection_patterns`) were established in Phase 40 and apply unchanged.
```

**Edit B — Insert `## Canonical Harness Interchange (v1) Mapping` section** immediately after the new `## Trust Boundaries` section. Content:

```markdown
## Canonical Harness Interchange (v1) Mapping

The harness interchange envelope (`graphify.harness.interchange/v1`, exported by `export_interchange_v1` in `graphify/harness_interchange.py`) is the canonical input format for `graphify import-harness`. Field mapping mirrors `graph_data_to_extraction()` exactly:

| Envelope field | Maps to extraction key | Notes |
|----------------|------------------------|-------|
| `schema` | `INTERCHANGE_SCHEMA_ID` constant (`"graphify.harness.interchange/v1"`) | Required at top level; mismatched schema is rejected by `_parse_interchange_v1`. |
| `graph.nodes[]` | `nodes[]` | Each node carries `id`, `label`, `file_type`, `source_file`. |
| `graph.edges[]` | `edges[]` | Each edge carries `source`, `target`, `relation`, `confidence`, `source_file`. |
| `provenance` | dropped | Provenance is consumed for audit, not merged into the graph. |

A future bump to `/v2` MUST update the `schema` row above. The schema-id constant is asserted to match this doc by `tests/test_harness_interchange.py::test_interchange_schema_id_locked`.
```

**Edit C — Replace the existing `## Non-goals (other phases)` heading** with `## Milestone Non-Goals (v1.11)` and append (do not delete the existing bullets) three new bullets at the end of that section:

```markdown
- **Real inverse round-trip** (harness export → import → graph equality) — deferred to a future harness-expansion phase.
- **New harness target formats** beyond what is shipped today — orthogonal capability extension.
- **Re-testing Phase 40 size caps and prompt-injection guards** (`MAX_HARNESS_IMPORT_BYTES`, `guard_harness_injection_patterns`) — already covered.
```

(The existing `- **Phase 40** — harness import, ...` and `- **Phase 41** — `--vault` selector / multi-root UX productization.` bullets remain at the top of the section.)

Total file growth: ~50 lines added, the heading at line for the existing non-goals section renamed in place. No other edits.
  </action>
  <verify>
    <automated>grep -c "^## Trust Boundaries$" docs/ELICITATION.md && grep -c "^## Canonical Harness Interchange (v1) Mapping$" docs/ELICITATION.md && grep -c "^## Milestone Non-Goals (v1.11)$" docs/ELICITATION.md && grep -c "graphify.harness.interchange/v1" docs/ELICITATION.md</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "^## Trust Boundaries$" docs/ELICITATION.md` returns `1`
    - `grep -c "^## Canonical Harness Interchange (v1) Mapping$" docs/ELICITATION.md` returns `1`
    - `grep -c "^## Milestone Non-Goals (v1.11)$" docs/ELICITATION.md` returns `1`
    - `grep -c "^## Non-goals (other phases)$" docs/ELICITATION.md` returns `0` (heading was renamed, not duplicated)
    - `grep -c "graphify.harness.interchange/v1" docs/ELICITATION.md` returns at least `1`
    - `grep -c "resolve_output" docs/ELICITATION.md` returns at least `1` (already present in original; must not regress)
    - `grep -c "tests/test_elicit.py" docs/ELICITATION.md` returns at least `1` (per D-12, doc references the test module as canonical record)
    - `grep -c "allow-vault-write" docs/ELICITATION.md` returns at least `1`
  </acceptance_criteria>
  <done>docs/ELICITATION.md has three new H2 sections in place; existing sections preserved; canonical schema id appears in the doc.</done>
</task>

<task type="auto">
  <name>Task 2: Add three doc-content regression tests + schema-id constant lock</name>
  <files>tests/test_elicit.py, tests/test_harness_interchange.py</files>
  <read_first>
    - tests/test_elicit.py (full file after Plan 57-01 changes, ~200 lines) — append-only edits.
    - tests/test_harness_interchange.py (full file) — match existing imports/style.
    - 57-PATTERNS.md "Doc-content regression-lock test" shared pattern — `Path(__file__).resolve().parents[1] / "docs" / "ELICITATION.md"`, `read_text(encoding="utf-8")`, `assert "## Trust Boundaries" in text`.
    - 57-RESEARCH.md "Code Examples: HARN-01: schema-id constant lock-in + doc parity" — exact assertion shape.
    - graphify/harness_interchange.py — confirm the public name `INTERCHANGE_SCHEMA_ID` and exact value `"graphify.harness.interchange/v1"`.
  </read_first>
  <action>
Append the following test functions:

**In tests/test_elicit.py (append after Plan 57-01 additions):**

1. `test_doc_has_trust_boundaries_section() -> None` — read `docs/ELICITATION.md` via `Path(__file__).resolve().parents[1] / "docs" / "ELICITATION.md"`, `read_text(encoding="utf-8")`. Assert each of these substrings is present (one assertion per substring for readable failures): `"## Trust Boundaries"`, `"resolve_output"`, `"<artifacts_dir>/elicitation.json"`, `"sanitize_harness_text"`.

2. `test_doc_has_milestone_non_goals_section() -> None` — same file load. Assert `"## Milestone Non-Goals (v1.11)"` is present and `"## Non-goals (other phases)"` is NOT present (the heading was renamed). Also assert `"Real inverse round-trip"` substring is present.

3. `test_doc_has_canonical_mapping() -> None` — same file load. Assert `"## Canonical Harness Interchange (v1) Mapping"` is present, `"graphify.harness.interchange/v1"` is present, and `"graph_data_to_extraction"` is present (the function whose mapping the prose mirrors).

**In tests/test_harness_interchange.py (append):**

4. `test_interchange_schema_id_locked() -> None` — `from graphify.harness_interchange import INTERCHANGE_SCHEMA_ID`. Assert `INTERCHANGE_SCHEMA_ID == "graphify.harness.interchange/v1"`. Then read `docs/ELICITATION.md` via the same `Path(__file__).resolve().parents[1] / "docs" / "ELICITATION.md"` pattern and assert `INTERCHANGE_SCHEMA_ID in doc_text` (drift lock per Pitfall 5; cheap and self-healing).

All four functions: no new top-level imports beyond what's already in each test file (both files already import `Path`; tests/test_harness_interchange.py already imports `INTERCHANGE_SCHEMA_ID` if it follows the project convention — verify and add if absent).
  </action>
  <verify>
    <automated>pytest tests/test_elicit.py::test_doc_has_trust_boundaries_section tests/test_elicit.py::test_doc_has_milestone_non_goals_section tests/test_elicit.py::test_doc_has_canonical_mapping tests/test_harness_interchange.py::test_interchange_schema_id_locked -x -q</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "def test_doc_has_trust_boundaries_section" tests/test_elicit.py` returns `1`
    - `grep -c "def test_doc_has_milestone_non_goals_section" tests/test_elicit.py` returns `1`
    - `grep -c "def test_doc_has_canonical_mapping" tests/test_elicit.py` returns `1`
    - `grep -c "def test_interchange_schema_id_locked" tests/test_harness_interchange.py` returns `1`
    - All four tests pass: pytest exit code 0 for the verify command above.
    - `grep -c 'INTERCHANGE_SCHEMA_ID == "graphify.harness.interchange/v1"' tests/test_harness_interchange.py` returns `1`
    - `grep -c 'docs.*ELICITATION\.md' tests/test_elicit.py` returns at least `3` (one per doc-content test).
  </acceptance_criteria>
  <done>Three doc-content tests in tests/test_elicit.py and one schema-id constant lock in tests/test_harness_interchange.py all pass. ELIC-02 and HARN-01 doc/code parity is locked.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| code constants ↔ user-facing prose | Schema id constant `INTERCHANGE_SCHEMA_ID` lives in `graphify/harness_interchange.py`; prose in `docs/ELICITATION.md` describes it. Drift between the two is invisible without a lock test. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-57-05 | Tampering | docs/ELICITATION.md drifts from `INTERCHANGE_SCHEMA_ID` after a future schema bump | mitigate | `test_interchange_schema_id_locked` reads the constant and asserts it appears in the doc text — fails immediately on drift. |
| T-57-06 | Information Disclosure | Trust-boundary doc fails to surface vault-write refusal contract; user assumes `import-harness` can land anywhere | mitigate | `test_doc_has_trust_boundaries_section` asserts substrings including `allow-vault-write` and `resolve_output` — locks the contract description into the doc. |
| T-57-07 | Repudiation | Future contributor renames `## Trust Boundaries` heading without test update | mitigate | grep-style substring assertions in three tests; renaming any of three required H2s breaks tests visibly. |
</threat_model>

<verification>
After both tasks: `pytest tests/test_elicit.py tests/test_harness_interchange.py -q` exits 0. `docs/ELICITATION.md` has three new H2s.
</verification>

<success_criteria>
- All three new H2 sections present in docs/ELICITATION.md (verifiable via grep).
- All four new test functions exist and pass.
- ELIC-02 closed (Trust Boundaries + Milestone Non-Goals documented in place).
- HARN-01 closed (canonical mapping prose + schema-id constant lock).
</success_criteria>

<output>
After completion, create `.planning/phases/57-elicitation-harness-increment/57-02-SUMMARY.md` describing: doc edit (3 H2 sections), 3 doc-content tests, 1 schema-id lock test, ELIC-02 + HARN-01 closed.
</output>
