---
phase: 11-narrative-mode-slash-commands
plan: 07
type: execute
wave: 6
status: conditional
conditional_reason: "Stretch (SLASH-06, SLASH-07) per ROADMAP + D-17 — ship ONLY if plans 11-01..11-06 came in under ~60% of planning-budget estimate. Otherwise defer to v1.4 sibling skill. Task 0 is a hard gate — Tasks 1-3 execute ONLY if Task 0 records GATE: proceed in 11-07-SUMMARY.md."
depends_on: [06]
files_modified:
  - graphify/commands/ghost.md
  - graphify/commands/challenge.md
  - tests/test_commands.py
autonomous: true
requirements: [SLASH-06, SLASH-07]
requirements_addressed: [SLASH-06, SLASH-07]
tags: [commands, stretch, phase-11]

must_haves:
  truths:
    - "`/ghost` command file exists at `graphify/commands/ghost.md` with documented fallback behavior when invoked against a vault with no prior graph"
    - "`/challenge <belief>` command file exists at `graphify/commands/challenge.md` with `$ARGUMENTS` parsing and documented fallback behavior"
    - "Both files route to the existing MCP tool surface (query_graph + get_annotations + get_agent_edges) — no new MCP tools are added in this plan (D-18 applies: no new graphify/ modules or algorithms)"
    - "Both files include `no_graph` fallback guard with verbatim hint"
    - "Test file tests/test_commands.py extends to assert the two stretch files exist and reference valid existing MCP tools"
    - "Tasks 1-3 execute ONLY if Task 0 records GATE: proceed in 11-07-SUMMARY.md. If GATE: defer, SUMMARY.md records the reason and subsequent tasks are skipped (plan-checker WARNING 4 fix — gate verify is now grep-verifiable and downstream tasks short-circuit if the gate says defer)."
  artifacts:
    - path: graphify/commands/ghost.md
      provides: "Slash-command prompt for /ghost — grounds response in user's annotations and voice patterns extracted from graph"
      contains: "get_annotations, no_graph, $ARGUMENTS"
    - path: graphify/commands/challenge.md
      provides: "Slash-command prompt for /challenge <belief> — pressure-tests belief against supporting vs contradicting graph edges"
      contains: "query_graph, $ARGUMENTS, supporting, contradicting, no_graph"
    - path: tests/test_commands.py
      provides: "Extended coverage asserting both stretch files exist with correct frontmatter"
      contains: "test_stretch_command_files_exist, test_challenge_md_has_evidence_sections"
  key_links:
    - from: "graphify/commands/ghost.md"
      to: "graphify/serve.py existing `get_annotations` MCP tool"
      via: "tool name reference"
      pattern: "MCP tool `get_annotations`"
    - from: "graphify/commands/challenge.md"
      to: "graphify/serve.py existing `query_graph` + `get_neighbors` MCP tools"
      via: "multi-tool orchestration"
      pattern: "MCP tool `query_graph`"
---

**Header note (plan-checker WARNING 4 fix):** Tasks 1-3 execute only if Task 0 records `GATE: proceed` in 11-07-SUMMARY.md. If `GATE: defer`, SUMMARY.md records the reason and subsequent tasks are skipped without modifying source files.

<objective>
**CONDITIONAL PLAN** — Ships SLASH-06 (`/ghost`) and SLASH-07 (`/challenge`) ONLY if earlier plans (11-01..11-06) came in under ~60% of planning budget. Otherwise this plan is deferred to a v1.4 sibling skill per D-17.

Both stretch commands ship as pure `.md` prompt files. They consume the EXISTING MCP tool surface (no new server tools):
- `/ghost` uses `get_annotations` (already in serve.py) to pull the user's own annotation text + voice patterns + their contributed nodes → prompt instructs Claude to adopt that voice.
- `/challenge <belief>` uses `query_graph` + `get_neighbors` to surface edges supporting vs. contradicting a belief, then instructs Claude to render evidence for-and-against.

Purpose: Complete Phase 11's requirement coverage for SLASH-06 and SLASH-07 without requiring new graphify/ code. Everything is prompt engineering against existing MCP tools.

Output: 2 `.md` files + extended tests.

**Decision gate:** Before starting, orchestrator or user MUST confirm that the earlier 6 plans completed under the ~60% threshold. If not, mark this plan as deferred in ROADMAP and STATE.md and do not execute. The gate decision MUST be recorded as a single grep-verifiable line `GATE: proceed` or `GATE: defer` at the top of 11-07-SUMMARY.md (plan-checker WARNING 4 fix).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/11-narrative-mode-slash-commands/11-CONTEXT.md
@.planning/phases/11-narrative-mode-slash-commands/11-RESEARCH.md
@.planning/phases/11-narrative-mode-slash-commands/11-PATTERNS.md
@.planning/phases/11-narrative-mode-slash-commands/11-06-SUMMARY.md
@graphify/serve.py
@graphify/commands/context.md
@graphify/commands/trace.md
@tests/test_commands.py

<interfaces>
<!-- Existing MCP tools (from serve.py) that the stretch commands invoke: -->

| Tool | Input | Output | Used by |
|------|-------|--------|---------|
| get_annotations | peer_id?: str, node_id?: str | list of annotations with author/voice metadata | /ghost |
| query_graph | seed_nodes: list[str], depth: int, token_budget: int | hybrid envelope with node + edge subgraph | /challenge |
| get_neighbors | node: str, depth: int | list of neighbor IDs with edge confidences | /challenge |
| god_nodes | top_n: int | list of most-connected nodes | /ghost (for fallback identity inference) |
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 0 (PRE-GATE): Confirm execution gate and record decision</name>
  <files>.planning/phases/11-narrative-mode-slash-commands/11-07-SUMMARY.md</files>
  <read_first>
    - .planning/STATE.md (budget / progress check)
    - .planning/phases/11-narrative-mode-slash-commands/11-01-SUMMARY.md through 11-06-SUMMARY.md
    - .planning/phases/11-narrative-mode-slash-commands/11-CONTEXT.md (D-17)
  </read_first>
  <action>
Check execution budget before proceeding:

1. Read all 6 prior SUMMARY files (11-01..11-06) to gather actual context consumption estimates.
2. Sum reported budget utilization. If total < 60% of planning estimate → proceed to Task 1.
3. If ≥ 60% → STOP. Mark this plan as `status: deferred` by updating ROADMAP to note deferral to v1.4 sibling skill. Do NOT execute Tasks 1–3.

**CRITICAL (plan-checker WARNING 4 fix):** Write the gate decision to the FIRST LINE of `.planning/phases/11-narrative-mode-slash-commands/11-07-SUMMARY.md`. The line must be either:
- `GATE: proceed` — Tasks 1-3 execute normally
- `GATE: defer` — Tasks 1-3 are skipped; SUMMARY records the deferral reason

Subsequent tasks (1, 2, 3) include a conditional guard in their verify blocks that short-circuits success if GATE: defer was recorded — this prevents accidental execution of the stretch commands when the gate was closed.

If `defer`: this plan remains in the repo as documentation of what was intended; no files are modified beyond 11-07-SUMMARY.md; the SUMMARY records the deferral reason and cites D-17.
  </action>
  <verify>
    <automated>test -f .planning/phases/11-narrative-mode-slash-commands/11-06-SUMMARY.md</automated>
    <automated>grep -q '^GATE:' .planning/phases/11-narrative-mode-slash-commands/11-07-SUMMARY.md 2>/dev/null</automated>
  </verify>
  <acceptance_criteria>
    - Prior SUMMARYs read
    - Decision (proceed or defer) recorded as `^GATE:` line in `.planning/phases/11-narrative-mode-slash-commands/11-07-SUMMARY.md` (plan-checker WARNING 4 fix — grep-verifiable)
    - If defer: no file edits occur in Tasks 1–3 (their verify short-circuits)
    - If proceed: Tasks 1–3 execute normally
  </acceptance_criteria>
  <done>
    Gate decision made and recorded as a grep-verifiable `GATE: proceed` or `GATE: defer` line at the top of 11-07-SUMMARY.md. Subsequent tasks conditional on the decision.
  </done>
</task>

<task type="auto">
  <name>Task 1: Create graphify/commands/ghost.md (SLASH-06)</name>
  <files>graphify/commands/ghost.md</files>
  <read_first>
    - graphify/commands/context.md (tone + structure reference from plan 11-04)
    - graphify/skill.md (voice reference)
    - .planning/phases/11-narrative-mode-slash-commands/11-CONTEXT.md (D-01..D-03, D-09, D-10)
    - .planning/phases/11-narrative-mode-slash-commands/11-PATTERNS.md (command file format)
    - graphify/serve.py (get_annotations tool signature — see list_tools() block)
    - .planning/phases/11-narrative-mode-slash-commands/11-07-SUMMARY.md (check GATE line — if `GATE: defer`, skip this task)
  </read_first>
  <action>
**Plan-checker WARNING 4 gate guard:** Before proceeding, grep-check the SUMMARY for the gate decision:
```bash
grep -q '^GATE: proceed' .planning/phases/11-narrative-mode-slash-commands/11-07-SUMMARY.md
```
If this check fails (i.e. `GATE: defer` recorded OR no GATE line exists), skip this task and record the skip in the SUMMARY. Do NOT create ghost.md.

If the gate is `proceed`, write `graphify/commands/ghost.md`:

```markdown
---
name: ghost
description: Answer in the user's voice, grounded in their graph contributions and annotations.
argument-hint: <question or prompt>
disable-model-invocation: true
---

Question or prompt to answer as the user: $ARGUMENTS

Step 1: Call the graphify MCP tool `get_annotations` with:
- `peer_id`: "self"  (falls back to most-annotated peer if "self" has no annotations)

Step 2: Call the graphify MCP tool `god_nodes` with:
- `top_n`: 10

Parse `meta.status` on both responses.

**If either response has `status` == `no_graph`:** render verbatim:
> No graph found at graphify-out/graph.json. Run `/graphify` to build one, then re-invoke this command.

**If `get_annotations` returns an empty list:** render:
> No annotations found — /ghost needs your own notes or rationales in the graph to learn your voice.
> Annotate nodes via `/graphify` workflow (rationale / peer_id="self"), then re-invoke.

**Otherwise:** render in the user's voice:
1. Extract voice patterns from the returned annotations — vocabulary, sentence structure, tone, idiom.
2. Extract the user's conceptual concerns from `god_nodes` — what they've been thinking about most.
3. Answer "$ARGUMENTS" AS THE USER — first-person, using their vocabulary, drawing on their conceptual universe.
4. Do NOT pretend to be a different person — this is reflective, not impersonation. Make clear the voice is learned from the user's own graph contributions.
5. Keep under 500 tokens.

End with a thinking-partner beat: "This is how you might frame it — does it match how you'd actually answer?"
```
  </action>
  <verify>
    <automated>grep -q '^GATE: defer' .planning/phases/11-narrative-mode-slash-commands/11-07-SUMMARY.md 2>/dev/null && exit 0 || test -f graphify/commands/ghost.md</automated>
    <automated>grep -q '^GATE: defer' .planning/phases/11-narrative-mode-slash-commands/11-07-SUMMARY.md 2>/dev/null && exit 0 || grep -q 'name: ghost' graphify/commands/ghost.md</automated>
    <automated>grep -q '^GATE: defer' .planning/phases/11-narrative-mode-slash-commands/11-07-SUMMARY.md 2>/dev/null && exit 0 || grep -q '\$ARGUMENTS' graphify/commands/ghost.md</automated>
    <automated>grep -q '^GATE: defer' .planning/phases/11-narrative-mode-slash-commands/11-07-SUMMARY.md 2>/dev/null && exit 0 || grep -q 'get_annotations' graphify/commands/ghost.md</automated>
    <automated>grep -q '^GATE: defer' .planning/phases/11-narrative-mode-slash-commands/11-07-SUMMARY.md 2>/dev/null && exit 0 || grep -q 'no_graph' graphify/commands/ghost.md</automated>
    <automated>grep -q '^GATE: defer' .planning/phases/11-narrative-mode-slash-commands/11-07-SUMMARY.md 2>/dev/null && exit 0 || grep -q 'disable-model-invocation: true' graphify/commands/ghost.md</automated>
  </verify>
  <acceptance_criteria>
    - If `GATE: defer`: task is a no-op (all verify commands short-circuit to success via `&& exit 0`)
    - If `GATE: proceed`:
      - `test -f graphify/commands/ghost.md` exits 0
      - `grep 'name: ghost' graphify/commands/ghost.md` matches
      - `grep '\$ARGUMENTS' graphify/commands/ghost.md` matches
      - `grep 'get_annotations' graphify/commands/ghost.md` matches
      - `grep 'god_nodes' graphify/commands/ghost.md` matches
      - `grep 'no_graph' graphify/commands/ghost.md` matches
      - `grep 'disable-model-invocation: true' graphify/commands/ghost.md` matches
  </acceptance_criteria>
  <done>
    If gate=proceed: ghost.md exists, correct frontmatter, correct MCP tool references, no-graph guard present.
    If gate=defer: task is a no-op; verify commands short-circuit successfully.
  </done>
</task>

<task type="auto">
  <name>Task 2: Create graphify/commands/challenge.md (SLASH-07)</name>
  <files>graphify/commands/challenge.md</files>
  <read_first>
    - graphify/commands/connect.md (multi-section rendering reference from plan 11-04)
    - graphify/skill.md (voice reference)
    - .planning/phases/11-narrative-mode-slash-commands/11-CONTEXT.md (D-01..D-03, D-09, D-10)
    - graphify/serve.py (query_graph and get_neighbors tool signatures)
    - .planning/phases/11-narrative-mode-slash-commands/11-07-SUMMARY.md (check GATE line — if `GATE: defer`, skip this task)
  </read_first>
  <action>
**Plan-checker WARNING 4 gate guard:** Before proceeding, grep-check the SUMMARY:
```bash
grep -q '^GATE: proceed' .planning/phases/11-narrative-mode-slash-commands/11-07-SUMMARY.md
```
If the gate is `defer` or missing, skip this task. Do NOT create challenge.md.

If the gate is `proceed`, write `graphify/commands/challenge.md`:

```markdown
---
name: challenge
description: Pressure-test a stated belief against graph evidence — surface supporting vs contradicting edges.
argument-hint: <belief>
disable-model-invocation: true
---

Belief to pressure-test: $ARGUMENTS

Step 1: Identify concepts in "$ARGUMENTS". Extract the 2–4 most-likely-to-be-nodes noun phrases from the belief.

Step 2: Call the graphify MCP tool `query_graph` with:
- `seed_nodes`: [identified concepts]
- `depth`: 2
- `budget`: 500

Parse `meta.status`.

**If `status` is `no_graph`:** render verbatim:
> No graph found at graphify-out/graph.json. Run `/graphify` to build one, then re-invoke this command.

**If no seed nodes matched any graph node:** render:
> No nodes in the current graph match the concepts in your belief. Try shorter or more specific terminology.

**If `status` is `ok`:** classify the returned edges into two buckets by confidence and relation type:

- **Supporting edges**: edges with `relation` of type `supports`, `extends`, `cites`, `defines`, or with `EXTRACTED` confidence where both endpoints are in the belief's concept set
- **Contradicting edges**: edges with `relation` of type `refutes`, `contradicts`, `opposes`, `differs_from`, or `AMBIGUOUS` confidence edges that link the belief's concepts to nodes in a DIFFERENT community

Render TWO DISTINCT SECTIONS:

    ## Evidence supporting
    [3–5 supporting edges — label, relation, source_file]

    ## Evidence contradicting
    [3–5 contradicting or missing-support gaps — label, relation, source_file]

If one side is empty, state so plainly — do NOT fabricate evidence.

End with a thinking-partner question: "The graph leans [toward / against / ambiguously on] this belief. Want to `/trace` the strongest supporting or contradicting node?"

Keep under 500 tokens.
```
  </action>
  <verify>
    <automated>grep -q '^GATE: defer' .planning/phases/11-narrative-mode-slash-commands/11-07-SUMMARY.md 2>/dev/null && exit 0 || test -f graphify/commands/challenge.md</automated>
    <automated>grep -q '^GATE: defer' .planning/phases/11-narrative-mode-slash-commands/11-07-SUMMARY.md 2>/dev/null && exit 0 || grep -q 'name: challenge' graphify/commands/challenge.md</automated>
    <automated>grep -q '^GATE: defer' .planning/phases/11-narrative-mode-slash-commands/11-07-SUMMARY.md 2>/dev/null && exit 0 || grep -q '\$ARGUMENTS' graphify/commands/challenge.md</automated>
    <automated>grep -q '^GATE: defer' .planning/phases/11-narrative-mode-slash-commands/11-07-SUMMARY.md 2>/dev/null && exit 0 || grep -q 'query_graph' graphify/commands/challenge.md</automated>
    <automated>grep -q '^GATE: defer' .planning/phases/11-narrative-mode-slash-commands/11-07-SUMMARY.md 2>/dev/null && exit 0 || grep -q 'Evidence supporting' graphify/commands/challenge.md</automated>
    <automated>grep -q '^GATE: defer' .planning/phases/11-narrative-mode-slash-commands/11-07-SUMMARY.md 2>/dev/null && exit 0 || grep -q 'Evidence contradicting' graphify/commands/challenge.md</automated>
    <automated>grep -q '^GATE: defer' .planning/phases/11-narrative-mode-slash-commands/11-07-SUMMARY.md 2>/dev/null && exit 0 || grep -q 'no_graph' graphify/commands/challenge.md</automated>
    <automated>grep -q '^GATE: defer' .planning/phases/11-narrative-mode-slash-commands/11-07-SUMMARY.md 2>/dev/null && exit 0 || grep -q 'do NOT fabricate' graphify/commands/challenge.md</automated>
  </verify>
  <acceptance_criteria>
    - If `GATE: defer`: task is a no-op (all verify commands short-circuit via `&& exit 0`)
    - If `GATE: proceed`:
      - `test -f graphify/commands/challenge.md` exits 0
      - `grep 'name: challenge' graphify/commands/challenge.md` matches
      - `grep '\$ARGUMENTS' graphify/commands/challenge.md` matches
      - `grep 'argument-hint: <belief>' graphify/commands/challenge.md` matches
      - `grep 'query_graph' graphify/commands/challenge.md` matches
      - `grep 'Evidence supporting' graphify/commands/challenge.md` matches
      - `grep 'Evidence contradicting' graphify/commands/challenge.md` matches
      - `grep 'no_graph' graphify/commands/challenge.md` matches
      - `grep 'do NOT fabricate' graphify/commands/challenge.md` matches (hallucination guard)
      - `grep 'disable-model-invocation: true' graphify/commands/challenge.md` matches
  </acceptance_criteria>
  <done>
    If gate=proceed: challenge.md exists with frontmatter, $ARGUMENTS, two distinct evidence sections, explicit anti-fabrication guard.
    If gate=defer: task is a no-op; verify commands short-circuit successfully.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Extend tests/test_commands.py for stretch files</name>
  <files>tests/test_commands.py</files>
  <read_first>
    - tests/test_commands.py (post-plan-11-04 — existing CORE_COMMANDS constant and test patterns)
    - graphify/commands/ghost.md (post-Task-1)
    - graphify/commands/challenge.md (post-Task-2)
    - .planning/phases/11-narrative-mode-slash-commands/11-07-SUMMARY.md (check GATE line — if `GATE: defer`, skip this task)
  </read_first>
  <behavior>
    - If GATE: defer — task is a no-op.
    - If GATE: proceed:
      - `test_stretch_command_files_exist`: ghost.md and challenge.md both exist
      - `test_stretch_command_files_have_required_frontmatter`: both files have name, description, argument-hint, disable-model-invocation: true
      - `test_stretch_command_files_have_no_graph_guard`: both files contain `no_graph`
      - `test_ghost_md_references_get_annotations`: ghost.md references the `get_annotations` existing MCP tool
      - `test_challenge_md_has_evidence_sections`: challenge.md contains both `Evidence supporting` and `Evidence contradicting` and `Evidence supporting` comes before `Evidence contradicting`
      - `test_challenge_md_has_anti_fabrication_guard`: challenge.md contains `do NOT fabricate`
  </behavior>
  <action>
**Plan-checker WARNING 4 gate guard:** Before proceeding, grep-check the SUMMARY. If `GATE: defer`, skip this task.

If gate is `proceed`, extend `tests/test_commands.py` by adding:

```python
STRETCH_COMMANDS = {
    "ghost": "get_annotations",
    "challenge": "query_graph",
}


def test_stretch_command_files_exist():
    for name in STRETCH_COMMANDS:
        assert (_commands_dir() / f"{name}.md").exists(), f"Missing commands/{name}.md"


def test_stretch_command_files_have_required_frontmatter():
    for name in STRETCH_COMMANDS:
        text = _read(name)
        assert text.startswith("---\n"), f"{name}.md missing YAML frontmatter opener"
        for field in (f"name: {name}", "description:", "argument-hint:", "disable-model-invocation: true"):
            assert field in text, f"{name}.md missing {field!r}"


def test_stretch_command_files_have_no_graph_guard():
    for name in STRETCH_COMMANDS:
        assert "no_graph" in _read(name), f"{name}.md missing no_graph guard"


def test_ghost_md_references_get_annotations():
    assert "get_annotations" in _read("ghost")


def test_challenge_md_has_evidence_sections():
    text = _read("challenge")
    idx_support = text.find("Evidence supporting")
    idx_contra = text.find("Evidence contradicting")
    assert idx_support != -1 and idx_contra != -1
    assert idx_support < idx_contra, "Evidence supporting must come before Evidence contradicting"


def test_challenge_md_has_anti_fabrication_guard():
    assert "do NOT fabricate" in _read("challenge"), "challenge.md must include anti-fabrication guard"
```

Ensure existing tests (CORE_COMMANDS-scoped) are unchanged.
  </action>
  <verify>
    <automated>grep -q '^GATE: defer' .planning/phases/11-narrative-mode-slash-commands/11-07-SUMMARY.md 2>/dev/null && exit 0 || pytest tests/test_commands.py -q</automated>
    <automated>grep -q '^GATE: defer' .planning/phases/11-narrative-mode-slash-commands/11-07-SUMMARY.md 2>/dev/null && exit 0 || pytest tests/test_commands.py::test_stretch_command_files_exist -q</automated>
    <automated>grep -q '^GATE: defer' .planning/phases/11-narrative-mode-slash-commands/11-07-SUMMARY.md 2>/dev/null && exit 0 || pytest tests/test_commands.py::test_challenge_md_has_evidence_sections -q</automated>
  </verify>
  <acceptance_criteria>
    - If `GATE: defer`: task is a no-op (all verify commands short-circuit via `&& exit 0`)
    - If `GATE: proceed`:
      - `grep 'STRETCH_COMMANDS' tests/test_commands.py` matches
      - `grep 'def test_stretch_command_files_exist' tests/test_commands.py` matches
      - `grep 'def test_challenge_md_has_evidence_sections' tests/test_commands.py` matches
      - `grep 'def test_ghost_md_references_get_annotations' tests/test_commands.py` matches
      - `grep 'def test_challenge_md_has_anti_fabrication_guard' tests/test_commands.py` matches
      - `pytest tests/test_commands.py -q` exits 0 (all prior + 6 new tests pass)
  </acceptance_criteria>
  <done>
    If gate=proceed: Stretch files have full test coverage; all prior tests still pass.
    If gate=defer: task is a no-op; verify commands short-circuit successfully.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| User belief text ($ARGUMENTS for /challenge) → prompt → MCP | Free-form user text becomes seed input to `query_graph`; echoed in rendered response |
| User's annotations (for /ghost) → response text | User's own authored content from graph annotations is reflected back |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-11-07-01 | Tampering | $ARGUMENTS passed to query_graph (ghost/challenge) | mitigate | The MCP tool `query_graph` already calls sanitize_label() on all seed_nodes per existing serve.py — no new sanitization needed in the .md prompt |
| T-11-07-02 | Spoofing | /ghost could be used to impersonate others | mitigate | Prompt explicitly requires `peer_id="self"` and includes "Do NOT pretend to be a different person" — narrative guard. No runtime enforcement at MCP layer, but this is a prompt-only feature with no destructive side effects |
| T-11-07-03 | Information Disclosure | /ghost reflects user's annotations verbatim in response | accept | Annotations are already authored by the user; echoing back to the user is not a disclosure |
| T-11-07-04 | Integrity | /challenge hallucinating evidence | mitigate | Prompt includes "do NOT fabricate evidence" guard and "If one side is empty, state so plainly" — tested by `test_challenge_md_has_anti_fabrication_guard` |
</threat_model>

<verification>
Plan-level check (only runs if gate=proceed):
- `grep -q '^GATE: proceed' .planning/phases/11-narrative-mode-slash-commands/11-07-SUMMARY.md`
- `pytest tests/test_commands.py -q` exits 0 (all 16+ tests now pass — 10 from plan 11-04 + 6 stretch)
- `ls graphify/commands/*.md | wc -l` == 7 (5 core + 2 stretch)
- All 7 command files carry `disable-model-invocation: true`

If gate=defer:
- `grep -q '^GATE: defer' .planning/phases/11-narrative-mode-slash-commands/11-07-SUMMARY.md`
- `ls graphify/commands/*.md | wc -l` == 5 (only the core files from plan 11-04)
- SUMMARY records the deferral reason; no other files modified
</verification>

<success_criteria>
- Gate decision recorded as grep-verifiable `^GATE:` line in 11-07-SUMMARY.md (plan-checker WARNING 4 fix)
- If gate=proceed:
  - SLASH-06 surface: /ghost command file exists, routes to existing `get_annotations` MCP tool, has no-graph fallback
  - SLASH-07 surface: /challenge command file exists, routes to existing `query_graph`, renders supporting vs contradicting evidence in two distinct sections with anti-fabrication guard
  - No new graphify/ modules added (D-18 honored)
  - No new MCP tools added (thin-wrapper promise honored)
- If gate=defer: plan is recorded as deferred in SUMMARY without changing any source files
</success_criteria>

<output>
After completion, create `.planning/phases/11-narrative-mode-slash-commands/11-07-SUMMARY.md`. The FIRST LINE MUST be either `GATE: proceed` or `GATE: defer` (plan-checker WARNING 4 fix — grep-verifiable from downstream tasks and wave-level gates).

If gate=defer: SUMMARY records the deferral decision, cites D-17, and notes that the two `.md` files were not authored in this phase. ROADMAP should be updated to move SLASH-06 and SLASH-07 to v1.4.

If gate=proceed: SUMMARY records both files authored + tests added + any learnings.
</output>
