---
phase: 73-dedup
plan: 02
type: execute
wave: 2
depends_on: [73-01]
files_modified:
  - .planning/phases/73-dedup/73-SPIKE.md
  - .planning/research/questions.md
autonomous: false
requirements: [DEDUP-01]
must_haves:
  truths:
    - "graphify has been run against three real external corpora (1 code, 1 doc-heavy, 1 PDF) producing per-corpus graph.json"
    - "scripts/dedup_spike.py output captured for all three corpora"
    - "73-SPIKE.md exists with all six required sections (Summary, Corpus, Method, Results, Recommendation, Appendix)"
    - "73-SPIKE.md reports per-corpus AND aggregate raw/residual rates"
    - "Recommendation (Ship | Defer) is tied to the locked rule: raw>5% AND residual>5%"
    - "Q-2026-05-07-01 status flipped to Resolved with link to 73-SPIKE.md"
  artifacts:
    - path: ".planning/phases/73-dedup/73-SPIKE.md"
      provides: "Canonical spike artifact with measurement + recommendation"
      contains: "## Summary"
      min_lines: 80
    - path: ".planning/research/questions.md"
      provides: "Updated question status"
      contains: "73-SPIKE.md"
  key_links:
    - from: ".planning/phases/73-dedup/73-SPIKE.md"
      to: "scripts/dedup_spike.py output"
      via: "embedded markdown table from Plan 01 script"
      pattern: "Raw %.*Residual %"
    - from: ".planning/research/questions.md"
      to: ".planning/phases/73-dedup/73-SPIKE.md"
      via: "wiki-style cross-reference"
      pattern: "73-SPIKE\\.md"
---

<objective>
Execute the spike: run graphify against three real external corpora, run the spike script from Plan 01, author 73-SPIKE.md, and flip Q-2026-05-07-01 status. Implements DEDUP-01.

Purpose: Resolve Q-2026-05-07-01 with a deterministic, reproducible Ship/Defer recommendation grounded in real measurement.

Output: `.planning/phases/73-dedup/73-SPIKE.md` (canonical artifact) and updated `.planning/research/questions.md`. NO graphify production code changes.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@./CLAUDE.md
@.planning/STATE.md
@.planning/phases/73-dedup/73-CONTEXT.md
@.planning/phases/73-dedup/73-RESEARCH.md
@scripts/dedup_spike.py
@.planning/research/questions.md

<interfaces>
**Verified corpus paths (per critical_context_overrides item 2):**
- Code: `~/Documents/silogia-repos/engineering-tools/claude-code-templates`
- Doc-heavy: `~/Documents/silogia-repos/engineering-tools/claude-cookbooks`
- PDF/paper: `~/Documents/silogia-repos/engineering-tools/claude-cookbooks/misc/data/`

**Decision rule (CONTEXT D-03, locked):** Ship if `raw > 5% AND residual > 5%`; else Defer.

**Enrichment policy (per critical_context_overrides item 1):** Per-corpus enrichment status MUST be documented in Method section. Recommendation: enrichment ON for the smallest corpus (PDF set), OFF for larger two; report per-corpus accordingly. Empty `description` falls back to `""` so fingerprint becomes `sha256(norm(label) + "|")` — this MUST be flagged in Method.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Run graphify against three corpora and capture per-corpus graph.json paths</name>
  <files>(no repo files modified — produces graph.json inside each corpus's own graphify-out/)</files>
  <read_first>
    - .planning/phases/73-dedup/73-CONTEXT.md (corpus shape constraint)
    - .planning/phases/73-dedup/73-RESEARCH.md (Pitfall 4: graphify run cost; Pitfall 5: graphify-out location)
    - graphify/__main__.py (lines 1622-1781 for CLI surface)
  </read_first>
  <action>
For each corpus, `cd` into it and run graphify. Per Pitfall 5, this writes `graphify-out/` inside each corpus, avoiding collision with this repo's own `graphify-out/`.

Recommended invocations (downsample if any run exceeds ~30 min):

```bash
# Ensure graphify is installed
pip install -e ".[all]"

# Corpus 1 — code repo (no enrichment; large)
cd ~/Documents/silogia-repos/engineering-tools/claude-code-templates
graphify run .

# Corpus 2 — doc-heavy (no enrichment by default; scope to a subdir if too large)
cd ~/Documents/silogia-repos/engineering-tools/claude-cookbooks
graphify run .   # if too slow, scope: graphify run multimodal/

# Corpus 3 — PDF/paper set (enable description enrichment if available; smallest corpus)
cd ~/Documents/silogia-repos/engineering-tools/claude-cookbooks/misc/data
graphify run .
```

If a `graphify run` flag for the description-enrichment pass is non-obvious, run `graphify run --help` first and record the canonical flag in the SPIKE.md Method section. If no such flag is exposed, document Option B (degenerate empty-description fallback per CONTEXT D-02) and proceed.

Capture the absolute path of each produced `graph.json`. Expected locations:
- `~/Documents/silogia-repos/engineering-tools/claude-code-templates/graphify-out/graph.json`
- `~/Documents/silogia-repos/engineering-tools/claude-cookbooks/graphify-out/graph.json` (or scoped subdir)
- `~/Documents/silogia-repos/engineering-tools/claude-cookbooks/misc/data/graphify-out/graph.json`

If any corpus run fails or exceeds the timebox, downsample (e.g. `graphify run subdir/`) and document the scope in SPIKE.md Corpus section.

DO NOT modify any graphify source files. This is read-only execution of the existing CLI.
  </action>
  <verify>
    <automated>test -f ~/Documents/silogia-repos/engineering-tools/claude-code-templates/graphify-out/graph.json && test -f ~/Documents/silogia-repos/engineering-tools/claude-cookbooks/graphify-out/graph.json && test -f ~/Documents/silogia-repos/engineering-tools/claude-cookbooks/misc/data/graphify-out/graph.json</automated>
  </verify>
  <acceptance_criteria>
    - Three `graph.json` files exist at the per-corpus paths above (or substitute paths recorded for SPIKE.md)
    - Each is valid JSON parseable by `python3 -c "import json,sys; json.load(open(sys.argv[1]))"`
    - File counts and byte sizes per corpus recorded for SPIKE.md Corpus section
  </acceptance_criteria>
  <done>Three graph.json files exist, paths captured for next task.</done>
</task>

<task type="auto">
  <name>Task 2: Run dedup_spike.py over all three graphs and capture stdout</name>
  <files>.planning/phases/73-dedup/73-SPIKE-stats.tmp.md (transient capture; overwritten/deleted)</files>
  <read_first>
    - scripts/dedup_spike.py
  </read_first>
  <action>
From the graphify repo root, run the spike script with named graphs:

```bash
cd ~/Documents/silogia-repos/engineering-tools/graphify
python3 scripts/dedup_spike.py \
  "code=$HOME/Documents/silogia-repos/engineering-tools/claude-code-templates/graphify-out/graph.json" \
  "doc=$HOME/Documents/silogia-repos/engineering-tools/claude-cookbooks/graphify-out/graph.json" \
  "paper=$HOME/Documents/silogia-repos/engineering-tools/claude-cookbooks/misc/data/graphify-out/graph.json" \
  > .planning/phases/73-dedup/73-SPIKE-stats.tmp.md
```

If aggregate residual is in the borderline range 4-6%, also run with `--min-score 0.6` for sensitivity (per Pitfall 3) and append that table to the tmp file:

```bash
python3 scripts/dedup_spike.py --min-score 0.6 \
  "code=..." "doc=..." "paper=..." >> .planning/phases/73-dedup/73-SPIKE-stats.tmp.md
```

The tmp file is consumed by Task 4 and removed at the end.
  </action>
  <verify>
    <automated>test -s .planning/phases/73-dedup/73-SPIKE-stats.tmp.md && grep -q "Raw %" .planning/phases/73-dedup/73-SPIKE-stats.tmp.md</automated>
  </verify>
  <acceptance_criteria>
    - Tmp file contains a markdown table with the AGGREGATE row
    - Aggregate raw_rate and residual_rate values readable
    - At least one collision-sample row present in the appendix table
  </acceptance_criteria>
  <done>Stats markdown captured for inclusion in 73-SPIKE.md.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Human spot-check of first 20 collision samples</name>
  <what-built>scripts/dedup_spike.py emitted a 20-row collision sample in the tmp stats file. Per CONTEXT D-04, an Appendix sample exists for human spot-check even though classification is automated.</what-built>
  <how-to-verify>
1. Open `.planning/phases/73-dedup/73-SPIKE-stats.tmp.md`
2. Scan the "Appendix: Collision Sample" table
3. Confirm the collisions look plausibly like real near-duplicates (e.g. "Transformer" / "transformer", "Multi-Head Attention" / "multi head attention") rather than spurious matches caused by extremely short or empty labels
4. Note any obviously degenerate groups (e.g. all-empty labels) — these will be called out in Method as a known limitation
  </how-to-verify>
  <resume-signal>Type "approved" or describe issues for adjustment.</resume-signal>
</task>

<task type="auto">
  <name>Task 4: Author .planning/phases/73-dedup/73-SPIKE.md</name>
  <files>.planning/phases/73-dedup/73-SPIKE.md</files>
  <read_first>
    - .planning/phases/73-dedup/73-SPIKE-stats.tmp.md (the captured stats from Task 2)
    - .planning/phases/73-dedup/73-CONTEXT.md (artifact section requirements, D-04)
    - .planning/phases/73-dedup/73-RESEARCH.md (Method context: 3-level dedup, schema reality)
  </read_first>
  <action>
Create `.planning/phases/73-dedup/73-SPIKE.md` with all six CONTEXT-required sections. Use the structure below verbatim, filling concrete numbers from the tmp stats file:

```markdown
# Phase 73 — DEDUP Spike

**Date:** 2026-05-08
**Phase:** 73 (DEDUP)
**Requirement:** DEDUP-01
**Resolves:** Q-2026-05-07-01

## Summary

Aggregate raw collision rate: **<X.XX>%**. Residual rate (after sem-sim coverage subtracted): **<Y.YY>%**.
Decision rule: ship iff raw>5% AND residual>5%. **Recommendation: <Ship | Defer>.**
<One-paragraph narrative tying the two numbers to the rule and noting any per-corpus heterogeneity.>

## Corpus

| Corpus | Path | File Count | Bytes | graphify Invocation |
|---|---|---:|---:|---|
| code | ~/Documents/silogia-repos/engineering-tools/claude-code-templates | <N> | <B> | `cd ... && graphify run .` |
| doc | ~/Documents/silogia-repos/engineering-tools/claude-cookbooks<scope> | <N> | <B> | `cd ... && graphify run <scope>` |
| paper | ~/Documents/silogia-repos/engineering-tools/claude-cookbooks/misc/data | <N> | <B> | `cd ... && graphify run .` |

**Enrichment status per corpus:** code=OFF, doc=OFF, paper=<ON|OFF — record actual>. Where enrichment is OFF, fingerprint degenerates to `sha256(norm(label) + "|")` per CONTEXT D-02.

## Method

### Normalization recipe (CONTEXT D-02, locked)
\`\`\`
norm(s)     = collapse_ws( strip_punct( s.lower() ) )    where strip_punct = regex [^\w\s] -> ""
fingerprint = sha256( norm(label) + "|" + norm(description[:200]) )
\`\`\`

### Schema reality
`description` is NOT in `REQUIRED_NODE_FIELDS` (graphify/validate.py). Per-node description sourced via `node.get("enriched_description") or node.get("description") or ""`. When enrichment has not run on a corpus, fingerprints reduce to label-only — explicitly flagged here as a methodological constraint.

### Concept-node universe
`file_type ∈ {document, paper, image, rationale}`. `code` excluded by default (AST-deduped at extraction time via `_make_id()` in graphify/extract.py:57-66; not the failure mode this spike targets).

### graphify's existing 3-level dedup (per build.py module header)
<Briefly summarize the three levels documented in graphify/build.py — the spike measures residual after these.>

### Sem-sim cross-check
For each fingerprint-collision group, the group is "covered" iff every node has at least one INFERRED `semantically_similar_to` edge linking it to another node in the group. `min_score` threshold: **0.0** (any sem-sim edge counts; per Pitfall 3, biases toward Defer). <Sensitivity at min_score=0.6 included if borderline.>

### Decision rule (CONTEXT D-03, locked)
Ship iff `raw_rate > 0.05` AND `residual_rate > 0.05`. Defer otherwise.

## Results

<Paste the markdown table from 73-SPIKE-stats.tmp.md here, including per-corpus rows AND the AGGREGATE row.>

**Per-corpus observation (per critical override #3):** Code-only corpora produce ~0 `semantically_similar_to` edges (LLM-extractor only emits these on docs/papers/images). Aggregate rate would mislead in isolation; the doc and paper corpora are the relevant signals for the decision.

## Recommendation

**<Ship | Defer>** — <rationale citing the specific raw and residual numbers vs the 5% threshold>.

If Ship: DEDUP-02..N (implementation in `_make_id()` / `build.py`) is unblocked. Out of scope for this phase.
If Defer: DEDUP-02..N stays in backlog. Re-examine if future corpora or the addition of fingerprint-friendly node types changes the residual.

## Appendix: Collision Sample

<Paste the appendix table from 73-SPIKE-stats.tmp.md (first 20 collision groups across corpora).>
```

Replace placeholders with actual numbers from the tmp file. After SPIKE.md is written and committed, delete the tmp file:

```bash
rm .planning/phases/73-dedup/73-SPIKE-stats.tmp.md
```
  </action>
  <verify>
    <automated>test -f .planning/phases/73-dedup/73-SPIKE.md && grep -c '^## ' .planning/phases/73-dedup/73-SPIKE.md | awk '$1 >= 6 {exit 0} {exit 1}' && grep -E 'Recommendation:.*\*\*(Ship|Defer)\*\*' .planning/phases/73-dedup/73-SPIKE.md</automated>
  </verify>
  <acceptance_criteria>
    - File exists at `.planning/phases/73-dedup/73-SPIKE.md`
    - Contains all six required sections: Summary, Corpus, Method, Results, Recommendation, Appendix
    - Recommendation is explicitly "Ship" or "Defer"
    - Per-corpus AND aggregate rates present
    - Tmp stats file deleted
  </acceptance_criteria>
  <done>Canonical spike artifact in place with measurement + recommendation tied to locked decision rule.</done>
</task>

<task type="auto">
  <name>Task 5: Flip Q-2026-05-07-01 status in research/questions.md</name>
  <files>.planning/research/questions.md</files>
  <read_first>
    - .planning/research/questions.md (locate Q-2026-05-07-01 entry; capture exact current status line)
  </read_first>
  <action>
Open `.planning/research/questions.md`, locate the Q-2026-05-07-01 entry, and update its status line to:

```
Status: Resolved — see .planning/phases/73-dedup/73-SPIKE.md (Phase 73 DEDUP spike, 2026-05-08)
```

Use Edit to replace the prior status line in-place. Do not duplicate measurement data into questions.md — the SPIKE.md is canonical.
  </action>
  <verify>
    <automated>grep -c "73-SPIKE.md" .planning/research/questions.md | awk '$1 >= 1 {exit 0} {exit 1}'</automated>
  </verify>
  <acceptance_criteria>
    - questions.md references `.planning/phases/73-dedup/73-SPIKE.md`
    - Q-2026-05-07-01 status reads "Resolved"
    - No measurement numbers duplicated from SPIKE.md into questions.md
  </acceptance_criteria>
  <done>Q-2026-05-07-01 marked Resolved with cross-link to canonical artifact.</done>
</task>

</tasks>

<verification>
- Three graph.json files produced
- 73-SPIKE.md has all six sections, per-corpus + aggregate stats, explicit Ship/Defer recommendation
- Q-2026-05-07-01 marked Resolved
- No graphify production code changed (`git diff graphify/` should be empty)
</verification>

<success_criteria>
- DEDUP-01 satisfied: spike artifact exists, raw + residual reported per-corpus + aggregate, recommendation tied to locked rule
- Q-2026-05-07-01 resolved
- Zero changes to `graphify/` source tree
</success_criteria>

<output>
After completion, create `.planning/phases/73-dedup/73-02-SUMMARY.md`
</output>
