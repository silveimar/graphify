---
phase: 29-doctor-diagnostics-dry-run-preview
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - graphify/detect.py
  - tests/test_detect.py
autonomous: true
requirements: [VAULT-15]
must_haves:
  truths:
    - "detect() returns an additive 'skipped' key: dict[str, list[str]] with all five reason keys present (nesting, exclude-glob, manifest, sensitive, noise-dir)."
    - "All existing detect() return keys remain unchanged — ABI is preserved (additive change only)."
    - "skipped['sensitive'] mirrors the existing skipped_sensitive list (zero-cost backcompat per RESEARCH §Open Questions #3)."
    - "Each pruning site in detect() body now appends to the matching skipped[reason] list."
    - "skipped[reason] lists are bounded at 10000 entries (T-29-05 OOM mitigation) — additional matches are counted but not stored."
  artifacts:
    - path: "graphify/detect.py"
      provides: "detect() with additive 'skipped' return key"
      contains: '"skipped":'
    - path: "tests/test_detect.py"
      provides: "test_detect_skip_reasons + test_detect_return_shape_backcompat"
      contains: "def test_detect_skip_reasons"
  key_links:
    - from: "graphify/detect.py"
      to: "graphify/doctor.py"
      via: "doctor.py dry-run reads result['skipped'] (Plan 29-03)"
      pattern: '"skipped"'
---

<objective>
Extend `graphify/detect.py:detect()` with an additive `skipped: dict[str, list[str]]` return key so the Plan 29-03 dry-run preview has a single source of truth for skip decisions (D-39). RESEARCH §C.1 Approach A: additive return-shape extension. ABI is preserved — every existing key remains.

Purpose: Enable `graphify doctor --dry-run` (Plan 29-03) to show why each file was pruned, grouped by reason, without reimplementing the scanner.

Output: Modified `graphify/detect.py` with single new return key. New `tests/test_detect.py::test_detect_skip_reasons` + `test_detect_return_shape_backcompat`. Zero changes to existing behavior or callers.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@./CLAUDE.md
@.planning/REQUIREMENTS.md
@.planning/phases/29-doctor-diagnostics-dry-run-preview/29-CONTEXT.md
@.planning/phases/29-doctor-diagnostics-dry-run-preview/29-RESEARCH.md
@.planning/phases/29-doctor-diagnostics-dry-run-preview/29-PATTERNS.md
@.planning/phases/29-doctor-diagnostics-dry-run-preview/29-VALIDATION.md
@.planning/phases/28-self-ingestion-hardening/28-CONTEXT.md
@graphify/detect.py
@tests/test_detect.py
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add skipped: dict[str, list[str]] return key to detect() with bounded accumulation</name>
  <files>graphify/detect.py</files>
  <read_first>
    - graphify/detect.py (lines 440–610 — full detect() body; understand all `continue` pruning sites)
    - .planning/phases/29-doctor-diagnostics-dry-run-preview/29-PATTERNS.md ("graphify/detect.py (MODIFY)" section — lists the 5 pruning sites to instrument)
    - .planning/phases/29-doctor-diagnostics-dry-run-preview/29-CONTEXT.md (D-39 — "single source of truth for skip decisions")
    - .planning/phases/28-self-ingestion-hardening/28-CONTEXT.md (D-15, D-16, D-18, D-25 — exclude-glob, nesting, manifest semantics this preserves)
  </read_first>
  <behavior>
    - At the top of detect() (near the existing `skipped_sensitive: list[str] = []` and `nested_paths: list[str] = []` accumulators ~lines 471/488), add a new dict accumulator with EXACTLY these keys (literal labels per D-38 + D-37):
        ```python
        _SKIP_CAP = 10000  # T-29-05 OOM mitigation
        skipped: dict[str, list[str]] = {
            "nesting": [],
            "exclude-glob": [],
            "manifest": [],
            "sensitive": [],
            "noise-dir": [],
        }
        skipped_overflow: dict[str, int] = {k: 0 for k in skipped}

        def _record_skip(reason: str, rel_path: str) -> None:
            if len(skipped[reason]) < _SKIP_CAP:
                skipped[reason].append(rel_path)
            else:
                skipped_overflow[reason] += 1
        ```
    - At each of the 5 pruning sites identified in PATTERNS.md, add a `_record_skip(reason, str(path_or_rel))` call alongside the existing `continue`:
        - Noise/hidden dir pruning (~line 509–518): `_record_skip("noise-dir", relpath)` for hidden/`__pycache__`/known noise directories.
        - `_is_nested_output` pruning (~line 519): `_record_skip("nesting", relpath)` (this one ALSO continues to populate the existing `nested_paths` list — do not remove that, the existing stderr WARNING line at ~482 depends on it).
        - File-level `_is_ignored` (~line 559) for both .graphifyignore and output.exclude paths: `_record_skip("exclude-glob", relpath)`.
        - File-level `prior_files` manifest skip (~line 562): `_record_skip("manifest", relpath)`.
        - `_is_sensitive` accumulator (~line 565): existing `skipped_sensitive.append(...)` stays; ADD `_record_skip("sensitive", relpath)` immediately after (the existing list is preserved; `skipped["sensitive"]` is the new mirror).
    - At the end of detect(), the return dict gains exactly ONE new key — `"skipped": skipped` — without removing or renaming any existing key. (Backcompat: `skipped_sensitive`, `nested_paths`-derived warning, `graphifyignore_patterns`, etc. are untouched.)
    - Optional but encouraged: include `"skipped_overflow": skipped_overflow` if doing so is trivial; otherwise drop it (Plan 29-03 doesn't strictly require overflow counts — it can compute `would_skip_counts[reason] = len(skipped[reason])` directly).
  </behavior>
  <action>
    Open graphify/detect.py. Locate the detect() function (around line 442). At the position where `skipped_sensitive: list[str] = []` is declared (line ~471), insert the new `_SKIP_CAP`, `skipped`, `skipped_overflow`, and nested `_record_skip` definitions exactly as shown in <behavior>.

    Then walk through detect()'s body and at each of the 5 pruning sites listed in PATTERNS.md, add the matching `_record_skip(reason, ...)` call. Preserve every existing line — this is purely additive instrumentation. Do NOT remove the existing `skipped_sensitive.append(...)` call or the `nested_paths` accumulator or the existing stderr WARNING block at line ~482.

    For the relative path argument, prefer the same string the existing site already uses (e.g., `relpath`, `str(rel)`, or `str(p.relative_to(root))`) — keep it consistent with whatever the existing accumulator uses for `skipped_sensitive` so doctor.py can display them uniformly.

    At the return statement (~line 595), add `"skipped": skipped,` as the LAST key (just before the closing brace) so the diff is minimal and review-friendly.

    Do NOT modify any other module. Do NOT change validate.py, doctor.py, output.py, or __main__.py. Do NOT add new stderr lines (the existing nested-output WARNING is sufficient — doctor.py will surface skip detail through the dataclass).
  </action>
  <verify>
    <automated>python -c "from graphify.detect import detect; import tempfile, pathlib; t = pathlib.Path(tempfile.mkdtemp()); (t/'.git').mkdir(); (t/'main.py').write_text('x=1'); r = detect(t); assert 'skipped' in r; assert set(r['skipped'].keys()) >= {'nesting','exclude-glob','manifest','sensitive','noise-dir'}; print('OK')"</automated>
  </verify>
  <done>
    - `grep -c '"skipped":' graphify/detect.py` returns >= 2 (dict literal + return).
    - `grep -c '"nesting"' graphify/detect.py` returns >= 1.
    - `grep -c '"exclude-glob"' graphify/detect.py` returns >= 1.
    - `grep -c '"noise-dir"' graphify/detect.py` returns >= 1.
    - `grep -c "_record_skip(" graphify/detect.py` returns >= 5 (one per pruning site).
    - `grep -c "skipped_sensitive" graphify/detect.py` returns >= original count (no removal — verify count is unchanged or higher).
    - Existing test suite passes: `pytest tests/test_detect.py -q` exits 0.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Add test_detect_skip_reasons + test_detect_return_shape_backcompat to tests/test_detect.py</name>
  <files>tests/test_detect.py</files>
  <read_first>
    - tests/test_detect.py (lines 1–80 for existing fixture/assertion patterns)
    - graphify/detect.py (the modified version from Task 1)
    - .planning/phases/29-doctor-diagnostics-dry-run-preview/29-PATTERNS.md ("tests/test_detect.py (MODIFY)" section — exact test stubs)
    - .planning/phases/29-doctor-diagnostics-dry-run-preview/29-VALIDATION.md (rows for "29-02 detect skip-reasons")
  </read_first>
  <behavior>
    - test_detect_skip_reasons: creates tmp_path with `.git/`, a noise dir (e.g., `node_modules/x.js`), and a normal file. Calls `detect(tmp_path)`. Asserts:
        - `"skipped" in result`
        - `isinstance(result["skipped"], dict)`
        - `set(result["skipped"].keys()) >= {"nesting", "exclude-glob", "manifest", "sensitive", "noise-dir"}`
        - `any("node_modules" in p for p in result["skipped"]["noise-dir"])`
    - test_detect_return_shape_backcompat: creates tmp_path with `.git/` and `main.py`. Calls `detect(tmp_path)`. Asserts EVERY existing key is still present:
        - `for key in ("files", "total_files", "total_words", "needs_graph", "warning", "skipped_sensitive", "graphifyignore_patterns"): assert key in result`
  </behavior>
  <action>
    Append both tests verbatim from PATTERNS.md "tests/test_detect.py (MODIFY)" section to tests/test_detect.py. Match the existing import style at the top of the file (likely `from graphify.detect import detect`). Use `tmp_path` as fixture argument for both tests. Do not modify any existing test in the file.
  </action>
  <verify>
    <automated>pytest tests/test_detect.py::test_detect_skip_reasons tests/test_detect.py::test_detect_return_shape_backcompat -q</automated>
  </verify>
  <done>
    - `grep -c "def test_detect_skip_reasons" tests/test_detect.py` returns 1.
    - `grep -c "def test_detect_return_shape_backcompat" tests/test_detect.py` returns 1.
    - `pytest tests/test_detect.py -q` exits 0 (no regressions in existing tests).
    - Full suite green: `pytest tests/ -q` exits 0.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| filesystem → detect() | detect() walks user CWD; pruning sites already validate paths via `_is_nested_output`, `_is_ignored`, `_is_sensitive` |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-29-05 | Denial of Service | skipped[reason] list growth | mitigate | Cap each list at 10000 entries via `_SKIP_CAP`; overflow counts go to skipped_overflow dict so memory stays bounded on pathological 10k+ file vaults |
| T-29-04 | Tampering | New return key surface | accept | Additive change only; no new path validation needed; existing pruning sites already validate before recording |
</threat_model>

<verification>
- `pytest tests/test_detect.py -q` exits 0.
- `pytest tests/ -q` exits 0 (no regression in 1657 baseline — additive change must not break any existing test).
- `python -c "from graphify.detect import detect; ..."` smoke test from <verify> passes.
</verification>

<success_criteria>
- detect() returns an additive `skipped` key with exactly the 5 reason buckets present (nesting, exclude-glob, manifest, sensitive, noise-dir).
- Every pre-existing detect() return key still present (ABI preserved per RESEARCH §"Open Questions" #3).
- 2 new tests in tests/test_detect.py, both green.
- Zero changes to other modules.
</success_criteria>

<output>
After completion, create `.planning/phases/29-doctor-diagnostics-dry-run-preview/29-02-SUMMARY.md`.
</output>
