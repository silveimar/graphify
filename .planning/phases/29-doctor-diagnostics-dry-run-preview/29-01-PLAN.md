---
phase: 29-doctor-diagnostics-dry-run-preview
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - graphify/doctor.py
  - tests/test_doctor.py
autonomous: true
requirements: [VAULT-14]
must_haves:
  truths:
    - "run_doctor() returns a DoctorReport carrying vault_detection, profile_validation_errors, resolved_output, ignore_list (grouped by 4 sources), manifest_history, would_self_ingest, recommended_fixes."
    - "format_report() emits sections in fixed order: Vault Detection, Profile Validation, Output Destination, Ignore-List, Recommended Fixes — every line prefixed with [graphify]."
    - "When zero issues exist, the Recommended Fixes section prints '[graphify] No issues detected.' (predictable shape)."
    - "_FIX_HINTS maps validator error substrings to actionable verb-first fix lines (D-40)."
    - "would_self_ingest is True when resolved destination falls under input scan; False when resolved.source == 'default' (D-12 backcompat)."
    - "run_doctor() performs no disk writes — read-only by design."
  artifacts:
    - path: "graphify/doctor.py"
      provides: "DoctorReport dataclass, PreviewSection dataclass, run_doctor(), format_report(), _FIX_HINTS table, _compute_would_self_ingest()"
      min_lines: 180
      contains: "def run_doctor"
    - path: "tests/test_doctor.py"
      provides: "12 unit tests covering non-dry-run DoctorReport behavior"
      contains: "from graphify.doctor import"
  key_links:
    - from: "graphify/doctor.py"
      to: "graphify/output.py"
      via: "from graphify.output import ResolvedOutput, is_obsidian_vault, resolve_output"
      pattern: "from graphify.output import"
    - from: "graphify/doctor.py"
      to: "graphify/profile.py"
      via: "validate_profile() returns list[str]"
      pattern: "validate_profile"
    - from: "graphify/doctor.py"
      to: "graphify/detect.py"
      via: "_SELF_OUTPUT_DIRS, _is_nested_output, _load_graphifyignore, _load_output_manifest"
      pattern: "from graphify.detect import"
---

<objective>
Create `graphify/doctor.py` (the new pipeline-stage module per D-32) implementing `run_doctor()` non-dry-run path, `format_report()`, `DoctorReport`/`PreviewSection` containers, `_FIX_HINTS` table, and `_compute_would_self_ingest()` helper. Also create `tests/test_doctor.py` with 12 unit tests covering the VAULT-14 behaviors per VALIDATION.md.

Purpose: Surface the resolved vault/profile/output state — already computed by Phases 27 and 28 — as a structured DoctorReport that __main__.py wiring (Plan 29-03) and the dry-run preview branch (Plan 29-03) consume.

Output: New `graphify/doctor.py` module + new `tests/test_doctor.py` with 12 passing unit tests. No __main__.py wiring yet (Plan 29-03), no dry-run branch yet (Plan 29-03).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@./CLAUDE.md
@.planning/REQUIREMENTS.md
@.planning/ROADMAP.md
@.planning/phases/29-doctor-diagnostics-dry-run-preview/29-CONTEXT.md
@.planning/phases/29-doctor-diagnostics-dry-run-preview/29-RESEARCH.md
@.planning/phases/29-doctor-diagnostics-dry-run-preview/29-PATTERNS.md
@.planning/phases/29-doctor-diagnostics-dry-run-preview/29-VALIDATION.md
@.planning/phases/27-vault-detection-profile-driven-output-routing/27-CONTEXT.md
@.planning/phases/28-self-ingestion-hardening/28-CONTEXT.md
@graphify/output.py
@graphify/profile.py
@graphify/detect.py
@tests/test_output.py

<interfaces>
<!-- Contracts the executor needs from existing modules. Use these directly — no exploration needed. -->

From graphify/output.py:
```python
class ResolvedOutput(NamedTuple):
    vault_detected: bool
    vault_path: Path | None
    notes_dir: Path
    artifacts_dir: Path
    source: Literal["profile", "cli-flag", "default"]
    exclude_globs: tuple[str, ...] = ()

def is_obsidian_vault(cwd: Path) -> bool: ...
def resolve_output(cwd: Path, *, cli_output: str | None = None) -> ResolvedOutput: ...
# resolve_output() may raise SystemExit(1) via _refuse() on missing-profile / malformed mode / sibling escape.
```

From graphify/profile.py:
```python
def load_profile(profile_path: Path) -> dict | None: ...
def validate_profile(profile: dict) -> list[str]:
    """Returns list of error strings; empty list = valid. ABI is locked (D-36)."""
```

From graphify/detect.py:
```python
_SELF_OUTPUT_DIRS: set[str] = {"graphify-out", "graphify_out"}
def _is_nested_output(rel_path: str, self_dirs: set[str]) -> bool: ...
def _load_graphifyignore(root: Path) -> list[str]: ...  # walks up to filesystem root looking for .graphifyignore
def _load_output_manifest(artifacts_dir: Path) -> dict:
    """Returns {'history': [...]} envelope; warn-and-empty on read failure (Phase 28 D-25)."""
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Create graphify/doctor.py with DoctorReport, _FIX_HINTS, run_doctor() (non-dry-run), format_report()</name>
  <files>graphify/doctor.py</files>
  <read_first>
    - graphify/output.py (full file — 172 lines; primary stylistic precedent per PATTERNS.md)
    - graphify/profile.py (lines 200–250 around validate_profile signature)
    - graphify/detect.py (lines 250–400 for _SELF_OUTPUT_DIRS, _is_nested_output, _load_graphifyignore, _load_output_manifest)
    - graphify/report.py (lines 70–90 for the lines-list renderer pattern)
    - .planning/phases/29-doctor-diagnostics-dry-run-preview/29-CONTEXT.md (D-30..D-41 — locked decisions)
    - .planning/phases/29-doctor-diagnostics-dry-run-preview/29-PATTERNS.md ("graphify/doctor.py (NEW)" section)
    - .planning/phases/27-vault-detection-profile-driven-output-routing/27-CONTEXT.md (ResolvedOutput contract)
    - .planning/phases/28-self-ingestion-hardening/28-CONTEXT.md (manifest schema, nesting guard)
  </read_first>
  <behavior>
    - DoctorReport dataclass exists with fields: vault_detection (bool), vault_path (Path | None), profile_validation_errors (list[str]), resolved_output (ResolvedOutput | None), ignore_list (dict[str, list[str]] grouped by D-37 source labels), manifest_history (list[dict] | None), would_self_ingest (bool), recommended_fixes (list[str]), preview (PreviewSection | None).
    - PreviewSection dataclass exists with fields: would_ingest_count (int), would_ingest_sample (list[str]), would_skip_grouped (dict[str, list[str]]), would_skip_counts (dict[str, int]), notes_dir (Path), artifacts_dir (Path). (Even though the dry-run branch is implemented in Plan 29-03, the dataclass is defined here so its import is stable.)
    - DoctorReport.is_misconfigured() -> bool returns True when profile_validation_errors is non-empty OR resolved_output is None OR would_self_ingest is True (D-35).
    - run_doctor(cwd: Path, *, dry_run: bool = False) -> DoctorReport: when dry_run=False, returns fully-populated DoctorReport with preview=None. (dry_run=True branch is a stub raising NotImplementedError — Plan 29-03 implements.)
    - format_report(report: DoctorReport) -> str returns sectioned text in the fixed order per D-34. Every emitted line is prefixed with "[graphify] ". When report has zero issues, the Recommended Fixes section emits exactly one line: "[graphify] No issues detected."
    - _FIX_HINTS is a list of (substring_or_regex_pattern: str, fix_line: str) tuples — order-preserving, first-match-wins. Required entries (D-40 examples + planner-drafted set covering profile.py:validate_profile and output.py:_refuse failure modes):
        - ("missing .graphify/profile.yaml",                   "[graphify] FIX: Create .graphify/profile.yaml — see docs/vault-adapter.md")
        - ("PyYAML",                                            "[graphify] FIX: Install PyYAML — pip install 'graphifyy[routing]'")
        - ("sibling-of-vault",                                  "[graphify] FIX: Set output.path to a directory inside (or beside) your vault, not above it")
        - ("output.mode",                                       "[graphify] FIX: Set output.mode to 'vault-relative', 'absolute', or 'sibling-of-vault' in .graphify/profile.yaml")
        - ("output.path",                                       "[graphify] FIX: Add an output.path: <relative-or-absolute> entry under output: in .graphify/profile.yaml")
        - ("WOULD_SELF_INGEST",                                 "[graphify] FIX: Move existing graphify-out/ outside the input scan, or add 'graphify-out/**' to .graphifyignore")
    - _compute_would_self_ingest(cwd: Path, resolved: ResolvedOutput | None) -> bool: returns False when resolved is None or resolved.source == "default" (D-12 backcompat); otherwise checks whether the resolved.notes_dir or resolved.artifacts_dir basenames fall under the cwd input-scan area using _is_nested_output() and the union (_SELF_OUTPUT_DIRS ∪ {resolved.notes_dir.name, resolved.artifacts_dir.name}).
    - Ignore-list (D-37) is built as dict with EXACTLY these keys and source labels:
        - "self-output-dirs": sorted(list(_SELF_OUTPUT_DIRS))  # ["graphify-out", "graphify_out"]
        - "resolved-basenames": [resolved.notes_dir.name, resolved.artifacts_dir.name] when distinct from self-output-dirs (deduped per-key only — NOT across sources, per D-37)
        - "graphifyignore-patterns": _load_graphifyignore(cwd)
        - "profile-exclude-globs": list(resolved.exclude_globs) if resolved else []
    - run_doctor() catches SystemExit from resolve_output() and stores resolved=None, then accumulates the "missing .graphify/profile.yaml" or matching error string into profile_validation_errors so _FIX_HINTS picks it up. (Pattern from PATTERNS.md "Catch-SystemExit" section.)
    - manifest_history: when resolved is not None, call _load_output_manifest(resolved.artifacts_dir) and store result["history"] (or None if resolved is None).
    - recommended_fixes is built by scanning profile_validation_errors against _FIX_HINTS (substring match) AND appending the WOULD_SELF_INGEST hint when would_self_ingest is True. One fix line per detected issue, in detection order (D-41).
  </behavior>
  <action>
    Create graphify/doctor.py implementing the behavior above. Module docstring per the PATTERNS.md "Module header pattern" section: list D-30..D-41 implemented decisions. Use `from __future__ import annotations` as first import. Stdlib only (`dataclasses`, `pathlib`, `re`, `sys`, `typing`). Then `from graphify.output import ResolvedOutput, is_obsidian_vault, resolve_output`, `from graphify.profile import load_profile, validate_profile`, `from graphify.detect import _SELF_OUTPUT_DIRS, _is_nested_output, _load_graphifyignore, _load_output_manifest`.

    Use `@dataclass` (not NamedTuple) for DoctorReport and PreviewSection because they carry mutable list/dict fields with `field(default_factory=...)`. Use keyword-only arguments on `run_doctor` per D-32 (`def run_doctor(cwd: Path, *, dry_run: bool = False) -> DoctorReport:`). Type hints on every parameter and return.

    For dry_run=True: `if dry_run: raise NotImplementedError("doctor --dry-run preview is implemented in Plan 29-03")`. Plan 29-03 will replace this branch.

    For SystemExit handling around resolve_output(): per the PATTERNS.md "Catch-SystemExit" section, also capture stderr emitted by _refuse() into the profile_validation_errors list. The simplest path: wrap the call with `contextlib.redirect_stderr(io.StringIO())` and parse the captured "[graphify] " lines back into the error list (split lines, strip the prefix, keep non-empty). This keeps the human stderr surface unchanged when invoked through __main__.py (Plan 29-03 will not redirect).

    For format_report(): build `lines: list[str]`, append section headers as `"[graphify] === Vault Detection ==="` etc., then bullet lines per D-34. Section order per D-34: Vault Detection, Profile Validation, Output Destination, Ignore-List, (Preview if report.preview is not None), Recommended Fixes. Every line starts with "[graphify] ". Return "\n".join(lines).

    Do not import or wire __main__.py — that's Plan 29-03.
  </action>
  <verify>
    <automated>python -c "from graphify.doctor import run_doctor, format_report, DoctorReport, PreviewSection, _FIX_HINTS; assert callable(run_doctor); assert callable(format_report); assert len(_FIX_HINTS) >= 6; print('OK')"</automated>
  </verify>
  <done>
    - File graphify/doctor.py exists.
    - `grep -c "def run_doctor" graphify/doctor.py` returns 1.
    - `grep -c "def format_report" graphify/doctor.py` returns 1.
    - `grep -c "class DoctorReport" graphify/doctor.py` returns 1.
    - `grep -c "class PreviewSection" graphify/doctor.py` returns 1.
    - `grep -c "_FIX_HINTS" graphify/doctor.py` returns >= 2 (definition + use).
    - `grep -c "from __future__ import annotations" graphify/doctor.py` returns 1.
    - `grep -c "from graphify.output import" graphify/doctor.py` returns 1.
    - `grep -c "from graphify.detect import" graphify/doctor.py` returns 1.
    - `python -c "from graphify.doctor import run_doctor, format_report, DoctorReport, PreviewSection, _FIX_HINTS"` exits 0.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Create tests/test_doctor.py with 12 unit tests for non-dry-run DoctorReport behaviors</name>
  <files>tests/test_doctor.py</files>
  <read_first>
    - graphify/doctor.py (the module created in Task 1)
    - tests/test_output.py (full file — primary test stylistic precedent per PATTERNS.md)
    - tests/test_main_flags.py (lines 50–80 for vault fixture pattern)
    - .planning/phases/29-doctor-diagnostics-dry-run-preview/29-VALIDATION.md (12 unit-test rows under "29-01 doctor.py module")
    - .planning/phases/29-doctor-diagnostics-dry-run-preview/29-PATTERNS.md ("tests/test_doctor.py (NEW)" section)
  </read_first>
  <behavior>
    - 12 named tests, one per VALIDATION.md row under "29-01 doctor.py module":
        1. test_run_doctor_vault_detected — vault with .obsidian/ → report.vault_detection is True, vault_path == tmp_path/vault.
        2. test_run_doctor_invalid_profile — vault with malformed output: in profile.yaml → report.profile_validation_errors is non-empty, report.is_misconfigured() is True.
        3. test_run_doctor_resolved_output — vault with valid profile → report.resolved_output is a ResolvedOutput with notes_dir set per profile.
        4. test_run_doctor_ignore_list_sources — report.ignore_list has all 4 source keys: "self-output-dirs", "resolved-basenames", "graphifyignore-patterns", "profile-exclude-globs" (D-37).
        5. test_run_doctor_manifest_history — fixture writes a synthetic output-manifest.json with one history entry → report.manifest_history is a list with that entry.
        6. test_run_doctor_self_ingest_detected — fixture sets resolved.notes_dir to live INSIDE the input scan (e.g., output.path: graphify-notes nested) → report.would_self_ingest is True.
        7. test_run_doctor_default_paths_not_self_ingest — non-vault directory → resolved.source == "default" → report.would_self_ingest is False (D-12 backcompat).
        8. test_format_report_section_order — assert format_report(report) contains substrings "Vault Detection", "Profile Validation", "Output Destination", "Ignore-List", "Recommended Fixes" in that order (use .index() comparisons).
        9. test_format_report_graphify_prefix — every non-empty line in format_report() output starts with "[graphify] ".
        10. test_fix_hints_coverage — for each known error pattern in _FIX_HINTS, simulate a profile_validation_errors entry containing that substring → assert format_report() emits the corresponding fix line.
        11. test_format_report_no_issues — clean valid vault → format_report() contains the literal "[graphify] No issues detected.".
        12. test_run_doctor_no_disk_writes — snapshot tmp_path file inventory before/after run_doctor() → assert no new files created.
    - All tests use tmp_path. Tests that require profile.yaml call `pytest.importorskip("yaml")` first (RESEARCH §Pitfall 2).
    - Vault fixture pattern (per PATTERNS.md "Vault fixture pattern"):
        ```python
        vault = tmp_path / "vault"
        vault.mkdir()
        (vault / ".obsidian").mkdir()
        (vault / ".git").mkdir()  # halts _load_graphifyignore walk-up per RESEARCH §Pitfall 6
        (vault / ".graphify").mkdir()
        (vault / ".graphify" / "profile.yaml").write_text("output:\n  mode: vault-relative\n  path: Atlas/Generated\n")
        ```
  </behavior>
  <action>
    Create tests/test_doctor.py mirroring tests/test_output.py header style. Module docstring: `"""Unit tests for graphify/doctor.py — Phase 29 diagnostics + dry-run."""`. Use `from __future__ import annotations` as first import.

    Imports: `from pathlib import Path`, `import pytest`, `from graphify.doctor import run_doctor, format_report, DoctorReport, PreviewSection, _FIX_HINTS`.

    Define a module-level helper `_make_vault(tmp_path: Path, *, profile_text: str | None = None) -> Path` that creates the .obsidian/.git/.graphify scaffolding and writes profile.yaml when profile_text is provided. Use it in every test that needs a vault to avoid copy-paste.

    Implement all 12 tests above. For test_fix_hints_coverage, iterate `for pattern, fix_line in _FIX_HINTS:` and synthesize a DoctorReport whose `profile_validation_errors=[pattern]` (or set `would_self_ingest=True` for the WOULD_SELF_INGEST entry) → call `format_report(report)` → assert `fix_line in formatted`.

    For test_run_doctor_self_ingest_detected, set `output.path: graphify-out/notes` (nested under the input scan) so `_is_nested_output` returns True. The test must NOT actually create a graphify-out/ tree — only the profile entry — so the test stays a pure unit test (no graphify-out/ side-effect).

    Do not test the dry-run branch (Plan 29-03 owns those tests). If any test happens to call `run_doctor(..., dry_run=True)` it should be wrapped in `with pytest.raises(NotImplementedError):` to document the stub.
  </action>
  <verify>
    <automated>pytest tests/test_doctor.py -q</automated>
  </verify>
  <done>
    - File tests/test_doctor.py exists.
    - `pytest tests/test_doctor.py -q` exits 0.
    - `grep -c "^def test_" tests/test_doctor.py` returns >= 12.
    - All 12 named tests from VALIDATION.md row "29-01 doctor.py module" appear in the file (`grep -c "def test_run_doctor_vault_detected\|def test_run_doctor_invalid_profile\|def test_run_doctor_resolved_output\|def test_run_doctor_ignore_list_sources\|def test_run_doctor_manifest_history\|def test_run_doctor_self_ingest_detected\|def test_run_doctor_default_paths_not_self_ingest\|def test_format_report_section_order\|def test_format_report_graphify_prefix\|def test_fix_hints_coverage\|def test_format_report_no_issues\|def test_run_doctor_no_disk_writes" tests/test_doctor.py` returns 12).
    - Full suite still green: `pytest tests/ -q` exits 0 (regression check).
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| filesystem → doctor.py | doctor.py reads CWD, profile.yaml, output-manifest.json, .graphifyignore — all dev-time inputs the user controls |
| output-manifest.json content → doctor.py | JSON content untrusted (could be hand-edited or corrupted) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-29-01 | Tampering | doctor.py path reads | mitigate | Reuse `output.py:resolve_output()` (already validates `output.path`, refuses sibling-of-vault escapes per Phase 27 D-03) — doctor.py adds NO new path validation |
| T-29-02 | Tampering | doctor.py reads output-manifest.json | mitigate | Reuse `detect._load_output_manifest()` which warns-and-returns-empty on malformed JSON per Phase 28 D-25 |
| T-29-03 | Information Disclosure | format_report prints sample paths to stdout | accept | Dev-time CLI tool; user already has filesystem read access; no PII risk. Path strings are not passed to HTML — `security.sanitize_label()` not required for plain text output |
</threat_model>

<verification>
- `pytest tests/test_doctor.py -q` exits 0 (12 tests pass).
- `pytest tests/ -q` exits 0 (no regression in 1657 baseline).
- `python -c "from graphify.doctor import run_doctor, format_report, DoctorReport, PreviewSection, _FIX_HINTS"` exits 0.
- `grep -c "^def test_" tests/test_doctor.py` >= 12.
</verification>

<success_criteria>
- graphify/doctor.py exists with DoctorReport, PreviewSection, run_doctor (non-dry-run path), format_report, _FIX_HINTS, _compute_would_self_ingest.
- 12 unit tests in tests/test_doctor.py, all green.
- No __main__.py changes (Plan 29-03 owns wiring).
- No detect.py changes (Plan 29-02 owns the skip-reasons additive change).
- Dry-run branch is a NotImplementedError stub (Plan 29-03 replaces).
</success_criteria>

<output>
After completion, create `.planning/phases/29-doctor-diagnostics-dry-run-preview/29-01-SUMMARY.md`.
</output>
